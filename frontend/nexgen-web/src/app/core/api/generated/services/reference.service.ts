// AUTO-GENERATED FROM api/openapi.json - DO NOT EDIT BY HAND.
// Regenerate with: bash tools/generate-api-client.sh
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';

import { BaseService } from '../base-service';
import { ApiConfiguration } from '../api-configuration';
import { StrictHttpResponse } from '../strict-http-response';

import { CurrencyDto } from '../models/currency-dto';
import { getGstRates } from '../fn/reference/get-gst-rates';
import { GetGstRates$Params } from '../fn/reference/get-gst-rates';
import { getGstRates$Plain } from '../fn/reference/get-gst-rates-plain';
import { GetGstRates$Plain$Params } from '../fn/reference/get-gst-rates-plain';
import { getReferenceCurrencies } from '../fn/reference/get-reference-currencies';
import { GetReferenceCurrencies$Params } from '../fn/reference/get-reference-currencies';
import { getReferenceCurrencies$Plain } from '../fn/reference/get-reference-currencies-plain';
import { GetReferenceCurrencies$Plain$Params } from '../fn/reference/get-reference-currencies-plain';
import { getScreens } from '../fn/reference/get-screens';
import { GetScreens$Params } from '../fn/reference/get-screens';
import { getScreens$Plain } from '../fn/reference/get-screens-plain';
import { GetScreens$Plain$Params } from '../fn/reference/get-screens-plain';
import { getStates } from '../fn/reference/get-states';
import { GetStates$Params } from '../fn/reference/get-states';
import { getStates$Plain } from '../fn/reference/get-states-plain';
import { GetStates$Plain$Params } from '../fn/reference/get-states-plain';
import { getTerms } from '../fn/reference/get-terms';
import { GetTerms$Params } from '../fn/reference/get-terms';
import { getTerms$Plain } from '../fn/reference/get-terms-plain';
import { GetTerms$Plain$Params } from '../fn/reference/get-terms-plain';
import { getUoms } from '../fn/reference/get-uoms';
import { GetUoms$Params } from '../fn/reference/get-uoms';
import { getUoms$Plain } from '../fn/reference/get-uoms-plain';
import { GetUoms$Plain$Params } from '../fn/reference/get-uoms-plain';
import { GstRatesResponse } from '../models/gst-rates-response';
import { ScreenDto } from '../models/screen-dto';
import { StateDto } from '../models/state-dto';
import { TermsDto } from '../models/terms-dto';
import { UomDto } from '../models/uom-dto';


/**
 * M2-B09 — the small, slow-changing lookup lists every screen needs before it can render a
 * dropdown, behind one cached route group. Closes KB-041 item <b>B6</b> and the
 * reference-data half of <b>C1</b>.
 *             
 * 
 * <b>Every action is a single `ICommonService` call plus a projection.</b> Noquery is written against `ApplicationDbContext` here and no business logic liveshere (ADR-002 §2). The one action that is not a service call — `gst-rates` — readsthe domain's own constants rather than restating them.
 * <b>Why `[NoScreenRight]` and not `[RequireScreen]`.</b> Reference data iswhat a screen needs <i>in order to render at all</i>; there is no single screen that owns"the list of states". Gating it on a screen right would deadlock the UI for exactly thereason KB-105 §2.4 gives for `GET /api/v1/me` — a user must be able to fetch thevocabulary before the app can decide what to show them. The endpoints still requireauthentication and are still tenant-scoped; this is an explicit, greppable opt-out fromthe screen-right axis, not from authorization.
 */
@Injectable({ providedIn: 'root' })
export class ReferenceService extends BaseService {
  constructor(config: ApiConfiguration, http: HttpClient) {
    super(config, http);
  }

  /** Path part for operation `getGstRates()` */
  static readonly GetGstRatesPath = '/api/v1/reference/gst-rates';

  /**
   * The two GST ladders, paired by index. Read from `CommonConstants`, never retyped.
   *
   * This action is not tenant-dependent — the ladders are compile-time constants — but it
   * shares the group's tenant-keyed policy anyway. Carving out a per-endpoint exemption
   * would buy a marginal cache-entry saving in exchange for two policies to reason about,
   * and the failure mode of getting that wrong is a cross-tenant leak. Uniform is safer.
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getGstRates$Plain()` instead.
   *
   * This method doesn't expect any request body.
   */
  getGstRates$Plain$Response(params?: GetGstRates$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<GstRatesResponse>> {
    const obs = getGstRates$Plain(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * The two GST ladders, paired by index. Read from `CommonConstants`, never retyped.
   *
   * This action is not tenant-dependent — the ladders are compile-time constants — but it
   * shares the group's tenant-keyed policy anyway. Carving out a per-endpoint exemption
   * would buy a marginal cache-entry saving in exchange for two policies to reason about,
   * and the failure mode of getting that wrong is a cross-tenant leak. Uniform is safer.
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getGstRates$Plain$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getGstRates$Plain(params?: GetGstRates$Plain$Params, context?: HttpContext): Observable<GstRatesResponse> {
    const resp = this.getGstRates$Plain$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<GstRatesResponse>): GstRatesResponse => r.body)
    );
  }

  /**
   * The two GST ladders, paired by index. Read from `CommonConstants`, never retyped.
   *
   * This action is not tenant-dependent — the ladders are compile-time constants — but it
   * shares the group's tenant-keyed policy anyway. Carving out a per-endpoint exemption
   * would buy a marginal cache-entry saving in exchange for two policies to reason about,
   * and the failure mode of getting that wrong is a cross-tenant leak. Uniform is safer.
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getGstRates()` instead.
   *
   * This method doesn't expect any request body.
   */
  getGstRates$Response(params?: GetGstRates$Params, context?: HttpContext): Observable<StrictHttpResponse<GstRatesResponse>> {
    const obs = getGstRates(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * The two GST ladders, paired by index. Read from `CommonConstants`, never retyped.
   *
   * This action is not tenant-dependent — the ladders are compile-time constants — but it
   * shares the group's tenant-keyed policy anyway. Carving out a per-endpoint exemption
   * would buy a marginal cache-entry saving in exchange for two policies to reason about,
   * and the failure mode of getting that wrong is a cross-tenant leak. Uniform is safer.
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getGstRates$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getGstRates(params?: GetGstRates$Params, context?: HttpContext): Observable<GstRatesResponse> {
    const resp = this.getGstRates$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<GstRatesResponse>): GstRatesResponse => r.body)
    );
  }

  /** Path part for operation `getUoms()` */
  static readonly GetUomsPath = '/api/v1/reference/uoms';

  /**
   * Units of measure. `UnitCode` is the key; this table has no integer id.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getUoms$Plain()` instead.
   *
   * This method doesn't expect any request body.
   */
  getUoms$Plain$Response(params?: GetUoms$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<UomDto>>> {
    const obs = getUoms$Plain(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Units of measure. `UnitCode` is the key; this table has no integer id.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getUoms$Plain$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getUoms$Plain(params?: GetUoms$Plain$Params, context?: HttpContext): Observable<Array<UomDto>> {
    const resp = this.getUoms$Plain$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<Array<UomDto>>): Array<UomDto> => r.body)
    );
  }

  /**
   * Units of measure. `UnitCode` is the key; this table has no integer id.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getUoms()` instead.
   *
   * This method doesn't expect any request body.
   */
  getUoms$Response(params?: GetUoms$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<UomDto>>> {
    const obs = getUoms(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Units of measure. `UnitCode` is the key; this table has no integer id.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getUoms$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getUoms(params?: GetUoms$Params, context?: HttpContext): Observable<Array<UomDto>> {
    const resp = this.getUoms$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<Array<UomDto>>): Array<UomDto> => r.body)
    );
  }

  /** Path part for operation `getStates()` */
  static readonly GetStatesPath = '/api/v1/reference/states';

  /**
   * States.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getStates$Plain()` instead.
   *
   * This method doesn't expect any request body.
   */
  getStates$Plain$Response(params?: GetStates$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<StateDto>>> {
    const obs = getStates$Plain(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * States.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getStates$Plain$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getStates$Plain(params?: GetStates$Plain$Params, context?: HttpContext): Observable<Array<StateDto>> {
    const resp = this.getStates$Plain$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<Array<StateDto>>): Array<StateDto> => r.body)
    );
  }

  /**
   * States.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getStates()` instead.
   *
   * This method doesn't expect any request body.
   */
  getStates$Response(params?: GetStates$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<StateDto>>> {
    const obs = getStates(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * States.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getStates$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getStates(params?: GetStates$Params, context?: HttpContext): Observable<Array<StateDto>> {
    const resp = this.getStates$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<Array<StateDto>>): Array<StateDto> => r.body)
    );
  }

  /** Path part for operation `getTerms()` */
  static readonly GetTermsPath = '/api/v1/reference/terms';

  /**
   * Active terms and conditions. The active filter is the domain's — this action calls
   * `GetAllActiveTermsAsync` and does not re-filter, so the API cannot disagree with
   * the Blazor screens about what "active" means.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getTerms$Plain()` instead.
   *
   * This method doesn't expect any request body.
   */
  getTerms$Plain$Response(params?: GetTerms$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TermsDto>>> {
    const obs = getTerms$Plain(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Active terms and conditions. The active filter is the domain's — this action calls
   * `GetAllActiveTermsAsync` and does not re-filter, so the API cannot disagree with
   * the Blazor screens about what "active" means.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getTerms$Plain$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getTerms$Plain(params?: GetTerms$Plain$Params, context?: HttpContext): Observable<Array<TermsDto>> {
    const resp = this.getTerms$Plain$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<Array<TermsDto>>): Array<TermsDto> => r.body)
    );
  }

  /**
   * Active terms and conditions. The active filter is the domain's — this action calls
   * `GetAllActiveTermsAsync` and does not re-filter, so the API cannot disagree with
   * the Blazor screens about what "active" means.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getTerms()` instead.
   *
   * This method doesn't expect any request body.
   */
  getTerms$Response(params?: GetTerms$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TermsDto>>> {
    const obs = getTerms(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Active terms and conditions. The active filter is the domain's — this action calls
   * `GetAllActiveTermsAsync` and does not re-filter, so the API cannot disagree with
   * the Blazor screens about what "active" means.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getTerms$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getTerms(params?: GetTerms$Params, context?: HttpContext): Observable<Array<TermsDto>> {
    const resp = this.getTerms$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<Array<TermsDto>>): Array<TermsDto> => r.body)
    );
  }

  /** Path part for operation `getScreens()` */
  static readonly GetScreensPath = '/api/v1/reference/screens';

  /**
   * The screen catalogue — the permission <b>vocabulary</b>, not the caller's rights.
   *
   * <b>The protection decision, recorded here because it is a security decision.</b>            This returns every screen name in the tenant, which is a map of the application's            surface. It is <i>not</i> sensitive per-user data: the same list is identical for            every user in the tenant, and it discloses nothing about who may do what. The            caller's own rights are a different endpoint (`GET /api/v1/me`, M2-A07) and are            deliberately not merged into this one — merging them would make this response            caller-dependent and force the cache key to include the user, multiplying the entry            count by the user base for no benefit.
   * 
   * It therefore requires authentication, is tenant-keyed, and is cached like the            rest of the group. It projects to V.SMART.Api.Contracts.ScreenDto specifically to drop the            `UserRights` navigation — returning the entity would put the tenant's entire            permission matrix on the wire behind a dropdown feed.
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getScreens$Plain()` instead.
   *
   * This method doesn't expect any request body.
   */
  getScreens$Plain$Response(params?: GetScreens$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ScreenDto>>> {
    const obs = getScreens$Plain(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * The screen catalogue — the permission <b>vocabulary</b>, not the caller's rights.
   *
   * <b>The protection decision, recorded here because it is a security decision.</b>            This returns every screen name in the tenant, which is a map of the application's            surface. It is <i>not</i> sensitive per-user data: the same list is identical for            every user in the tenant, and it discloses nothing about who may do what. The            caller's own rights are a different endpoint (`GET /api/v1/me`, M2-A07) and are            deliberately not merged into this one — merging them would make this response            caller-dependent and force the cache key to include the user, multiplying the entry            count by the user base for no benefit.
   * 
   * It therefore requires authentication, is tenant-keyed, and is cached like the            rest of the group. It projects to V.SMART.Api.Contracts.ScreenDto specifically to drop the            `UserRights` navigation — returning the entity would put the tenant's entire            permission matrix on the wire behind a dropdown feed.
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getScreens$Plain$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getScreens$Plain(params?: GetScreens$Plain$Params, context?: HttpContext): Observable<Array<ScreenDto>> {
    const resp = this.getScreens$Plain$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<Array<ScreenDto>>): Array<ScreenDto> => r.body)
    );
  }

  /**
   * The screen catalogue — the permission <b>vocabulary</b>, not the caller's rights.
   *
   * <b>The protection decision, recorded here because it is a security decision.</b>            This returns every screen name in the tenant, which is a map of the application's            surface. It is <i>not</i> sensitive per-user data: the same list is identical for            every user in the tenant, and it discloses nothing about who may do what. The            caller's own rights are a different endpoint (`GET /api/v1/me`, M2-A07) and are            deliberately not merged into this one — merging them would make this response            caller-dependent and force the cache key to include the user, multiplying the entry            count by the user base for no benefit.
   * 
   * It therefore requires authentication, is tenant-keyed, and is cached like the            rest of the group. It projects to V.SMART.Api.Contracts.ScreenDto specifically to drop the            `UserRights` navigation — returning the entity would put the tenant's entire            permission matrix on the wire behind a dropdown feed.
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getScreens()` instead.
   *
   * This method doesn't expect any request body.
   */
  getScreens$Response(params?: GetScreens$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ScreenDto>>> {
    const obs = getScreens(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * The screen catalogue — the permission <b>vocabulary</b>, not the caller's rights.
   *
   * <b>The protection decision, recorded here because it is a security decision.</b>            This returns every screen name in the tenant, which is a map of the application's            surface. It is <i>not</i> sensitive per-user data: the same list is identical for            every user in the tenant, and it discloses nothing about who may do what. The            caller's own rights are a different endpoint (`GET /api/v1/me`, M2-A07) and are            deliberately not merged into this one — merging them would make this response            caller-dependent and force the cache key to include the user, multiplying the entry            count by the user base for no benefit.
   * 
   * It therefore requires authentication, is tenant-keyed, and is cached like the            rest of the group. It projects to V.SMART.Api.Contracts.ScreenDto specifically to drop the            `UserRights` navigation — returning the entity would put the tenant's entire            permission matrix on the wire behind a dropdown feed.
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getScreens$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getScreens(params?: GetScreens$Params, context?: HttpContext): Observable<Array<ScreenDto>> {
    const resp = this.getScreens$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<Array<ScreenDto>>): Array<ScreenDto> => r.body)
    );
  }

  /** Path part for operation `getReferenceCurrencies()` */
  static readonly GetReferenceCurrenciesPath = '/api/v1/reference/currencies';

  /**
   * Currencies, as reference data for dropdowns.
   *
   * <b>Distinct from `CurrencyController`, deliberately.</b> That controller is the
   *             CRUD surface for the Currency master — paged, sorted, filtered, writable
   *             (`/api/v1/currencies`). This is a flat, cached, read-only list for populating a
   *             selector (`/api/v1/reference/currencies`). Different lifetime, different shape,
   *             different cacheability; the route group and these summaries are what keep them
   *             distinguishable in Swagger. It excludes the `CurrencyRates` navigation — the
   *             daily rate feed changes on a different clock and is what makes the entity unsafe to
   *             cache.
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getReferenceCurrencies$Plain()` instead.
   *
   * This method doesn't expect any request body.
   */
  getReferenceCurrencies$Plain$Response(params?: GetReferenceCurrencies$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<CurrencyDto>>> {
    const obs = getReferenceCurrencies$Plain(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Currencies, as reference data for dropdowns.
   *
   * <b>Distinct from `CurrencyController`, deliberately.</b> That controller is the
   *             CRUD surface for the Currency master — paged, sorted, filtered, writable
   *             (`/api/v1/currencies`). This is a flat, cached, read-only list for populating a
   *             selector (`/api/v1/reference/currencies`). Different lifetime, different shape,
   *             different cacheability; the route group and these summaries are what keep them
   *             distinguishable in Swagger. It excludes the `CurrencyRates` navigation — the
   *             daily rate feed changes on a different clock and is what makes the entity unsafe to
   *             cache.
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getReferenceCurrencies$Plain$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getReferenceCurrencies$Plain(params?: GetReferenceCurrencies$Plain$Params, context?: HttpContext): Observable<Array<CurrencyDto>> {
    const resp = this.getReferenceCurrencies$Plain$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<Array<CurrencyDto>>): Array<CurrencyDto> => r.body)
    );
  }

  /**
   * Currencies, as reference data for dropdowns.
   *
   * <b>Distinct from `CurrencyController`, deliberately.</b> That controller is the
   *             CRUD surface for the Currency master — paged, sorted, filtered, writable
   *             (`/api/v1/currencies`). This is a flat, cached, read-only list for populating a
   *             selector (`/api/v1/reference/currencies`). Different lifetime, different shape,
   *             different cacheability; the route group and these summaries are what keep them
   *             distinguishable in Swagger. It excludes the `CurrencyRates` navigation — the
   *             daily rate feed changes on a different clock and is what makes the entity unsafe to
   *             cache.
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getReferenceCurrencies()` instead.
   *
   * This method doesn't expect any request body.
   */
  getReferenceCurrencies$Response(params?: GetReferenceCurrencies$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<CurrencyDto>>> {
    const obs = getReferenceCurrencies(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Currencies, as reference data for dropdowns.
   *
   * <b>Distinct from `CurrencyController`, deliberately.</b> That controller is the
   *             CRUD surface for the Currency master — paged, sorted, filtered, writable
   *             (`/api/v1/currencies`). This is a flat, cached, read-only list for populating a
   *             selector (`/api/v1/reference/currencies`). Different lifetime, different shape,
   *             different cacheability; the route group and these summaries are what keep them
   *             distinguishable in Swagger. It excludes the `CurrencyRates` navigation — the
   *             daily rate feed changes on a different clock and is what makes the entity unsafe to
   *             cache.
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getReferenceCurrencies$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getReferenceCurrencies(params?: GetReferenceCurrencies$Params, context?: HttpContext): Observable<Array<CurrencyDto>> {
    const resp = this.getReferenceCurrencies$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<Array<CurrencyDto>>): Array<CurrencyDto> => r.body)
    );
  }

}
