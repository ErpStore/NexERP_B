// AUTO-GENERATED FROM api/openapi.json - DO NOT EDIT BY HAND.
// Regenerate with: bash tools/generate-api-client.sh
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';

import { BaseService } from '../base-service';
import { ApiConfiguration } from '../api-configuration';
import { StrictHttpResponse } from '../strict-http-response';

import { createCurrency } from '../fn/currency/create-currency';
import { CreateCurrency$Params } from '../fn/currency/create-currency';
import { createCurrency$Plain } from '../fn/currency/create-currency-plain';
import { CreateCurrency$Plain$Params } from '../fn/currency/create-currency-plain';
import { CurrencyVM } from '../models/currency-vm';
import { CurrencyVMPagedResult } from '../models/currency-vm-paged-result';
import { deleteCurrency } from '../fn/currency/delete-currency';
import { DeleteCurrency$Params } from '../fn/currency/delete-currency';
import { exportCurrencies } from '../fn/currency/export-currencies';
import { ExportCurrencies$Params } from '../fn/currency/export-currencies';
import { getCurrencies } from '../fn/currency/get-currencies';
import { GetCurrencies$Params } from '../fn/currency/get-currencies';
import { getCurrencies$Plain } from '../fn/currency/get-currencies-plain';
import { GetCurrencies$Plain$Params } from '../fn/currency/get-currencies-plain';
import { getCurrencyById } from '../fn/currency/get-currency-by-id';
import { GetCurrencyById$Params } from '../fn/currency/get-currency-by-id';
import { getCurrencyById$Plain } from '../fn/currency/get-currency-by-id-plain';
import { GetCurrencyById$Plain$Params } from '../fn/currency/get-currency-by-id-plain';
import { getCurrencyImportTemplate } from '../fn/currency/get-currency-import-template';
import { GetCurrencyImportTemplate$Params } from '../fn/currency/get-currency-import-template';
import { importCurrencies } from '../fn/currency/import-currencies';
import { ImportCurrencies$Params } from '../fn/currency/import-currencies';
import { importCurrencies$Plain } from '../fn/currency/import-currencies-plain';
import { ImportCurrencies$Plain$Params } from '../fn/currency/import-currencies-plain';
import { ImportResult } from '../models/import-result';
import { updateCurrency } from '../fn/currency/update-currency';
import { UpdateCurrency$Params } from '../fn/currency/update-currency';
import { updateCurrency$Plain } from '../fn/currency/update-currency-plain';
import { UpdateCurrency$Plain$Params } from '../fn/currency/update-currency-plain';

@Injectable({ providedIn: 'root' })
export class CurrencyService extends BaseService {
  constructor(config: ApiConfiguration, http: HttpClient) {
    super(config, http);
  }

  /** Path part for operation `getCurrencies()` */
  static readonly GetCurrenciesPath = '/api/v1/currencies';

  /**
   * M2-B02 — the reference implementation of the paged list contract (ADR-002 §2 and its
   * M2-B02 addendum). Every future list endpoint copies this shape: one
   * `[FromQuery]` typed query record in, one V.SMART.Api.Contracts.PagedResult`1 out, the
   * service's filter dictionary produced by V.SMART.Api.Contracts.FilterDictionaryAdapter and
   * never seen on the wire.
   *             
   * 
   * The controller-local paged-response record it replaces is deleted: 60–80copies of it would become 60–80 interfaces in the generated TypeScript client. TheJSON is unchanged — the four property names were already ADR-002's.
   * <b>Behaviour change to note:</b> the default `pageSize` is now thecontract-wide 20, where this endpoint alone previously defaulted to 10. Callers thatsend `pageSize` explicitly are unaffected.
   * Validation is declarative on V.SMART.Api.Contracts.CurrencyQuery, so an invalid`pageNumber`/`pageSize`/`sort`/date range is rejected by M2-A06's`InvalidModelStateResponseFactory` before this method runs. The explicit`ModelState` guard below is the belt-and-braces path for a caller that invokes theaction directly (unit tests), and matches `Create`/`Update`.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getCurrencies$Plain()` instead.
   *
   * This method doesn't expect any request body.
   */
  getCurrencies$Plain$Response(params?: GetCurrencies$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<CurrencyVMPagedResult>> {
    const obs = getCurrencies$Plain(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * M2-B02 — the reference implementation of the paged list contract (ADR-002 §2 and its
   * M2-B02 addendum). Every future list endpoint copies this shape: one
   * `[FromQuery]` typed query record in, one V.SMART.Api.Contracts.PagedResult`1 out, the
   * service's filter dictionary produced by V.SMART.Api.Contracts.FilterDictionaryAdapter and
   * never seen on the wire.
   *             
   * 
   * The controller-local paged-response record it replaces is deleted: 60–80copies of it would become 60–80 interfaces in the generated TypeScript client. TheJSON is unchanged — the four property names were already ADR-002's.
   * <b>Behaviour change to note:</b> the default `pageSize` is now thecontract-wide 20, where this endpoint alone previously defaulted to 10. Callers thatsend `pageSize` explicitly are unaffected.
   * Validation is declarative on V.SMART.Api.Contracts.CurrencyQuery, so an invalid`pageNumber`/`pageSize`/`sort`/date range is rejected by M2-A06's`InvalidModelStateResponseFactory` before this method runs. The explicit`ModelState` guard below is the belt-and-braces path for a caller that invokes theaction directly (unit tests), and matches `Create`/`Update`.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getCurrencies$Plain$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getCurrencies$Plain(params?: GetCurrencies$Plain$Params, context?: HttpContext): Observable<CurrencyVMPagedResult> {
    const resp = this.getCurrencies$Plain$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<CurrencyVMPagedResult>): CurrencyVMPagedResult => r.body)
    );
  }

  /**
   * M2-B02 — the reference implementation of the paged list contract (ADR-002 §2 and its
   * M2-B02 addendum). Every future list endpoint copies this shape: one
   * `[FromQuery]` typed query record in, one V.SMART.Api.Contracts.PagedResult`1 out, the
   * service's filter dictionary produced by V.SMART.Api.Contracts.FilterDictionaryAdapter and
   * never seen on the wire.
   *             
   * 
   * The controller-local paged-response record it replaces is deleted: 60–80copies of it would become 60–80 interfaces in the generated TypeScript client. TheJSON is unchanged — the four property names were already ADR-002's.
   * <b>Behaviour change to note:</b> the default `pageSize` is now thecontract-wide 20, where this endpoint alone previously defaulted to 10. Callers thatsend `pageSize` explicitly are unaffected.
   * Validation is declarative on V.SMART.Api.Contracts.CurrencyQuery, so an invalid`pageNumber`/`pageSize`/`sort`/date range is rejected by M2-A06's`InvalidModelStateResponseFactory` before this method runs. The explicit`ModelState` guard below is the belt-and-braces path for a caller that invokes theaction directly (unit tests), and matches `Create`/`Update`.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getCurrencies()` instead.
   *
   * This method doesn't expect any request body.
   */
  getCurrencies$Response(params?: GetCurrencies$Params, context?: HttpContext): Observable<StrictHttpResponse<CurrencyVMPagedResult>> {
    const obs = getCurrencies(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * M2-B02 — the reference implementation of the paged list contract (ADR-002 §2 and its
   * M2-B02 addendum). Every future list endpoint copies this shape: one
   * `[FromQuery]` typed query record in, one V.SMART.Api.Contracts.PagedResult`1 out, the
   * service's filter dictionary produced by V.SMART.Api.Contracts.FilterDictionaryAdapter and
   * never seen on the wire.
   *             
   * 
   * The controller-local paged-response record it replaces is deleted: 60–80copies of it would become 60–80 interfaces in the generated TypeScript client. TheJSON is unchanged — the four property names were already ADR-002's.
   * <b>Behaviour change to note:</b> the default `pageSize` is now thecontract-wide 20, where this endpoint alone previously defaulted to 10. Callers thatsend `pageSize` explicitly are unaffected.
   * Validation is declarative on V.SMART.Api.Contracts.CurrencyQuery, so an invalid`pageNumber`/`pageSize`/`sort`/date range is rejected by M2-A06's`InvalidModelStateResponseFactory` before this method runs. The explicit`ModelState` guard below is the belt-and-braces path for a caller that invokes theaction directly (unit tests), and matches `Create`/`Update`.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getCurrencies$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getCurrencies(params?: GetCurrencies$Params, context?: HttpContext): Observable<CurrencyVMPagedResult> {
    const resp = this.getCurrencies$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<CurrencyVMPagedResult>): CurrencyVMPagedResult => r.body)
    );
  }

  /** Path part for operation `createCurrency()` */
  static readonly CreateCurrencyPath = '/api/v1/currencies';

  /**
   * Creates a currency. A duplicate name is the service's refusal, surfaced as 409 with the
   * service's message verbatim in `title` (ADR-002 §4); a malformed body is 400 with the
   * per-field `errors` dictionary.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `createCurrency$Plain()` instead.
   *
   * This method sends `application/*+json` and handles request body of type `application/*+json`.
   */
  createCurrency$Plain$Response(params?: CreateCurrency$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<CurrencyVM>> {
    const obs = createCurrency$Plain(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Creates a currency. A duplicate name is the service's refusal, surfaced as 409 with the
   * service's message verbatim in `title` (ADR-002 §4); a malformed body is 400 with the
   * per-field `errors` dictionary.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `createCurrency$Plain$Response()` instead.
   *
   * This method sends `application/*+json` and handles request body of type `application/*+json`.
   */
  createCurrency$Plain(params?: CreateCurrency$Plain$Params, context?: HttpContext): Observable<CurrencyVM> {
    const resp = this.createCurrency$Plain$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<CurrencyVM>): CurrencyVM => r.body)
    );
  }

  /**
   * Creates a currency. A duplicate name is the service's refusal, surfaced as 409 with the
   * service's message verbatim in `title` (ADR-002 §4); a malformed body is 400 with the
   * per-field `errors` dictionary.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `createCurrency()` instead.
   *
   * This method sends `application/*+json` and handles request body of type `application/*+json`.
   */
  createCurrency$Response(params?: CreateCurrency$Params, context?: HttpContext): Observable<StrictHttpResponse<CurrencyVM>> {
    const obs = createCurrency(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Creates a currency. A duplicate name is the service's refusal, surfaced as 409 with the
   * service's message verbatim in `title` (ADR-002 §4); a malformed body is 400 with the
   * per-field `errors` dictionary.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `createCurrency$Response()` instead.
   *
   * This method sends `application/*+json` and handles request body of type `application/*+json`.
   */
  createCurrency(params?: CreateCurrency$Params, context?: HttpContext): Observable<CurrencyVM> {
    const resp = this.createCurrency$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<CurrencyVM>): CurrencyVM => r.body)
    );
  }

  /** Path part for operation `getCurrencyById()` */
  static readonly GetCurrencyByIdPath = '/api/v1/currencies/{id}';

  /**
   * One currency by id. Answers 404 in the canonical problem shape when the row does not
   * exist.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getCurrencyById$Plain()` instead.
   *
   * This method doesn't expect any request body.
   */
  getCurrencyById$Plain$Response(params: GetCurrencyById$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<CurrencyVM>> {
    const obs = getCurrencyById$Plain(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * One currency by id. Answers 404 in the canonical problem shape when the row does not
   * exist.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getCurrencyById$Plain$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getCurrencyById$Plain(params: GetCurrencyById$Plain$Params, context?: HttpContext): Observable<CurrencyVM> {
    const resp = this.getCurrencyById$Plain$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<CurrencyVM>): CurrencyVM => r.body)
    );
  }

  /**
   * One currency by id. Answers 404 in the canonical problem shape when the row does not
   * exist.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getCurrencyById()` instead.
   *
   * This method doesn't expect any request body.
   */
  getCurrencyById$Response(params: GetCurrencyById$Params, context?: HttpContext): Observable<StrictHttpResponse<CurrencyVM>> {
    const obs = getCurrencyById(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * One currency by id. Answers 404 in the canonical problem shape when the row does not
   * exist.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getCurrencyById$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getCurrencyById(params: GetCurrencyById$Params, context?: HttpContext): Observable<CurrencyVM> {
    const resp = this.getCurrencyById$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<CurrencyVM>): CurrencyVM => r.body)
    );
  }

  /** Path part for operation `updateCurrency()` */
  static readonly UpdateCurrencyPath = '/api/v1/currencies/{id}';

  /**
   * Updates the currency identified by the route id. The route id is what the service acts
   * on; any `currId` in the body is ignored.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `updateCurrency$Plain()` instead.
   *
   * This method sends `application/*+json` and handles request body of type `application/*+json`.
   */
  updateCurrency$Plain$Response(params: UpdateCurrency$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<CurrencyVM>> {
    const obs = updateCurrency$Plain(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Updates the currency identified by the route id. The route id is what the service acts
   * on; any `currId` in the body is ignored.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `updateCurrency$Plain$Response()` instead.
   *
   * This method sends `application/*+json` and handles request body of type `application/*+json`.
   */
  updateCurrency$Plain(params: UpdateCurrency$Plain$Params, context?: HttpContext): Observable<CurrencyVM> {
    const resp = this.updateCurrency$Plain$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<CurrencyVM>): CurrencyVM => r.body)
    );
  }

  /**
   * Updates the currency identified by the route id. The route id is what the service acts
   * on; any `currId` in the body is ignored.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `updateCurrency()` instead.
   *
   * This method sends `application/*+json` and handles request body of type `application/*+json`.
   */
  updateCurrency$Response(params: UpdateCurrency$Params, context?: HttpContext): Observable<StrictHttpResponse<CurrencyVM>> {
    const obs = updateCurrency(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Updates the currency identified by the route id. The route id is what the service acts
   * on; any `currId` in the body is ignored.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `updateCurrency$Response()` instead.
   *
   * This method sends `application/*+json` and handles request body of type `application/*+json`.
   */
  updateCurrency(params: UpdateCurrency$Params, context?: HttpContext): Observable<CurrencyVM> {
    const resp = this.updateCurrency$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<CurrencyVM>): CurrencyVM => r.body)
    );
  }

  /** Path part for operation `deleteCurrency()` */
  static readonly DeleteCurrencyPath = '/api/v1/currencies/{id}';

  /**
   * Deletes a currency. A delete guard's refusal is 409 carrying the service's message
   * verbatim (BR-SO-001), not 400.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `deleteCurrency()` instead.
   *
   * This method doesn't expect any request body.
   */
  deleteCurrency$Response(params: DeleteCurrency$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
    const obs = deleteCurrency(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Deletes a currency. A delete guard's refusal is 409 carrying the service's message
   * verbatim (BR-SO-001), not 400.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `deleteCurrency$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  deleteCurrency(params: DeleteCurrency$Params, context?: HttpContext): Observable<void> {
    const resp = this.deleteCurrency$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<void>): void => r.body)
    );
  }

  /** Path part for operation `exportCurrencies()` */
  static readonly ExportCurrenciesPath = '/api/v1/currencies/export';

  /**
   * <b>GET api/v1/currencies/export?format=xlsx</b> — the filtered set as a workbook.
   *             
   *              
   * It takes the same V.SMART.Api.Contracts.CurrencyQuery the list endpoint does, so the same             query string produces the same rows; only `pageNumber`/`pageSize` are ignored,             because an export is the whole filtered set rather than a page of it.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `exportCurrencies()` instead.
   *
   * This method doesn't expect any request body.
   */
  exportCurrencies$Response(params?: ExportCurrencies$Params, context?: HttpContext): Observable<StrictHttpResponse<Blob>> {
    const obs = exportCurrencies(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * <b>GET api/v1/currencies/export?format=xlsx</b> — the filtered set as a workbook.
   *             
   *              
   * It takes the same V.SMART.Api.Contracts.CurrencyQuery the list endpoint does, so the same             query string produces the same rows; only `pageNumber`/`pageSize` are ignored,             because an export is the whole filtered set rather than a page of it.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `exportCurrencies$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  exportCurrencies(params?: ExportCurrencies$Params, context?: HttpContext): Observable<Blob> {
    const resp = this.exportCurrencies$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<Blob>): Blob => r.body)
    );
  }

  /** Path part for operation `getCurrencyImportTemplate()` */
  static readonly GetCurrencyImportTemplatePath = '/api/v1/currencies/import-template';

  /**
   * <b>GET api/v1/currencies/import-template</b> — the blank workbook a user fills in,
   *             produced by the unmodified `IExcelTemplateService.CreateTemplateAsync`.
   *
   * It does <b>not</b> call `DownloadTemplate(uploadType)`: that method's
   * `LoadHeadings` switch knows four item-oriented upload types only
   * (`ExcelTemplateService.cs:46-76`) and returns an empty header array for anything
   * else, so a "Currency" upload type would silently produce an empty template. Passing the
   * headers explicitly wraps the same service without modifying it.
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `getCurrencyImportTemplate()` instead.
   *
   * This method doesn't expect any request body.
   */
  getCurrencyImportTemplate$Response(params?: GetCurrencyImportTemplate$Params, context?: HttpContext): Observable<StrictHttpResponse<Blob>> {
    const obs = getCurrencyImportTemplate(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * <b>GET api/v1/currencies/import-template</b> — the blank workbook a user fills in,
   *             produced by the unmodified `IExcelTemplateService.CreateTemplateAsync`.
   *
   * It does <b>not</b> call `DownloadTemplate(uploadType)`: that method's
   * `LoadHeadings` switch knows four item-oriented upload types only
   * (`ExcelTemplateService.cs:46-76`) and returns an empty header array for anything
   * else, so a "Currency" upload type would silently produce an empty template. Passing the
   * headers explicitly wraps the same service without modifying it.
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `getCurrencyImportTemplate$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  getCurrencyImportTemplate(params?: GetCurrencyImportTemplate$Params, context?: HttpContext): Observable<Blob> {
    const resp = this.getCurrencyImportTemplate$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<Blob>): Blob => r.body)
    );
  }

  /** Path part for operation `importCurrencies()` */
  static readonly ImportCurrenciesPath = '/api/v1/currencies/import';

  /**
   * <b>POST api/v1/currencies/import</b> — `multipart/form-data` with `file`.
   *             
   *              
   * Every row goes through `ICurrencyService.CreateAsync`. The controller parses             the spreadsheet and does nothing else: duplicate detection, validation and persistence             stay in the business service, and its refusal message is reported verbatim             (BR-SO-001). A bad row is reported, not thrown; the remaining rows are still             processed.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `importCurrencies$Plain()` instead.
   *
   * This method sends `multipart/form-data` and handles request body of type `multipart/form-data`.
   */
  importCurrencies$Plain$Response(params?: ImportCurrencies$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<ImportResult>> {
    const obs = importCurrencies$Plain(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * <b>POST api/v1/currencies/import</b> — `multipart/form-data` with `file`.
   *             
   *              
   * Every row goes through `ICurrencyService.CreateAsync`. The controller parses             the spreadsheet and does nothing else: duplicate detection, validation and persistence             stay in the business service, and its refusal message is reported verbatim             (BR-SO-001). A bad row is reported, not thrown; the remaining rows are still             processed.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `importCurrencies$Plain$Response()` instead.
   *
   * This method sends `multipart/form-data` and handles request body of type `multipart/form-data`.
   */
  importCurrencies$Plain(params?: ImportCurrencies$Plain$Params, context?: HttpContext): Observable<ImportResult> {
    const resp = this.importCurrencies$Plain$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<ImportResult>): ImportResult => r.body)
    );
  }

  /**
   * <b>POST api/v1/currencies/import</b> — `multipart/form-data` with `file`.
   *             
   *              
   * Every row goes through `ICurrencyService.CreateAsync`. The controller parses             the spreadsheet and does nothing else: duplicate detection, validation and persistence             stay in the business service, and its refusal message is reported verbatim             (BR-SO-001). A bad row is reported, not thrown; the remaining rows are still             processed.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `importCurrencies()` instead.
   *
   * This method sends `multipart/form-data` and handles request body of type `multipart/form-data`.
   */
  importCurrencies$Response(params?: ImportCurrencies$Params, context?: HttpContext): Observable<StrictHttpResponse<ImportResult>> {
    const obs = importCurrencies(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * <b>POST api/v1/currencies/import</b> — `multipart/form-data` with `file`.
   *             
   *              
   * Every row goes through `ICurrencyService.CreateAsync`. The controller parses             the spreadsheet and does nothing else: duplicate detection, validation and persistence             stay in the business service, and its refusal message is reported verbatim             (BR-SO-001). A bad row is reported, not thrown; the remaining rows are still             processed.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `importCurrencies$Response()` instead.
   *
   * This method sends `multipart/form-data` and handles request body of type `multipart/form-data`.
   */
  importCurrencies(params?: ImportCurrencies$Params, context?: HttpContext): Observable<ImportResult> {
    const resp = this.importCurrencies$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<ImportResult>): ImportResult => r.body)
    );
  }

}
