{
  "_comment": "Deployment-supplied runtime configuration. Replace apiBaseUrl with the absolute origin of the V.SMART API when the SPA is not served from the same origin, e.g. https://erp.example.com/api. A blank or missing value makes the app fail at startup by design.",
  "apiBaseUrl": "/api"
}
