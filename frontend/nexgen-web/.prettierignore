dist
coverage
playwright-report
test-results
.angular
package-lock.json

# M2-B10 - the generated API client is a build artefact of tools/generate-api-client.sh.
# Prettier does not own it: reformatting generated output would make the CI drift check
# depend on the prettier version, and the files carry /* eslint-disable */ headers of their
# own. Regenerate it, never format it.
src/app/core/api/generated
