---
doc_id: KB-093
title: Autonomous Runner State
module: execution
source_files: []
entities: []
api_endpoints: []
database_tables: []
business_rules: []
status: active
confidence: n/a
last_verified: 2026-08-25
dependencies: [KB-089, KB-091, KB-092, KB-081]

selection_note: |
  2026-08-25 Select-only pass (this session), tip `074ad1e` on `master`, tree clean
  (`git status --porcelain` empty, `git branch --show-current` = `master`). The only new
  commit since the prior pass (`701eef2` -> `074ad1e`) is KB-081/KB-003's `M2-C10` diagnosis:
  it corrects `M2-C10`'s `Blocked` reason from a stale `environment` classification to the
  real blocker (**Q-85**, an API-contract decision on whether money crosses the wire as a
  string or a double), and raises Q-85. No code changed, no branch touched, and `M2-C10`
  stays `Blocked` — it does not become `Ready`. Re-ran the five-part test against every
  `**Ready**` row in `task-tracker.md`: only `M0-11` (line 86, `Product Decision`, fails part
  2, owner-only per KB-091 §8). `M0-06` (line 84) and `M2-C10` (line 156) both read `Blocked`,
  not `Ready`. `git branch -a --no-merged master` re-run this pass: no live sibling branch on
  `M0-11`'s files (it has none — pure decision task), consistent with prior passes.
  `current-task.md` already carried the correct "pool is empty" narrative for this exact HEAD
  and was left unchanged. **No task selected this pass — `nextTaskId` returned empty.**
  Previous note, superseded by the above:

  2026-08-25 Select-only pass (this session), tip `701eef2` on `master`, tree clean
  (`git status --porcelain` empty, `git branch --show-current` = `master`). `Status` row read
  `STOPPED` (M0-06 merge record) — no human stop request found. `current-task.md` is stale
  relative to this HEAD: it still describes the `M2-B10` close-out and does not mention the
  `M0-06` merge (`b057ceb`/`701eef2`) or a branch discovered this pass, so step 2 ("current
  task not finished, resume it") could not be trusted from that file alone and was checked
  directly against git and the tracker instead.

  Re-ran the five-part test against every `**Ready**` row in `task-tracker.md`: only two —
  `M0-04` (line 67, footnote ⁴˒⁷⁰) and `M0-11` (line 86). `M0-06` is no longer `Ready`; the
  merge (`b057ceb`) closed it `Blocked` on Q-25/Q-26 (tracker line 84), so it drops out of
  consideration entirely, not just failing a part.

  **`M0-11` still fails part 2** — `task_type: Product Decision`, owner-only, never
  self-selectable (KB-091 §8).

  **`M0-04` fails part 5, and this is a new finding this pass.** Tracker footnote ⁷⁰ reopened
  `M0-04` `Blocked → Ready` at `beee0f9` after renaming the *prior* stale branch to
  `archive/M0-04-runbook-stale-lineage`. But `git branch --no-merged master` (re-run this
  pass) shows a **second, live** `migration/M0-04-credential-rotation-runbook` branch —
  `git merge-base master migration/M0-04-credential-rotation-runbook` = `beee0f9` itself, i.e.
  it was cut from that exact "now selectable" commit, not from the earlier stale point. Its
  4 commits (`e437fe5`, `622cc9e`, `1133cfc`, `5b67161`) show a full M0-04 attempt already ran
  there: all four AI-deliverable artefacts committed (`docs/runbooks/credential-rotation.md`,
  the credential inventory, the verification checklist, Q-19 confirmed already answered), the
  task closed in its own designed terminal state `BLOCKED` (no human with production access
  participated — the task's own *Target Result* requires exactly this when that happens), and
  that branch's own subsequent select-only passes (`1133cfc`, `5b67161`) independently found
  nothing else dependency-ready either. None of this is merged into `master` — `git
  merge-base --is-ancestor e437fe5 master` fails — so `master`'s tracker/footnote ⁷⁰ and
  `current-task.md` are simply unaware of it; the two lineages diverged at `beee0f9` when
  `master` went on to merge `M0-06` instead. Selecting `M0-04` again from `master` now would
  cut a **second** branch duplicating work already done and already closed on the first one —
  exactly the "sibling branch already open on the same files" condition part 5 exists to catch,
  and exactly the runner-has-no-concurrency-control failure mode: re-running would not advance
  the task, only fork it. **`M0-04`'s real status is the unmerged branch's `BLOCKED`, awaiting
  owner review/merge, not `Ready` for a fresh attempt.**

  No other tracker row reads `Ready`. **No task selected this pass — `nextTaskId` returned
  empty.** `current-task.md` was not rewritten by this pass (selection-only instructions), but
  it is now stale on three counts a future session should reconcile: the `M0-06` merge, the
  `M0-04` branch's completed/`Blocked` outcome, and footnote ⁷⁰'s `Ready` label being
  superseded by that outcome. Previous note, superseded by the above:

  2026-08-25 Select-only pass (this session), tip `5d7c8df` on `master`, tree clean
  (`git status --porcelain` empty, `git branch --show-current` = `master`). `Status` row
  already read `STOPPED` (not `STOP_REQUESTED`) — no human stop request found.
  `current-task.md` already carried the correct "nothing selectable" narrative for this
  exact HEAD (M2-B10 closed, nothing behind it dependency-ready), so this pass re-verified
  the five-part "can actually be done" test directly against `task-tracker.md` rather than
  trusting the inherited note. Grepped the tracker for every `**Ready**` row: only two,
  unchanged from the prior pass — `M0-06` (tracker line 84, still fails part 5: sibling
  branch `migration/M0-06-remove-default-admin` still present in `git branch --no-merged
  master`, re-confirmed this pass) and `M0-11` (tracker line 86, still fails part 2:
  `task_type: Product Decision`, owner-only, never self-selectable per KB-091 §8). No other
  row reads `Ready`. **No task selected this pass — `nextTaskId` returned empty.**
  `current-task.md` left unchanged (its narrative already matches this HEAD). Previous note,
  superseded by the above:

  2026-08-25 Select-only pass (this session), tip `9f2a750` on `master`, tree clean
  (`git status --porcelain` empty, `git branch --show-current` = `master`, ahead of
  `origin/master` by 2 unpushed commits: `1d7e8cd`, `9f2a750`). `runner-state.md`'s own
  `Status` row already read `STOPPED` (not `STOP_REQUESTED`) after last session's `M2-B10`
  merge and push — no human stop request found. `current-task.md` already carried the correct
  "nothing selectable" narrative for this exact HEAD, so this pass re-verified the five-part
  "can actually be done" test directly against `task-tracker.md` rather than trusting the
  inherited note. Only two rows read `Ready`: `M0-06` (still fails part 5 — sibling branch
  `migration/M0-06-remove-default-admin` still present in `git branch --no-merged master`,
  re-run this pass) and `M0-11` (still fails part 2 — `task_type: Product Decision`,
  owner-only, never self-selectable). Checked whether `M2-B10`'s merge to `master`
  (`d4c9f60`, 2026-08-25) released anything: `M2-D01` names `M2-B10` as one of three Hard
  prerequisites (`depends_on: [M2-C05-03, M2-A02, M2-B10]`); `M2-A02` is now also `Completed`
  and merged, but `M2-C05-03` still reads `Blocked` (tracker line 166, real blocker
  `M2-C05-01`), so `M2-D01` stays `Blocked` — confirmed against `task-tracker.md` line 179
  directly, not inferred. Grepped the tracker for every `**Ready**` table row: only `M0-06`
  and `M0-11` (lines 84, 86). No other row is `Ready`. **No task selected this pass —
  `nextTaskId` returned empty.** `current-task.md` left unchanged (its narrative already
  matches this HEAD). Previous note, superseded by the above:

  2026-08-24 Session close-out (this session) for `M2-A03`, tip `21dc055` on
  `migration/M2-A03-permission-matrix-harness` (base `13ee72a`). An independently validated
  `FAIL` verdict (attempt 1 of 4, `scopeOk: true`, `failureCategory: environment`, 0
  escalations) was carried into this session's launch context; this session recorded the
  outcome — task file § Execution Record (2026-08-24) + frontmatter `status: Blocked` +
  `last_verified: 2026-08-24`, `task-tracker.md` row + footnote ⁶³ + "Current state"
  narrative, this file's `Status` row, `current-task.md` rewritten to point at `M2-A03` with
  its Run State showing the failure — without re-running the full verification suite itself
  beyond spot-checking `dotnet build`, `dotnet test` (470/470) and `gh api ... /protection`
  (`gh: command not found`), since no code was changed by this close-out
  (`git diff --stat 13ee72a..HEAD -- V.SMART/` empty). **Not fixable by re-dispatch**: the sole
  unmet criterion is GitHub branch-protection configuration outside the git tree, unreadable
  and unsettable from any execution session (no push permitted; the branch is not on `origin`).
  Same class and same disposition as the `M0-07` attempt-1 stop
  (`failure-log.md:305-379`). **`BLOCKED` — owner: Vivek** (mark the CI job required on
  `master`, accept the criterion as a standing manual gate, or move it to a successor task).
  Per this task's explicit instruction, **no other task was started** even though `M2-B03`
  (`Documentation`, `depends_on: [M2-A02, M2-B02]`, both `Completed`/merged) clears the
  five-part "can actually be done" test and would otherwise be next. `nextTaskId` returned
  empty. Previous note, superseded by the above:

  2026-08-24 Session close-out (this session) for `M2-A10`, tip `02a4633` on
  `migration/M2-A10-api-rights-seeding`. An independently validated `PASS` verdict (final of 3
  validation-attempt passes, `scopeOk: true`, `failureCategory: none`, 1 escalation across the
  run) was carried into this session's launch context; this session recorded the outcome — task
  file Execution Record (session close-out section) and frontmatter/header already read
  `status: Needs Review` from the branch's own commits, `task-tracker.md` row/footnote ⁶¹/
  "Current state"/rollup counts, this file, `current-task.md` rewritten — without re-running the
  verification commands itself (no code was changed by this close-out). `failure-log.md`'s four
  `M2-A10` attempt entries (1 FAIL, 2 diagnosis-fix, 1 FAIL, 1 diagnosis-fix) were already
  present in the working tree from prior turns and were committed to the task branch in this
  session (`02a4633`) rather than duplicated here. No new discoveries recorded beyond what the
  branch's own commits already carry: Q-28 was already `ANSWERED` before this task started and
  needed no further edit on `master`; R-73 was added to `technical-debt-register.md` by the
  implementing session, on the branch, not merged. Closed `Needs Review`, not `Completed`: this
  project requires owner integration (merge) before `Completed` regardless of a task's own
  Completion Conditions, and only the repository owner may set `Completed` (KB-088 §Who may set
  Completed). Re-ran the five-part "can actually be done" test against every `Ready` row left in
  the tracker: `M0-06` still fails part 5 (`migration/M0-06-remove-default-admin` still present,
  unmerged, `git branch --no-merged master` re-run this session); `M0-11` still fails part 2
  (`Product Decision`, owner-only). No `Ready` row remains that clears the test — **no task
  selected this pass.** `current-task.md` rewritten to record the stall and the outstanding
  owner decisions, carrying forward `M2-A10`'s safety-property note (`UserId == 1` gate),
  R-43 (no `WebApplicationFactory` host) and R-73. Previous note, superseded by the above:

  2026-08-24 Session close-out (this session) for `M2-A09`, tip `c3c595e` on
  `migration/M2-A09-screen-catalogue-phantoms`. Independently validated `PASS` (attempt 1 of 3,
  `scopeOk: true`, `failureCategory: none`, 0 escalations) was carried into this session's
  launch context; this session recorded the outcome — task file Execution Record and
  frontmatter (`status: Needs Review`), `task-tracker.md` row/footnote ⁶⁰/"Current state"/
  rollup counts, this file, `current-task.md` rewritten — without re-running the verification
  commands itself (no code was changed by this close-out). No new discoveries recorded: R-65
  was already marked resolved in `technical-debt-register.md` and
  `server-side-authorization-spec.md` §1.3 already updated, both by the implementing session in
  the same commit (`c3c595e`); nothing else in `docs/kb/` referenced the phantom names or
  needed a change. Closed `Needs Review`, not `Completed`: this project requires owner
  integration (merge) before `Completed` regardless of a task's own Completion Conditions, and
  only the repository owner may set `Completed` (KB-088 §Who may set Completed). Re-checked the
  "can actually be done" test against the tracker's remaining `Ready` rows: `M0-06` still fails
  part 5 (`migration/M0-06-remove-default-admin` still present, unmerged, `git branch
  --no-merged master` re-run this session); `M0-11` still fails part 2 (`Product Decision`,
  owner-only); `M2-A10` clears all five parts — `depends_on: [M2-A01-03]` only, `Completed` and
  merged; not a `Product Decision`; the one open question that named it (Q-28) is `ANSWERED`
  and grepping `open-questions.md` finds no other hit for `M2-A10`; its task file carries no ⛔
  banner; `git branch --no-merged master` (re-run this session) shows no branch touching any of
  its `source_files` (`AuthController.cs`, `UserRightService.cs`, `Login.razor`,
  `AuthControllerRightsSeedingTests.cs`) — confirmed against the full branch list (`M0-03-01`,
  `M0-03-02`, `M0-04`, `M0-06`, `M0-08`, `M2-A02`, `M2-A08`, `M2-A09`, `M2-B12-01`, `M2-C10`,
  none overlapping). **`M2-A10` selected**, not dispatched. Classification taken directly from
  the task file's own frontmatter (`complexity: LOW`, `risk: MEDIUM`, explicit wins over
  derivation, KB-091 §4) — per §5.1's LOW-complexity table Investigate and Implement route to
  `sonnet`; risk `MEDIUM` does not force `opus` for Validate (§5.2 item 2 forces `opus` only at
  risk `HIGH`), so Validate also routes to `sonnet`. `current-task.md` rewritten to point at
  `M2-A10`, carrying forward `M2-A09`'s now-150-name catalogue (for any test needing a real
  screen name), `M2-A02`'s still-unmerged status, and Q-71. Previous note, superseded by the
  above:

  2026-08-24 Session close-out (this session) for `M2-A02`, tip `634d30c` on
  `migration/M2-A02-currency-authorization`. Independently validated `PASS` (attempt 1 of 3,
  `scopeOk: true`, `failureCategory: none`, 0 escalations) was carried into this session's
  launch context; this session recorded the outcome — task file Execution Record,
  `task-tracker.md` row/footnote ⁵⁹/"Current state", this file, `open-questions.md` already
  carried Q-71 from the implementing session — without re-running the verification commands
  itself (no code was changed by this close-out). Closed `Needs Review`, not `Completed`: this
  project requires owner integration (merge) before `Completed` regardless of a task's own
  Completion Conditions, and only the repository owner may set `Completed` (KB-088 §Who may
  set Completed). Re-checked the "can actually be done" test against the tracker's remaining
  `Ready` rows: `M0-06` still fails part 5 (`migration/M0-06-remove-default-admin` still
  present, unmerged); `M0-11` still fails part 2 (`Product Decision`, owner-only); `M2-A09` and
  `M2-A10` both clear all five parts — `depends_on: [M2-A01-03]` only, `Completed` and merged;
  neither is a `Product Decision`; no open question gates either (grepped
  `open-questions.md`, no hit); neither task file carries a ⛔ banner; `git branch --no-merged
  master` (re-run this session) shows no branch touching `M2-A09`'s files
  (`ScreenCatalogue.cs`, `ScreenRightStartupValidator.cs`,
  `ScreenRightStartupValidatorTests.cs`) or `M2-A10`'s (`AuthController.cs`-adjacent), and
  neither overlaps `M2-A02`'s still-unmerged branch (`Controllers/`, `tests/` only). Rank step
  1 (priority) settles it: `M2-A09` is **P0**, `M2-A10` is **P1**. **`M2-A09` selected**, not
  dispatched. Classification taken directly from the task file's own frontmatter
  (`complexity: LOW`, `risk: LOW`, explicit wins over derivation, KB-091 §4) — Investigate,
  Implement and Validate all route to `sonnet` per §5.1's LOW-complexity table, no HIGH-risk
  floor applying. `current-task.md` rewritten to point at `M2-A09`, carrying forward `M2-A02`'s
  unmerged-branch status (so `M2-A03`/`M2-B03` are correctly still read as `Blocked`) and Q-71.
  Previous note, superseded by the above:

  2026-08-24 Select-only pass (this session, later), starting from tip `762bb1d` on `master`
  (clean tree, verified via `git status --porcelain` before trusting this file, per the
  standing process note below — no output, nothing uncommitted). This file and
  `current-task.md` both still described the prior pass's conclusion ("no task is
  selectable") which predates three commits this session found already on `master` and
  neither file reflected: `7ea013d` (fixed `M2-C13` staleness — irrelevant to selection,
  `M2-C13` unblocks nothing), `dc74c5f` (KB-109, the Q-28/R-65 decision brief) and, decisively,
  `762bb1d` — **Q-28 and R-65 both answered (option A each) by the owner**, which the
  commit's own body states creates `M2-A09` and `M2-A10` and unblocks `M2-A02`. Re-ran the
  five-part test myself against all three newly-`Ready` rows rather than trust the commit
  message: `task-tracker.md` row for `M2-A02` now reads `Ready`²⁷˒⁵⁸ *(unblocked 2026-08-24)*,
  `M2-A09` and `M2-A10` both `Ready`⁵⁸ — all three `depends_on: [M2-A01-03]` only
  (`task-tracker.md` lines 112, 119, 120; frontmatter of each task file confirms the same),
  and `M2-A01-03` is `Completed` and merged (prior sessions' own re-verification, unchanged).
  (1) Hard prerequisite `M2-A01-03` genuinely `Completed`, merged — satisfied for all three.
  (2) None is `task_type: Product Decision` — `M2-A02` and `M2-A09` are `Security`, `M2-A10`
  is `Security` per its own file (checked `tasks/M2-A10.md` frontmatter). (3) No unanswered
  open question gates any of the three — Q-28/R-65 *were* the gates and both are now answered;
  grepped `open-questions.md` for `M2-A02`/`M2-A09`/`M2-A10`, no other hit. (4) None of the
  three task files carries a ⛔ banner (grepped). (5) `git branch --no-merged master` (re-run
  this session) shows no branch touching any of the three tasks' `source_files` or their task
  files — confirmed clean for all three (branches present are for `M0-03-01`, `M0-03-02`,
  `M0-04`, `M0-06`, `M0-08`, `M2-A08`, `M2-B12-01`, `M2-C10`, none overlapping). **All three
  clear the five-part test and are genuinely independent** (no file overlap between them —
  `M2-A02` touches `CurrencyController.cs`/`Program.cs`/`ApplicationDbContext.cs`/Currency
  Razor pages/`RightsHelper.cs`/`CurrencyVM.cs`; `M2-A09` touches only
  `ScreenCatalogue.cs`; `M2-A10` touches only `AuthController.cs`/`AuthController`-adjacent
  files per its own frontmatter — verified by reading `tasks/M2-A09.md` and `tasks/M2-A10.md`
  frontmatter `source_files`). Applied the rank order (KB-082 §Ready-task selection rule):
  step 1 priority — `M2-A02` and `M2-A09` are both **P0**, `M2-A10` is **P1** and drops out at
  this step. Step 2, most downstream unblocking, breaks the `M2-A02`/`M2-A09` tie: grepped
  every task file's `depends_on` frontmatter for `M2-A02` and `M2-A09` — three task files name
  `M2-A02` (`M2-A03`, `M2-B03`, `M2-D01`), none name `M2-A09` directly (its downstream value is
  the catalogue-integrity fix R-65 itself, not a `depends_on` edge). **`M2-A02` wins on rank**
  and is also explicitly the larger of the two — task-tracker.md's own "Currently unblocked"
  narrative for this commit calls it "the largest single release in this run," unblocking
  three of six G2 exit criteria transitively through `M2-A03`/`M2-B03`/`M2-B10`. `M2-A09` is
  listed as `tiedCandidates` since it is genuinely selectable and independent, ranked below
  `M2-A02` only by priority-then-downstream-count, not disqualified. `M2-A10` is not tied — it
  loses at step 1 (P1 < P0) before rank comparison is needed. Classification taken from
  KB-091 §4: `task_type: Security` → base complexity **HIGH** already at the ceiling (§4.1);
  `business_rules: [BR-AUTH-002]` non-empty and the task explicitly touches authorization
  (further raises would be recorded, but HIGH has no level above it, §4.2); risk **HIGH** per
  §4.3 (Security type, `business_rules` populated, and the change is observable — every client
  including the live Blazor UI's parity anchor is affected by the new `403`s). Per §5.1/§5.2
  HIGH-complexity, HIGH-risk routing: Investigate `opus`, Implement `opus`, Validate `opus`
  (risk HIGH forces `opus` for validation regardless of complexity, §5.2 item 2 — moot here
  since complexity HIGH already selects it). Not a safety stop: working tree clean (verified),
  branch will cut fresh from `master` at `762bb1d`, no sibling branch on any of this task's
  files, not a `Product Decision`, no DBA/credential/environment need disclosed by the task
  file (it substitutes `IUserRightsProvider` or seeds fixtures in-process, both documented as
  the task's own decision to make, not a blocked prerequisite). Selection only, per this
  session's instructions — `current-task.md` not yet rewritten by this pass. Previous note,
  superseded by the above:

  2026-08-24 Select-only pass (this session), starting from tip `8d330c8` on `master` (clean
  tree, verified via `git status --porcelain` before trusting this file — the initial
  conversation snapshot claimed branch `migration/M2-B11-health-checks-logging` at `7b4b86c`,
  which was stale; `git branch --show-current` showed `master` at `8d330c8` with nothing
  uncommitted). Found that `master` had moved three commits past what this file and
  `current-task.md` describe: `2328c94` (merge `M2-C13`), `e5e291c` ("M2-C13 Completed and
  merged; R-69 resolved") and `5f32f2a`/`8d330c8` (wording/G2-ownership fixes) — none of which
  touched this file or `current-task.md`. `task-tracker.md`'s own row for `M2-C13` now reads
  `Completed`⁵⁵˒⁵⁶˒⁵⁷ with footnote ⁵⁷ confirming the merge, the counts line reads "48
  `Completed`, 1 `Needs Review`", and the branch is gone from `git branch --no-merged master`.
  **But the tracker's own prose is internally inconsistent with its own row**: the "Current
  state" narrative (lines 269-271, 302-304) and this file's/`current-task.md`'s selection notes
  still describe `M2-C13` as `Needs Review`/unmerged/awaiting owner review — stale text the
  `Completed` commits (`e5e291c`, `5f32f2a`, `8d330c8`) never touched. Per CLAUDE.md's authority
  order, git/the row wins over the stale prose; this note corrects it rather than propagating
  it further. **This changes nothing about selectability**: grepped `task-tracker.md` and
  `dependency-graph.md` for `M2-C13` and confirmed no row lists it in `depends_on` — its merge
  releases no task, exactly as the prior note already concluded. Re-ran the five-part test
  against the tracker's three remaining `Ready` rows myself rather than trust the inherited
  claim: `M0-06` still fails part 5 (`migration/M0-06-remove-default-admin` still present,
  unmerged, in `git branch --no-merged master`); `M0-11` still fails part 2 (`task_type:
  Product Decision`, owner-only); `M2-A02` still fails part 3 (`open-questions.md` Q-28 row
  still has no resolution filled in, and no commit since 2026-08-24 touches R-65 or Q-28).
  **No task is selectable this pass.** Selection only, per this session's instructions —
  neither `current-task.md` nor `task-tracker.md`'s stale prose rewritten in this pass; noted
  here so the next session does not need to re-discover the inconsistency. Previous note,
  superseded by the above:

  2026-08-24 Session close-out (this session) for `M2-C13`, tip `3e821cc` on
  `migration/M2-C13-defer-confirm-host`. Independently validated `PASS` (attempt 1 of 3,
  `scopeOk: true`, `failureCategory: none`, 0 escalations) was carried into this session's
  launch context; this session recorded the outcome — task file Execution Record and
  frontmatter, task-tracker.md row/footnote ⁵⁶/"Current state", this file, investigation
  registry (`INV-048`, a negative result: no prior investigation of the deferral existed) —
  without re-running the verification commands itself (no code was changed by this close-out).
  Closed `Needs Review`, not `Completed`: this project requires owner integration (merge)
  before `Completed` regardless of a task's own Completion Conditions, and only the repository
  owner may set `Completed` (KB-088 §Who may set Completed). Re-checked the "can actually be
  done" test against the tracker's three remaining `Ready` rows (`M0-06`, `M0-11`, `M2-A02`)
  via `git branch --no-merged master` and confirmed none has changed status since the prior
  entry — all three still fail the five-part test, so **no task is selectable** this pass.
  `current-task.md` rewritten to record that. No new open question raised — the one
  investigation unknown (whether PrimeNG's `requireConfirmation$` is literally a plain
  `Subject`) was not promoted to `open-questions.md` because nothing is blocked on it; the
  implementation's own mutation test empirically confirms the queue is load-bearing, which is
  the behaviour that matters. Previous note, superseded by the above:

  2026-08-24 Select-only pass (previous session), starting from tip 521fe36 on master (clean tree,
  verified via `git status --porcelain` before trusting this file, per the process note below).
  This file and current-task.md both still read `STOPPED` / "No task is currently selectable",
  written before two things happened on master, neither reflected back into either file:
  `M2-C04-03` was merged (`ec8fb52`) and closed `Completed` (task-tracker.md, commit `4472698`,
  "M2-C04-03 and M2-C04 Completed and merged; pool is empty"); and `M2-C13` was then created
  `Ready` by commit `521fe36`, which added footnote 55 stating outright: "the only executable
  work left". Re-ran the five-part test myself rather than trust either claim: (1) `M2-C13`
  `depends_on: [M2-C04-03]`, and task-tracker.md row for `M2-C04-03` reads `Completed` with the
  merge commit `ec8fb52` confirmed present in `git log master` — hard prerequisite satisfied;
  (2) `task_type: Frontend`, not `Product Decision`; (3) grepped open-questions.md for `M2-C13` —
  no hit, nothing gates it; (4) task file `tasks/M2-C13.md` carries no ⛔ banner, `status: Ready`
  in its own frontmatter, `last_verified: 2026-08-24`; (5) `git branch --no-merged master` lists
  no branch touching any of its five `source_files` (`app.component.html`, `app.component.ts`,
  `confirm-dialog.service.ts`, `confirm-dialog.component.ts`, `angular.json`) or `M2-C13.md`.
  The tracker's own "Current state — 2026-08-24" prose block (line ~269, "Nothing is
  selectable... three Ready rows") was not refreshed by the `M2-C13` commit and is now stale;
  footnote 55, added in the same commit, is the newer and authoritative statement and is what
  this selection follows. Classification taken directly from the task file's own frontmatter
  (`complexity: MEDIUM`, `risk: MEDIUM`) rather than re-derived, per KB-091 §4 ("explicit
  complexity/risk in frontmatter wins"). Selection only — not dispatched — per this session's
  instructions; `current-task.md` was not rewritten by this pass (Select-only). Previous note,
  superseded by the above:

  2026-08-24 Session close-out (previous session) for `M2-C04-03`, tip `1806bca` on
  `migration/M2-C04-03-feedback-primitives`. Independently validated `PASS` (attempt 2 of 4,
  `scopeOk: true`, `failureCategory: none`) was already carried into this session's launch
  context; this session recorded the outcome — task file Execution Record, task-tracker.md
  footnote 53 and status/current-state, this file, open-questions.md (Q-80), and rewrote
  current-task.md — without re-running the verification commands itself (no code was
  changed by this close-out). Closed `Needs Review`, not `Completed`: the Completion
  Conditions require a human keyboard-only and screen-reader pass, and only the repository
  owner may set `Completed` (KB-088 §Who may set Completed). Re-checked the "can actually be
  done" test against the tracker's remaining three `Ready` rows (`M0-06`, `M0-11`, `M2-A02`)
  and confirmed via `git branch --no-merged master` that none has changed status since the
  prior entry — all three still fail the five-part test, so **no task is selectable** this
  pass. `current-task.md` rewritten to record that rather than pointing at the now-closed
  `M2-C04-03`. Previous note, superseded by the above:

  2026-08-23 Select-only pass (this session), starting from tip 9fc7bb9 on
  master. Re-read runner-state.md and task-tracker.md rather than trusting
  the file's own frontmatter, which was stale: M2-C10 (the previous
  selection_note's pick) was dispatched by an intervening session, hit
  attempt 1 FAIL category `environment` (its binding criterion needs a
  MEASURED wire format from a live [Authorize]d endpoint; this workstation
  has empty ConnectionStrings:MasterDb and Jwt:Secret -- not a code
  defect), and was recorded Blocked, owner Vivek (task-tracker.md
  footnote 52, row line 153). task-tracker.md's own "Current state"
  section (line 268) states outright: "Only one task is genuinely
  selectable: M2-C04-03" -- the other three Ready rows all fail the
  five-part test (M0-06 already has a branch; M0-11 is a Product
  Decision; M2-A02 is gated on unanswered Q-28 and R-65). M2-C04-03
  (modal/drawer/toast/empty-loading-error states, 3 d, P0,
  depends_on: [M2-C04-01]) independently re-verified: M2-C04-01 is
  Completed and merged (task-tracker.md line 158); task_type Frontend,
  not a Product Decision; no unanswered open question gates its own
  scope; task file re-specified for Angular by M2-C12-01 2026-08-22, no
  live ⛔ banner; git branch --no-merged master (checked this session)
  lists no branch touching this task's files or M2-C04-03.md. current-task.md
  was still pointing at M2-C10 (stale, written before M2-C10's environment
  block was discovered) -- corrected to M2-C04-03 in the same pass. Not
  dispatched -- selection only, per this session's instructions.
---

> **⚠ The selection note this file carried into 2026-08-21 was written by a session that then
> died, and one of its assertions was false by the time the next session read it.** It ended
> *"No safety-stop condition applies: working tree clean, branch will cut fresh from `master`."*
> The working tree was **not** clean. Between that sentence being written (16:04 IST) and the next
> session starting (~16:23), the same run implemented ~979 lines of `M2-B06` into the working tree
> and was killed before committing any of it. The next session therefore opened on a `RUNNING`
> state claiming *attempt 0, not yet dispatched* over a tree that already held most of the
> implementation.
>
> **This is the second time in this run that a claim inherited from bookkeeping has been wrong**
> (see **Process note — inherited status**), and the failure mode is the same both times: the
> record was written *before* the action it describes finished. The correction is not "read more
> carefully" — it is that **`git status --porcelain` belongs at the top of every run, before the
> state file is believed at all.** A `RUNNING` row and a dirty tree together mean a killed run, not
> a fresh one.
>
> **What the Select phase itself got right, and is preserved:** candidates were `M2-B06` (P1, 1 wk,
> `depends_on: [M2-A06, M2-B01]`, both `Completed` and merged) and `M2-B11` (P2, 3 d,
> `depends_on: [M2-A06]`); both passed the five-part "can actually be done" test; rank step 1
> settled it on priority. Classification `task_type: Backend` (base MEDIUM), raised to **HIGH** by
> `estimate: 1 wk` and a three-project `source_files`; risk **MEDIUM**. `M2-B06` was the correct
> pick and it has now been executed and closed `Needs Review`.

# Autonomous Runner State

**Machine-owned. Small by design.** This is the runner's own control state — whether a run is
live, what it is on, how many attempts it has spent, and why it stopped. It is written at
every state transition by `.claude/workflows/migration-runner.js`, so a run that is killed
mid-task resumes from this file rather than from a conversation.

It is **not** a status report. Those live elsewhere and this file never duplicates them:

| Question | Read instead |
|---|---|
| What is every task's status? | [`task-tracker.md`](task-tracker.md) (KB-081) — the authority |
| What is the active task, in detail? | [`current-task.md`](current-task.md) (KB-089) |
| Why did an attempt fail, and what was tried? | [`failure-log.md`](failure-log.md) (KB-092) |
| What are the routing and retry rules? | [`autonomous-runner.md`](autonomous-runner.md) (KB-091) |

If this file and KB-081 disagree about a task's status, **KB-081 wins** and this file is
corrected.

---

## State

| Field | Value |
|---|---|
| **Status** | `STOPPED` — **Select-only pass (this session), tip `66e3f43` on `master`, tree clean (`git status --porcelain` empty, `git branch --show-current` = `master`).** `current-task.md` already carried the correct "no active task, every remaining path needs a person" narrative for this exact HEAD, so this pass re-verified the five-part "can actually be done" test directly against `task-tracker.md` rather than trusting the inherited note. Grepped for every `Ready` row: only `M0-11` (tracker line 86, footnote ¹⁷) — fails part 2, `task_type: Product Decision`, owner-only per KB-091 §8. `M2-B12-02` reads `Blocked` (footnote ⁹⁰), not `Ready` — phase 1 already merged, phase 2 needs a named DBA. No other row is `Ready`. **No task selected this pass — `nextTaskId` returned empty.** `current-task.md` left unchanged (its narrative already matches this HEAD). Previous note, superseded by the above:

| **Status (previous, superseded)** | `STOPPED` — **`M2-B12-02` phase 1 merged to `master` 2026-08-25 on owner instruction (`02e4c7a`, `--no-ff`, no conflicts); nothing pushed.** Documentation only — 6 files, all under `docs/`, **4,138 insertions**: `Q-10-numbering-constraints.sql` (3,745 lines, 51 series × 4 query blocks, generated from KB-100 §9), **KB-101** the DBA runbook, KB-100 §11, the KB-005 registrations and tracker footnote ⁹⁰. **This merge closes the duplication trap the previous `Status` row flagged.** `master`'s row moves `Ready` → `Blocked`, so a runner pass reading `master` can no longer select `M2-B12-02` and rebuild work that already exists — which the last two passes came close to doing and avoided only by stopping early for the wrong reason. **`M2-B12-02` did NOT close `Completed`:** phase 2 needs a **named DBA** running the script against a live tenant, phase 3 pastes the output into KB-101 §5 and interprets it. **One acceptance criterion stays unmet and is recorded as unmet** — the runbook must name *the* DBA and *the* tenant databases, and neither exists in this repository (`M0-02` records the identical gap); KB-101 §2.1 supplies a query that **derives** the tenant list rather than guessing it. **Q-10 stays open; R-12 stays `Inferred (high confidence)`** — no duplicates have been looked for yet. **Selection pool after this merge: empty.** The sole `Ready` row is `M0-11`, a `Product Decision` that KB-091 §8 makes owner-only. `nextTaskId`: none. |

| **Status (previous, superseded)** | `BLOCKED` — **`M2-B12-02` is blocked on a named DBA for phase 2. Phase 1 is DONE — and this pass did not do it.** (2026-08-25, tip `729da72` on `master`, tree clean.) **The marker is right; the runner's reasoning for it was not.** Its stop reason states, correctly, *"This does not block starting: Phase 1 (writing the read-only SQL script, the DBA runbook, and the results-doc scaffold) is **fully AI-executable** and is this session's actual scope"* — and it then stopped without writing any of it, for the **second consecutive pass on this same task**, after footnote ⁸⁹ named the error and footnote ⁸⁵ recorded it against `M2-C10`. **`requiresHuman` is still being read as a start-blocker on a two-phase task; it is not one.** **What makes `BLOCKED` correct anyway:** phase 1 was delivered *outside the runner*, by the interactive session, and is committed at **`ce93c6d`** on `migration/M2-B12-02-verify-unique-constraints` — `Q-10-numbering-constraints.sql` (3,745 lines, 51 series × 4 query blocks, generated from KB-100 §9) and **KB-101**, the DBA runbook. So the only thing genuinely outstanding is phase 2, which needs a **named DBA** with production credentials. 🚩 **`master` does not know this.** Its `task-tracker.md` row still reads **`Ready`**⁸⁹ and `master` carries **none** of the phase-1 artefacts — both live only on the unmerged branch. **Any runner pass reading `master` will therefore select `M2-B12-02` again and can rebuild work that already exists**; this pass avoided that only by stopping early. The fix is to merge the branch, not to re-run. **No duplication occurred this pass:** no branch was cut, no artefact written, `master` gained only bookkeeping. `nextTaskId`: none — once `M2-B12-02` reads `Blocked` on `master`, the sole `Ready` row is `M0-11`, a `Product Decision` that KB-091 §8 makes owner-only. |

| **Status (previous, superseded)** | `STOPPED` — **`M2-B12-02` selected 2026-08-25 at tip `92e8b28`, then the run stopped **without attempting Phase 1**. No branch was cut and no artefact was produced.** 🚩 **This is a repeat of the error footnote ⁸⁵ records against `M2-C10`, and footnote ⁸⁹ explicitly warned against it** — the run's own stop reason concedes the point: *"Phase 1 (the SQL script + DBA runbook + results-doc scaffold) is **fully AI-executable**"*, and then it stopped anyway. **`requiresHuman` is not a start-blocker on a task designed in two phases.** `tasks/M2-B12-02.md` §*The credential problem, stated honestly* splits it deliberately: step 1 is a **read-only SQL script**, written without any database; step 2 is *"a named DBA, using their own credentials"* executing it. The task's *Target Result* closes it **`Blocked`** if no DBA participates — that is a **terminal** state reached **after** the AI half is delivered, exactly as `M0-04` was run: runbook, inventory and checklist all committed, then `Blocked` on four named humans. Marking it `BLOCKED` before writing a line inverts that. **Corrected marker:** `STOPPED`, per this file's own vocabulary — *"a clean, expected end — budget reached, or no ready task"* does not fit either, but `BLOCKED` (*"a run halted needing a human"*) overstates it: nothing needed a human yet. **The tracker row stays `Ready`** (footnote ⁸⁹) — `M2-B12-02` has **not** been attempted and remains the one self-selectable task. `nextTaskId`: `M2-B12-02`, Phase 1. |

| **Status (previous, superseded)** | `STOPPED` — **Select-only pass, 2026-08-25, tip `074ad1e` on `master`, tree clean.** No dependency-ready task remained at that tip. Superseded by `M2-B12-01`'s subsequent merge (`f10b5fc`) and `M2-B12-02`'s release to `Ready` (`92e8b28`), both above. |
| **Status (previous, superseded)** | `STOPPED` — **Four stale duplicate lineages archived 2026-08-25 on owner instruction; `master` untouched (`3669d37`), nothing merged, nothing pushed.** Each was an abandoned earlier attempt whose work had already landed on `master` **via a differently-named branch**, so the pair was invisible to a name-based check and each leftover was 176-332 commits behind — merging one would have dragged `master`'s files *backwards*. Verified before renaming, by locating the merge commit on `master` for each: `migration/M0-03-01-externalise-appsettings-secrets` → superseded by `migration/M0-03-01-appsettings-secrets` (`f55db52`); `migration/M0-03-02-hardcoded-connection-strings` → by `…-hardcoded-connection-strings-csharp` (`ec2f0f3`); `migration/M0-08-gitignore-build-output` → by `migration/M0-08-build-output-guard` (`f873a9a`); `migration/M2-A08-row-level-scoping` → by `migration/M2-A08-row-scope-and-account-gates` (`380c805`). **The M2-A08 leftover carried real code**, not just bookkeeping — `dfd3cbb`, the expired-QR-token predicate for Q-05/R-16 — so it was checked individually rather than assumed: the identical predicate and comment are present on `master` at `Repository/MasterRepository/Admins/UserRepository.cs:69` (the file **moved** from `AdminRepository/` to `Admins/`, which is why a path-based grep misses it), and `KB-120` exists as `architecture/row-scope-and-account-gates.md`. Renamed, not deleted — `archive/M0-03-01-externalise-stale-lineage`, `archive/M0-03-02-connstrings-stale-lineage`, `archive/M0-08-gitignore-stale-lineage`, `archive/M2-A08-row-level-scoping-stale-lineage` — matching the existing `archive/*-stale-lineage` convention, so every commit is still reachable and the rename is reversible. **Effect on selection: none** — no archived branch belonged to a `Ready` row, so this does not clear part 5 for anything. The unmerged `migration/*` queue is now **3**, not 8: `M0-04` (`Blocked` by design, deliverables complete), `M2-B12-01` (`Blocked`, escalation budget exhausted) and `M2-C10` (`Blocked`, attempt 1 `FAIL`/`environment`). **Two process findings:** `wt-M2-A08` held the archived branch and followed the rename cleanly (worktree still valid, tree was clean); and `wt-M2-B01` has **1 uncommitted file** (`docs/kb/execution/runner-state.md`) that no live session claims — the orphaned-working-tree pattern already recorded in this file's process notes. `nextTaskId`: none. |

| **Status (previous, superseded)** | `BLOCKED` — **No dependency-ready task remains.** Select-only pass at master tip `701eef2` on `master`, tree clean (`git status --porcelain` empty, `git branch --show-current` = `master`). Re-ran the five-part "can actually be done" test against every `**Ready**` row in `task-tracker.md`: only `M0-04` (line 67) and `M0-11` (line 86) read `Ready`. `M0-11` fails part 2 (`task_type: Product Decision`, owner-only, never self-selectable per KB-091 §8). `M0-04` fails part 5: a **live, unmerged** `migration/M0-04-credential-rotation-runbook` branch (cut from `beee0f9`, the very commit that reopened `M0-04` as `Ready`) already ran the full task and closed it in its own designed terminal state `BLOCKED` (all four AI-deliverable artefacts committed: runbook, credential inventory, verification checklist, Q-19 confirmation), independently of `master`'s tracker/footnote ⁷⁰ which is simply unaware of that branch. Selecting `M0-04` again would fork a duplicate second attempt rather than advance the task. `M0-06` itself is no longer `Ready` at all (merged, closed `Blocked` on Q-25/Q-26). No other tracker row reads `Ready`. **Conclusion: nothing is dependency-ready and selectable this pass; `nextTaskId` is empty. Who can unblock it: owner Vivek** — must either merge the M0-04 branch with its BLOCKED outcome, or answer Q-25/Q-26 to unblock dependent tasks. See `selection_note` for the full evidence trail. `current-task.md` left unrewritten (selection-only pass) but is now stale on the `M0-06` merge, the `M0-04` branch's outcome, and footnote ⁷⁰. Previous status, superseded by the above: |

| **Status (previous, superseded)** | `STOPPED` — **`M0-06` merged to `master` 2026-08-25 on owner instruction (`b057ceb`, `--no-ff`); nothing pushed.** The branch was 240 commits behind `master`; **no code conflicted** — all 8 conflicts were `docs/kb/` bookkeeping. Resolutions: `task-tracker` took `master`'s `M0-10` (`Completed`) and the branch's `M0-06` (`Blocked`¹⁶); `runner-state` and `current-task` took `master`'s, 240 commits newer; `investigation-registry` took `master`'s with `INV-035`'s stale *reserved* row updated to `Complete` and `INV-038` released; `INDEX`, `open-questions`, `failure-log` and `prompt-template` were unioned. **One semantic break `git` could not detect:** the branch's new namespace `V.SMART.Shared.Tests.Data` shadowed the *relative* `Data.ApplicationDbContext` in `master`'s `UserRepositoryQrTokenTests` (added after the branch was cut), so the merged tree failed to compile until both references were qualified `global::V.SMART.Shared.Data.ApplicationDbContext`, matching `NoPagesReferenceFromDomainTests.cs:57`. **Verified on the merged tree:** `V.SMART.Shared` **0 errors**; Shared tests **96 passed / 0 failed / 1 skipped** (91 + 6 new `SeedDataTests`); Api tests **508 passed / 0 failed**, unchanged from the recorded baseline. **`M0-06` did NOT close `Completed`** — its acceptance criterion 2 is still unmet, so the tracker row moved `Ready` → `Blocked` on **Q-25** and **Q-26**, both owner-only. **Net effect on selection: the pool did not grow.** `Ready` is now exactly `M0-04` and `M0-11`, both of which still fail the five-part test (`M0-04` is `Blocked` in substance on its own branch; `M0-11` is a `Product Decision`). **Residual exposure, measured this pass:** the published PBKDF2 hash is still at `HEAD` in **109** `.cs` files under `Migrations/`; **1** of them (`20260217110637_InitialCreate.cs:7562`) is a real `InsertData` that provisioning replays — the other 108 are `.Designer.cs` snapshots. Removing the seed from the model does **not** remove it from the provisioning path; that is exactly what Q-26 asks the owner to decide. `nextTaskId`: none. |

| **Status (previous, superseded)** | `STOPPED` — **`M0-04` was unblocked 2026-08-25 and is now the selectable task; tip `b96e33b`.** Three prior Select passes correctly found nothing ready, but all of them — and several of this session's reports — treated `M0-04` as blocked on production access. **It is not.** Every one of its acceptance criteria is a document, and its *Target Result* states it closes **`Blocked`** if no human performs the rotation (step 10: *"Do not report Completed because the documents were written"*). The owner's 2026-08-19 deferral described the **rotation**, and was applied to the whole task. Q-19, its first step, was already answered 2026-08-12. **Part 5 cleared without waiving it:** the stale sibling `migration/M0-04-credential-rotation-runbook` (one commit, **324 behind `master`**) was renamed **`archive/M0-04-runbook-stale-lineage`**, preserving its 269-line runbook at `1f905db`. **That runbook is to be harvested, not merged** — its procedures are sound and stack-independent, but its evidence is stale (`Jwt:Secret` cited at `appsettings.json:12`; it is at `:37` with value `""` since `M0-03`, and the Api project has been tracked since `623b1e1`). Owner: **Vivek**. |
| **Status (previous, superseded)** | `STOPPED` — **`M2-B10` merged 2026-08-25; `master` pushed to `origin` (164 commits, `2a45330..d4c9f60`).** G2 criterion 4 is **met**. `M2-B10` closed KB-114 §13's gaps rather than documenting them — 45 `[ProducesResponseType]` added, the 404 on `GET /currencies/{id}` and the 409 on `DELETE` now present — then committed `api/openapi.json` and 62 generated client files with a CI job that regenerates and diff-checks both. Verified on the merged result across both stacks: Api **0 errors / 6693 warnings**, **508 Api tests** (470 → 508), `typecheck`/`lint`/`format:check` clean, **309 frontend tests**, `build` exit 0, bundle **571.20 kB unchanged**. **Two findings from the push itself:** branch protection on `master` **exists** and requires pull requests (so `M2-A03`'s remaining item is an edit to an existing ruleset, not creating one — footnote ⁶⁹), and every merge in this run bypassed that rule with owner authority (**Q-82**). Owner: **Vivek**. |
| **Status (previous, superseded)** | `RUNNING` — **`M2-B10` selected this pass (2026-08-24, tip `c2a9140` on `master`, tree clean — `git status --porcelain` empty, `git branch --show-current` = `master`).** Applied the five-part "can actually be done" test against `task-tracker.md`'s three `Ready` rows: `M0-06` fails part 5 — sibling branch `migration/M0-06-remove-default-admin` still present in `git branch --no-merged master`; `M0-11` fails part 2 — `task_type: Product Decision`, owner-only, never self-selectable; `M2-B10` clears all five — Hard prerequisite `M2-B03` is `Completed` and merged (`2630657`, tracker row 130); not a `Product Decision`; no open question in `open-questions.md` names it; `tasks/M2-B10.md` frontmatter carries no ⛔ banner (`status: Not Started`, `last_verified: 2026-08-12`); no unmerged branch (`git branch --no-merged master`) touches its `source_files` (`Program.cs`, `V.SMART.Api.csproj`, `CurrencyController.cs`, `AuthController.cs`, `.github/workflows/`, `ADR-002`). No tie — `M2-B10` is the only row clearing the test. **Classification derived per KB-091 §4** (task file carries no explicit `complexity`/`risk` override): base `DevOps` → MEDIUM (§4.1); one raise applies — `estimate: 3 d` (§4.2) — giving **complexity HIGH**; `source_files` includes `Program.cs` → **risk HIGH** per §4.3. Per §5.1 HIGH-complexity routing, and confirmed independently by §5.2 item 2 (risk HIGH forces `opus` for validation regardless): Investigate `opus`, Implement `opus`, Validate `opus`. Not a safety stop: working tree clean, branch will cut fresh from `master` at `c2a9140`, no sibling branch on `M2-B10`'s files, not a `Product Decision`, no DBA/credential/environment need disclosed by the task file — though the task's own text still names the SPA consumer "React", stale since ADR-007 (2026-08-20) superseded `ADR-003`; that is a re-specification note for the implementer, not a selection blocker. **Attempt 0, not yet dispatched.** |
| **Status (previous, superseded)** | `STOPPED` — **`M2-A03` and `M2-B03` are both merged to `master` (2026-08-24).** `M2-A03`'s permission-matrix harness landed at `d94d8ce`: **470 Api tests** (364 → 470) and it found **no enforcement holes**, independently confirming `M2-A02`'s enforcement. It stays **`Needs Review`**, not `Completed`, on one criterion only — the CI job must be a *required* status check, which is GitHub branch protection and cannot be set from here. `M2-B03` landed as **KB-114** (`controller-conventions.md`): `PASS` on attempt 2, where attempt 1 caught a genuine business-rule defect — the template asserted `IsDuplicate…Async` yields a 409 out of create/update, true of `CurrencyService` but **false** of `MfgPoService`, so a `POST` copied from it would have created a duplicate the live screen refuses. Across both merges the diff touches only `tests/` and `docs/kb/` — **no production code**. Owner: **Vivek**. Nothing pushed. |
| **Status (previous, superseded)** | `STOPPED` — **Session close-out for `M2-A09`, this pass (2026-08-24), tip `c3c595e` on `migration/M2-A09-screen-catalogue-phantoms`.** An independently validated `PASS` verdict (attempt 1 of 3, `scopeOk: true`, `failureCategory: none`, 0 escalations) was carried into this session's launch context; this session recorded the outcome only — task file Execution Record + frontmatter (`status: Needs Review`), `task-tracker.md` row + footnote ⁶⁰ + "Current state" narrative + rollup counts, this file, and rewrote `current-task.md` — without re-running the verification commands itself (no code was changed by this close-out). No new KB discoveries: R-65 and `server-side-authorization-spec.md` §1.3 were already updated by the implementing session in the same commit. Closed **`Needs Review`, not `Completed`**: this project requires owner integration (merge) before `Completed` regardless of the task's own Completion Conditions, and only the repository owner may set `Completed` (KB-088 §Who may set Completed). Branch left unmerged, unpushed, for review. Re-ran the five-part "can actually be done" test against the tracker's remaining `Ready` rows: `M0-06` still fails part 5 (sibling branch present); `M0-11` still fails part 2 (`Product Decision`); `M2-A10` clears all five parts — `depends_on: [M2-A01-03]` only, `Completed`/merged; `Security`, not `Product Decision`; Q-28 (the question naming it) is `ANSWERED`; no ⛔ banner; no unmerged branch touches its `source_files`. **`M2-A10` selected next, not yet dispatched.** |
| **Status (previous, superseded)** | `RUNNING` — **`M2-A09` selected this pass (2026-08-24, tip `e7d80ee` on `master`, tree clean — `git status --porcelain` empty, `git branch --show-current` = `master`).** Re-confirmed the prior pass's selection rather than re-deriving it from scratch: `current-task.md` already pointed at `M2-A09` with a matching rationale, so this pass re-verified the five-part test directly against current state instead of trusting the inherited note. (1) Hard prerequisite `M2-A01-03` — `Completed`, merged (task-tracker.md, unchanged). (2) `task_type: Security`, not `Product Decision`. (3) Grepped `open-questions.md` for `M2-A09` — no hit; nothing gates it. (4) `tasks/M2-A09.md` frontmatter carries no ⛔ banner, `status: Ready`, `last_verified: 2026-08-24`. (5) `git branch --no-merged master` shows no branch touching `ScreenCatalogue.cs`, `ScreenRightStartupValidator.cs` or `ScreenRightStartupValidatorTests.cs`; `M2-A02`'s still-unmerged branch touches only `Controllers/`/`tests/` (no overlap with `Authorization/`). Ranked ahead of sibling `M2-A10` (also `Ready`, same `depends_on`) on priority alone — `M2-A09` is P0, `M2-A10` is P1; both are independent and listed as `tiedCandidates` only in the loose sense that `M2-A10` remains selectable next, not because they tie on rank. **Classification taken directly from `tasks/M2-A09.md`'s own frontmatter** (`complexity: LOW`, `risk: LOW` — explicit values win over derivation per KB-091 §4, even though `task_type: Security` would otherwise default risk to HIGH per §4.3; the task file's own LOW risk is accepted as a considered override, not re-derived). Per §5.1 LOW-complexity routing, no floor overridden (risk LOW, not HIGH, so §5.2 item 2 does not apply): Investigate `haiku`, Implement `sonnet`, Validate `sonnet` (§5.2 item 1: validation never `haiku`, already satisfied). Not a safety stop: working tree clean, branch will cut fresh from `master` at `e7d80ee`, no sibling branch on `M2-A09`'s files, not a `Product Decision`, no DBA/credential/environment need disclosed by the task file. **Attempt 0, not yet dispatched.** |
| **Status (previous, superseded)** | `STOPPED` — **Session close-out for `M2-A02`, this pass (2026-08-24), tip `634d30c` on `migration/M2-A02-currency-authorization`.** An independently validated `PASS` verdict (attempt 1 of 3, `scopeOk: true`, `failureCategory: none`, 0 escalations) was carried into this session's launch context; this session recorded the outcome only — task file Execution Record, `task-tracker.md` row + footnote ⁵⁹ + "Current state" narrative, this file, and rewrote `current-task.md` — without re-running the verification commands itself (no code was changed by this close-out). Closed **`Needs Review`, not `Completed`**: this project requires owner integration (merge) before `Completed` regardless of the task's own Completion Conditions, and only the repository owner may set `Completed` (KB-088 §Who may set Completed). Branch left unmerged, unpushed, for review. Re-ran the five-part "can actually be done" test against the tracker's remaining `Ready` rows: `M0-06` still fails part 5 (sibling branch present); `M0-11` still fails part 2 (`Product Decision`); `M2-A09` and `M2-A10` both clear all five parts, mutually independent, no overlap with `M2-A02`'s unmerged branch or with each other — `M2-A09` wins on priority (P0 vs `M2-A10`'s P1). **`M2-A09` selected next, not yet dispatched.** |
| **Status (previous, superseded)** | `RUNNING` — **`M2-A02` selected this pass (2026-08-24, tip `762bb1d` on `master`, tree clean).** Owner answered Q-28 and R-65 (option A each, KB-109, commit `762bb1d`), which unblocked `M2-A02`, `M2-A09` and `M2-A10` — all three `Ready`, mutually independent, depend only on `M2-A01-03` (`Completed`, merged). `M2-A02` and `M2-A09` are both P0; `M2-A02` wins rank on most-downstream-unblocking (3 dependents — `M2-A03`, `M2-B03`, `M2-D01` — vs 0 for `M2-A09`) and is the larger release (unblocks 3 of 6 G2 exit criteria transitively). `M2-A10` is P1, ranked below both. See `selection_note` for the full five-part re-verification. Selection only this pass — not yet dispatched. |
| **Status (previous, superseded)** | `STOPPED` — **Select-only pass, 2026-08-24, tip `8d330c8` on `master`.** `M2-C13` is now confirmed `Completed` **and merged** (`2328c94`/`e5e291c`, three commits ahead of what this file previously described) — task-tracker.md row reads `Completed`⁵⁵˒⁵⁶˒⁵⁷, no row names it in `depends_on`, so the merge releases no task. Re-ran the five-part "can actually be done" test against the tracker's three remaining `Ready` rows (`M0-06`, `M0-11`, `M2-A02`) directly against current `git`/KB state rather than trusting the inherited note — all three still fail it (see `selection_note` above for detail), so **no task is selectable this pass**. |
| **Status (previous, superseded)** | `RUNNING` — **`M2-C13` selected this pass (2026-08-24, tip `521fe36` on `master`, tree clean).** Defer the confirm-dialog host to bring the initial bundle back inside Angular's 600 kB warning budget (R-69). Selection only, then dispatched and closed in a later pass — see Status above. |
| **Stop reason (after `M2-B10`)** | **`M2-B10` was the only dispatched task and it closed clean, independently validated `PASS` on attempt 1 of 1; nothing behind it merges without owner review, and nothing else is selectable.** All 18 actions across 6 controllers carry an explicit operation id, a resource tag and `[ProducesResponseType]` for every status; `api/openapi.json` committed (81,652 bytes, valid, 18 operations, 1:1 with the controller surface); `ng-openapi-gen` 1.0.5 chosen (rejected `@hey-api/openapi-ts`, `openapi-typescript`, `openapi-generator-cli`) and generated into `frontend/nexgen-web/src/app/core/api/generated/` — the Angular path `M2-C01` reserved, not the React-era path the stale task file named; `decimal?` → `number | null`, flagged to `M2-C10` (INV-051, KB-112). CI job `api-contract` added to the single `ci.yml`; the drift check was proven to fail (operation-id rename, hand-edited generated file) and to pass clean, both reverted. `V.SMART.Shared/` diff empty; controller diff is attributes/comments only (mechanically verified). `V.SMART.Api` warning gate 6,693 = baseline. **Not `Completed`** — owner integration required (KB-088 § Who may set Completed); left unmerged for review. `M2-D01`'s `M2-B10` prerequisite half stays unreleased until the branch merges. Full record: tracker footnote ⁶⁷, `tasks/M2-B10.md` § Execution Record (2026-08-24). |
| **Stop reason (after `M2-A02`)** | **`M2-A02` was the only dispatched task and it closed clean, independently validated `PASS`; nothing behind it merges without owner review, and the remaining `Ready` rows now split into two genuinely independent, selectable siblings.** `[RequireScreen("Currency")]` and per-action `[RequireRight(...)]` land on `CurrencyController` (`CurrencyController.cs:21,53,71,81,95,109`) on `migration/M2-A02-currency-authorization` (tip `634d30c`); the controller diff is usings, attributes and a provenance comment only. New `tests/V.SMART.Api.Tests/CurrencyAuthorizationTests.cs` (45 tests) proves the matrix by reflecting the real attributes onto the real `ScreenRightAuthorizationFilter`; full API suite 357/357, Shared suite 90/91 (1 pre-existing unrelated skip). Anonymous-401 and 403-`application/problem+json` proved at policy/`ObjectResult` level only — no `WebApplicationFactory` host exists, so over-the-wire proof is explicitly `M2-A03`'s. Raised **Q-71**: whether some task should now switch on the dormant "unannotated controller is an error" direction in `ScreenRightAuthorizationFilter.cs`/`ScreenRightStartupValidator.cs`, now that every endpoint is annotated or exempt. **Not `Completed`** — owner integration required (KB-088 §Who may set Completed); left unmerged for review. `M2-A03` and `M2-B03` stay `Blocked` until this branch is merged to `master`. Full record: tracker footnote ⁵⁹, `tasks/M2-A02.md` § Execution Record (2026-08-24). |
| **Stop reason (final, after `M2-C13`)** | **`M2-C13` was the only dispatched task and it closed clean, independently validated `PASS` on attempt 1 of 3; nothing behind it merges without owner review, and nothing else is selectable.** Deferred the `<app-confirm-dialog>` host behind `@defer (when confirmHostRequested())` on `migration/M2-C13-defer-confirm-host` (tip `3e821cc`) — `p-confirmdialog`, `p-dialog`, `app-form-field` and `app-textarea` move into a lazy `confirm-dialog-component` chunk (148.61 kB raw / 29.06 kB transfer). `ConfirmDialogService` now queues pre-mount `confirm()` requests rather than emitting into PrimeNG's plain-`Subject` `requireConfirmation$`, replayed via `markHostMounted()` from `ConfirmDialogComponent`'s `afterNextRender`; `confirm-dialog.deferred.spec.ts` (5 new tests) proves the first-call case through the real `@defer` trigger and was independently confirmed to fail without the fix (mutation test: `4 failed | 1 passed` with the queue removed, `5 passed` restored, both in an out-of-repo copy). Measured: `npm run build` **711.75 kB → 571.20 kB raw / 158.24 kB → 136.72 kB transfer**, no budget warning (was: `▲ bundle initial exceeded maximum budget... 711.75 kB`). All 10 acceptance criteria `MET`; `typecheck`/`lint`/`format:check` clean; `test:ci` **309 passed / 47 files** (up from 304/46); `build` exit 0. Diff confined to `frontend/` and `docs/kb/`, 7 files, 280/16 — `git rev-parse master` = merge-base = `521fe36`, so HEAD is the merged result on top of `master`. R-69 marked `RESOLVED` in the same commit; the "`M2-C03` lands next" claim it and footnote ⁵⁴ carried was corrected. **Not `Completed`** — this project requires owner integration (merge) before `Completed` regardless of the task's own Completion Conditions (KB-088 §Who may set Completed); left unmerged for review. Full record: tracker footnote ⁵⁶, `tasks/M2-C13.md` § Execution Record (2026-08-24). |
| **Stop reason (after `M2-C04-03`)** | **`M2-C04-03` was the only dispatched task and it closed clean, independently validated `PASS`; nothing behind it merges without owner review, and nothing else is selectable.** All 14 overlay/feedback primitives (KB-051 §Overlays, §Feedback) built over PrimeNG 22.1.0 only, on `migration/M2-C04-03-feedback-primitives` (tip `1806bca`). Attempt 1 `FAIL` (`acceptance-criterion`) — the focus-trap/restore/scroll-lock matrix was 8/12 asserted, not 12/12, and the confirm dialog had **no** focus-restoration code at all (PrimeNG 22.1's `p-confirmdialog` depends on `pAutoFocus` on its own accept/reject buttons, which this component's custom `#footer` replaces). Diagnosed and fixed at `56b4c1d`: an `effect` captures the invoker, `afterEveryRender` focuses the dialog panel, `onDialogHide()` restores focus. Attempt 2 independently validated `PASS`, all 20 acceptance criteria `MET`, `scopeOk: true`. Verified: `typecheck`/`lint`/`format:check` all exit 0; `test:ci` 304 passed / 46 files; `build` exit 0, 711.75 kB raw / 158.28 kB gzip with one non-fatal budget warning (**R-69**, inside KB-050's `<250 kB` gzip target). Scope confined to `frontend/nexgen-web/` and `docs/kb/`; no `V.SMART/**`, no schema, BR-SO-003 stays server-side. Documentation landed on the branch: KB-051, KB-050, the INV-006 amendment (including the measured PrimeNG confirm-dialog focus defect), **Q-78**/**Q-79** and **R-69**–**R-72**. One new question raised at this close-out, **Q-80** (an undocumented `maxlength=500` on the BR-SO-003 reason textarea). **Not `Completed`** — the Completion Conditions require a human keyboard-only and screen-reader pass, not yet done, and only the repository owner may set `Completed` (KB-088 §Who may set Completed). Full record: tracker footnote ⁵³, `tasks/M2-C04-03.md` § Execution Record (2026-08-24), `failure-log.md`. |
| **Stop reason (final, after `M2-C04-02`)** | **`M2-C04-02` was the only dispatched task and it closed clean, independently validated `PASS`; nothing behind it merges without owner review.** Form layer for KB-051 §Forms — `app-form-layout`, `app-form-section`, `app-form-field` and all 14 inputs — built on `migration/M2-C04-02-form-controls` (tip `2eb7d8e`). Attempt 1 `FAIL` (readonly expressed as disabled on nine controls, dropping values out of the tab order), attempt 2 `FAIL` (`app-file-upload` missing its loading row), both diagnosed and fixed in place (`802af10`, `b6b3738`); attempt 3 independently validated `PASS`, `scopeOk: true`, all 17 acceptance criteria `MET`. Two items openly still owed, neither a retry item: the human keyboard-only pass (a Completion Condition, not checkable by any session) and this tracker row itself (now corrected, footnote 51). `M2-C05`/`M2-C05-01` stay `Blocked` until the branch is reviewed and merged — a `Needs Review` Hard prerequisite does not release a successor (CLAUDE.md § Standing constraints). Discoveries recorded during implementation, confirmed still present at close-out: **R-68** (client-side-only party-completeness gate in `CustomerSelection.razor`, no SPA owner), the INV-006 amendment (`DataAnnotations` → Angular `Validators` mapping surface), **Q-73** (TrimmedInputText whitespace quirk, intentional or not) and **Q-74** (how `{ value, isAmount }` projects onto the server's separate amount/percent columns) — none blocking. Full record: tracker footnote ⁵¹, `tasks/M2-C04-02.md` § Execution Record (2026-08-23) — session close-out, `failure-log.md`. |
| **Stop reason (final, after `M2-C01`)** | **`M2-C01` was the only dependency-ready, non-excluded task and it closed clean; nothing behind it is ready.** Angular workspace + TypeScript `strict` + lint + test + CI at `frontend/nexgen-web/`, replacing the React tree deleted in the same commit (`migration/M2-C01-angular-workspace`, `67410b0`). Attempt 1 of 3, 0 escalations, independently validated **`PASS`**. 15 of 16 acceptance criteria `MET` outright; the 16th (a "no `vite`/`zod` entry anywhere" clause) is a disclosed **specification defect** — no Angular 22 workspace can satisfy it literally, since `@angular/build`/`@angular/cli`/`@angular/forms` pull `vite`/`zod` as their own transitive dependencies, and no React remnant of either was found. Two new discoveries recorded in the same commit: **R-51** (PrimeNG 22 ships client-side licence-banner enforcement, no key configured) and **Q-66** (whether a paid licence is required — owner decision). Nothing in the dependency graph is released by a `Needs Review` `M2-C01` — `M2-C04-01`, `M2-C10`, `M2-C02` and the rest of the `M2-C` tree all need it `Completed` and merged. Full record: tracker footnote ⁴⁰, `tasks/M2-C01.md` § Execution Record (2026-08-21). |
| **Stop reason (after `M2-B11`)** | **`M2-B11` was the only candidate and it closed clean; nothing behind it is ready.** Health checks (`/health/live`, `/health/ready`) and a new `ILogger`-based `StructuredLoggingService` land on `migration/M2-B11-health-checks-logging`. Attempt 1 failed the CI warning-gate ratchet (6694 measured vs. 6693 baseline, a `CS8767` nullability mismatch in the new `TenantInfoDestructuringPolicy`); attempt 2 fixed it with an annotation-only change (`81ad961`) and re-measured at 6693 = baseline, gate `PASSED`. Independent validator verdict **`PASS`**, `scopeOk: true`, no regressions. `ILoggingService.cs` is byte-identical; `FileLoggingService` is kept as the Blazor/MAUI registration; the new sink is wired only in `V.SMART.Api`. R-23 marked resolved **for `V.SMART.Api` only**. Two criteria stated as unmet rather than glossed: no `LogUserAction` event was observed over HTTP (no call site exists in `V.SMART.Api` — out of scope to add one) and the Blazor host was not started. Nothing in the dependency graph names `M2-B11` as a prerequisite, so this close releases no other task. Full record: tracker footnote ³⁶, `failure-log.md`, `tasks/M2-B11.md` § Execution Record (Close-out). |
| **Stop reason (after `M2-B09`)** | **The owner merged `M2-B01`, which refilled the pool, and `M2-B09` was executed to completion.** It closed `Needs Review` on `migration/M2-B09-reference-endpoints` (`d1175db`) — six cached reference endpoints, the R-15 boundary fix, KB-124, **162** Api tests (117 → 162), every build at its exact baseline, scope verified by diff. Two criteria openly unmet: no Blazor screen opened, and no end-to-end two-tenant HTTP test (proven at the policy level instead — the residual risk, recorded in KB-124 §6). **Remaining pool after it:** `M2-B06` (P1, 1 wk, released by the same merge, not yet started) and `M2-B11` (P2, 3 d). Both are genuinely selectable — this is the first time in this run that has been true. |
| **Stop reason (after `M2-B06`)** | **The candidate set is empty on every path, and one merge fixes most of it.** `M2-B06` was the last candidate: sound task, but it specifies every endpoint under `/api/v1` and **`master` has no `/api/v1`** — the prefix and the `ApiRoutes.V1` constant exist only on the unmerged `migration/M2-B01-api-versioning` branch. Its `depends_on` did not declare `M2-B01`, so neither selection step saw it: step 1 checks *declared* prerequisites, step 2 checks *file* overlap, and this collision is on a route surface. `depends_on` corrected to `[M2-A06, M2-B01]`; **`Blocked`, no re-specification needed**, unlike `M2-B05`. **Merging `M2-B01` alone releases `M2-B06`, `M2-B09` and `M2-B11`.** Full account: tracker footnote ³², `failure-log.md`. |
| **Stop reason (after `M2-B05`)** | **`M2-B05` was selected cleanly and then falsified at Investigate — `Blocked`, category `specification`, owner Vivek, no code written and no branch.** It won Select legitimately: P1, 2 d, prerequisite merged, **zero file overlap** with any of the seven unmerged branches, no `⛔` banner. Its own step 2 says to re-verify rather than trust the document; doing so showed the task's premise is false. There are **no `screenCode` literals** — the code resolves the screen code at runtime from the database by name (`GetScreenCodeByScreenNameAsync`, **166** sites, **61** Razor pages; **0** literals in **244** inspected stock calls). The real magic numbers are **55 bare `6`/`7` `storeId` literals** (`REJECTION STORE`/`REWORK STORE`) → **R-66**. **R-10 was `Confirmed` from a signature, not a call site**, and this task was sized at 2 days on that. Full account: **INV-044**, tracker footnote ³¹, `failure-log.md`. |
| **Stop reason (after `M0-01-03`)** | **The merge queue, now seven branches deep.** `M0-01-03` was run to the limit of what a session can honestly do and closed `Needs Review`; what remains on it is a **named operator** and runbook **§7**, neither of which is a technical blocker. Nothing else is selectable. **Correction worth carrying:** the original §8 item 5 stop on `M0-01-03` rested on the *task file's* claim that no SQL Server was reachable and no credential existed. Both were false — `MSSQL$SQLEXPRESS` was running and reachable by **Windows integrated auth**, so no credential was ever needed. Footnote ²¹ recorded that on 2026-08-19; the task file was never updated, and the runner stopped on the stale premise. **Running it produced R-65**, a silent-lockout defect in `ScreenCatalogue.cs` that blocks `M2-A02` and would not have been found any other way. |
| **Stop reason (initial, at Select)** | **No task could be done autonomously, and the reason was the merge queue rather than execution capacity.** Five branches are validated `PASS` and unmerged, so none of their dependents is released. Of what remains: `M0-01-03` (P0, the rank winner) needs the rebuild **drill executed**, and its own step 7 hands that to a human — KB-091 §8 item 5. The environment half of that block **is now false** (tracker footnote ²¹: `MSSQL$SQLEXPRESS` is running, reachable by Windows integrated auth, `sqlcmd` and the `SqlServer` module both present — re-verified this session), so what actually remains unavailable is a **named operator** to sign the drill log and the **UI smoke test** (start the Blazor host, log in, run one report, print one document through `Sp_Print_CompanyDetails`). `M2-B09` is the only other unclaimed `Ready` task and it is dropped at selection step 2: it shares **`V.SMART.Api/Program.cs` and `Controllers/CurrencyController.cs`** with `M2-B01`, which is live in `wt-M2-B01`. Nothing here is a defect; it is a queue that needs the owner. |
| **Run started** | 2026-08-19 (spans the 2026-08-19→2026-08-21 autonomous run through `M2-B02`, `M2-A01-02`, `M2-A01-03`, `M2-B12-01`, `M2-A08`, `M2-B04`, and now into `M0-01-03`). |
| **Last transition** | 2026-08-21 — **the owner instructed "merge the queue" and eight branches were merged to `master`.** Each was merged `--no-ff`, one at a time, with a build after the code-bearing ones; a safety tag `pre-merge-queue-2026-08-21` marks `master` at `52060dc` before the first. `Program.cs` was the expected collision and it resolved cleanly: `M2-A08`'s changes auto-merged, and `M2-B09`'s conflicted with `M2-B11`'s only as two additions at one point — both kept, with `UseOutputCache()` landing after `UseAuthentication`/`UseAuthorization` exactly as M2-B09's own comment requires. **Four cross-branch id collisions were found and resolved at merge, which is the fifth occurrence of the failure mode the *id allocation* process note predicts:** footnote ²⁸ was claimed three times (`M2-B04` kept it; `M2-A07` → ³⁷, `M2-C00` → ³⁸, `M2-A08` → ³⁹); **`Q-37`** twice (`M2-A07` kept it, `M2-C00` → **Q-38**); **`INV-042`** twice (`M2-A07` kept it, `M2-B04` → **INV-047**, with `tasks/M2-B04.md`'s own cross-reference corrected); and the `KB-100 +` next-free pointer disagreed on both sides (`KB-108` `M2-A08` and `KB-113` `M2-B11` both recorded, next free now **KB-109**). Two documents recorded *different* deliveries at the same line and taking either side alone would have erased a true record — `api-readiness-assessment.md` rows B5/B6 and C1/C2 were merged so all four deliveries survive. Before this: `M2-B11` closed `Needs Review` after 2 attempts, independently validated `PASS`. |
| **Current task** | **None dispatched. `M0-04` is the expected pick** — P0, `Security`, `depends_on: []`, no banner, and no `migration/*` sibling since the archive rename. Its deliverables are four documents: `docs/runbooks/credential-rotation.md`, a credential inventory (C-1…C-6, each Confirmed/Inferred/Unknown with `file:line` re-derived against **current** `master`), a human verification checklist, and Q-19 (already answered). **It must close `Blocked`, not `Completed`** — the rotation itself needs production SQL / GST gateway access nobody here has. **Excluded:** `M0-06` (branch exists), `M0-11` (`Product Decision`). |
| **Current task (previous, superseded)** | **None dispatched.** **`M2-B10`** (OpenAPI polish + generated TypeScript client in CI, `DevOps`, P0) is the only selectable task: `depends_on: [M2-B03]`, now `Completed` and merged; no banner; no sibling branch. It carries **G2 criterion 4**, the last of the three that `Q-28`/`R-65` released. **It inherits a known problem and should read [KB-114](../api/controller-conventions.md) §13 first:** `CurrencyController` declares `[ProducesResponseType]` on `GetAll` only and `AuthController.Login` declares none, so the OpenAPI document it generates from currently has **no 404 for `GET /currencies/{id}` and no 409 for `DELETE`** — a generated client inherits exactly those gaps. Whether `M2-B10` fixes the annotations or records them is its call to make explicitly, not to discover late. **Excluded:** `M0-06` (branch exists), `M0-11` (`Product Decision`, owner-only). |
| **Current task (previous, superseded)** | **`M2-A02` — apply the authorization filter to `CurrencyController` + denial tests.** Selected 2026-08-24 (tip `762bb1d`), then dispatched, implemented, independently validated `PASS`, and closed `Needs Review` this session. See Stop reason (after `M2-A02`) above. |
| **Current phase** | **Record/Complete** (session close-out; `M2-B10` independently validated `PASS`, `Needs Review`, left unmerged for owner review — no other task selectable). Previous: **Select** (KB-088 §7) — `M2-A03` selected this pass, Investigate/Implement/Validate not yet started. Previous: `M2-A02`, `M2-A09`, `M2-A10` all closed `Needs Review` in prior sessions and were merged to `master` (`a7bbc34`) by the repository owner between sessions, releasing `M2-A03` and `M2-B03`. Before that: `M2-A09` selected this pass, Investigate/Implement/Validate not yet started. Previous: `M2-A02` closed this pass at **Record/Complete** (session close-out; independently validated `PASS`, `Needs Review`, left unmerged for owner review). Before that: `M2-C13` closed a prior session-close-out pass: independently validated `PASS`, `Needs Review`, left unmerged for owner review. `M2-C04-03` closed 2026-08-24 at **Record/Complete** (session close-out; independently validated `PASS`, `Needs Review`, then merged to `master` and closed `Completed`). **Previous:** `M2-C10` dispatched by an intervening session, attempt 1 `FAIL` (category `environment`), closed **Blocked**, owner Vivek. `M2-C04-02` closed 2026-08-23 at **Record/Complete** (session close-out; independently validated `PASS`, `Needs Review`, awaiting owner review and merge). Before that: `M2-C04-01` closed 2026-08-23 at **Escalate/Record** (KB-091 §8 item 5 safety stop) then, after the owner ruled, at **Record/Complete** — Completed and merged (`4d4b0c3`, `a4150e0`), R-45 resolved. Before that: `M2-C12-05` closed 2026-08-22 at **Record/Complete** (session close-out; independently validated `PASS`, `Needs Review`, awaiting owner review). `M2-C12-02` closed 2026-08-22 at **Record/Complete** (session close-out; `Needs Review`, awaiting owner review); `M2-C12-01` closed 2026-08-22 at **Escalate** (KB-091 §6.3) — 3 attempts, 1 of 1 escalations spent, `Blocked`, awaiting owner ruling on Q-70. `M2-C12-03`/`M2-C12-04` have since closed clean (`PASS`/`PASS`) and merged — see Status above. |
| **Current agent** | None — `M2-A03` selected, not dispatched. Previous: None — session close-out pass, nothing dispatched yet. `M2-A09` (selected, not dispatched): will route per Current model below. Previous: `M2-A02` (closed): implementer/validator `opus` (per Current model below), independently validated `PASS` on attempt 1 of 3. Before that: `M2-C13` (closed): implementer/validator model per Current model below, `PASS` on attempt 1 of 3. `M2-C04-03` (closed): implementer `opus` (attempt 1, then diagnosis pass), independent validator `opus` (attempt 1 `FAIL`, attempt 2 `PASS`). `M2-C10` (closed, Blocked): implementer `sonnet` (attempt 1, `FAIL` category `environment`, no independent validator dispatched — closed at Investigate/Escalate before a validate pass). `M2-C04-02` (closed): implementer `opus`, independent validator `opus` (3 dispatches — attempt 1, attempt 2, attempt 3/final `PASS`). |
| **Current model** | **`M2-A03`: classified from KB-091 §4 (no explicit `complexity`/`risk` override in `tasks/M2-A03.md` frontmatter).** `task_type: Testing` → base MEDIUM (§4.1); raised to HIGH by `estimate: 3 d`, non-empty `business_rules: [BR-AUTH-002]`, `source_files` spanning `V.SMART.Api` and `V.SMART.Shared`, and the task touching authorization (§4.2 — any one raise is enough to reach the HIGH ceiling from MEDIUM). Risk HIGH per §4.3 (`business_rules` populated). Per §5.1 HIGH-complexity routing plus the §5.2 item 2 risk-HIGH floor: Investigate `opus`, Implement `opus`, Validate `opus`. Not yet dispatched. Previous: **`M2-A09`: classified from KB-091 §4 (task file frontmatter carries explicit `complexity: LOW`, `risk: LOW`).** Explicit frontmatter wins over derivation — even though `task_type: Security` would otherwise default risk to HIGH per §4.3, the task file's own LOW risk is accepted as a considered override. Per §5.1 LOW-complexity routing: Investigate `haiku`, Implement `sonnet`, Validate `sonnet` (§5.2 item 3: `haiku` never writes `.cs`; §5.2 item 1: validation never `haiku`). No HIGH-risk floor applies (risk is LOW, so §5.2 item 2 does not trigger). Escalate to `opus` for both diagnosis and implementation if a retry is needed after `FAIL` (§5.2 item 4). Not yet dispatched. Previous: **`M2-A02`: classified from KB-091 §4 (task file frontmatter carries no explicit `complexity`/`risk` override).** `task_type: Security` → base complexity **HIGH** (§4.1, already the ceiling). `business_rules: [BR-AUTH-002]` non-empty and the task is explicitly an authorization change (§4.2 raise conditions apply but complexity has no level above HIGH). Risk **HIGH** per §4.3 (Security type; `business_rules` populated; changes behaviour a live client — the Blazor parity anchor — can observe once every caller without Currency rights starts getting `403`). Per §5.1 HIGH-complexity routing plus the §5.2 risk-HIGH floor: Investigate `opus`, Implement `opus`, Validate `opus`. Dispatched, independently validated `PASS` on attempt 1 of 3 — no diagnose-escalation needed. Previous: **`M2-C13`: classification taken directly from the task file's own frontmatter** — `complexity: MEDIUM`, `risk: MEDIUM` (explicit in frontmatter wins over derivation, KB-091 §4). Per KB-091 §5.1 MEDIUM-complexity routing: Investigate `sonnet`, Implement `sonnet`, Validate `sonnet` (Diagnose-first `sonnet`, Diagnose-escalated `opus` if a retry is needed after a `FAIL`). No HIGH-risk floor applies (risk is MEDIUM, not HIGH). Closed `PASS` on attempt 1 of 3 — no diagnose-escalation needed. Previously: `M2-C04-03` (closed): classified `task_type: Frontend` → base MEDIUM, raised by `estimate: 3 d` (meets the "3 d or more" threshold, KB-091 §4.2) → complexity **HIGH**; no other §4.2 raise applies (`depends_on` names 1 task, below the 3-task threshold; `business_rules: [BR-SO-003]` is non-empty but the task file states outright that BR-SO-003 is *not implemented in the client* — no calculation/permission/numbering logic lands in this task; `source_files` are five pre-existing Blazor reference files in one project (`V.SMART.Shared`), read-only). Risk MEDIUM — default; no HIGH trigger (no schema/secrets/`Program.cs`/`appsettings*`, not Security/Product-Decision type, builds isolated Angular feedback components with no live-Blazor-observable behaviour change). Per KB-091 §5.1 HIGH-complexity routing: Implement `opus`, Validate `opus` (diagnosis pass, if needed, also `opus`) — used on both attempts. Previously: `M2-C10`: classified `task_type: Frontend` → base MEDIUM, raised by `estimate: 2 d` — below the 3 d raise threshold, so **no raise applies** on estimate; `depends_on` names 1 task; `business_rules: [BR-CALC-001, BR-STK-001]` is non-empty but the task implements no calculation itself (it is the parsing/formatting primitive `M2-C04-02`'s numeric controls already consume behind a port); `source_files` span three read-only Blazor/domain files in one project. Complexity **MEDIUM** (no §4.2 raise clears the threshold). Risk MEDIUM — default. Per KB-091 §5.1 MEDIUM-complexity routing: Implement `sonnet`, Validate `sonnet` (escalate to `opus` if a diagnosis pass is needed after a `FAIL`) — used on its one dispatched attempt, which failed `environment`, not code. Before that: `M2-C04-02`: classified `task_type: Frontend` → base MEDIUM, raised by `estimate: 4 d` (≥3 d) → complexity HIGH; risk MEDIUM. Per KB-091 §5.1 HIGH-complexity routing: Implement `opus`, Validate `opus`. Before that: `M2-C04-01` classified `task_type: Frontend` → base MEDIUM, raised by `estimate: 3 d` → complexity HIGH; risk MEDIUM. |
| **Attempt** | `M2-A09`: **attempt 0, not yet dispatched.** Selected this pass; Investigate/Implement have not started. Previous: `M2-A02`: **1 of 3 used, 0 escalations**, independently validated `PASS`, `scopeOk: true`, `failureCategory: none`. Closed `Needs Review`, unmerged, left for owner review. Before that: `M2-C13`: **attempt 0, not yet dispatched (at selection; later dispatched and closed `PASS` on attempt 1 of 3 — see Status above).** Selected that pass; Investigate/Implement have not started. `M2-C04-03`: **2 of 4 used, 0 escalations.** Attempt 1 implemented, independently validated `FAIL` (`acceptance-criterion`, focus-trap/restore/scroll-lock matrix 8/12, confirm dialog missing focus restoration entirely); diagnosed and fixed (`56b4c1d`). Attempt 2 **independently validated `PASS`**, `scopeOk: true`, `failureCategory: none`, all 20 acceptance criteria `MET`. Closed `Needs Review`, unmerged. `M2-C10` (closed, Blocked): **1 of 3 used, 0 escalations.** Attempt 1 `FAIL`, category `environment` — its binding criterion needs a MEASURED wire format from a live `[Authorize]`d endpoint, and `ConnectionStrings:MasterDb`/`Jwt:Secret` are both empty on this workstation; not a code defect, so not retried. Closed `Blocked`, owner Vivek. `M2-C04-02` (closed): **3 of 4 used, 0 escalations.** Attempt 1 implemented, independently validated `FAIL` (nine controls routed `readonly` into PrimeNG's `[disabled]`, dropping values out of the tab order); diagnosed and fixed (`802af10`). Attempt 2 independently validated `FAIL` (`app-file-upload` missing its loading row, incomplete empty/loading/error triad); diagnosed and fixed (`b6b3738`). Attempt 3 **independently validated `PASS`**, `scopeOk: true`, `failureCategory: none`, all 17 acceptance criteria `MET`. Closed `Needs Review`, unmerged. `M2-C04-01`: **1 of 3 used, 0 escalations** (task closed `Completed`/merged after the owner ruling — see Status/Current phase). Attempt 1 implemented, independently validated `FAIL` (`acceptance-criterion`, 14/16 met); one diagnosis pass fixed the lint-gap failure (commit `e16693a`) but the `format:check`/R-45 failure needed owner authorisation outside the task's file list, so the session closed `Blocked` rather than spending attempt 2 on a fix it cannot make. `M2-C12-05` (closed): **1 of 5 used, 0 escalations**, independently validated `PASS`, `Needs Review`. `M2-C12-02` (closed): **1 of 4 used, 0 escalations**, independently validated `PASS`, `Needs Review`. `M2-C12-01`: **3 of 4 used, 1 of 1 escalations spent** — attempt 1 `FAIL` (criterion 2 evidence missing), attempt 2 `FAIL` (criterion 7 unmet, plus a weakened axe criterion caught and restored in attempt 3), attempt 3 `FAIL` (criterion 7 still unmet — architecturally unsatisfiable alongside criterion 2). Escalated rather than a 4th attempt; status **`Blocked`**, owner **Vivek**, question **Q-70**. `M2-C01` (closed): **1 of 3** used, 0 escalations, independently validated `PASS`, `Needs Review`. `M2-B11` (closed): **2 of 4** used, 0 escalations. Attempt 1 `FAIL` (`implementation-error`, CI warning ratchet), attempt 2 `PASS` after an annotation-only fix. **Independently validated** — the validator re-ran every acceptance criterion and every command itself rather than trusting the implementer's report. `M2-B06` (closed): **1 of 3** used, 0 escalations, closed `Needs Review` with two acceptance criteria openly unmet. **No independent validator was dispatched** — the verification below was run directly in the session, which is a weaker guarantee than an independent re-derivation and is recorded as such. `M0-01-03`: **1 of 3** used, 0 escalations, no validator dispatched, `Needs Review`. `M2-B04` (closed): **2 of 3** used, 0 escalations, verdict `PASS`, `Needs Review`. `M2-B12-01`: **2 of 3** used, **1 of 1 escalations spent**, verdict `FAIL` at `fa4a2ad`, status **`Blocked`** — *not* `PASS`. `M2-A08`: **1 of 3** used, 0 escalations, `PASS`, `Needs Review`. |
| **Escalations** | 1 — `M2-C12-01`, 2026-08-22, KB-091 §6.3, reason: the task's own acceptance criteria (2 and 7) contradict each other; resolution is a specification decision, not a patch. |
| **Last validation** | `M2-A02` — **independently validated, `PASS`, attempt 1 of 3, `scopeOk: true`, `failureCategory: none`.** All 18 acceptance criteria re-checked `MET` against branch tip `634d30c` (two flagged, not failed, as literally unsatisfiable against current `master`, not as defects — see the task file's own Execution Record and tracker footnote ⁵⁹ for the full itemised list). `dotnet build V.SMART.Api` **0 errors / 2 warnings** (both pre-existing NU1608); `dotnet test tests/V.SMART.Api.Tests` **357/357**; the new `CurrencyAuthorizationTests` filter alone **45/45**; `dotnet test tests/V.SMART.Shared.Tests` **90/91** (1 pre-existing unrelated skip). `git diff master...HEAD --stat`: 8 files, 631 insertions / 32 deletions — `CurrencyController.cs` (usings + attributes + a provenance comment only), 6 `docs/kb/` files, one new 471-line test file. Screen-string safety check re-verified by direct inspection: `"Currency"` byte-identical across `ApplicationDbContext.cs:1155`, `CurrencyList.razor:252`, `CurrencyUpsert.razor:135`, `ScreenCatalogue.cs:37`; distinct `"Currency Today"` at `:1171`/`:53` not conflated. `ScreenRightStartupValidator.cs:75-104` re-read to confirm the newly annotated controller cannot break host startup. No regressions: pre-existing `CurrencyControllerErrorContractTests` pass unmodified; every other API controller checked for annotation state (`CurrencyExcelController`, `FilesController` annotated; `MeController`/`ReferenceController` `[NoScreenRight]`; `AuthController` `[AllowAnonymous]`) so no sibling endpoint is left as an unnoticed bypass; no `V.SMART.Shared`/`V.SMART.Web`/`V.SMART` change; no EF migration. What failed: nothing — the two residual limits (anonymous-401 and 403-`application/problem+json` proved at policy/`ObjectResult` level, not over the wire) are forced by the task's own "must not edit `Authorization/**` or `Program.cs`" constraint, not a shortfall; end-to-end proof is explicitly `M2-A03`'s. Full record: `tasks/M2-A02.md` § Execution Record (2026-08-24) and § Execution Record — close-out; `task-tracker.md` footnote ⁵⁹. Previous validation: `M2-C04-03` — **independently validated, `PASS`, attempt 2 of 4, `scopeOk: true`, `failureCategory: none`.** All 20 acceptance criteria re-derived `MET` against branch tip `1806bca`: all 14 components exported from `shared/components/index.ts`, each over the named PrimeNG surface and no other library (`no-restricted-imports` passes); the focus-trap/restore/`Esc`/scroll-lock matrix now 12/12 (`modal.component.spec.ts:42,50,54,67,81`; `drawer.component.spec.ts:59,64,72,79,88`; `confirm-dialog.component.spec.ts:84-92,150-163,165-178,180-190`), non-vacuity proven by an independent mutation probe (reverting `confirm-dialog.component.{ts,html}` to the pre-fix commit reproduced `1 failed | 303 passed`, then restored to `304 passed`); `app-confirm-dialog`'s reason field disables Confirm until non-empty after trim (`confirm-dialog.component.ts:74`); no component implements BR-SO-003 itself (grepped for `HttpClient`/`fetch`/`XMLHttpRequest`/`decimal.js` — none found); error toasts sticky, success toasts auto-dismiss at 4 s; `app-error-state` requires `message` with no default (`@ts-expect-error` test, `tsc --noEmit` exit 0) and a repository-wide grep for `"An error occurred"` returns nothing; `app-empty-state`'s two variants distinct by test; `app-permission-denied-state` imports nothing from `core/auth/**` (grepped, zero hits); skeleton + table/form presets exist, no bare spinner for first load; `app-busy-overlay` region-scoped by default; tooltip opens on focus, context menu keyboard-operable; `prefers-reduced-motion` asserted at the stylesheet level (jsdom limitation stated, not glossed); `axe` zero critical violations across both directories in both themes (12 tests); every component `OnPush` with an `app-`-prefixed selector, `npm run lint --max-warnings=0` exit 0 including template-a11y rules; `MessageService` touched only by `feedback/toast.service.ts` (6 hits, all there); no `Underconstruction` equivalent; no raw colour/spacing literal (spacing enforced by ESLint on `.ts`/`.html`, by `no-raw-colour.spec.ts` on `.scss`); all five `npm` gates pass — `typecheck` exit 0, `lint` exit 0 "All files pass linting.", `format:check` exit 0, `test:ci` exit 0 (304 passed / 46 files), `build` exit 0 (711.75 kB raw / 158.28 kB gzip, one non-fatal **R-69** budget warning). No regression: `git diff --name-status master...HEAD` (77 files) touches no `V.SMART/**`, no schema, no `core/theme/**`/`tokens.css`/`form/**`/`core/auth/**`/`frontend/vsmart-erp/**`. One item flagged, not a criterion failure: `confirm-dialog.component.html:23`'s `[maxlength]="500"` has no `file:line` rule behind it — recorded as **Q-80**. Two items openly outstanding, neither a retry item: the human keyboard-only/screen-reader pass (Completion Condition, not automatable) and this tracker row/`current-task.md` (orchestrator-owned, corrected at this close-out). Full record: `tasks/M2-C04-03.md` § Execution Record (2026-08-24) — session close-out; `failure-log.md`; `task-tracker.md` footnote ⁵³. Previous validation: `M2-C04-02` — **independently validated, `PASS`, attempt 3 of 4, `scopeOk: true`, `failureCategory: none`.** All 17 acceptance criteria re-derived `MET` directly against branch tip `2eb7d8e`: the 14-input + 3-layout-component surface exported through `shared/components/index.ts`; exactly one validation-display mechanism (`app-form-field`) with no other error markup found in the directory; labels above fields, `*`/`aria-required`, and readonly proven distinct from disabled by `readonly.spec.ts` (12 tests) plus a negative control that reproduces the attempt-1 failure when the old binding is restored; create-mode autofocus asserted in `form-layout.component.spec.ts`; every control's *documented* keyboard model asserted by `userEvent`, with three keys (radio-group arrows, the date-picker calendar grid, masked `p-inputnumber` typing) named as genuine jsdom/PrimeNG automation limits and independently re-measured in `node_modules/primeng/fesm2022/primeng-datepicker.mjs` rather than accepted; `axe` zero critical violations across all 17 controls in both themes plus an all-fields-in-error case; the date picker accepts typed input, calendar never the only path; select/multi-select/combobox/file-upload all render the empty/loading/error triad (file-upload's loading row was the attempt-2 fix); every control `OnPush` and a `ControlValueAccessor` over the one named PrimeNG surface, no second component library (`no-restricted-imports` lint passes); the three numeric controls hold branded `Money`/`Qty` opaque types with zero `parseFloat`/`toFixed`/`Math.round` outside prose/assertions (`git grep` re-run); decimal places sourced from an injected precision policy traceable to `Companydetails.cs:208` (re-read), never hardcoded; `amount-or-percent-input` emits `{ value, isAmount }` with no arithmetic, matching `DebitNote.cs:95,109,117,146` (re-read) and the confirmed polarity in `CalculationService.cs:29-31` (re-read); no party cascade, duplicate-line check or quantity-balance rule in any control; no permission input anywhere in the directory; `render-count.spec.ts` proves zero sibling re-checks while typing, with two negative controls proving the probe actually works; no hand-written validator schema for a real entity (`fake-decimal-port.ts` is an unexported fixture); no raw colour/spacing literal (`git grep` for hex literals returns no matches); all five `npm` gates pass. Two items openly `NOT MET`/outstanding but neither a criterion nor a retry item: the human keyboard-only pass (Completion Condition, not automatable) and the tracker row itself (orchestrator-owned, corrected at this close-out). No regression: `git diff --stat master...HEAD -- V.SMART frontend/vsmart-erp` empty; all 8 pre-existing `M2-C01`/`M2-C04-01` specs still pass inside the 29-file/215-test total. Full record: `tasks/M2-C04-02.md` § Execution Record (2026-08-23) — session close-out; `failure-log.md`; `task-tracker.md` footnote ⁵¹. Previous validation: `M2-C04-01` — **independently validated, `FAIL`, attempt 1 of 3, category `acceptance-criterion`.** 14 of 16 acceptance criteria re-derived `MET` directly, including a from-scratch WCAG contrast recomputation over 110 token pairs (0 failures) not taken on trust from `contrast.spec.ts`. Two `NOT MET`: `npm run lint` did not fail on a raw hex literal in an external `.html` template (the ban was registered only on the `**/*.ts` ESLint block); and `npm run format:check` printed `Code style issues found in 27 files` (all pre-existing scaffold, CRLF-only, `git config core.autocrlf=true` + no `endOfLine` override in `.prettierrc`). No regression: `git diff --name-only master HEAD | grep -c '^V.SMART'` → 0, `UserThemePreference.cs:20` unchanged, Blazor untouched. Diagnosis pass fixed the lint gap (commit `e16693a`, one file already on the task's *Files Expected to Change*; re-verified with four probes plus `typecheck`/`test:ci`/`build` all still passing) and confirmed the `format:check` root cause independently rather than accepting it, but left it unfixed — the only real fixes touch `.prettierrc`/`.gitattributes`/`core.autocrlf`, none authorised for this task. Full record: `tasks/M2-C04-01.md` § Execution Record (2026-08-23); `failure-log.md` (`M2-C04-01 · attempt 1 · independent validation · 2026-08-23 · FAIL` and the diagnosis entry immediately after it); `task-tracker.md` footnote ⁴⁹. Previous validation: `M2-C12-05` — **independently validated, `PASS`, attempt 1 of 5.** All 8 acceptance criteria and the Testing Requirement re-checked directly against tip `1e940bd`: AC1 no ⛔ banner survives anywhere in `tasks/` (whole-directory grep, not just the batch — zero hits); AC2 atomicity rule holds in all six files (per-file grep empty; a separate `\breact\b` sweep found 17 hits, all either the literal KB-050 path or explicitly historical); AC3 re-specification note present once per file, matching `M2-C01.md:28`'s shape; AC4 all `frontend/` build-target paths are `frontend/nexgen-web/…`, the ten remaining `frontend/vsmart-erp/` occurrences individually read and confirmed read-only prior-art/must-not-change clauses, and all 12 quoted commands verified against KB-083's tables; AC5 `depends_on`/`business_rules`/`priority`/`estimate`/`Gate` byte-unchanged in all six (diff loop `SAME`); AC6 `V.SMART/` citations byte-unchanged in five of six, `M2-D02-01`'s five removed citations traced to the deleted Fresh-Session Execution Prompt block with every fact confirmed to survive elsewhere; AC7 `git diff --name-only master...HEAD | grep -v '^docs/kb/'` empty; AC8 KB-090's 13 required sections present in all six. `dotnet build V.SMART.Api` re-run as a regression check, 0 errors / 6695 warnings, exact `KB-086` baseline. `dependency-graph.md`'s "no edit needed" claim independently re-derived (one framework-neutral hit across 311 lines). No regressions, no scope violations, no new open question. Two cosmetic imprecisions in the Execution Record's own line/hit counts, and one pre-existing citation-staleness finding (`ApplicationDbContext.cs:1156`, `CurrencyController.cs` line count — drift from later unrelated merges, not this batch's defect), both recorded and neither affecting a criterion. Full record: `tasks/M2-C12-05.md` § Execution Record (2026-08-22) — Close-out; `task-tracker.md` footnote ⁴⁶. Previous validation: `M2-C12-02` — **independently validated, `PASS`, attempt 1 of 4.** All 8 acceptance criteria `MET`: AC1 no ⛔ banner survives in the batch (17 unrelated files still carry it, confirmed by re-run); AC2 atomicity rule holds, per-file grep output quoted and reproduces the Execution Record byte-for-byte (all hits are the substring `vite` inside **Vitest**, or no output for `M2-C11`); AC3 re-specification note present (`M2-C02.md:35`, `M2-C03.md:31`, `M2-C10.md:25` — the `M2-C01` shape; `M2-C11.md:38` deliberately reads *Visited*, not *Re-specified*); AC4 `frontend/` paths and quoted commands verified against KB-083, with `frontend/vsmart-erp/` occurrences individually inspected and found to be "must not change"/citation uses only; AC5 `depends_on`/`business_rules`/`priority`/`estimate`/`Gate` byte-unchanged in all four files (diff loop reproduced, `BYTE-IDENTICAL`); AC6 `V.SMART/` citations byte-unchanged except two losses inside the deleted Fresh-Session Execution Prompt block, both individually justified and the underlying facts confirmed to survive elsewhere in the same files; AC7 diff confined to `docs/kb/`, footprint matches exactly the declared six files (independently re-run this close-out session, matches); AC8 KB-090 required sections present in all four (three renamed/pruned per the merged `M2-C12-01` precedent, `M2-C11` regenerated to the full template as `task-template.md:207-209` permits). No regressions: nothing under `V.SMART/`, `frontend/`, `tests/` or `.github/` touched; `Q-38` correctly left unanswered, not inferred. Full record: `tasks/M2-C12-02.md` § Execution Record (2026-08-22) and § Execution Record (2026-08-22) — session close-out; `task-tracker.md` row. Previous validation: `M2-C12-01` — **independently validated, `FAIL`, attempt 3 of 4, category `architecture`.** Re-ran every acceptance criterion directly: AC1 MET (no ⛔ banner survives in the batch, 21 unrelated files still carry it); AC2 MET (per-file grep output reproduces the Execution Record byte-for-byte, no live wrong-stack instruction); AC3 MET (re-specification note present in all four files); AC4 MET with two disclosed literal deviations (three `frontend/vsmart-erp/` paths inside "Files That Must Not Change", factually correct per `git ls-files`); AC5 MET (`depends_on`/`business_rules`/`priority`/`estimate`/`Gate` byte-unchanged); AC6 MET (`file:line` citations into `V.SMART/` byte-unchanged); **AC7 NOT MET** — `git diff --name-only master...HEAD` printed 7 paths, not 4 (`failure-log.md`, the four `M2-C04*` files, `M2-C12-01.md` itself, `open-questions.md`); AC8 MET (KB-090 required sections present, only additions/renames per ADR-007). No regressions: `git diff --stat master...HEAD -- . ':(exclude)docs'` empty — no `V.SMART/`, no `frontend/`, no schema, no TypeScript. Full verdict and evidence: `tasks/M2-C12-01.md` § Execution Record, `failure-log.md` (`M2-C12-01 · attempt 3 · independent validation · 2026-08-22 · FAIL (architecture)`). Previous validation: `M2-C01` (Angular re-spec) — **independently validated, `PASS`, attempt 1 of 3.** `npm ci` **exit 0**, 517 packages, 0 vulnerabilities; `npm run lint` **exit 0**, zero warnings; `npm run test:ci` **exit 0**, 6/6 passed, runner **Vitest** (not Karma); `npm run build` **exit 0**, initial gzip **104.20 kB** (KB-050's <250 kB budget); `npm run e2e` **exit 0**, 1/1 passed; `npm run format:check` and `npm run typecheck` both **exit 0**; `bash tools/check-no-build-output.sh` **exit 0**; `dotnet build V.SMART.Api --no-incremental` **0 errors / 6694 warnings** (baseline, unaffected — proves the backend untouched); `git diff --name-only` against `V.SMART/` and `frontend/vsmart-erp/` **empty**. 15 of 16 acceptance criteria `MET` outright; the 16th is a disclosed specification defect (see Stop reason). Full record: `tasks/M2-C01.md` § Execution Record (2026-08-21). Previous validation: `M2-B11` — **independently validated, `PASS`, attempt 2 of 4.** `dotnet build V.SMART.Api --no-incremental` **6694 warnings / 0 errors** (plain build — the wrong measurement for the ratchet, recorded as such); the gate measurement (`dotnet restore` → `dotnet build --no-restore --no-incremental` → `tools/compare-warnings.sh`) → **6693 warnings, measured = baseline, Gate: PASSED, exit 0**; `dotnet test tests/V.SMART.Api.Tests` **179 passed / 0 failed** (148 → 179); `dotnet test tests/V.SMART.Shared.Tests` **84 passed / 0 failed**; `V.SMART.Web` **0 errors / 6697 warnings**, its exact baseline. Runtime: `/health/live` 200 with the master DB down and no database touched; `/health/ready` 200 with master+tenant healthy, 503 naming `master-db` when it is down; a credential grep of every emitted `diagnostics-*.json` found zero hits for `Password`/`TenantInfo`/`ConnectionString`/a server or database name. `git diff -- ILoggingService.cs` empty. All 20 acceptance criteria in `tasks/M2-B11.md` re-checked directly by the validator. Two criteria openly unmet, stated not glossed: the tenant-unreachable-while-master-healthy 503 proved only at unit level (writing a bogus `Tenants` row was out of scope), and no `LogUserAction` event was observed at runtime (no call site exists in `V.SMART.Api`). Previous validation: `M2-B06` — **self-verified, not independently validated.** `dotnet build V.SMART.Api --no-incremental` **0 errors / 6694 warnings** (the exact M2-B04-verified baseline); `dotnet build V.SMART.Web --no-incremental` **0 / 6697** (its exact baseline — the Razor call-site adaptation adds no warning); `dotnet test tests/V.SMART.Api.Tests` **148 passed / 0 failed** (117 → 148, the growth being this task's file-endpoint suite); `dotnet test tests/V.SMART.Shared.Tests` **84 passed / 0 failed**, no regression from the `CompanyService` signature change. **All seven required negative tests pass and are reported individually** — traversal, cross-tenant indistinguishability, 413, disallowed extension, unknown id, 401, 403 — with N6/N7 proved at the **policy level** (attributes present; `ScreenRightAuthorizationFilterTests` proves the filter denies) rather than over the wire. **Round trip passes byte-identical**, which is the assertion `WebFileUploadService` would fail. Scope verified by diff: `WebFileUploadService.cs`, `WebFileOpener.cs`, `MauiFileUploadService.cs`, `DesktopFileOpener.cs`, `ExcelExportService.cs`, `ExcelTemplateService.cs`, `IExcelTemplateService.cs`, `IFileOpener.cs`, `IFileUploadService.cs` and `Migrations/**` are all byte-unchanged. Previous validation: `M2-B04`, tip `5ca1c10` — validator verdict **`PASS`**, `failureCategory: none`, `scopeOk: true`, attempt 2 of 3. |
| **Tasks processed this run** | **`M2-C04-03` — modal, drawer, toast, empty/loading/error states; 2 attempts, 0 escalations, independently validated `PASS` on attempt 2 (`scopeOk: true`, `failureCategory: none`); `Needs Review`, unmerged (branch `migration/M2-C04-03-feedback-primitives`, tip `1806bca`).** `M2-C04-02` — form controls + validation display; 3 attempts, 0 escalations, independently validated `PASS` on attempt 3 (`scopeOk: true`, `failureCategory: none`); `Needs Review`, unmerged (branch `migration/M2-C04-02-form-controls`, tip `2eb7d8e`).** `M2-C04-01` (Angular) — design tokens, theme, light/dark; 1 attempt, 0 escalations, independently validated `FAIL` (`acceptance-criterion`, 14/16 met); diagnosis fixed the lint-gap failure, left the `format:check`/R-45 failure blocked on the owner; `Blocked`, unmerged (branch `migration/M2-C04-01-design-tokens-angular`, tip `e16693a`).** `M2-C12-05` — re-spec the M2-D tree (six files) for Angular, plus the whole-tree tracker/dependency-graph restatement; 1 attempt, 0 escalations, independently validated `PASS`, `Needs Review`, unmerged (branch `migration/M2-C12-05-respec`, tip `1e940bd`). This closes `M2-C12` — zero `⛔` banners remain anywhere in `tasks/`.** `M2-C12-02` — re-spec auth, app shell, decimal handling, pilot-adoption question for Angular; 1 attempt, 0 escalations, independently validated `PASS`, `Needs Review`, unmerged (branch `migration/M2-C12-02-respec`, tip `eaa2337`). `M2-C11` deliberately left `Blocked` on Q-38, not re-specified. Everything below is prior history, unchanged by this close-out: current run (started 2026-08-19, spans to 2026-08-21): `M0-12-01`, `M0-13`, `M0-12-02`, `M0-09`, `M2-A01-01`, `M2-B07` — all `Completed` and merged (the React `M2-C01`, merged `12f172f` 2026-08-19, was later superseded by ADR-007 and re-specified for Angular — see below). `M2-C04-01` — implemented, validated `PASS`, `Completed` and merged. `M2-A06` — implemented, validated `PASS`, owner-merged and `Completed`. `M2-B02` — implemented, validated `PASS`, `Completed` and merged, released `M2-B09`. `M2-A01-02` — implemented, validated `PASS`, `Completed` and merged, released `M2-A01-03`. `M2-A01-03` — implemented across 2 attempts, validated `PASS`, `Completed` and merged, released `M2-A02`/`M2-A07`/`M2-A08`. `M2-B12-01` — 2 attempts, **`Blocked`**, escalation budget exhausted (unmerged). `M2-A08` — implemented, validated `PASS`, `Needs Review` (unmerged). `M2-B04` — attempt 1 stopped with no implementer result, attempt 2 validated `PASS`, `Needs Review` (unmerged). `M0-01-03` — runbook §§2–6 executed and passing, `Needs Review` (unmerged), §7 and a named operator outstanding. `M2-B09` — implemented, `Needs Review` (unmerged). **`M2-B06` — API half adopted from a killed run and verified, second half implemented this session, `Needs Review` (unmerged).** `M2-B05` — selected, then **`Blocked`**: premise falsified, awaiting owner re-specification. **`M2-B11` — health checks + Serilog-shaped structured logging, 2 attempts (1 `FAIL` on the CI warning ratchet, fixed, 1 `PASS`), independently validated, `Needs Review` (unmerged).** **`M2-C01` (Angular re-spec) — Angular 22 workspace replacing the React tree, 1 attempt, independently validated `PASS`, `Needs Review` (unmerged).** `M2-C12-01` — re-spec the design-system tree, 3 attempts + 1 escalation, independently validated `FAIL` (architecture), **`Blocked`** on owner ruling (Q-70), unmerged. |
| **Classification** | `M2-C01` — `task_type: Frontend` (base MEDIUM per KB-091 §4.1); raised once by `estimate: 3 d` (meets the "3 d or more" threshold, §4.2) → complexity **HIGH**. `depends_on` names 2 tasks (below the 3-task raise threshold); `business_rules: []`; `source_files` is entirely within the frontend tree plus `.gitignore`/CI config, not spanning the four backend projects; does not touch auth, tenancy, numbering or calculation logic — so only the one raise applies. Risk: none of the HIGH triggers apply (no schema, no secrets/`Program.cs`/`appsettings*.json`, `business_rules` empty, no observable live-Blazor behaviour change — this stands up a new Angular tree, it does not touch Blazor) → risk **MEDIUM** (default). `M2-B06` (closed) — `task_type: Backend` (base MEDIUM), `business_rules: []`, `estimate: 1 wk` (≥3 d, raise 1), `source_files` spans `V.SMART.Shared`, `V.SMART.Web` and `V.SMART` (raise 2) → complexity **HIGH**; risk **MEDIUM**. **The risk assessment was wrong in one respect and it is worth recording why.** It read *"no observable live-Blazor behaviour change — Blazor keeps its own file-handling code"*, which is true of the endpoints but **not** of acceptance criterion 5: removing `IBrowserFile` from `ICompanyService` necessarily edits a live Razor page, and the naive edit would have converted a *"File size is too large"* toast into a thrown `IOException` (`OpenReadStream` throws when the file exceeds the limit it is handed). The hazard was caught and neutralised at the call site, but a classification that says "no observable Blazor change" about a task whose own criteria mandate a Razor edit is a classification that would not have flagged it. `M2-A01-02`, `M2-A01-03` (closed) — complexity **HIGH**, risk **HIGH**. `M2-B04` — `task_type: Backend`, `business_rules: [BR-APPR-001]`, `estimate: 1 wk`; `opus` both attempts, `PASS` on attempt 2. `M0-01-03` — `task_type: Database`, complexity **MEDIUM**, risk **HIGH** (DDL against a live instance) → `opus`. `M2-B11` — `task_type: DevOps`, `business_rules: []`, `estimate: 3 d`, `source_files` spans `V.SMART.Shared` and `V.SMART.Api` → complexity **MEDIUM**; risk **MEDIUM** (touches the middleware pipeline shared with `M2-A06`, and a credential-leak surface via `TenantInfo`, but no business logic and no schema change). |
| **Models this run** | `M2-C04-03`: complexity HIGH per routing table (§5.1) → Implement `opus`, Validate `opus`; selected, not dispatched. `M2-C10`: complexity MEDIUM → Implement `sonnet`, Validate `sonnet`; implementer dispatched for attempt 1 (`FAIL`, `environment`), no validator reached. `M2-C01`: complexity HIGH per routing table (§5.1) → Implement `opus`, Validate `opus`; both dispatched, attempt 1 `PASS`. `M2-B11`: Implement `opus`, Validate `opus` (both attempts). `M2-B06`: **no agent dispatched** — classified `opus`/`opus` (complexity HIGH) but executed directly in the session after the owner scoped it at the adopt/discard decision, so no independent validator re-derived the result. `M2-A01-02`: Implement `opus`, Validate `opus`. `M2-A01-03`: Implement `opus`, Validate `opus`. `M2-B12-01`: Implement `opus`, Validate `opus` (both attempts). `M2-A08`: Implement `opus`, Validate `opus`. `M2-B04`: Implement `opus` (attempt 1 stopped `environment`-category before reaching validate; attempt 2 same route, `PASS`), Validate `opus`. `M0-01-03`: Implement `opus`, Validate `opus`. |
| **Next ready task** | **`M2-A09` selected this pass (2026-08-24, tip `e7d80ee`).** `M2-A02` (previously selected) has since been dispatched, closed `Needs Review`, independently validated `PASS`, and left unmerged — it is no longer a pending candidate. `M2-A10` is `Ready`, same `depends_on: [M2-A01-03]`, independent of `M2-A09` (no file overlap), ranked below on priority (P1 vs P0) — the next selectable task once `M2-A09` closes. See `selection_note` for the full test. Everything below predates this pass: `M2-C04-03` closed this session (see Status above) and, being `Needs Review` rather than `Completed`+merged, releases nothing. `M2-C10` stays `Blocked` on the environment (attempt 1 `FAIL`, category `environment`, owner Vivek) and is not a candidate until a reachable DB + credential exists or its wire-format criterion is relaxed. task-tracker.md's own "Current state" section (2026-08-24) confirms no `Ready` row passes the five-part test: `M0-06` (branch exists), `M0-11` (`Product Decision`), `M2-A02` (**Q-28**/**R-65**). `M2-C05`/`M2-C05-01` are **not** released by `M2-C04-02`'s `PASS` — they need it `Completed` and merged, and it is `Needs Review`, unmerged, awaiting owner review. Everything below predates this pass and `M2-C12`'s closure and is superseded by it: **`M2-C12-03`** — re-spec the list/CRUD shell (`M2-C05.md`, `M2-C05-01.md`, `M2-C05-02.md`, `M2-C05-03.md`, `M2-C06.md`) for Angular. `Ready`, `P0`, 1 d, `depends_on: [M2-C00, M2-C01]` (both `Completed`/merged); five-part "can actually be done" test passed (see `current-task.md`). Selected, not dispatched, in this close-out session. `M2-C12-04` is an equally-selectable tied candidate, not dropped. `M2-C12-05` stays `Blocked` behind all four sub-batches by design. Everything below predates this and `M2-C12-02`'s closure and still holds otherwise: **None — `M2-C12-01` is `Blocked` on an owner ruling (Q-70), and `M2-C12-02`..`-05` carry the identical unsatisfiable criterion 7, so none is worth dispatching until Q-70 is answered** *(superseded: Q-70 was answered and `M2-C12-02` has since closed clean — see above)*. Everything below predates this closure and still holds otherwise: **None, and the reason has changed — it is no longer the merge queue, it is the specifications.** `M2-C01`'s merge cleared the last dependency blocking the frontend tree, and the tree still cannot be worked: **25 task files carry a `⛔ STOP — this specification is superseded` banner** — the whole of `M2-C02…C11` and `M2-D01…D03`. [CLAUDE.md](../../../CLAUDE.md) makes a ⛔ banner a **stop-and-report**, never a licence to infer the missing spec, so none of them is self-selectable no matter what the dependency graph says. **`M2-C00` is the precedent for the fix:** it rewrote KB-050 for Angular *and* re-specified `M2-C01` in the same change, which is exactly why `M2-C01` was selectable this run. The equivalent re-specification work for the remaining 25 does not exist as a task yet and needs the owner to authorise it. **Excluded for other reasons:** `M2-A02` — gated on unanswered **Q-28** and on **R-65**; `M2-A04` — reads `Blocked` while its only listed prerequisite `M2-A01-02` is `Completed` and merged, **blocking reason unrecorded**, needs an owner ruling; `M0-06` — `Ready` but `migration/M0-06-remove-default-admin` already exists (test part 5); `M0-11` — a `Product Decision`, owner-only; `M2-B05` — `Blocked`, premise falsified, awaiting re-specification onto **R-66**; `M2-B12-01` — `Blocked`, `FAIL`, escalation budget exhausted; `M0-01-03` — merged but `Needs Review`, awaiting a **named operator** for runbook §7. |
| **Process note — an orphaned working tree** | 🚩 **A run was killed mid-implementation and left ~979 lines of uncommitted work that no live session would claim.** At the start of 2026-08-21's second session the tree held a near-complete `M2-B06` API implementation — `FilesController.cs`, `CurrencyExcelController.cs`, two contracts, four service files, `Middleware/*` and `Program.cs` — with mtimes of 16:04–16:18 IST, minutes old, while `runner-state.md` said `RUNNING`, *attempt 0, not yet dispatched*. **How it was resolved without guessing:** `ListAgents` found two live peer Claude sessions and **both were asked directly**; both ruled themselves out with specifics (different repositories entirely). `Get-Process devenv` returned nothing, eliminating a human in Visual Studio. `git worktree list` showed the three known sibling worktrees, none on these files. That left a dead runner session as the only consistent explanation, which the 16:04 bookkeeping write corroborates. **The decision was put to the owner, not taken:** adopt, halt-and-report, discard-and-restart, or skip to `M2-B11`. The owner chose **adopt**. The adopted work was then committed **separately** (`e9b143b`) and **verified rather than trusted** — build re-run at baseline, the stream copy confirmed present, every must-not-change file confirmed byte-unchanged, the line-102 defect confirmed still in place. **Three things this should change:** (1) `git status --porcelain` runs *before* this file is believed, every run — a `RUNNING` row over a dirty tree means a killed run; (2) `ListAgents` plus a direct question is a cheap and conclusive concurrency check, and it belongs beside `git worktree list` in Select; (3) an implementer that writes files before committing anything leaves no record of its own existence — the only evidence it ran at all was filesystem mtimes. |
| **Process note — inherited status** | 🚩 **A `PASS` recorded in this file was false, and this session propagated it once before checking.** The inherited state listed `M2-B12-01` as validated `PASS` and awaiting merge. That branch's own tip is *"Record close-out — BLOCKED, escalation budget exhausted, **corrects a premature PASS**"*, and its runner-state states that the earlier `PASS` was claimed for tip `58e7bee` whose own failure-log entry recorded `FAIL` — *"no genuine `PASS` of `58e7bee` exists anywhere in this repository."* It was caught only because `git stash list` incidentally printed that commit subject. The same check then found `M2-B01` and `M0-10`, two finished branches the inherited state did not mention at all. **A status inherited from a sibling branch is a claim, not a fact.** `git log --oneline -2 <branch>` costs nothing and is now part of the Select phase, alongside `git worktree list`. |
| **Process note — concurrency** | **Three sibling worktrees were live when this run resumed** — `wt-M0-10` (`migration/M0-10-candelete-guard-audit`), `wt-M2-A08` (`migration/M2-A08-row-level-scoping`) and `wt-M2-B01` (`migration/M2-B01-api-versioning`) — none of them this session's. `git worktree list` is therefore part of selection, not a curiosity: the tracker alone cannot see them. Note also that **`M2-A08` now has two branches** (`migration/M2-A08-row-level-scoping` and `migration/M2-A08-row-scope-and-account-gates`), which is duplicated work by two sessions on one task and needs an owner decision about which to keep. **RESOLVED 2026-08-25 — all three removed** (`git worktree remove`; `git worktree prune` now reports nothing, `git worktree list` shows only the main tree). Every branch survives the removal untouched: `migration/M0-10-candelete-guard-audit` (`fc8e0c0`) and `migration/M2-B01-api-versioning` (`045a7f4`) are both already merged to `master`; the third's branch was archived the same day as `archive/M2-A08-row-level-scoping-stale-lineage` (`6e6633a`). **The `M2-A08` two-branch question above is settled** — `migration/M2-A08-row-scope-and-account-gates` is the one that merged (`380c805`) and its content was verified present on `master` before the loser was archived, so no owner decision is outstanding. `wt-M2-B01` held **uncommitted** work: a four-day-stale `STOP_REQUESTED` edit for a run that had long since ended. The state was discarded as worthless; the *finding* it carried was not — see the 🚩 note under **Requesting a stop**. **Still true, and the reason this note stays:** `git worktree list` is part of selection, because the tracker cannot see worktrees. One residue remains — `wt-docs-kb071/`, 10 generated `obj/bin` files, no `.git` and no source, left by a worktree whose tracked files were removed without its ignored build output; it is inert and untracked, not a worktree. |
| **Process note — id allocation** | **Four cross-branch id collisions have now occurred, all on 2026-08-19** — six KB/INV/Q ids, `M2-C01`'s footnote ¹⁸, and a `Q-31` double-claim caught during `M2-B07`'s merge bookkeeping (`Q-31` was already held by `M2-B07` itself; the new question became **Q-32**). Every one was caught by hand at merge, which is not a control. `grep`-before-claim cannot see a sibling branch, and it cannot see an id claimed earlier in the same session. `git branch --no-merged master` must be checked before claiming any id. This recurs until the allocation rule itself changes. |

### Status values

| Status | Means |
|---|---|
| `RUNNING` | A run is live and processing a task |
| `STOPPED` | No run is live. A clean, expected end — budget reached, or no ready task |
| `BLOCKED` | A run halted needing a human. **Stop reason and owner are mandatory** |
| `STOP_REQUESTED` | A human asked the current run to finish its task and stop. The runner checks this at the top of each task and exits cleanly |

---

## Requesting a stop

Set **Status** to `STOP_REQUESTED` and record who asked and why. The runner reads this file at
the start of every task, so the request takes effect at the next task boundary — the in-flight
task finishes its validate/record cycle rather than being abandoned half-implemented.

That is the safe stop. Killing the run mid-task is also safe for the repository — every
transition is written here before the next begins — but it leaves a task part-implemented on
its branch, which someone has to reconcile.

> 🚩 **`STOP_REQUESTED` has been clobbered every time it was used — do not trust it alone.**
> The flag was set twice on 2026-08-20/21 and a live run overwrote it at its next transition
> *both* times, continuing on through `M2-B12-01`, `M2-A08` and `M2-B04` after the first.
> The mechanism is the runner's own write-back: it rewrites this row at each transition from
> state it loaded *before* the human edit, so a request set mid-task is silently lost. The
> second attempt survived only as an **uncommitted** edit in `wt-M2-B01`, which is how it was
> recovered on 2026-08-25 — four days after the run it was trying to stop had ended.
> **After setting the flag, re-read this row to confirm it survived; if it did not, kill the
> run rather than asking again.** Recovered from `wt-M2-B01` before that worktree was removed;
> the stale state itself was discarded, this finding is the part worth keeping.

---

## Pre-run flags for `M0-15`

Both were `safetyStop` conditions ([KB-091 §8](autonomous-runner.md#8-safety-limits--the-runner-stops-and-asks))
and a run will halt on them rather than measure something unreproducible. **Both are now
resolved (2026-08-17). Kept as history — re-check them, do not assume they stay clear.**

- ✅ **Branch point — RESOLVED 2026-08-17 by re-cutting from `master`.** The branch had been cut
  from `migration/M0-08-gitignore-build-output`; this stopped the first run. It was reset to
  `master` and the three non-M0-08 commits were cherry-picked back, dropping only `e0a7092`
  (M0-08), which remains safe on its own branch. New history:
  `31cfa95` (master) → `998f7d0` → `7905c83` → `fece832`.
  Verified: `git merge-base HEAD master` → `31cfa95`, **identical to master's tip**;
  `git merge-base --is-ancestor e0a7092 HEAD` → false.
  Pre-re-cut state is preserved at tag `backup/M0-15-pre-recut-2026-08-17` (`ef861c3`).
  **Side effect:** M0-08's `Needs Review` status travelled with `e0a7092`, so KB-081 on this
  branch again lists M0-08 as `Ready`. It self-corrects when M0-08 is reviewed and merged.
- ✅ **Dirty working tree — RESOLVED 2026-08-17.**
  `V.SMART/V.SMART.Shared/Services/MultiCompanyService/TenantDbContextFactory.cs` and
  `V.SMART/V.SMART.Web/appsettings.json` were stashed as
  `PRE-M0-15: local tenant DB debugging …` (stash commit `6dbf4b47b8ff`) — local tenant-DB
  debugging work, not part of any task: null/empty-tenant guards in the factory, and a
  `MasterDb` connection string repointed at a local `.\SQLEXPRESS` / `NexGenErpDb_Master`.
  Recoverable with `git stash apply`; the stash holds full file contents, not just a diff.
  `V.SMART.Api/` remains untracked **by design** — see the untracked-directory checkout trap in
  `CLAUDE.md`; never stash or clean it.

No flag is open as of 2026-08-17, so a run may open M0-15. Re-verify both before each run —
they are cheap to check (`git merge-base HEAD master`, `git status --porcelain`) and expensive
to get wrong, since the whole point of M0-15 is a baseline someone else can reproduce.

### Process note — this file goes stale between runs, and it is the orchestrator's own doing

**Three times in the 2026-08-21→23 run the committed `Status` row was left describing a run that
had already ended**, most recently claiming `RUNNING` on `M2-C10` after that task had been
`Blocked` on the environment. The mechanism is a loop, not an oversight:

1. The runner writes its final state **after** the close-out commit, so the update lands as an
   *uncommitted* working-tree change on the task branch.
2. That write is terser than the prose it replaces, so the reviewing session discards it as a
   downgrade — correctly, on content.
3. Discarding it also discards the `RUNNING → STOPPED` transition, and nothing else writes it.

**The fix is not "stop discarding".** It is that whoever discards the runner's state write **owns
writing the transition themselves**, in the same turn. A `RUNNING` row over a clean tree with no
live agent means this loop, not a killed run — the killed-run signature is `RUNNING` over a
*dirty* tree (see **Process note — an orphaned working tree**).
