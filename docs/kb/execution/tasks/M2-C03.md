---
doc_id: TASK-M2-C03
title: App shell — header, permission-filtered sidebar, breadcrumbs, command palette
module: execution
task_id: M2-C03
milestone: M2
parent_task: n/a
task_type: Frontend
priority: P0
status: Not Started
estimate: 1.5 wk
depends_on: [M2-C02, M2-C04-01]
source_files:
  - V.SMART/V.SMART.Shared/Layout/NavMenu.razor
  - V.SMART/V.SMART.Shared/Layout/MainLayout.razor
  - V.SMART/V.SMART.Shared/Layout/BlankLayout.razor
  - V.SMART/V.SMART.Shared/Components/PageHeader.razor
  - V.SMART/V.SMART.Shared/Components/SmartBackButton.razor
  - V.SMART/V.SMART.Shared/Components/UnsavedChangesModal.razor
  - V.SMART/V.SMART.Shared/Services/FinancialYearHelper.cs
  - V.SMART/V.SMART.Shared/Data/Enum/UserRole.cs
api_endpoints:
  - "GET /api/v1/me"
business_rules: []
confidence: n/a
last_verified: 2026-08-12
---

# M2-C03 — App shell: header, permission-filtered sidebar, breadcrumbs, ⌘K palette

> # ⛔ STOP — this specification is superseded. Do not implement it as written.
>
> **[`ADR-007`](../../decisions/ADR-007-angular-stack.md) replaced React with Angular + PrimeNG on
> 2026-08-20.** Everything below that names **React, Vite, Mantine, TanStack, Zustand, React Hook
> Form, Zod, `.tsx`, hooks or JSX is stale.** Its *intent* is still valid; its *implementation
> detail is not.*
>
> **If you are a runner and selected this task: stop and report.** Re-specifying it is a
> documentation change requiring the owner, not something to infer mid-implementation. Building
> React here would add to work that is already being discarded.
>
> **What survives, and is the reason this file was not deleted:** the acceptance criteria's
> *intent*, the ERP behaviour being reproduced, the `file:line` evidence about the **existing
> Blazor** implementation, the business rules, and the dependency relationships. None of that was
> ever a React decision.
>
> **What must be re-derived:** every stack-specific instruction, file path under
> `frontend/nexgen-web/`, component API, test-harness detail and command.
>
> **Gating task:** **`M2-C00`** rewrites [KB-050](../../frontend-new/react-architecture.md) for
> Angular. Until that lands there is no authoritative structure to specify against, which is
> precisely why these files were left stale rather than rewritten in a hurry against a guess.

| Field | Value |
|---|---|
| Task ID | M2-C03 |
| Milestone | M2 — Foundation |
| Parent | — (this is a top-level task) |
| Type | Frontend |
| Priority | P0 |
| Status | Not Started |
| Estimate | 1.5 wk |
| Gate | G2 |

## Task Objective

Build the application shell described in
[KB-051 §Application shell](../../frontend-new/design-system.md#application-shell):

1. **Header** (48 px) — menu toggle, product mark, global search entry (⌘K), financial-year
   selector, tenant name, notifications slot, theme toggle, user menu.
2. **Sidebar** — the rail ↔ expanded two-mode idea kept from `NavMenu.razor`, rebuilt as a
   single-level accordion, **filtered by screen rights**, with Recent and Favourites.
3. **Breadcrumbs + `PageHeader`** — title, breadcrumb trail, page-action slot, sticky footer
   action slot.
4. **⌘K command palette** — jump to any permitted screen; the extension point for record
   search comes later.
5. The **dirty-form navigation blocker** (`useBlocker`), replacing `UnsavedChangesModal.razor`.

Proposed location: `frontend/nexgen-web/src/layouts/` and
`frontend/nexgen-web/src/shared/components/navigation/` — **TO BE CREATED**. `src/layouts/`
(with `AppShell.tsx`, `AuthLayout.tsx`, `PrintLayout.tsx`) is named by
[KB-050 §Project structure](../../frontend-new/react-architecture.md#project-structure); the
`navigation/` subfolder and its file names are **proposed** by this task.

> ### The rule this task exists to get right
>
> **The sidebar is filtered by SCREEN RIGHTS (`UserRight` × `Screens`), not by role.**
> Deny-by-default: a screen with **no matching `UserRight` row** is **hidden AND its route is
> refused**. The existing nav does the opposite — it gates on
> `<AuthorizeView Roles="Administrator,ERPAdmin,User">`
> (`V.SMART/V.SMART.Shared/Layout/NavMenu.razor:36`, `:148`), which admits every role that
> exists and one that does not (R-31).
>
> And, as in M2-C02: **the client filter is rendering only.** The server re-checks every
> request (ADR-004). A hidden nav item is a convenience, never a control.

## Why This Task Exists

- **It is the first screen anyone sees after login**, and it is where "this is a new product"
  is either communicated or not. KB-051 names the ⌘K palette as "the most visible *this is a
  new product* signal".
- **`NavMenu.razor` is 888 LOC across 32 nav groups** (Confirmed: `wc -l` on
  `V.SMART/V.SMART.Shared/Layout/NavMenu.razor` = 888; the group inventory is KB-015). It has
  a mini icon rail, an expanded `MudNavMenu` tree, and a separate collapsed production-user
  menu. Reproducing it one-for-one would import 888 lines of accumulated special cases;
  rebuilding it against the permission model produces something both smaller and more correct.
- **R-31 (Confirmed).** `NavMenu.razor:36` and `:148` declare
  `<AuthorizeView Roles="Administrator,ERPAdmin,User">`, but `UserRole`
  (`V.SMART/V.SMART.Shared/Data/Enum/UserRole.cs`) contains only `Administrator` and `User`.
  `"ERPAdmin"` is a dead reference. More importantly, a role list containing *every* real role
  is not a filter at all — the visible menu today is effectively ungated, and the real gating
  happens inside each page via `BaseUserRightsComponent`. The React sidebar fixes the layer:
  **filter the navigation itself, by rights.**
- **Screen rights are the actual model.** 152 seeded `Screens` rows × five flags per user
  (Confirmed, [KB-013](../../architecture/auth-and-permissions.md#2-screen-rights--the-core-permission-model)).
  Navigation filters on `view && !hidden`, reproducing `IsHide` semantics
  ([KB-050 §Permission-based rendering](../../frontend-new/react-architecture.md#permission-based-rendering)).
- **The ERP is financial-year-scoped.** `V.SMART/V.SMART.Shared/Services/FinancialYearHelper.cs`
  exists (Confirmed) and the header must carry the FY selector, because a document list read
  under the wrong FY is a support call, not a cosmetic issue.
- **Keyboard-first is the operating assumption.** Operators run whole shifts without reaching
  for a mouse. A palette that reaches any of ~140 screens in three keystrokes is a throughput
  feature, not a flourish.

## Prerequisites

| Prerequisite | Why |
|---|---|
| **M2-C02** | The permission store, `useScreenRights`, `RequireScreen`, `useAuth`. The sidebar filter reads that store; there is nothing to filter without it. |
| **M2-C04-01** | Tokens: shell dimensions (48 px header, 240 px sidebar, 56 px rail), spacing, elevation, motion, breakpoints, and `ThemeToggle` — which this task **places** in the header. |
| M2-C04-03 (soft) | `Modal`/`ConfirmDialog` for the unsaved-changes dialog, `Skeleton` for the shell's loading state. If unavailable, use bare Mantine with a `TODO`; do not build parallel primitives. |
| A screen-name → route map | See [Investigation Requirements](#investigation-requirements). The sidebar cannot filter by screen right without knowing which route belongs to which of the 152 `Screens` names. |

## Dependencies

| Dependency | Class | Note |
|---|---|---|
| M2-C02 | Hard | Permission store and guards. Note KB-080 §9: "M2-C03 and M2-C02 share the permission store" — they must not be parallelised. |
| M2-C04-01 | Hard | Tokens, breakpoints, `ThemeToggle`. |
| M2-A07 (`GET /api/v1/me`) | Hard *(transitively via M2-C02)* | Supplies user, tenant, role and the full rights set. |
| M2-C04-03 | Soft | Overlays and skeletons. |
| M2-C04-02 | Soft | The FY selector and palette input ideally use `Select` / `Combobox`. |
| KB-053 page map | Information | The route inventory the screen-name mapping is built against. |
| M2-B09 (reference-data endpoints) | Soft | Financial-year list. If it does not exist, derive client-side from a documented rule and record the follow-up — do **not** invent an endpoint. |
| M2-C08 | Testing | The document editor is the heaviest consumer of the dirty-form blocker. |

## Required Existing Knowledge

| Doc | Why |
|---|---|
| [KB-051 §Application shell, §Component inventory (Navigation), §Responsive behaviour, §Accessibility commitments](../../frontend-new/design-system.md#application-shell) | The specification. |
| [KB-013 §2 Screen rights](../../architecture/auth-and-permissions.md#2-screen-rights--the-core-permission-model) | The model the sidebar filters on; deny-by-default. |
| [ADR-004](../../decisions/ADR-004-server-side-authorization.md) | The client filter is rendering only. |
| [KB-050 §Permission-based rendering, §Performance targets](../../frontend-new/react-architecture.md) | Nav filters on `view && !hidden`; route chunks are lazy. |
| [KB-015 §Layout and navigation, §Shared components](../../architecture/frontend-architecture-existing.md#layout-and-navigation) | `NavMenu.razor`'s two modes, the 32 groups, `PageHeader`, `SmartBackButton`, `UnsavedChangesModal`. |
| [KB-053 page map](../../frontend-new/page-map.md) | The 440 legacy routes and the proposed consolidation. |
| [KB-060 R-31](../../risks/technical-debt-register.md) | The dead `ERPAdmin` role. |

## Investigation Requirements

**One substantial new investigation — this is the task's main risk, and it must not be
guessed.**

**INV-033 — Screen-name → route mapping for navigation.** The sidebar filters by
`Screens.ScreenName`, but navigation items are routes. Nothing in the repository currently
joins the two: `BaseUserRightsComponent` requires each page to *declare* its own
`ScreenName`, per page. Therefore:

1. Extract the nav tree from `NavMenu.razor` (32 groups, with routes) — the group list is
   already recorded in KB-015 and KB-053; **reuse it, do not re-derive**.
2. Extract each page's declared `ScreenName` from its `BaseUserRightsComponent` override.
3. Produce the join: nav item → route → screen name, and **record every unmatched entry
   explicitly** as either "no screen right governs this item" or "screen name not yet
   determined". A guessed mapping silently hides a screen from a user who should see it, or
   shows one they should not — and the second failure is invisible until the server refuses.
4. Record the result as a **data file in the React app** (`navigation.config.ts`), not as
   scattered literals, so M2-B05's typed `ScreenCodes` work can later validate it
   mechanically.

Also confirm, and record:

- **R-31**: `NavMenu.razor:36` and `:148` list `Roles="Administrator,ERPAdmin,User"` while
  `UserRole` has only `Administrator` and `User`.
- **The production-user mode.** `NavMenu.razor:462-464` defines
  `ShowProductionMenu => CustomAuth?.IsQrLogin == true && CustomAuth?.IsProductionUser == true`
  (Confirmed) — it requires **both** a QR login and a production user, and collapses the menu
  to a shop-floor-only view. Determine whether `GET /api/v1/me` exposes those two facts. If it
  does not, **raise it as a blocking question for M2-A07 and ship the shell without the
  production mode**, recording the gap. Do not infer the flags from anything else.

Record INV-033 in [KB-003](../../investigation-registry.md) with per-row confidence.

## Repository Areas

| Area | Status | Role |
|---|---|---|
| `frontend/nexgen-web/src/layouts/` | **TO BE CREATED** (path named by KB-050) | `AppShell.tsx`, `AuthLayout.tsx`, `PrintLayout.tsx`. |
| `frontend/nexgen-web/src/shared/components/navigation/` | **TO BE CREATED** (proposed) | `Sidebar`, `NavGroup`, `NavItem`, `Header`, `Breadcrumbs`, `CommandPalette`, `PageHeader`, `UserMenu`, `FinancialYearSelector`, `Tabs`. |
| `frontend/nexgen-web/src/app/router.tsx` | Created by M2-C01, extended by M2-C02 | Gains the `AppShell` layout route and the blocker. |
| `frontend/nexgen-web/src/shared/auth/` | Created by M2-C02 | **Consumed, never modified.** |
| `V.SMART/V.SMART.Shared/Layout/**`, `Components/PageHeader.razor`, `SmartBackButton.razor`, `UnsavedChangesModal.razor` | Exist | **Read-only reference.** |

## Existing Behavior to Preserve

This task **supersedes** the Blazor shell for React users. All the Razor files below keep
serving Blazor unchanged.

| Superseded Razor file | Behaviour and treatment |
|---|---|
| `V.SMART/V.SMART.Shared/Layout/MainLayout.razor` (373 LOC) | The app shell. Replaced by `AppShell.tsx`. Note it injects `SessionTimeoutService` at line 10 — **not ported** (R-17); the idle timeout came from M2-C02. |
| `V.SMART/V.SMART.Shared/Layout/NavMenu.razor` (888 LOC) | Sidebar with a mini icon rail and an expanded `MudNavMenu` tree over 32 groups. The **two-mode idea is kept** because it is genuinely good for a 32-group menu (KB-051). The **role gating at `:36` and `:148` is replaced by screen-right filtering.** The **production-user collapse** at `:462-464` (`IsQrLogin && IsProductionUser`) is preserved *only if* `/api/v1/me` exposes both flags — otherwise it is recorded as a gap and deferred. |
| `V.SMART/V.SMART.Shared/Layout/BlankLayout.razor` | Login / QR-login layout. Becomes `AuthLayout.tsx` (login only; QR login is out of scope, per M2-C02). |
| `V.SMART/V.SMART.Shared/Components/PageHeader.razor` | Title + breadcrumbs via `PageHeader.BreadcrumbItem` (`PageHeader.razor:16,27`). Reproduced as `<PageHeader>` with the same trail concept. |
| `V.SMART/V.SMART.Shared/Components/SmartBackButton.razor` | Context-aware back. Folded into breadcrumbs plus browser history — a separate back button in a routed SPA duplicates a control the browser already provides. Record the decision. |
| `V.SMART/V.SMART.Shared/Components/UnsavedChangesModal.razor` | Dirty-form guard. Replaced by React Router v7 `useBlocker` plus M2-C04-03's `ConfirmDialog`. |
| The `/instantSearch` page (`NavMenu.razor:404,495,812`) | Replaced by the ⌘K palette (KB-051). The Blazor page keeps working; the React app simply has no `/instantSearch` route. |

**Not superseded, and explicitly out of scope:** the 32 groups' destination screens. This task
builds the frame and the nav data, not the pages behind it.

## Target Result

```
┌───────────────────────────────────────────────────────────────────────────┐
│ HEADER 48px  [☰] Mark │ Search (⌘K) │ FY ▾ · Tenant · 🔔 · ☾ · Avatar      │
├──────────┬────────────────────────────────────────────────────────────────┤
│ SIDEBAR  │  Breadcrumb                                    [page actions]  │
│ 240px    │  Page title                                                    │
│ (rail 56)│  … route outlet …                                              │
│          │  Sticky footer: [Cancel]                    [Save] [Save+New]  │
└──────────┴────────────────────────────────────────────────────────────────┘
```

| Component | Contract |
|---|---|
| `AppShell` | The authenticated layout route. Composes header, sidebar, breadcrumbs, outlet and footer slot. Renders a skeleton while the permission store is bootstrapping. |
| `AuthLayout` | Centred card for `/login`. |
| `PrintLayout` | No chrome — for print/preview routes. Created now so print routes never have to unpick the shell later. |
| `Sidebar` | Two modes: 240 px expanded and 56 px rail, user-toggled and persisted; single-level accordion; **items filtered by `view && !hidden`**; pinned **Recent** (last visited, local) and **Favourites** (user-pinned, local for now); a filter box that searches the visible tree. |
| `NavGroup` / `NavItem` | A group renders **only if at least one child survives the filter** — an empty group is worse than no group. `NavItem` marks the active route and exposes it via `aria-current="page"`. |
| `Header` | Menu toggle, product mark, ⌘K search entry, `FinancialYearSelector`, tenant name, notifications slot (empty placeholder wired later), `ThemeToggle` from M2-C04-01, `UserMenu` with logout from M2-C02. |
| `Breadcrumbs` | Derived from route metadata, not hand-passed per page. Last crumb is the current page and is not a link. |
| `PageHeader` | Title, breadcrumbs, status-badge slot, page-action slot. |
| `CommandPalette` | ⌘K / Ctrl-K. Fuzzy search over **permitted** screens only. Arrow keys, `Enter`, `Esc`, recent-first ordering. Extensible with future record-search providers, but ships with screens only. |
| `FinancialYearSelector` | Current FY plus switching. Sourced per the investigation; if no endpoint exists, derived from a documented rule with the follow-up recorded. |
| `Tabs` | Underline style, scrollable overflow, keyboard-navigable — KB-051 lists it under Navigation, and the document editor needs it. |
| `useUnsavedChangesBlocker` | `useBlocker` + `ConfirmDialog`; also handles the browser `beforeunload`. |
| `navigation.config.ts` | The nav tree as **data**: group → item → route → `screenName`. The output of INV-033. |

## Implementation Steps

1. **Run INV-033** and record it before writing the sidebar. Reuse KB-015/KB-053 for the group
   and route inventory; do not re-derive 888 lines.
2. **Create the branch** `migration/M2-C03-app-shell`.
3. **Write `navigation.config.ts`** from the investigation: a typed tree of
   `{ group, items: [{ label, route, screenName, icon }] }`. Every item either carries a
   `screenName` or an explicit `screenName: null` with a comment saying why no right governs
   it. **No silent omissions.**
4. **Build `AppShell`, `AuthLayout`, `PrintLayout`** using M2-C04-01's dimension and
   breakpoint tokens. Wire `AppShell` as a layout route under M2-C02's `RequireAuth`.
5. **Build `Sidebar` / `NavGroup` / `NavItem`.** Filter with `useScreenRights` per item on
   `view && !hidden`; hide a group when every child is filtered out. Persist rail/expanded
   locally. Implement full keyboard navigation (see [React Changes](#react-changes)).
6. **Build `Header`**, placing `ThemeToggle` (M2-C04-01) and `UserMenu` (logout from M2-C02).
   Show the tenant name from the permission store. Leave the notifications control as an inert
   placeholder with a `TODO` — a bell that does nothing is acceptable; a bell that silently
   drops notifications is not.
7. **Build `FinancialYearSelector`** per the investigation's finding, with the source
   documented at the call site.
8. **Build `Breadcrumbs` + `PageHeader`**, deriving the trail from route `handle` metadata so
   a page cannot forget to set it.
9. **Build `CommandPalette`.** Fuzzy-match label and route over the **filtered** nav tree
   only. Recent-first. `⌘K`/`Ctrl+K` opens from anywhere except inside a text input where the
   combination is already bound; `Esc` closes and restores focus.
10. **Build `Tabs`** (underline, scrollable, arrow-key navigable).
11. **Build `useUnsavedChangesBlocker`** on `useBlocker` + `ConfirmDialog`, plus a
    `beforeunload` handler for hard navigations. Verify it blocks in-app navigation, browser
    back, and tab close.
12. **Wire route-level lazy loading.** Every feature route is `React.lazy` + `Suspense` with a
    skeleton fallback (KB-050 §Performance targets). Record the shell's own gzipped size
    against the `< 250 KB gzip` budget — the shell is the thing that budget is really about.
13. **Write the tests** in [Testing](#testing), especially the permission-filtering ones.
14. **Verify** with the M2-C01 command set, update documentation, record INV-033, review, commit.

## Files Expected to Change

| File | Change |
|---|---|
| `frontend/nexgen-web/src/app/router.tsx` | Add the `AppShell` layout route, `PrintLayout` routes, breadcrumb `handle` metadata, and lazy route loading. |
| `frontend/nexgen-web/src/app/providers.tsx` | Mount the command-palette provider if a provider is needed. |
| `frontend/nexgen-web/src/shared/components/index.ts` | Export the navigation surface. |
| `frontend/nexgen-web/README.md` | Add a "Navigation and shell" section, including the rights-not-roles rule and how to add a nav item. |
| `docs/kb/frontend-new/design-system.md` (KB-051) | Record the shell as built, with any deviation and its reason. Bump `last_verified`. |
| `docs/kb/frontend-new/react-architecture.md` (KB-050) | Confirm the nav-filtering rule and the lazy-loading approach; record the shell's bundle size. |
| `docs/kb/frontend-new/page-map.md` (KB-053) | Add the screen-name column produced by INV-033, or link to `navigation.config.ts` as its source. |
| `docs/kb/risks/technical-debt-register.md` (KB-060) | R-31: record that the React shell does not reproduce the role gating; the Blazor dead-role reference remains open. |
| `docs/kb/investigation-registry.md` (KB-003) | Add **INV-033**. |
| `docs/kb/open-questions.md` (KB-004) | Add the production-user-mode question if `/api/v1/me` lacks the flags. |
| `docs/kb/execution/task-tracker.md` (KB-081) | M2-C03 → Completed. |

## Files Expected to Be Created

**TO BE CREATED**; `src/layouts/*` is named by KB-050, the rest is proposed:

`src/layouts/AppShell.tsx` · `AuthLayout.tsx` · `PrintLayout.tsx`
`src/shared/components/navigation/`: `Sidebar.tsx` · `NavGroup.tsx` · `NavItem.tsx` ·
`Header.tsx` · `UserMenu.tsx` · `FinancialYearSelector.tsx` · `Breadcrumbs.tsx` ·
`PageHeader.tsx` · `CommandPalette.tsx` · `Tabs.tsx` · `useUnsavedChangesBlocker.ts` ·
`useRecentScreens.ts` · `useFavourites.ts` · `navigation.config.ts` · `types.ts` ·
`index.ts` · `README.md`, plus a `*.test.tsx` alongside each and `a11y.test.tsx`.

## Files That Must Not Change

- `V.SMART/**` — including all four Layout files and `NavMenu.razor`. The Blazor shell keeps
  serving Blazor users for the whole strangler period. **In particular, do not "fix" R-31 in
  `NavMenu.razor`** — that is a separate Blazor task; this one declines to reproduce the bug.
- `frontend/nexgen-web/src/shared/auth/**` — owned by M2-C02. If the store lacks something
  the sidebar needs, **report it**; do not extend the store here.
- `frontend/nexgen-web/src/shared/theme/**` — owned by M2-C04-01.
- `frontend/nexgen-web/src/shared/components/form/**`, `overlay/**`, `feedback/**` — owned by
  M2-C04-02/03.
- `frontend/vsmart-erp/**`, `docs/ARCHITECTURE.md`.

## API Changes

Not applicable — this task adds no HTTP surface.

It **consumes** `GET /api/v1/me` (M2-A07) indirectly, through M2-C02's permission store; it
issues no request of its own. Two consumption facts must be recorded rather than assumed:

- Whether `/me` exposes `IsQrLogin` and `IsProductionUser`, which the Blazor production menu
  requires (`NavMenu.razor:462-464`). If not → raise for M2-A07, ship without the production
  mode.
- Where the financial-year list comes from. If no endpoint exists (M2-B09), derive it
  client-side from a documented rule and record the follow-up. **Do not invent an endpoint.**

## React Changes

| Aspect | Detail |
|---|---|
| **Route** | `AppShell` as a layout route under M2-C02's `RequireAuth`; `AuthLayout` for `/login`; `PrintLayout` for print routes. Breadcrumb data comes from each route's `handle`. Every feature route is `React.lazy` + `Suspense`. **No `/instantSearch` route** — the palette replaces it. |
| **Component** | `AppShell`, `AuthLayout`, `PrintLayout`, `Sidebar`, `NavGroup`, `NavItem`, `Header`, `UserMenu`, `FinancialYearSelector`, `Breadcrumbs`, `PageHeader`, `CommandPalette`, `Tabs`. |
| **API integration** | n/a directly. All identity, tenant and rights data comes from M2-C02's store. The FY list is either a reference-data query (M2-B09) or a documented client derivation. |
| **State** | Read-only from M2-C02's auth store. Locally owned and persisted: sidebar mode (rail/expanded), Recent screens, Favourites, palette recents. A small Zustand UI-shell store is acceptable per ADR-003 ("auth/session/tenant/UI shell only"); nothing server-derived is duplicated into it. |
| **Validation** | n/a — the shell has no form. The palette's input is a search box, not a validated field. |
| **Permissions** | **The core of this task.** Nav items are filtered on `view && !hidden` from `useScreenRights(screenName)`; an item whose screen key is absent is hidden (deny-by-default, matching `RightsHelper.cs`). A group with no surviving children does not render. The command palette searches **only the filtered tree** — otherwise it becomes a directory of screens the user cannot open, which is both a poor experience and an information leak about the tenant's configuration. Every navigable route is additionally wrapped in `RequireScreen`, so a hand-typed URL is refused, not merely un-linked. **All of this is rendering only; the server re-checks every request (ADR-004).** Role is **not** used for filtering anywhere — R-31's `Roles="Administrator,ERPAdmin,User"` pattern is not reproduced. |
| **Loading state** | While the permission store is bootstrapping, `AppShell` renders a **shell-shaped skeleton** — header bar, sidebar column, content block — never a spinner and never a flash of an empty sidebar, which reads as "you have no permissions". Route transitions show a content-area skeleton while the lazy chunk loads; the shell itself never unmounts. |
| **Empty state** | A user with **zero** permitted screens gets an explicit sidebar message — "No screens are available for your account. Contact your administrator." — not an empty rail. The command palette with no matches shows "No screens match '<query>'". Recent and Favourites, when empty, show a one-line explanation rather than blank space. |
| **Error state** | If `navigation.config.ts` references a screen name absent from the rights map, the item is hidden **and the mismatch is logged once in development** — a silent mismatch is exactly how a screen goes missing for a whole tenant. A failed FY load renders an inline error in the header control with retry, and does **not** take down the shell. A route-level error renders `ErrorState` inside the content area with the shell intact, so the user can navigate away. |
| **Accessibility** | **Landmarks:** `<header>`, `<nav aria-label="Main">`, `<main>`, with a skip-to-content link as the first focusable element. **Keyboard interaction:** `Sidebar` — `Tab` reaches the nav, arrow keys move between items, `Enter`/`Space` activates, `Right`/`Left` expand/collapse a group, `Home`/`End` jump; the rail/expand toggle has an accessible name and `aria-expanded`; the active item carries `aria-current="page"`. `CommandPalette` — `⌘K`/`Ctrl+K` opens from anywhere except where the combination is already bound in a text input, `ArrowUp`/`ArrowDown` move, `Enter` navigates, `Esc` closes and **restores focus to the invoker**; the listbox uses `aria-activedescendant` and results are announced politely. `Breadcrumbs` — an ordered list in a `<nav aria-label="Breadcrumb">`, last crumb `aria-current="page"` and not a link. `Tabs` — arrow keys move, `Home`/`End` jump, `role="tablist"`/`tab`/`tabpanel` wired. `UserMenu` — a menu button with `aria-haspopup`, arrow-key navigation, `Esc` closes and restores focus. All: visible 2 px focus ring, `prefers-reduced-motion` honoured on sidebar and palette transitions, `axe` clean in both themes, functional at 200% zoom. |
| **Responsive behaviour** | Per [KB-051 §Responsive behaviour](../../frontend-new/design-system.md#responsive-behaviour) using M2-C04-01's breakpoints: `≥1440` full 240 px sidebar; `1024–1439` collapses to the 56 px rail; `768–1023` becomes an overlay drawer with a focus trap and `Esc` to close; `<768` read-and-approve only — the shell must still be fully navigable there, since approvals run on phones. The header's FY selector and tenant name collapse into the user menu below `1024` rather than being dropped: FY is not optional information. |

## Business Logic

**Newly introduced — presentation behaviour only.**

The behaviour added is navigation: composing a layout, filtering a menu against a rights map,
deriving breadcrumbs from routes, searching a screen list, and blocking navigation away from a
dirty form.

**No ERP business rule is implemented.** In particular:

- Permission **enforcement** is server-side (ADR-004). This task renders a filtered menu; it
  decides nothing.
- The financial-year selector **selects** an FY; it does not compute financial-year
  boundaries. Those live in `FinancialYearHelper.cs` server-side, and any FY-derived value the
  UI shows must come from the server.
- The unsaved-changes blocker is a UX guard over `isDirty`; it applies no rule about what may
  be saved.

## Testing

| Test | Assertion |
|---|---|
| `Sidebar.permissions.test.tsx` | With rights for 3 of 10 screens, exactly 3 items render. An item whose screen key is **absent** from the map does not render (deny-by-default). An item with `view: true, hidden: true` does not render. A group whose children are all filtered out does not render. |
| `Sidebar.roles.test.tsx` | Changing the user's `role` between `Administrator` and `User`, with rights held constant, changes **nothing** in the sidebar — the explicit R-31 regression test. |
| `Sidebar.keyboard.test.tsx` | Arrow keys move, `Enter` activates, `Right`/`Left` expand/collapse, `Home`/`End` jump, the active item has `aria-current="page"`, the rail toggle exposes `aria-expanded`. |
| `Sidebar.empty.test.tsx` | A user with zero permitted screens sees the explanatory message, not an empty rail. |
| `CommandPalette.test.tsx` | Only permitted screens are searchable; a screen the user lacks rights to **never appears**, even by exact name. `⌘K`/`Ctrl+K` opens; arrows move; `Enter` navigates; `Esc` closes and restores focus; no-match shows the empty message; recents are ordered first. |
| `Breadcrumbs.test.tsx` | The trail is derived from route metadata; the last crumb is `aria-current="page"` and is not a link. |
| `AppShell.loading.test.tsx` | While the permission store is bootstrapping, a shell-shaped skeleton renders — never an empty sidebar. |
| `AppShell.error.test.tsx` | A route error renders `ErrorState` inside the content area with header and sidebar still usable. |
| `navigationConfig.test.ts` | Every entry has either a `screenName` or an explicit `null` with a comment; no duplicate routes; every route referenced exists in the router. |
| `useUnsavedChangesBlocker.test.tsx` | Blocks in-app navigation while dirty; the confirm dialog's Discard proceeds and Cancel stays; a clean form does not block; `beforeunload` is registered only while dirty. |
| `responsive.test.tsx` | At the three breakpoints the sidebar is expanded, rail, and overlay-drawer respectively; the drawer traps focus and closes on `Esc`. |
| `a11y.test.tsx` | `axe` over the shell in both themes, expanded and rail: zero critical violations. Landmarks and the skip link are present. |

## Acceptance Criteria

- [ ] `AppShell`, `AuthLayout` and `PrintLayout` exist; `AppShell` is the layout route under
      `RequireAuth`.
- [ ] The sidebar is filtered **only** by screen rights (`view && !hidden`). Role is not read
      anywhere in the navigation code — proven by `Sidebar.roles.test.tsx` and by
      `git grep -n "role" -- src/shared/components/navigation` returning no permission use.
- [ ] An item whose screen key is absent from the rights map is hidden, **and** its route is
      refused by `RequireScreen` when typed directly.
- [ ] A nav group with no surviving children does not render.
- [ ] `navigation.config.ts` exists as the single nav data source; every entry has a
      `screenName` or an explicit `null` with a stated reason; `navigationConfig.test.ts`
      passes.
- [ ] The command palette searches **only** permitted screens; a screen the user lacks rights
      to is unreachable through it even by exact name.
- [ ] `⌘K` / `Ctrl+K` opens the palette; `Esc` closes it and restores focus to the invoker.
- [ ] The sidebar supports both the 240 px expanded and 56 px rail modes, user-toggled and
      persisted, reproducing `NavMenu.razor`'s two-mode idea.
- [ ] The header carries: menu toggle, ⌘K entry, financial-year selector, tenant name,
      notifications placeholder, `ThemeToggle` (from M2-C04-01) and `UserMenu` with logout.
- [ ] Breadcrumbs derive from route metadata, not per-page props; the last crumb is
      `aria-current="page"` and is not a link.
- [ ] `useUnsavedChangesBlocker` blocks in-app navigation, browser back and tab close while
      dirty, and does nothing when clean.
- [ ] The shell renders a shell-shaped skeleton while bootstrapping — never an empty sidebar.
- [ ] A user with zero permitted screens sees an explicit explanation.
- [ ] Landmarks (`header`, `nav aria-label="Main"`, `main`) and a skip-to-content link are
      present; every documented keyboard model is asserted by a `user-event` test; `axe`
      reports zero critical violations in both themes and both sidebar modes.
- [ ] Responsive behaviour matches KB-051 at all four breakpoints; the `768–1023` overlay
      drawer traps focus and closes on `Esc`.
- [ ] Feature routes are `React.lazy`-loaded; the shell's gzipped size is recorded against
      KB-050's `< 250 KB gzip` target.
- [ ] **INV-033** is recorded, including every unmatched nav item, with per-row confidence.
- [ ] R-31 is recorded as **not reproduced** in React, with the Blazor defect left open.
- [ ] The production-user mode (`NavMenu.razor:462-464`, `IsQrLogin && IsProductionUser`) is
      either implemented from real `/me` data or explicitly deferred with a raised question —
      never inferred.
- [ ] No file under `V.SMART/` changed.
- [ ] `npm run typecheck`, `npm run lint`, `npm run test -- --run` and `npm run build` pass.

## Verification Commands

```bash
cd frontend/nexgen-web && npm run typecheck
```

```bash
cd frontend/nexgen-web && npm run lint
```

```bash
cd frontend/nexgen-web && npm run test -- --run
```

```bash
cd frontend/nexgen-web && npm run build
```

```bash
cd frontend/nexgen-web && npm run e2e
```

```bash
git grep -n "role" -- frontend/nexgen-web/src/shared/components/navigation
```

```bash
git diff --name-only -- V.SMART
```

> The `npm` commands are those **established by M2-C01**. The `git` commands are verified
> working in this repository (INV-029). `git diff --name-only -- V.SMART` must print nothing.

## Documentation Updates

| Document | Update |
|---|---|
| [KB-051](../../frontend-new/design-system.md) | Record the shell as built: header contents, sidebar modes, palette scope, breadcrumb derivation, and any deviation with its reason. Bump `last_verified`. |
| [KB-050](../../frontend-new/react-architecture.md) | Confirm the `view && !hidden` nav rule as implemented; record the shell's bundle size against the performance targets. |
| [KB-053](../../frontend-new/page-map.md) | Add the screen-name column from INV-033, or reference `navigation.config.ts` as its source of truth. |
| [KB-013](../../architecture/auth-and-permissions.md) | Extend the "React client" subsection: navigation is filtered by screen rights, not role; the filter is rendering only. |
| [KB-060](../../risks/technical-debt-register.md) | R-31: the React shell does not reproduce the role gating; the Blazor dead-role reference remains **open**. |
| [KB-003](../../investigation-registry.md) | Add **INV-033 — screen-name → route mapping for navigation**. |
| [KB-004](../../open-questions.md) | The production-user-mode question, if `/me` lacks `IsQrLogin` / `IsProductionUser`. |
| [KB-081](../task-tracker.md) | M2-C03 → Completed. |
| `frontend/nexgen-web/README.md` | "Navigation and shell": how to add a nav item, the rights-not-roles rule, and the requirement that every nav item declare a screen name or an explicit exemption. |

## Investigation Registry Updates

Add **INV-033**:

```yaml
Finding:        Mapped <n> navigation items across 32 groups from NavMenu.razor to their
                routes and to the Screens.ScreenName each is governed by. <m> items have no
                governing screen right and are recorded as exempt with a reason; <k> items
                could not be mapped and are recorded as Unknown. The mapping is stored as
                data in frontend/nexgen-web/src/shared/components/navigation/navigation.config.ts.
                Confirmed alongside: NavMenu.razor:36 and :148 declare
                Roles="Administrator,ERPAdmin,User" while UserRole contains only
                Administrator and User (R-31); NavMenu.razor:462-464 defines
                ShowProductionMenu as IsQrLogin && IsProductionUser.
Evidence:       V.SMART/V.SMART.Shared/Layout/NavMenu.razor:36,148,404,462-464,495,812 ;
                V.SMART/V.SMART.Shared/Data/Enum/UserRole.cs ;
                V.SMART/V.SMART.Shared/Shared/BaseUserRightsComponent.cs ;
                KB-015, KB-053
Business rule:  n/a
Confidence:     Confirmed per mapped row; Unknown recorded explicitly per unmapped row
Last verified:  <date of execution>
```

Every unmapped row must appear individually. A mapping table that hides its gaps is worse
than no table, because the next session will trust it.

## Git Strategy

| Field | Value |
|---|---|
| Branch | `migration/M2-C03-app-shell` |
| Commit | `M2-C03: Add app shell with permission-filtered sidebar, breadcrumbs and command palette` |
| Expected changed files | New `frontend/nexgen-web/src/layouts/**` and `src/shared/components/navigation/**`; modified `src/app/router.tsx`, `src/app/providers.tsx`, `src/shared/components/index.ts`, `frontend/nexgen-web/README.md`, and the KB documents listed above. |
| Rollback | Revert the branch's merge commit. The shell is the entry point for every later feature route, so if any feature screen has merged on top, revert in reverse dependency order. Nothing is persisted server-side; only local sidebar-mode, Recent and Favourites keys become inert. |

Do not merge. Do not push to `master`. Leave the branch for review. The review must include:
(a) a keyboard-only pass through the sidebar and palette, and (b) a check of the sidebar under
a **low-privilege** test user — the permission filter is the part most likely to be verified
only as an administrator, which is exactly the account that would never notice a bug in it.

## Fresh-Session Execution Prompt

Copy everything in the block below into a new AI session.

```text
============================================================
ERP MIGRATION — TASK EXECUTION PROMPT
============================================================

TASK ID:
M2-C03

TASK NAME:
App shell — header, permission-filtered sidebar, breadcrumbs, command palette

ROLE
You are an engineer working on the V.SMART / NexGen ERP
modernization project. You are executing exactly one task.

PROJECT OBJECTIVE
We are replacing the existing Blazor Server frontend with a new
React frontend while preserving the existing ERP business
behaviour, business services, database behaviour and business
rules wherever possible. The backend is extended, never
rewritten. Business logic currently trapped in Razor @code is
extracted into server-side services before any React screen
replaces it.

CURRENT ARCHITECTURE
Repository root: C:\Kumar\NexGen-ERP---2025-master\NexGen-ERP---2025-master
(this is the git repository root; the parent directory is not
part of the project)

  V.SMART/V.SMART.Shared   .NET 9 class library — ALL domain code:
                           196 EF entity sets, 285 business services,
                           ~190 repositories + UnitOfWork, 274 ViewModels,
                           333 Razor pages, 440 routes
  V.SMART/V.SMART.Web      Blazor Server host (live UI, stays running)
  V.SMART/V.SMART.Api      ASP.NET Core Web API (.NET 9)
  V.SMART/V.SMART          .NET MAUI Blazor Hybrid host

SQL Server + EF Core 9, code-first, database-per-tenant.

Task-specific architecture context:
  - The React app is at frontend/nexgen-web/. M2-C01 created it,
    M2-C02 added auth and the permission store, M2-C04-01 added
    the tokens, breakpoints and ThemeToggle you will place.
  - THE PERMISSION MODEL. 152 seeded Screens rows, each with five
    per-user flags via UserRight: CanView, CanCreate, CanEdit,
    CanDelete, IsHide. Navigation filters on view && !hidden.
    DENY BY DEFAULT: a screen with NO matching UserRight row is
    HIDDEN and its route is REFUSED.
  - THE EXISTING NAV DOES THE OPPOSITE.
    V.SMART/V.SMART.Shared/Layout/NavMenu.razor is 888 LOC across
    32 nav groups, with a mini icon rail and an expanded
    MudNavMenu tree. Lines 36 and 148 gate on
    <AuthorizeView Roles="Administrator,ERPAdmin,User">. The
    UserRole enum (V.SMART/V.SMART.Shared/Data/Enum/UserRole.cs)
    contains ONLY Administrator and User, so "ERPAdmin" is a dead
    reference (risk R-31) — and a role list containing every real
    role is not a filter at all. The visible menu today is
    effectively ungated; real gating happens inside each page via
    BaseUserRightsComponent. YOUR SIDEBAR FIXES THE LAYER: filter
    the NAVIGATION, by RIGHTS.
  - AND, exactly as in M2-C02: THE CLIENT FILTER IS RENDERING
    ONLY. The server re-checks every request (ADR-004). A hidden
    nav item is a convenience, never a control. Never describe it
    as security.
  - The ERP is financial-year scoped
    (V.SMART/V.SMART.Shared/Services/FinancialYearHelper.cs), so
    the header carries an FY selector. A document list read under
    the wrong FY is a support call, not a cosmetic issue.
  - Operators run whole shifts without reaching for a mouse. A
    palette that reaches any of roughly 140 screens in three
    keystrokes is a throughput feature, not a flourish. KB-051
    calls it "the most visible 'this is a new product' signal".

SOURCE OF TRUTH
The project knowledge base is at docs/kb/. Read it before
investigating the repository. Use docs/kb/INDEX.md for
question-to-document routing.

Required reading for this task:
  KB-051   docs/kb/frontend-new/design-system.md
           Sections Application shell, Component inventory
           (Navigation), Responsive behaviour, Accessibility
           commitments. This is the specification.
  KB-013   docs/kb/architecture/auth-and-permissions.md
           section 2, Screen rights.
  ADR-004  docs/kb/decisions/ADR-004-server-side-authorization.md
  KB-050   docs/kb/frontend-new/react-architecture.md
           Permission-based rendering; Performance targets.
  KB-015   docs/kb/architecture/frontend-architecture-existing.md
           Layout and navigation; Shared components.
  KB-053   docs/kb/frontend-new/page-map.md
  KB-060   docs/kb/risks/technical-debt-register.md  R-31, R-17
  KB-083   docs/kb/execution/prompt-template.md

Authority order when sources conflict:
  1. Current source code
  2. Database schema / EF migrations (for storage)
  3. The knowledge base (for interpretation)
  4. Older prose documentation — hypothesis only
docs/ARCHITECTURE.md is superseded and contains known factual
errors. Do not rely on it.

INVESTIGATION REGISTRY
Before any repository investigation, search
docs/kb/investigation-registry.md.

Relevant investigations:
  INV-006  Existing UI: routing, layout, components — Complete.
           REUSE the 32-group inventory; do not re-derive 888
           lines.
  INV-004  Authentication, screen rights — Complete. Reuse.
  INV-033  Screen-name to route mapping for navigation — DOES
           NOT EXIST. You create it. It is this task's main risk.

Before investigating the repository, search
`docs/kb/investigation-registry.md` and the relevant
knowledge-base documents via `docs/kb/INDEX.md`. If an
investigation is **Complete** and not stale, reuse its findings
and cite the `doc_id` — do not re-derive them. If it is
**Partial**, investigate only the documented gap. If it is absent
or contradicted by current code, investigate, then record the
finding, its `file:line` evidence, and its confidence in the
knowledge base so that future sessions do not repeat this work.
Record negative results too — "grepped for X, found none" is a
finding.

CURRENT TASK
Build the application shell from KB-051:
  1. Header, 48px: menu toggle, product mark, global search entry
     (Cmd-K), financial-year selector, tenant name, notifications
     slot, theme toggle, user menu.
  2. Sidebar: the rail (56px) and expanded (240px) two-mode idea
     kept from NavMenu.razor, rebuilt as a single-level
     accordion, FILTERED BY SCREEN RIGHTS, with pinned Recent and
     Favourites and a filter box.
  3. Breadcrumbs and PageHeader: title, trail, page-action slot,
     sticky footer action slot.
  4. Cmd-K command palette: jump to any PERMITTED screen.
  5. The dirty-form navigation blocker (useBlocker), replacing
     UnsavedChangesModal.razor.

Proposed locations, TO BE CREATED. src/layouts/ with AppShell,
AuthLayout and PrintLayout is named by KB-050; the navigation/
subfolder and its file names are proposed by this task and must
be reported as proposed:
  src/layouts/{AppShell.tsx, AuthLayout.tsx, PrintLayout.tsx}
  src/shared/components/navigation/
    Sidebar.tsx NavGroup.tsx NavItem.tsx Header.tsx UserMenu.tsx
    FinancialYearSelector.tsx Breadcrumbs.tsx PageHeader.tsx
    CommandPalette.tsx Tabs.tsx useUnsavedChangesBlocker.ts
    useRecentScreens.ts useFavourites.ts navigation.config.ts
    types.ts index.ts README.md
  plus a *.test.tsx alongside each and a11y.test.tsx

INVESTIGATION REQUIRED — INV-033, and it must NOT be guessed
The sidebar filters by Screens.ScreenName, but navigation items
are ROUTES. Nothing in the repository joins the two:
BaseUserRightsComponent requires each PAGE to declare its own
ScreenName. So:
  1. Extract the nav tree from NavMenu.razor — 32 groups, with
     routes. The group list is ALREADY recorded in KB-015 and
     KB-053. REUSE IT.
  2. Extract each page's declared ScreenName from its
     BaseUserRightsComponent override.
  3. Produce the join: nav item -> route -> screen name, and
     RECORD EVERY UNMATCHED ENTRY EXPLICITLY as either "no screen
     right governs this item" or "screen name not yet
     determined". A guessed mapping either hides a screen from a
     user who should see it, or shows one they should not — and
     the second failure is invisible until the server refuses.
  4. Store the result as DATA in navigation.config.ts, not as
     scattered literals, so M2-B05's typed ScreenCodes work can
     later validate it mechanically.
Also confirm and record:
  - R-31: NavMenu.razor:36 and :148 list
    Roles="Administrator,ERPAdmin,User" while UserRole has only
    Administrator and User.
  - THE PRODUCTION-USER MODE. NavMenu.razor:462-464 defines
    ShowProductionMenu => CustomAuth?.IsQrLogin == true &&
    CustomAuth?.IsProductionUser == true — it needs BOTH a QR
    login and a production user, and collapses the menu to a
    shop-floor-only view. Determine whether GET /api/v1/me
    exposes those two facts. If it does not, RAISE IT as a
    blocking question for M2-A07 and ship the shell WITHOUT the
    production mode, recording the gap. Do not infer the flags
    from anything else.
Record INV-033 with per-row confidence. Every unmapped row must
appear individually: a mapping table that hides its gaps is worse
than no table, because the next session will trust it.

WHY THIS TASK EXISTS
It is the first screen anyone sees after login and the place
where the product either reads as new or does not.

NavMenu.razor is 888 LOC of accumulated special cases.
Reproducing it one-for-one imports all of them; rebuilding it
against the permission model produces something both smaller and
more correct.

And it fixes the gating LAYER. Today the menu is role-gated with
a list containing every role, and the real check happens inside
each page. Filtering the navigation by rights is what makes the
menu tell the truth about what a user can do.

PREREQUISITES
  M2-C02      permission store, useScreenRights, RequireScreen,
              useAuth. HARD. KB-080 section 9 notes that M2-C03
              and M2-C02 share the permission store and must not
              be parallelised.
  M2-C04-01   tokens: 48px header, 240px sidebar, 56px rail,
              spacing, elevation, motion, breakpoints, and the
              ThemeToggle you PLACE in the header. HARD.
  M2-C04-03   soft: Modal/ConfirmDialog for the unsaved-changes
              dialog and Skeleton for the shell loading state. If
              unavailable, use bare Mantine with a TODO — do NOT
              build parallel primitives.

CURRENT IMPLEMENTATION
Confirmed:
  - V.SMART/V.SMART.Shared/Layout/NavMenu.razor — 888 lines, 32
    nav groups, mini icon rail plus expanded MudNavMenu tree.
      :36 and :148   <AuthorizeView Roles="Administrator,ERPAdmin,User">
      :404, :495, :812  the "Instant Search" item, route
                        instantSearch
      :462-464       ShowProductionMenu => IsQrLogin &&
                     IsProductionUser
  - V.SMART/V.SMART.Shared/Layout/MainLayout.razor — 373 lines,
    the app shell. Line 10 injects SessionTimeoutService. DO NOT
    PORT THAT (risk R-17, shared singleton idle clock); the idle
    timeout already arrived with M2-C02.
  - V.SMART/V.SMART.Shared/Layout/BlankLayout.razor — the login
    and QR-login layout. Becomes AuthLayout (login only; QR login
    is out of scope per M2-C02).
  - V.SMART/V.SMART.Shared/Components/PageHeader.razor:16 exposes
    List<BreadcrumbItem> Breadcrumbs and :27 defines
    BreadcrumbItem.
  - V.SMART/V.SMART.Shared/Components/SmartBackButton.razor —
    context-aware back.
  - V.SMART/V.SMART.Shared/Components/UnsavedChangesModal.razor —
    dirty-form guard.
  - V.SMART/V.SMART.Shared/Services/FinancialYearHelper.cs
    exists.
  - The React app has NO shell: M2-C01's placeholder route and
    M2-C02's /login are all that exist.
Unknown until you check:
  - which Screens.ScreenName governs each nav item (INV-033)
  - whether /api/v1/me exposes IsQrLogin and IsProductionUser
  - where the financial-year list comes from

TARGET IMPLEMENTATION
Layout:
  header 48px across the top; sidebar 240px expanded or 56px
  rail on the left; content area with breadcrumb, page title,
  page actions, the route outlet, and a sticky footer action row.

Component contracts:
  AppShell       the authenticated layout route under M2-C02's
                 RequireAuth; renders a SHELL-SHAPED SKELETON
                 while the permission store bootstraps
  AuthLayout     centred card for /login
  PrintLayout    no chrome, for print and preview routes.
                 Created NOW so print routes never have to unpick
                 the shell later.
  Sidebar        two modes, user-toggled and persisted;
                 single-level accordion; items filtered on
                 view && !hidden; pinned Recent (last visited,
                 local) and Favourites (user-pinned, local for
                 now); a filter box over the visible tree
  NavGroup       renders ONLY if at least one child survives the
                 filter — an empty group is worse than no group
  NavItem        marks the active route with aria-current="page"
  Header         menu toggle, product mark, Cmd-K entry,
                 FinancialYearSelector, tenant name,
                 notifications slot (an INERT placeholder with a
                 TODO — a bell that does nothing is acceptable, a
                 bell that silently drops notifications is not),
                 ThemeToggle from M2-C04-01, UserMenu with logout
                 from M2-C02
  Breadcrumbs    derived from route handle metadata, NOT passed
                 per page, so a page cannot forget to set it
  PageHeader     title, breadcrumbs, status-badge slot, page
                 actions
  CommandPalette Cmd-K / Ctrl-K; fuzzy search over PERMITTED
                 screens only; arrows, Enter, Esc; recents first;
                 extensible with future record-search providers
                 but ships with screens only
  FinancialYearSelector
                 current FY plus switching, sourced per the
                 investigation
  Tabs           underline, scrollable overflow, arrow-key
                 navigable
  useUnsavedChangesBlocker
                 useBlocker plus ConfirmDialog, plus a
                 beforeunload handler while dirty
  navigation.config.ts
                 the nav tree AS DATA: group -> item -> route ->
                 screenName. The output of INV-033.

Wire route-level lazy loading: every feature route is React.lazy
plus Suspense with a skeleton fallback. Record the shell's own
gzipped size against KB-050's < 250 KB gzip budget — the shell is
what that budget is really about.

RELEVANT FILES
Read-only reference:
  V.SMART/V.SMART.Shared/Layout/NavMenu.razor
  V.SMART/V.SMART.Shared/Layout/MainLayout.razor
  V.SMART/V.SMART.Shared/Layout/BlankLayout.razor
  V.SMART/V.SMART.Shared/Components/PageHeader.razor
  V.SMART/V.SMART.Shared/Components/SmartBackButton.razor
  V.SMART/V.SMART.Shared/Components/UnsavedChangesModal.razor
  V.SMART/V.SMART.Shared/Services/FinancialYearHelper.cs
  V.SMART/V.SMART.Shared/Data/Enum/UserRole.cs
  V.SMART/V.SMART.Shared/Shared/BaseUserRightsComponent.cs
Consumed, never modified:
  frontend/nexgen-web/src/shared/auth/**            (M2-C02)
  frontend/nexgen-web/src/shared/theme/**           (M2-C04-01)
  frontend/nexgen-web/src/shared/components/form/**,
    overlay/**, feedback/**                         (M2-C04-02/03)
To be modified:
  frontend/nexgen-web/src/app/router.tsx
  frontend/nexgen-web/src/app/providers.tsx
  frontend/nexgen-web/src/shared/components/index.ts
  frontend/nexgen-web/README.md
  docs/kb/frontend-new/design-system.md
  docs/kb/frontend-new/react-architecture.md
  docs/kb/frontend-new/page-map.md
  docs/kb/architecture/auth-and-permissions.md
  docs/kb/risks/technical-debt-register.md
  docs/kb/investigation-registry.md
  docs/kb/open-questions.md
  docs/kb/execution/task-tracker.md
To be created:
  the src/layouts/** and
  src/shared/components/navigation/** files listed above

BUSINESS RULES TO PRESERVE
None — this task does not touch business behaviour. Specifically:
  - Permission ENFORCEMENT is server-side (ADR-004). You render a
    filtered menu; you decide nothing.
  - FinancialYearSelector SELECTS a financial year. It does NOT
    compute financial-year boundaries — those live in
    FinancialYearHelper.cs server-side, and any FY-derived value
    the UI shows must come from the server.
  - The unsaved-changes blocker is a UX guard over isDirty. It
    applies no rule about what may be saved.

CONSTRAINTS
- Do not rewrite existing business services.
- Do not reimplement ERP business logic in React/TypeScript.
- Do not change unrelated modules.
- Do not make assumptions without checking source code.
- Do not repeat completed investigations.
- Do not modify the database schema unless this task explicitly
  authorizes it. (It does not.)
- Preserve existing API behaviour wherever practical.
- The server remains authoritative for validation, calculations,
  permissions and document numbering.
- Do not start another task after completing this one.
- Do not modify anything under V.SMART/. In particular DO NOT
  "fix" R-31 in NavMenu.razor — that is a separate Blazor task.
  This one declines to reproduce the bug.
- Do not port SessionTimeoutService or anything resembling it
  (R-17).
- Do not use ROLE for any navigation or route decision. Filter by
  screen rights only.
- Do not describe the client filter as security, anywhere.
- Do not modify src/shared/auth/** — if the store lacks something
  the sidebar needs, REPORT it.
- Do not modify src/shared/theme/** or the M2-C04-02/03
  component directories.
- Do not build any ERP screen. This task builds the frame and the
  nav data, not the pages behind it.
- Do not create an /instantSearch route — the palette replaces
  it.
- Do not invent an endpoint for the financial-year list. If none
  exists, derive it client-side from a documented rule and record
  the follow-up.
- Do not guess a screen-name mapping. Unknown is a valid,
  recordable answer; a wrong mapping is not.

EXECUTION PROCEDURE
1. Read the referenced knowledge-base documents.
2. Search the investigation registry.
3. Confirm whether the required investigation already exists.
4. Inspect the actual source code.
5. Verify every assumption against code before acting on it.
6. Implement only this task.
7. Run the required verification commands and tests.
8. Review the full git diff.
9. Update documentation.
10. Update the investigation registry if required.

EXPECTED FILE CHANGES
Modified:  frontend/nexgen-web/src/app/router.tsx
           frontend/nexgen-web/src/app/providers.tsx
           frontend/nexgen-web/src/shared/components/index.ts
           frontend/nexgen-web/README.md
           docs/kb/frontend-new/design-system.md
           docs/kb/frontend-new/react-architecture.md
           docs/kb/frontend-new/page-map.md
           docs/kb/architecture/auth-and-permissions.md
           docs/kb/risks/technical-debt-register.md
           docs/kb/investigation-registry.md
           docs/kb/open-questions.md
           docs/kb/execution/task-tracker.md
Created:   src/layouts/** and
           src/shared/components/navigation/**
Must not change: V.SMART/** ; frontend/vsmart-erp/** ;
           frontend/nexgen-web/src/shared/auth/** ;
           frontend/nexgen-web/src/shared/theme/** ;
           frontend/nexgen-web/src/shared/components/{form,overlay,feedback}/** ;
           docs/ARCHITECTURE.md

TESTS
  Sidebar.permissions.test.tsx
    with rights for 3 of 10 screens exactly 3 items render; an
    item whose screen key is ABSENT does not render
    (deny-by-default); view true with hidden true does not
    render; a group whose children are all filtered out does not
    render.
  Sidebar.roles.test.tsx
    changing the user's role between Administrator and User with
    rights held constant changes NOTHING in the sidebar. This is
    the explicit R-31 regression test.
  Sidebar.keyboard.test.tsx
    arrows move; Enter activates; Right/Left expand and collapse;
    Home/End jump; the active item has aria-current="page"; the
    rail toggle exposes aria-expanded.
  Sidebar.empty.test.tsx
    a user with zero permitted screens sees the explanatory
    message, not an empty rail.
  CommandPalette.test.tsx
    only permitted screens are searchable; a screen the user
    lacks rights to NEVER appears, even by exact name; Cmd-K /
    Ctrl-K opens; arrows move; Enter navigates; Esc closes and
    restores focus; no-match shows the empty message; recents are
    ordered first.
  Breadcrumbs.test.tsx
    the trail derives from route metadata; the last crumb is
    aria-current="page" and is not a link.
  AppShell.loading.test.tsx
    while the permission store bootstraps, a shell-shaped
    skeleton renders — never an empty sidebar, which reads as
    "you have no permissions".
  AppShell.error.test.tsx
    a route error renders ErrorState inside the content area with
    header and sidebar still usable.
  navigationConfig.test.ts
    every entry has a screenName or an explicit null with a
    comment; no duplicate routes; every referenced route exists
    in the router.
  useUnsavedChangesBlocker.test.tsx
    blocks in-app navigation while dirty; Discard proceeds and
    Cancel stays; a clean form does not block; beforeunload is
    registered only while dirty.
  responsive.test.tsx
    at the three breakpoints the sidebar is expanded, rail and
    overlay-drawer respectively; the drawer traps focus and
    closes on Esc.
  a11y.test.tsx
    axe over the shell in both themes and both sidebar modes:
    zero critical violations; landmarks and the skip link
    present.

  Command: cd frontend/nexgen-web && npm run test -- --run

ACCESSIBILITY TO IMPLEMENT AND TEST
  Landmarks: <header>, <nav aria-label="Main">, <main>, with a
  skip-to-content link as the FIRST focusable element.
  Sidebar: Tab reaches the nav; arrows move between items;
    Enter/Space activates; Right/Left expand/collapse a group;
    Home/End jump; the rail toggle has an accessible name and
    aria-expanded; the active item has aria-current="page".
  CommandPalette: Cmd-K/Ctrl-K opens from anywhere EXCEPT where
    the combination is already bound inside a text input;
    ArrowUp/ArrowDown move; Enter navigates; Esc closes and
    RESTORES FOCUS TO THE INVOKER; the listbox uses
    aria-activedescendant; results announced politely.
  Breadcrumbs: an ordered list inside
    <nav aria-label="Breadcrumb">; last crumb aria-current="page"
    and not a link.
  Tabs: arrows move; Home/End jump; role tablist/tab/tabpanel
    wired.
  UserMenu: menu button with aria-haspopup; arrow-key navigation;
    Esc closes and restores focus.
  All: visible 2px focus ring; prefers-reduced-motion honoured on
  sidebar and palette transitions; functional at 200% zoom.

RESPONSIVE BEHAVIOUR TO IMPLEMENT
  >=1440       full 240px sidebar
  1024-1439    collapses to the 56px rail
  768-1023     becomes an overlay drawer with a focus trap and
               Esc to close
  <768         read-and-approve only, but the shell must remain
               FULLY NAVIGABLE — approvals run on phones
  Below 1024 the FY selector and tenant name collapse into the
  user menu rather than being dropped. FY is not optional
  information.

ACCEPTANCE CRITERIA
1.  AppShell, AuthLayout and PrintLayout exist; AppShell is the
    layout route under RequireAuth.
2.  The sidebar is filtered ONLY by screen rights
    (view && !hidden). Role is not read anywhere in the
    navigation code, proven by Sidebar.roles.test.tsx and by
    git grep -n "role" -- src/shared/components/navigation
    showing no permission use.
3.  An item whose screen key is absent is hidden AND its route is
    refused by RequireScreen when typed directly.
4.  A nav group with no surviving children does not render.
5.  navigation.config.ts is the single nav data source; every
    entry has a screenName or an explicit null with a stated
    reason; navigationConfig.test.ts passes.
6.  The command palette searches ONLY permitted screens; a screen
    the user lacks rights to is unreachable through it even by
    exact name.
7.  Cmd-K / Ctrl-K opens the palette; Esc closes it and restores
    focus to the invoker.
8.  The sidebar supports both 240px expanded and 56px rail modes,
    user-toggled and persisted.
9.  The header carries menu toggle, Cmd-K entry, FY selector,
    tenant name, notifications placeholder, ThemeToggle and
    UserMenu with logout.
10. Breadcrumbs derive from route metadata, not per-page props;
    the last crumb is aria-current="page" and not a link.
11. useUnsavedChangesBlocker blocks in-app navigation, browser
    back and tab close while dirty, and does nothing when clean.
12. The shell renders a shell-shaped skeleton while
    bootstrapping — never an empty sidebar.
13. A user with zero permitted screens sees an explicit
    explanation.
14. Landmarks and a skip-to-content link are present; every
    documented keyboard model is asserted by a user-event test;
    axe reports zero critical violations in both themes and both
    sidebar modes.
15. Responsive behaviour matches KB-051 at all four breakpoints;
    the 768-1023 overlay drawer traps focus and closes on Esc.
16. Feature routes are React.lazy-loaded; the shell's gzipped
    size is recorded against the < 250 KB gzip target.
17. INV-033 is recorded, including EVERY unmatched nav item, with
    per-row confidence.
18. R-31 is recorded as NOT reproduced in React, with the Blazor
    defect left open.
19. The production-user mode (NavMenu.razor:462-464) is either
    implemented from real /me data or explicitly deferred with a
    raised question — never inferred.
20. git diff --name-only -- V.SMART prints nothing.
21. npm run typecheck, lint, test and build all pass.

VERIFICATION COMMANDS
  cd frontend/nexgen-web && npm run typecheck
  cd frontend/nexgen-web && npm run lint
  cd frontend/nexgen-web && npm run test -- --run
  cd frontend/nexgen-web && npm run build
  cd frontend/nexgen-web && npm run e2e
  git grep -n "role" -- frontend/nexgen-web/src/shared/components/navigation
  git diff --name-only -- V.SMART

The npm commands were established and verified by task M2-C01.
The git commands are verified working in this repository
(INV-029). git diff --name-only -- V.SMART must print nothing.

DOCUMENTATION REQUIREMENTS
  docs/kb/frontend-new/design-system.md (KB-051)
      Record the shell as built: header contents, sidebar modes,
      palette scope, breadcrumb derivation, and any deviation
      with its reason. Bump last_verified.
  docs/kb/frontend-new/react-architecture.md (KB-050)
      Confirm the view && !hidden nav rule as implemented; record
      the shell's bundle size against the performance targets.
  docs/kb/frontend-new/page-map.md (KB-053)
      Add the screen-name column from INV-033, or reference
      navigation.config.ts as its source of truth.
  docs/kb/architecture/auth-and-permissions.md (KB-013)
      Extend the "React client" subsection: navigation is
      filtered by screen rights, not role, and the filter is
      rendering only.
  docs/kb/risks/technical-debt-register.md (KB-060)
      R-31: the React shell does not reproduce the role gating;
      the Blazor dead-role reference REMAINS OPEN.
  docs/kb/investigation-registry.md (KB-003)
      Add INV-033 using the Finding / Evidence / Business rule /
      Confidence / Last verified format, with every unmapped row
      listed individually.
  docs/kb/open-questions.md (KB-004)
      The production-user-mode question, if /me lacks IsQrLogin
      and IsProductionUser.
  docs/kb/execution/task-tracker.md (KB-081)
      Set M2-C03 to Completed.
  frontend/nexgen-web/README.md
      "Navigation and shell": how to add a nav item, the
      rights-not-roles rule, and the requirement that every nav
      item declare a screen name or an explicit exemption.

GIT
Branch:  migration/M2-C03-app-shell
Commit:  M2-C03: Add app shell with permission-filtered sidebar, breadcrumbs and command palette
Do not merge. Do not push to master. Leave the branch for review.
The review must include (a) a keyboard-only pass through the
sidebar and palette, and (b) a check of the sidebar under a
LOW-PRIVILEGE test user — the permission filter is the part most
likely to be verified only as an administrator, which is exactly
the account that would never notice a bug in it.

FINAL RESPONSE
When finished, report:
1.  Task ID
2.  Task status (Completed / Needs Review / Blocked)
3.  What was implemented
4.  Files created
5.  Files modified
6.  Files deleted
7.  Tests executed
8.  Test results
9.  Documentation updated
10. Investigation registry updated
11. Architectural decisions taken
12. Unexpected findings
13. Assumptions made
14. Deviations from this task
15. Recommended next task

IMPORTANT:
EXECUTE ONLY THIS TASK.
DO NOT START THE NEXT TASK.
============================================================
```
