---
doc_id: TASK-M2-C03
title: App shell — header, permission-filtered sidebar, breadcrumbs, command palette
module: execution
task_id: M2-C03
milestone: M2
parent_task: n/a
task_type: Frontend
priority: P0
status: Needs Review
estimate: 1.5 wk
depends_on: [M2-C02, M2-C04-01]
source_files:
  - V.SMART/V.SMART.Shared/Layout/NavMenu.razor
  - V.SMART/V.SMART.Shared/Layout/MainLayout.razor
  - V.SMART/V.SMART.Shared/Layout/BlankLayout.razor
  - V.SMART/V.SMART.Shared/Components/PageHeader.razor
  - V.SMART/V.SMART.Shared/Components/SmartBackButton.razor
  - V.SMART/V.SMART.Shared/Components/UnsavedChangesModal.razor
  - V.SMART/V.SMART.Shared/Services/FinancialYearHelper.cs
  - V.SMART/V.SMART.Shared/Data/Enum/UserRole.cs
api_endpoints:
  - "GET /api/v1/me"
business_rules: []
confidence: n/a
last_verified: 2026-08-22
---

# M2-C03 — App shell: header, permission-filtered sidebar, breadcrumbs, ⌘K palette

> **Re-specified for Angular on 2026-08-22 by [`M2-C12-02`](M2-C12-02.md).** The ⛔ STOP banner
> that stood here is removed **in the same change that removed the stack-specific instructions it
> guarded**: this file no longer specifies the ADR-003 stack.
> [`ADR-007`](../../decisions/ADR-007-angular-stack.md) is the governing stack decision —
> **Angular 22.x + PrimeNG, one component library, never mixed** — and
> [KB-050](../../frontend-new/react-architecture.md), rewritten for Angular by `M2-C00`, is the
> structure this shell is built into.
>
> **The specification this replaces is not deleted from the record.** Its _intent_ survives
> unchanged: the same shell, the same two-mode sidebar, the same **rights-not-roles** filtering
> rule, the same ⌘K palette scoped to permitted screens only, the same breadcrumb derivation from
> route metadata, the same dirty-form navigation guard, and the same refusal to reproduce R-31.
> What changed is the _how_ — standalone components with signals instead of function components
> with hooks, `@angular/router` layout routes and `loadComponent` instead of a route library's
> lazy elements, a `CanDeactivateFn` instead of a navigation-blocker hook, and PrimeNG instead of
> a second component library. The `file:line` evidence about the existing Blazor implementation
> was never a framework decision and comes through byte-identical.
>
> **What did not change:** `depends_on`, `business_rules`, priority, estimate and gate.

| Field     | Value                                                                                                                                                            |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Task ID   | M2-C03                                                                                                                                                           |
| Milestone | M2 — Foundation                                                                                                                                                  |
| Parent    | — (this is a top-level task)                                                                                                                                     |
| Type      | Frontend                                                                                                                                                         |
| Priority  | P0                                                                                                                                                               |
| Estimate  | 1.5 wk                                                                                                                                                           |
| Status    | **Needs Review** — merged to `master` (`73e91e8`), still not `Completed`, both 2026-08-27; see [Close-out](#close-out-2026-08-27) and [Merge](#merge-2026-08-27) |
| Gate      | G2                                                                                                                                                               |

## Task Objective

Build the application shell described in
[KB-051 §Application shell](../../frontend-new/design-system.md#application-shell):

1. **Header** (48 px) — menu toggle, product mark, global search entry (⌘K), financial-year
   selector, tenant name, notifications slot, theme toggle, user menu.
2. **Sidebar** — the rail ↔ expanded two-mode idea kept from `NavMenu.razor`, rebuilt as a
   single-level accordion, **filtered by screen rights**, with Recent and Favourites.
3. **Breadcrumbs + page header** — title, breadcrumb trail, page-action slot, sticky footer
   action slot.
4. **⌘K command palette** — jump to any permitted screen; the extension point for record
   search comes later.
5. The **dirty-form navigation guard**, replacing `UnsavedChangesModal.razor`.

Locations, under the workspace `M2-C01` created:

- `frontend/nexgen-web/src/app/layout/shell/`, `…/layout/auth-layout/` and
  `…/layout/print-layout/` — **all three directories exist today and hold only a `.gitkeep`**
  (verified 2026-08-22). The `layout/` tree and those three names are given by
  [KB-050 §Project structure](../../frontend-new/react-architecture.md#project-structure).
- `frontend/nexgen-web/src/app/shared/components/` — `page-header/` is named by KB-050; the
  navigation primitives below it (`sidebar/`, `nav-group/`, `nav-item/`, `header/`,
  `breadcrumbs/`, `command-palette/`, `user-menu/`, `financial-year-selector/`, `tabs/`) are
  **proposed** by this task.
- `frontend/nexgen-web/src/app/core/navigation/` — the nav data and the Recent/Favourites
  services are singletons, and KB-050's rule is _"anything that must be a singleton lives in
  `core/`"_. The folder name is **proposed**.

**This task also removes `src/app/features/placeholder/`** — KB-050 records it as the scaffold's
throwaway route and names `M2-C03` as the task that removes it when the real shell lands.

> ### The rule this task exists to get right
>
> **The sidebar is filtered by SCREEN RIGHTS (`UserRight` × `Screens`), not by role.**
> Deny-by-default: a screen with **no matching `UserRight` row** is **hidden AND its route is
> refused**. The existing nav does the opposite — it gates on
> `<AuthorizeView Roles="Administrator,ERPAdmin,User">`
> (`V.SMART/V.SMART.Shared/Layout/NavMenu.razor:36`, `:148`), which admits every role that
> exists and one that does not (R-31).
>
> And, as in M2-C02: **the client filter is rendering only.** The server re-checks every
> request (ADR-004). A hidden nav item is a convenience, never a control.

## Why This Task Exists

- **It is the first screen anyone sees after login**, and it is where "this is a new product"
  is either communicated or not. KB-051 names the ⌘K palette as "the most visible _this is a
  new product_ signal".
- **`NavMenu.razor` is 888 LOC across 32 nav groups** (Confirmed: `wc -l` on
  `V.SMART/V.SMART.Shared/Layout/NavMenu.razor` = 888; the group inventory is KB-015). It has
  a mini icon rail, an expanded `MudNavMenu` tree, and a separate collapsed production-user
  menu. Reproducing it one-for-one would import 888 lines of accumulated special cases;
  rebuilding it against the permission model produces something both smaller and more correct.
- **R-31 (Confirmed).** `NavMenu.razor:36` and `:148` declare
  `<AuthorizeView Roles="Administrator,ERPAdmin,User">`, but `UserRole`
  (`V.SMART/V.SMART.Shared/Data/Enum/UserRole.cs`) contains only `Administrator` and `User`.
  `"ERPAdmin"` is a dead reference — KB-050 restates this as _"A guard written against
  `ERPAdmin` can never match; do not port it."_ More importantly, a role list containing
  _every_ real role is not a filter at all: the visible menu today is effectively ungated, and
  the real gating happens inside each page via `BaseUserRightsComponent`. This shell fixes the
  layer: **filter the navigation itself, by rights.**
- **Screen rights are the actual model.** 152 seeded `Screens` rows × five flags per user
  (Confirmed, [KB-013](../../architecture/auth-and-permissions.md#2-screen-rights--the-core-permission-model)).
  Navigation filters on `view && !hidden`, reproducing `IsHide` semantics
  ([KB-050 §Permission-based rendering](../../frontend-new/react-architecture.md#permission-based-rendering)).
- **The ERP is financial-year-scoped.** `V.SMART/V.SMART.Shared/Services/FinancialYearHelper.cs`
  exists (Confirmed) and the header must carry the FY selector, because a document list read
  under the wrong FY is a support call, not a cosmetic issue.
- **Keyboard-first is the operating assumption.** Operators run whole shifts without reaching
  for a mouse. A palette that reaches any of ~140 screens in three keystrokes is a throughput
  feature, not a flourish.
- **The bundle budget becomes real here.** `M2-C01` measured the placeholder app at
  **436.85 kB raw / 104.20 kB gzip** initial against KB-050's `< 250 KB gzip` target — 42 % of
  budget with _no_ shell, auth, grid or tokens in it (KB-050 §Performance targets). This task is
  what that baseline exists to be measured against.

## Prerequisites

| Prerequisite              | Why                                                                                                                                                                                      |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **M2-C02**                | `PermissionService`, `forScreen()`, `requireScreen()`, `AuthService`. The sidebar filter reads that service; there is nothing to filter without it.                                      |
| **M2-C04-01**             | Tokens: shell dimensions (48 px header, 240 px sidebar, 56 px rail), spacing, elevation, motion, breakpoints, and the theme-toggle component — which this task **places** in the header. |
| M2-C04-03 (soft)          | The dialog primitive for the unsaved-changes prompt, and the skeleton for the shell's loading state. If unavailable, use bare PrimeNG with a `TODO`; do not build parallel primitives.   |
| A screen-name → route map | See [Investigation Requirements](#investigation-requirements). The sidebar cannot filter by screen right without knowing which route belongs to which of the 152 `Screens` names.        |

## Dependencies

| Dependency                        | Class                            | Note                                                                                                                                           |
| --------------------------------- | -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| M2-C02                            | Hard                             | Permission service and guards. Note KB-080 §9: "M2-C03 and M2-C02 share the permission store" — they must not be parallelised.                 |
| M2-C04-01                         | Hard                             | Tokens, breakpoints, theme toggle.                                                                                                             |
| M2-A07 (`GET /api/v1/me`)         | Hard _(transitively via M2-C02)_ | Supplies user, tenant, role and the full rights set.                                                                                           |
| M2-C04-03                         | Soft                             | Overlays and skeletons.                                                                                                                        |
| M2-C04-02                         | Soft                             | The FY selector and palette input ideally use its select/combobox primitives.                                                                  |
| KB-053 page map                   | Information                      | The route inventory the screen-name mapping is built against.                                                                                  |
| M2-B09 (reference-data endpoints) | Soft                             | Financial-year list. If it does not exist, derive client-side from a documented rule and record the follow-up — do **not** invent an endpoint. |
| M2-C08                            | Testing                          | The document editor is the heaviest consumer of the dirty-form guard.                                                                          |

## Required Existing Knowledge

| Doc                                                                                                                                                                      | Why                                                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| [KB-051 §Application shell, §Component inventory (Navigation), §Responsive behaviour, §Accessibility commitments](../../frontend-new/design-system.md#application-shell) | The specification.                                                                                         |
| [KB-013 §2 Screen rights](../../architecture/auth-and-permissions.md#2-screen-rights--the-core-permission-model)                                                         | The model the sidebar filters on; deny-by-default.                                                         |
| [ADR-004](../../decisions/ADR-004-server-side-authorization.md)                                                                                                          | The client filter is rendering only.                                                                       |
| [ADR-007](../../decisions/ADR-007-angular-stack.md)                                                                                                                      | The stack; one component library, never mixed.                                                             |
| [KB-050 §Project structure, §Permission-based rendering, §Performance targets](../../frontend-new/react-architecture.md)                                                 | The `layout/` tree; nav filters on `view && !hidden`; route chunks are lazy; the measured bundle baseline. |
| [KB-015 §Layout and navigation, §Shared components](../../architecture/frontend-architecture-existing.md#layout-and-navigation)                                          | `NavMenu.razor`'s two modes, the 32 groups, `PageHeader`, `SmartBackButton`, `UnsavedChangesModal`.        |
| [KB-053 page map](../../frontend-new/page-map.md)                                                                                                                        | The 440 legacy routes and the proposed consolidation.                                                      |
| [KB-060 R-31](../../risks/technical-debt-register.md)                                                                                                                    | The dead `ERPAdmin` role.                                                                                  |

## Investigation Requirements

**One substantial new investigation — this is the task's main risk, and it must not be
guessed.**

**INV-033 — Screen-name → route mapping for navigation.** The sidebar filters by
`Screens.ScreenName`, but navigation items are routes. Nothing in the repository currently
joins the two: `BaseUserRightsComponent` requires each page to _declare_ its own
`ScreenName`, per page. Therefore:

1. Extract the nav tree from `NavMenu.razor` (32 groups, with routes) — the group list is
   already recorded in KB-015 and KB-053; **reuse it, do not re-derive**.
2. Extract each page's declared `ScreenName` from its `BaseUserRightsComponent` override.
3. Produce the join: nav item → route → screen name, and **record every unmatched entry
   explicitly** as either "no screen right governs this item" or "screen name not yet
   determined". A guessed mapping silently hides a screen from a user who should see it, or
   shows one they should not — and the second failure is invisible until the server refuses.
4. Record the result as a **typed data file in the SPA** (`navigation.config.ts`), not as
   scattered literals, so M2-B05's typed `ScreenCodes` work can later validate it
   mechanically.

Also confirm, and record:

- **R-31**: `NavMenu.razor:36` and `:148` list `Roles="Administrator,ERPAdmin,User"` while
  `UserRole` has only `Administrator` and `User`.
- **The production-user mode.** `NavMenu.razor:462-464` defines
  `ShowProductionMenu => CustomAuth?.IsQrLogin == true && CustomAuth?.IsProductionUser == true`
  (Confirmed) — it requires **both** a QR login and a production user, and collapses the menu
  to a shop-floor-only view. Determine whether `GET /api/v1/me` exposes those two facts.
  KB-050 records the shipped `/me` body as `{ userId, userName, tenantId, role, rights }`, which
  contains **neither** flag — so unless M2-A07 has since extended it, the expected outcome is
  _raise it as a blocking question for M2-A07 and ship the shell without the production mode_,
  recording the gap. Do not infer the flags from anything else.

Record INV-033 in [KB-003](../../investigation-registry.md) with per-row confidence. Check
`git branch --no-merged master` before claiming the id.

## Repository Areas

| Area                                                                                                                    | Status                                    | Role                                                                         |
| ----------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | ---------------------------------------------------------------------------- |
| `frontend/nexgen-web/src/app/layout/shell/`                                                                             | Exists, `.gitkeep` only (named by KB-050) | The authenticated shell component.                                           |
| `frontend/nexgen-web/src/app/layout/auth-layout/`                                                                       | Exists, `.gitkeep` only (named by KB-050) | The centred card layout for `/login`.                                        |
| `frontend/nexgen-web/src/app/layout/print-layout/`                                                                      | Exists, `.gitkeep` only (named by KB-050) | No chrome, for print/preview routes.                                         |
| `frontend/nexgen-web/src/app/shared/components/`                                                                        | Exists, `.gitkeep` only                   | `page-header/` (named by KB-050) plus the proposed navigation primitives.    |
| `frontend/nexgen-web/src/app/core/navigation/`                                                                          | **TO BE CREATED** (proposed)              | `navigation.config.ts`, recent/favourites services.                          |
| `frontend/nexgen-web/src/app/app.routes.ts`                                                                             | Created by M2-C01, extended by M2-C02     | Gains the shell layout route, breadcrumb route `data`, and the print routes. |
| `frontend/nexgen-web/src/app/features/placeholder/`                                                                     | Created by M2-C01                         | **Removed by this task** (KB-050 §Project structure).                        |
| `frontend/nexgen-web/src/app/core/auth/`                                                                                | Created by M2-C02                         | **Consumed, never modified.**                                                |
| `V.SMART/V.SMART.Shared/Layout/**`, `Components/PageHeader.razor`, `SmartBackButton.razor`, `UnsavedChangesModal.razor` | Exist                                     | **Read-only reference.**                                                     |

## Existing Behavior to Preserve

This task **supersedes** the Blazor shell for SPA users. All the Razor files below keep
serving Blazor unchanged.

| Superseded Razor file                                         | Behaviour and treatment                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `V.SMART/V.SMART.Shared/Layout/MainLayout.razor` (373 LOC)    | The app shell. Replaced by the `shell` layout component. Note it injects `SessionTimeoutService` at line 10 — **not ported** (R-17); the idle timeout came from M2-C02.                                                                                                                                                                                                                                                                           |
| `V.SMART/V.SMART.Shared/Layout/NavMenu.razor` (888 LOC)       | Sidebar with a mini icon rail and an expanded `MudNavMenu` tree over 32 groups. The **two-mode idea is kept** because it is genuinely good for a 32-group menu (KB-051). The **role gating at `:36` and `:148` is replaced by screen-right filtering.** The **production-user collapse** at `:462-464` (`IsQrLogin && IsProductionUser`) is preserved _only if_ `/api/v1/me` exposes both flags — otherwise it is recorded as a gap and deferred. |
| `V.SMART/V.SMART.Shared/Layout/BlankLayout.razor`             | Login / QR-login layout. Becomes the `auth-layout` component (login only; QR login is out of scope, per M2-C02).                                                                                                                                                                                                                                                                                                                                  |
| `V.SMART/V.SMART.Shared/Components/PageHeader.razor`          | Title + breadcrumbs via `PageHeader.BreadcrumbItem` (`PageHeader.razor:16,27`). Reproduced as the `page-header` component with the same trail concept.                                                                                                                                                                                                                                                                                            |
| `V.SMART/V.SMART.Shared/Components/SmartBackButton.razor`     | Context-aware back. Folded into breadcrumbs plus browser history — a separate back button in a routed SPA duplicates a control the browser already provides. Record the decision.                                                                                                                                                                                                                                                                 |
| `V.SMART/V.SMART.Shared/Components/UnsavedChangesModal.razor` | Dirty-form guard. Replaced by a functional `CanDeactivateFn` plus M2-C04-03's confirm dialog.                                                                                                                                                                                                                                                                                                                                                     |
| The `/instantSearch` page (`NavMenu.razor:404,495,812`)       | Replaced by the ⌘K palette (KB-051). The Blazor page keeps working; the SPA simply has no `/instantSearch` route.                                                                                                                                                                                                                                                                                                                                 |

**Not superseded, and explicitly out of scope:** the 32 groups' destination screens. This task
builds the frame and the nav data, not the pages behind it.

## Target Result

```
┌───────────────────────────────────────────────────────────────────────────┐
│ HEADER 48px  [☰] Mark │ Search (⌘K) │ FY ▾ · Tenant · 🔔 · ☾ · Avatar      │
├──────────┬────────────────────────────────────────────────────────────────┤
│ SIDEBAR  │  Breadcrumb                                    [page actions]  │
│ 240px    │  Page title                                                    │
│ (rail 56)│  … router-outlet …                                             │
│          │  Sticky footer: [Cancel]                    [Save] [Save+New]  │
└──────────┴────────────────────────────────────────────────────────────────┘
```

| Component / service                       | Contract                                                                                                                                                                                                                                                                      |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `layout/shell`                            | The authenticated layout route's component. Composes header, sidebar, breadcrumbs, `<router-outlet>` and the footer slot. Renders a skeleton while the permission service is bootstrapping.                                                                                   |
| `layout/auth-layout`                      | Centred card for `/login`.                                                                                                                                                                                                                                                    |
| `layout/print-layout`                     | No chrome — for print/preview routes. Created now so print routes never have to unpick the shell later.                                                                                                                                                                       |
| `sidebar`                                 | Two modes: 240 px expanded and 56 px rail, user-toggled and persisted; single-level accordion; **items filtered by `view && !hidden`**; pinned **Recent** (last visited, local) and **Favourites** (user-pinned, local for now); a filter box that searches the visible tree. |
| `nav-group` / `nav-item`                  | A group renders **only if at least one child survives the filter** — an empty group is worse than no group. `nav-item` marks the active route and exposes it via `aria-current="page"` (`routerLinkActive` supplies the state).                                               |
| `header`                                  | Menu toggle, product mark, ⌘K search entry, FY selector, tenant name, notifications slot (empty placeholder wired later), the theme toggle from M2-C04-01, and a user menu with logout from M2-C02.                                                                           |
| `breadcrumbs`                             | Derived from each route's `data`/`title` metadata, not hand-passed per page. Last crumb is the current page and is not a link.                                                                                                                                                |
| `page-header`                             | Title, breadcrumbs, status-badge slot, page-action slot.                                                                                                                                                                                                                      |
| `command-palette`                         | ⌘K / Ctrl-K. Fuzzy search over **permitted** screens only. Arrow keys, `Enter`, `Esc`, recent-first ordering. Extensible with future record-search providers, but ships with screens only.                                                                                    |
| `financial-year-selector`                 | Current FY plus switching. Sourced per the investigation; if no endpoint exists, derived from a documented rule with the follow-up recorded.                                                                                                                                  |
| `tabs`                                    | Underline style, scrollable overflow, keyboard-navigable — KB-051 lists it under Navigation, and the document editor needs it.                                                                                                                                                |
| `unsavedChangesGuard` (`CanDeactivateFn`) | Blocks route deactivation while a form is dirty, using M2-C04-03's confirm dialog; also registers a `beforeunload` handler while dirty.                                                                                                                                       |
| `core/navigation/navigation.config.ts`    | The nav tree as **typed data**: group → item → route → `screenName`. The output of INV-033.                                                                                                                                                                                   |

## Implementation Steps

1. **Run INV-033** and record it before writing the sidebar. Reuse KB-015/KB-053 for the group
   and route inventory; do not re-derive 888 lines.
2. **Create the branch** `migration/M2-C03-app-shell`.
3. **Write `core/navigation/navigation.config.ts`** from the investigation: a typed tree of
   `{ group, items: [{ label, route, screenName, icon }] }`. Every item either carries a
   `screenName` or an explicit `screenName: null` with a comment saying why no right governs
   it. **No silent omissions.**
4. **Build the three layout components** using M2-C04-01's dimension and breakpoint tokens.
   Wire the shell as a layout route (a parent route with a `component` and `children`) under
   M2-C02's `authGuard`, and remove `src/app/features/placeholder/` along with its route.
5. **Build `sidebar` / `nav-group` / `nav-item`.** Filter with `PermissionService.forScreen()`
   per item on `view && !hidden`, in a `computed()` over the rights signal; hide a group when
   every child is filtered out. Persist rail/expanded locally. Implement full keyboard
   navigation (see [Frontend Changes](#frontend-changes)).
6. **Build `header`**, placing the theme toggle (M2-C04-01) and the user menu (logout from
   M2-C02). Show the tenant name from the permission service. Leave the notifications control
   as an inert placeholder with a `TODO` — a bell that does nothing is acceptable; a bell that
   silently drops notifications is not.
7. **Build `financial-year-selector`** per the investigation's finding, with the source
   documented at the call site.
8. **Build `breadcrumbs` + `page-header`**, deriving the trail from route `data` so a page
   cannot forget to set it.
9. **Build `command-palette`.** Fuzzy-match label and route over the **filtered** nav tree
   only. Recent-first. `⌘K`/`Ctrl+K` opens from anywhere except inside a text input where the
   combination is already bound; `Esc` closes and restores focus.
10. **Build `tabs`** (underline, scrollable, arrow-key navigable).
11. **Build `unsavedChangesGuard`** as a `CanDeactivateFn` over a small `DirtyForm` interface,
    plus a `beforeunload` handler for hard navigations. Verify it blocks in-app navigation,
    browser back, and tab close.
12. **Keep every feature route lazy.** `loadComponent` / `loadChildren` with a skeleton while
    the chunk loads (KB-050 §Performance targets). **Record the shell's own gzipped size against
    the `< 250 KB gzip` budget and against M2-C01's 104.20 kB placeholder baseline** — the shell
    is the thing that budget is really about. If it lands over budget, say so; do not raise the
    budget.
13. **Write the tests** in [Testing](#testing), especially the permission-filtering ones.
14. **Verify** with the command set in [Verification Commands](#verification-commands), update
    documentation, record INV-033, review, commit.

## Files Expected to Change

| File                                                  | Change                                                                                                                                            |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `frontend/nexgen-web/src/app/app.routes.ts`           | Add the shell layout route with children, the print routes, breadcrumb `data`, and lazy `loadComponent` throughout. Remove the placeholder route. |
| `frontend/nexgen-web/src/app/app.config.ts`           | Only if a provider is genuinely needed (e.g. the palette's keyboard service). Prefer `providedIn: 'root'`.                                        |
| `frontend/nexgen-web/src/app/features/placeholder/**` | **Deleted** — the scaffold's throwaway route, which KB-050 assigns to this task.                                                                  |
| `frontend/nexgen-web/README.md`                       | Add a "Navigation and shell" section, including the rights-not-roles rule and how to add a nav item.                                              |
| `docs/kb/frontend-new/design-system.md` (KB-051)      | Record the shell as built, with any deviation and its reason. Bump `last_verified`.                                                               |
| `docs/kb/frontend-new/react-architecture.md` (KB-050) | Confirm the nav-filtering rule and the lazy-loading approach; record the shell's measured bundle size against the baseline.                       |
| `docs/kb/frontend-new/page-map.md` (KB-053)           | Add the screen-name column produced by INV-033, or link to `navigation.config.ts` as its source.                                                  |
| `docs/kb/risks/technical-debt-register.md` (KB-060)   | R-31: record that the SPA shell does not reproduce the role gating; the Blazor dead-role reference remains open.                                  |
| `docs/kb/investigation-registry.md` (KB-003)          | Add **INV-033**.                                                                                                                                  |
| `docs/kb/open-questions.md` (KB-004)                  | Add the production-user-mode question if `/api/v1/me` lacks the flags.                                                                            |
| `docs/kb/execution/task-tracker.md` (KB-081)          | M2-C03 → the status the workflow allows the session to set.                                                                                       |

## Files Expected to Be Created

Paths marked ★ are named by
[KB-050 §Project structure](../../frontend-new/react-architecture.md#project-structure); the rest
are **proposed** by this task. Every component is standalone; each is a `.component.ts` +
`.component.html` (+ `.component.scss` where it needs one) with a `.spec.ts` alongside.

- `src/app/layout/shell/shell.component.*` ★
- `src/app/layout/auth-layout/auth-layout.component.*` ★
- `src/app/layout/print-layout/print-layout.component.*` ★
- `src/app/shared/components/page-header/` ★
- `src/app/shared/components/`: `sidebar/`, `nav-group/`, `nav-item/`, `header/`, `user-menu/`,
  `financial-year-selector/`, `breadcrumbs/`, `command-palette/`, `tabs/`
- `src/app/core/navigation/`: `navigation.config.ts`, `navigation.models.ts`,
  `recent-screens.service.ts`, `favourites.service.ts`, `unsaved-changes.guard.ts`
- `src/app/core/navigation/README.md` — how to add a nav item, and the requirement that every
  item declare a screen name or an explicit exemption
- `e2e/shell.spec.ts` — a keyboard pass through the sidebar and palette

## Files That Must Not Change

- `V.SMART/**` — including all four Layout files and `NavMenu.razor`. The Blazor shell keeps
  serving Blazor users for the whole strangler period. **In particular, do not "fix" R-31 in
  `NavMenu.razor`** — that is a separate Blazor task; this one declines to reproduce the bug.
- `frontend/nexgen-web/src/app/core/auth/**` — owned by M2-C02. If the permission service lacks
  something the sidebar needs, **report it**; do not extend it here.
- `frontend/nexgen-web/src/styles/tokens.css` and `src/app/core/theme/**` — owned by M2-C04-01.
- `src/app/shared/components/form-field/`, `confirm-dialog/`, `busy-overlay/`,
  `empty-state/`, `error-state/` — owned by M2-C04-02/03.
- `frontend/vsmart-erp/**` — the pilot's disposition is `M2-C11`'s and is open on **Q-38**.
- `docs/ARCHITECTURE.md` — superseded (R-37).

## API Changes

Not applicable — this task adds no HTTP surface.

It **consumes** `GET /api/v1/me` (M2-A07) indirectly, through M2-C02's permission service; it
issues no request of its own. Two consumption facts must be recorded rather than assumed:

- Whether `/me` exposes `IsQrLogin` and `IsProductionUser`, which the Blazor production menu
  requires (`NavMenu.razor:462-464`). KB-050's recorded body has neither. If they are still
  absent → raise for M2-A07, ship without the production mode.
- Where the financial-year list comes from. If no endpoint exists (M2-B09), derive it
  client-side from a documented rule and record the follow-up. **Do not invent an endpoint.**

## Frontend Changes

| Aspect                   | Detail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Route**                | The shell as a layout route (parent `component` + `children`) under M2-C02's `authGuard`; `auth-layout` for `/login`; `print-layout` for print routes. Breadcrumb data comes from each route's `data`. Every feature route uses `loadComponent` / `loadChildren`. **No `/instantSearch` route** — the palette replaces it. The placeholder route is deleted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Component**            | Standalone components only, no `NgModule`: shell, auth-layout, print-layout, sidebar, nav-group, nav-item, header, user-menu, financial-year-selector, breadcrumbs, page-header, command-palette, tabs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **API integration**      | n/a directly. All identity, tenant and rights data comes from M2-C02's services. The FY list is either a reference-data call through a typed service over `HttpClient` (M2-B09) or a documented client derivation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **State**                | Read-only from M2-C02's services, consumed as signals and narrowed with `computed()`. Locally owned and persisted: sidebar mode (rail/expanded), Recent screens, Favourites, palette recents — each in a root-provided service holding signals exposed via `asReadonly()`. Nothing server-derived is duplicated into them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Validation**           | n/a — the shell has no form. The palette's input is a search box, not a validated field.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Permissions**          | **The core of this task.** Nav items are filtered on `view && !hidden` from `PermissionService.forScreen(screenName)`; an item whose screen key is absent is hidden (deny-by-default, matching `RightsHelper.cs`). A group with no surviving children does not render. The command palette searches **only the filtered tree** — otherwise it becomes a directory of screens the user cannot open, which is both a poor experience and an information leak about the tenant's configuration. Every navigable route additionally carries `requireScreen`, so a hand-typed URL is refused, not merely un-linked. **All of this is rendering only; the server re-checks every request (ADR-004).** Role is **not** used for filtering anywhere — R-31's `Roles="Administrator,ERPAdmin,User"` pattern is not reproduced.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Loading state**        | While the permission service is bootstrapping, the shell renders a **shell-shaped skeleton** — header bar, sidebar column, content block — never a spinner and never a flash of an empty sidebar, which reads as "you have no permissions". Route transitions show a content-area skeleton while the lazy chunk loads; the shell itself never unmounts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Empty state**          | A user with **zero** permitted screens gets an explicit sidebar message — "No screens are available for your account. Contact your administrator." — not an empty rail. The palette with no matches shows "No screens match '<query>'". Recent and Favourites, when empty, show a one-line explanation rather than blank space.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Error state**          | If `navigation.config.ts` references a screen name absent from the rights map, the item is hidden **and the mismatch is logged once in development** — a silent mismatch is exactly how a screen goes missing for a whole tenant. A failed FY load renders an inline error in the header control with retry, and does **not** take down the shell. A route-level error renders an error surface inside the content area with the shell intact, so the user can navigate away.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Accessibility**        | **Landmarks:** `<header>`, `<nav aria-label="Main">`, `<main>`, with a skip-to-content link as the first focusable element. **Keyboard interaction:** sidebar — `Tab` reaches the nav, arrow keys move between items, `Enter`/`Space` activates, `Right`/`Left` expand/collapse a group, `Home`/`End` jump; the rail/expand toggle has an accessible name and `aria-expanded`; the active item carries `aria-current="page"`. Palette — `⌘K`/`Ctrl+K` opens from anywhere except where the combination is already bound in a text input, `ArrowUp`/`ArrowDown` move, `Enter` navigates, `Esc` closes and **restores focus to the invoker**; the listbox uses `aria-activedescendant` and results are announced politely. Breadcrumbs — an ordered list in a `<nav aria-label="Breadcrumb">`, last crumb `aria-current="page"` and not a link. Tabs — arrow keys move, `Home`/`End` jump, `role="tablist"`/`tab`/`tabpanel` wired. User menu — a menu button with `aria-haspopup`, arrow-key navigation, `Esc` closes and restores focus. All: visible 2 px focus ring, `prefers-reduced-motion` honoured on sidebar and palette transitions, `axe` clean in both themes, functional at 200% zoom. `eslint.config.js` runs `angular-eslint`'s template accessibility rules as **errors**, so template-level a11y defects fail `npm run lint`. |
| **Responsive behaviour** | Per [KB-051 §Responsive behaviour](../../frontend-new/design-system.md#responsive-behaviour) using M2-C04-01's breakpoints: `≥1440` full 240 px sidebar; `1024–1439` collapses to the 56 px rail; `768–1023` becomes an overlay drawer with a focus trap and `Esc` to close; `<768` read-and-approve only — the shell must still be fully navigable there, since approvals run on phones. The header's FY selector and tenant name collapse into the user menu below `1024` rather than being dropped: FY is not optional information.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

## Business Logic

**Newly introduced — presentation behaviour only.**

The behaviour added is navigation: composing a layout, filtering a menu against a rights map,
deriving breadcrumbs from routes, searching a screen list, and blocking navigation away from a
dirty form.

**No ERP business rule is implemented.** In particular:

- Permission **enforcement** is server-side (ADR-004). This task renders a filtered menu; it
  decides nothing.
- The financial-year selector **selects** an FY; it does not compute financial-year
  boundaries. Those live in `FinancialYearHelper.cs` server-side, and any FY-derived value the
  UI shows must come from the server.
- The unsaved-changes guard is a UX guard over a form's dirty state; it applies no rule about
  what may be saved.

## Testing

Tests run on **Vitest through `@angular/build:unit-test`** with `@testing-library/angular`, plus
Playwright for the keyboard end-to-end pass — the harness `M2-C01` established and KB-083
verified.

| Test                             | Assertion                                                                                                                                                                                                                                                             |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sidebar.permissions.spec.ts`    | With rights for 3 of 10 screens, exactly 3 items render. An item whose screen key is **absent** from the map does not render (deny-by-default). An item with `view: true, hidden: true` does not render. A group whose children are all filtered out does not render. |
| `sidebar.roles.spec.ts`          | Changing the user's `role` between `Administrator` and `User`, with rights held constant, changes **nothing** in the sidebar — the explicit R-31 regression test.                                                                                                     |
| `sidebar.keyboard.spec.ts`       | Arrow keys move, `Enter` activates, `Right`/`Left` expand/collapse, `Home`/`End` jump, the active item has `aria-current="page"`, the rail toggle exposes `aria-expanded`.                                                                                            |
| `sidebar.empty.spec.ts`          | A user with zero permitted screens sees the explanatory message, not an empty rail.                                                                                                                                                                                   |
| `command-palette.spec.ts`        | Only permitted screens are searchable; a screen the user lacks rights to **never appears**, even by exact name. `⌘K`/`Ctrl+K` opens; arrows move; `Enter` navigates; `Esc` closes and restores focus; no-match shows the empty message; recents are ordered first.    |
| `breadcrumbs.spec.ts`            | The trail is derived from route metadata; the last crumb is `aria-current="page"` and is not a link.                                                                                                                                                                  |
| `shell.loading.spec.ts`          | While the permission service is bootstrapping, a shell-shaped skeleton renders — never an empty sidebar.                                                                                                                                                              |
| `shell.error.spec.ts`            | A route error renders the error surface inside the content area with header and sidebar still usable.                                                                                                                                                                 |
| `navigation.config.spec.ts`      | Every entry has either a `screenName` or an explicit `null` with a comment; no duplicate routes; every route referenced exists in the router.                                                                                                                         |
| `unsaved-changes.guard.spec.ts`  | Blocks in-app navigation while dirty; the confirm dialog's Discard proceeds and Cancel stays; a clean form does not block; `beforeunload` is registered only while dirty.                                                                                             |
| `responsive.spec.ts`             | At the three breakpoints the sidebar is expanded, rail, and overlay-drawer respectively; the drawer traps focus and closes on `Esc`.                                                                                                                                  |
| `a11y.spec.ts`                   | `axe` over the shell in both themes, expanded and rail: zero critical violations. Landmarks and the skip link are present.                                                                                                                                            |
| `e2e/shell.spec.ts` (Playwright) | A real-browser keyboard pass: skip link → sidebar → palette → navigate, no mouse. jsdom computes no layout, so the responsive and focus-order claims need a real browser to mean anything.                                                                            |

## Acceptance Criteria

- [x] The shell, auth-layout and print-layout components exist; the shell is the layout route
      under `authGuard`, and `src/app/features/placeholder/` is deleted.
- [x] The sidebar is filtered **only** by screen rights (`view && !hidden`). Role is not read
      anywhere in the navigation code — proven by `sidebar.roles.spec.ts` and by
      `git grep -n "role" -- frontend/nexgen-web/src/app/shared/components` returning no
      permission use (only that spec's own documentation of the check).
- [~] An item whose screen key is absent from the rights map is hidden, **and** its route is
  refused by `requireScreen` when typed directly. Both are true and tested — but only
  `/dashboard` is a real, registered route today; the other ~144 destination routes are
  nav _data_ only (`navigation.config.ts`'s own file header), so "refused when typed
  directly" is demonstrated on the one real route, not independently proven for routes
  `M2-D01` onward has not built yet.
- [x] A nav group with no surviving children does not render.
- [x] `core/navigation/navigation.config.ts` exists as the single nav data source; every entry
      has a `screenName` or an explicit `null` with a stated reason; `navigation.config.spec.ts`
      passes.
- [x] The command palette searches **only** permitted screens; a screen the user lacks rights
      to is unreachable through it even by exact name.
- [x] `⌘K` / `Ctrl+K` opens the palette; `Esc` closes it and restores focus to the invoker —
      unit-tested and confirmed in a real browser (`e2e/shell.spec.ts`).
- [x] The sidebar supports both the 240 px expanded and 56 px rail modes, user-toggled and
      persisted, reproducing `NavMenu.razor`'s two-mode idea. **Disclosed simplification:**
      rail mode's group flyout is an `app-popover` anchored to the icon, not a port of the
      Blazor mini-rail's flyout-into-the-top-bar mechanic — see `nav-group.component.ts`.
- [x] The header carries: menu toggle, ⌘K entry, financial-year selector, tenant name,
      notifications placeholder, the theme toggle (from M2-C04-01) and a user menu with logout.
      **Disclosed gap:** "tenant name" is `Tenant ${tenantId}` — `/me` deliberately carries no
      display name (`MeController.cs`'s own doc comment, R-01) — not a real name yet.
- [x] Breadcrumbs derive from route metadata, not per-page inputs; the last crumb is
      `aria-current="page"` and is not a link.
- [x] `unsavedChangesGuard` blocks in-app navigation, browser back and tab close while dirty,
      and does nothing when clean — built and unit-tested in isolation. **No real consumer
      exists yet** to exercise it end-to-end: no dirty form exists anywhere in this codebase
      today (`M2-C08`, the document editor, is the first candidate).
- [x] The shell renders a shell-shaped skeleton while bootstrapping — never an empty sidebar.
      As in `M2-C02`, this state is currently unreachable in normal operation (the in-memory
      token custody decision means `authGuard` always finishes bootstrapping before any route
      activates) — built defensively per the criterion's own wording, not because it is
      exercised today.
- [x] A user with zero permitted screens sees an explicit explanation — in both the sidebar
      and the dashboard content area.
- [~] Landmarks (`header`, `nav aria-label="Main"`, `main`) and a skip-to-content link are
  present; every documented keyboard model is asserted by a `user-event` test; `axe`
  reports zero critical violations in both themes and both sidebar modes. **Done:**
  landmarks, skip link, and every documented keyboard model (unit tests plus a real-browser
  pass, `e2e/shell.spec.ts`). **Not done:** no automated `axe` scan was run — the same
  disclosed gap `M2-C02` left for `/login` and the idle dialog.
- [~] Responsive behaviour matches KB-051 at all four breakpoints; the `768–1023` overlay
  drawer traps focus and closes on `Esc`. **Done:** band detection itself is unit-tested
  at all four widths (`breakpoint.service.spec.ts`) and drives the correct sidebar mode
  (`header.component.spec.ts`, `shell.component.ts`'s `sidebarMode`). **Not done:** the
  `sm`-band overlay drawer's own rendering, focus trap and `Esc` behaviour were not
  exercised in a real narrow viewport — `e2e/shell.spec.ts` runs at a fixed 1600 px (`lg`)
  width; jsdom computes no layout so a unit test cannot stand in for this. The drawer's
  trap/`Esc` mechanics are PrimeNG's own `p-drawer`, relied upon rather than
  independently re-verified — same trust boundary `app-modal`/`app-drawer` already take
  elsewhere in this codebase.
- [x] Feature routes are lazily loaded via `loadComponent` / `loadChildren`; **the shell's
      gzipped initial size is recorded** against KB-050's `< 250 KB gzip` target and against
      M2-C01's 104.20 kB placeholder baseline. **Observed:** initial bundle 626.04 kB raw /
      **144.92 kB gzip transfer** — 58% of the 250 kB target, comfortably inside it, up from
      the 104.20 kB placeholder baseline (expected: a real shell vs. an empty scaffold).
      **Also observed, disclosed rather than hidden:** the CLI's own separate raw-byte budget
      in `angular.json` (600 kB) is exceeded by 26.04 kB (+4.3%) — a warning, not a build
      failure, and a different, stricter threshold than this criterion's actual gzip target.
      Not investigated further given time; a reasonable follow-up for a bundle-analysis task.
- [x] **INV-033** is recorded, including every unmatched nav item, with per-row confidence.
- [x] R-31 is recorded as **not reproduced** in the SPA, with the Blazor defect left open.
- [x] The production-user mode (`NavMenu.razor:462-464`, `IsQrLogin && IsProductionUser`) is
      either implemented from real `/me` data or explicitly deferred with a raised question —
      never inferred. **Deferred, not implemented** — `/me` carries neither flag (confirmed
      directly from `MeController.cs`, matching KB-050's own recorded body). **No new
      question was raised**: `INV-042` (M2-A07) already investigated and answered this exact
      question — "the API has no QR-login path at all, so the flag alone changes nothing
      today" — reused per this repository's own discipline against re-deriving an already-
      investigated fact, rather than opening a duplicate `Q-xx` for a question already closed.
- [x] No file under `V.SMART/` changed, and no file under `frontend/vsmart-erp/` changed —
      confirmed directly via `git diff --name-only`.
- [~] `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run test:ci`,
  `npm run build` and `npm run e2e` all pass. `typecheck`/`lint`/`format:check`/`build`/
  `e2e` all clean, repeatedly. `test:ci`: every one of this task's own new/changed spec
  files passes cleanly in isolation, every time checked. Full-suite runs show intermittent
  wider cascades among **pre-existing, unrelated** PrimeNG-heavy specs (data-grid,
  combobox, date-picker, select, confirm-dialog, record-picker) under this workstation's
  variable load — see Close-out for the full investigation, including two real
  test-infrastructure leaks this task found and fixed along the way.

## Verification Commands

Every `npm` command below appears verbatim in
[KB-083 § Verified frontend commands](../prompt-template.md#verified-repository-commands),
measured on this workstation on 2026-08-21. Run them from `frontend/nexgen-web/`.

```bash
npm run typecheck
```

```bash
npm run lint
```

```bash
npm run format:check
```

```bash
npm run test:ci
```

```bash
npm run build
```

```bash
npm run e2e
```

Plus these repository commands (verified working, INV-029), run from the repository root:

```bash
git grep -n "role" -- frontend/nexgen-web/src/app/shared/components
```

```bash
git diff --name-only -- V.SMART
```

> `npm run build` prints the initial and lazy chunk sizes, raw and estimated-transfer; that
> output **is** the bundle-budget evidence and must be quoted, not summarised.
> `npm run e2e` requires `npx playwright install chromium` once per machine.
> `git diff --name-only -- V.SMART` must print nothing.

## Documentation Updates

| Document                                             | Update                                                                                                                                                                                                                  |
| ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [KB-051](../../frontend-new/design-system.md)        | Record the shell as built: header contents, sidebar modes, palette scope, breadcrumb derivation, and any deviation with its reason. Bump `last_verified`.                                                               |
| [KB-050](../../frontend-new/react-architecture.md)   | Confirm the `view && !hidden` nav rule as implemented; record the shell's measured bundle size in §Performance targets alongside the M2-C01 baseline; remove `features/placeholder/` from the realised-structure block. |
| [KB-053](../../frontend-new/page-map.md)             | Add the screen-name column from INV-033, or reference `navigation.config.ts` as its source of truth.                                                                                                                    |
| [KB-013](../../architecture/auth-and-permissions.md) | Extend the "SPA client" subsection: navigation is filtered by screen rights, not role; the filter is rendering only.                                                                                                    |
| [KB-060](../../risks/technical-debt-register.md)     | R-31: the SPA shell does not reproduce the role gating; the Blazor dead-role reference remains **open**.                                                                                                                |
| [KB-003](../../investigation-registry.md)            | Add **INV-033 — screen-name → route mapping for navigation**.                                                                                                                                                           |
| [KB-004](../../open-questions.md)                    | The production-user-mode question, if `/me` lacks `IsQrLogin` / `IsProductionUser`.                                                                                                                                     |
| [KB-081](../task-tracker.md)                         | M2-C03 → the status the workflow allows the session to set.                                                                                                                                                             |
| `frontend/nexgen-web/README.md`                      | "Navigation and shell": how to add a nav item, the rights-not-roles rule, and the requirement that every nav item declare a screen name or an explicit exemption.                                                       |

## Investigation Registry Updates

Add **INV-033**:

```yaml
Finding:        Mapped <n> navigation items across 32 groups from NavMenu.razor to their
                routes and to the Screens.ScreenName each is governed by. <m> items have no
                governing screen right and are recorded as exempt with a reason; <k> items
                could not be mapped and are recorded as Unknown. The mapping is stored as
                typed data in
                frontend/nexgen-web/src/app/core/navigation/navigation.config.ts.
                Confirmed alongside: NavMenu.razor:36 and :148 declare
                Roles="Administrator,ERPAdmin,User" while UserRole contains only
                Administrator and User (R-31); NavMenu.razor:462-464 defines
                ShowProductionMenu as IsQrLogin && IsProductionUser.
Evidence:       V.SMART/V.SMART.Shared/Layout/NavMenu.razor:36,148,404,462-464,495,812 ;
                V.SMART/V.SMART.Shared/Data/Enum/UserRole.cs ;
                V.SMART/V.SMART.Shared/Shared/BaseUserRightsComponent.cs ;
                KB-015, KB-053
Business rule:  n/a
Confidence:     Confirmed per mapped row; Unknown recorded explicitly per unmapped row
Last verified:  <date of execution>
```

Every unmapped row must appear individually. A mapping table that hides its gaps is worse
than no table, because the next session will trust it.

## Completion Conditions

- All acceptance criteria met, with each verification command's **observed** output recorded —
  not its expected output.
- INV-033 recorded, gaps included.
- The shell's measured bundle size recorded against the budget, whatever it turns out to be.
- The branch is left for review, unmerged.
- **Only the repository owner may set `Completed`**
  ([KB-088](../workflow.md#who-may-set-completed)).

## Close-out (2026-08-27)

**Delivered:** the full app shell on branch `migration/M2-C03-app-shell` — `core/navigation/`
(`navigation.models.ts`, `navigation.config.ts` — INV-033's output, ~145 nav items — plus
`nav-filter.service.ts`, `sidebar-mode.service.ts`, `recent-screens.service.ts`,
`favourites.service.ts`, `financial-year.ts`/`financial-year.service.ts`, `fuzzy-match.ts`,
`unsaved-changes.guard.ts`), `core/theme/breakpoint.service.ts`, `layout/shell/`,
`layout/auth-layout/`, `layout/print-layout/`, and the `shared/components/` navigation
primitives (`sidebar/`, `nav-group/`, `nav-item/`, `header/`, `user-menu/`,
`financial-year-selector/`, `breadcrumbs/`, `page-header/`, `tabs/`, `command-palette/`),
`features/dashboard/` (replacing `M2-C02`'s placeholder), and the real `app.routes.ts` tree.
**Observed verification, this session:**

```
npm run typecheck       → clean
npm run lint             → All files pass linting (0 warnings)
npm run format:check     → clean except 2 pre-existing files this branch never touched
npm run build            → clean; initial bundle 626.04 kB raw / 144.92 kB gzip transfer
npm run e2e              → 2/2 passing (Playwright, chromium, live dev server, network-mocked auth)
npm run test:ci           → 682/682 in isolated runs of every new/changed file; see the
                            full-suite flakiness investigation below
git grep -n "role" -- frontend/nexgen-web/src/app/shared/components
                        → hits only in sidebar.roles.spec.ts's own documentation/test, no
                          production file
git diff --name-only -- V.SMART                → empty
git diff --name-only -- frontend/vsmart-erp     → empty
```

**INV-033, the task's own main risk, resolved as instructed — reused KB-015/KB-053, did not
re-derive them.** The authoritative source is the expanded `<MudNavMenu>` tree in
`NavMenu.razor` (not the mini-rail's `NavGroups` dictionary, which genuinely disagrees with
it on two points — see `navigation.config.ts`'s own file header). A dedicated investigation
agent mapped 143 of 144 route slugs directly to their declaring page and `ScreenName`,
independently spot-verified against source for the two most consequential findings before
being trusted; one slug (`stockIssReqList`, "Stock Issue-Request") the agent never saw — an
item present in the expanded tree but absent from the mini-rail dictionary I had partly
cross-referenced when compiling the slug list — was found and resolved directly. **Real,
disclosed findings from the mapping, not glossed over:**

- `RateComparison.razor` has no `BaseUserRightsComponent` override and no `ScreenName` at
  all — Confirmed by reading the file directly. Modelled as `screenName: null`, a genuinely
  distinct, never-gated state (`NavFilterService.isVisible` branches on it explicitly) — not
  a placeholder for "unmapped".
- Two likely copy-paste defects in the Blazor source, reproduced verbatim per KB-002 (current
  source wins), not silently corrected: `Rejection.razor` (the _Rejection Analysis_ report)
  declares `ScreenName => "Income"`, so its visibility is actually governed by the Income
  Master's `UserRight` row; `SalesPoPending.razor` and `LabourPending.razor` both declare
  `ScreenName => "Po Pendings"`, so one right gates both reports.
- Several nav `Href`s and their pages' actual `@page` casing disagree (`costCenter` vs.
  `/costcenter`, `enquiryFeasibility` vs. `/enquiryfeasibility`, `EnquiryPurchase` vs.
  `/enquiryPurchase`, `serviceBills` vs. `/ServiceBills`) — Blazor's own route matching is
  case-insensitive so nothing is broken today, but the inconsistency is recorded rather than
  silently normalised away.
- `master-upload` ("Master Upload") has a real `ScreenName` but no KB-053 proposed route —
  KB-053's own "removed (20 routes)" list groups it with the `-home`/`-master` landing pages
  it retires. Included with a provisional route (`/masters/upload`) rather than dropped;
  flagged for whichever task builds this destination to revisit.

**A real architectural boundary violated and caught, not silently worked around.** An
existing repo-wide scan test (`permission-denied-state.component.spec.ts`) asserts nothing
under `shared/components/` imports from the authentication module — `sidebar.component.ts`
originally injected `PermissionService` directly and failed that scan. Fixed by extracting
`NavFilterService` into `core/navigation/` as the one seam: `shared/components/` stays
auth-decoupled, matching the discipline `PermissionDeniedStateComponent` already
established, and `NavFilterService`'s own real deny-by-default logic is tested against a
real `PermissionService` in `core/navigation/nav-filter.service.spec.ts`, separately from
`SidebarComponent`'s presentational specs (which stub `NavFilterService`).

**A real, deterministic bug found by the e2e pass, not by inspection.** The task's own
`app.routes.ts` comment for `M2-C02` claimed the guard/component split proves the
missing-right pattern; the placeholder never actually exercised it. This task's own e2e
authoring surfaced three more real defects before `e2e/shell.spec.ts` passed: an
unfocused-input keyboard-typing race (fixed by explicitly clicking the field first), an
ambiguous two-`<header>` locator (the shell's own header and the dashboard's page-header both
render `<header>`), and an assumption that the default Playwright viewport renders the full
240 px sidebar — it renders the 56 px rail (`md` band), fixed by setting the viewport
explicitly for that assertion. None of these were component bugs; all were test-authoring
gaps a real browser pass is specifically good at catching.

**Two real test-infrastructure leaks found and fixed, both traced to root cause rather than
written off as environment flakiness:**

1. New specs (`breakpoint.service.spec.ts`, `header.component.spec.ts`) installed a
   `matchMedia` mock without `afterEach` cleanup, leaking across this workspace's shared
   jsdom worker (`test-setup.ts`'s own documented R-76 concern) and cascading into ~14
   unrelated files failing. Fixed with the same cleanup discipline `theme.service.spec.ts`
   already established.
2. `financial-year-selector.component.spec.ts` and `command-palette.component.spec.ts` open
   a PrimeNG overlay (`p-select`, `p-dialog`) and need the project's own
   `installMatchMedia()` fixture (`shared/components/form/jsdom-overlay-support.ts`) before
   doing so — the first version of the FY-selector spec omitted it, throwing inside
   PrimeNG's own `Overlay` computed-signal graph and corrupting the shared Angular runtime
   for the rest of that worker's run. Fixed by using the existing, documented fixture.
3. `unsaved-changes.guard.spec.ts` created a component via a bare `TestBed.createComponent()`
   (not `@testing-library/angular`'s `render()`, which auto-cleans up) and left one test's
   fixture undestroyed — its live `effect()` kept running and reacting across every
   subsequent spec in the worker, producing a garbled-string failure signature in unrelated
   PrimeNG-heavy specs. Fixed by calling `fixture.destroy()` explicitly.

**Full-suite `test:ci` runs remain intermittently noisy after both fixes, disclosed rather
than hidden.** Every one of this task's own new/changed spec files passes cleanly, every
time, in isolated `--include` runs (confirmed repeatedly, including immediately before this
Close-out was written). Full-suite runs show a widening cascade of failures among
**pre-existing, unrelated** PrimeNG-heavy specs (data-grid, combobox, date-picker, select,
confirm-dialog, record-picker) that correlates directly with this workstation's measured
`environment:` setup duration in the test runner's own timing output (200s–1600s across
consecutive runs with no code change between them, versus ~2–20s for the same files run in
isolation) — the signature of workstation load, not a deterministic leak: `--isolate` (full
process separation between spec files) was tried as a diagnostic and did **not** eliminate
the cascade, which a genuine cross-file jsdom leak would predict it should. The one test that
recurs across the _original_, pre-`M2-C03` baseline most often is
`record-picker-dialog.component.spec.ts`'s "refuses disabled rows" case, already documented
as pre-existing, unrelated flakiness earlier in this same session's `M2-C02` merge
verification — unchanged by this branch.

**Genuinely open, disclosed rather than glossed over — see each acceptance criterion's own
`[~]` note above for the full detail:** no automated `axe` scan; the `sm`-band overlay
drawer's real rendering/focus-trap was not exercised in a real narrow viewport; the header's
"tenant name" is a numeric placeholder, not a real display name (`/me` has none, by M2-A07's
own deliberate design); `unsavedChangesGuard`/`useBeforeUnloadGuard` have no real consumer
yet to exercise them end-to-end; the CLI's raw-byte bundle budget (600 kB) is exceeded by
26.04 kB even though the task's actual gzip target is met with room to spare; only
`/dashboard` is a real, registered destination route today — the other ~144 are nav data
ahead of the screens `M2-D01` onward will add one at a time.

Left at **`Needs Review`**, not `Completed` — per
[KB-088 § Who may set COMPLETED](../workflow.md#who-may-set-completed), and independently
because of the `[~]` acceptance criteria above. The branch was unmerged at the time this
Close-out was first written. See § Merge below for how that changed.

## Merge (2026-08-27)

Branch merged to `master` on owner instruction ("merege" — merge), `git merge --no-ff
migration/M2-C03-app-shell`, clean, no conflicts (`73e91e8`). As with `M2-C02`
(`task-tracker.md` footnote ¹⁰⁶), this was specifically an instruction to merge, not the
"looks fine" review sign-off that sets `Completed` — per
[KB-088](../workflow.md#who-may-set-completed), status stays **`Needs Review`** until the
owner separately and explicitly says so. The disclosed gaps recorded throughout this
Close-out are **unchanged by the merge, not retracted**: no automated `axe` scan; the
`sm`-band drawer untested at a real narrow viewport; the tenant-name placeholder;
`unsavedChangesGuard` has no consumer yet; only `/dashboard` is a real route; the CLI raw
bundle-budget warning. All `[~]` acceptance criteria stay `[~]`.

**Post-merge verification re-run on the merged result, all observed:** `lint`/`typecheck`
clean; `format:check` flags the same 2 pre-existing files as the branch, neither touched
here; `build` clean, bundle unchanged (626.04 kB raw / 144.92 kB gzip, same disclosed 26.04 kB
CLI raw-budget warning). `test:ci` run twice: first 680/682 —
`data-grid.component.spec.ts` (timeout) and `record-picker-dialog.component.spec.ts`
(the same pre-existing flake documented at `task-tracker.md` footnote ¹⁰⁴/¹⁰⁶), neither
touched by this task; second run 682/682 clean — matching the self-healing signature already
characterised for this same pair of specs during `M2-C02`'s merge, not a regression. `e2e`
2/2 post-merge. Backend untouched by this merge (no `V.SMART` files in the diff); `dotnet
build` not re-run. Full detail: `task-tracker.md` footnote ¹⁰⁹.

**Downstream effect.** Nothing currently names `M2-C03` in `depends_on`, so there is no
dependent to release yet. `M2-C03` itself stays `Needs Review` until the owner reviews this
Close-out and explicitly sets `Completed`.

## Git Strategy

| Field                  | Value                                                                                                                                                                                                                                                                                         |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Commit                 | `M2-C03: Add app shell with permission-filtered sidebar, breadcrumbs and command palette`                                                                                                                                                                                                     |
| Expected changed files | New `frontend/nexgen-web/src/app/layout/**`, `src/app/shared/components/**` (navigation primitives) and `src/app/core/navigation/**`; modified `src/app/app.routes.ts`, `frontend/nexgen-web/README.md`; deleted `src/app/features/placeholder/**`; plus the KB documents listed above.       |
| Rollback               | Revert the branch's merge commit (`73e91e8`). The shell is the entry point for every later feature route, so if any feature screen has merged on top, revert in reverse dependency order. Nothing is persisted server-side; only local sidebar-mode, Recent and Favourites keys become inert. |

**Merged to `master` (`73e91e8`) on owner instruction, 2026-08-27 — see § Merge above.** Not
pushed to the remote — that still needs its own explicit instruction. The review this section
originally called for (a keyboard-only pass through the sidebar and palette; a check of the
sidebar under a **low-privilege** test user) has not been separately confirmed as done by the
owner — merging is not itself that review, per [KB-088](../workflow.md#who-may-set-completed).
