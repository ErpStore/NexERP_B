---
doc_id: TASK-M2-C06
title: RecordPickerDialog — the DetailsModal replacement
module: execution
task_id: M2-C06
milestone: M2
parent_task: n/a
task_type: Frontend
priority: P0
status: Not Started
estimate: 1 wk
depends_on: [M2-C05-01]
source_files:
  - V.SMART/V.SMART.Shared/Components/DetailsModal.razor
  - V.SMART/V.SMART.Shared/Components/MasterModal.razor
  - V.SMART/V.SMART.Shared/Components/CustomerSelection.razor
  - V.SMART/V.SMART.Shared/Components/VendorSelection.razor
  - V.SMART/V.SMART.Shared/Services/ExcelExportService.cs
  - V.SMART/V.SMART.Shared/Pages/SalesAndLabour_pages/SalesPo_Pages/MfgPOUpsert.razor
api_endpoints:
  - "GET /api/v1/{resource}"
business_rules: []
confidence: n/a
last_verified: 2026-08-12
---

# M2-C06 — RecordPickerDialog — the DetailsModal replacement

> # ⛔ STOP — this specification is superseded. Do not implement it as written.
>
> **[`ADR-007`](../../decisions/ADR-007-angular-stack.md) replaced React with Angular + PrimeNG on
> 2026-08-20.** Everything below that names **React, Vite, Mantine, TanStack, Zustand, React Hook
> Form, Zod, `.tsx`, hooks or JSX is stale.** Its *intent* is still valid; its *implementation
> detail is not.*
>
> **If you are a runner and selected this task: stop and report.** Re-specifying it is a
> documentation change requiring the owner, not something to infer mid-implementation. Building
> React here would add to work that is already being discarded.
>
> **What survives, and is the reason this file was not deleted:** the acceptance criteria's
> *intent*, the ERP behaviour being reproduced, the `file:line` evidence about the **existing
> Blazor** implementation, the business rules, and the dependency relationships. None of that was
> ever a React decision.
>
> **What must be re-derived:** every stack-specific instruction, file path under
> `frontend/nexgen-web/`, component API, test-harness detail and command.
>
> **Gating task:** **`M2-C00`** rewrites [KB-050](../../frontend-new/react-architecture.md) for
> Angular. Until that lands there is no authoritative structure to specify against, which is
> precisely why these files were left stale rather than rewritten in a hurry against a guess.

| Field | Value |
|---|---|
| Task ID | M2-C06 |
| Milestone | M2 — Foundation |
| Parent | — (standalone task) |
| Type | Frontend |
| Priority | P0 |
| Status | Not Started |
| Estimate | 1 wk |
| Gate | G2 |

## Task Objective

Build `RecordPickerDialog`: a searchable, **server-paged**, multi-select record picker over
an upstream document or master list, returning the selected records to its caller in the
order the user selected them.

It replaces `V.SMART/V.SMART.Shared/Components/DetailsModal.razor` — the component through
which the entire ERP performs "pull lines from an upstream document" and "choose a master
record", referenced by **33** page files.

`RecordPickerDialog` is a generic primitive. It knows how to search, page, display, select
and return records. It knows nothing about sales orders, delivery challans, items or
quantities. Every domain rule about *what may be pulled* and *what happens when it is*
stays behind the API.

## Why This Task Exists

- **[KB-015](../../architecture/frontend-architecture-existing.md#shared-components-22-components)
  names it the highest-value component to rebuild first**, and states that
  *"the `DetailsModal` + `MasterModal` + `*Selection` trio is how the whole ERP does 'pull
  lines from an upstream document' — reproducing this interaction well is the single biggest
  UX lever in the new frontend."*
- **It is a hard dependency of the document editor.**
  [KB-050 §Document editor pattern](../../frontend-new/react-architecture.md#document-editor-pattern-the-core-abstraction)
  places *"Pull from upstream document → RecordPickerDialog"* inside `LineItemGrid`. Without
  it, M2-C07 and M2-C08 cannot be exercised realistically, and the M3-5 sales wave stalls.
- **The current implementation does not scale and cannot page.** `DetailsModal` receives the
  entire candidate set as an in-memory `List<Dictionary<string, object>>`
  (`DetailsModal.razor:110`) and filters it client-side with a case-insensitive `Contains`
  across every field (`:120-124`). On a tenant with a large document history that is both a
  memory cost in the Blazor circuit and a latency cost. The React replacement is
  server-paged, which is only possible because M2-B02 defines the contract and M2-C05-01
  implements the grid.
- **It carries a subtle behaviour that is easy to lose and expensive to lose.** Selections
  are returned **ordered by the sequence in which the user ticked them**, not by row order
  (`DetailsModal.razor:150-154`, `:186-195`, `:205-213`). Operators use this to control the
  line order of the document they are building. A naive rewrite returns rows in grid order
  and nobody notices until a customer receives a delivery challan with the lines in the
  wrong sequence.

## Prerequisites

- **M2-C05-01** — the server-paged `DataGrid` core. `RecordPickerDialog` composes it; it
  does not implement a second table.
- **M2-C04-03** — `Modal`, `Drawer`, `ConfirmDialog`, feedback states.
- **M2-C04-02** — form controls for the search field and any header filters.
- **M2-B02** — the paging/sort/filter contract the picker queries through.

## Dependencies

| Dependency | Class | Note |
|---|---|---|
| M2-C05-01 | Hard | The grid inside the dialog. Building a bespoke table here would fork the keyboard model and the accessibility work. |
| M2-C04-03 | Hard | `Modal` with focus trap and `Esc` handling. |
| M2-C04-02 | Hard | Search input and filter controls. |
| M2-B02 | Hard | Paging/sort/filter contract. |
| M2-B10 | Soft | Generated client. The picker is generic over an endpoint, so it must accept the query function as a prop rather than importing a specific operation. |
| M2-C05-03 | Soft | Empty/loading/error states inside the dialog. If M2-C05-03 has not landed, the picker still must not ship a blank body — implement a minimal accessible placeholder and note the follow-up. |
| M2-C07 | Deployment | `LineItemGrid` is the primary consumer. Coordinate the callback contract before M2-C07 starts. |
| M5-03, M5-09 | Testing | Component tests and axe. |

## Required Existing Knowledge

| Doc | Why |
|---|---|
| [KB-015 §Shared components](../../architecture/frontend-architecture-existing.md#shared-components-22-components) | The trio and the mapping to `RecordPickerDialog`. |
| [KB-051 §Overlays](../../frontend-new/design-system.md#overlays) | `RecordPickerDialog` is specified there as *"the `DetailsModal.razor` replacement: searchable, multi-select, column-configurable picker over an upstream document"*. |
| [KB-050 §Document editor pattern](../../frontend-new/react-architecture.md#document-editor-pattern-the-core-abstraction) | Where the picker sits in the editor. |
| [ADR-003](../../decisions/ADR-003-react-stack.md) | Stack; no optimistic updates for money/stock/status. |
| [ADR-005](../../decisions/ADR-005-reporting-and-printing.md) | Export stays server-side — relevant, because the existing dialog exports. |
| M2-C05-01's task file | The grid API being composed. |

## Investigation Requirements

**Reuse INV-006, plus one new investigation that genuinely matters.**

- **Reuse** INV-006 (Complete) for the component inventory.
- **New:** survey the **33** files that render `<DetailsModal` and classify how each uses it.
  The questions that change the design:
  1. Which are **multi-select line pulls** from an upstream document, and which are
     **single-record master picks**?
  2. Which pass `HeaderContent`, and what do they put in it?
  3. Which rely on `HiddenColumns` and why (is it per-user, per-screen, or per-context)?
  4. Which depend on the `IsNewItem` / `IsQtyChanged` cell highlighting
     (`DetailsModal.razor:218-230`)?
  5. Which depend on **selection order** in the returned list?
  6. How is the candidate set produced today — an existing service method per call site, or
     ad-hoc LINQ in `@code`? This determines how much backend work each future consumer
     needs, and is exactly the sort of thing that turns a "1 week" component into a
     four-week surprise in M3-5.

  Record counts, the classification table, and `file:line` evidence for at least one
  representative of each category, with a confidence rating. **Record negative results** —
  e.g. "no call site passes `HeaderContent`" is a finding that removes a prop.

- **Do not** attempt to extract the business logic of any call site. That is `<W>-03` work in
  the module waves ([KB-080 §10](../README.md#10-module-migration-task-pattern)). This
  investigation classifies; it does not migrate.

## Repository Areas

| Area | Role |
|---|---|
| `frontend/nexgen-web/src/shared/components/RecordPickerDialog/` | **TO BE CREATED.** This task. |
| `frontend/nexgen-web/src/shared/components/DataGrid/` | **TO BE CREATED (M2-C05-01).** Composed. |
| `V.SMART/V.SMART.Shared/Components/DetailsModal.razor` | Read-only reference — the component being replaced. |
| `V.SMART/V.SMART.Shared/Pages/**` | Read-only — the 33 call sites, surveyed not modified. |

## Existing Behavior to Preserve

`V.SMART/V.SMART.Shared/Components/DetailsModal.razor` (259 lines), verified 2026-08-12.
All **Confirmed** unless stated.

| # | Existing behaviour | Evidence | Disposition |
|---|---|---|---|
| 1 | Generic over untyped rows: `List<Dictionary<string, object>>` | `:110` | **Replaced with a generic type parameter.** `RecordPickerDialog<TRow>` is typed from the generated client. Untyped dictionaries are why the current dialog needs parallel `Columns`/`ColumnFields` arrays kept in index lockstep (`:58-62`) — a latent bug class. |
| 2 | Three parallel column arrays: `Columns` (display), `ColumnFields` (data keys), `HiddenColumns` (by display name) | `:104-106`, `:42-45`, `:58-62` | **Replaced** by one `ColumnDef` list carrying title, field and visibility — the M2-C05-01 column model. Record as an intentional simplification. |
| 3 | Free-text search across **all** `ColumnFields`, case-insensitive `Contains`, applied client-side | `:120-124` | **Preserved in behaviour, moved to the server.** The user-visible contract — "type anything, match anywhere" — stays. The implementation becomes a server query so it works past the first page. |
| 4 | **Select-all applies to the *filtered* rows only**, not the whole data set | `:181-198` (`ToggleSelectAll` iterates `FilteredData`) | **Preserved and made explicit.** With server paging, "all" is ambiguous: the header checkbox selects the **current page** and the UI says so. Silently selecting an unseen 4,000 rows would be a data-integrity hazard. |
| 5 | **Selections are returned ordered by selection sequence**, not row order | `:150-154` (`.OrderBy(w => w.SelectionOrder)`), assigned at `:186-195` and `:205-213` | **Preserved verbatim.** This is load-bearing: it controls line order in the document being built. |
| 6 | Pre-selection honoured from a `"Selected"` key in the incoming row | `:130-134` | **Preserved** as an `initialSelection` prop keyed by row id. |
| 7 | Conditional cell highlighting: `ItemCode` yellow when `IsNewItem`, `UtilQty` red when `IsQtyChanged` | `:218-230` | **Replaced by a generic, caller-supplied `getCellState(row, field)`.** Hardcoding `ItemCode`/`UtilQty` in a shared component is domain leakage into presentation — precisely what this migration exists to stop. Colour alone must not carry the meaning ([KB-051](../../frontend-new/design-system.md#status-vocabulary)); add an icon or text. |
| 8 | Footer buttons: **Update** (confirm), **Print** (which actually exports Excel), **Close** | `:89-93`, `:232-256` | **Preserved in function, corrected in labelling.** The button labelled "Print" calls `ExcelExportService.ExportPendingListToExcel` and downloads an `.xlsx` (`:241-244`). Label it **Export**. Record this as a deliberate correction of a mislabel, not a feature change. |
| 9 | Export is server-side (`ExcelExportService`), base64 → JS interop download | `:241-244`; `V.SMART/V.SMART.Shared/Services/ExcelExportService.cs:113` | **Preserved server-side** per [ADR-005](../../decisions/ADR-005-reporting-and-printing.md); the base64 hop is dropped in favour of a blob. |
| 10 | Backdrop is `static` and `data-bs-keyboard="false"` — **`Esc` does not close the dialog** | `:10-11` | **Deliberately changed.** [KB-051 §Accessibility commitments](../../frontend-new/design-system.md#accessibility-commitments) requires modals to trap focus and close on `Esc`. A picker is a non-destructive selection dialog; `Esc` cancels. Record the change. |
| 11 | Empty result renders a spanning **"No data found."** row | `:75-82` | Replaced by the M2-C05-03 empty-state pair (no data vs no results for this search). |
| 12 | `ConfirmSelection` throws `InvalidOperationException("Please select Dc from the list.")` on a null list, and the catch immediately rethrows | `:156-168` | **Do not reproduce.** The guard is unreachable as written (`selectedItems` is the result of `.ToList()` and cannot be null), and the empty case is not handled at all. In React, disable **Update** while nothing is selected. Record this as a defect observed in the superseded component (do **not** fix `DetailsModal.razor` — it is out of scope). |
| 13 | Fixed `modal-xl`, `max-height:60vh` table, `min-width:800px` | `:14`, `:35-36` | Replaced by responsive sizing per [KB-051](../../frontend-new/design-system.md#responsive-behaviour). |

**Business rules:** none is implemented by this component today, and none may be implemented
by its replacement. The dialog selects records; the server decides what selection means.

## Target Result

```tsx
<RecordPickerDialog<PendingQuoteRow>
  opened={open}
  onClose={close}
  title="Pending quotations"
  columns={pendingQuoteColumns}
  queryKey={qk.quotations.pending(customerId)}
  fetchPage={fetchPendingQuotes}          // (query) => Promise<Paged<PendingQuoteRow>>
  getRowId={r => String(r.quoteSubId)}
  selectionMode="multiple"
  initialSelection={alreadyPulledIds}
  disabledRowIds={alreadyPulledIds}
  getCellState={(row, field) => …}        // caller-supplied, generic
  confirmLabel="Add selected"
  onConfirm={rows => appendLines(rows)}   // rows in SELECTION order
  exportEndpoint={exportPendingQuotes}    // optional; server-side
/>
```

Behavioural target:

- **Server-paged and server-searched.** Typing debounces and re-queries; results past page 1
  are reachable; the dialog never holds the whole candidate set.
- **Selection survives paging and searching.** Ticking three rows on page 1, paging to page 3
  and ticking two more returns five rows. Selection is keyed by `getRowId` and lives above
  the grid, not in the row objects.
- **Selection order is the return order.** Guaranteed by an ordered selection structure, and
  asserted by a test.
- **Select-all is page-scoped and labelled as such** ("Select all 25 on this page"), with a
  visible count of the total selection ("5 selected") that persists across pages.
- **Nothing selected → Confirm is disabled**, with an accessible explanation.
- **Keyboard-first.** Open → focus lands in the search field. `↓` moves into the grid.
  `Space` toggles a row. `Enter` confirms. `Esc` cancels. Focus returns to the trigger.
- **Single-select mode** exists and, in it, `Enter` (or double-click / row activation)
  selects and confirms in one action — the master-pick path must not cost two keystrokes.
- **Export** is optional and, when present, calls a server endpoint.

## Implementation Steps

1. Read the KB documents above; search `docs/kb/investigation-registry.md`.
2. Read `V.SMART/V.SMART.Shared/Components/DetailsModal.razor` in full and re-verify every
   line number in *Existing Behavior to Preserve*. Report any that differ.
3. Run the 33-call-site survey described under *Investigation Requirements* and record it
   **before** fixing the prop surface. The survey decides which props are real.
4. Create `frontend/nexgen-web/src/shared/components/RecordPickerDialog/` (**TO BE CREATED**).
5. Define the props in `types.ts`, generic over `TRow`. The picker takes a `fetchPage`
   function and a `queryKey` rather than importing a specific generated operation — that is
   what makes one component serve 33 call sites.
6. Implement `useRecordSelection.ts`: an insertion-ordered map keyed by row id
   (`Map<string, TRow>` preserves insertion order and is the natural fit for behaviour 5).
   Expose `toggle`, `selectPage`, `clearPage`, `clear`, `selected` (ordered array),
   `isSelected`, `count`.
7. Implement `RecordPickerDialog.tsx`: Mantine `Modal` (focus-trapped, `Esc`-closable),
   containing the search field, an optional `header` slot, `DataGrid` from M2-C05-01, a
   selection summary, and the footer.
8. Wire search into `useDataGridQuery`'s filter state so it participates in the same
   URL-free, dialog-local query state. **The dialog must not write to the page URL** —
   the page behind it owns the URL, and a picker's search term is transient.
9. Implement page-scoped select-all with an indeterminate header checkbox and explicit
   labelling.
10. Implement `disabledRowIds` so already-pulled lines render disabled and unselectable, with
    a tooltip. Whether a row *is* already pulled is decided by the caller from server data —
    the dialog never derives it.
11. Implement single-select mode with row activation confirming immediately.
12. Implement optional export by delegating to M2-C05-03's `useGridExport`. Label the button
    **Export**, not "Print".
13. Accessibility pass: `role="dialog"` with `aria-modal`, labelled by the title, focus trap,
    focus restoration, `aria-live` announcement of the selection count, and the grid's own
    ARIA model intact inside the dialog.
14. Write the tests listed under *Testing*.
15. Run verification commands, review the full `git diff`, update docs and the registry.

## Files Expected to Change

- `frontend/nexgen-web/src/shared/api/queryKeys.ts` — picker key helpers.
- `frontend/nexgen-web/src/shared/components/index.ts` — barrel export, if M2-C01 created one.
- `docs/kb/architecture/frontend-architecture-existing.md` (KB-015) — record the survey result
  and the `DetailsModal` defect at `:156-168`.
- `docs/kb/frontend-new/design-system.md` (KB-051) — mark `RecordPickerDialog` as built and
  record the deliberate divergences (`Esc` closes; page-scoped select-all; Export not Print).
- `docs/kb/investigation-registry.md` (KB-003), `docs/kb/execution/task-tracker.md` (KB-081).

## Files Expected to Be Created

All **TO BE CREATED**, under
`frontend/nexgen-web/src/shared/components/RecordPickerDialog/`:

- `RecordPickerDialog.tsx`
- `RecordPickerDialog.module.css`
- `useRecordSelection.ts`
- `RecordPickerFooter.tsx`
- `types.ts`
- `index.ts`
- `__tests__/RecordPickerDialog.test.tsx`
- `__tests__/useRecordSelection.test.ts`
- `__tests__/selectionOrder.test.tsx`
- `__tests__/RecordPickerDialog.a11y.test.tsx`
- `__tests__/handlers.ts` (MSW)

## Files That Must Not Change

- `V.SMART/**` — including `DetailsModal.razor`, which keeps serving the Blazor app
  unchanged, defects and all, until its call sites are migrated wave by wave. **Do not fix
  the unreachable guard at `DetailsModal.razor:156-168` in this task** — record it.
- `frontend/nexgen-web/src/shared/components/DataGrid/**` — compose it; if it needs a new
  capability, that is a change request against M2-C05-01, not an in-place edit.
- `frontend/vsmart-erp/**`, `db/**`, `docs/kb/decisions/**`.

## API Changes

Not applicable — this task adds no HTTP surface. `RecordPickerDialog` is generic over a
caller-supplied `fetchPage` function that conforms to M2-B02's paged contract, so it adds no
endpoint of its own and hardcodes no resource.

If the survey shows that many call sites have **no** server endpoint capable of producing
their candidate list (likely, since candidate sets are assembled in Razor `@code` today),
record that as the finding it is: it is per-wave `<W>-03`/`<W>-06` backend work, and it
should appear in the M3-5 estimate rather than surfacing as a surprise.

## React Changes

| Aspect | Detail |
|---|---|
| **Route** | n/a — a shared overlay component. |
| **Component** | `RecordPickerDialog<TRow>` under `frontend/nexgen-web/src/shared/components/RecordPickerDialog/` (**TO BE CREATED**), composing `DataGrid` (M2-C05-01) inside `Modal` (M2-C04-03). Replaces `V.SMART/V.SMART.Shared/Components/DetailsModal.razor`. |
| **API integration** | Caller-supplied `fetchPage` + `queryKey`, executed through TanStack Query with `keepPreviousData`. The dialog imports no specific generated operation. |
| **State** | Selection in a dialog-local insertion-ordered map. Query state dialog-local — **never** written to the page URL. No Zustand store. |
| **Validation** | n/a. Confirm is disabled when the selection is empty; that is an affordance, not validation. |
| **Permissions** | n/a inside the dialog. The caller decides whether the picker may open. A 403 from `fetchPage` renders `PermissionDeniedState` inside the dialog body. |
| **Loading state** | Skeleton rows inside the dialog body; the dialog frame, search field and footer stay stable so the dialog does not resize as data arrives. |
| **Empty state** | Two variants: no candidate records at all, versus no matches for the current search (with **Clear search**). |
| **Error state** | `ErrorState` inside the dialog body with the server message, correlation id and Retry. The dialog stays open — closing it on error would lose the user's existing selection. |
| **Accessibility** | `role="dialog"`, `aria-modal="true"`, labelled by the title; focus trapped; focus moves to the search field on open and returns to the trigger on close; `Esc` cancels (a **deliberate change** from `data-bs-keyboard="false"` at `DetailsModal.razor:11`); selection count announced via `aria-live="polite"`; disabled rows expose `aria-disabled` with an accessible reason; the grid's ARIA model is preserved inside the dialog. |
| **Responsive behaviour** | ≥1024 px: large centred modal. 768–1023 px: near-full-screen modal with the grid dropping low-priority columns. <768 px: full-screen sheet with a stacked row layout; per [KB-051](../../frontend-new/design-system.md#responsive-behaviour) phone is read-and-approve, so the picker is available for viewing and single-select but is not the supported way to build a 40-line document. |

## Business Logic

**Newly introduced — presentation only, with justification.**

`RecordPickerDialog` is new code. What is new is *record selection interaction*: search,
page, select, order, confirm.

**No ERP business rule is implemented client-side by this task.** Explicitly:

- The dialog does **not** decide which records are eligible to be pulled. `disabledRowIds`
  and the candidate list both come from the caller, which gets them from the server.
- The dialog does **not** compute balance quantities, check for duplicate lines, validate
  against an upstream document's remaining quantity, or apply any pricing. Those are
  server-side rules — balance-quantity derivation across `Ref*SubId` chains is INV-013,
  scheduled for M3-5, and must not be pre-empted in TypeScript.
- The dialog does **not** apply the domain-specific cell highlighting the old component
  hardcoded (`ItemCode`/`IsNewItem`, `UtilQty`/`IsQtyChanged` at `DetailsModal.razor:218-230`).
  It exposes a generic `getCellState` hook and lets the caller — eventually, the server's
  row data — decide.

The one behaviour that *is* preserved verbatim is **selection ordering**
(`DetailsModal.razor:150-154`), and it is preserved because it is user-visible workflow, not
because it is a business rule.

## Testing

| # | Test | Asserts |
|---|---|---|
| 1 | Opens with focus in the search field; `Esc` closes and restores focus to the trigger | A11y + deliberate divergence |
| 2 | Typing debounces and issues a **server** query; results are not filtered client-side | Server search |
| 3 | Selection made on page 1 persists after paging to page 3 and back | Cross-page selection |
| 4 | Selection persists across a search change | Cross-search selection |
| 5 | **Confirm returns rows in the order they were selected, not grid order** — select row 5, then row 2, then row 9; expect `[5, 2, 9]` | Behaviour 5, the load-bearing one |
| 6 | Header checkbox selects **only the current page**, shows indeterminate, and the label states the page scope | Behaviour 4, made explicit |
| 7 | Total selection count is displayed and survives paging | UX |
| 8 | Confirm is disabled with zero selected and has an accessible explanation | Replaces the unreachable guard at `DetailsModal.razor:156-168` |
| 9 | `initialSelection` pre-selects the matching rows | Behaviour 6 |
| 10 | `disabledRowIds` rows are unselectable, `aria-disabled`, and excluded from select-all | Correctness |
| 11 | Single-select mode: row activation selects and confirms in one action | Master-pick path |
| 12 | A 403 renders `PermissionDeniedState` inside the dialog; the dialog stays open | Error UX |
| 13 | An error does not clear the existing selection | Data-loss prevention |
| 14 | Export delegates to the server endpoint; no client-side file generation | ADR-005 |
| 15 | The dialog never mutates the page URL query string | Isolation |
| 16 | axe: no critical violations, open and empty, open and populated | M5-09 |

## Acceptance Criteria

- [ ] `RecordPickerDialog<TRow>` exists, is generic, and takes `fetchPage` + `queryKey` rather
      than importing a resource-specific operation.
- [ ] It composes `DataGrid` from M2-C05-01; no second table implementation exists in the
      directory.
- [ ] Search and paging are server-driven; the component never receives or holds the full
      candidate set.
- [ ] Selection is keyed by `getRowId`, survives paging and search changes, and is returned
      **in selection order**, proven by test 5.
- [ ] Select-all is page-scoped, indeterminate-capable, and labelled with its scope.
- [ ] Confirm is disabled while nothing is selected.
- [ ] `Esc` cancels and focus is restored to the trigger — a deliberate, documented divergence
      from `DetailsModal.razor:10-11`.
- [ ] No domain field name (`ItemCode`, `UtilQty`, `IsNewItem`, `IsQtyChanged`, or any other)
      appears anywhere in the component directory: `grep -rniE 'itemcode|utilqty|isnewitem|isqtychanged'`
      returns no hits outside test fixtures.
- [ ] The export control is labelled **Export** and calls a server endpoint; no client-side
      file-generation library is added.
- [ ] The dialog does not write to the page URL.
- [ ] The 33-call-site survey is complete and recorded in the investigation registry with
      counts, a classification table, and `file:line` evidence.
- [ ] The `DetailsModal.razor:156-168` unreachable-guard defect is **recorded, not fixed**.
- [ ] All 16 tests pass; axe reports no critical violations.
- [ ] `npm run lint`, `npm run test`, `npm run build` pass.
- [ ] `git diff --stat -- V.SMART/` produces no output.

## Verification Commands

Frontend commands as established by **M2-C01**; run from `frontend/nexgen-web/`.

```bash
npm ci
```

```bash
npm run lint
```

```bash
npm run test
```

```bash
npm run build
```

Repository-level (verified per [KB-083](../prompt-template.md#verified-repository-commands)):

```bash
git status --porcelain
```

```bash
git diff --stat -- V.SMART/
```

Survey helper (read-only, used during investigation):

```bash
git grep -l "<DetailsModal" -- "V.SMART/V.SMART.Shared/Pages/"
```

## Documentation Updates

| Document | Change |
|---|---|
| `docs/kb/architecture/frontend-architecture-existing.md` (KB-015) | Record the 33-call-site survey: the multi-select-pull vs single-master-pick split, `HeaderContent`/`HiddenColumns`/cell-highlight usage, selection-order dependence, and how candidate sets are produced today. Record the `DetailsModal.razor:156-168` defect. Bump `last_verified`. |
| `docs/kb/frontend-new/design-system.md` (KB-051) | Mark `RecordPickerDialog` as built; record the three deliberate divergences (`Esc` closes, page-scoped select-all, Export not Print) and the generic `getCellState` replacing hardcoded domain highlighting. Bump `last_verified`. |
| `docs/kb/frontend-new/react-architecture.md` (KB-050) | Confirm the *Document editor pattern* reference to `RecordPickerDialog` against the delivered prop surface. |
| `docs/kb/execution/task-tracker.md` (KB-081) | M2-C06 status. |
| `docs/kb/investigation-registry.md` (KB-003) | The survey + findings below. |

## Investigation Registry Updates

```yaml
Finding:        DetailsModal.razor is the ERP's universal record picker: it is rendered
                by 33 page files, takes untyped List<Dictionary<string,object>> rows with
                three parallel column arrays, filters client-side across all fields, scopes
                select-all to the FILTERED rows, and returns selected rows ORDERED BY THE
                SEQUENCE THE USER SELECTED THEM rather than by row order.
Evidence:       V.SMART/V.SMART.Shared/Components/DetailsModal.razor:104-124, :150-154,
                :181-198, :205-213
Business rule:  n/a
Confidence:     Confirmed
Last verified:  2026-08-12
```

```yaml
Finding:        DetailsModal.ConfirmSelection contains an unreachable null guard — it tests
                the result of .ToList() for null and throws
                InvalidOperationException("Please select  Dc from the list."), while the
                catch immediately rethrows. The genuinely reachable case, an EMPTY
                selection, is not handled at all.
Evidence:       V.SMART/V.SMART.Shared/Components/DetailsModal.razor:144-169
Business rule:  n/a
Confidence:     Confirmed
Last verified:  2026-08-12
```

```yaml
Finding:        DetailsModal hardcodes domain-specific cell highlighting in a shared
                component: ItemCode is highlighted yellow when the row carries IsNewItem,
                and UtilQty red when it carries IsQtyChanged.
Evidence:       V.SMART/V.SMART.Shared/Components/DetailsModal.razor:218-230
Business rule:  n/a
Confidence:     Confirmed
Last verified:  2026-08-12
```

Plus the full call-site classification table, and a negative result for any expected usage
pattern that turns out not to exist.

## Git Strategy

| Field | Value |
|---|---|
| **Branch** | `migration/M2-C06-record-picker-dialog` |
| **Commit** | `M2-C06: Implement RecordPickerDialog replacing DetailsModal` |
| **Expected changed files** | `frontend/nexgen-web/src/shared/components/RecordPickerDialog/**` (new), `frontend/nexgen-web/src/shared/api/queryKeys.ts`, `frontend/nexgen-web/src/shared/components/index.ts`, `docs/kb/architecture/frontend-architecture-existing.md`, `docs/kb/frontend-new/design-system.md`, `docs/kb/frontend-new/react-architecture.md`, `docs/kb/investigation-registry.md`, `docs/kb/execution/task-tracker.md` |
| **Rollback approach** | Revert the branch's merge commit. Purely additive: no Blazor call site is migrated by this task, `DetailsModal.razor` keeps serving all 33 pages, and no server or database state is touched. The only cost of a revert is that M2-C07's "pull from upstream" path loses its dialog and must be stubbed. |

Never merge or push to `master` from an execution session.

## Fresh-Session Execution Prompt

Copy everything in the block below into a new AI session.

```text
============================================================
ERP MIGRATION — TASK EXECUTION PROMPT
============================================================

TASK ID:
M2-C06

TASK NAME:
RecordPickerDialog — the DetailsModal replacement

ROLE
You are an engineer working on the V.SMART / NexGen ERP
modernization project. You are executing exactly one task.

PROJECT OBJECTIVE
We are replacing the existing Blazor Server frontend with a new
React frontend while preserving the existing ERP business
behaviour, business services, database behaviour and business
rules wherever possible. The backend is extended, never
rewritten. Business logic currently trapped in Razor @code is
extracted into server-side services before any React screen
replaces it.

CURRENT ARCHITECTURE
Repository root: C:\Kumar\NexGen-ERP---2025-master\NexGen-ERP---2025-master
(this is the git repository root; the parent directory is not
part of the project)

  V.SMART/V.SMART.Shared   .NET 9 class library — ALL domain code:
                           196 EF entity sets, 285 business services,
                           ~190 repositories + UnitOfWork, 274 ViewModels,
                           333 Razor pages, 440 routes
  V.SMART/V.SMART.Web      Blazor Server host (live UI, stays running)
  V.SMART/V.SMART.Api      ASP.NET Core Web API (.NET 9) — the React
                           backend; currently 2 controllers / 6 endpoints
  V.SMART/V.SMART          .NET MAUI Blazor Hybrid host

SQL Server + EF Core 9, code-first, database-per-tenant.

Task-specific architecture context:
The React application DOES NOT EXIST YET. It is created by task
M2-C01 at frontend/nexgen-web/. Every frontend path in this
prompt is TO BE CREATED. The DataGrid you compose is created by
M2-C05-01; Modal and the feedback states come from M2-C04-03.

Stack per ADR-003: React 19, TypeScript strict, Vite 6, React
Router v7, TanStack Query v5, Zustand, React Hook Form, Zod,
Mantine 7, CSS Modules + design tokens, TanStack Table v8 +
TanStack Virtual, Recharts, Axios with a GENERATED OpenAPI
client, decimal.js, react-i18next, Vitest/RTL/MSW/Playwright.

SOURCE OF TRUTH
The project knowledge base is at docs/kb/. Read it before
investigating the repository. Use docs/kb/INDEX.md for
question-to-document routing.

Required reading for this task:
  ADR-003  docs/kb/decisions/ADR-003-react-stack.md
  ADR-005  docs/kb/decisions/ADR-005-reporting-and-printing.md
  KB-015   docs/kb/architecture/frontend-architecture-existing.md
  KB-050   docs/kb/frontend-new/react-architecture.md
  KB-051   docs/kb/frontend-new/design-system.md
  KB-083   docs/kb/execution/prompt-template.md
  KB-002   docs/kb/source-of-truth-rules.md
  TASK     docs/kb/execution/tasks/M2-C06.md      (the full task spec)
  TASK     docs/kb/execution/tasks/M2-C05-01.md   (the DataGrid you compose)
  TASK     docs/kb/execution/tasks/M2-C04-03.md   (Modal and feedback states)

Authority order when sources conflict:
  1. Current source code
  2. Database schema / EF migrations (for storage)
  3. The knowledge base (for interpretation)
  4. Older prose documentation — hypothesis only
docs/ARCHITECTURE.md is superseded and contains known factual
errors. Do not rely on it.

INVESTIGATION REGISTRY
Before any repository investigation, search
docs/kb/investigation-registry.md.

Relevant investigations:
  INV-006  Existing UI: routing, layout, components — Complete
  INV-013  Balance-quantity derivation across Ref*SubId chains —
           Scheduled for M3-5. DO NOT pre-empt it in TypeScript.
  INV-024  @code triage per module — runs one wave ahead

Before investigating the repository, search
docs/kb/investigation-registry.md and the relevant knowledge-base
documents via docs/kb/INDEX.md. If an investigation is Complete
and not stale, reuse its findings and cite the doc_id — do not
re-derive them. If it is Partial, investigate only the documented
gap. If it is absent or contradicted by current code,
investigate, then record the finding, its file:line evidence,
and its confidence in the knowledge base so that future sessions
do not repeat this work. Record negative results too — "grepped
for X, found none" is a finding.

CURRENT TASK
Build RecordPickerDialog: a searchable, SERVER-PAGED,
multi-select record picker over an upstream document or master
list, returning the selected records to its caller IN THE ORDER
THE USER SELECTED THEM.

It replaces V.SMART/V.SMART.Shared/Components/DetailsModal.razor,
the component through which the entire ERP performs "pull lines
from an upstream document" and "choose a master record". It is
rendered by 33 page files.

REQUIRED INVESTIGATION (do this BEFORE fixing the prop surface)
Survey all 33 files that render <DetailsModal and classify:
  1. Which are multi-select line pulls from an upstream document,
     and which are single-record master picks?
  2. Which pass HeaderContent, and what goes in it?
  3. Which rely on HiddenColumns, and why?
  4. Which depend on the IsNewItem / IsQtyChanged cell
     highlighting?
  5. Which depend on SELECTION ORDER in the returned list?
  6. How is the candidate set produced today — an existing
     service method per call site, or ad-hoc LINQ in @code?
Question 6 matters most: it determines how much backend work each
future consumer needs, and is exactly the sort of thing that
turns a one-week component into a four-week surprise in M3-5.

Record counts, a classification table, and file:line evidence for
at least one representative of each category, with a confidence
rating. Record negative results — "no call site passes
HeaderContent" is a finding that removes a prop.

Do NOT extract the business logic of any call site. That is
<W>-03 work in the module waves. This survey classifies; it does
not migrate.

Helper (read-only):
  git grep -l "<DetailsModal" -- "V.SMART/V.SMART.Shared/Pages/"

WHY THIS TASK EXISTS
KB-015 names DetailsModal the highest-value component to rebuild
first: "the DetailsModal + MasterModal + *Selection trio is how
the whole ERP does 'pull lines from an upstream document' —
reproducing this interaction well is the single biggest UX lever
in the new frontend." It is also a hard dependency of
LineItemGrid (M2-C07) and the DocumentEditor (M2-C08), and
therefore of the M3-5 sales wave.

CURRENT IMPLEMENTATION
V.SMART/V.SMART.Shared/Components/DetailsModal.razor, 259 lines.
Verified 2026-08-12; all Confirmed. RE-VERIFY every line number
below and report any that differ.

  :110       Rows arrive as List<Dictionary<string, object>> —
             completely untyped.
  :104-106   Three PARALLEL column arrays: Columns (display
             names), ColumnFields (data keys), HiddenColumns (by
             display name).
  :42-45,
  :58-62     Those arrays are kept in index lockstep by
             ColumnFields.IndexOf(f) — a latent bug class.
  :120-124   Free-text search across ALL ColumnFields,
             case-insensitive Contains, applied CLIENT-SIDE over
             the whole in-memory set.
  :181-198   ToggleSelectAll iterates FilteredData — select-all
             applies to the FILTERED rows only.
  :150-154   ConfirmSelection returns
             .Where(w => w.Selected).OrderBy(w => w.SelectionOrder)
             — selections are returned IN THE ORDER THE USER
             TICKED THEM, not in row order. SelectionOrder is
             assigned at :186-195 and :205-213.
  :130-134   Pre-selection is honoured from a "Selected" key in
             the incoming row dictionary.
  :218-230   GetCellStyle hardcodes DOMAIN highlighting in a
             SHARED component: ItemCode yellow when IsNewItem,
             UtilQty red when IsQtyChanged.
  :89-93     Footer buttons: Update (confirm), Print, Close.
  :232-256   The button labelled "Print" actually calls
             ExcelExportService.ExportPendingListToExcel and
             downloads an .xlsx (:241-244 base64 -> JS interop).
  :10-11     data-bs-backdrop="static" and
             data-bs-keyboard="false" — Esc does NOT close the
             dialog today.
  :75-82     Empty result renders one spanning "No data found."
             row.
  :144-169   ConfirmSelection guards the result of .ToList()
             against null (unreachable) and the catch immediately
             rethrows; the genuinely reachable EMPTY-selection
             case is not handled at all.
  :14, :35-36 Fixed modal-xl, max-height:60vh, min-width:800px.

  Also Confirmed: ExcelExportService.ExportPendingListToExcel<T>
  at V.SMART/V.SMART.Shared/Services/ExcelExportService.cs:113.

BEHAVIOUR TO PRESERVE VERBATIM
  * SELECTION ORDER IS THE RETURN ORDER (:150-154). This is
    load-bearing: operators use it to control the line order of
    the document they are building. A naive rewrite returns rows
    in grid order and nobody notices until a customer receives a
    delivery challan with its lines in the wrong sequence.
  * "Type anything, match anywhere" free-text search — same
    user-visible contract, moved to the server so it works past
    page 1.
  * Pre-selection from the caller.
  * Select-all NOT meaning "everything" — see the deliberate
    change below.

DELIBERATE CHANGES (record each as intentional, with reason)
  * Typed rows: RecordPickerDialog<TRow> replaces untyped
    dictionaries and the three parallel arrays with ONE ColumnDef
    list (the M2-C05-01 column model).
  * Server paging and server search replace the in-memory set.
  * Select-all becomes PAGE-SCOPED and is LABELLED as such
    ("Select all 25 on this page"), with a running total ("5
    selected") that persists across pages. With server paging,
    "all" is ambiguous, and silently selecting an unseen 4,000
    rows would be a data-integrity hazard.
  * Esc CANCELS the dialog and focus returns to the trigger.
    KB-051 requires modals to trap focus and close on Esc; the
    current data-bs-keyboard="false" is an accessibility defect.
  * The "Print" button is relabelled EXPORT, because that is what
    it does.
  * Hardcoded ItemCode/UtilQty highlighting is replaced by a
    generic caller-supplied getCellState(row, field). Colour
    alone must not carry meaning — add an icon or text.
  * The unreachable null guard is replaced by disabling Confirm
    while nothing is selected.

TARGET IMPLEMENTATION
A generic component under
frontend/nexgen-web/src/shared/components/RecordPickerDialog/
(TO BE CREATED) with roughly this surface:

  <RecordPickerDialog<TRow>
    opened onClose title
    columns
    queryKey
    fetchPage            // (query) => Promise<Paged<TRow>>
    getRowId
    selectionMode="single" | "multiple"
    initialSelection
    disabledRowIds
    getCellState
    confirmLabel
    onConfirm            // rows in SELECTION order
    exportEndpoint       // optional, server-side
  />

Taking fetchPage + queryKey rather than importing a specific
generated operation is what lets ONE component serve 33 call
sites.

Guaranteed behaviour:
  - Server-paged and server-searched; the dialog never holds the
    whole candidate set.
  - Selection survives paging AND search changes: tick three rows
    on page 1, page to page 3, tick two more -> confirm returns
    five.
  - Selection order is the return order (use an insertion-ordered
    Map<string, TRow> keyed by getRowId).
  - Select-all is page-scoped, indeterminate-capable, and
    labelled with its scope.
  - Confirm disabled while nothing is selected, with an
    accessible explanation.
  - Keyboard-first: open -> focus in the search field; Down moves
    into the grid; Space toggles a row; Enter confirms; Esc
    cancels; focus returns to the trigger.
  - Single-select mode: row activation selects AND confirms in
    one action — the master-pick path must not cost two
    keystrokes.
  - disabledRowIds rows render disabled, aria-disabled, with a
    tooltip, and are excluded from select-all.
  - The dialog NEVER writes to the page URL. The page behind it
    owns the URL; a picker's search term is transient.
  - Optional export delegates to the server endpoint.

RELEVANT FILES
Read-only reference (exist today; DO NOT MODIFY):
  V.SMART/V.SMART.Shared/Components/DetailsModal.razor
  V.SMART/V.SMART.Shared/Components/MasterModal.razor
  V.SMART/V.SMART.Shared/Components/CustomerSelection.razor
  V.SMART/V.SMART.Shared/Components/VendorSelection.razor
  V.SMART/V.SMART.Shared/Services/ExcelExportService.cs
  V.SMART/V.SMART.Shared/Pages/**  (the 33 call sites — survey only)

To be created, under
frontend/nexgen-web/src/shared/components/RecordPickerDialog/:
  RecordPickerDialog.tsx
  RecordPickerDialog.module.css
  useRecordSelection.ts
  RecordPickerFooter.tsx
  types.ts
  index.ts
  __tests__/RecordPickerDialog.test.tsx
  __tests__/useRecordSelection.test.ts
  __tests__/selectionOrder.test.tsx
  __tests__/RecordPickerDialog.a11y.test.tsx
  __tests__/handlers.ts

API CHANGES
None. This task adds no HTTP surface. The picker is generic over
a caller-supplied fetchPage conforming to M2-B02's paged
contract, so it hardcodes no resource.

If the survey shows that many call sites have NO server endpoint
capable of producing their candidate list — likely, since
candidate sets are assembled in Razor @code today — record that
as the finding it is. It is per-wave <W>-03/<W>-06 backend work
and belongs in the M3-5 estimate, not in a surprise.

BUSINESS RULES TO PRESERVE
None — this task does not touch business behaviour.

Explicitly, the dialog must NOT:
  - decide which records are eligible to be pulled
    (disabledRowIds and the candidate list come from the caller,
    which gets them from the server);
  - compute balance quantities, check for duplicate lines,
    validate against an upstream document's remaining quantity,
    or apply pricing. Balance-quantity derivation across
    Ref*SubId chains is INV-013, scheduled for M3-5 — do not
    pre-empt it in TypeScript;
  - carry any domain field name in its own code.

The one behaviour preserved verbatim is SELECTION ORDERING, and
it is preserved because it is user-visible workflow.

CONSTRAINTS
- Do not rewrite existing business services.
- Do not reimplement ERP business logic in React/TypeScript.
- Do not change unrelated modules.
- Do not make assumptions without checking source code.
- Do not repeat completed investigations.
- Do not modify the database schema unless this task explicitly
  authorizes it. (It does not.)
- Preserve existing API behaviour wherever practical.
- The server remains authoritative for validation, calculations,
  permissions and document numbering.
- Do not start another task after completing this one.

Task-specific constraints:
- Do not modify anything under V.SMART/.
  `git diff --stat -- V.SMART/` must produce no output.
- DO NOT FIX the unreachable guard at DetailsModal.razor:144-169.
  DetailsModal keeps serving all 33 Blazor pages unchanged,
  defects and all, until its call sites migrate wave by wave.
  Record the defect; do not touch it.
- Do not migrate any Blazor call site in this task.
- Do not build a second table. Compose DataGrid from M2-C05-01.
  If it needs a new capability, that is a change request against
  M2-C05-01, not an in-place edit.
- No domain field name may appear in the component directory.
  `grep -rniE 'itemcode|utilqty|isnewitem|isqtychanged'` over it
  must return no hits outside test fixtures.
- Do not add a client-side file-generation library (ADR-005).
- Do not write to the page URL from the dialog.
- Do not add a second UI component library.

EXECUTION PROCEDURE
1. Read the referenced knowledge-base documents.
2. Search the investigation registry.
3. Confirm whether the required investigation already exists.
4. Inspect the actual source code.
5. Verify every assumption against code before acting on it.
6. Implement only this task.
7. Run the required verification commands and tests.
8. Review the full git diff.
9. Update documentation.
10. Update the investigation registry if required.

EXPECTED FILE CHANGES
Modified:
  frontend/nexgen-web/src/shared/api/queryKeys.ts
  frontend/nexgen-web/src/shared/components/index.ts
  docs/kb/architecture/frontend-architecture-existing.md
  docs/kb/frontend-new/design-system.md
  docs/kb/frontend-new/react-architecture.md
  docs/kb/investigation-registry.md
  docs/kb/execution/task-tracker.md

Created:
  frontend/nexgen-web/src/shared/components/RecordPickerDialog/**

Must not change:
  V.SMART/**  (especially DetailsModal.razor)
  frontend/nexgen-web/src/shared/components/DataGrid/**
  frontend/vsmart-erp/**
  db/**
  docs/kb/decisions/**

TESTS
Vitest + React Testing Library + MSW, via `npm run test` from
frontend/nexgen-web/. Write all of these:

 1. Opens with focus in the search field; Esc closes and restores
    focus to the trigger.
 2. Typing debounces and issues a SERVER query; results are not
    filtered client-side.
 3. Selection made on page 1 persists after paging to page 3 and
    back.
 4. Selection persists across a search change.
 5. Confirm returns rows in SELECTION order, not grid order:
    select row 5, then row 2, then row 9 -> expect [5, 2, 9].
 6. The header checkbox selects only the current page, shows
    indeterminate, and its label states the page scope.
 7. The total selection count is displayed and survives paging.
 8. Confirm is disabled with zero selected and carries an
    accessible explanation.
 9. initialSelection pre-selects the matching rows.
10. disabledRowIds rows are unselectable, aria-disabled, and
    excluded from select-all.
11. Single-select mode: row activation selects and confirms in
    one action.
12. A 403 renders PermissionDeniedState inside the dialog and the
    dialog stays open.
13. An error does not clear the existing selection.
14. Export delegates to the server endpoint; no client-side file
    generation.
15. The dialog never mutates the page URL query string.
16. axe: no critical violations, open-and-empty and
    open-and-populated.

Test 5 is the one that protects real customers. Write it first.

ACCEPTANCE CRITERIA
- RecordPickerDialog<TRow> exists, is generic, and takes
  fetchPage + queryKey rather than a resource-specific operation.
- It composes DataGrid from M2-C05-01; no second table exists in
  the directory.
- Search and paging are server-driven; the component never holds
  the full candidate set.
- Selection is keyed by getRowId, survives paging and search, and
  is returned in SELECTION order (test 5).
- Select-all is page-scoped, indeterminate-capable, and labelled
  with its scope.
- Confirm is disabled while nothing is selected.
- Esc cancels and focus returns to the trigger.
- grep -rniE 'itemcode|utilqty|isnewitem|isqtychanged' over the
  component directory returns no hits outside test fixtures.
- The export control is labelled Export and calls a server
  endpoint; no client-side file-generation library is added.
- The dialog does not write to the page URL.
- The 33-call-site survey is complete and recorded with counts, a
  classification table, and file:line evidence.
- The DetailsModal.razor:144-169 defect is RECORDED, NOT FIXED.
- All 16 tests pass; axe reports no critical violations.
- npm run lint, npm run test and npm run build pass.
- git diff --stat -- V.SMART/ produces no output.

VERIFICATION COMMANDS
The frontend commands below are the ones established by task
M2-C01; they are cited as established there, not as
independently verified. Run each from frontend/nexgen-web/.

  npm ci
  npm run lint
  npm run test
  npm run build

Repository-level commands (verified for this repository per
KB-083):

  git status --porcelain
  git diff --stat -- V.SMART/     # must produce no output
  git grep -l "<DetailsModal" -- "V.SMART/V.SMART.Shared/Pages/"

DOCUMENTATION REQUIREMENTS
Update, keeping full frontmatter (doc_id, title, module,
source_files, entities, api_endpoints, database_tables,
business_rules, status, confidence, last_verified, dependencies)
and bumping last_verified:

  docs/kb/architecture/frontend-architecture-existing.md (KB-015)
    — record the 33-call-site survey (multi-select pull vs
      single master pick, HeaderContent / HiddenColumns / cell
      highlighting usage, selection-order dependence, and how
      candidate sets are produced today), plus the
      DetailsModal.razor:144-169 defect.
  docs/kb/frontend-new/design-system.md (KB-051)
    — mark RecordPickerDialog as built; record the deliberate
      divergences (Esc closes, page-scoped select-all, Export not
      Print, generic getCellState replacing hardcoded domain
      highlighting).
  docs/kb/frontend-new/react-architecture.md (KB-050)
    — confirm the Document editor pattern's RecordPickerDialog
      reference against the delivered prop surface.
  docs/kb/execution/task-tracker.md (KB-081)
    — set M2-C06 to Needs Review.

Add to docs/kb/investigation-registry.md, in the KB-083 evidence
format (Finding / Evidence / Business rule / Confidence / Last
verified):
  - the DetailsModal capability finding (33 call sites, untyped
    dictionary rows, parallel column arrays, client-side search,
    filtered select-all, selection-ordered return);
  - the unreachable-guard defect at :144-169;
  - the hardcoded domain highlighting at :218-230;
  - the full call-site classification table;
  - a negative result for any expected usage pattern that turns
    out not to exist.

GIT
Branch:  migration/M2-C06-record-picker-dialog
Commit:  M2-C06: Implement RecordPickerDialog replacing DetailsModal
Do not merge. Do not push to master. Leave the branch for review.

FINAL RESPONSE
When finished, report:
1.  Task ID
2.  Task status (Completed / Needs Review / Blocked)
3.  What was implemented
4.  Files created
5.  Files modified
6.  Files deleted
7.  Tests executed
8.  Test results
9.  Documentation updated
10. Investigation registry updated
11. Architectural decisions taken
12. Unexpected findings
13. Assumptions made
14. Deviations from this task
15. Recommended next task

IMPORTANT:
EXECUTE ONLY THIS TASK.
DO NOT START THE NEXT TASK.
============================================================
```
