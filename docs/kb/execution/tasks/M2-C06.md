---
doc_id: TASK-M2-C06
title: RecordPickerDialog — the DetailsModal replacement
module: execution
task_id: M2-C06
milestone: M2
parent_task: n/a
task_type: Frontend
priority: P0
status: Needs Review
estimate: 1 wk
depends_on: [M2-C05-01]
source_files:
  - V.SMART/V.SMART.Shared/Components/DetailsModal.razor
  - V.SMART/V.SMART.Shared/Components/MasterModal.razor
  - V.SMART/V.SMART.Shared/Components/CustomerSelection.razor
  - V.SMART/V.SMART.Shared/Components/VendorSelection.razor
  - V.SMART/V.SMART.Shared/Services/ExcelExportService.cs
  - V.SMART/V.SMART.Shared/Pages/SalesAndLabour_pages/SalesPo_Pages/MfgPOUpsert.razor
api_endpoints:
  - "GET /api/v1/{resource}"
business_rules: []
confidence: n/a
last_verified: 2026-08-26
---

# M2-C06 — RecordPickerDialog — the DetailsModal replacement

> **Re-specified for Angular on 2026-08-22 by [`M2-C12-03`](M2-C12-03.md).** The ⛔ STOP banner
> that stood here is removed **in the same change that removed the stack-specific instructions it
> guarded**: this file no longer specifies the ADR-003 stack.
> [`ADR-007`](../../decisions/ADR-007-angular-stack.md) is the governing stack decision —
> **Angular 22.x + PrimeNG** — and [KB-050](../../frontend-new/react-architecture.md), rewritten
> for Angular by `M2-C00`, is the structure this component is built into. Its target directory,
> `shared/components/record-picker-dialog/`, is named in KB-050's project structure.
>
> **The specification this replaces is not deleted from the record.** Its *intent* survives
> unchanged, and this file's most important content was never framework-specific: the 13-row
> table of `DetailsModal.razor` behaviour with `file:line` evidence, the 33-call-site survey the
> task must run before fixing its input surface, and above all **behaviour 5 — selections are
> returned in the order the user ticked them**, which controls line order in the document being
> built. What changed is the *how* — a PrimeNG `p-dialog` instead of a third-party modal, signals
> and an injectable selection holder instead of hooks, and Angular's own HTTP testing utilities
> instead of a request-mocking library. Every `file:line` citation into `V.SMART/` comes through
> byte-identical.

| Field | Value |
|---|---|
| Task ID | M2-C06 |
| Milestone | M2 — Foundation |
| Parent | — (standalone task) |
| Type | Frontend |
| Priority | P0 |
| Status | **Needs Review** *(implemented 2026-08-26, unmerged — see § Execution Record)* |
| Estimate | 1 wk |
| Gate | G2 |

## Objective

Build `RecordPickerDialog`: a searchable, **server-paged**, multi-select record picker over
an upstream document or master list, returning the selected records to its caller in the
order the user selected them.

It replaces `V.SMART/V.SMART.Shared/Components/DetailsModal.razor` — the component through
which the entire ERP performs "pull lines from an upstream document" and "choose a master
record", referenced by **33** page files.

`RecordPickerDialog` is a generic primitive. It knows how to search, page, display, select
and return records. It knows nothing about sales orders, delivery challans, items or
quantities. Every domain rule about *what may be pulled* and *what happens when it is*
stays behind the API.

## Scope

`frontend/nexgen-web/src/app/shared/components/record-picker-dialog/`, the 33-call-site survey,
and the KB updates listed under *Documentation Updates*. Nothing else.

## Out of Scope

- **Migrating any call site.** All 33 keep using `DetailsModal.razor` until their module wave.
- **Fixing `DetailsModal.razor`.** Its defects are recorded, not repaired — the Blazor app must
  keep working unchanged.
- **Extracting the business logic of any call site.** That is `<W>-03` work in the module waves
  ([KB-080 §10](../README.md#10-module-migration-task-pattern)).
- **A second table.** The dialog composes `DataGrid`; it does not implement its own grid.

## Why This Task Exists

- **[KB-015](../../architecture/frontend-architecture-existing.md#shared-components-22-components)
  names it the highest-value component to rebuild first**, and states that
  *"the `DetailsModal` + `MasterModal` + `*Selection` trio is how the whole ERP does 'pull
  lines from an upstream document' — reproducing this interaction well is the single biggest
  UX lever in the new frontend."*
- **It is a hard dependency of the document editor.**
  [KB-050 §Document editor pattern](../../frontend-new/react-architecture.md#document-editor-pattern-the-core-abstraction)
  places *"Pull from upstream document → RecordPickerDialog"* inside `LineItemGrid`. Without
  it, M2-C07 and M2-C08 cannot be exercised realistically, and the M3-5 sales wave stalls.
- **The current implementation does not scale and cannot page.** `DetailsModal` receives the
  entire candidate set as an in-memory `List<Dictionary<string, object>>`
  (`DetailsModal.razor:110`) and filters it client-side with a case-insensitive `Contains`
  across every field (`:120-124`). On a tenant with a large document history that is both a
  memory cost in the Blazor circuit and a latency cost. The replacement is server-paged,
  which is only possible because M2-B02 defines the contract and M2-C05-01 implements the grid.
- **It carries a subtle behaviour that is easy to lose and expensive to lose.** Selections
  are returned **ordered by the sequence in which the user ticked them**, not by row order
  (`DetailsModal.razor:150-154`, `:186-195`, `:205-213`). Operators use this to control the
  line order of the document they are building. A naive rewrite returns rows in grid order
  and nobody notices until a customer receives a delivery challan with the lines in the
  wrong sequence.

## Prerequisites

- **M2-C05-01** — the server-paged `DataGrid` core, including its **detached** query-state mode.
  `RecordPickerDialog` composes it; it does not implement a second table.
- **M2-C04-03** — modal, drawer, confirm dialog, feedback states.
- **M2-C04-02** — form controls for the search field and any header filters.
- **M2-B02** — the paging/sort/filter contract the picker queries through.

## Dependencies

| Dependency | Class | Note |
|---|---|---|
| M2-C05-01 | Hard | The grid inside the dialog, and the detached query-state mode that keeps the dialog off the page URL. Building a bespoke table here would fork the keyboard model and the accessibility work. |
| M2-C04-03 | Hard | Modal with focus trap and `Esc` handling. |
| M2-C04-02 | Hard | Search input and filter controls. |
| M2-B02 | Hard | Paging/sort/filter contract. |
| M2-B10 | Soft | Generated client. The picker is generic over an endpoint, so it must accept the query function as an input rather than importing a specific operation. |
| M2-C05-03 | Soft | Empty/loading/error states inside the dialog. If M2-C05-03 has not landed, the picker still must not ship a blank body — implement a minimal accessible placeholder and note the follow-up. |
| M2-C07 | Deployment | `LineItemGrid` is the primary consumer. Coordinate the callback contract before M2-C07 starts. |
| M5-03, M5-09 | Testing | Component tests and axe. This task's own `axe` scan (test 17) is written here, not deferred to M5-09. |

## Relevant Documentation

| doc_id | Path | Why |
|---|---|---|
| KB-015 | [`architecture/frontend-architecture-existing.md`](../../architecture/frontend-architecture-existing.md#shared-components-22-components) | The trio and the mapping to `RecordPickerDialog`. |
| KB-051 | [`frontend-new/design-system.md`](../../frontend-new/design-system.md#overlays) | `RecordPickerDialog` is specified there as *"the `DetailsModal.razor` replacement: searchable, multi-select, column-configurable picker over an upstream document"*. |
| KB-050 | [`frontend-new/react-architecture.md`](../../frontend-new/react-architecture.md#document-editor-pattern-the-core-abstraction) | Where the picker sits in the editor, and the `record-picker-dialog/` directory name. |
| ADR-007 | [`decisions/ADR-007-angular-stack.md`](../../decisions/ADR-007-angular-stack.md) | Stack; one component library; no client-side money/stock/status decisions. |
| ADR-005 | [`decisions/ADR-005-reporting-and-printing.md`](../../decisions/ADR-005-reporting-and-printing.md) | Export stays server-side — relevant, because the existing dialog exports. |
| — | `tasks/M2-C05-01.md` | The grid API being composed. |

## Relevant Existing Code

| Area | Role |
|---|---|
| `frontend/nexgen-web/src/app/shared/components/record-picker-dialog/` | **TO BE CREATED.** This task. |
| `frontend/nexgen-web/src/app/shared/components/data-grid/` | **TO BE CREATED (M2-C05-01).** Composed. |
| `frontend/nexgen-web/eslint.config.js` | The `no-restricted-imports` rules — one component library only. |
| `V.SMART/V.SMART.Shared/Components/DetailsModal.razor` | Read-only reference — the component being replaced. |
| `V.SMART/V.SMART.Shared/Pages/**` | Read-only — the 33 call sites, surveyed not modified. |

## Investigation Requirements

**Reuse INV-006, plus one new investigation that genuinely matters.**

- **Reuse** INV-006 (Complete) for the component inventory.
- **New:** survey the **33** files that render `<DetailsModal` and classify how each uses it.
  The questions that change the design:
  1. Which are **multi-select line pulls** from an upstream document, and which are
     **single-record master picks**?
  2. Which pass `HeaderContent`, and what do they put in it?
  3. Which rely on `HiddenColumns` and why (is it per-user, per-screen, or per-context)?
  4. Which depend on the `IsNewItem` / `IsQtyChanged` cell highlighting
     (`DetailsModal.razor:218-230`)?
  5. Which depend on **selection order** in the returned list?
  6. How is the candidate set produced today — an existing service method per call site, or
     ad-hoc LINQ in `@code`? This determines how much backend work each future consumer
     needs, and is exactly the sort of thing that turns a "1 week" component into a
     four-week surprise in M3-5.

  Record counts, the classification table, and `file:line` evidence for at least one
  representative of each category, with a confidence rating. **Record negative results** —
  e.g. "no call site passes `HeaderContent`" is a finding that removes an input.

  A read-only helper for the survey — this is a plain `git grep` over the Blazor pages,
  narrowing KB-083's verified `git grep -l "<pattern>" HEAD` row with a pathspec. It is an
  investigation aid, **not** one of this task's verification commands (those are under
  *Verification Commands* and all appear verbatim in KB-083):
  `git grep -l "<DetailsModal" -- "V.SMART/V.SMART.Shared/Pages/"`.

- **Do not** attempt to extract the business logic of any call site. That is `<W>-03` work in
  the module waves ([KB-080 §10](../README.md#10-module-migration-task-pattern)). This
  investigation classifies; it does not migrate.

## Existing Behavior to Preserve

`V.SMART/V.SMART.Shared/Components/DetailsModal.razor` (259 lines), verified 2026-08-12.
All **Confirmed** unless stated.

| # | Existing behaviour | Evidence | Disposition |
|---|---|---|---|
| 1 | Generic over untyped rows: `List<Dictionary<string, object>>` | `:110` | **Replaced with a generic type parameter.** `RecordPickerDialogComponent<TRow>` is typed from the generated client. Untyped dictionaries are why the current dialog needs parallel `Columns`/`ColumnFields` arrays kept in index lockstep (`:58-62`) — a latent bug class. |
| 2 | Three parallel column arrays: `Columns` (display), `ColumnFields` (data keys), `HiddenColumns` (by display name) | `:104-106`, `:42-45`, `:58-62` | **Replaced** by one column-definition list carrying title, field and visibility — the M2-C05-01 column model. Record as an intentional simplification. |
| 3 | Free-text search across **all** `ColumnFields`, case-insensitive `Contains`, applied client-side | `:120-124` | **Preserved in behaviour, moved to the server.** The user-visible contract — "type anything, match anywhere" — stays. The implementation becomes a server query so it works past the first page. |
| 4 | **Select-all applies to the *filtered* rows only**, not the whole data set | `:181-198` (`ToggleSelectAll` iterates `FilteredData`) | **Preserved and made explicit.** With server paging, "all" is ambiguous: the header checkbox selects the **current page** and the UI says so. Silently selecting an unseen 4,000 rows would be a data-integrity hazard. |
| 5 | **Selections are returned ordered by selection sequence**, not row order | `:150-154` (`.OrderBy(w => w.SelectionOrder)`), assigned at `:186-195` and `:205-213` | **Preserved verbatim.** This is load-bearing: it controls line order in the document being built. |
| 6 | Pre-selection honoured from a `"Selected"` key in the incoming row | `:130-134` | **Preserved** as an `initialSelection` input keyed by row id. |
| 7 | Conditional cell highlighting: `ItemCode` yellow when `IsNewItem`, `UtilQty` red when `IsQtyChanged` | `:218-230` | **Replaced by a generic, caller-supplied `getCellState(row, field)`.** Hardcoding `ItemCode`/`UtilQty` in a shared component is domain leakage into presentation — precisely what this migration exists to stop. Colour alone must not carry the meaning ([KB-051](../../frontend-new/design-system.md#status-vocabulary)); add an icon or text. |
| 8 | Footer buttons: **Update** (confirm), **Print** (which actually exports Excel), **Close** | `:89-93`, `:232-256` | **Preserved in function, corrected in labelling.** The button labelled "Print" calls `ExcelExportService.ExportPendingListToExcel` and downloads an `.xlsx` (`:241-244`). Label it **Export**. Record this as a deliberate correction of a mislabel, not a feature change. |
| 9 | Export is server-side (`ExcelExportService`), base64 → JS interop download | `:241-244`; `V.SMART/V.SMART.Shared/Services/ExcelExportService.cs:113` | **Preserved server-side** per [ADR-005](../../decisions/ADR-005-reporting-and-printing.md); the base64 hop is dropped in favour of a blob. |
| 10 | Backdrop is `static` and `data-bs-keyboard="false"` — **`Esc` does not close the dialog** | `:10-11` | **Deliberately changed.** [KB-051 §Accessibility commitments](../../frontend-new/design-system.md#accessibility-commitments) requires modals to trap focus and close on `Esc`. A picker is a non-destructive selection dialog; `Esc` cancels. Record the change. |
| 11 | Empty result renders a spanning **"No data found."** row | `:75-82` | Replaced by the M2-C05-03 empty-state pair (no data vs no results for this search). |
| 12 | `ConfirmSelection` throws `InvalidOperationException("Please select Dc from the list.")` on a null list, and the catch immediately rethrows | `:156-168` | **Do not reproduce.** The guard is unreachable as written (`selectedItems` is the result of `.ToList()` and cannot be null), and the empty case is not handled at all. In the replacement, disable **Update** while nothing is selected. Record this as a defect observed in the superseded component (do **not** fix `DetailsModal.razor` — it is out of scope). |
| 13 | Fixed `modal-xl`, `max-height:60vh` table, `min-width:800px` | `:14`, `:35-36` | Replaced by responsive sizing per [KB-051](../../frontend-new/design-system.md#responsive-behaviour). |

**Business rules:** none is implemented by this component today, and none may be implemented
by its replacement. The dialog selects records; the server decides what selection means.

## Target Result

```html
<app-record-picker-dialog
  [visible]="open()"
  (visibleChange)="open.set($event)"
  header="Pending quotations"
  [columns]="pendingQuoteColumns"
  [fetchPage]="fetchPendingQuotes"
  [getRowId]="byQuoteSubId"
  selectionMode="multiple"
  [initialSelection]="alreadyPulledIds()"
  [disabledRowIds]="alreadyPulledIds()"
  [getCellState]="cellState"
  confirmLabel="Add selected"
  [exportRequest]="exportPendingQuotes"
  (confirm)="appendLines($event)">
</app-record-picker-dialog>
```

`fetchPage` is `(query: DataGridQuery) => Observable<Paged<TRow>>` — a caller-supplied function,
which is what makes one component serve 33 call sites. `confirm` emits rows in **selection
order**.

Behavioural target:

- **Server-paged and server-searched.** Typing debounces and re-queries; results past page 1
  are reachable; the dialog never holds the whole candidate set.
- **Selection survives paging and searching.** Ticking three rows on page 1, paging to page 3
  and ticking two more returns five rows. Selection is keyed by `getRowId` and lives above
  the grid, not in the row objects.
- **Selection order is the return order.** Guaranteed by an insertion-ordered selection
  structure, and asserted by a test.
- **Select-all is page-scoped and labelled as such** ("Select all 25 on this page"), with a
  visible count of the total selection ("5 selected") that persists across pages.
- **Nothing selected → Confirm is disabled**, with an accessible explanation.
- **Keyboard-first.** Open → focus lands in the search field. `↓` moves into the grid.
  `Space` toggles a row. `Enter` confirms. `Esc` cancels. Focus returns to the trigger.
- **Single-select mode** exists and, in it, `Enter` (or double-click / row activation)
  selects and confirms in one action — the master-pick path must not cost two keystrokes.
- **Export** is optional and, when present, calls a server endpoint.

## Implementation Requirements

1. Read the KB documents above; search `docs/kb/investigation-registry.md`.
2. Read `V.SMART/V.SMART.Shared/Components/DetailsModal.razor` in full and re-verify every
   line number in *Existing Behavior to Preserve*. Report any that differ.
3. Run the 33-call-site survey described under *Investigation Requirements* and record it
   **before** fixing the input surface. The survey decides which inputs are real.
4. Create `frontend/nexgen-web/src/app/shared/components/record-picker-dialog/`
   (**TO BE CREATED**) — the directory name KB-050's project structure already reserves.
5. Define the inputs in `record-picker-dialog.model.ts`, generic over `TRow`. The picker takes
   a `fetchPage` function rather than importing a specific generated operation.
6. Implement `record-selection.ts`: an insertion-ordered map keyed by row id
   (`Map<string, TRow>` preserves insertion order and is the natural fit for behaviour 5),
   held in a signal. Expose `toggle`, `selectPage`, `clearPage`, `clear`, `selected` (ordered
   array), `isSelected`, `count`.
7. Implement `record-picker-dialog.component.ts` / `.html`: a PrimeNG `p-dialog`
   (focus-trapped, `Esc`-closable, `[modal]="true"`), containing the search field, an optional
   `header` template slot, `DataGrid` from M2-C05-01, a selection summary, and the footer.
   **No second component library** — ADR-007's one-library rule
   (`ADR-007-angular-stack.md:170-173`), enforced by `eslint.config.js`.
8. Instantiate `DataGridQueryState` in its **detached** mode and wire the search field into its
   filter state, so the dialog's query state is component-local. **The dialog must not write
   to the page URL** — the page behind it owns the URL, and a picker's search term is
   transient. Provide the state at the dialog component's own injector level, never in root.
9. Implement page-scoped select-all with an indeterminate header checkbox and explicit
   labelling.
10. Implement `disabledRowIds` so already-pulled lines render disabled and unselectable, with
    a tooltip. Whether a row *is* already pulled is decided by the caller from server data —
    the dialog never derives it.
11. Implement single-select mode with row activation confirming immediately.
12. Implement optional export by delegating to M2-C05-03's `GridExportService`. Label the
    button **Export**, not "Print".
13. Accessibility pass: `role="dialog"` with `aria-modal`, labelled by the header, focus trap,
    focus restoration, `aria-live` announcement of the selection count, and the grid's own
    ARIA model intact inside the dialog.
14. Write the tests listed under *Testing Requirements*.
15. Run verification commands, review the full `git diff`, update docs and the registry.

### Files expected to change

- `frontend/nexgen-web/src/app/shared/components/index.ts` — barrel export, if one exists.
- `docs/kb/architecture/frontend-architecture-existing.md` (KB-015) — record the survey result
  and the `DetailsModal` defect at `:156-168`.
- `docs/kb/frontend-new/design-system.md` (KB-051) — mark `RecordPickerDialog` as built and
  record the deliberate divergences (`Esc` closes; page-scoped select-all; Export not Print).
- `docs/kb/frontend-new/react-architecture.md` (KB-050) — confirm the *Document editor pattern*
  reference against the delivered input surface.
- `docs/kb/investigation-registry.md` (KB-003), `docs/kb/execution/task-tracker.md` (KB-081).

### Files expected to be created

All **TO BE CREATED**, under
`frontend/nexgen-web/src/app/shared/components/record-picker-dialog/`:

- `record-picker-dialog.component.ts`, `.html`, `.css`
- `record-selection.ts`
- `record-picker-footer.component.ts`, `.html`
- `record-picker-dialog.model.ts`
- `index.ts`
- `record-picker-dialog.component.spec.ts`
- `record-selection.spec.ts`
- `selection-order.spec.ts`
- `record-picker-dialog.a11y.spec.ts`
- `test-fixtures.ts`

### Files that must not change

- `V.SMART/**` — including `DetailsModal.razor`, which keeps serving the Blazor app
  unchanged, defects and all, until its call sites are migrated wave by wave. **Do not fix
  the unreachable guard at `DetailsModal.razor:156-168` in this task** — record it.
- `frontend/nexgen-web/src/app/shared/components/data-grid/**` — compose it; if it needs a new
  capability, that is a change request against M2-C05-01, not an in-place edit.
- `frontend/vsmart-erp/**`, `db/**`, `docs/kb/decisions/**`.

## API Changes

Not applicable — this task adds no HTTP surface. `RecordPickerDialog` is generic over a
caller-supplied `fetchPage` function that conforms to M2-B02's paged contract, so it adds no
endpoint of its own and hardcodes no resource.

If the survey shows that many call sites have **no** server endpoint capable of producing
their candidate list (likely, since candidate sets are assembled in Razor `@code` today),
record that as the finding it is: it is per-wave `<W>-03`/`<W>-06` backend work, and it
should appear in the M3-5 estimate rather than surfacing as a surprise.

## Frontend Changes

| Aspect | Detail |
|---|---|
| **Route** | n/a — a shared overlay component. |
| **Component** | Standalone `RecordPickerDialogComponent<TRow>` under `frontend/nexgen-web/src/app/shared/components/record-picker-dialog/` (**TO BE CREATED**), composing `DataGrid` (M2-C05-01) inside a PrimeNG `p-dialog` (M2-C04-03). Replaces `V.SMART/V.SMART.Shared/Components/DetailsModal.razor`. |
| **API integration** | Caller-supplied `fetchPage`, executed through the grid's typed service over `HttpClient`. The dialog imports no specific generated operation. Previous rows stay visible while the next page loads. |
| **State** | Selection in an insertion-ordered map held in a signal, provided at the dialog's own injector level. Query state is `DataGridQueryState` in **detached** mode — **never** written to the page URL. No global state service. |
| **Validation** | n/a. Confirm is disabled when the selection is empty; that is an affordance, not validation. |
| **Permissions** | n/a inside the dialog. The caller decides whether the picker may open. A 403 from `fetchPage` renders the permission-denied state inside the dialog body. |
| **Loading state** | Skeleton rows inside the dialog body; the dialog frame, search field and footer stay stable so the dialog does not resize as data arrives. |
| **Empty state** | Two variants: no candidate records at all, versus no matches for the current search (with **Clear search**). |
| **Error state** | `ErrorState` inside the dialog body with the server message, correlation id and Retry. The dialog stays open — closing it on error would lose the user's existing selection. |
| **Accessibility** | `role="dialog"`, `aria-modal="true"`, labelled by the header; focus trapped; focus moves to the search field on open and returns to the trigger on close; `Esc` cancels (a **deliberate change** from `data-bs-keyboard="false"` at `DetailsModal.razor:11`); selection count announced via `aria-live="polite"`; disabled rows expose `aria-disabled` with an accessible reason; the grid's ARIA model is preserved inside the dialog. |
| **Responsive behaviour** | ≥1024 px: large centred modal. 768–1023 px: near-full-screen modal with the grid dropping low-priority columns. <768 px: full-screen sheet with a stacked row layout; per [KB-051](../../frontend-new/design-system.md#responsive-behaviour) phone is read-and-approve, so the picker is available for viewing and single-select but is not the supported way to build a 40-line document. |

## Business Rules

**None.** `RecordPickerDialog` is new code. What is new is *record selection interaction*:
search, page, select, order, confirm.

**No ERP business rule is implemented client-side by this task.** Explicitly:

- The dialog does **not** decide which records are eligible to be pulled. `disabledRowIds`
  and the candidate list both come from the caller, which gets them from the server.
- The dialog does **not** compute balance quantities, check for duplicate lines, validate
  against an upstream document's remaining quantity, or apply any pricing. Those are
  server-side rules — balance-quantity derivation across `Ref*SubId` chains is INV-013,
  scheduled for M3-5, and must not be pre-empted in TypeScript.
- The dialog does **not** apply the domain-specific cell highlighting the old component
  hardcoded (`ItemCode`/`IsNewItem`, `UtilQty`/`IsQtyChanged` at `DetailsModal.razor:218-230`).
  It exposes a generic `getCellState` input and lets the caller — eventually, the server's
  row data — decide.

The one behaviour that *is* preserved verbatim is **selection ordering**
(`DetailsModal.razor:150-154`), and it is preserved because it is user-visible workflow, not
because it is a business rule.

## Testing Requirements

Unit tests run on the Angular CLI's `@angular/build:unit-test` builder with
`@testing-library/angular`, invoked by `npm run test:ci` (KB-083 § Verified frontend commands).
HTTP is stubbed with `provideHttpClientTesting`.

| # | Test | Asserts |
|---|---|---|
| 1 | Opens with focus in the search field; `Esc` closes and restores focus to the trigger | Accessibility + deliberate divergence |
| 2 | Typing debounces and issues a **server** query; results are not filtered client-side | Server search |
| 3 | Selection made on page 1 persists after paging to page 3 and back | Cross-page selection |
| 4 | Selection persists across a search change | Cross-search selection |
| 5 | **Confirm emits rows in the order they were selected, not grid order** — select row 5, then row 2, then row 9; expect `[5, 2, 9]` | Behaviour 5, the load-bearing one |
| 6 | Header checkbox selects **only the current page**, shows indeterminate, and the label states the page scope | Behaviour 4, made explicit |
| 7 | Total selection count is displayed and survives paging | UX |
| 8 | Confirm is disabled with zero selected and has an accessible explanation | Replaces the unreachable guard at `DetailsModal.razor:156-168` |
| 9 | `initialSelection` pre-selects the matching rows | Behaviour 6 |
| 10 | `disabledRowIds` rows are unselectable, `aria-disabled`, and excluded from select-all | Correctness |
| 11 | Single-select mode: row activation selects and confirms in one action | Master-pick path |
| 12 | A 403 renders the permission-denied state inside the dialog; the dialog stays open | Error UX |
| 13 | An error does not clear the existing selection | Data-loss prevention |
| 14 | Export delegates to the server endpoint; no client-side file generation | ADR-005 |
| 15 | The dialog never mutates the page URL query string | Isolation |
| 16 | No domain field name appears in the component directory outside test fixtures | Domain-leakage guard |
| 17 | `a11y.spec.ts` — `axe` reports no critical violations with the dialog open and empty, and open and populated | Runtime a11y scan, carried over unchanged |

**Accessibility verification.** Test 17's `axe` scan is carried over **unchanged** from the
specification this file replaces: a runtime scan is stack-independent, so it survives the
Angular re-specification, and the `angular-eslint` template rules do **not** substitute for it
(Q-69, answered 2026-08-22 in the conservative direction — the criterion is restored;
`../../open-questions.md`). No `axe` package is installed in `frontend/nexgen-web/` today
(`package.json`, verified 2026-08-22), and KB-083's verified table has no `axe` command — but
**M2-C04-01** installs `axe-core` as a dev dependency (`M2-C04-01.md:388`), reached through this
task's Hard `depends_on: M2-C05-01` → `M2-C04-02` → `M2-C04-01`, so it is present before this
task runs. Drive it from `a11y.spec.ts` under the existing `npm run test:ci`; do **not** invent
a new npm script or add a dependency here.

Running **in addition, not instead**: `npm run lint` runs `angular-eslint`'s template
accessibility rules **as errors** (`frontend/nexgen-web/eslint.config.js:97-103`); tests 1, 8
and 10 cover focus and disabled-row semantics; the rest is a recorded manual keyboard pass. The
repository-wide `axe`-in-CI pass remains **M5-09's** scope.

## Acceptance Criteria

- [ ] `RecordPickerDialogComponent<TRow>` exists, is generic, and takes a `fetchPage` function
      rather than importing a resource-specific operation.
- [ ] It composes `DataGrid` from M2-C05-01; no second table implementation exists in the
      directory.
- [ ] Search and paging are server-driven; the component never receives or holds the full
      candidate set.
- [ ] Selection is keyed by `getRowId`, survives paging and search changes, and is emitted
      **in selection order**, proven by test 5.
- [ ] Select-all is page-scoped, indeterminate-capable, and labelled with its scope.
- [ ] Confirm is disabled while nothing is selected.
- [ ] `Esc` cancels and focus is restored to the trigger — a deliberate, documented divergence
      from `DetailsModal.razor:10-11`.
- [ ] No domain field name (`ItemCode`, `UtilQty`, `IsNewItem`, `IsQtyChanged`, or any other)
      appears anywhere in the component directory: `grep -rniE 'itemcode|utilqty|isnewitem|isqtychanged'`
      returns no hits outside test fixtures.
- [ ] The export control is labelled **Export** and calls a server endpoint; no client-side
      file-generation library is added.
- [ ] The dialog does not write to the page URL.
- [ ] The 33-call-site survey is complete and recorded in the investigation registry with
      counts, a classification table, and `file:line` evidence.
- [ ] The `DetailsModal.razor:156-168` unreachable-guard defect is **recorded, not fixed**.
- [ ] All 17 tests pass; `axe` reports no critical violations (test 17).
- [ ] No second component library is imported; `npm run lint` passes with the
      `no-restricted-imports` rules in force.
- [ ] `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run test:ci` and
      `npm run build` all pass.
- [ ] Nothing under `V.SMART/` appears in the diff.

## Verification Commands

Every `npm` command below appears verbatim in
[KB-083 § Verified frontend commands](../prompt-template.md#verified-repository-commands),
measured on this workstation on 2026-08-21. Run them from `frontend/nexgen-web/`.

```bash
npm ci
```

```bash
npm run typecheck
```

```bash
npm run lint
```

```bash
npm run format:check
```

```bash
npm run test:ci
```

```bash
npm run build
```

Plus this repository command, which is its own row in KB-083 § Verified repository
commands, run from the repository root:

```bash
git status --porcelain
```

> To confirm the .NET solution was untouched, read the full `git diff` rather than quoting a
> diff invocation KB-083 has not verified. There is **no coverage script** in
> `frontend/nexgen-web/package.json` and no coverage row in KB-083 — do not invent one.

## Documentation Updates

| Document | Change | Frontmatter |
|---|---|---|
| `docs/kb/architecture/frontend-architecture-existing.md` (KB-015) | Record the 33-call-site survey: the multi-select-pull vs single-master-pick split, `HeaderContent`/`HiddenColumns`/cell-highlight usage, selection-order dependence, and how candidate sets are produced today. Record the `DetailsModal.razor:156-168` defect. | Bump `last_verified` |
| `docs/kb/frontend-new/design-system.md` (KB-051) | Mark `RecordPickerDialog` as built; record the three deliberate divergences (`Esc` closes, page-scoped select-all, Export not Print) and the generic `getCellState` replacing hardcoded domain highlighting. | Bump `last_verified` |
| `docs/kb/frontend-new/react-architecture.md` (KB-050) | Confirm the *Document editor pattern* reference to `RecordPickerDialog` against the delivered input surface. | Bump `last_verified` |
| `docs/kb/execution/task-tracker.md` (KB-081) | M2-C06 status. | — |
| `docs/kb/investigation-registry.md` (KB-003) | The survey + findings below. | — |

### Investigation registry updates

```yaml
Finding:        DetailsModal.razor is the ERP's universal record picker: it is rendered
                by 33 page files, takes untyped List<Dictionary<string,object>> rows with
                three parallel column arrays, filters client-side across all fields, scopes
                select-all to the FILTERED rows, and returns selected rows ORDERED BY THE
                SEQUENCE THE USER SELECTED THEM rather than by row order.
Evidence:       V.SMART/V.SMART.Shared/Components/DetailsModal.razor:104-124, :150-154,
                :181-198, :205-213
Business rule:  n/a
Confidence:     Confirmed
Last verified:  2026-08-12
```

```yaml
Finding:        DetailsModal.ConfirmSelection contains an unreachable null guard — it tests
                the result of .ToList() for null and throws
                InvalidOperationException("Please select  Dc from the list."), while the
                catch immediately rethrows. The genuinely reachable case, an EMPTY
                selection, is not handled at all.
Evidence:       V.SMART/V.SMART.Shared/Components/DetailsModal.razor:144-169
Business rule:  n/a
Confidence:     Confirmed
Last verified:  2026-08-12
```

```yaml
Finding:        DetailsModal hardcodes domain-specific cell highlighting in a shared
                component: ItemCode is highlighted yellow when the row carries IsNewItem,
                and UtilQty red when it carries IsQtyChanged.
Evidence:       V.SMART/V.SMART.Shared/Components/DetailsModal.razor:218-230
Business rule:  n/a
Confidence:     Confirmed
Last verified:  2026-08-12
```

Plus the full call-site classification table, and a negative result for any expected usage
pattern that turns out not to exist. Check `git branch --no-merged master` before claiming any
new Q/R/INV id.

## Completion Conditions

- All acceptance criteria met, with each verification command's **observed** output recorded.
- The 33-call-site survey recorded **before** the input surface was fixed.
- Selection-order behaviour proven by test 5.
- The branch left for review, unmerged.
- **Only the repository owner may set `Completed`**
  ([KB-088](../workflow.md#who-may-set-completed)).

## Git Strategy

| Field | Value |
|---|---|
| **Branch** | `migration/M2-C06-record-picker-dialog` |
| **Commit** | `M2-C06: Implement RecordPickerDialog replacing DetailsModal` |
| **Expected changed files** | `frontend/nexgen-web/src/app/shared/components/record-picker-dialog/**` (new), `frontend/nexgen-web/src/app/shared/components/index.ts`, `docs/kb/architecture/frontend-architecture-existing.md`, `docs/kb/frontend-new/design-system.md`, `docs/kb/frontend-new/react-architecture.md`, `docs/kb/investigation-registry.md`, `docs/kb/execution/task-tracker.md` |
| **Rollback approach** | Revert the branch's merge commit. Purely additive: no Blazor call site is migrated by this task, `DetailsModal.razor` keeps serving all 33 pages, and no server or database state is touched. The only cost of a revert is that M2-C07's "pull from upstream" path loses its dialog and must be stubbed. |

Never merge or push to `master` from an execution session.

---

## Execution Record (2026-08-26)

**Status: `Needs Review`.** Implemented on branch `migration/M2-C06-record-picker-dialog`,
unmerged. Per [KB-088 § Who may set COMPLETED](../workflow.md#who-may-set-completed) only the
repository owner may close this to `Completed`, and that has not happened.

### The survey was run first, and it changed the design

Recorded as **INV-054** in [`investigation-registry.md`](../../investigation-registry.md)
before the input surface was fixed, as *Implementation Requirements* 3 demands. Four of its
findings changed what was built:

1. **41 instances, not 33.** Five files render `<DetailsModal>` more than once.
2. **All 41 are multi-select upstream-document pulls; none is a single-record master pick.**
   `selectionMode="single"` therefore preserves **nothing that exists today** — it is new
   capability, aimed at `CustomerSelection`/`VendorSelection`, and it is documented as such in
   `record-picker-dialog.model.ts` rather than presented as parity. Whether any call site is
   *functionally* single-pick is not answerable from source → **Q-93**.
3. **Pre-selection is dead code** — `["Selected"]` is written `false` in 75 places and `true`
   in none — so behaviour 6 preserves nothing observable either. `initialSelection` was built
   anyway, for the already-pulled case, and labelled new capability.
4. **`HiddenColumns` is passed by all 41 and is load-bearing**: the hidden fields are exactly
   the id keys the selection handlers read out of the returned rows. **Hidden must not mean
   absent**, which is why the column model carries a visibility flag and still delivers the
   value.

**All 13 line ranges in *Existing Behavior to Preserve* were re-verified against
`DetailsModal.razor` (259 lines). Every one is accurate; none is stale.** No correction to
report.

### Deviations from the task file, each deliberate

| Deviation | Why |
|---|---|
| Requirement 12 says export delegates to **M2-C05-03's `GridExportService`**. It does not exist — M2-C05-03 has not landed, and it is a *Soft* dependency. | The `exportRequest` input takes the caller's server call directly and downloads the blob. `TODO(M2-C05-03)` marks the seam; the contract — the server produces the file — is identical either way. |
| Requirement 7 says "a PrimeNG `p-dialog`". The component composes **`app-modal`** (M2-C04-03), which *is* a `p-dialog` with `[modal]="true"`, `[focusTrap]`, `closeOnEscape` and focus restoration. | Reimplementing the frame would fork focus management from the layer whose own specs already prove it. M2-C04-03 is a Hard dependency for exactly this. |
| Requirement 8 says provide the query state "at the dialog component's own injector level". It is an **instance field**, not a provider. | Strictly narrower: an instance field cannot be reached from any injector at all, let alone root. The requirement's intent — never in root, never shared — is met more tightly than a provider would meet it. |
| Requirement 10 (`disabledRowIds`) and behaviour 7 (`getCellState`) need per-row and per-cell rendering that **`DataGrid` does not expose**, and this task may not edit `DataGrid`. | Selection correctness is enforced in TypeScript (`onSelectionProposed` refuses disabled ids; select-all excludes them). The *visual and ARIA* half is applied to the rendered DOM by `#decorate()`, reached by `:host ::ng-deep`. Recorded as **R-78**: the fix is a change request against M2-C05-01, then delete `#decorate()`. |
| The free-text search parameter is an **input** (`searchParam`, default `'search'`) rather than a fixed wire name. | M2-B02 defines no such parameter (`PagedQuery.cs:36-44`). Inventing one silently would be inventing a contract → **Q-91**; whether server search should still match hidden id columns → **Q-92**. |

### Verification — observed output, 2026-08-26

Node v24.19.0, npm 11.17.0 (the version note in `current-task.md` was re-checked; no `nvm`
switch was needed this session). Run from `frontend/nexgen-web/`.

| Command | Observed |
|---|---|
| `npm run typecheck` | Passed — no output beyond the script banner (all three `tsc -p … --noEmit` projects). |
| `npm run lint` | `All files pass linting.` |
| `npm run format:check` | `All matched files use Prettier code style!` |
| `npm run test:ci` | `Test Files 54 passed (54)` · `Tests 386 passed (386)`. |
| `npm run build` | `Application bundle generation complete. [7.810 seconds]`, initial total 571.20 kB / 136.72 kB transfer (re-run after the final test addition: `[5.988 seconds]`). |
| `git status --porcelain` (repo root) | Only `frontend/nexgen-web/src/app/shared/components/index.ts` modified and the new `record-picker-dialog/` directory, plus the KB documents. **Nothing under `V.SMART/`.** |
| `grep -rniE 'itemcode\|utilqty\|isnewitem\|isqtychanged'` in the component directory | No hits — exit status 1. |

`npm ci` was **not** re-run: `node_modules/` was already installed and unchanged, no
dependency was added or removed, and re-running it would have proved nothing this session's
`build` and `test:ci` did not. Stated rather than claimed.

**Timing-flake correction, 2026-08-26 (validation retry 2).** Independent validation caught
`test:ci` failing where this record had claimed it green, and the follow-up fix caught only
part of it. Two distinct harness defects, both in this task's own specs, neither in the
component:

1. `record-picker-dialog.component.spec.ts` drove the search box with `userEvent.type`, which
   awaits between keystrokes; one keystroke plus a change-detection pass over the dialog costs
   more than the host fixture's 5 ms debounce window (`test-fixtures.ts:105`), so the debounce
   committed once per character and three requests were outstanding when `expectOne` ran.
   Fixed by dispatching the keystrokes synchronously, the idiom
   `data-grid.component.spec.ts:167-170` already uses.
2. Both dialog-rendering spec files sit near vitest's 5 s default — measured 5293 ms for
   `selection-order.spec.ts` and 5561 ms for test 3 — so which test goes red is decided by
   machine load. Both files now set `vi.setConfig({ testTimeout: 30_000 })`. A timeout is a
   harness ceiling, not an assertion: **no assertion was weakened, widened or removed.**

Re-verified after (2): `npm run test:ci` × 3 consecutive, each
`Test Files 54 passed (54)` · `Tests 386 passed (386)`, exit 0; `npm run typecheck` exit 0;
`npm run lint` `All files pass linting.`; `npm run format:check`
`All matched files use Prettier code style!`; `npm run build`
`Application bundle generation complete. [9.004 seconds]`.

The 17 required tests are distributed as: **1–4, 6–16** in
`record-picker-dialog.component.spec.ts` (16 tests), **5** alone in `selection-order.spec.ts`
— its own file so it is hard to delete by accident — and **17** in
`record-picker-dialog.a11y.spec.ts`, which also asserts `aria-modal`, the dialog label and the
polite selection-count announcement. `record-selection.spec.ts` adds 9 unit tests for the
ordering, page-scope and adoption properties the component's promises rest on.

### Known limits, stated rather than hidden

- **jsdom cannot run axe's `color-contrast` rule** (no stylesheet, no layout), so test 17
  disables it, exactly as the overlay and form layers' own a11y specs do. Contrast is covered
  by computation in `core/theme/contrast.spec.ts`. The repository-wide axe-in-CI pass is still
  M5-09's.
- **The keyboard model is asserted only where a test can see it.** Focus-into-search on open,
  `Esc`-to-cancel with focus restoration, disabled-row semantics and the disabled confirm
  button are covered by tests 1, 8 and 10; `↓` into the grid, `Space` to toggle and `Enter` to
  confirm come from `DataGrid`'s own keyboard model, which has its own specs. Row activation
  in single-select mode is asserted through a double click (test 11), not through `Enter`.
- **`#decorate()` is coupled to `DataGrid`'s class names and cell coordinates** — see R-78.
  Selection correctness does not depend on it; the visual and ARIA half does.
- **No call site was migrated, and nothing under `V.SMART/` was touched.**
  `DetailsModal.razor` keeps serving all 33 pages, its unreachable guard at `:156-168`
  included — recorded in KB-015, not fixed.
