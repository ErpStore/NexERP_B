---
doc_id: TASK-M2-C09
title: ReportPage framework
module: execution
task_id: M2-C09
milestone: M2
parent_task: none
task_type: Frontend
priority: P1
status: Not Started
estimate: 1 wk
depends_on: [M2-C05-01, M2-B08]
source_files:
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/ReportService/TrackReportService/ReportExecutor.cs
api_endpoints:
  - "GET /api/v1/reports/{slug}"
  - "GET /api/v1/reports/{slug}/export"
  - "GET /api/v1/{resource}/{id}/print"
business_rules: [BR-RPT-001]
confidence: n/a
last_verified: 2026-08-12
---

# M2-C09 — `ReportPage` framework

> # ⛔ STOP — this specification is superseded. Do not implement it as written.
>
> **[`ADR-007`](../../decisions/ADR-007-angular-stack.md) replaced React with Angular + PrimeNG on
> 2026-08-20.** Everything below that names **React, Vite, Mantine, TanStack, Zustand, React Hook
> Form, Zod, `.tsx`, hooks or JSX is stale.** Its *intent* is still valid; its *implementation
> detail is not.*
>
> **If you are a runner and selected this task: stop and report.** Re-specifying it is a
> documentation change requiring the owner, not something to infer mid-implementation. Building
> React here would add to work that is already being discarded.
>
> **What survives, and is the reason this file was not deleted:** the acceptance criteria's
> *intent*, the ERP behaviour being reproduced, the `file:line` evidence about the **existing
> Blazor** implementation, the business rules, and the dependency relationships. None of that was
> ever a React decision.
>
> **What must be re-derived:** every stack-specific instruction, file path under
> `frontend/nexgen-web/`, component API, test-harness detail and command.
>
> **Gating task:** **`M2-C00`** rewrites [KB-050](../../frontend-new/react-architecture.md) for
> Angular. Until that lands there is no authoritative structure to specify against, which is
> precisely why these files were left stale rather than rewritten in a hurry against a guess.

| Field | Value |
|---|---|
| Task ID | M2-C09 |
| Milestone | M2 — Foundation |
| Parent | — |
| Type | Frontend |
| Priority | P1 |
| Status | Not Started |
| Estimate | 1 wk |
| Gate | G2 |

## Task Objective

Build **one** declarative `ReportPage` framework so that the ~40 existing report screens become
~40 **configuration objects** rather than ~40 React components.

A report definition supplies:

1. **Parameters** — what the user fills in before running.
2. **Columns** — what the result grid shows.
3. **The procedure slug** — which server report to call.
4. **Export options** — which of `xlsx | pdf | csv` are offered.

Plus a **PDF viewer** for `GET /api/v1/{resource}/{id}/print`, rendered from a blob URL.

**In scope for M2:** the framework, the definition contract, the registry, the parameter panel,
the result grid, the export and print affordances, the long-running/cancel state, and the
right-filtered report picker — proven end-to-end by **one or two** real report definitions.

**Not in scope:** the other ~38 definitions. Writing configuration objects is per-report work in
the report wave, and it is cheap **only after** this framework exists. Adding them here would
turn a one-week framework task into a multi-week data-entry task and would bury the framework's
defects under volume.

## Why This Task Exists

- **[ADR-005](../../decisions/ADR-005-reporting-and-printing.md) mandates it**, in as many
  words: *"The ~40 report screens are built from one declarative `ReportPage` framework — a
  report definition supplies parameters, columns, the procedure slug, and export options. Forty
  screens become forty configuration objects."* [KB-052's feature map](../../frontend-new/feature-mapping.md)
  reaches the same conclusion independently: all ~40 report screens *"share one shape:
  **parameter panel → server-paged grid → export**"*, rating the work *"Medium overall, **Low
  per report** after the framework exists."*
- **[KB-053](../../frontend-new/page-map.md) already routes them:** *"All become
  `/reports/{slug}` driven by one `ReportPage` framework."*
- **Forty bespoke report screens is how the existing codebase got to 333 Razor pages.** The
  reason ~40 screens can collapse into one component is that they genuinely differ only in
  data, not in behaviour. Every one of them is: collect parameters, POST-equivalent them to a
  stored procedure, page the rows, export the rows. Building the fortieth report should be
  writing an object literal, not opening an editor.
- **The 300-second timeout is real, and a spinner is not an adequate answer to it.**
  `ReportExecutor` sets `SetCommandTimeout(300)`. A user staring at an indeterminate spinner for
  four minutes concludes the system has hung and reloads — re-issuing a five-minute query
  against a database that is still running the first one.
- **Reports bypass the service layer entirely**, so `UserRight` does not gate report *data*
  (ADR-005). The picker must be right-filtered client-side for usability, **and** the server
  must enforce independently. This is the ADR-004 §3 rule applied to reports.

## Prerequisites

- **M2-C05-01** — the `DataGrid` core. The result grid is a `DataGrid`, not a new table. Its
  task file states that `ReportPage` declares it as a hard dependency.
- **M2-C05-03** — grid states and server-side export. The report grid inherits the five states
  and the no-client-file-generation rule. **Do not re-derive either.**
- **M2-B08 — report + print endpoints.** **Hard.** Without them this task has nothing to call.
  Read `docs/kb/execution/tasks/M2-B08.md` for the planned contracts, then **verify against the
  produced controllers** — a task file is a plan, not evidence. If M2-B08 has not landed, see
  *API Changes*.
- **M2-C04-03** — feedback primitives (`EmptyState`, `ErrorState`, `Skeleton`, `Toast`,
  `PermissionDeniedState`, `BusyOverlay`). Composed, never re-created.
- **M2-C02 / M2-C03** — auth and the permission store, for the right-filtered picker.
- **R-04 is ADR-005's mandatory prerequisite** and belongs to the backend: only 12 of the 94
  stored procedures the application calls have DDL in source control. That is **M2-B08's**
  blocker, not this task's — but if it is still open, the endpoints this task consumes cannot be
  trusted in a fresh environment. **Check R-04's status and record it.**

## Dependencies

| Dependency | Class | Note |
|---|---|---|
| M2-C05-01 | Hard | The `DataGrid` core. The result grid is a `DataGrid`. |
| M2-C05-03 | Hard | Grid states, the export blob/`Content-Disposition` pattern, and the `noClientFileGeneration` guard. Reuse; do not fork. |
| M2-B08 | **Hard** | The three endpoints. Read its task file for the plan; verify the built controllers. |
| M2-C04-03 | Hard | Feedback primitives. |
| M2-C02 / M2-C03 | Hard | The permission store and navigation, for the right-filtered picker. |
| M2-A06 | Hard | `ProblemDetails` + correlation id — essential when a report times out. |
| M2-B10 | Soft | Generated client, including the binary-response operations. |
| M2-C10 | **Hard** | decimal.js. Report grids display money. Amounts render through M2-C10's formatting, never JS floats. |
| M2-C06 | Soft | Form primitives for the parameter panel. |
| ADR-005 | Information | Binding. React never generates a PDF, a spreadsheet or a CSV. |
| M5-09 | Testing | axe in CI + a manual keyboard pass. |
| Report wave | **Consumer** | Writes the remaining ~38 definitions against this contract. |

## Required Existing Knowledge

| Doc | Why |
|---|---|
| [ADR-005](../../decisions/ADR-005-reporting-and-printing.md) | The whole decision — the endpoints, the framework mandate, the 300-second consequence, and the `UserRight` gap. |
| [ADR-003](../../decisions/ADR-003-react-stack.md) | Stack and non-negotiable rules. |
| [ADR-004 §3](../../decisions/ADR-004-server-side-authorization.md) | Rights are for rendering; the server re-checks. Applies verbatim to the picker. |
| [ADR-002 §4](../../decisions/ADR-002-rest-api-layer.md) | The error contract. |
| [KB-052 §Reports and Dashboard](../../frontend-new/feature-mapping.md) | The ~40 screens, grouped, and the "one shape" finding. |
| [KB-053 §Reports](../../frontend-new/page-map.md) | The existing route → proposed slug table. **Use these slugs; do not invent new ones.** |
| [KB-051 §State patterns](../../frontend-new/design-system.md) | The five states, plus what a long-running state must look like. |
| [KB-050](../../frontend-new/react-architecture.md) | Error handling and page composition. |
| M2-C05-01 and M2-C05-03 task files | The `DataGrid` contract and its export pattern. |
| M2-B08's task file | The endpoint contracts — **if it exists yet**. |

## Investigation Requirements

**Reuse INV-009** (Complete — *Reporting: FastReport + stored procedures*) and
[ADR-005](../../decisions/ADR-005-reporting-and-printing.md). **Do not re-derive the reporting
architecture** and do not go reading `.frx` files.

Five narrow confirmations, each recorded with `file:line` evidence:

1. **The three endpoint contracts as M2-B08 actually built them.** Read the produced
   controllers, not the plan. For `GET /api/v1/reports/{slug}`: how are parameters passed
   (query string? typed?), what is the paged envelope, and **is it the same envelope M2-B02
   defined for list endpoints** — because if the report grid needs a second paging shape, that
   is a finding worth escalating rather than absorbing.
2. **How the parameter *metadata* reaches the client.** Does the server describe a report's
   parameters (a `GET /api/v1/reports/{slug}/definition`, or an OpenAPI schema), or must the
   client hold the definitions? **This is the single most consequential unknown in the task.**
   If the client holds them, a parameter change in a stored procedure silently breaks a report
   with no compile-time signal, and that risk must be recorded, not absorbed silently.
3. **Whether the export honours the current parameters, sort and filter.** M2-C05-03 raises the
   same question for list export and calls an export that ignores filters *a defect to record,
   not to paper over*. A report exported with different parameters than the ones on screen is
   worse: the user has no way to tell.
4. **Whether the report endpoints carry `[RequireScreen]` / `[RequireRight]`.** ADR-005 requires
   it explicitly because reports bypass the service layer. **If they do not, that is a security
   finding to escalate to M2-B08 and M2-A08 — not something the client can compensate for.**
   Record it either way, including the negative.
5. **What a 300-second timeout actually returns.** A `500`? A `504`? A `ProblemDetails` with a
   `SqlException` message? An aborted connection with no response at all? The UI's most
   important error state depends on the answer, and it cannot be guessed.

**Record every negative result.** If an endpoint does not exist, that is a finding.

**Do not** investigate individual stored procedures, `.frx` templates, or per-report business
rules. That is report-wave work.

## Repository Areas

| Area | Role |
|---|---|
| `frontend/nexgen-web/src/features/reports/` | **TO BE CREATED.** The framework's home. |
| `frontend/nexgen-web/src/shared/components/DataGrid/` | **TO BE CREATED (M2-C05-01/03).** Composed here, unmodified. |
| `frontend/nexgen-web/src/shared/lib/decimal/` | **TO BE CREATED (M2-C10).** All amount formatting. |
| `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/ReportService/TrackReportService/ReportExecutor.cs` | **Read-only reference.** The server-side executor that stays. |

## Existing Behavior to Preserve

Verified **2026-08-12**. **Re-verify every line number.**

| # | Behaviour | Evidence | Confidence | Disposition |
|---|---|---|---|---|
| 1 | `ReportExecutor.ExecuteAsync<T>(string procedureName, params SqlParameter[])` builds `EXEC dbo.{procedureName}` with the parameter names appended, and runs it through `FromSqlRaw(...).AsNoTracking().ToListAsync()`. | `ReportExecutor.cs:21-47`, specifically `:27-39` | **Confirmed** | **Preserve.** Reached over HTTP instead of in-process. Not modified by this task. |
| 2 | **`SetCommandTimeout(300)` — the 300-second timeout is real.** | `ReportExecutor.cs:25` | **Confirmed** | **The reason this task needs a long-running state.** |
| 3 | The DataTable path sets `command.CommandTimeout = 300` independently. | `ReportExecutor.cs:88-125`, specifically `:107` | **Confirmed** | Same consequence, second code path. |
| 4 | **`ExecuteAsync<T>` performs NO paging.** It executes the procedure and materialises the **entire** result set with `ToListAsync()`. A commented-out variant with `@Skip`/`@Take` exists at `:48-86` and is **dead code**. | `ReportExecutor.cs:35-39`; `:48-86` (commented out) | **Confirmed** | **A finding that shapes the whole task.** ADR-005 promises *paged JSON*, but the executor cannot page. Either M2-B08 pages after materialising (memory cost, no latency benefit) or the procedures gain paging parameters (an R-04-scale change to 94 procedures). **Confirm which M2-B08 chose, and design the grid's paging expectations around the answer rather than assuming server paging is cheap.** |
| 5 | Failures are wrapped as `throw new Exception($"Error while executing procedure: {procedureName}", ex)` — the procedure name is exposed in the message. | `ReportExecutor.cs:41-46`, `:121-124` | **Confirmed** | **Do not surface this string to end users.** It leaks a database object name and means nothing to an operator. The UI shows M2-A06's `ProblemDetails` and the correlation id. Flag it to M2-B08 if it reaches the wire. |
| 6 | 104 FastReport `.frx` templates across 5 tenant folders, rendered by `ReportService.Generate_Report`, with per-tenant override and per-screen `PrintSetting` (watermark, logo, ISO logo, copy count, copy name). | [ADR-005 §Context](../../decisions/ADR-005-reporting-and-printing.md) (INV-009) | **Confirmed** | **Untouched.** React renders the resulting PDF from a blob URL and never generates one. |
| 7 | 94 stored procedures surface on ~40 report screens. | [ADR-005 §Context](../../decisions/ADR-005-reporting-and-printing.md) (INV-009) | **Confirmed** | Untouched. |
| 8 | **Reports bypass the service layer and query the database directly, so `UserRight` does not gate report data.** | [ADR-005 §Consequences](../../decisions/ADR-005-reporting-and-printing.md) | **Confirmed** | The picker filters by right for **usability**; the server enforces for **security** (M2-B08 + ADR-004). Investigation item 4. |
| 9 | **R-04:** only 12 of the 94 procedures have DDL in source control (13 `.sql` files exist; `Sp_Print_PurchaseOrder.sql` is dead code). ADR-005 names fixing this a **mandatory prerequisite**. | [ADR-005 §Mandatory prerequisite](../../decisions/ADR-005-reporting-and-printing.md) | **Confirmed** | Backend/M2-B08's blocker. **Check and record its status**; it is not this task's to fix. |
| 10 | The dashboard's Blazor-ApexCharts is replaced by Recharts. | [ADR-005 §Consequences](../../decisions/ADR-005-reporting-and-printing.md) | **Confirmed** | **Out of scope.** The dashboard is its own screen; `DashboardService` is 3,095 LOC. This task builds report *pages*. |

**Business rule involved:** **BR-RPT-001** family ([KB-030](../../business-rules/business-rule-inventory.md))
— **not implemented here, only invoked.** Every report's logic lives in its stored procedure.
This task's only obligation is that it passes the parameters through faithfully and displays
what comes back.

## Target Result

### The report definition contract

```
ReportDefinition = {
  slug            the server slug — from KB-053's table, never invented
  title, group    for the picker
  screenName      the Screens.ScreenName this report belongs to, for
                  right-filtering (presentation only)
  parameters[]    typed descriptors: name, label, kind
                  (date | dateRange | select | multiSelect | text |
                   number | boolean | lookup), required, default,
                  and for lookups the reference-data source (M2-B09)
  columns[]       DataGrid column definitions, including which are
                  amounts (formatted through M2-C10) and which are
                  right-aligned
  exports[]       which of xlsx | pdf | csv are offered
  defaultSort?    optional
}
```

- **Definitions are data.** A definition contains no JSX, no fetch call, no conditional
  business logic, and no report-specific component. If a report needs a bespoke component, the
  framework has a gap — **record the gap** rather than adding an escape hatch, because the first
  escape hatch is how forty configuration objects become forty components again.
- **Definitions live in a registry**, keyed by slug, so `/reports/:slug` resolves one lookup and
  an unknown slug renders a proper not-found state rather than a blank page.

### The page

- **Route `/reports/:slug`**, per [KB-053](../../frontend-new/page-map.md).
- **Parameter panel** built from the descriptors using M2-C06's form primitives + Zod, with
  parameter state **in the URL query string** so a report run is shareable and bookmarkable, and
  the browser back button behaves.
- **The report does not run on mount.** It runs when the user submits, or when the URL already
  carries a complete parameter set. Auto-running an unparameterised five-minute query on
  navigation is a denial-of-service against your own database.
- **Result grid** — a `DataGrid` (M2-C05-01) with M2-C05-03's states, server paging per the
  confirmed contract, and amounts through M2-C10.
- **Export** — reuses M2-C05-03's `useGridExport` pattern against
  `GET /api/v1/reports/{slug}/export`, **carrying the exact parameters the displayed result was
  produced from** — not the current form values, which the user may have edited since.
- **Report picker** at `/reports`, grouped per [KB-052](../../frontend-new/feature-mapping.md)
  (Accounts, Track, Analysis, Pending, Rating, Issue Summary), **filtered by screen right from
  the permission store** — presentation only (ADR-004 §3).

### The long-running state — a first-class deliverable

Because of `ReportExecutor.cs:25`, a report may legitimately run for **five minutes**. The UI
must therefore:

1. Show **elapsed time**, counting up, from the moment the request was issued. A number that
   moves is the difference between "working" and "hung".
2. **Escalate the message** as time passes — a short skeleton phase, then explicit copy naming
   the report and stating that long reports can take several minutes.
3. Offer **Cancel** throughout, wired to an `AbortController`.
4. **Tell the truth about what Cancel does.** It abandons the *client's* wait. It does **not**
   stop the server: ADR-005 records that the 300-second query *"will block an HTTP request
   thread just as it blocks a Blazor circuit today"*, and that there is **no** background
   infrastructure. Copy must not imply the query was stopped, and cancelling then immediately
   re-running must warn rather than silently double the database load.
5. **Never block the rest of the application.** No page-blocking overlay. Navigation away is
   allowed and aborts the request.
6. Announce state transitions via `aria-live="polite"`, so a screen-reader user is not left in
   silence for four minutes.
7. On timeout, render a distinct **timeout** state — not a generic error. The copy names the
   likely cause (too wide a date range) and offers a narrower re-run plus the correlation id.
   Investigation item 5 determines what the server actually returns.

### Printing

- `GET /api/v1/{resource}/{id}/print` returns `application/pdf`. The client requests it as a
  **blob**, creates an object URL, renders it in an `<iframe>` or a new tab, and **revokes the
  URL** when done.
- **React never generates a PDF.** No `jspdf`, no `pdfmake`, no `@react-pdf/renderer`, no
  client-side templating of a printable document. The 104 `.frx` templates stay server-side,
  untouched.
- Print failures show the server's message and the correlation id, never a generic toast.

## Implementation Steps

1. Read [ADR-005](../../decisions/ADR-005-reporting-and-printing.md) in full,
   [KB-052 §Reports](../../frontend-new/feature-mapping.md), [KB-053 §Reports](../../frontend-new/page-map.md),
   [KB-051 §State patterns](../../frontend-new/design-system.md), and M2-C05-01/M2-C05-03.
2. Search `docs/kb/investigation-registry.md`. Reuse INV-009. Perform the five narrow
   confirmations. **If M2-B08 has not landed, stop and report Blocked** — see *API Changes*.
3. Read `ReportExecutor.cs` in full (it is 128 lines) and re-verify claims 1–5 above, especially
   claim 4 (no paging) and claim 2 (the timeout).
4. Define `ReportDefinition` and its parameter descriptors in
   `frontend/nexgen-web/src/features/reports/types.ts`. Derive request and response types **from
   the generated client**; hand-write nothing OpenAPI already describes.
5. Build the registry in `reportRegistry.ts` — a slug-keyed map with a typed lookup and an
   unknown-slug path.
6. Implement `ReportParameterPanel.tsx` from the descriptors, over M2-C06's primitives with Zod
   validation. Bind parameter state to the URL query string.
7. Implement `useReportQuery.ts`: TanStack Query keyed by slug + parameters, `enabled: false`
   until submitted, `AbortController` for cancel, and an elapsed-time tick. Set the query's
   timeout above 300 s so the **client** does not give up before the server does — a client that
   times out at 30 s makes every long report look broken.
8. Implement `ReportPage.tsx`: parameter panel → result grid → export, composing `DataGrid`.
   The page holds no report-specific logic.
9. Implement `ReportRunningState.tsx`: elapsed timer, escalating copy, Cancel, `aria-live`.
   **Include the honest copy about what Cancel does and does not do.**
10. Implement `ReportTimeoutState.tsx` as a distinct branch of error handling.
11. Implement export by **reusing** M2-C05-03's `useGridExport` pattern, sending the parameters
    the **displayed result** came from. Add a test that proves this — not the live form values.
12. Implement `PrintButton.tsx` / `usePrintDocument.ts`: blob request, object URL, viewer,
    **revoke**.
13. Implement `ReportPicker.tsx` at `/reports`, grouped, filtered by screen right from the
    permission store, with an explicit note in the code that this is presentation only.
14. **Extend the no-client-file-generation guard.** M2-C05-03 forbids `xlsx`, `exceljs`,
    `papaparse`, `jspdf`, `pdfmake`. Extend the assertion to cover PDF *rendering/templating*
    libraries under `features/reports/` as well. ADR-005 is easiest to violate exactly here,
    where someone reasonably wants "a nicer PDF preview".
15. Write **one or two** real report definitions to prove the framework end-to-end. Pick two
    with **different parameter shapes** — e.g. a date-range report and one with a multi-select
    — so the contract is exercised rather than merely instantiated. Do **not** write all forty.
16. Write the tests under *Testing*.
17. Run verification, review the full `git diff`, update docs and the registry.

## Files Expected to Change

- `frontend/nexgen-web/src/app/routes.tsx` — `/reports` and `/reports/:slug`.
- `frontend/nexgen-web/src/shared/api/queryKeys.ts` — report query keys.
- `frontend/nexgen-web/package.json` — **no new runtime dependency expected.** Any addition must
  be justified explicitly against ADR-005.
- `docs/kb/frontend-new/page-map.md` (KB-053) — mark the report routes as implemented; record
  the slugs the framework actually registered.
- `docs/kb/frontend-new/feature-mapping.md` (KB-052), `docs/kb/frontend-new/design-system.md`
  (KB-051), `docs/kb/frontend-new/react-architecture.md` (KB-050),
  `docs/kb/investigation-registry.md` (KB-003), `docs/kb/execution/task-tracker.md` (KB-081),
  `docs/kb/INDEX.md`.

## Files Expected to Be Created

All **TO BE CREATED**, under `frontend/nexgen-web/src/features/reports/`:

- `types.ts`, `reportRegistry.ts`
- `ReportPage.tsx`, `ReportPage.module.css`
- `ReportParameterPanel.tsx`
- `ReportResultGrid.tsx`
- `ReportRunningState.tsx`, `ReportTimeoutState.tsx`
- `ReportPicker.tsx`
- `useReportQuery.ts`, `useReportExport.ts`, `usePrintDocument.ts`
- `PrintButton.tsx`, `PdfViewer.tsx`
- `definitions/` — **one or two** real definitions only
- `index.ts`
- `__tests__/ReportPage.test.tsx`, `__tests__/useReportQuery.test.ts`,
  `__tests__/ReportRunningState.test.tsx`, `__tests__/ReportPicker.test.tsx`,
  `__tests__/usePrintDocument.test.ts`, `__tests__/noClientFileGeneration.test.ts`,
  `__tests__/fixtures.ts`

**Plus one KB artefact** — the report-definition contract and the slug ↔ definition catalogue,
so the report wave has a written contract rather than an example to copy.
**Allocate its `doc_id` from KB-100+ after grepping `docs/kb/INDEX.md`.** As of 2026-08-12
`docs/kb/INDEX.md:60` records the KB-100+ range as *"none yet — allocate sequentially from
KB-100"*; **M2-A08 also allocates from this range**, so re-grep and take the next genuinely free
id rather than assuming a particular number.

## Files That Must Not Change

- **All of `V.SMART/**`** — including `ReportExecutor.cs`, the 104 `.frx` templates, and every
  stored procedure. This task consumes HTTP endpoints and nothing else.
- `frontend/nexgen-web/src/shared/components/DataGrid/**` — M2-C05-01/03's. If the grid cannot
  serve a report need, that is a change request against those tasks, not an in-place edit or a
  fork.
- `frontend/nexgen-web/src/shared/components/{EmptyState,ErrorState,Skeleton,Toast}/**` —
  M2-C04-03's.
- `frontend/nexgen-web/src/shared/lib/decimal/**` — M2-C10's.
- `frontend/vsmart-erp/**`, `db/**`, `docs/kb/decisions/**`.

## API Changes

Not applicable — this task adds no HTTP surface. It **consumes** the three endpoints
[ADR-005](../../decisions/ADR-005-reporting-and-printing.md) defines and **M2-B08** builds:

```
GET /api/v1/reports/{slug}?<typed parameters>          → paged JSON
GET /api/v1/reports/{slug}/export?format=xlsx|pdf|csv  → file
GET /api/v1/{resource}/{id}/print?template={name}      → application/pdf
```

**Confirm the real routes, parameter passing, paging envelope and error shapes before coding**
(investigation items 1–5).

If M2-B08 has not landed:

1. Record the gap in the investigation registry as a negative result.
2. Report **Blocked**, proposing the contracts above and naming
   `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/ReportService/TrackReportService/ReportExecutor.cs`
   as the executor to wrap.
3. **Do not** implement the endpoints here.
4. **Do not** call stored procedures from the client by any route, and **do not** build a PDF,
   spreadsheet or CSV in the browser as a stopgap. ADR-005 already took that decision, in the
   opposite direction, and a reporting stopgap becomes permanent because it appears to work.

## React Changes

| Aspect | Detail |
|---|---|
| **Route** | `/reports` (picker) and `/reports/:slug` (a report), per [KB-053](../../frontend-new/page-map.md). Parameters live in the URL query string. |
| **Component** | `ReportPage`, `ReportParameterPanel`, `ReportResultGrid`, `ReportRunningState`, `ReportTimeoutState`, `ReportPicker`, `PdfViewer`, `PrintButton`, plus `useReportQuery` / `useReportExport` / `usePrintDocument`, under `frontend/nexgen-web/src/features/reports/` (**TO BE CREATED**). |
| **API integration** | `GET /api/v1/reports/{slug}` via the generated client, as a TanStack Query keyed by slug + parameters, `enabled: false` until submitted, with an `AbortController`. Export and print are **imperative** blob requests, not cache entries — a downloaded file is not cacheable state. **The client timeout must exceed 300 s.** |
| **State** | Parameters in the URL. Results in the query cache. Elapsed time and cancellation local to the hook. No new store. |
| **Validation** | Zod schemas derived from the parameter descriptors — presence, type and range only. **No report business rule is validated client-side**; the procedure decides. |
| **Permissions** | The picker is filtered by `screenName` against the permission store. **Presentation only** (ADR-004 §3) — reports bypass `UserRight` server-side (claim 8), so a `403` from the report endpoint renders `PermissionDeniedState` inline, and the absence of server enforcement is a finding for M2-B08, not something the client compensates for. |
| **Loading state** | Skeleton for the first seconds, then the **long-running state**: elapsed timer, escalating copy, Cancel, `aria-live="polite"`. Never an indeterminate spinner alone; never a page-blocking overlay. |
| **Empty state** | Two distinct variants, exactly as [KB-051](../../frontend-new/design-system.md) and M2-C05-03 require: *not run yet* (prompting for parameters — the state a report page is in most of the time) and *ran, returned nothing* (offering narrower parameters). Conflating them is the defect M2-C05-03 exists partly to fix. |
| **Error state** | `ErrorState` with the server's message, `detail`, a copyable correlation id and Retry. **Timeout is a distinct branch** with its own copy. `403` → `PermissionDeniedState` inline. Never surface the raw `"Error while executing procedure: {name}"` string (claim 5). |
| **Accessibility** | The parameter panel is a labelled form with a real submit; the grid is announced when results arrive; the elapsed timer uses `aria-live="polite"` with a sensible update cadence, not every second; Cancel is keyboard-reachable throughout; the PDF viewer has an accessible name and a download fallback, because an embedded PDF is not reliably accessible. |
| **Responsive behaviour** | ≥1280: parameter panel beside or above the grid. <1280: panel collapses to a summary chip row showing the active parameters with an Edit affordance — **the active parameters stay visible at all widths** ([KB-051 principle 3](../../frontend-new/design-system.md) — never hide state). A user must never be unable to see what the numbers on screen were filtered by. |

## Business Logic

**Preserved unchanged — by delegation.**

Every report's logic is a stored procedure, executed server-side by `ReportExecutor`
(`ReportExecutor.cs:21-47`). Every printed document is a FastReport `.frx` template rendered
server-side. **This task implements none of it and must not.**

**The React code must contain none of the following:**

- Any recomputation, re-aggregation, re-grouping or re-sorting of report data that changes the
  values shown. Client-side *display* sorting of an already-returned page is acceptable only if
  the confirmed contract makes sorting client-side; otherwise sort is a server parameter.
- Any subtotal, total or variance derived in the browser. If a report needs a total, the
  procedure returns it. A client-derived total that disagrees with the printed report by one
  paisa destroys trust in both.
- Any float arithmetic on an amount. Amounts render through **M2-C10**'s decimal formatting.
- Any generation of a PDF, spreadsheet or CSV. **Forbidden dependencies:** `jspdf`, `pdfmake`,
  `@react-pdf/renderer`, `xlsx`, `exceljs`, `papaparse` — asserted by a test, extending
  M2-C05-03's guard.
- Any report-specific branching inside the framework. A `if (slug === 'daybook')` in
  `ReportPage.tsx` means the definition contract is wrong; fix the contract.
- Any client-side permission decision that suppresses data. The picker filters for usability;
  it never becomes the security boundary.

**BR-RPT-001** is invoked, not implemented. The one obligation is **parameter fidelity**: what
the user entered is what the procedure receives, and what the procedure returned is what the
user sees.

## Testing

| # | Test | Asserts |
|---|---|---|
| 1 | A definition renders its declared parameters with the right control per `kind` | Contract |
| 2 | Required parameters block submission with accessible messages | Contract |
| 3 | Submitting issues **one** request to `GET /api/v1/reports/{slug}` with exactly the declared parameters | Fidelity |
| 4 | **The report does not run on mount** with an incomplete parameter set | No accidental five-minute query |
| 5 | A complete parameter set in the URL **does** run on mount | Shareable runs |
| 6 | Parameters round-trip through the URL; back/forward restores them | Bookmarkability |
| 7 | After 5 s the running state shows an elapsed timer and escalated copy, not a bare spinner | The 300-second reality |
| 8 | Cancel aborts the request, returns to the parameter state, and the copy does **not** claim the server query was stopped | Honesty |
| 9 | The client query timeout **exceeds 300 s** | A client that gives up first makes every long report look broken |
| 10 | A timeout renders the distinct timeout state with the correlation id, not a generic error | Error contract |
| 11 | The raw `"Error while executing procedure: {name}"` string is **never** displayed | `ReportExecutor.cs:41-46` |
| 12 | *Not run yet* and *ran, returned nothing* render as distinct states | KB-051 |
| 13 | Export sends the parameters **the displayed result came from**, not edited form values | Data integrity — a mismatched export is undetectable by the user |
| 14 | Export uses the `Content-Disposition` filename; the object URL is revoked | Reuses M2-C05-03 |
| 15 | Print requests a blob, renders it, and **revokes** the object URL | No leak |
| 16 | No PDF is constructed client-side; `package.json` contains none of `jspdf`, `pdfmake`, `@react-pdf/renderer`, `xlsx`, `exceljs`, `papaparse` | ADR-005 enforcement |
| 17 | The picker hides reports whose `screenName` the user lacks — **and** a direct navigation to that slug still renders the server's `403` inline | ADR-004 §3: the client enforces nothing |
| 18 | An unknown slug renders a not-found state, not a blank page or a crash | Registry robustness |
| 19 | Amount columns render through M2-C10's decimal formatting; no JS float arithmetic on any amount | Decimal safety |
| 20 | Adding a second definition requires **no** change to any framework file — proven by the two shipped definitions having different parameter shapes | **The entire premise of the task** |
| 21 | axe: no critical violations in parameter / running / results / empty / error / timeout states | M5-09 |

**Test 20 is the one that decides whether this task succeeded.** If adding a report needs a
framework edit, forty screens have not become forty configuration objects and the task is not
done. Write tests 4, 13 and 20 first.

## Acceptance Criteria

- [ ] One `ReportPage` framework exists, driven entirely by `ReportDefinition` objects.
- [ ] **Adding a report requires no framework change**, proven by two shipped definitions with
      different parameter shapes.
- [ ] No report-specific branching exists in any framework file.
- [ ] Routes are `/reports` and `/reports/:slug`, using **KB-053's slugs**.
- [ ] Parameters live in the URL and round-trip through back/forward.
- [ ] The report does not run on mount with an incomplete parameter set.
- [ ] The long-running state shows elapsed time, escalating copy, and a Cancel whose copy does
      **not** claim the server query was stopped.
- [ ] The client query timeout exceeds the server's 300 s.
- [ ] Timeout is a distinct state with the correlation id.
- [ ] The raw `"Error while executing procedure: …"` string never reaches the user.
- [ ] *Not run yet* and *ran, returned nothing* are visually and textually distinct.
- [ ] Export carries the parameters the displayed result came from, proven by test.
- [ ] Print renders a server-produced PDF from a blob URL and revokes it; **no PDF is generated
      client-side**.
- [ ] `package.json` contains no PDF/spreadsheet/CSV generation library, asserted by a test.
- [ ] The picker is right-filtered client-side **and** a direct navigation still receives the
      server's decision.
- [ ] The five narrow confirmations are recorded with evidence, **including negative results** —
      in particular whether the report endpoints carry `[RequireScreen]`/`[RequireRight]`, and
      whether/how server paging works given `ExecuteAsync` does not page.
- [ ] R-04's status is checked and recorded.
- [ ] The report-definition contract is documented in a new KB-100+ artefact and registered in
      `docs/kb/INDEX.md`.
- [ ] Only one or two definitions are shipped; the remaining ~38 are explicitly deferred.
- [ ] All 21 tests pass; axe reports no critical violations.
- [ ] `npm run lint`, `npm run test`, `npm run build` pass.
- [ ] `git diff --stat -- V.SMART/` produces no output.

## Verification Commands

Frontend commands as established by **M2-C01**; run from `frontend/nexgen-web/`.

```bash
npm ci
```

```bash
npm run lint
```

```bash
npm run test
```

```bash
npm run build
```

Repository-level (verified per [KB-083](../prompt-template.md#verified-repository-commands)):

```bash
git status --porcelain
```

```bash
git diff --stat -- V.SMART/
```

The last command must produce **no output**.

## Documentation Updates

| Document | Change |
|---|---|
| **New artefact, `doc_id` from KB-100+** | The `ReportDefinition` contract and the slug ↔ definition catalogue, so the report wave has a written contract rather than an example to copy. Record which definitions shipped and which ~38 remain. |
| `docs/kb/frontend-new/page-map.md` (KB-053) | Mark the report routes as implemented; record the slugs actually registered against the proposed table. Bump `last_verified`. |
| `docs/kb/frontend-new/feature-mapping.md` (KB-052) | Confirm the "one shape" finding against the built framework; record any report whose shape did **not** fit, because that is the one that will break the wave. |
| `docs/kb/frontend-new/design-system.md` (KB-051) | Record the long-running state, the elapsed-timer pattern, the Cancel copy, the timeout state, and the two report empty states. |
| `docs/kb/frontend-new/react-architecture.md` (KB-050) | Record the `ReportPage` composition and the PDF-from-blob rule. |
| `docs/kb/investigation-registry.md` (KB-003) | The five confirmations, with negatives. |
| `docs/kb/INDEX.md` | Register the new artefact; update the KB-100+ row. |
| `docs/kb/execution/task-tracker.md` (KB-081) | M2-C09 status (currently `Blocked` at `task-tracker.md:138`). |

Do not edit ADR-005.

## Investigation Registry Updates

```yaml
Finding:        ReportExecutor.ExecuteAsync<T> executes EXEC dbo.{procedureName} via
                FromSqlRaw and materialises the ENTIRE result set with
                AsNoTracking().ToListAsync() — IT PERFORMS NO PAGING. A commented-out
                variant taking @Skip/@Take exists and is dead code. Both execution paths
                set a 300-second timeout: SetCommandTimeout(300) on the EF path and
                command.CommandTimeout = 300 on the DataTable path. Failures are wrapped as
                "Error while executing procedure: {procedureName}", exposing the database
                object name in the message.
Evidence:       V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/ReportService/TrackReportService/ReportExecutor.cs:21-47, :25, :35-39, :41-46, :48-86, :88-125, :107, :121-124
Business rule:  BR-RPT-001
Confidence:     Confirmed
Last verified:  2026-08-12
```

```yaml
Finding:        <the three report/print endpoints as built: routes, parameter passing, the
                paged envelope and whether it matches M2-B02's; how parameter METADATA
                reaches the client, if at all; whether export honours the displayed
                parameters; whether the endpoints carry [RequireScreen]/[RequireRight];
                what a 300-second timeout actually returns>
Evidence:       <controller path:line, or "grepped X, found none">
Business rule:  BR-RPT-001
Confidence:     Confirmed | Inferred | Unknown
Last verified:  2026-08-12
```

Record negative results explicitly — above all **if the report endpoints do not carry screen-right
attributes**, which ADR-005 requires precisely because reports bypass the service layer. That is
a security finding for M2-B08 and M2-A08, not a client concern. Also record R-04's status at the
time of this task.

## Git Strategy

| Field | Value |
|---|---|
| **Branch** | `migration/M2-C09-report-page-framework` |
| **Commit** | `M2-C09: Add the declarative ReportPage framework` |
| **Expected changed files** | `frontend/nexgen-web/src/features/reports/**` (new), `frontend/nexgen-web/src/app/routes.tsx`, `frontend/nexgen-web/src/shared/api/queryKeys.ts`, the new KB-100+ artefact, `docs/kb/frontend-new/{page-map.md,feature-mapping.md,design-system.md,react-architecture.md}`, `docs/kb/investigation-registry.md`, `docs/kb/INDEX.md`, `docs/kb/execution/task-tracker.md` |
| **Rollback approach** | Revert the branch's merge commit. The `/reports` routes disappear; nothing else is affected, because this task only reads existing endpoints and touches no server behaviour, schema or data. The Blazor report screens keep running throughout, so users lose nothing. **The real cost of a revert is schedule, not function:** the entire report wave is blocked behind this framework, and ~40 screens have no other migration path. |

Never merge or push to `master` from an execution session.

## Fresh-Session Execution Prompt

Copy everything in the block below into a new AI session.

```text
============================================================
ERP MIGRATION — TASK EXECUTION PROMPT
============================================================

TASK ID:
M2-C09

TASK NAME:
ReportPage framework

ROLE
You are an engineer working on the V.SMART / NexGen ERP
modernization project. You are executing exactly one task.

PROJECT OBJECTIVE
We are replacing the existing Blazor Server frontend with a new
React frontend while preserving the existing ERP business
behaviour, business services, database behaviour and business
rules wherever possible. The backend is extended, never
rewritten. Business logic currently trapped in Razor @code is
extracted into server-side services before any React screen
replaces it.

CURRENT ARCHITECTURE
Repository root: C:\Kumar\NexGen-ERP---2025-master\NexGen-ERP---2025-master
(this is the git repository root; the parent directory is not
part of the project)

  V.SMART/V.SMART.Shared   .NET 9 class library — ALL domain code:
                           196 EF entity sets, 285 business services,
                           ~190 repositories + UnitOfWork, 274 ViewModels,
                           333 Razor pages, 440 routes
  V.SMART/V.SMART.Web      Blazor Server host (live UI, stays running)
  V.SMART/V.SMART.Api      ASP.NET Core Web API (.NET 9) — the React
                           backend; currently 2 controllers / 6 endpoints
  V.SMART/V.SMART          .NET MAUI Blazor Hybrid host

SQL Server + EF Core 9, code-first, database-per-tenant.

Task-specific architecture context:
The React application DOES NOT EXIST YET. It is created by task
M2-C01 at frontend/nexgen-web/. Every frontend path in this
prompt is TO BE CREATED. The DataGrid you compose comes from
M2-C05-01 and M2-C05-03; the feedback primitives from M2-C04-03;
the decimal wrappers from M2-C10.

Stack per ADR-003: React 19, TypeScript strict, Vite 6, React
Router v7, TanStack Query v5, Zustand, React Hook Form, Zod,
Mantine 7, CSS Modules + design tokens, TanStack Table v8 +
TanStack Virtual, Recharts, Axios with a GENERATED OpenAPI
client, decimal.js, react-i18next, Vitest/RTL/MSW/Playwright.

ADR-005 IS BINDING HERE: reporting, printing and Excel
export/import stay SERVER-SIDE and are exposed over HTTP.
THE REACT CLIENT RENDERS PDFs FROM A BLOB URL AND NEVER
GENERATES THEM. The 104 FastReport .frx templates stay
server-side, untouched.

SOURCE OF TRUTH
The project knowledge base is at docs/kb/. Read it before
investigating the repository. Use docs/kb/INDEX.md for
question-to-document routing.

Required reading for this task:
  ADR-005  docs/kb/decisions/ADR-005-reporting-and-printing.md
           (READ IT IN FULL — the endpoints, the framework
            mandate, the 300-second consequence, the UserRight
            gap, and R-04)
  ADR-003  docs/kb/decisions/ADR-003-react-stack.md
  ADR-004  docs/kb/decisions/ADR-004-server-side-authorization.md (§3)
  ADR-002  docs/kb/decisions/ADR-002-rest-api-layer.md (§4)
  KB-052   docs/kb/frontend-new/feature-mapping.md  (Reports and
           Dashboard — the ~40 screens, grouped)
  KB-053   docs/kb/frontend-new/page-map.md  (Reports — the
           existing route -> proposed SLUG table. USE THESE SLUGS;
           DO NOT INVENT NEW ONES)
  KB-051   docs/kb/frontend-new/design-system.md  (State patterns)
  KB-050   docs/kb/frontend-new/react-architecture.md
  KB-030   docs/kb/business-rules/business-rule-inventory.md
           (BR-RPT-001 family)
  KB-083   docs/kb/execution/prompt-template.md
  KB-002   docs/kb/source-of-truth-rules.md
  TASK     docs/kb/execution/tasks/M2-C09.md      (the full task spec)
  TASK     docs/kb/execution/tasks/M2-C05-01.md   (the DataGrid core)
  TASK     docs/kb/execution/tasks/M2-C05-03.md   (grid states +
           the server-side export pattern you REUSE)
  TASK     docs/kb/execution/tasks/M2-C04-03.md   (the primitives
           you compose)
  TASK     docs/kb/execution/tasks/M2-C10.md      (decimal wrappers)
  TASK     docs/kb/execution/tasks/M2-B08.md      (the endpoints
           you consume — read it for the PLANNED contracts, then
           VERIFY AGAINST THE PRODUCED CONTROLLERS. A task file is
           a plan, not evidence.)

Authority order when sources conflict:
  1. Current source code
  2. Database schema / EF migrations (for storage)
  3. The knowledge base (for interpretation)
  4. Older prose documentation — hypothesis only
docs/ARCHITECTURE.md is superseded and contains known factual
errors. Do not rely on it.

INVESTIGATION REGISTRY
Before any repository investigation, search
docs/kb/investigation-registry.md.

Relevant investigations:
  INV-009  Reporting: FastReport + stored procedures — Complete.
           REUSE IT. Do not re-derive the reporting architecture
           and do not go reading .frx files.
  INV-006  Existing UI: routing, layout, components — Complete
  INV-011  Business rules cross-module sweep — Partial

Before investigating the repository, search
docs/kb/investigation-registry.md and the relevant knowledge-base
documents via docs/kb/INDEX.md. If an investigation is Complete
and not stale, reuse its findings and cite the doc_id — do not
re-derive them. If it is Partial, investigate only the documented
gap. If it is absent or contradicted by current code,
investigate, then record the finding, its file:line evidence,
and its confidence in the knowledge base so that future sessions
do not repeat this work. Record negative results too — "grepped
for X, found none" is a finding.

CURRENT TASK
Build ONE declarative ReportPage framework so that the ~40
existing report screens become ~40 CONFIGURATION OBJECTS rather
than ~40 React components.

A report definition supplies:
  1. PARAMETERS   — what the user fills in before running
  2. COLUMNS      — what the result grid shows
  3. THE SLUG     — which server report to call
  4. EXPORTS      — which of xlsx | pdf | csv are offered
Plus a PDF viewer for GET /api/v1/{resource}/{id}/print, rendered
from a blob URL.

IN SCOPE FOR M2: the framework, the definition contract, the
registry, the parameter panel, the result grid, export and print,
the long-running/cancel state, and the right-filtered report
picker — PROVEN END TO END BY ONE OR TWO REAL DEFINITIONS.

NOT IN SCOPE: the other ~38 definitions. Writing configuration
objects is per-report work in the report wave, cheap ONLY AFTER
this framework exists. Adding them here turns a one-week framework
task into a multi-week data-entry task AND BURIES THE FRAMEWORK'S
DEFECTS UNDER VOLUME.

WHY THIS TASK EXISTS
ADR-005 mandates it in as many words: "The ~40 report screens are
built from one declarative ReportPage framework — a report
definition supplies parameters, columns, the procedure slug, and
export options. Forty screens become forty configuration objects."
KB-052 reaches the same conclusion independently: all ~40 screens
"share one shape: parameter panel -> server-paged grid -> export",
rated "Medium overall, LOW PER REPORT after the framework exists."
KB-053 already routes them: "All become /reports/{slug} driven by
one ReportPage framework."

Forty bespoke report screens is how the existing codebase got to
333 Razor pages. Building the fortieth report should be writing an
object literal, not opening an editor.

CURRENT IMPLEMENTATION
Verified 2026-08-12. Classification per KB-002.
RE-VERIFY EVERY LINE NUMBER and report differences.

  V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/ReportService/TrackReportService/ReportExecutor.cs
  (128 lines — READ IT IN FULL, it is short)
    :21-47   ExecuteAsync<T>(string procedureName,
             params SqlParameter[] parameters) builds
             "EXEC dbo.{procedureName}" with the parameter names
             appended (:27-33) and runs it through
             FromSqlRaw(...).AsNoTracking().ToListAsync() (:35-39).
    :25      _context.Database.SetCommandTimeout(300);
             *** THE 300-SECOND TIMEOUT IS REAL. ***
    :35-39   *** ExecuteAsync<T> PERFORMS NO PAGING. *** It
             materialises the ENTIRE result set.
    :48-86   A commented-out variant taking @Skip/@Take — DEAD
             CODE.
    :41-46   Failures wrapped as
             throw new Exception($"Error while executing
             procedure: {procedureName}", ex)
             — THE DATABASE OBJECT NAME IS IN THE MESSAGE.
             DO NOT SURFACE THIS STRING TO END USERS.
    :88-125  ExecuteDataTableAsync — a second path, with
             command.CommandTimeout = 300 at :107 and the same
             error wrapping at :121-124.
  All Confirmed.

  *** CLAIM 4 SHAPES THE WHOLE TASK. *** ADR-005 promises "paged
  JSON", but the executor CANNOT PAGE. Either M2-B08 pages AFTER
  materialising (memory cost, no latency benefit) or the
  procedures gained paging parameters (an R-04-scale change across
  94 procedures). CONFIRM WHICH M2-B08 CHOSE and design the grid's
  paging expectations around the answer, rather than assuming
  server paging is cheap.

  From ADR-005 / INV-009, all Confirmed:
    - 104 FastReport .frx templates across 5 tenant folders,
      rendered by ReportService.Generate_Report, with per-tenant
      override and per-screen PrintSetting (watermark, logo, ISO
      logo, copy count, copy name). UNTOUCHED.
    - 94 stored procedures surface on ~40 report screens.
      UNTOUCHED.
    - REPORTS BYPASS THE SERVICE LAYER AND QUERY THE DATABASE
      DIRECTLY, SO UserRight DOES NOT GATE REPORT DATA. The picker
      filters by right for USABILITY; the server must enforce for
      SECURITY (M2-B08 + ADR-004).
    - R-04: only 12 of the 94 procedures have DDL in source
      control (13 .sql files exist; Sp_Print_PurchaseOrder.sql is
      dead code). ADR-005 names fixing this a MANDATORY
      PREREQUISITE. It is M2-B08's blocker, NOT YOURS — but CHECK
      AND RECORD ITS STATUS.
    - The dashboard's Blazor-ApexCharts is replaced by Recharts.
      OUT OF SCOPE. DashboardService is 3,095 LOC and is its own
      screen. You build report PAGES.

REQUIRED INVESTIGATION (five narrow confirmations)
  1. THE THREE ENDPOINT CONTRACTS AS M2-B08 ACTUALLY BUILT THEM.
     Read the produced controllers, not the plan. For
     GET /api/v1/reports/{slug}: how are parameters passed (query
     string? typed?), what is the paged envelope, and IS IT THE
     SAME ENVELOPE M2-B02 DEFINED for list endpoints? If reports
     need a second paging shape, ESCALATE that rather than
     absorbing it.
  2. HOW PARAMETER *METADATA* REACHES THE CLIENT. Does the server
     describe a report's parameters (a
     GET /api/v1/reports/{slug}/definition, or an OpenAPI schema),
     or must the client hold the definitions? *** THIS IS THE
     SINGLE MOST CONSEQUENTIAL UNKNOWN IN THE TASK. *** If the
     client holds them, a parameter change in a stored procedure
     SILENTLY BREAKS a report with no compile-time signal.
     RECORD THAT RISK; do not absorb it silently.
  3. WHETHER EXPORT HONOURS THE CURRENT PARAMETERS, SORT AND
     FILTER. M2-C05-03 calls an export that ignores filters "a
     defect to record, not to paper over". For a report it is
     worse: THE USER HAS NO WAY TO TELL.
  4. WHETHER THE REPORT ENDPOINTS CARRY [RequireScreen] /
     [RequireRight]. ADR-005 requires it EXPLICITLY because
     reports bypass the service layer. IF THEY DO NOT, THAT IS A
     SECURITY FINDING TO ESCALATE TO M2-B08 AND M2-A08 — not
     something the client can compensate for. RECORD IT EITHER
     WAY, INCLUDING THE NEGATIVE.
  5. WHAT A 300-SECOND TIMEOUT ACTUALLY RETURNS. A 500? A 504? A
     ProblemDetails carrying a SqlException message? An aborted
     connection with no response at all? YOUR MOST IMPORTANT ERROR
     STATE DEPENDS ON THE ANSWER AND IT CANNOT BE GUESSED.

Record every negative result. Do NOT investigate individual stored
procedures, .frx templates, or per-report business rules — that is
report-wave work.

TARGET IMPLEMENTATION

THE REPORT DEFINITION CONTRACT
  ReportDefinition = {
    slug          the server slug — FROM KB-053's TABLE, NEVER
                  INVENTED
    title, group  for the picker
    screenName    the Screens.ScreenName this report belongs to,
                  for right-filtering (PRESENTATION ONLY)
    parameters[]  typed descriptors: name, label, kind
                  (date | dateRange | select | multiSelect | text |
                   number | boolean | lookup), required, default,
                  and for lookups the reference-data source
                  (M2-B09)
    columns[]     DataGrid column definitions, marking which are
                  amounts (formatted through M2-C10) and which are
                  right-aligned
    exports[]     which of xlsx | pdf | csv are offered
    defaultSort?  optional
  }
  DEFINITIONS ARE DATA. A definition contains NO JSX, NO fetch
  call, NO conditional business logic, NO report-specific
  component. If a report needs a bespoke component, THE FRAMEWORK
  HAS A GAP — RECORD THE GAP rather than adding an escape hatch.
  The first escape hatch is how forty configuration objects become
  forty components again.
  Definitions live in a REGISTRY keyed by slug, so /reports/:slug
  is one lookup and an unknown slug renders a proper not-found
  state rather than a blank page.

THE PAGE
  - Route /reports/:slug per KB-053.
  - Parameter panel from the descriptors using M2-C06's form
    primitives + Zod, with PARAMETER STATE IN THE URL QUERY STRING
    so a run is shareable and bookmarkable and Back works.
  - *** THE REPORT DOES NOT RUN ON MOUNT. *** It runs when the
    user submits, or when the URL already carries a COMPLETE
    parameter set. Auto-running an unparameterised five-minute
    query on navigation is a denial-of-service against your own
    database.
  - Result grid = a DataGrid (M2-C05-01) with M2-C05-03's states,
    server paging per the CONFIRMED contract, amounts through
    M2-C10.
  - Export REUSES M2-C05-03's useGridExport pattern against
    GET /api/v1/reports/{slug}/export, CARRYING THE EXACT
    PARAMETERS THE DISPLAYED RESULT WAS PRODUCED FROM — not the
    current form values, which the user may have edited since.
  - Report picker at /reports, grouped per KB-052 (Accounts,
    Track, Analysis, Pending, Rating, Issue Summary), FILTERED BY
    SCREEN RIGHT from the permission store — PRESENTATION ONLY
    (ADR-004 §3).

THE LONG-RUNNING STATE — A FIRST-CLASS DELIVERABLE
Because of ReportExecutor.cs:25 a report may legitimately run for
FIVE MINUTES. A user staring at an indeterminate spinner for four
minutes concludes the system has hung and reloads — re-issuing a
five-minute query against a database still running the first one.
The UI must:
  1. Show ELAPSED TIME, counting up from when the request was
     issued. A number that moves is the difference between
     "working" and "hung".
  2. ESCALATE THE MESSAGE as time passes — a short skeleton phase,
     then explicit copy naming the report and stating that long
     reports can take several minutes.
  3. Offer CANCEL throughout, wired to an AbortController.
  4. *** TELL THE TRUTH ABOUT WHAT CANCEL DOES. *** It abandons
     the CLIENT's wait. It does NOT stop the server: ADR-005
     records that the 300-second query "will block an HTTP request
     thread just as it blocks a Blazor circuit today", and that
     there is NO background infrastructure. Copy must not imply
     the query was stopped, and cancelling then immediately
     re-running must WARN rather than silently double the database
     load.
  5. NEVER block the rest of the application. No page-blocking
     overlay. Navigating away is allowed and aborts the request.
  6. Announce transitions via aria-live="polite" so a
     screen-reader user is not left in silence for four minutes.
  7. On timeout, render a DISTINCT TIMEOUT STATE — not a generic
     error. Name the likely cause (too wide a date range), offer a
     narrower re-run, and show the correlation id.

PRINTING
  - GET /api/v1/{resource}/{id}/print returns application/pdf.
    Request it as a BLOB, create an object URL, render it in an
    iframe or a new tab, and REVOKE THE URL when done.
  - REACT NEVER GENERATES A PDF. No jspdf, no pdfmake, no
    @react-pdf/renderer, no client-side templating of a printable
    document. The 104 .frx templates stay server-side, untouched.
  - Print failures show the server's message and the correlation
    id, never a generic toast.

ANTI-CLIENT-GENERATION GUARD (mandatory)
Extend M2-C05-03's noClientFileGeneration test to cover
features/reports/ as well, asserting package.json contains NONE
of: jspdf, pdfmake, @react-pdf/renderer, xlsx, exceljs,
papaparse. ADR-005 is easiest to violate exactly here, where
someone reasonably wants "a nicer PDF preview".

RELEVANT FILES
Read-only reference (exists today; DO NOT MODIFY):
  V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/ReportService/TrackReportService/ReportExecutor.cs

To be created, under frontend/nexgen-web/src/features/reports/:
  types.ts
  reportRegistry.ts
  ReportPage.tsx
  ReportPage.module.css
  ReportParameterPanel.tsx
  ReportResultGrid.tsx
  ReportRunningState.tsx
  ReportTimeoutState.tsx
  ReportPicker.tsx
  useReportQuery.ts
  useReportExport.ts
  usePrintDocument.ts
  PrintButton.tsx
  PdfViewer.tsx
  definitions/          (ONE OR TWO real definitions ONLY)
  index.ts
  __tests__/ReportPage.test.tsx
  __tests__/useReportQuery.test.ts
  __tests__/ReportRunningState.test.tsx
  __tests__/ReportPicker.test.tsx
  __tests__/usePrintDocument.test.ts
  __tests__/noClientFileGeneration.test.ts
  __tests__/fixtures.ts

To be modified:
  frontend/nexgen-web/src/app/routes.tsx
  frontend/nexgen-web/src/shared/api/queryKeys.ts

Plus ONE KB ARTEFACT — the ReportDefinition contract and the
slug <-> definition catalogue, so the report wave has a WRITTEN
CONTRACT rather than an example to copy.
ALLOCATE ITS doc_id FROM KB-100+ AFTER GREPPING docs/kb/INDEX.md.
As of 2026-08-12 INDEX.md:60 records KB-100+ as "none yet —
allocate sequentially from KB-100"; M2-A08 ALSO ALLOCATES FROM
THIS RANGE, so re-grep and take the next genuinely free id rather
than assuming a particular number.

API SURFACE CONSUMED (NOT created by this task)
  GET /api/v1/reports/{slug}?<typed parameters>
      -> paged JSON
  GET /api/v1/reports/{slug}/export?format=xlsx|pdf|csv
      -> file, with Content-Disposition supplying the filename
  GET /api/v1/{resource}/{id}/print?template={name}
      -> application/pdf

Confirm the real routes, parameter passing, paging envelope and
error shapes before coding.

If M2-B08 has not landed:
  1. Record the gap as a negative result.
  2. Report Blocked, proposing the contracts above and naming
     ReportExecutor.cs as the executor to wrap.
  3. DO NOT implement the endpoints here.
  4. DO NOT call stored procedures from the client by any route,
     and DO NOT build a PDF, spreadsheet or CSV in the browser as
     a stopgap. ADR-005 already took that decision, in the
     OPPOSITE direction, and a reporting stopgap becomes permanent
     BECAUSE IT APPEARS TO WORK.

BUSINESS RULES TO PRESERVE
BR-RPT-001 family (KB-030) — INVOKED, NOT IMPLEMENTED.

Every report's logic is a stored procedure executed server-side by
ReportExecutor. Every printed document is a FastReport .frx
template rendered server-side. This task implements NONE of it.

THE REACT CODE MUST CONTAIN NONE OF THE FOLLOWING:
  - any recomputation, re-aggregation, re-grouping or re-sorting
    of report data that CHANGES THE VALUES SHOWN;
  - any subtotal, total or variance derived in the browser. If a
    report needs a total, THE PROCEDURE RETURNS IT. A
    client-derived total that disagrees with the printed report by
    one paisa destroys trust in both;
  - any float arithmetic on an amount — use M2-C10's decimal
    formatting;
  - any generation of a PDF, spreadsheet or CSV;
  - any report-specific branching inside the framework. An
    `if (slug === 'daybook')` in ReportPage.tsx means the
    definition contract is wrong — FIX THE CONTRACT;
  - any client-side permission decision that suppresses data. The
    picker filters for usability; IT NEVER BECOMES THE SECURITY
    BOUNDARY.

The one obligation is PARAMETER FIDELITY: what the user entered is
what the procedure receives, and what the procedure returned is
what the user sees.

CONSTRAINTS
- Do not rewrite existing business services.
- Do not reimplement ERP business logic in React/TypeScript.
- Do not change unrelated modules.
- Do not make assumptions without checking source code.
- Do not repeat completed investigations.
- Do not modify the database schema unless this task explicitly
  authorizes it. (It does not.)
- Preserve existing API behaviour wherever practical.
- The server remains authoritative for validation, calculations,
  permissions and document numbering.
- Do not start another task after completing this one.

Task-specific constraints:
- DO NOT MODIFY ANYTHING UNDER V.SMART/ — including
  ReportExecutor.cs, the .frx templates, and every stored
  procedure. `git diff --stat -- V.SMART/` must produce NO OUTPUT.
- DO NOT write all ~40 definitions. ONE OR TWO, with DIFFERENT
  parameter shapes, to prove the framework.
- DO NOT add any client-side file-generation or PDF-rendering
  library.
- DO NOT create a second DataGrid, EmptyState, ErrorState,
  Skeleton or Toast. Compose M2-C05-01/03's and M2-C04-03's. If
  one is inadequate, that is a change request against those tasks,
  not an in-place edit or a fork.
- DO NOT run a report on mount with an incomplete parameter set.
- DO NOT set a client query timeout below the server's 300 s.
- DO NOT surface the raw "Error while executing procedure: {name}"
  string to end users.
- DO NOT build the dashboard — it is a separate screen and out of
  scope.
- DO NOT invent report slugs. Use KB-053's table.
- Do not add a second UI component library.

EXECUTION PROCEDURE
1. Read the referenced knowledge-base documents.
2. Search the investigation registry.
3. Confirm whether the required investigation already exists.
4. Inspect the actual source code.
5. Verify every assumption against code before acting on it.
6. Implement only this task.
7. Run the required verification commands and tests.
8. Review the full git diff.
9. Update documentation.
10. Update the investigation registry if required.

EXPECTED FILE CHANGES
Modified:
  frontend/nexgen-web/src/app/routes.tsx
  frontend/nexgen-web/src/shared/api/queryKeys.ts
  docs/kb/frontend-new/page-map.md
  docs/kb/frontend-new/feature-mapping.md
  docs/kb/frontend-new/design-system.md
  docs/kb/frontend-new/react-architecture.md
  docs/kb/investigation-registry.md
  docs/kb/INDEX.md
  docs/kb/execution/task-tracker.md

Created:
  frontend/nexgen-web/src/features/reports/**
  the ReportDefinition contract artefact (doc_id from KB-100+)

Must not change:
  V.SMART/**  (especially ReportExecutor.cs, the .frx templates,
               and the stored procedures)
  frontend/nexgen-web/src/shared/components/DataGrid/**
    (M2-C05-01/03's)
  frontend/nexgen-web/src/shared/components/{EmptyState,ErrorState,
    Skeleton,Toast}/**  (M2-C04-03's)
  frontend/nexgen-web/src/shared/lib/decimal/**  (M2-C10's)
  frontend/vsmart-erp/**
  db/**
  docs/kb/decisions/**

TESTS
Vitest + React Testing Library + MSW, via `npm run test` from
frontend/nexgen-web/. Write all of these:

 1. A definition renders its declared parameters with the right
    control per kind.
 2. Required parameters block submission with accessible messages.
 3. Submitting issues ONE request to GET /api/v1/reports/{slug}
    with exactly the declared parameters.
 4. THE REPORT DOES NOT RUN ON MOUNT with an incomplete parameter
    set.                                    <-- write this first
 5. A COMPLETE parameter set in the URL DOES run on mount.
 6. Parameters round-trip through the URL; back/forward restores
    them.
 7. After 5 s the running state shows an elapsed timer and
    escalated copy, not a bare spinner.
 8. Cancel aborts the request, returns to the parameter state, and
    the copy does NOT claim the server query was stopped.
 9. The client query timeout EXCEEDS 300 s.
10. A timeout renders the distinct timeout state with the
    correlation id, not a generic error.
11. The raw "Error while executing procedure: {name}" string is
    NEVER displayed.
12. "Not run yet" and "ran, returned nothing" render as DISTINCT
    states.
13. Export sends THE PARAMETERS THE DISPLAYED RESULT CAME FROM,
    not edited form values.                 <-- write this first
14. Export uses the Content-Disposition filename; the object URL
    is revoked.
15. Print requests a blob, renders it, and REVOKES the object URL.
16. No PDF is constructed client-side; package.json contains none
    of jspdf, pdfmake, @react-pdf/renderer, xlsx, exceljs,
    papaparse.
17. The picker HIDES reports whose screenName the user lacks —
    AND a direct navigation to that slug STILL renders the
    server's 403 inline.
18. An unknown slug renders a not-found state, not a blank page or
    a crash.
19. Amount columns render through M2-C10's decimal formatting; no
    JS float arithmetic on any amount.
20. ADDING A SECOND DEFINITION REQUIRES NO CHANGE TO ANY FRAMEWORK
    FILE — proven by the two shipped definitions having DIFFERENT
    parameter shapes.                       <-- write this first
21. axe: no critical violations in parameter / running / results /
    empty / error / timeout states.

TEST 20 IS THE ONE THAT DECIDES WHETHER THIS TASK SUCCEEDED. If
adding a report needs a framework edit, forty screens have not
become forty configuration objects and the task is not done.

ACCEPTANCE CRITERIA
- One ReportPage framework exists, driven entirely by
  ReportDefinition objects.
- ADDING A REPORT REQUIRES NO FRAMEWORK CHANGE, proven by two
  shipped definitions with different parameter shapes.
- No report-specific branching in any framework file.
- Routes are /reports and /reports/:slug, using KB-053's slugs.
- Parameters live in the URL and round-trip through back/forward.
- The report does not run on mount with an incomplete parameter
  set.
- The long-running state shows elapsed time, escalating copy, and
  a Cancel whose copy does NOT claim the server query was stopped.
- The client query timeout exceeds the server's 300 s.
- Timeout is a distinct state with the correlation id.
- The raw "Error while executing procedure: ..." string never
  reaches the user.
- "Not run yet" and "ran, returned nothing" are visually and
  textually distinct.
- Export carries the parameters the displayed result came from,
  proven by test.
- Print renders a server-produced PDF from a blob URL and revokes
  it; NO PDF is generated client-side.
- package.json contains no PDF/spreadsheet/CSV generation library,
  asserted by a test.
- The picker is right-filtered client-side AND a direct navigation
  still receives the server's decision.
- The five narrow confirmations are recorded with evidence,
  INCLUDING NEGATIVE RESULTS — in particular whether the report
  endpoints carry [RequireScreen]/[RequireRight], and whether/how
  server paging works given ExecuteAsync does not page.
- R-04's status is checked and recorded.
- The ReportDefinition contract is documented in a new KB-100+
  artefact and registered in docs/kb/INDEX.md.
- Only one or two definitions shipped; the remaining ~38
  explicitly deferred.
- All 21 tests pass; axe reports no critical violations.
- npm run lint, npm run test and npm run build pass.
- git diff --stat -- V.SMART/ produces no output.

VERIFICATION COMMANDS
The frontend commands below are the ones established by task
M2-C01; they are cited as established there, not as
independently verified. Run each from frontend/nexgen-web/.

  npm ci
  npm run lint
  npm run test
  npm run build

Repository-level commands (verified for this repository per
KB-083):

  git status --porcelain
  git diff --stat -- V.SMART/     # must produce no output

NEVER use a solution-level build command. The solution on disk,
NexGen-ERP---2025-master.sln, is UNTRACKED (KB-083, INV-029).
This task changes no backend code, so you should not need to build
it; if you must, the verified command is
`dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj`.

DOCUMENTATION REQUIREMENTS
Update, keeping full frontmatter (doc_id, title, module,
source_files, entities, api_endpoints, database_tables,
business_rules, status, confidence, last_verified, dependencies)
and bumping last_verified:

  NEW ARTEFACT (doc_id from KB-100+)
    — the ReportDefinition contract and the slug <-> definition
      catalogue. Record which definitions shipped and which ~38
      remain, so the report wave has a written contract rather
      than an example to copy.
  docs/kb/frontend-new/page-map.md (KB-053)
    — mark the report routes as implemented; record the slugs
      actually registered against the proposed table.
  docs/kb/frontend-new/feature-mapping.md (KB-052)
    — confirm the "one shape" finding against the built framework,
      and RECORD ANY REPORT WHOSE SHAPE DID NOT FIT, because that
      is the one that will break the wave.
  docs/kb/frontend-new/design-system.md (KB-051)
    — record the long-running state, the elapsed-timer pattern,
      the Cancel copy, the timeout state, and the two report empty
      states.
  docs/kb/frontend-new/react-architecture.md (KB-050)
    — record the ReportPage composition and the PDF-from-blob
      rule.
  docs/kb/INDEX.md
    — register the new artefact; update the KB-100+ row.
  docs/kb/execution/task-tracker.md (KB-081)
    — set M2-C09 to Needs Review (it is currently Blocked).

Add to docs/kb/investigation-registry.md, in the KB-083 evidence
format (Finding / Evidence / Business rule / Confidence / Last
verified):
  - ReportExecutor's no-paging behaviour, the 300-second timeouts
    on both paths, and the procedure name in the error message;
  - the three endpoint contracts as built, including the paging
    envelope and whether it matches M2-B02's;
  - how parameter metadata reaches the client, if at all;
  - whether export honours the displayed parameters;
  - WHETHER THE REPORT ENDPOINTS CARRY [RequireScreen] /
    [RequireRight] — RECORD THE NEGATIVE IF NOT, and escalate to
    M2-B08 and M2-A08;
  - what a 300-second timeout actually returns;
  - R-04's status at the time of this task.

Do not edit ADR-005.

GIT
Branch:  migration/M2-C09-report-page-framework
Commit:  M2-C09: Add the declarative ReportPage framework
Do not merge. Do not push to master. Leave the branch for review.

FINAL RESPONSE
When finished, report:
1.  Task ID
2.  Task status (Completed / Needs Review / Blocked)
3.  What was implemented
4.  Files created
5.  Files modified
6.  Files deleted
7.  Tests executed
8.  Test results
9.  Documentation updated
10. Investigation registry updated
11. Architectural decisions taken
12. Unexpected findings
13. Assumptions made
14. Deviations from this task
15. Recommended next task

IMPORTANT:
EXECUTE ONLY THIS TASK.
DO NOT START THE NEXT TASK.
============================================================
```
