---
doc_id: TASK-M2-C12-01
title: Re-specify for Angular — the design-system tree
module: execution
task_id: M2-C12-01
milestone: M2
parent_task: M2-C12
task_type: Documentation
priority: P0
status: Blocked
estimate: 1 d
depends_on: [M2-C00, M2-C01]
source_files:
  - docs/kb/execution/tasks/M2-C04.md
  - docs/kb/execution/tasks/M2-C04-01.md
  - docs/kb/execution/tasks/M2-C04-02.md
  - docs/kb/execution/tasks/M2-C04-03.md
business_rules: []
confidence: n/a
last_verified: 2026-08-22
complexity: MEDIUM
risk: LOW
---

# M2-C12-01 — Re-specify for Angular: the design-system tree

| Field | Value |
|---|---|
| Task ID | M2-C12-01 |
| Milestone | M2 — Foundation |
| Parent | [M2-C12](M2-C12.md) |
| Type | Documentation |
| Priority | P0 |
| Status | Blocked — owner: Vivek (Q-70) |
| Estimate | 1 d |
| Gate | G2 |

## Objective

Re-specify **this batch only** — the design-system tree — against
[`ADR-007`](../../decisions/ADR-007-angular-stack.md) and the Angular rewrite of
[KB-050](../../frontend-new/react-architecture.md), removing the `⛔ STOP` banner from each file
*in the same change that removes its React content*.

**Files in this batch — no others may be edited except the ones named under "Also" below:**

- `docs/kb/execution/tasks/M2-C04.md`
- `docs/kb/execution/tasks/M2-C04-01.md`
- `docs/kb/execution/tasks/M2-C04-02.md`
- `docs/kb/execution/tasks/M2-C04-03.md`

Read [`M2-C12`](M2-C12.md) first. It holds the translation table, the rationale, the
out-of-scope list and the failure history. This file does not repeat them; it narrows them.

## The atomicity rule — inherited from the parent, and non-negotiable

> **A file's ⛔ banner may only be removed in the same change that removes its React
> instructions.** No file may come to rest banner-removed and React-bearing — that strips the
> only guard stopping a runner from building the wrong stack while leaving the instruction to
> build it. If you cannot finish a file, **leave its banner in place** and report it as not
> done. Half-done is worse than not started.

Attempt 1 of the unsplit parent failed exactly this way across 23 files. Do not repeat it.

## Acceptance Criteria

1. Every file in this batch has **no** `⛔ STOP — this specification is superseded` banner.
   Verify with `grep -rl '⛔ STOP — this specification is superseded' docs/kb/execution/tasks/ --exclude='M2-C12*'`
   and confirm none of this batch's files appear.
2. **The atomicity rule holds**: for every file in the batch,
   `grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite'`
   returns **only** lines that explicitly mark themselves historical ("the React specification
   this replaces", "no longer specifies", "not deleted from the record"). Quote the actual
   output per file in the Execution Record — a count alone is not evidence.
3. Each file carries a re-specification note naming `M2-C12-01` and the date, in the shape
   [`M2-C01`](M2-C01.md) uses.
4. Every path given under `frontend/` is `frontend/nexgen-web/…`, and every quoted command
   appears verbatim in [KB-083's verified Angular table](../prompt-template.md#verified-repository-commands).
5. **`depends_on`, `business_rules`, `priority`, `estimate` and gate are byte-unchanged** in
   every file — prove by diffing those frontmatter keys, do not assert it.
6. Every `file:line` citation into `V.SMART/` is byte-unchanged, or its change is justified
   individually. This Blazor evidence was never a React decision and survives.
7. The diff touches **only** the files named above
   — `git diff --name-only master...HEAD` must list nothing else.
8. Each file still satisfies [KB-090](../task-template.md)'s required section set.

## Testing Requirements

No code, so no suite. Validation is by inspection plus the greps above, **with their output
quoted**. In addition:

- **Read one file in this batch end to end as if about to implement it.** If it needs a detail
  you would have to invent, it is not re-specified. Name the file read and the outcome.

## Documentation Updates

| Document | Update |
|---|---|
| The batch files | Re-specified; banner removed; note added |
| [KB-081](../task-tracker.md) | This sub-task's own row only. **The 25 re-specified rows and `dependency-graph.md` belong to `M2-C12-05`**, the last batch, so the tree is restated once. |
| [KB-004](../../open-questions.md) | Any question raised by an ambiguous re-derivation. Check `git branch --no-merged master` before claiming an id. |

## Completion Conditions

- All 8 acceptance criteria met, greps quoted.
- The atomicity rule holds for every file — verified, not assumed.
- **Only the repository owner may set `Completed`** ([KB-088](../workflow.md#who-may-set-completed)).

## Git Strategy

| | |
|---|---|
| Branch | `migration/m2-c12-01-respec`, cut fresh from `master` |
| Commit | `M2-C12-01: <imperative summary>` |
| Merge | Left for review. Never merged or pushed from an execution session. |

## Execution Record (2026-08-22)

**Who wrote this, and what that limits.** The four batch files were re-specified by the
implementing session and committed as `3d1ccd3` on `migration/M2-C12-01-respec`. That session did
**not** write this section, and validation therefore returned `FAIL` on criterion 2's evidence
obligation — *"Quote the actual output per file in the Execution Record"* — with the greps
surviving only in a transcript. This record is written by the diagnosis pass of 2026-08-22
(KB-092, `M2-C12-01 · attempt 1 · diagnosis`). **Every command below was re-run by that pass on
`migration/M2-C12-01-respec` at tip `3d1ccd3`; nothing here is copied from the implementer's
report.** What cannot be reconstructed first-hand is the implementer's *reasoning*; where a
judgement call is recorded below, it is inferred from the committed text and labelled as such.
The same limitation was recorded for `M2-C00`'s attempt-1 narrative.

### Criterion 1 — no `⛔ STOP` banner survives in the batch · **MET**

```
$ grep -rl '⛔ STOP — this specification is superseded' docs/kb/execution/tasks/ --exclude='M2-C12*'
docs/kb/execution/tasks/M2-C02.md
docs/kb/execution/tasks/M2-C03.md
docs/kb/execution/tasks/M2-C05-01.md
docs/kb/execution/tasks/M2-C05-02.md
docs/kb/execution/tasks/M2-C05-03.md
docs/kb/execution/tasks/M2-C05.md
docs/kb/execution/tasks/M2-C06.md
docs/kb/execution/tasks/M2-C07.md
docs/kb/execution/tasks/M2-C08-01.md
docs/kb/execution/tasks/M2-C08-02.md
docs/kb/execution/tasks/M2-C08-03.md
docs/kb/execution/tasks/M2-C08.md
docs/kb/execution/tasks/M2-C09.md
docs/kb/execution/tasks/M2-C10.md
docs/kb/execution/tasks/M2-C11.md
docs/kb/execution/tasks/M2-D01.md
docs/kb/execution/tasks/M2-D02-01.md
docs/kb/execution/tasks/M2-D02-02.md
docs/kb/execution/tasks/M2-D02-03.md
docs/kb/execution/tasks/M2-D02.md
docs/kb/execution/tasks/M2-D03.md
```

21 files, all outside this batch. **None of `M2-C04.md`, `M2-C04-01.md`, `M2-C04-02.md`,
`M2-C04-03.md` appears.** The 21 remaining are `M2-C12-02`..`-05`'s work.

### Criterion 2 — the atomicity rule · **MET**, output quoted per file

Run from `docs/kb/execution/tasks/`, one file at a time, with
`grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite'`:

```
===== M2-C04.md =====
(no output, exit 1)

===== M2-C04-01.md =====
140:  flat config, Vitest through `@angular/build:unit-test`, `@testing-library/angular`, and a bare
624:> in this section describes the **React/Mantine implementation this file no longer specifies**.
631:**It was real, it passed, and it is gone.** The discarded React/Mantine version of this task was

===== M2-C04-02.md =====
116:- M2-C01's toolchain: Angular 22.x, `strict`, ESLint flat config, Vitest through
125:| M2-C01 | Hard | Workspace, PrimeNG 22.1.x, `@angular/forms`, Vitest, `@testing-library/angular`. |
352:| Permissions | n/a at the control level. Gating is M2-C02's `has-right` directive / permission service, applied by the screen. Do not add a `requiredRight` input to controls — it would scatter permission logic across 140 screens and invite the belief that the client enforces it. |
378:Tests use **Vitest through `@angular/build:unit-test`** with `@testing-library/angular` and its

===== M2-C04-03.md =====
105:- M2-C01's toolchain: Angular 22.x, `strict`, Vitest through `@angular/build:unit-test`,
116:| M2-C01 | Hard | Workspace, PrimeNG 22.1.x, Vitest, `@testing-library/angular`. |
318:Tests use **Vitest through `@angular/build:unit-test`** with `@testing-library/angular` and its
```

*(Re-run 2026-08-22 after the attempt-3 a11y restoration below; the hit **set** is unchanged,
three line numbers moved because text was inserted above them:
`M2-C04-01.md` 617→624 and 624→631, `M2-C04-03.md` 317→318.)*

**Every hit accounted for, none of them a live instruction to build the ADR-003 stack:**

| Hit | Why it is not a violation |
|---|---|
| `M2-C04-01.md:140`, `M2-C04-02.md:116,125,378`, `M2-C04-03.md:105,116,318` | Substring false positive on `vite` inside **Vitest** — the *current, verified* Angular test runner. KB-083's Angular table: *"Runner is **Vitest 4.1.11** through `@angular/build:unit-test`, the Angular CLI's default at 22.1.5. **Not Karma**"*. Naming it is required, not stale. |
| `M2-C04-02.md:352` | Substring false positive on `vite` inside the word "in**vite**" ("invite the belief that the client enforces it"). Not a tool reference at all. |
| `M2-C04-01.md:624` | Inside the blockquote that opens `## Historical Execution Record`, and it self-marks: *"the React/Mantine implementation this file **no longer specifies**"*. |
| `M2-C04-01.md:631` | Same section: *"The **discarded** React/Mantine version of this task was …"*. Past tense, explicitly discarded. |

`M2-C04.md` returns nothing at all.

### Criterion 3 — re-specification note · **MET**

```
$ grep -n 'Re-specified for Angular on 2026-08-22' M2-C04.md M2-C04-01.md M2-C04-02.md M2-C04-03.md
M2-C04.md:27
M2-C04-01.md:25
M2-C04-02.md:28
M2-C04-03.md:27
```

All four lines read `> **Re-specified for Angular on 2026-08-22 by [\`M2-C12-01\`](M2-C12-01.md).**`
— the blockquote shape `M2-C01.md:28` uses, including the "not deleted from the record" clause.

### Criterion 4 — `frontend/` paths and quoted commands · **MET, with two readings recorded**

Commands. Every `npm` command quoted anywhere in the batch is one of five, and all five appear
verbatim in KB-083's Angular table (`npm run typecheck`, `npm run lint`, `npm run format:check`,
`npm run test:ci`, `npm run build`). The extraction
(`grep -noE '\`(npm|npx|ng|dotnet) [^\`]*\`' M2-C04*.md | sort -u`) returns those five for every
file, plus exactly one other token.

**Reading 1 — the one non-KB-083 command is a prohibition, not an instruction.**
`M2-C04-01.md:646-649` reads *"do not run repo-root `npx prettier --write` over `docs/kb/`"*, in
the historical lessons section. It is the only `npx`/`ng`/`dotnet` token in the batch, it is
never offered as a verification step, and removing it would delete a true operational warning.
Recorded rather than silently accepted.

**Reading 2 — three `frontend/` paths are not `frontend/nexgen-web/`.**

```
$ grep -noE 'frontend/[A-Za-z0-9_./*-]+' M2-C04*.md | grep -v 'frontend/nexgen-web'
M2-C04-01.md:411:frontend/vsmart-erp/**
M2-C04-02.md:331:frontend/vsmart-erp/**
M2-C04-03.md:278:frontend/vsmart-erp/**
```

*(Re-run 2026-08-22 after the attempt-3 a11y restoration; same three hits, two line numbers
moved by one because a row was inserted above them.)*

All three are entries in **"Files That Must Not Change"**. `frontend/vsmart-erp/` is a real,
tracked directory (`git ls-files frontend/vsmart-erp` → 40 files), so naming it as off-limits is
factually correct and rewriting it to `frontend/nexgen-web/` would make the entry false. The
criterion's wording ("every path given under `frontend/` is `frontend/nexgen-web/…`") is aimed at
*where the work goes*; a do-not-touch fence is the opposite. 36 of the 39 `frontend/` references
are `frontend/nexgen-web/…`.

### Criterion 5 — protected frontmatter keys byte-unchanged · **MET**

`master` vs `HEAD`, per file, comparing the value strings of each protected key:

| File | `depends_on` | `business_rules` | `priority` | `estimate` | Gate row |
|---|---|---|---|---|---|
| `M2-C04.md` | `[M2-C01]` unchanged | `[]` unchanged | `P0` unchanged | `2 wk` unchanged | `Gate / G2` unchanged |
| `M2-C04-01.md` | `[M2-C01]` unchanged | `[]` unchanged | `P0` unchanged | `3 d` unchanged | `Gate / G2` unchanged |
| `M2-C04-02.md` | `[M2-C04-01]` unchanged | `[]` unchanged | `P0` unchanged | `4 d` unchanged | `Gate / G2` unchanged |
| `M2-C04-03.md` | `[M2-C04-01]` unchanged | `[BR-SO-003]` unchanged | `P0` unchanged | `3 d` unchanged | `Gate / G2` unchanged |

Every value string is byte-identical on both sides; only the header-table *line number* moves,
because the banner above it changed length. The two frontmatter keys that did change are
`last_verified` (→ `2026-08-22`, in all four) and `M2-C04-01`'s `status` — neither is in this
criterion's list. See Deviation 2.

### Criterion 6 — `V.SMART/` `file:line` citations byte-unchanged · **MET**

Every `<path>.(razor|cs|css):<line>` token was extracted from `master` and from `HEAD` for all
four files, sorted unique, and diffed:

```
===== M2-C04.md =====     IDENTICAL (0 citations)
===== M2-C04-01.md =====  IDENTICAL (2 citations)
===== M2-C04-02.md =====  IDENTICAL (4 citations)
===== M2-C04-03.md =====  IDENTICAL (0 citations)
```

Zero removed, zero added, zero altered. No individual justification was needed because no
citation moved.

### Criterion 7 — the diff · **NOT MET as literally written; see the self-contradiction note below**

```
$ git diff --name-only master...HEAD          # re-run 2026-08-22, attempt 3
docs/kb/execution/failure-log.md
docs/kb/execution/tasks/M2-C04-01.md
docs/kb/execution/tasks/M2-C04-02.md
docs/kb/execution/tasks/M2-C04-03.md
docs/kb/execution/tasks/M2-C04.md
docs/kb/execution/tasks/M2-C12-01.md
docs/kb/open-questions.md
```

Seven paths, not four. The four batch files are the deliverable; the other three are
`open-questions.md` (**authorised** by this file's *Documentation Updates* table),
this file's own Execution Record (**required** by criterion 2 and by KB-088 §4) and
`failure-log.md` (KB-092, written by the validation/diagnosis passes, orchestrator-owned).
**This criterion is reported `not met` rather than made to pass**, because the only way to make
it pass would be to delete evidence the other criteria require. See the note below; the wording
is `M2-C12-05`'s to repair.

Nothing outside `docs/kb/`: `git diff --stat master...HEAD -- . ':(exclude)docs'` is empty.

### Criterion 8 — KB-090's required section set · **MET**

`## ` headings, `master` vs `HEAD`:

- **The three child files** (`M2-C04-01`, `-02`, `-03`): `## React Changes` →
  `## Frontend Changes` (the same section, renamed off the dead stack);
  `## Completion Conditions` **added**; `## Fresh-Session Execution Prompt` **removed**, which
  `task-template.md:194,207-209` directs.
- **`M2-C04.md` (the parent) is additions only** — its heading diff against `master` is `5a6,7`:
  it gains `## Completion Conditions` and `## Git Strategy` and loses nothing. It never carried
  a `## React Changes` or a `## Fresh-Session Execution Prompt` section, so the line above does
  **not** apply to it. *(Corrected 2026-08-22 by the attempt-2 diagnosis pass; the earlier
  wording read "all four", which was wrong for the parent.)*
- `M2-C04-01.md`: three sections — `## Execution Record (2026-08-19)`,
  `## Execution Record — attempt 2 (2026-08-20)` and `## Execution Record (2026-08-20)`, 211
  lines in total — were condensed into one 36-line `## Historical Execution Record — the
  discarded React implementation`. The condensed section names its own git retrieval path
  (`git show 56b8ae2:docs/kb/execution/tasks/M2-C04-01.md`), so nothing is destroyed; it is moved
  out of the working text. Verified: the three originals are retrievable from `master`.

No required section is missing from any file.

## Testing Requirements — the end-to-end read

**File read end to end: `docs/kb/execution/tasks/M2-C04-03.md` (465 lines at the time of the read; 468 after
attempt 3's three-line a11y restoration, `wc -l` observed 2026-08-22), read in full on
2026-08-22 as if about to implement it.**

**Outcome: implementable without inventing a detail.** Every PrimeNG surface is named explicitly
(`p-dialog`, `p-drawer`, `p-confirmdialog` + `ConfirmationService`, `p-popover`, `[pTooltip]`,
`p-contextmenu`, `p-toast` + `MessageService`, `p-message`, `p-blockui` + `p-progressspinner`,
`p-skeleton`, `p-progressbar`), with the three components that have no PrimeNG surface marked
"— (own markup + tokens)". Every file to be created is named. The keyboard model is specified per
component rather than as "make it accessible". The five verification commands are KB-083's.

**Two things a reader would notice, neither of them an invented detail:**

1. `frontend/nexgen-web/src/app/shared/components/index.ts` is listed under *Files Expected to
   Change* (`:248`), but it does not exist — `git ls-files frontend/nexgen-web/src/app/shared/`
   returns only five `.gitkeep` files. It belongs under *Files Expected to be Created*. Cosmetic;
   the intent is unambiguous.
2. The task correctly declines to guess the `ProblemDetails` field names, routing that to an
   *Investigation Requirement* ("read the code, not the task file") with the placeholders
   `<detail-field>` / `<traceId-field>` left explicit in the INV-006 amendment template. That is
   the right handling of an unknown, not a gap.

## Deviations — recorded because they existed only in a chat message

**1. The `axe` acceptance criterion was dropped in attempt 1 and has been RESTORED in attempt 3.**
On `master` the criterion read, e.g. `M2-C04-03.md:347` *"`axe` reports zero critical violations
across every component in **both** themes"* (likewise `M2-C04-02.md:372`, `M2-C04.md:191`,
`M2-C04-01.md:388,417`). Attempt 1 replaced it in the three children with a static template-lint
criterion and dropped it entirely from the parent. That is a change to acceptance-criteria
*semantics*, which `M2-C12` § Out of Scope (`M2-C12.md:140-142`) forbids in terms: *"Intent, ERP
behaviour, acceptance-criteria semantics … all stay as they are. This task changes how, never
what or whether."* A runtime a11y scan and a static template lint are not the same check, and
`axe` is stack-independent, so removing it was never React removal.

**Resolution taken (2026-08-22, attempt 3):** the criterion is restored in all four files, with
only its *how* translated — `a11y.test.tsx` → `a11y.spec.ts`, driven from `@testing-library/angular`.

| File | Restored at |
|---|---|
| `M2-C04.md` | `:187` — *"`axe` runs in CI over every primitive's stories/tests with **zero critical violations**"*, byte-identical to `master:191` |
| `M2-C04-01.md` | `:469` test-table row; `:511` acceptance criterion (*"`axe` reports zero critical violations in both themes"*, as `master:417`); `:388` records the `axe-core` **dev** dependency this implies |
| `M2-C04-02.md` | `:391` test-table row; `:408` acceptance criterion, byte-identical to `master:372` |
| `M2-C04-03.md` | `:334` test-table row; `:365` acceptance criterion, byte-identical to `master:347` |

The template-lint criterion added by attempt 1 is **kept alongside** it, marked *"in addition"* —
it removes nothing and reflects a rule set that genuinely exists in `eslint.config.js`. The
factual observation that no `axe` package is installed today
(`frontend/nexgen-web/package.json`, verified 2026-08-22) survives as prose, not as a reason to
drop the criterion: `M2-C04-01` now installs `axe-core` as a dev dependency. **Q-69** stays open
only as the owner's option to choose differently (option (c), deferring coverage to M5-09); the
committed deliverable no longer depends on the answer.

**2. `M2-C04-01`'s status moved `Needs Review` → `Ready`,** and a pre-existing duplicate `status:`
frontmatter key (`Blocked` at `:10`, `Needs Review` at `:21` on `master`) was collapsed to one.
Raised as **Q-68**; the tracker row deliberately not touched, because `M2-C12-05` restates all 25.
The other three files keep `status: Not Started`, unchanged.

**3. Three `frontend/vsmart-erp/` paths survive** against criterion 4's literal wording. See
criterion 4, Reading 2.

**4. The diff is five files, not four** (six including this one). See the note below.

## Criterion 7 contradicts this file's own *Documentation Updates* table — do not re-litigate it

Criterion 7 says the diff must list *nothing* but the four batch files. The *Documentation
Updates* table above authorises **KB-004** (`open-questions.md`) and this task's own **KB-081**
row, and the *Objective* says "no others may be edited **except the ones named under 'Also'
below**" — a reference to a list that does not exist in this file. KB-088 §3 step 2 and §4
additionally make `tasks/<TASK-ID>.md` an unconditional **Always** update, which is this very
section. Attempt 1 took the permissive horn for `open-questions.md` (correctly — Q-67 and Q-68 are
real findings) and the restrictive horn for its own Execution Record, and that inconsistency is
the whole reason validation failed. **The resolution, for `M2-C12-02`..`-05` as well: criterion 7
means "no *task-specification* file outside the batch"; the task's own record and the documents
its *Documentation Updates* table names are always in scope.** `M2-C12-05` should fix the wording
when it restates the tree.

## What was verified about the substance, and by whom

Independently re-derived by the validator on 2026-08-22 against the installed workspace, not
accepted on report: every PrimeNG selector named in the batch exists in `primeng` **22.1.0**;
`definePreset` is declared at `@primeuix/themes/dist/index.d.mts:6`; the
`darkModeSelector?: 'system' | 'none' | (string & {})` option at
`@primeuix/styled/dist/index.d.mts:494`; `frontend/nexgen-web/src/app/app.config.ts:34` is the
bare `Aura` preset; `tsconfig.spec.json:6` includes exactly `src/**/*.d.ts` and
`src/**/*.spec.ts`. Re-read on disk by the diagnosis pass:
`UserThemePreference.cs:20` = `public bool IsDarkMode { get; set; } = false;`,
`Companydetails.cs:208` = `public int DecimalPlaces { get; set; } = 2;`,
`DebitNote.cs:95,109,117,146` = the four `…AmtOrPer` booleans.

**No build, test or `dotnet` command was run, and none was needed.**
`git diff --stat master...HEAD -- . ':(exclude)docs'` is empty — the diff is Markdown under
`docs/kb/` only, which `M2-C12` § Testing Requirements names as sufficient evidence that no
`V.SMART.*` build or suite is affected. `dotnet test` would have measured nothing attributable to
this task.

## Status

`Blocked` — closed out 2026-08-22 after the escalation budget was exhausted (see the Execution
Record below). The four files are re-specified on this branch and the branch is committed and
unmerged, but validation cannot pass as the task is currently specified. **Blocked on a human,
not a task**: the repository owner (**Vivek**) must resolve **Q-70** — reword acceptance
criterion 7 (or accept the six/seven-path diff as compliant) — before another attempt is
worthwhile. **Only the repository owner may set `Completed`**
([KB-088](../workflow.md#who-may-set-completed)).

Outstanding, none of it blocking the batch:

- **Criterion 7 is reported `not met`** and deliberately not engineered around. See its section.
- **KB-081's row for this task still reads `Ready`** while this file reads `Needs Review`
  (`task-tracker.md:148`). The *Documentation Updates* table above asks this task to update its
  own row; the execution session that ran attempt 3 was instructed that `task-tracker.md` is
  **orchestrator-owned and must not be written**, so the row is handed back rather than edited,
  and the disagreement is recorded here instead of being silently left.
- **Q-68** — does a discarded-implementation task's status reset? Owner.
- **Q-69** — now only an option for the owner, not a blocker: the `axe` criterion was **restored**
  in attempt 3 (Deviation 1), so the deliverable conforms to `M2-C12.md:140-142` whichever way
  the owner answers.

## Execution Record (2026-08-22) — session close-out

**This is a close-out, not a fourth implementation attempt.** No file under `V.SMART/`,
`frontend/`, or the `M2-C04*` batch itself was touched in this session. Only bookkeeping —
this file, `task-tracker.md`, `runner-state.md`, `current-task.md` — was written, per
[review-templates.md § Session close-out](../review-templates.md#session-close-out).

**What was actually done.** Re-read the branch state left by attempt 3 (commit `09001a3` on
`migration/M2-C12-01-respec`) and the validator's `FAIL` verdict and diagnosis, both already
recorded in the sections above and in `failure-log.md`. Confirmed the branch is unchanged since
that verdict:

```
$ git log --oneline master..HEAD
09001a3 M2-C12-01: Raise Q-70 on the criterion-7 contradiction and log the attempt-3 diagnosis
8443a8d M2-C12-01: Restore the axe accessibility criterion across the M2-C04 batch
65f5c3e M2-C12-01: Correct the Execution Record's heading claim and log the attempt-2 diagnosis
03293d2 M2-C12-01: Correct the observed line count in the Execution Record's end-to-end read
19c47b8 M2-C12-01: Write the Execution Record the batch's evidence obligation requires
3d1ccd3 M2-C12-01: Re-specify the design-system tree (M2-C04*) for Angular and remove its supersession banners

$ git diff --name-only master...HEAD
docs/kb/execution/failure-log.md
docs/kb/execution/tasks/M2-C04-01.md
docs/kb/execution/tasks/M2-C04-02.md
docs/kb/execution/tasks/M2-C04-03.md
docs/kb/execution/tasks/M2-C04.md
docs/kb/execution/tasks/M2-C12-01.md
docs/kb/open-questions.md
```

Seven paths, matching the validator's last observation exactly — nothing drifted between the
validator's run and this close-out. `git status` at session start showed only
`docs/kb/execution/runner-state.md` modified in the working tree (orchestrator bookkeeping,
outside this branch's diff, left untouched by this session other than the close-out fields
below). Confirmed `Q-70` is already present at `docs/kb/open-questions.md:42`, raised by the
attempt-3 diagnosis pass with the full contradiction, the three-attempt history, and a proposed
(not applied) rewording — no duplicate question was raised.

**Outcome:** `STOPPED` — escalation budget exhausted (1 of 1 used; `maxRetries=2` already spent
across attempts 1–3 before the escalation). Per
[KB-091 §6.3](../autonomous-runner.md#6-3), a task whose own acceptance criteria contradict each
other is a specification defect, not an implementation failure — no fourth attempt can satisfy
both AC2 (evidence must be quoted in the Execution Record, which is itself a required file in
the diff) and AC7 (the diff may contain only the four `M2-C04*` files) simultaneously, because
AC2's obligation, the Documentation Updates table's KB-081/KB-004 authorisation, and
[KB-088 §4](../workflow.md#4-which-documents-to-update)'s unconditional "Always" update to
`tasks/<TASK-ID>.md` each independently force paths beyond the four into the diff.

**What differed from the plan, and why:** nothing new. This session's only job was to make the
already-decided `STOPPED` outcome durable in the repository — updating this file's frontmatter
`status` (`Needs Review` → `Blocked`) and its prose `## Status` section to name the human owner,
and propagating the same status honestly into `task-tracker.md`, `runner-state.md`, and
`current-task.md`. No acceptance criterion was reworded, no grep re-run to manufacture a
different diff, and no attempt made to trim the diff to four files — doing so was attempt 1's
(and the criterion-2-violating half of) the very inconsistency this close-out is recording, not
fixing.

**Commands run this session and their exact output:** the two `git` commands quoted above, plus
`grep -n "Q-70" docs/kb/open-questions.md` (one match, `:42`, confirming no duplicate). No
`dotnet`/`npm`/build/test command applies — the diff remains Markdown-only under `docs/kb/`,
confirmed again by `git diff --stat master...HEAD -- . ':(exclude)docs'` returning empty.

**Next step for whoever resumes this task:** read `docs/kb/open-questions.md:42` (Q-70), get
Vivek's ruling on acceptance criterion 7's wording (or on accepting the six/seven-path diff as
compliant), apply that ruling to `M2-C12-01.md` **and** `M2-C12-02`..`-05` (the same criterion 7
wording is duplicated across all five sub-task files), then re-validate. Do not re-attempt
without that ruling — three attempts already proved the criterion is unsatisfiable as written.
