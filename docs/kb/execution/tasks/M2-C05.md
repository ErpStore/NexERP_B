---
doc_id: TASK-M2-C05
title: DataGrid
module: execution
task_id: M2-C05
milestone: M2
parent_task: n/a
task_type: Frontend
priority: P0
status: Not Started
estimate: 1.5 wk
depends_on: [M2-C04-02, M2-B02]
source_files:
  - V.SMART/V.SMART.Shared/Components/ColumnMenu.razor
  - V.SMART/V.SMART.Shared/Shared/BaseUserRightsComponent.cs
  - V.SMART/V.SMART.Shared/Services/ColumnPreferenceService.cs
  - V.SMART/V.SMART.Shared/Services/IColumnPreferenceService.cs
  - V.SMART/V.SMART.Shared/ViewModels/GridColumn.cs
  - V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/AdminViewmodel/ColumnPreferenceVM.cs
  - V.SMART/V.SMART.Shared/Data/Master/Admin_Module/UserColumnPreference.cs
  - V.SMART/V.SMART.Shared/Data/Master/MasterScreeenManagement_Module/UserPreference.cs
  - V.SMART/V.SMART.Api/Controllers/CurrencyController.cs
api_endpoints: []
business_rules: []
confidence: n/a
last_verified: 2026-08-12
---

# M2-C05 — DataGrid

| Field | Value |
|---|---|
| Task ID | M2-C05 |
| Milestone | M2 — Foundation |
| Parent | — (this is the parent task) |
| Type | Frontend |
| Priority | P0 |
| Status | Not Started |
| Estimate | 1.5 wk |
| Gate | G2 |

## Task Objective

Build `DataGrid`, the project-owned, server-paged, headless-table-based grid component that
every one of the ~150 React list screens renders. It is the most-used component in the new
frontend and the base on which `RecordPickerDialog` (M2-C06), `LineItemGrid` (M2-C07) and
`ReportPage` (M2-C09) are built.

`DataGrid` is a **generic presentation primitive**. It knows about pages, sorts, filters,
columns, selection, density, states and export. It knows nothing about sales orders,
customers, stock or tax. All ERP semantics stay behind the API.

Delivered by three children:

1. **M2-C05-01** — the server-paged table core (TanStack Table v8 headless + TanStack
   Virtual, URL-synchronised query state, sticky header, pinning, resizing, selection,
   row actions, ARIA grid keyboard model).
2. **M2-C05-02** — per-user column preferences, persisted through the API against the two
   preference contracts that already exist in the database.
3. **M2-C05-03** — the empty / loading / error triad and server-side export.

## Why This Task Exists

- **It is on the M2 critical path.** [KB-080 §9](../README.md#critical-path) puts
  `M2-C01 → M2-C04-01 → M2-C04-02 → M2-C05-01 → M2-C05-03 → M2-D01` on the path to gate
  **G2**, whose exit criteria include *"server paging … Excel export"* for Currency and
  Customer Master.
- **It retires R-22** ([KB-060 R-22](../../risks/technical-debt-register.md), *"Two
  overlapping design systems in the UI"*) for list screens. Today a list screen is a
  `<QuickGrid>` (93 `.razor` files, Confirmed) or a hand-written Bootstrap 5 `<table>`,
  styled by Bootstrap while the rest of the shell is styled by MudBlazor 8.11.0
  (`V.SMART/V.SMART.Shared/V.SMART.Shared.csproj:56`). One owned component replaces both.
- **It is the leverage point of the frontend.** [ADR-003](../../decisions/ADR-003-react-stack.md)
  chose a headless table precisely so that density, keyboard model and server-driven paging
  are under project control. [KB-051](../../frontend-new/design-system.md#data-display)
  names `DataGrid` *"the most important component"*.
- **The server-paging contract already exists and must be honoured, not invented.**
  `SearchWithDynamicFilterAsync(int pageNumber, int pageSize, Dictionary<string, object>? filters)`
  returning `(List<TVM>, int TotalCount)` appears 194 times across the codebase and is
  already consumed by the API at
  `V.SMART/V.SMART.Api/Controllers/CurrencyController.cs:41-47`. M2-B02 formalises it;
  `DataGrid` consumes M2-B02's formalisation.

## Child Tasks

| Task | Name | Type | P | Depends on | Est. | File |
|---|---|---|---|---|---|---|
| M2-C05-01 | Server-paged table core | Frontend | P0 | M2-C04-02, M2-B02 | 4 d | [↗](M2-C05-01.md) |
| M2-C05-02 | Column preferences + persistence | Frontend | P1 | M2-C05-01 | 3 d | [↗](M2-C05-02.md) |
| M2-C05-03 | Empty / loading / error states + export | Frontend | P1 | M2-C05-01 | 2 d | [↗](M2-C05-03.md) |

**M2-C05-01 must complete first.** M2-C05-02 and M2-C05-03 both extend the core's public
API surface and can then run in parallel — they touch different files
(`useColumnPreferences.ts` + `ColumnManager/` versus `DataGridStates.tsx` +
`useGridExport.ts`), with only `DataGrid.tsx` shared. If one pair of hands does both, run
them sequentially in the order listed.

**M2-C05-01 additionally blocks M2-C06, M2-C07 and M2-C09**
([KB-080 §9 Parallel Work](../README.md#parallel-work-1)) — start it early.

## Combined Acceptance Criteria

- [ ] `frontend/nexgen-web/src/shared/components/DataGrid/` exists and exports a typed,
      generic `DataGrid<TRow>` built on TanStack Table v8 + TanStack Virtual, with no
      MudBlazor, Bootstrap or QuickGrid involvement.
- [ ] Paging, sorting and filtering are **server-driven** and issued through the generated
      OpenAPI client (M2-B10). No client-side page slicing of a full result set anywhere in
      the component.
- [ ] Grid query state (page, size, sort, filters) is mirrored in the URL query string, so a
      filtered grid is shareable and survives a refresh
      ([KB-050](../../frontend-new/react-architecture.md#data-fetching-conventions)).
- [ ] 10,000 rows scroll virtualised without dropping below the 60 fps target recorded in
      [KB-050 §Performance targets](../../frontend-new/react-architecture.md#performance-targets).
- [ ] The grid implements the ARIA `grid` pattern: arrow-key cell navigation, `Home`/`End`,
      `PageUp`/`PageDown`, visible 2 px focus ring, no keyboard trap.
- [ ] Per-user column visibility persists across sessions through an API endpoint and is
      stored in the **existing** database shapes — `UserPreference.PreferenceJson`
      (`V.SMART/V.SMART.Shared/Data/Master/MasterScreeenManagement_Module/UserPreference.cs`)
      and/or `UserColumnPreference.ColumnJson`
      (`V.SMART/V.SMART.Shared/Data/Master/Admin_Module/UserColumnPreference.cs`) — with no
      schema change.
- [ ] A preference row written by the Blazor app is readable by the React grid and vice
      versa, proven by a round-trip test.
- [ ] Every one of the five states — first load, refetch, empty (no data), empty (filtered
      to nothing), error — has a distinct, tested presentation per
      [KB-051 §State patterns](../../frontend-new/design-system.md#state-patterns).
- [ ] Export produces a file from a **server** endpoint; the React code never builds an
      xlsx, csv or pdf byte stream.
- [ ] No ERP business rule is implemented in `DataGrid`. A reviewer can read the whole
      component directory and find no domain vocabulary beyond generic column metadata.
- [ ] `npm run lint`, `npm run test` and `npm run build` (commands established by M2-C01)
      all pass from `frontend/nexgen-web/`.
- [ ] `axe` reports no critical violations against the grid's test harness page.

## Rollup Gate Contribution

Contributes to **G2** as follows:

| G2 criterion | Contribution |
|---|---|
| Currency **and** Customer Master fully working in React: … server paging, validation, error contract, Excel export | `DataGrid` delivers the *server paging*, *error contract display* and *Excel export* halves outright. M2-D01 and M2-D02-03 supply the feature configuration on top of it. |
| The TypeScript client is generated from OpenAPI in CI | `DataGrid` consumes only generated types (M2-B10). Any hand-written request/response type inside the component is a gate failure, not a style preference. |
| A user with no rights on a screen is refused by the **API**, not just the UI | Not this task. `DataGrid` renders a `PermissionDeniedState` when the API returns 403, but enforcement is M2-A01/M2-A02. Client gating here is a UX affordance only ([ADR-003](../../decisions/ADR-003-react-stack.md) non-negotiable rule 3). |
| Parity test M2-D03 passes | Indirect. Grid column sets and filter behaviour are what the parity test compares for the Customer Master list. |

M2-C05 also unblocks **M2-C06**, **M2-C07** and **M2-C09**, and therefore the whole of the
M3-5 document-module wave, which [KB-080 §9](../README.md#critical-path) notes is *"not on
the M2 critical path … but on M3's"*.

---

> **No fresh-session prompt lives in this file.** This is a parent task; it is not executed
> directly. The copy-paste executable prompts are in the child files:
> [M2-C05-01.md](M2-C05-01.md), [M2-C05-02.md](M2-C05-02.md), [M2-C05-03.md](M2-C05-03.md).
> Open one child, in one new session, at a time.
