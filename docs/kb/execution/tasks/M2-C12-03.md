---
doc_id: TASK-M2-C12-03
title: Re-specify for Angular — the list and CRUD shell
module: execution
task_id: M2-C12-03
milestone: M2
parent_task: M2-C12
task_type: Documentation
priority: P0
status: Needs Review
estimate: 1 d
depends_on: [M2-C00, M2-C01]
source_files:
  - docs/kb/execution/tasks/M2-C05.md
  - docs/kb/execution/tasks/M2-C05-01.md
  - docs/kb/execution/tasks/M2-C05-02.md
  - docs/kb/execution/tasks/M2-C05-03.md
  - docs/kb/execution/tasks/M2-C06.md
business_rules: []
confidence: n/a
last_verified: 2026-08-22
complexity: MEDIUM
risk: LOW
---

# M2-C12-03 — Re-specify for Angular: the list and CRUD shell

| Field | Value |
|---|---|
| Task ID | M2-C12-03 |
| Milestone | M2 — Foundation |
| Parent | [M2-C12](M2-C12.md) |
| Type | Documentation |
| Priority | P0 |
| Status | Needs Review |
| Estimate | 1 d |
| Gate | G2 |

## Objective

Re-specify **this batch only** — the list and CRUD shell — against
[`ADR-007`](../../decisions/ADR-007-angular-stack.md) and the Angular rewrite of
[KB-050](../../frontend-new/react-architecture.md), removing the `⛔ STOP` banner from each file
*in the same change that removes its React content*.

**Files in this batch — no others may be edited except the ones named under "Also" below:**

- `docs/kb/execution/tasks/M2-C05.md`
- `docs/kb/execution/tasks/M2-C05-01.md`
- `docs/kb/execution/tasks/M2-C05-02.md`
- `docs/kb/execution/tasks/M2-C05-03.md`
- `docs/kb/execution/tasks/M2-C06.md`

Read [`M2-C12`](M2-C12.md) first. It holds the translation table, the rationale, the
out-of-scope list and the failure history. This file does not repeat them; it narrows them.

## The atomicity rule — inherited from the parent, and non-negotiable

> **A file's ⛔ banner may only be removed in the same change that removes its React
> instructions.** No file may come to rest banner-removed and React-bearing — that strips the
> only guard stopping a runner from building the wrong stack while leaving the instruction to
> build it. If you cannot finish a file, **leave its banner in place** and report it as not
> done. Half-done is worse than not started.

Attempt 1 of the unsplit parent failed exactly this way across 23 files. Do not repeat it.

## Acceptance Criteria

1. Every file in this batch has **no** `⛔ STOP — this specification is superseded` banner.
   Verify with `grep -rl '⛔ STOP — this specification is superseded' docs/kb/execution/tasks/ --exclude='M2-C12*'`
   and confirm none of this batch's files appear.
2. **The atomicity rule holds**: for every file in the batch,
   `grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite'`
   returns **only** lines that explicitly mark themselves historical ("the React specification
   this replaces", "no longer specifies", "not deleted from the record"). Quote the actual
   output per file in the Execution Record — a count alone is not evidence.
3. Each file carries a re-specification note naming `M2-C12-03` and the date, in the shape
   [`M2-C01`](M2-C01.md) uses.
4. Every path given under `frontend/` is `frontend/nexgen-web/…`, and every quoted command
   appears verbatim in [KB-083's verified Angular table](../prompt-template.md#verified-repository-commands).
5. **`depends_on`, `business_rules`, `priority`, `estimate` and gate are byte-unchanged** in
   every file — prove by diffing those frontmatter keys, do not assert it.
6. Every `file:line` citation into `V.SMART/` is byte-unchanged, or its change is justified
   individually. This Blazor evidence was never a React decision and survives.
7. **The diff is confined to `docs/kb/`, and within it to this batch's own footprint.** That
   footprint is: the batch files named above; **this task file** (`M2-C12-03.md` — its Execution
   Record is where criterion 2's grep output is quoted, and KB-088 §4 makes it an *Always*
   update); [`task-tracker.md`](../task-tracker.md) (this sub-task's own row); and, only if the
   work actually raises one, [`open-questions.md`](../../open-questions.md),
   [`failure-log.md`](../failure-log.md), [`runner-state.md`](../runner-state.md) and
   [`current-task.md`](../current-task.md). Nothing under `V.SMART/`, `tests/`, `frontend/` or
   `.github/` may appear — verify with
   `git diff --name-only master...HEAD | grep -v '^docs/kb/'`, expected **empty**.
8. Each file still satisfies [KB-090](../task-template.md)'s required section set.

## Testing Requirements

No code, so no suite. Validation is by inspection plus the greps above, **with their output
quoted**. In addition:

- **Read one file in this batch end to end as if about to implement it.** If it needs a detail
  you would have to invent, it is not re-specified. Name the file read and the outcome.

## Documentation Updates

| Document | Update |
|---|---|
| The batch files | Re-specified; banner removed; note added |
| [KB-081](../task-tracker.md) | This sub-task's own row only. **The 25 re-specified rows and `dependency-graph.md` belong to `M2-C12-05`**, the last batch, so the tree is restated once. |
| [KB-004](../../open-questions.md) | Any question raised by an ambiguous re-derivation. Check `git branch --no-merged master` before claiming an id. |

## Completion Conditions

- All 8 acceptance criteria met, greps quoted.
- The atomicity rule holds for every file — verified, not assumed.
- **Only the repository owner may set `Completed`** ([KB-088](../workflow.md#who-may-set-completed)).

## Git Strategy

| | |
|---|---|
| Branch | `migration/m2-c12-03-respec`, cut fresh from `master` |
| Commit | `M2-C12-03: <imperative summary>` |
| Merge | Left for review. Never merged or pushed from an execution session. |

---

## Execution Record (2026-08-22)

Branch `migration/M2-C12-03-respec`, cut fresh from `master` at `f8b4dad`. All five batch files
re-specified for Angular; each file's ⛔ banner was removed in the **same write** that replaced its
stack-specific content — no file was ever left banner-removed and React-bearing, and no
intermediate commit exists in which one was.

### What changed, per file

| File | Banner | Re-spec note | Substantive re-derivation |
|---|---|---|---|
| `M2-C05.md` | removed | added | Parent re-framed: `DataGrid` is now a **wrapper over PrimeNG `p-table`**, not a headless-table build. ADR-007 reverses ADR-003's headless decision explicitly (`ADR-007-angular-stack.md:144-152`, *"PrimeNG's table covers `DataGrid`"*), so this is the ADR's call, not this batch's. Child sequencing, the G2 rollup and the 194-reference paging contract are unchanged. |
| `M2-C05-01.md` | removed | added | The largest change in the batch. `p-table` in `[lazy]="true"` mode; `[virtualScroll]`; `DataGridQueryState` holding signals with **two modes** — route-bound via `ActivatedRoute`/`Router` query params (KB-050's URL-as-state rule) and **detached** for M2-C06's dialog, which must not write the page URL. `HttpClient` + explicit `refresh()` replaces the query library, so "previous page stays visible" becomes the grid's own stated responsibility rather than a library flag. Added an up-front empirical check that `p-table`'s virtual scroller meets KB-050's 10,000-row / 60 fps target, with **escalate, do not fork to a second grid library** as the failure path. The 993-line file is now 589 lines. |
| `M2-C05-02.md` | removed | added | Injectable `ColumnPreferencesService` holding signals; RxJS `debounceTime(750)` + `switchMap` for the cancelling save; PrimeNG overlay for the manager; a hand-written type guard where the replaced text specified a schema library, because ADR-007 dropped it in favour of OpenAPI-generated shapes (`ADR-007-angular-stack.md:95`). **The two-mechanism finding, all 12 `V.SMART/` citations and both registry YAML blocks are byte-identical.** |
| `M2-C05-03.md` | removed | added | Angular templates + signals; `HttpClient` with `responseType: 'blob'`. Added one Angular-specific investigation the replaced text could not have had: whether `Content-Disposition` is actually readable, i.e. whether `Access-Control-Expose-Headers` is set — a gap to raise against M2-B06, not to work around. The i18n hazard is now concrete: this workspace ships `@ngx-translate/core`, so "never route the 409 message through a translation lookup" names a real risk. **BR-SO-001 re-verified against source** (below). |
| `M2-C06.md` | removed | added | PrimeNG `p-dialog`; insertion-ordered selection map in a signal; `DataGridQueryState` in **detached** mode provided at the dialog's own injector level, which is how "the dialog must not write to the page URL" is now enforced structurally rather than by instruction. The 13-row `DetailsModal.razor` behaviour table, the 33-call-site survey and all three registry YAML blocks are byte-identical. |

All five now use `frontend/nexgen-web/src/app/shared/components/<kebab-name>/`, matching KB-050's
realised tree (`react-architecture.md:147` names `data-grid/` and `record-picker-dialog/`
literally) and the workspace on disk.

**The `Fresh-Session Execution Prompt` block was deleted from all four files that carried one**
(`M2-C05-01`, `M2-C05-02`, `M2-C05-03`, `M2-C06`; `M2-C05` never had one). This is not scope creep:
[KB-090](../task-template.md) §*Sections deliberately not in this template* lists that block as
**superseded**, `task-template.md:194`, and each one duplicated the full ADR-003 stack description
in prose. Keeping them would have meant rewriting ~1,800 lines of duplicated stack text that the
current template says should not exist. `M2-C12-02` set the same precedent for its batch.

### Criterion 1 — banner grep

```
$ grep -rl '⛔ STOP — this specification is superseded' docs/kb/execution/tasks/ --exclude='M2-C12*'
docs/kb/execution/tasks/M2-C02.md
docs/kb/execution/tasks/M2-C03.md
docs/kb/execution/tasks/M2-C07.md
docs/kb/execution/tasks/M2-C08-01.md
docs/kb/execution/tasks/M2-C08-02.md
docs/kb/execution/tasks/M2-C08-03.md
docs/kb/execution/tasks/M2-C08.md
docs/kb/execution/tasks/M2-C09.md
docs/kb/execution/tasks/M2-C10.md
docs/kb/execution/tasks/M2-C11.md
docs/kb/execution/tasks/M2-D01.md
docs/kb/execution/tasks/M2-D02-01.md
docs/kb/execution/tasks/M2-D02-02.md
docs/kb/execution/tasks/M2-D02-03.md
docs/kb/execution/tasks/M2-D02.md
docs/kb/execution/tasks/M2-D03.md
```

**None of this batch's five files appears.** The 16 that remain are `M2-C12-04`'s and
`M2-C12-05`'s batches, plus `M2-C02`/`M2-C03`/`M2-C10`/`M2-C11` which are `M2-C12-02`'s and are
cleared on that unmerged branch, not this one.

### Criterion 2 — atomicity grep, per file

```
$ for f in M2-C05 M2-C05-01 M2-C05-02 M2-C05-03 M2-C06; do
    echo "--- $f.md";
    grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' \
      docs/kb/execution/tasks/$f.md;
    echo "   (exit $?)";
  done
--- M2-C05.md
   (exit 1)
--- M2-C05-01.md
   (exit 1)
--- M2-C05-02.md
   (exit 1)
--- M2-C05-03.md
   (exit 1)
--- M2-C06.md
   (exit 1)
```

**Zero matches in all five** — not "only historical matches", none at all. The criterion permits
lines that mark themselves historical; this batch needed none, because each re-specification note
describes what changed in prose ("a schema-validation library", "a headless table library")
without naming the products. Note that the pattern's bare `vite` also matches **Vitest**, the
workspace's actual unit-test runner; the five files therefore describe it by its builder name,
`@angular/build:unit-test`, which is what KB-083's row and `angular.json` actually configure.

### Criterion 3 — re-specification notes

Each file opens with a note in `M2-C01.md:28-37`'s shape, naming `M2-C12-03` and `2026-08-22`,
stating that the banner was removed *in the same change* that removed the guarded content, and
that the superseded specification is not deleted from the record.

### Criterion 4 — paths and commands

```
$ grep -ohE 'frontend/[A-Za-z0-9_.*{}/-]+' <the five files> | sed -E 's#^(frontend/[a-z-]+)/.*#\1/...#' | sort -u
frontend/nexgen-web/...
frontend/vsmart-erp/...
```

Every `frontend/nexgen-web/` path is correct; the two `frontend/vsmart-erp/` mentions are the
*must-not-change* entries naming the 19.2 pilot whose disposition `M2-C11` owns — they are
references to a directory, not paths for new code.

```
$ awk '<extract the contents of every fenced bash block>' <the five files> | sort -u
git status --porcelain
npm ci
npm run build
npm run format:check
npm run lint
npm run test:ci
npm run typecheck
```

All seven are verbatim rows in
[KB-083 § Verified repository commands](../prompt-template.md#verified-repository-commands) —
six in the frontend sub-table (measured 2026-08-21) and `git status --porcelain` in the main
table. Three commands that the replaced text used are **gone**: `npm run test` (the workspace has
a `test` script, but KB-083 verified `test:ci`), and any coverage command (no such script exists).

**Corrected on attempt 2 (2026-08-22).** Attempt 1 also removed every `axe` invocation, on the
true-but-incomplete premise that no accessibility-testing dependency is installed today
(`frontend/nexgen-web/package.json`, verified 2026-08-22). That was a **regression**:
`M2-C12.md:140-142` forbids changing acceptance-criteria *semantics*, `axe` is stack-independent,
and **Q-69 is already answered in the conservative direction** — the criterion is restored, with
`M2-C04-02.md:391-392,408` the merged worked example. The five files now carry the `axe`
criterion again, translated only in *how*: an `a11y.spec.ts` driven from
`@testing-library/angular` under the existing `npm run test:ci`, with `axe-core` supplied as a
dev dependency by **M2-C04-01** (`M2-C04-01.md:388`), which every file in this batch reaches
through a Hard `depends_on` chain. The `angular-eslint` template-accessibility coverage added by
attempt 1 is kept **alongside**, not instead. **No new command is introduced** — the scan runs
inside `npm run test:ci`, a verbatim KB-083 row. One non-KB-083 command survives, in
`M2-C06.md`, and is labelled as such: the
read-only `git grep -l "<DetailsModal" -- …` survey helper, presented inline as an investigation
aid and explicitly not as a verification command.

### Criterion 5 — frontmatter keys byte-unchanged

```
$ for f in <the five>; do
    diff <(git show master:…/$f.md | tr -d '\r' | sed -n '/^---$/,/^---$/p' \
             | grep -E '^(depends_on|business_rules|priority|estimate):') \
         <(sed -n '/^---$/,/^---$/p' …/$f.md \
             | grep -E '^(depends_on|business_rules|priority|estimate):')
    diff <(git show master:…/$f.md | tr -d '\r' | grep -E '^\| (Gate|Priority|Estimate) \|') \
         <(grep -E '^\| (Gate|Priority|Estimate) \|' …/$f.md)
  done
=== M2-C05      frontmatter keys identical / gate-priority-estimate rows identical
=== M2-C05-01   frontmatter keys identical / gate-priority-estimate rows identical
=== M2-C05-02   frontmatter keys identical / gate-priority-estimate rows identical
=== M2-C05-03   frontmatter keys identical / gate-priority-estimate rows identical
=== M2-C06      frontmatter keys identical / gate-priority-estimate rows identical
```

Every `diff` produced no output. `M2-C05-03.md` keeps `business_rules: [BR-SO-001]`. The only
frontmatter change anywhere is `last_verified: 2026-08-12 → 2026-08-22` in all five.

### Criterion 6 — `V.SMART/` citations byte-unchanged

```
$ for f in <the five>; do
    diff <(git show master:…/$f.md | tr -d '\r' \
             | grep -oE 'V\.SMART/[A-Za-z0-9_./-]+\.(cs|razor|csproj)(:[0-9]+(-[0-9]+)?)?' | sort -u) \
         <(grep -oE 'V\.SMART/[A-Za-z0-9_./-]+\.(cs|razor|csproj)(:[0-9]+(-[0-9]+)?)?' …/$f.md | sort -u)
  done
--- M2-C05      (no output)
--- M2-C05-01   (no output)
--- M2-C05-02   (no output)
--- M2-C05-03   9d8
                < V.SMART/V.SMART.Shared/Services/ExcelExportService.cs:113
--- M2-C06      (no output)
```

**The single difference is justified and is not a lost citation.** The dropped occurrence was at
`M2-C05-03.md:603` on `master` — inside the deleted *Fresh-Session Execution Prompt* block. The
same citation survives twice in the retained body, as `ExcelExportService.cs:24`, `:113`
(*Why This Task Exists*) and in the *Existing Behavior to Preserve* row.

**One citation was independently re-verified rather than trusted**, because BR-SO-001 is the only
business rule in the batch and `M2-C05-03` requires a byte-for-byte test against it:

```
$ sed -n '486,490p;596,600p' V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/SalesService/MfgPoService.cs
                        PoSubIds.Contains(qs.RefPoSubId.Value));
                if (hasDc)
                    return (false, "Cannot delete this Sales Order as a Sales DC transaction exists.");
…
                if (hasDc)
                    return (false, "Cannot cancel this Item as a Sales DC transaction exists.");
```

Both sentences are still at `:488` and `:598`, word-for-word. **Confirmed**, 2026-08-22.

### Criterion 7 — diff confined to `docs/kb/` and to this footprint

```
$ git diff --cached --name-only master
docs/kb/execution/task-tracker.md
docs/kb/execution/tasks/M2-C05-01.md
docs/kb/execution/tasks/M2-C05-02.md
docs/kb/execution/tasks/M2-C05-03.md
docs/kb/execution/tasks/M2-C05.md
docs/kb/execution/tasks/M2-C06.md
docs/kb/open-questions.md

$ git diff --cached --name-only master | grep -v '^docs/kb/'
   (exit 1 — no output)
```

Plus this file, staged with the record above. Eight paths, every one inside the footprint
criterion 7 names: the five batch files, this task file, the tracker's own `M2-C12-03` row, and
`open-questions.md` for Q-83 below. Nothing under `V.SMART/`, `tests/`, `frontend/` or
`.github/`. **No build or test command was run, and none is claimed** — this task writes no code,
and the `docs/kb/`-only diff is the evidence that no build could be affected.

### Criterion 8 — KB-090's required section set

All 13 required headings — *Objective, Scope, Out of Scope, Dependencies, Relevant Documentation,
Relevant Existing Code, Business Rules, Implementation Requirements, Acceptance Criteria, Testing
Requirements, Documentation Updates, Completion Conditions, Git Strategy* — are present in all
five files, checked by an exact `^## <heading>$` grep per file per heading; zero missing. Several
files also keep non-template sections the replaced text had (*Why This Task Exists*, *Existing
Behavior to Preserve*, *Target Result*, *API Changes*, *Frontend Changes*, *Verification
Commands*), which KB-090 permits and which carry the Blazor evidence.

### Testing Requirement — the end-to-end read

**File read end to end as if about to implement it: `M2-C05-01.md`**, the batch's hardest and
highest-fan-out file.

**Outcome: implementable, with two stop conditions that are specified rather than left to
invention.** The reader learns the component's directory and file list, the PrimeNG inputs to use
(`[lazy]`, `[virtualScroll]`, `[virtualScrollItemSize]`), the two query-state modes and which
Angular APIs back them, the column-model field list (traced to `GridColumn.cs:3-14`), the 14 tests
and the exact commands. Nothing in it requires guessing a stack decision.

The two things it cannot supply are supplied as **instructions to stop**, not as gaps: M2-B02's
paging contract does not exist yet, and the file says *"stop and report Blocked — do not guess the
contract"*; and whether `p-table`'s virtual scroller meets the 60 fps target is unknown, so the
file makes measuring it step 3, before the build, with escalation — not a silent fallback to a
second grid library — as the failure path. A task that names its unknowns and their exits is
specified; one that hides them is not.

Two things a reader will notice and should not be surprised by. The `<app-data-grid>` block under
*Target Result* is marked illustrative — final input names are the implementer's design decision,
the capabilities are not. And the file deliberately does **not** name a PrimeNG component for
every affordance; where PrimeNG's own keyboard handling might already cover an ARIA-grid key, the
instruction is to verify each key against the list rather than assume coverage.

### Discovered, and where it is recorded

- **Q-83 ([KB-004](../../open-questions.md))** — `ADR-007-angular-stack.md:98` says *"`LineItemGrid`
  re-evaluated, **see below**"*, and there is no "below": a full-file grep finds `LineItemGrid`
  only at `:98` and `:206`. The nearest resolving prose is `:144-152`. This did **not** block this
  batch — `DataGrid` maps cleanly to PrimeNG Table and this batch cites `:144-152` directly rather
  than following the dangling pointer — but `M2-C12-04` will hit it while re-specifying `M2-C07`,
  and must not infer a table technology from a pointer that goes nowhere. Accepted ADRs are
  immutable, so even a pointer fix is the owner's call. `git branch --no-merged master` checked
  before claiming the id; highest elsewhere is Q-70.
- **No accessibility-audit tooling is installed in the Angular workspace *today*** —
  `frontend/nexgen-web/package.json` has no such dependency and KB-083 no such command, verified
  2026-08-22. **This does not survive the dependency chain, and attempt 1 wrongly treated it as if
  it did:** `M2-C04-01` installs `axe-core` as a dev dependency (`M2-C04-01.md:388`) and every
  file in this batch depends on it transitively, so the scanner is present before any of them can
  run. Recorded inside each of the five files rather than as a separate KB row, because it is an
  instruction those files need at the point of use: the `axe` scan is theirs to write, `npm run
  lint`'s `angular-eslint` template accessibility rules run **in addition**, and the
  repository-wide `axe`-in-CI pass is M5-09's. Q-69 governs and is answered; it was **not**
  re-raised.
- **No investigation registry entry was added.** KB-003 was searched for `M2-C05*`, `M2-C06` and
  `M2-C12-03` and returned nothing; this task performed no repository investigation beyond
  re-verifying citations it was carrying, so there is no new finding to record and KB-003 is
  untouched. Recording the negative result here rather than in KB-003 keeps the diff inside
  criterion 7's footprint.
- **No new business rule.** BR-SO-001 was re-verified, not discovered, and is unchanged.

## Execution Record (2026-08-22) — session close-out

This session did not implement anything further. It reviewed the two implementation attempts
already on `migration/M2-C12-03-respec` (`f2ed0b3`, `1c412ba`), confirmed the independent
validator's verdict, and performed the KB-088 §4 session close-out.

**Attempt 1** (`f2ed0b3`) re-specified all five batch files for Angular/ADR-007, removing each
`⛔` banner atomically with its React content. Independent validation of that attempt found a
**regression**: attempt 1 also deleted the stack-independent runtime `axe` accessibility
criterion from all five files, reasoning that no accessibility-scanner dependency is installed
today. That premise is true but incomplete — `M2-C04-01` installs `axe-core` as a dev
dependency and every file in this batch reaches it through a Hard `depends_on` chain — so the
deletion changed acceptance-criteria semantics, which `M2-C12.md:140-142` forbids. Diagnosed as
an `implementation-error`, not a specification defect; recorded in `failure-log.md`.

**Attempt 2** (`1c412ba`) restored the `axe` criterion and its test row in all five files,
translated only in *how* (an `a11y.spec.ts` under the existing `npm run test:ci`, no new command
or dependency), corrected two imprecise `ADR-007` line citations (`:151-158` → `:144-152`,
`:152` → `:149`), and corrected this task file's own Execution Record so it no longer asserted
the removal.

**Independent validation of attempt 2: `PASS`.** All 8 acceptance criteria `MET` (banner grep,
atomicity grep, re-spec notes, `frontend/`-path and command verification against KB-083,
frontmatter/citation byte-diffs, criterion-7 confinement, KB-090 section coverage — each
re-derived directly by the validator, not taken on trust), `scopeOk: true`,
`failureCategory: none`, `regressions: none`. Two attempts of 4 used, 0 escalations. Full
validator evidence, including every quoted command, is reproduced in the Execution Record above
(2026-08-22) and in `failure-log.md`.

**Verified again in this close-out session, independently of the validator's report:**

```
$ git status --porcelain
 M docs/kb/execution/failure-log.md
 M docs/kb/execution/runner-state.md
```

Both modifications are orchestrator/close-out bookkeeping files this session itself is updating
(criterion 7 names both as touchable when the work raises a discovery — here, this close-out's
own record-keeping). No file under `V.SMART/`, `frontend/`, `tests/` or `.github/` is modified.
Branch `migration/M2-C12-03-respec` (tip `1c412ba`) remains **unmerged and unpushed**.

**Status set to `Needs Review`, not `Completed`.** Per
[KB-088 "Who may set `Completed`"](../workflow.md#who-may-set-completed), only the repository
owner may close a task `Completed`; this task's own Completion Conditions restate the same rule.
The work is validated `PASS` and ready for the owner's review and merge decision, but a human
integration step remains.

**Discoveries recorded, and where:** the attempt-1 regression and its fix are recorded in
`docs/kb/execution/failure-log.md`; `open-questions.md` already carries **Q-83** (the dangling
`ADR-007-angular-stack.md:98` "see below" pointer for `LineItemGrid`), raised during attempt 1
and unaffected by attempt 2. No new discovery arose in this close-out session itself.

**Note for whoever re-specifies `M2-C07` next (`M2-C12-04`):** do not resolve **Q-83** by
inference — the pointer names no resolving section, only `ADR-007-angular-stack.md:144-152`'s
adjacent prose, and accepted ADRs are immutable, so even a pointer fix is the owner's call.
