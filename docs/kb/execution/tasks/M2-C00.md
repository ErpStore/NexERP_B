---
doc_id: TASK-M2-C00
title: Rewrite KB-050 frontend architecture for Angular
module: execution
task_id: M2-C00
milestone: M2
parent_task: n/a
task_type: Documentation
priority: P0
status: Needs Review
estimate: 2 d
depends_on: [G0]
source_files:
  - frontend/vsmart-erp/src/app/app.config.ts
  - frontend/vsmart-erp/src/app/app.routes.ts
  - frontend/vsmart-erp/src/app/core/auth/auth.service.ts
  - frontend/vsmart-erp/src/app/core/auth/auth.guard.ts
  - frontend/vsmart-erp/src/app/core/auth/auth.interceptor.ts
api_endpoints: []
business_rules: []
confidence: n/a
last_verified: 2026-08-20
---

# M2-C00 — Rewrite KB-050 frontend architecture for Angular

| Field | Value |
|---|---|
| Task ID | M2-C00 |
| Milestone | M2 — Foundation |
| Type | Documentation |
| Priority | P0 |
| Estimate | 2 d |
| Depends on | G0 |
| Gate | G2 |

## Why this task exists

[ADR-007](../../decisions/ADR-007-angular-stack.md) selected Angular + PrimeNG on 2026-08-20,
superseding ADR-003's React stack. [KB-050](../../frontend-new/react-architecture.md) — the
frontend architecture proposal that **every `M2-C` task cites as its primary input** — still
describes a React application.

**This task is the gate on the entire `M2-C` tree.** Until KB-050 describes Angular, there is no
authoritative structure to specify the other tasks against, and re-specifying them first would
mean writing detail against a guess.

## Scope

Rewrite KB-050 for Angular. The existing document already carries a section-by-section map, added
when ADR-007 landed, of what is dead and what still binds — **start from that map, do not
re-derive it.**

**Rewrite:**

- *Recommended stack* → ADR-007's table. Do not re-decide it; ADR-007 is authoritative.
- *Project structure* → the Angular layout: standalone components, `core/` / `shared/` /
  `features/`, lazy feature routes.
- *Data-fetching conventions* → typed services over `HttpClient` with explicit `refresh()`.
  TanStack Query is out (ADR-007 records why).
- *Authentication flow* → Angular functional guards and an HTTP interceptor. **Token storage is
  `M2-C02`'s decision**, not this document's — state the constraint (ADR-004) and the open choice,
  and **do not** carry over the pilot's `localStorage` JWT, which is XSS-exposed.
- *Permission-based rendering* → the same 152 × 5 matrix, expressed as a structural directive
  rather than a component wrapper.
- *Error handling* → **this section predates `M2-A06`.** The real contract now exists: rewrite
  against [ADR-002 §4](../../decisions/ADR-002-rest-api-layer.md) and
  `V.SMART/V.SMART.Api/Middleware/ApiProblems.cs`, including that a business-rule refusal is a
  `409` whose `title` carries the service's message **verbatim** (BR-SO-001).

**Keep, with only the framework nouns changed:**

- *Design constraints from the existing system* — facts about the ERP.
- *Document editor pattern* and *Workflow commands* — the core abstraction, a shape rather than a
  library.
- *Performance targets*, *Accessibility*, *What is deliberately not rebuilt*.

**Read the existing Angular pilot** (`frontend/vsmart-erp/`, Angular 19.2 + PrimeNG 19.1) and
record what its structure gets right and where it should not be copied — it has 9 components
including an auth service, guard and interceptor, and `M2-C11` adopts it as the baseline.

## Acceptance criteria

- [ ] KB-050 describes an Angular application end to end; no section instructs React, Vite,
      Mantine, TanStack, Zustand, React Hook Form or Zod as the thing to build.
- [ ] Its `title` and `doc_id` remain **KB-050** — ~20 task files cite it and must not have to
      change twice. The filename may change; if it does, every citation changes in the same commit.
- [ ] The stack section matches ADR-007's table exactly, and says so rather than restating the
      rationale.
- [ ] The error-handling section matches the **shipped** `ApiProblems` contract, with `file:line`
      evidence.
- [ ] Token storage is stated as an open decision owned by `M2-C02`, with the pilot's
      `localStorage` approach explicitly **not** endorsed.
- [ ] The pilot's hardcoded `http://localhost:5144` in **both** `environment.ts` and
      `environment.prod.ts` is recorded as a defect to remove, not a pattern to copy.
- [ ] The ⛔ STOP banner is removed from KB-050 — superseded by the rewrite itself.
- [ ] The `M2-C01` task file is re-specified for Angular in the same change, since it is the next
      task and its banner otherwise blocks it.
- [ ] No code is written. This is a documentation task.

## Do not

Write any code. Re-decide ADR-007's stack choices — implement them as recorded, and if the ADR is
wrong, say so and stop rather than quietly diverging. Re-specify the whole `M2-C` tree: only
`M2-C01` is in scope here; the rest are re-specified as they come up. Delete the React app —
that is the re-scoped `M2-C01`'s call.

---

## Execution Record (2026-08-20)

**Status: `Needs Review`.** Implemented on `migration/M2-C00-kb050-angular-rewrite` — `aebc477`
(attempt 1) plus one correction commit (attempt 2), cut from `master` tip `ec70620`. Not merged,
not pushed. **Attempt 1 was validated `FAIL` (category `acceptance-criterion`); attempt 2 is a
narrow correction pass over the three defects that verdict named, and nothing else.** The full
verdict is in [`../failure-log.md`](../failure-log.md) (KB-092) under *M2-C00 · attempt 1*.

### What was actually done

- **Rewrote [KB-050](../../frontend-new/react-architecture.md)** for Angular. `doc_id: KB-050`,
  the title and the filename `react-architecture.md` are unchanged, so none of the ~20 task files
  citing it had to move. The `⚠ PARTLY SUPERSEDED` banner that stood at `:18` on `master` is gone —
  superseded by the rewrite itself.
  - *Recommended stack* is ADR-007's decision table copied with only the "Change from ADR-003"
    column dropped (`ADR-007-angular-stack.md:86-103`); the rationale is **not** restated, only
    pointed at.
  - *Project structure*, *data-fetching*, *authentication flow* and *permission-based rendering*
    rewritten for standalone components, typed services over `HttpClient`, functional guards and
    interceptors, and a structural directive over the unchanged 152 × 5 matrix — with the client
    marked a UX affordance and the server authoritative per ADR-004 / BR-AUTH-002.
  - *Error handling* rewritten against the **shipped** contract, not the pre-`M2-A06` sketch:
    `V.SMART/V.SMART.Api/Middleware/ApiProblems.cs` and `ProblemTypes.cs`, including BR-SO-001
    (a business-rule refusal is a `409` whose `title` carries the service's message verbatim,
    `ApiProblems.cs:47-53`).
  - *Design constraints*, *document editor pattern*, *workflow commands*, *performance* and
    *accessibility* kept, with framework nouns changed only.
  - Token storage is recorded as **`M2-C02`'s open decision**, with the pilot's `localStorage`
    JWT explicitly not endorsed; the pilot's hardcoded `http://localhost:5144` in **both**
    `environment.ts` and `environment.prod.ts` is recorded as a defect to remove.
- **Re-specified [`M2-C01`](M2-C01.md)** for Angular in the same change — Angular target tree,
  PrimeNG-only dependency baseline, no-Karma runner, no hardcoded API host, CI jobs re-pointed —
  with its STOP banner replaced by an explanatory banner and the React execution record preserved
  verbatim. No other `M2-C` task file was touched, per this task's *Do not* list.
- **Read the Angular pilot** (`frontend/vsmart-erp/`) and recorded what it gets right and where it
  must not be copied, as an **INV-040 amendment** (the shipped error contract read from the client side) and an **INV-021 re-verification** (pilot facts only) in the investigation registry. **Negative result recorded and
  independently re-confirmed at validation:** `grep -rniE "permission|hasRight|screenright|rights"
  frontend/vsmart-erp/src/` returns no matches — the pilot has no permission gating of any kind.
- **Raised `Q-37`** (the `M2-C11` archive-vs-adopt contradiction) rather than deciding it — that is
  a re-specification call this task is forbidden to make.
- **No code was written.** `git diff --stat master...HEAD` is five Markdown files under `docs/kb/`;
  nothing under `V.SMART/`, `frontend/`, `db/`, `.github/` or `Migrations/`.

### What attempt 1 got wrong, and what attempt 2 changed

| # | Defect found at validation | Correction |
|---|---|---|
| 1 | The **Tables** row of the stack table had been rewritten (`LineItemGrid` re-evaluated — `M2-C07`'s call … with AG Grid as the named fallback), so 16 of 17 rows matched ADR-007 rather than all 17 — while `react-architecture.md:64-65` asserted the copy dropped *only* the "Change from ADR-003" column. The substituted text was true but restated ADR-007:117-125's rationale, which the criterion forbids | Restored the row byte-identical (**PrimeNG Table**; `LineItemGrid` re-evaluated, see below) and added a short note under the table saying that *"see below"* is ADR-007's own forward reference to its §*Key rationales* (`ADR-007-angular-stack.md:117-125`), not a pointer into KB-050. The table now matches columns 1–2 of `ADR-007-angular-stack.md:86-103` byte for byte, and the sentence above it is true |
| 2 | **Four ADR-007 line ranges pointed at text that is not there**, twice attributing a verbatim inline quotation to lines that do not contain it. ADR-007 has one commit (`2f5ba16`, 197 lines) and was untouched by this change, so these were wrong as written, not stale | `react-architecture.md:205` `:180-186` → **`:151-153`**; `:226` `:188-190` → **`:155-157`**; `:407` `:145-148` → **`:140-142`**; `:96` `:139-149` → **`:137-142`**. Two further ranges not flagged but over-wide were tightened in `M2-C01.md`: `:76-82` → `:76-80`, `:126-128` → `:127-128` |
| 3 | **This file was never updated** — no `## Execution Record`, frontmatter still `status: Ready`. KB-088 §4 lists `tasks/<TASK-ID>.md` as **"Always — frontmatter status + Execution Record"** | This section, and `status: Needs Review` |

Each of the eight ADR-007 ranges KB-050 now cites was re-read from disk this session and matched
against the text it is offered for.

### Known and deliberate — not defects, recorded so they are not "fixed" twice

- **Three anchor links are broken** by the authorised heading rename *"What is deliberately not
  rebuilt in React"* → *"… in the SPA"*: `M2-C08-01.md` (×2) and `M2-C08-02.md` still target
  `#what-is-deliberately-not-rebuilt-in-react`. Both files are stale under ADR-007 and are corrected
  when those tasks are re-specified; editing them here would be re-specifying `M2-C` files this task
  is told not to touch. Disclosed in KB-050 itself. **All nine other cited anchors still resolve** —
  verified at validation.
- **The filename stays `react-architecture.md`** — the criterion permits it, and changing it would
  move ~20 citations for no behavioural gain.
- The stack was **implemented as ADR-007 records it, never re-decided**.

### Validation

Documentation task — no build, no test run is required or meaningful. The independent validator
nevertheless ran `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` on the attempt-1 tree and
reported **6695 Warning(s) / 0 Error(s)**, exactly the KB-083 baseline; attempt 2 changes only
Markdown, so that result still stands. (`dotnet test` is not usable as evidence here — see KB-083.)
Re-validation of the corrected branch is the runner's next step; **`Needs Review` means unmerged
and unreviewed, not signed off.** Only the repository owner sets `Completed`.
