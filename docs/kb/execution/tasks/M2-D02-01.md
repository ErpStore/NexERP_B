---
doc_id: TASK-M2-D02-01
title: Customer Master — @code triage + business-logic extraction
module: execution
task_id: M2-D02-01
milestone: M2
parent_task: M2-D02
task_type: Backend
priority: P0
status: Not Started
estimate: 4 d
depends_on: [M2-D01]
source_files:
  - V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/CustomerUpsert.razor
  - V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/CustomerList.razor
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/MasterService/GeneralService/CustomerService.cs
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/IBusinessService/IMasterServices/IGeneralService/ICustomerService.cs
  - V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/GeneralViewModel/CustomerVM.cs
  - V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/GeneralViewModel/CustomerIndirectVM.cs
  - V.SMART/V.SMART.Shared/Data/Master/General_Module/Customer.cs
  - V.SMART/V.SMART.Shared/Data/Master/General_Module/CustomerIndirect.cs
  - V.SMART/V.SMART.Shared/Data/Master/General_Module/ContactPerson.cs
  - V.SMART/V.SMART.Shared/Data/Configuration/MasterConfiguration/CustomerConfiguration.cs
  - V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs
  - V.SMART/V.SMART.Shared/Repository/MasterRepository/Generals/CustomerRepository.cs
  - V.SMART/V.SMART.Shared/Repository/IRepository/IMasterRepository/IGenerals/ICustomerRepository.cs
  - V.SMART/V.SMART.Shared/Mappings/MasterMapping/GeneralMasterProfile/CustomerMapping.cs
  - V.SMART/V.SMART.Shared/Repository/Repository.cs
  - V.SMART/V.SMART.Shared/Services/ForeignKeyUsageChecker.cs
entities: [Customer, CustomerIndirect, ContactPerson, State, Currency, ItemSub]
api_endpoints: []
database_tables: [Customer, CustomerIndirect, ContactPerson, State, Currency]
business_rules: []
confidence: n/a
last_verified: 2026-08-12
---

# M2-D02-01 — Customer Master `@code` triage + business-logic extraction

| Field | Value |
|---|---|
| Task ID | M2-D02-01 |
| Milestone | M2 — Foundation |
| Parent | M2-D02 |
| Type | Backend |
| Priority | P0 |
| Status | Not Started |
| Estimate | 4 d |
| Gate | G2 |

> **`business_rules` in the frontmatter is empty on purpose.** No `BR-CUST-*` rule exists in
> [KB-030](../../business-rules/business-rule-inventory.md) today (Confirmed — grep for
> `BR-CUST` across `docs/kb/` returns nothing). **This task ALLOCATES the `BR-CUST-nnn`
> series.** On completion, the frontmatter must be rewritten with the ids actually
> allocated, and KB-030 must carry them.

## Task Objective

Triage every line of the `@code` blocks in the two Customer Master Razor pages into the
three INV-024 buckets — **presentation** (discard), **data loading** (becomes an API call),
**business logic** (extract to a server-side service) — record that triage as a written
artefact, and then move the business-logic bucket out of the page and into
`ICustomerService` / `CustomerService` **without changing observable behaviour**.

The Blazor pages keep working, against the same database, through the extracted service.
No React code is written in this task. No controller is written in this task.

Two deliverables, both mandatory:

1. **A triage document** — `docs/kb/business-rules/customer-master-rules.md` — recording,
   per `@code` member, which bucket it falls in and why, plus the extracted business rules
   with `file:line` evidence and a `Confirmed`/`Inferred`/`Unknown` rating.
2. **The extraction itself** — new methods on `ICustomerService`, implemented in
   `CustomerService`, with `CustomerUpsert.razor`'s `@code` reduced to presentation and
   data-loading only.

## Why This Task Exists

**This is the first real application of migration principle 3, "extract before rebuild"
([KB-080 §3](../README.md#3-migration-principles)).** Everything about it is a template.
Whatever this task decides — how a triage is recorded, what counts as "business logic",
what an extracted service method signature looks like, how the extraction is proved
behaviour-preserving — is what every `<W>-02` and `<W>-03` step in M3 and M4 will copy
([KB-080 §10](../README.md#10-module-migration-task-pattern)). Getting the *shape* right
here is worth more than getting Customer Master shipped quickly.

It serves **R-06** — ~184,000 LOC (57% of 321,661 Razor LOC) of logic sits inside `@code`
blocks — and it is the module-level instantiation of **INV-024**, which
[KB-003](../../investigation-registry.md) schedules as "task `02` of every wave".

**It also exists because Customer Master cannot be done the way Currency was.** M2-D01
exposed Currency through `CurrencyController` with **zero service changes**, because
`ICurrencyService` already had `GetByIdAsync`, `CreateAsync` and `UpdateAsync`
(`V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/IBusinessService/IMasterServices/IAccountsService/ICurrencyService.cs:12-19`,
Confirmed). `ICustomerService` has **four methods and none of them writes a customer**
(`.../IGeneralService/ICustomerService.cs:14-18`, Confirmed). The entire create/update
path — validation, GST/PAN derivation, the transaction, the child-collection
synchronisation — lives in `CustomerUpsert.razor`'s `@code` block
(`CustomerUpsert.razor:954-1214`, Confirmed). There is no service to wrap. **Without this
task, M2-D02-02 would have no choice but to reimplement Customer write logic in a
controller, which principle 3 forbids.**

That contrast is the point of the vertical slice: Currency proved the *easy* case,
Customer proves the *real* case. If the extraction pattern chosen here is awkward, it is
far cheaper to change it now than after sixty more modules have copied it.

## Prerequisites

- **M2-D01** must be Completed. Currency must be working end-to-end in React before
  Customer Master starts, because M2-D01 is what proves the controller template, the error
  contract and the generated client actually work.
- Gate **G0** passed (transitively, via M2-D01) — in particular **M0-12-01**, which creates
  `tests/V.SMART.Shared.Tests/` (TO BE CREATED as of 2026-08-12). This task writes tests
  into that project; without it there is nowhere to put them.
- The Blazor host must be runnable against a tenant database. The extraction is validated
  by **using the Blazor UI**, not by reading the diff.

## Dependencies

| Dependency | Class | Note |
|---|---|---|
| M2-D01 — Currency end-to-end in React | Hard | Proves the slice's plumbing before Customer starts. Also establishes the Zod/DataAnnotations mirroring convention this task's rules must feed. |
| M0-12-01 — test project created | Hard | `tests/V.SMART.Shared.Tests/` (TO BE CREATED) is where the extraction's characterisation tests live. |
| M2-B07 — `AddVSmartDomain()` DI extension | Hard | `V.SMART/V.SMART.Api/Program.cs:102` registers `ICurrencyService` and nothing else (Confirmed). `ICustomerService` is not registered in the API host; M2-B07 is what fixes that for every service at once. This task must **not** add a one-off `AddScoped<ICustomerService, …>` line to `Program.cs`. |
| INV-024 — `@code` triage per module | Information | This task is INV-024's first instantiation. The registry row must be amended, not replaced. |
| KB-030 — business rule inventory | Information | Supplies the rule format and the `BR-<AREA>-<nnn>` id convention. This task appends a new `BR-CUST-*` section. |
| M2-D02-02 — `CustomersController` | Testing | Consumes this task's service methods. If a signature here is wrong, it surfaces there. Do not pre-empt it. |
| M2-A01-02 — `[RequireScreen]`/`[RequireRight]` | Information | Not needed by this task (services carry no authorization, by design — BR-AUTH-002), but the screen name this task records is what M2-D02-02 will annotate with. |

## Required Existing Knowledge

Read before touching the repository:

| Doc | Path | Why |
|---|---|---|
| KB-083 | `docs/kb/execution/prompt-template.md` | Generation rules + the verified-commands table |
| KB-080 §9, §10 | `docs/kb/execution/README.md` | M2-D's place in the plan; the 14-step module pattern this task is step 02+03 of |
| KB-002 | `docs/kb/source-of-truth-rules.md` | Confirmed / Inferred / Unknown discipline |
| KB-003 | `docs/kb/investigation-registry.md` | INV-024, INV-011, INV-025 |
| KB-030 | `docs/kb/business-rules/business-rule-inventory.md` | Rule format, id convention, existing `BR-*` ids (do not collide) |
| KB-011 | `docs/kb/architecture/backend-architecture.md` | Repository/UnitOfWork conventions, service naming |
| KB-020 | `docs/kb/modules/module-inventory.md` | Customer's module boundary and its dependants |
| ADR-002 | `docs/kb/decisions/ADR-002-rest-api-layer.md` | §2 (payloads are the existing `…VM`s, unchanged), §3 (workflow commands are server-side), §4 (409 carries the service's message verbatim) |
| ADR-004 | `docs/kb/decisions/ADR-004-server-side-authorization.md` | Why services stay authorization-free and the filter does the work |
| KB-005 | `docs/kb/INDEX.md` | Question-to-document routing |

## Investigation Requirements

**Update an existing investigation (INV-024) + record new business rules.**

Follow the anti-repetition protocol. Concretely:

1. **Search KB-003 first.** INV-024 (`@code` triage per module) is *Scheduled*, not
   Complete — no module has been triaged yet. INV-011 (business rules, cross-module) is
   *Partial* and contains **no Customer rule** (Confirmed by grepping `BR-CUST` across
   `docs/kb/` — no hits). So there is nothing to reuse for Customer specifically.
2. **Reuse, do not re-derive:** the repository/UnitOfWork conventions (INV-002/KB-011), the
   permission model (INV-004/KB-013 — Customer is `ScreenCode` 15, seeded at
   `V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs:1166`), and the module boundary
   (INV-007/KB-020).
3. **Investigate only the gap:** the two Customer pages' `@code`, `CustomerService`,
   `CustomerRepository`, `CustomerMapping`, and the three entities.
4. **Record negative results.** Several are already known and must be preserved in writing
   so no later session re-derives them — see *Existing Behavior to Preserve*, items marked
   *negative finding*.

## Repository Areas

| Area | Path | Role in this task |
|---|---|---|
| Razor pages | `V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/` | The triage subject. Two files. |
| Business service | `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/MasterService/GeneralService/CustomerService.cs` | The extraction target |
| Service interface | `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/IBusinessService/IMasterServices/IGeneralService/ICustomerService.cs` | Gains new members |
| ViewModels | `V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/GeneralViewModel/` | `CustomerVM`, `CustomerIndirectVM`, `ContactPersonVM` |
| Entities | `V.SMART/V.SMART.Shared/Data/Master/General_Module/` | `Customer.cs`, `CustomerIndirect.cs`, `ContactPerson.cs` |
| EF configuration | `V.SMART/V.SMART.Shared/Data/Configuration/MasterConfiguration/CustomerConfiguration.cs` | Indexes; read-only for this task |
| Mapping | `V.SMART/V.SMART.Shared/Mappings/MasterMapping/GeneralMasterProfile/CustomerMapping.cs` | Has a defect this task must address — see BR-CUST-017 |
| Repository | `V.SMART/V.SMART.Shared/Repository/MasterRepository/Generals/CustomerRepository.cs` | `GetCustomerWithAllRelatedDataAsync` is the eager-load entry point |
| Generic repository | `V.SMART/V.SMART.Shared/Repository/Repository.cs:185-224` | `ExistsByNameAsync` — the duplicate-name check |
| Tests | `tests/V.SMART.Shared.Tests/` | **TO BE CREATED by M0-12-01** |
| Triage artefact | `docs/kb/business-rules/customer-master-rules.md` | **TO BE CREATED by this task** |

## Existing Behavior to Preserve

Every claim below was verified against the working tree on 2026-08-12. **Re-verify every
line number before relying on it** — the working tree had 37 uncommitted entries at plan
time and lines may have moved.

### Measured `@code` density (Confirmed)

| File | Total lines | `@code` opens at | `@code` block | Share |
|---|---|---|---|---|
| `Customer_Pages/CustomerList.razor` | 984 | `:394` | **591 lines** (`:394-984`) | 60.1 % |
| `Customer_Pages/CustomerUpsert.razor` | 1,309 | `:678` | **632 lines** (`:678-1309`) | 48.3 % |
| **Combined** | **2,293** | — | **1,223 lines** | **53.3 %** |

Consistent with R-06's system-wide 57 % (KB-080 §1). `CustomerList.razor` declares
`@page "/customer"` (`:1`); `CustomerUpsert.razor` declares **two** routes,
`@page "/customer/create"` (`:1`) and `@page "/customer/update/{CustId:int}"` (`:2`).

### The rules to allocate

The ids below are the **proposed allocation**. The executing session confirms or adjusts
them, but must not renumber anything already in KB-030.

| BR id | Statement | Evidence | Confidence |
|---|---|---|---|
| **BR-CUST-001** | Customer name must be unique. Checked via the generic `ExistsByNameAsync("CustName", trimmedName, "CustId", excludeId)`; on a hit the save aborts with "Customer name already exists." and **no rows are written**. `excludeId` is `null` for create and `CustId` for update. | `CustomerUpsert.razor:960-966`; `Repository/Repository.cs:185-224` | Confirmed |
| **BR-CUST-002** | PAN is **derived** from GST: when GST is exactly 15 characters, `PANNo = GSTNo.Substring(2, 10)`; otherwise PAN is cleared to `string.Empty`. Applies to the customer **and** to every consignee (`CustomerIndirect`) row independently. | `CustomerUpsert.razor:729-736` (consignee), `:753-761` (customer) | Confirmed |
| **BR-CUST-003** | The customer's GST is upper-cased and trimmed on entry; the consignee's GST is **trimmed only, not upper-cased**. | `CustomerUpsert.razor:750` vs `:727` | Confirmed — the asymmetry is in the code; whether it is intended is **Unknown** |
| **BR-CUST-004** | Business type constrains customer type (`SupTyp`): `Local`/`InterState` → `{B2B, SEZWP, SEZWOP}`, defaulting to `B2B`; `Imports`/`Exports` → `{SEZWP, SEZWOP, EXPWP, EXPWOP}`, defaulting to `SEZWP`; any other value sets `SupTyp = null`. The default is applied only when the current value is empty or not in the allowed set. | `CustomerUpsert.razor:837-878` | Confirmed |
| **BR-CUST-005** | Selecting `Imports` or `Exports` forces `GSTNo = "URP"`, selects the state with `StateCode == 99` (the "URP" state) and clears PAN. Switching away clears GST, PAN and state **only if** the current GST is blank, shorter than 15 characters, or literally `URP`. | `CustomerUpsert.razor:1229-1247` | Confirmed |
| **BR-CUST-006** | GST validation branches on business type. `Imports`/`Exports`: GST must equal `URP` (case-insensitive). Otherwise: GST is required, must be exactly 15 characters, and must match `^(URP\|[0-9]{2}[A-Z]{5}[0-9]{4}[A-Z]{1}[1-9A-Z]{1}Z[0-9A-Z]{1})$`. | `CustomerUpsert.razor:1140-1166` | Confirmed |
| **BR-CUST-007** | PAN validation: for `Imports`/`Exports` PAN is optional but, if present, must match `^[A-Z]{5}[0-9]{4}[A-Z]{1}$`; otherwise PAN is **required** and must match the same pattern. | `CustomerUpsert.razor:1147-1151`, `:1168-1175` | Confirmed |
| **BR-CUST-008** | The selected state must exist in the `State` list; `StateName` is **denormalised onto the customer** from the matched `State` row, both on change and again during validation. A `StateId` of 0 or an unmatched code is a validation error. | `CustomerUpsert.razor:770-799`, `:1178-1186` | Confirmed |
| **BR-CUST-009** | Every consignee row **with a non-blank `AltCustName`** must carry a GST of exactly 15 characters matching the GST pattern, and a PAN matching the PAN pattern. Error messages are prefixed with the consignee name. | `CustomerUpsert.razor:1188-1211` | Confirmed |
| **BR-CUST-010** | Opening balance drives two derived fields: if `OpenBal` has a value, `OpenBalPndg = OpenBal` and `OpenBalDate = DateTime.Now`; otherwise both are set to `null`. Applied on **every** save, create and update alike — so re-saving an existing customer **resets its pending balance to the opening balance and re-stamps the date**. | `CustomerUpsert.razor:981-990` | Confirmed. That an edit silently resets `OpenBalPndg` is **Inferred** to be a defect; raise it, do not fix it here |
| **BR-CUST-011** | Create stamps `CreatedBy` (current username) and `CreatedDate = DateTime.Now`; update stamps `ModifiedBy`/`ModifiedDate`. Create never sets Modified\*; update never re-sets Created\*. | `CustomerUpsert.razor:994-995`, `:1032-1033` | Confirmed |
| **BR-CUST-012** | Consignee rows with a blank `AltCustName`, and contact rows with a blank `ContactPersonName`, are **silently discarded** — never inserted, never updated, and (on update) deleted because they fall out of the retained-id set. No warning is shown. | `CustomerUpsert.razor:1000`, `:1007`, `:1047`, `:1073` | Confirmed |
| **BR-CUST-013** | On update, child collections are synchronised by id-set difference: rows present in the originally loaded `Customer.CustomerIndirects` / `Customer.ContactPersons` whose id is absent from the current editor list are **deleted**; rows with id 0 are inserted; the rest are updated. | `CustomerUpsert.razor:1029-1030`, `:1037-1086` | Confirmed |
| **BR-CUST-014** | Delete is refused when: the customer does not exist ("Customer not found or already removed."); **or** `ForeignKeyUsageChecker.GetUsageTableAsync<Customer>(custId)` reports a referencing table ("Cannot delete Customer '{name}' because it is used in {table} Screen."); **or** the customer is inactive ("Customer is inactive. You cannot delete this record."). Messages are product UX and must survive verbatim. | `CustomerService.cs:142-168`; `Services/ForeignKeyUsageChecker.cs:21` | Confirmed |
| **BR-CUST-015** | Delete runs inside `BeginTransactionAsync`, deletes only the `Customer` row, commits, then writes a `LogUserAction` entry with screen `"Customer List"`. Child rows are removed by the database's cascade, not by the service. | `CustomerService.cs:171-212` | Confirmed for the service body; **Unknown** whether the FK cascade is `Cascade` or `Restrict` — the executing session must verify against `Migrations/` or the live schema and record the answer |
| **BR-CUST-016** | List filtering supports exactly five keys — `Customer` (LIKE on `CustName`), `CreatedBy` (LIKE), `FromDate` (`CreatedDate >= date`), `ToDate` (`CreatedDate <= end-of-day`), `Status` (`Active` → `!Inactive`, `In Active` → `Inactive`). Unknown keys are ignored silently. Ordering is fixed: `OrderByDescending(CustId)`. | `CustomerService.cs:81-139`, `:65` | Confirmed |
| **BR-CUST-017** | **Defect, negative finding.** `CustomerVM` declares `CustomerIndirectVMs` and `ContactPersonVMs` (`CustomerVM.cs:134-135`) but `CustomerMapping` maps neither — entity→VM only customises `CurrName`, and VM→entity explicitly **ignores** `CustomerIndirects`, `ContactPersons` and `ItemSubs`. Name mismatch (`CustomerIndirects` vs `CustomerIndirectVMs`) means AutoMapper will not match them by convention either. **`CustomerVM` therefore cannot round-trip child collections today.** Grepping `docs/` and `V.SMART/` for `CustomerIndirectVMs`/`ContactPersonVMs` returns hits **only** in `CustomerVM.cs` — no producer, no consumer. | `Mappings/MasterMapping/GeneralMasterProfile/CustomerMapping.cs:16-26`; `CustomerVM.cs:134-135`; negative grep | Confirmed |

### Negative findings — record these so nobody re-derives them

- **`ICustomerService` has no write path.** Its four members are
  `CanDeleteCustomerAsync`, `SearchWithDynamicFilterAsync`, `DeleteCustomerByCustIdAsync`,
  `GetCustomerListAsync` (`ICustomerService.cs:14-18`). There is **no** `UpsertCustomerAsync`,
  **no** `GetCustomerByIdAsync`, **no** `CreateAsync`/`UpdateAsync`. Confirmed.
- **`CustomerUpsert.razor` injects `IUnitOfWork` directly** (`:13`) and performs its own
  transaction (`:957`). It is a Razor page acting as a service. Confirmed.
- **`CustomerRepository` is almost entirely commented out.** Of its 266 lines, one live
  method survives — `GetCustomerWithAllRelatedDataAsync` (`:31-39`, eager-loading `State`,
  `Currency`, `CustomerIndirects`, `ContactPersons`). Lines `:26-29`, `:41-58`, `:62-185`
  and `:187-262` are commented-out code including a dead `UpdateAsync` override and a dead
  `InstantSearchCustomersAsync`. Do **not** revive them; do **not** delete them in this
  task either (that is unrelated cleanup). Confirmed.
- **`ApplicationDbContext` applies exactly three explicit `IEntityTypeConfiguration`s** —
  `UserConfiguration` (`:430`), `ItemConfiguration` (`:432`), `CustomerConfiguration`
  (`:434`); `grep -c ApplyConfiguration` returns 3. Customer is one of only three entities
  the team hand-tuned, which is itself evidence that Customer is a high-traffic master.
  `CustomerConfiguration` declares the key plus eight indexes including a composite
  `(Inactive, CustName)` and a covering index on `CustName`
  (`CustomerConfiguration.cs:16-52`). Confirmed.
- **`CustomerList.razor`'s `@code` contains almost no business logic.** It is paging
  (`:664-771`), modal state (`:863-919`), column definitions (`:526-621`), column
  preferences (`:623-657`), filter plumbing (`:926-980`) and a delete flow that simply
  calls the service (`:798-858`). This is the *presentation + data loading* case and is the
  contrast that makes the triage meaningful. Confirmed.
- **Services carry no authorization.** Consistent with BR-AUTH-002 — enforcement is
  `BaseUserRightsComponent` in the UI only. Both pages set
  `protected override string ScreenName => "Customer"`
  (`CustomerList.razor:398`, `CustomerUpsert.razor:681`), matching the seeded
  `Screens { Id = 15, ScreenCode = 15, ScreenName = "Customer" }`
  (`ApplicationDbContext.cs:1166`). Do not add permission checks to the service.

### Repository conventions this extraction must follow (Confirmed)

- `IUnitOfWork` exposes **189** typed repository properties and `BeginTransactionAsync()`
  (`Repository/IRepository/IUnitOfWork.cs:85`, `:135-137`); there are **302**
  `BeginTransaction*` call sites solution-wide.
- Services take and return **ViewModels**, not entities
  (`CustomerService.cs:43`, `:215` return `CustomerVM`).
- Method naming: `SearchWithDynamicFilterAsync`, `Get…ByIdAsync`, `Upsert…Async`,
  `CanDelete…Async`.
- The result-tuple idiom for a write is `(bool Success, string Message, TVm? Entity)`, as
  used by `ICurrencyService.CreateAsync`/`UpdateAsync`
  (`ICurrencyService.cs:15-16`). Reuse it.

## Target Result

1. `docs/kb/business-rules/customer-master-rules.md` exists, with proper KB frontmatter,
   containing (a) the per-member `@code` triage table for both pages and (b) the
   `BR-CUST-*` rules with evidence and confidence.
2. `ICustomerService` gains, at minimum:

   ```csharp
   Task<CustomerVM?> GetCustomerByIdAsync(int custId);
   Task<(bool Success, string Message, CustomerVM? Customer)> UpsertCustomerAsync(CustomerVM vm);
   Task<IReadOnlyList<string>> ValidateCustomerAsync(CustomerVM vm);
   CustomerVM ApplyBusinessTypeDefaults(CustomerVM vm);   // BR-CUST-004, BR-CUST-005
   string? DerivePanFromGst(string? gstNo);               // BR-CUST-002
   ```

   The exact shape is the executing session's call, but it must satisfy: one call performs
   a whole save (ADR-002 §3 — the client never orchestrates a multi-step business
   operation); validation is callable independently so the controller can return a field
   -keyed 400; and derivation helpers are callable independently so the React form can ask
   the server rather than reimplementing regexes in TypeScript.
3. `CustomerVM` round-trips its children — BR-CUST-017 is fixed by mapping
   `CustomerIndirects ↔ CustomerIndirectVMs` and `ContactPersons ↔ ContactPersonVMs` in
   `CustomerMapping`. **This is the one behaviour change this task authorises**, and only
   because the current state is a dead-end: the API cannot accept a customer with
   consignees through a VM that drops them. It is behaviour-*adding*, not
   behaviour-*altering* — nothing reads those properties today (Confirmed, BR-CUST-017).
4. `CustomerUpsert.razor`'s `@code` block calls `customerService.UpsertCustomerAsync(...)`
   and no longer contains: the transaction, `ExistsByNameAsync`, the child-collection
   diff, the audit stamping, `ValidateCustomer`, or the `OpenBalPndg` derivation. It keeps:
   parameters, tab state, breadcrumbs, list bindings, `StateHasChanged` plumbing, toast
   calls, navigation, and the `On…Changed` handlers **as thin wrappers that call the
   service's derivation helpers**.
5. `@code` in `CustomerUpsert.razor` is materially smaller. Record the before/after line
   count in the triage document; a reduction of at least 250 lines is expected from
   `:954-1214` alone.
6. Characterisation tests in `tests/V.SMART.Shared.Tests/` pin BR-CUST-002 and
   BR-CUST-004 through BR-CUST-013.
7. `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` still reports **0 errors**.

## Implementation Steps

1. **Read the knowledge base** listed under *Required Existing Knowledge*, then
   `docs/kb/investigation-registry.md`. Confirm INV-024 is still *Scheduled* and that no
   `BR-CUST-*` id exists. Record what you confirmed.
2. **Re-verify every line number** in *Existing Behavior to Preserve*. The counts were
   taken on 2026-08-12 against a tree with uncommitted changes. If a number has moved,
   correct it in this file **and** carry the corrected number into the triage document.
   Re-measure `@code` size with, for each page,
   `grep -n '^@code' <file>` and `awk 'END{print NR}' <file>`.
3. **Produce the triage.** Create `docs/kb/business-rules/customer-master-rules.md`. For
   **every** member of both `@code` blocks — fields, properties, methods, lambdas — record
   a row: `Member · Lines · Bucket · Disposition · Note`. Buckets are exactly:
   - `Presentation` — UI state, tab state, modal state, breadcrumbs, column metadata,
     toasts, navigation. **Disposition: discard** (rebuilt natively in React).
   - `Data loading` — reads that exist only to fill the screen. **Disposition: becomes an
     API call.**
   - `Business logic` — anything that decides, derives, validates or persists.
     **Disposition: extract to `CustomerService`.**
   A member that spans buckets is split into rows, not rounded to one bucket. Justify
   every `Presentation` verdict on a method longer than 20 lines — that is where
   misclassification hides.
4. **Record the rules.** In the same document, write each `BR-CUST-nnn` in KB-030's format
   (Statement · Evidence · Confidence · Disposition · Migration note). Allocate ids
   sequentially from `BR-CUST-001`. Do not renumber existing rules in KB-030.
5. **Write characterisation tests first**, in `tests/V.SMART.Shared.Tests/`, against the
   **current** behaviour, before moving any code. Cover at minimum: PAN derivation at
   lengths 14/15/16 (BR-CUST-002); each business-type → `SupTyp` set and default
   (BR-CUST-004); the Imports/Exports forcing rule and the asymmetric clear-on-switch-back
   (BR-CUST-005); each GST and PAN validation branch (BR-CUST-006, BR-CUST-007);
   consignee validation including the blank-name skip (BR-CUST-009, BR-CUST-012); the
   `OpenBal` derivation including the reset-on-edit (BR-CUST-010); the child id-set diff
   for insert/update/delete (BR-CUST-013). These tests must **pass against the pre-extraction
   code** where the logic is reachable, and must still pass afterwards. Where logic is only
   reachable inside the Razor page, note it and write the test against the extracted method
   immediately after step 6 — flag this in the triage document as a limitation.
6. **Extract, in this order**, keeping each step compiling:
   1. Pure derivation helpers — `DerivePanFromGst`, `ApplyBusinessTypeDefaults`. No
      database access. Move the regexes and the `SupTyp` lists as **constants on the
      service**, not duplicated literals.
   2. `ValidateCustomerAsync` — the whole of `ValidateCustomer` (`:1127-1214`) plus the
      duplicate-name check (`:960`). It must return the existing message strings
      **verbatim**; they are product UX (ADR-002 §4). Prefer returning them keyed by field
      so M2-D02-02 can produce a `ProblemDetails.errors` dictionary, but preserve the exact
      text.
   3. `GetCustomerByIdAsync` — wraps `GetCustomerWithAllRelatedDataAsync` and maps to
      `CustomerVM`. Requires the BR-CUST-017 mapping fix.
   4. `UpsertCustomerAsync` — the whole of `UpsertCustomer` (`:954-1125`) **minus** the
      toast calls, the `NavigationGuard.clear` JS interop, and the navigation. Those three
      are presentation and stay in the page. Keep the transaction, the per-row
      `SaveAsync` sequence, the audit `LogUserAction` (screen `"Customer"`), and the
      rollback-on-exception. **Do not "improve" the N+1 `SaveAsync` pattern** — that is a
      behaviour-risking optimisation and belongs to a separate, approved task.
7. **Fix BR-CUST-017** in `CustomerMapping`: map both child collections in both directions.
   Verify no other profile already maps them (grep `CustomerIndirectVMs` — expected: hits
   only in `CustomerVM.cs`).
8. **Rewire `CustomerUpsert.razor`.** `@code` calls the service. Remove the
   `@inject IUnitOfWork _unitOfWork` line (`:13`) **only if** nothing else in the page uses
   it — `LoadStates` (`:884`) and `LoadCurrencies` (`:900`) currently do. Either move those
   two reads onto the service too (preferred — they become reference-data endpoints in
   M2-B09) or keep the injection and say so explicitly in the triage document. Do not leave
   the decision implicit.
9. **Do not touch `CustomerList.razor`** beyond what the triage requires. Its `@code` is
   already service-backed; triage it, document it, change nothing.
10. **Build**: `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` → 0 errors. Confirm
    the warning count has not risen above the 6,695 baseline.
11. **Validate against the running Blazor app** — this is the `<W>-04` discipline pulled
    into this task because M2-D has no separate verify step. Against a **non-production**
    tenant database, exercise through the Blazor UI: create a Local customer with two
    consignees and two contacts; edit it, removing one consignee and adding another; enter
    a blank-named consignee row and confirm it is still silently dropped; switch business
    type to Exports and confirm GST becomes `URP`, state becomes 99 and PAN clears;
    attempt a duplicate name and confirm the same error text; attempt to delete a customer
    referenced elsewhere and confirm the same refusal message. After each, inspect the
    persisted rows. Record the evidence in the triage document.
12. **Update KB-030**: append a `## Customer Master rules` section linking to the new
    document, and add the `BR-CUST-*` ids to its frontmatter `business_rules` list.
13. **Update KB-003**: amend the INV-024 row to *Partial*, noting Customer Master is the
    first module triaged and linking to the new document.
14. **Rewrite this file's frontmatter** `business_rules:` with the ids actually allocated,
    set `status: Completed`, bump `last_verified`.
15. **Review the full diff**, then commit on the task branch. Do not merge.

## Files Expected to Change

- `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/IBusinessService/IMasterServices/IGeneralService/ICustomerService.cs`
- `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/MasterService/GeneralService/CustomerService.cs`
- `V.SMART/V.SMART.Shared/Mappings/MasterMapping/GeneralMasterProfile/CustomerMapping.cs`
- `V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/CustomerUpsert.razor`
- `docs/kb/business-rules/business-rule-inventory.md` (KB-030)
- `docs/kb/investigation-registry.md` (KB-003)
- `docs/kb/execution/tasks/M2-D02-01.md` (this file)

## Files Expected to Be Created

- `docs/kb/business-rules/customer-master-rules.md` — **TO BE CREATED**
- Test files under `tests/V.SMART.Shared.Tests/` — that directory is **TO BE CREATED by
  M0-12-01**, not by this task. If it does not exist when this task runs, the task is
  **Blocked**, not "do it without tests".

## Files That Must Not Change

- `V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/CustomerList.razor` —
  triage only.
- `V.SMART/V.SMART.Shared/Data/Master/General_Module/*.cs` — no entity changes. No schema
  change is authorised by this task.
- `V.SMART/V.SMART.Shared/Data/Configuration/MasterConfiguration/CustomerConfiguration.cs`
- `V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs`
- `V.SMART/V.SMART.Shared/Migrations/**` — no migration.
- `V.SMART/V.SMART.Api/**` — no controller, no `Program.cs` DI edit. That is M2-D02-02 and
  M2-B07.
- `frontend/**` — no React, no Angular.
- Any other module's service, page or ViewModel.
- The commented-out blocks in `CustomerRepository.cs` — leave them exactly as they are.

## API Changes

| Aspect | Value |
|---|---|
| Method | n/a — this task creates no endpoint |
| Route | n/a |
| Request | n/a |
| Response | n/a |
| Authentication | n/a |
| Authorization | n/a — services are authorization-free by design (BR-AUTH-002, ADR-004 §1) |
| Tenant context | n/a at the API layer. The service resolves its `ApplicationDbContext` through the existing `ITenantProvider`/`ITenantDbContextFactory` chain, unchanged. |
| Error model | n/a. But the **message strings** this task preserves are what M2-D02-02 will surface: validation failures → 400 `errors`; delete refusals → 409 with the service message verbatim (ADR-002 §4). |
| Paging | n/a |
| Filtering | n/a |
| Sorting | n/a |
| Idempotency | n/a |

## React Changes

| Aspect | Value |
|---|---|
| Route | n/a |
| Component | n/a |
| API integration | n/a |
| State | n/a |
| Validation | n/a — **but** the rules recorded here are the input to M2-D02-03's Zod schema. The GST and PAN regexes and the `SupTyp` sets must be transcribed there from this task's document, not re-read from the Razor page. |
| Permissions | n/a |
| Loading state | n/a |
| Empty state | n/a |
| Error state | n/a |
| Accessibility | n/a |
| Responsive behaviour | n/a |

**No React code is written in this task.** `frontend/nexgen-web/` does not exist yet
(TO BE CREATED by M2-C01); `frontend/` currently contains only `vsmart-erp`, the Angular
pilot being archived by M2-C11.

## Business Logic

**Extracted.**

Business logic is moved from `CustomerUpsert.razor`'s `@code` block into
`ICustomerService`/`CustomerService` **with no change in observable behaviour**. Every
message string, every regex, every branch and every ordering is preserved verbatim,
including the three items identified as probable defects (BR-CUST-003's GST-casing
asymmetry, BR-CUST-010's opening-balance reset on edit, BR-CUST-012's silent discard of
blank-named child rows). Those are **recorded and raised as open questions, not fixed** —
fixing them here would make the parity test M2-D03 fail for the right reason at the wrong
time, and would conflate extraction with change.

One exception, explicitly authorised: **BR-CUST-017's mapping gap is fixed** (child
collections become mapped in `CustomerMapping`). This adds behaviour to a code path that
has no current consumer, so it cannot alter existing behaviour; without it there is no VM
capable of carrying a customer's consignees and contacts across the API boundary, and
M2-D02-02 would be blocked.

## Testing

| Level | What | Where | Command |
|---|---|---|---|
| Characterisation (unit) | BR-CUST-002, -004, -005, -006, -007, -009, -010, -012, -013 — written **before** extraction, passing before and after | `tests/V.SMART.Shared.Tests/` (TO BE CREATED by M0-12-01) | `dotnet test tests/V.SMART.Shared.Tests/` — command established by M0-12-01 |
| Unit | `ValidateCustomerAsync` returns the exact legacy message strings for each failure branch | same | same |
| Unit | `UpsertCustomerAsync` child-collection diff: insert / update / delete / blank-name discard | same | same |
| Integration | Round-trip `GetCustomerByIdAsync` → `UpsertCustomerAsync` preserves consignees and contacts (proves the BR-CUST-017 fix) | same | same |
| Manual, against the running Blazor app | The eleven scenarios in implementation step 11, verified by inspecting persisted rows | tenant database (non-production) | n/a — recorded as evidence in the triage document |

No API tests and no component tests in this task — they belong to M2-D02-02 and M2-D02-03.

## Acceptance Criteria

- [ ] `docs/kb/business-rules/customer-master-rules.md` exists with valid KB frontmatter
      (`doc_id`, `title`, `module`, `source_files`, `entities`, `database_tables`,
      `business_rules`, `status`, `confidence`, `last_verified`, `dependencies`).
- [ ] That document contains a triage row for **every** member of both `@code` blocks,
      each assigned exactly one of `Presentation` / `Data loading` / `Business logic`.
- [ ] The triage document records the measured `@code` line counts **before and after**
      for `CustomerUpsert.razor`, and the before count for `CustomerList.razor`.
- [ ] Every `BR-CUST-nnn` allocated carries a statement, `file:line` evidence, a
      `Confirmed`/`Inferred`/`Unknown` rating, a disposition and a migration note.
- [ ] KB-030 links to the new document and lists the new ids in its frontmatter.
- [ ] No `BR-` id already present in KB-030 has been renumbered or reused.
- [ ] `ICustomerService` exposes a single-call save and an independently callable
      validation method.
- [ ] `CustomerUpsert.razor`'s `@code` contains **no** `BeginTransactionAsync`, **no**
      `ExistsByNameAsync`, **no** `_unitOfWork.Customers.CreateAsync/UpdateAsync`, **no**
      `_unitOfWork.CustomerIndirects.*`, **no** `_unitOfWork.ContactPersons.*` and **no**
      `Regex.IsMatch` — verified by grep.
- [ ] Every message string in `ValidateCustomer` (`CustomerUpsert.razor:1127-1214` at plan
      time) appears **byte-identical** in the extracted service — verified by grep, per
      string.
- [ ] `CustomerMapping` maps `CustomerIndirects ↔ CustomerIndirectVMs` and
      `ContactPersons ↔ ContactPersonVMs` in both directions.
- [ ] `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` reports **0 errors** and no
      more than 6,695 warnings.
- [ ] `dotnet test tests/V.SMART.Shared.Tests/` passes, including at least one test per
      rule listed in the *Testing* table.
- [ ] The eleven manual Blazor scenarios in step 11 were executed and their persisted-row
      evidence is recorded in the triage document.
- [ ] `V.SMART/V.SMART.Api/` is unchanged — `git diff --stat V.SMART/V.SMART.Api/` is empty.
- [ ] `frontend/` is unchanged — `git diff --stat frontend/` is empty.
- [ ] No file under `V.SMART/V.SMART.Shared/Migrations/` was added or modified.
- [ ] KB-003's INV-024 row is amended to *Partial* and names Customer Master.
- [ ] Any defect found but not fixed is recorded in `docs/kb/open-questions.md` with a new
      `Q-xx` id, and cross-referenced from the rule that describes it.

## Verification Commands

```bash
dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj
```

```bash
dotnet build V.SMART/V.SMART.Web/V.SMART.Web.csproj
```

```bash
dotnet test tests/V.SMART.Shared.Tests/
```

```bash
git status --porcelain
```

```bash
git diff --stat V.SMART/V.SMART.Api/ frontend/ V.SMART/V.SMART.Shared/Migrations/
```

```bash
grep -n "BeginTransactionAsync\|ExistsByNameAsync\|Regex.IsMatch\|_unitOfWork.Customers\|_unitOfWork.CustomerIndirects\|_unitOfWork.ContactPersons" "V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/CustomerUpsert.razor"
```

```bash
grep -n "^@code" "V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/CustomerUpsert.razor"
```

```bash
awk 'END{print FILENAME": "NR}' "V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/CustomerUpsert.razor"
```

```bash
grep -rn "CustomerIndirectVMs\|ContactPersonVMs" --include=*.cs V.SMART/
```

> `dotnet test tests/V.SMART.Shared.Tests/` is valid **only because M0-12-01 is a
> prerequisite**. As of 2026-08-12 there is no test project in the solution (INV-023,
> Confirmed) and `dotnet test` finds nothing. If M0-12-01 has not landed, stop and report
> Blocked.
>
> `dotnet build V.SMART/V.SMART.Web/V.SMART.Web.csproj` is listed in KB-083's verified
> table but its result is **not yet measured**. Record the result; do not treat an
> unexpected number as a failure of this task without investigating.

## Documentation Updates

| Document | Change | Frontmatter |
|---|---|---|
| `docs/kb/business-rules/customer-master-rules.md` | **Created.** The triage table + the `BR-CUST-*` rules + the manual-validation evidence. | full KB frontmatter; `status: complete`; `confidence: mixed`; `last_verified` today |
| `docs/kb/business-rules/business-rule-inventory.md` (KB-030) | Add a `## Customer Master rules` section linking to the new document; append the new ids. | add ids to `business_rules`; add the Customer paths to `source_files`; bump `last_verified` |
| `docs/kb/investigation-registry.md` (KB-003) | Amend the **INV-024** row: status *Partial*, gap "all modules except Customer Master", doc link to the new document. | bump `last_verified` |
| `docs/kb/open-questions.md` (KB-004) | Add a `Q-xx` for each defect found and deliberately not fixed — at minimum the GST-casing asymmetry (BR-CUST-003), the opening-balance reset on edit (BR-CUST-010), and the silent discard of blank-named child rows (BR-CUST-012). | bump `last_verified` |
| `docs/kb/execution/tasks/M2-D02-01.md` (this file) | `status: Completed`; fill `business_rules` with the allocated ids. | bump `last_verified` |
| `docs/kb/execution/task-tracker.md` (KB-081) | Mark M2-D02-01 Completed — **only if the file exists.** It is referenced by KB-080 but did not exist on 2026-08-12. Do not create it as a side effect. | n/a |

## Investigation Registry Updates

**Yes — one row amended, no new row.** INV-024 moves from *Scheduled* to **Partial**, with
the gap stated as "every module other than Customer Master". Customer Master is INV-024's
first instance and its recorded methodology.

Record these negative results explicitly so no later session repeats them:

- "Grepped `docs/kb/` for `BR-CUST` before starting — no hits. The `BR-CUST-*` series was
  unallocated."
- "Grepped `V.SMART/` for `CustomerIndirectVMs` and `ContactPersonVMs` — hits only in
  `CustomerVM.cs:134-135`. No producer, no consumer."
- "`ICustomerService` had no write method and no get-by-id before this task."
- "`CustomerRepository.cs` is 266 lines of which one method is live."

## Git Strategy

| | |
|---|---|
| **Branch** | `migration/M2-D02-01-customer-code-triage-extraction` |
| **Commit** | `M2-D02-01: Triage Customer @code and extract business logic into CustomerService` |
| **Expected changed files** | `ICustomerService.cs`, `CustomerService.cs`, `CustomerMapping.cs`, `CustomerUpsert.razor`, new files under `tests/V.SMART.Shared.Tests/`, `docs/kb/business-rules/customer-master-rules.md` (new), `docs/kb/business-rules/business-rule-inventory.md`, `docs/kb/investigation-registry.md`, `docs/kb/open-questions.md`, `docs/kb/execution/tasks/M2-D02-01.md` |
| **Rollback** | Revert the branch's merge commit. The change is code-movement plus documentation; no schema change, no migration, no data change, so a revert restores the previous behaviour exactly. The characterisation tests are the safety net: if a revert is needed, they should already have caught the reason. |

Do not merge. Do not push to `master`. Leave the branch for review.

## Fresh-Session Execution Prompt

Copy everything in the block below into a new AI session.

```text
============================================================
ERP MIGRATION — TASK EXECUTION PROMPT
============================================================

TASK ID:
M2-D02-01

TASK NAME:
Customer Master - @code triage + business-logic extraction

ROLE
You are an engineer working on the V.SMART / NexGen ERP
modernization project. You are executing exactly one task.

PROJECT OBJECTIVE
We are replacing the existing Blazor Server frontend with a new
React frontend while preserving the existing ERP business
behaviour, business services, database behaviour and business
rules wherever possible. The backend is extended, never
rewritten. Business logic currently trapped in Razor @code is
extracted into server-side services before any React screen
replaces it.

CURRENT ARCHITECTURE
Repository root: C:\Kumar\NexGen-ERP---2025-master\NexGen-ERP---2025-master
(this is the git repository root; the parent directory is not
part of the project)

  V.SMART/V.SMART.Shared   .NET 9 class library — ALL domain code:
                           196 EF entity sets, 285 business services,
                           ~190 repositories + UnitOfWork, 274 ViewModels,
                           333 Razor pages, 440 routes
  V.SMART/V.SMART.Web      Blazor Server host (live UI, stays running)
  V.SMART/V.SMART.Api      ASP.NET Core Web API (.NET 9) — the React
                           backend; currently 2 controllers / 6 endpoints
  V.SMART/V.SMART          .NET MAUI Blazor Hybrid host

SQL Server + EF Core 9, code-first, database-per-tenant.

Task-specific architecture context:
The domain layer's conventions, all Confirmed on 2026-08-12:
  - IUnitOfWork exposes 189 typed repository properties plus
    BeginTransactionAsync()
    (V.SMART/V.SMART.Shared/Repository/IRepository/IUnitOfWork.cs:85
    and :135-137). There are 302 BeginTransaction* call sites
    solution-wide.
  - Business services take and return ViewModels, never entities.
  - Service method naming convention:
    SearchWithDynamicFilterAsync, Get...ByIdAsync,
    Upsert...Async, CanDelete...Async.
  - The write-result tuple idiom is
    (bool Success, string Message, TVm? Entity), as used by
    ICurrencyService.CreateAsync / UpdateAsync.
  - Multi-tenancy is database-per-tenant. A service reaches its
    ApplicationDbContext through ITenantProvider /
    ITenantDbContextFactory. Do not change any of that.
  - Business services contain NO permission checks. That is
    intentional (BR-AUTH-002 / ADR-004): enforcement is being
    moved to an API authorization filter by M2-A01-02, not into
    services. Do not add permission checks to CustomerService.

SOURCE OF TRUTH
The project knowledge base is at docs/kb/. Read it before
investigating the repository. Use docs/kb/INDEX.md for
question-to-document routing.

Required reading for this task:
  KB-083  docs/kb/execution/prompt-template.md
          (generation rules + the VERIFIED COMMANDS table)
  KB-080  docs/kb/execution/README.md
          (section 9 = M2; section 10 = the 14-step module
           pattern this task instantiates)
  KB-002  docs/kb/source-of-truth-rules.md
  KB-003  docs/kb/investigation-registry.md
  KB-030  docs/kb/business-rules/business-rule-inventory.md
          (rule format and the BR-<AREA>-<nnn> id convention)
  KB-011  docs/kb/architecture/backend-architecture.md
  KB-020  docs/kb/modules/module-inventory.md
  ADR-002 docs/kb/decisions/ADR-002-rest-api-layer.md
          (sections 2, 3 and 4)
  ADR-004 docs/kb/decisions/ADR-004-server-side-authorization.md
  KB-005  docs/kb/INDEX.md
  This task's full specification:
          docs/kb/execution/tasks/M2-D02-01.md

Authority order when sources conflict:
  1. Current source code
  2. Database schema / EF migrations (for storage)
  3. The knowledge base (for interpretation)
  4. Older prose documentation — hypothesis only
docs/ARCHITECTURE.md is superseded and contains known factual
errors. Do not rely on it.

INVESTIGATION REGISTRY
Before any repository investigation, search
docs/kb/investigation-registry.md.

Relevant investigations:
  INV-024  "@code triage per module" — status Scheduled.
           NOTHING has been triaged yet. This task is its FIRST
           instance. Amend the row to Partial when done.
  INV-011  Business rules, cross-module sweep — status Partial.
           Contains NO Customer rule. Confirmed: grepping
           docs/kb/ for "BR-CUST" returns no hits, so the
           BR-CUST-nnn series is unallocated and THIS TASK
           allocates it.
  INV-002  Backend layering, repository/UoW, service conventions
           — Complete. REUSE IT. Do not re-derive the repository
           or UnitOfWork conventions.
  INV-004  Authentication and screen rights — Complete. REUSE IT.
           Customer is ScreenCode 15
           (V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs:1166).
  INV-007  Module inventory and dependency graph — Complete.
           REUSE IT for Customer's module boundary.
  INV-023  Testing and CI — Complete. There was no test project;
           M0-12-01 creates tests/V.SMART.Shared.Tests/. Verify
           it exists before running dotnet test.

Before investigating the repository, search
docs/kb/investigation-registry.md and the relevant knowledge-base
documents via docs/kb/INDEX.md. If an investigation is Complete
and not stale, reuse its findings and cite the doc_id — do not
re-derive them. If it is Partial, investigate only the documented
gap. If it is absent or contradicted by current code,
investigate, then record the finding, its file:line evidence, and
its confidence in the knowledge base so that future sessions do
not repeat this work. Record negative results too — "grepped for
X, found none" is a finding.

CURRENT TASK
Do two things, both mandatory.

(1) TRIAGE. Classify every line of the @code blocks in the two
Customer Master Razor pages into exactly three buckets:
  - Presentation  -> discard (React rebuilds it natively)
  - Data loading  -> becomes an API call
  - Business logic-> EXTRACT to a server-side service
Record the triage as a written document at
docs/kb/business-rules/customer-master-rules.md, with a row per
@code member: Member | Lines | Bucket | Disposition | Note. A
member that spans buckets is split into rows, never rounded into
one bucket. Justify every "Presentation" verdict on a method
longer than 20 lines.

In the same document, record the business rules you found as
BR-CUST-001, BR-CUST-002, ... in KB-030's format
(Statement / Evidence file:line / Confidence / Disposition /
Migration note). Allocate ids sequentially. Never renumber an
existing BR id.

(2) EXTRACT. Move the business-logic bucket out of
CustomerUpsert.razor and into ICustomerService / CustomerService
with NO change in observable behaviour. The Blazor page keeps
working, against the same database, through the extracted
service.

WHY THIS TASK EXISTS
It serves R-06: roughly 184,000 LOC — 57% of 321,661 Razor LOC —
sits inside @code blocks. It is the module-level instance of
INV-024. It is step 02 + step 03 of the 14-step module pattern in
KB-080 section 10.

Most importantly, it is the FIRST real application of migration
principle 3, "extract before rebuild". Whatever this task decides
— how a triage is recorded, what counts as business logic, what
an extracted service method looks like, how the extraction is
proved behaviour-preserving — becomes the template that every M3
and M4 wave copies. Getting the SHAPE right matters more than
getting Customer Master finished quickly. If the pattern turns
out awkward, say so in your final report: it is far cheaper to
change it now than after sixty more modules have copied it.

It also exists because Customer Master cannot be done the way
Currency was. Confirmed on 2026-08-12:
  - ICurrencyService already had GetByIdAsync, CreateAsync and
    UpdateAsync, so CurrencyController wrapped it with ZERO
    service changes.
  - ICustomerService has FOUR methods and NONE of them writes a
    customer. The entire create/update path lives in
    CustomerUpsert.razor's @code block.
Without this task, M2-D02-02 would have to reimplement Customer
write logic inside a controller, which principle 3 forbids.

PREREQUISITES
  M2-D01   Currency end-to-end in React — must be Completed.
  M0-12-01 Test project created at tests/V.SMART.Shared.Tests/
           — must be Completed. If that directory does not
           exist, STOP and report the task as Blocked. Do not
           proceed without a place to put characterisation
           tests.
  M2-B07   AddVSmartDomain() DI extension. Relevant because
           V.SMART/V.SMART.Api/Program.cs:102 registers only
           ICurrencyService. Do NOT add a one-off
           AddScoped<ICustomerService, CustomerService>() line
           to Program.cs — M2-B07 owns that.

CURRENT IMPLEMENTATION
All of the following was verified on 2026-08-12 against a working
tree that had 37 uncommitted entries. RE-VERIFY EVERY LINE NUMBER
before relying on it, and correct this list if anything moved.

MEASURED @code DENSITY (Confirmed):
  Pages/Master_Module_pages/Customer_Pages/CustomerList.razor
    984 lines total; @code opens at :394; @code block is
    :394-984 = 591 lines = 60.1% of the file.
    Routes: @page "/customer" (:1)
  Pages/Master_Module_pages/Customer_Pages/CustomerUpsert.razor
    1309 lines total; @code opens at :678; @code block is
    :678-1309 = 632 lines = 48.3% of the file.
    Routes: @page "/customer/create" (:1) and
            @page "/customer/update/{CustId:int}" (:2)
  Combined: 2293 lines, 1223 of them inside @code = 53.3%.

THE SERVICE HAS NO WRITE PATH (Confirmed):
  V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/
  IBusinessService/IMasterServices/IGeneralService/
  ICustomerService.cs:14-18 declares exactly four members:
    CanDeleteCustomerAsync(int custId)
    SearchWithDynamicFilterAsync(int, int, Dictionary<string,object>?)
    DeleteCustomerByCustIdAsync(int custId)
    GetCustomerListAsync(string topic, DateTime from, DateTime to)
  There is NO UpsertCustomerAsync, NO GetCustomerByIdAsync, NO
  CreateAsync and NO UpdateAsync.

  CustomerService.cs is 279 lines implementing exactly those
  four, plus a nested static CustomerFilterBuilder (:81-139).

THE PAGE IS ACTING AS A SERVICE (Confirmed):
  CustomerUpsert.razor:13 injects IUnitOfWork directly.
  CustomerUpsert.razor:957 opens its own database transaction.
  CustomerUpsert.razor:954-1125  UpsertCustomer()   — the whole
                                 create/update path
  CustomerUpsert.razor:1127-1214 ValidateCustomer() — all
                                 validation

BUSINESS RULES CURRENTLY TRAPPED IN @code (all Confirmed,
file:line as of 2026-08-12). These are the proposed BR-CUST
allocation; confirm or adjust, but do not collide with existing
KB-030 ids:

  BR-CUST-001 Customer name must be unique. Checked with the
    generic ExistsByNameAsync("CustName", trimmed, "CustId",
    excludeId) where excludeId is null on create and CustId on
    update; on a hit the save aborts with the exact message
    "Customer name already exists." and writes nothing.
    Evidence: CustomerUpsert.razor:960-966;
    Repository/Repository.cs:185-224
  BR-CUST-002 PAN is DERIVED from GST: when GST is exactly 15
    characters, PANNo = GSTNo.Substring(2, 10); otherwise PAN is
    cleared to string.Empty. Applies to the customer AND to each
    consignee row independently.
    Evidence: CustomerUpsert.razor:729-736 and :753-761
  BR-CUST-003 The customer's GST is upper-cased AND trimmed on
    entry; the consignee's GST is trimmed ONLY. The asymmetry is
    Confirmed to exist; whether it is intended is Unknown.
    Evidence: CustomerUpsert.razor:750 versus :727
  BR-CUST-004 Business type constrains customer type (SupTyp):
    Local/InterState  -> {B2B, SEZWP, SEZWOP},  default B2B
    Imports/Exports   -> {SEZWP, SEZWOP, EXPWP, EXPWOP},
                         default SEZWP
    anything else     -> SupTyp = null
    The default is applied only when the current value is empty
    or not in the allowed set.
    Evidence: CustomerUpsert.razor:837-878
  BR-CUST-005 Choosing Imports or Exports forces GSTNo = "URP",
    selects the state whose StateCode == 99, and clears PAN.
    Switching AWAY clears GST/PAN/state only if the current GST
    is blank, shorter than 15 chars, or literally "URP".
    Evidence: CustomerUpsert.razor:1229-1247
  BR-CUST-006 GST validation branches on business type.
    Imports/Exports: GST must equal "URP" (case-insensitive).
    Otherwise: GST required, exactly 15 characters, and matching
    ^(URP|[0-9]{2}[A-Z]{5}[0-9]{4}[A-Z]{1}[1-9A-Z]{1}Z[0-9A-Z]{1})$
    Evidence: CustomerUpsert.razor:1140-1166
  BR-CUST-007 PAN validation: for Imports/Exports PAN is
    optional but if present must match ^[A-Z]{5}[0-9]{4}[A-Z]{1}$
    otherwise PAN is REQUIRED and must match the same pattern.
    Evidence: CustomerUpsert.razor:1147-1151 and :1168-1175
  BR-CUST-008 The selected state must exist; StateName is
    DENORMALISED onto the customer from the matched State row,
    both on change and again during validation. StateId of 0 or
    an unmatched code is a validation error.
    Evidence: CustomerUpsert.razor:770-799 and :1178-1186
  BR-CUST-009 Every consignee row with a non-blank AltCustName
    must carry a 15-character GST matching the GST pattern and a
    PAN matching the PAN pattern. Messages are prefixed with the
    consignee name.
    Evidence: CustomerUpsert.razor:1188-1211
  BR-CUST-010 Opening balance drives two derived fields: if
    OpenBal has a value then OpenBalPndg = OpenBal and
    OpenBalDate = DateTime.Now; otherwise both become null.
    Applied on EVERY save, so re-saving an existing customer
    RESETS its pending balance and re-stamps the date. Confirmed
    behaviour; Inferred to be a defect.
    Evidence: CustomerUpsert.razor:981-990
  BR-CUST-011 Create stamps CreatedBy + CreatedDate; update
    stamps ModifiedBy + ModifiedDate. Create never sets
    Modified*; update never re-sets Created*.
    Evidence: CustomerUpsert.razor:994-995 and :1032-1033
  BR-CUST-012 Consignee rows with a blank AltCustName, and
    contact rows with a blank ContactPersonName, are SILENTLY
    discarded — never inserted, never updated, and on update
    deleted because they fall out of the retained-id set. No
    warning is shown.
    Evidence: CustomerUpsert.razor:1000, :1007, :1047, :1073
  BR-CUST-013 On update, child collections are synchronised by
    id-set difference: rows in the originally loaded
    Customer.CustomerIndirects / Customer.ContactPersons whose
    id is absent from the current editor list are DELETED; rows
    with id 0 are inserted; the rest are updated.
    Evidence: CustomerUpsert.razor:1029-1030 and :1037-1086
  BR-CUST-014 Delete is refused when the customer does not
    exist ("Customer not found or already removed."), or when
    ForeignKeyUsageChecker.GetUsageTableAsync<Customer>(custId)
    reports a referencing table ("Cannot delete Customer
    '{name}' because it is used in {table} Screen."), or when
    the customer is inactive ("Customer is inactive. You cannot
    delete this record."). These strings are product UX and must
    survive verbatim.
    Evidence: CustomerService.cs:142-168;
    Services/ForeignKeyUsageChecker.cs:21
  BR-CUST-015 Delete runs inside BeginTransactionAsync, deletes
    only the Customer row, commits, then writes LogUserAction
    with screen "Customer List". Child rows are removed by the
    database cascade, not by the service. Whether the FK is
    Cascade or Restrict is UNKNOWN — verify against
    V.SMART/V.SMART.Shared/Migrations/ or the live schema and
    record the answer.
    Evidence: CustomerService.cs:171-212
  BR-CUST-016 List filtering supports exactly five keys —
    Customer (LIKE on CustName), CreatedBy (LIKE), FromDate
    (CreatedDate >= date), ToDate (CreatedDate <= end of day),
    Status ("Active" -> !Inactive, "In Active" -> Inactive).
    Unknown keys are ignored silently. Ordering is fixed at
    OrderByDescending(CustId).
    Evidence: CustomerService.cs:81-139 and :65
  BR-CUST-017 DEFECT / NEGATIVE FINDING. CustomerVM declares
    CustomerIndirectVMs and ContactPersonVMs
    (CustomerVM.cs:134-135) but CustomerMapping maps NEITHER:
    entity->VM only customises CurrName, and VM->entity
    explicitly Ignores CustomerIndirects, ContactPersons and
    ItemSubs. The names differ (CustomerIndirects vs
    CustomerIndirectVMs) so AutoMapper will not match them by
    convention either. CustomerVM therefore CANNOT round-trip
    child collections today. Grepping V.SMART/ for
    CustomerIndirectVMs / ContactPersonVMs returns hits only in
    CustomerVM.cs — no producer, no consumer.
    Evidence: Mappings/MasterMapping/GeneralMasterProfile/
    CustomerMapping.cs:16-26; CustomerVM.cs:134-135

OTHER CONFIRMED FACTS:
  - CustomerList.razor's @code has almost NO business logic. It
    is paging (:664-771), modal state (:863-919), column
    definitions (:526-621), column preferences (:623-657),
    filter plumbing (:926-980) and a delete flow that just calls
    the service (:798-858). It is the "presentation + data
    loading" case, and it is the contrast that makes this triage
    meaningful.
  - CustomerRepository.cs is 266 lines of which ONE method is
    live: GetCustomerWithAllRelatedDataAsync (:31-39), eager
    loading State, Currency, CustomerIndirects, ContactPersons.
    Lines :26-29, :41-58, :62-185 and :187-262 are commented-out
    code, including a dead UpdateAsync override and a dead
    InstantSearchCustomersAsync. Do NOT revive them. Do NOT
    delete them either — that is unrelated cleanup.
  - ApplicationDbContext applies exactly THREE explicit entity
    configurations: UserConfiguration (:430), ItemConfiguration
    (:432), CustomerConfiguration (:434). grep -c
    ApplyConfiguration returns 3. Customer is one of only three
    entities the team hand-tuned.
    CustomerConfiguration.cs:16-52 declares the key plus eight
    indexes, including a composite (Inactive, CustName) and a
    covering index on CustName.
  - CustomerVM carries DataAnnotations that already drive
    ModelState validation: [Required] + [StringLength(100)] on
    CustName, [StringLength(15)] on GSTNo, [StringLength(10)] on
    PANNo, [RegularExpression(@"^[1-9][0-9]{5}$")] on PinCode,
    [RegularExpression(@"^[a-zA-Z\s]+$")] on Location,
    [EmailAddress] on Email, [Range] on OpenBal and Distance.
    See CustomerVM.cs:20-118. These are SEPARATE from, and
    weaker than, the @code validation above. Both must survive.
  - Business services contain no permission checks anywhere. Do
    not add any.

TARGET IMPLEMENTATION
1. docs/kb/business-rules/customer-master-rules.md exists, with
   proper KB frontmatter, containing (a) the per-member @code
   triage table for BOTH pages and (b) the BR-CUST rules with
   evidence and confidence ratings.
2. ICustomerService gains at minimum:
     Task<CustomerVM?> GetCustomerByIdAsync(int custId);
     Task<(bool Success, string Message, CustomerVM? Customer)>
         UpsertCustomerAsync(CustomerVM vm);
     Task<IReadOnlyList<string>> ValidateCustomerAsync(CustomerVM vm);
     CustomerVM ApplyBusinessTypeDefaults(CustomerVM vm);
     string? DerivePanFromGst(string? gstNo);
   The exact shape is your call, but it must satisfy three
   constraints: ONE call performs a whole save (ADR-002 section
   3 — the client never orchestrates a multi-step business
   operation); validation is callable on its own so the
   controller can return a field-keyed 400; and the derivation
   helpers are callable on their own so the React form can ask
   the server instead of reimplementing regexes in TypeScript.
3. CustomerMapping maps CustomerIndirects <-> CustomerIndirectVMs
   and ContactPersons <-> ContactPersonVMs in BOTH directions,
   fixing BR-CUST-017.
4. CustomerUpsert.razor's @code calls
   customerService.UpsertCustomerAsync(...) and no longer
   contains the transaction, ExistsByNameAsync, the
   child-collection diff, the audit stamping, ValidateCustomer,
   or the OpenBalPndg derivation. It KEEPS parameters, tab
   state, breadcrumbs, list bindings, StateHasChanged plumbing,
   toast calls, navigation, and the On...Changed handlers as
   thin wrappers over the service's derivation helpers.
5. @code in CustomerUpsert.razor is materially smaller. Record
   the before/after line counts in the triage document. A
   reduction of at least 250 lines is expected from :954-1214
   alone.
6. Characterisation tests in tests/V.SMART.Shared.Tests/ pin
   BR-CUST-002 and BR-CUST-004 through BR-CUST-013.
7. dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj still
   reports 0 errors.

RELEVANT FILES
Verified to exist on 2026-08-12:
  V.SMART/V.SMART.Shared/Pages/Master_Module_pages/
    Customer_Pages/CustomerList.razor            (984 lines)
    Customer_Pages/CustomerUpsert.razor          (1309 lines)
  V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/
    MasterService/GeneralService/CustomerService.cs   (279)
    IBusinessService/IMasterServices/IGeneralService/
      ICustomerService.cs                             (20)
  V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/
    GeneralViewModel/CustomerVM.cs                    (141)
    GeneralViewModel/CustomerIndirectVM.cs
  V.SMART/V.SMART.Shared/Data/Master/General_Module/
    Customer.cs                                       (147)
    CustomerIndirect.cs
    ContactPerson.cs
  V.SMART/V.SMART.Shared/Data/Configuration/
    MasterConfiguration/CustomerConfiguration.cs      (55)
  V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs
  V.SMART/V.SMART.Shared/Repository/MasterRepository/
    Generals/CustomerRepository.cs                    (266)
  V.SMART/V.SMART.Shared/Repository/IRepository/
    IMasterRepository/IGenerals/ICustomerRepository.cs
  V.SMART/V.SMART.Shared/Repository/Repository.cs
  V.SMART/V.SMART.Shared/Repository/IRepository/IUnitOfWork.cs
  V.SMART/V.SMART.Shared/Mappings/MasterMapping/
    GeneralMasterProfile/CustomerMapping.cs           (30)
  V.SMART/V.SMART.Shared/Services/ForeignKeyUsageChecker.cs
  V.SMART/V.SMART.Api/Controllers/CurrencyController.cs
    — read as the reference for what M2-D02-02 will build

TO BE CREATED:
  docs/kb/business-rules/customer-master-rules.md
    — created by THIS task
  tests/V.SMART.Shared.Tests/
    — created by M0-12-01, NOT by this task. If it is missing,
      stop and report Blocked.

DOES NOT EXIST — never reference it as if it does:
  frontend/nexgen-web/   — created by M2-C01
  frontend/ currently contains only vsmart-erp, the Angular
  pilot being archived by M2-C11.

BUSINESS RULES TO PRESERVE
All seventeen BR-CUST rules listed under CURRENT IMPLEMENTATION,
verbatim. Specifically:
  - Every user-facing message string is preserved BYTE-IDENTICAL.
    They are product UX written by the domain team (ADR-002
    section 4). Verify each one by grep after extraction.
  - Every regex is moved, not retyped. Copy the literal.
  - Every branch and ordering is preserved, INCLUDING the three
    items believed to be defects: BR-CUST-003's GST-casing
    asymmetry, BR-CUST-010's opening-balance reset on edit, and
    BR-CUST-012's silent discard of blank-named child rows.
    RECORD them as open questions. DO NOT FIX THEM in this task.
    Fixing them here would make the M2-D03 parity test fail for
    the right reason at the wrong time, and would conflate
    extraction with change.
  - Do NOT "improve" the N+1 SaveAsync-per-child-row pattern at
    CustomerUpsert.razor:1000-1012 and :1047-1086. It is a
    behaviour-risking optimisation and needs its own approved
    task.

ONE behaviour change IS authorised, and only one: fixing
BR-CUST-017 by mapping the child collections in CustomerMapping.
This adds behaviour to a code path that has no current consumer,
so it cannot alter existing behaviour, and without it there is no
ViewModel able to carry a customer's consignees and contacts
across the API boundary — M2-D02-02 would be blocked.

CONSTRAINTS
- Do not rewrite existing business services.
- Do not reimplement ERP business logic in React/TypeScript.
- Do not change unrelated modules.
- Do not make assumptions without checking source code.
- Do not repeat completed investigations.
- Do not modify the database schema unless this task explicitly
  authorizes it. IT DOES NOT. No entity change, no migration.
- Preserve existing API behaviour wherever practical.
- The server remains authoritative for validation, calculations,
  permissions and document numbering.
- Do not start another task after completing this one.
- Write NO controller and NO React code. Those are M2-D02-02 and
  M2-D02-03.
- Do not edit V.SMART/V.SMART.Api/ at all, including
  Program.cs DI registration — M2-B07 owns that.
- Do not add permission checks to CustomerService.
- Do not modify CustomerList.razor beyond what the triage
  requires; triage it, document it, change nothing.
- Do not delete or revive the commented-out blocks in
  CustomerRepository.cs.
- If tests/V.SMART.Shared.Tests/ does not exist, STOP and report
  Blocked. Do not proceed without characterisation tests.

EXECUTION PROCEDURE
1. Read the referenced knowledge-base documents.
2. Search the investigation registry.
3. Confirm whether the required investigation already exists.
4. Inspect the actual source code.
5. Verify every assumption against code before acting on it.
   In particular, RE-VERIFY EVERY LINE NUMBER in this prompt and
   correct any that have moved.
6. Produce the triage document FIRST, before moving any code.
7. Write the characterisation tests SECOND, against current
   behaviour, before moving any code.
8. Extract in this order, keeping the solution compiling at
   each step:
     a. pure derivation helpers (DerivePanFromGst,
        ApplyBusinessTypeDefaults) — no database access; move
        the regexes and the SupTyp lists to constants on the
        service rather than duplicating literals
     b. ValidateCustomerAsync — all of ValidateCustomer plus the
        duplicate-name check; messages verbatim; prefer keying
        by field so M2-D02-02 can build a ProblemDetails.errors
        dictionary, but preserve the exact text
     c. GetCustomerByIdAsync — wraps
        GetCustomerWithAllRelatedDataAsync, maps to CustomerVM;
        needs the BR-CUST-017 mapping fix
     d. UpsertCustomerAsync — all of UpsertCustomer MINUS the
        toast calls, the NavigationGuard.clear JS interop and
        the navigation, which are presentation and stay in the
        page; keep the transaction, the per-row SaveAsync
        sequence, the LogUserAction audit entry with screen
        "Customer", and the rollback-on-exception
9. Fix BR-CUST-017 in CustomerMapping.
10. Rewire CustomerUpsert.razor's @code to call the service.
    Decide explicitly whether to also move LoadStates (:884) and
    LoadCurrencies (:900) onto the service — moving them is
    preferred, because they become reference-data endpoints in
    M2-B09 — and if you keep the IUnitOfWork injection at :13,
    say so and say why in the triage document. Do not leave the
    decision implicit.
11. Run the required verification commands and tests.
12. Validate against the RUNNING BLAZOR APP. Against a
    NON-PRODUCTION tenant database, exercise through the Blazor
    UI and then inspect the persisted rows:
      - create a Local customer with two consignees and two
        contacts
      - edit it: remove one consignee, add another
      - enter a blank-named consignee row; confirm it is still
        silently dropped
      - switch business type to Exports; confirm GST becomes
        "URP", state becomes 99 and PAN clears
      - attempt a duplicate name; confirm the identical error
        text
      - attempt to delete a customer referenced elsewhere;
        confirm the identical refusal message
    Record the evidence in the triage document.
13. Review the full git diff.
14. Update documentation.
15. Update the investigation registry.

EXPECTED FILE CHANGES
Modified:
  V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/
    IBusinessService/IMasterServices/IGeneralService/
    ICustomerService.cs
  V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/
    MasterService/GeneralService/CustomerService.cs
  V.SMART/V.SMART.Shared/Mappings/MasterMapping/
    GeneralMasterProfile/CustomerMapping.cs
  V.SMART/V.SMART.Shared/Pages/Master_Module_pages/
    Customer_Pages/CustomerUpsert.razor
  docs/kb/business-rules/business-rule-inventory.md
  docs/kb/investigation-registry.md
  docs/kb/open-questions.md
  docs/kb/execution/tasks/M2-D02-01.md

Created:
  docs/kb/business-rules/customer-master-rules.md
  test files under tests/V.SMART.Shared.Tests/

Must not change:
  V.SMART/V.SMART.Api/**            (M2-D02-02 and M2-B07)
  frontend/**                       (M2-C01 onward)
  V.SMART/V.SMART.Shared/Migrations/**
  V.SMART/V.SMART.Shared/Data/Master/General_Module/*.cs
  V.SMART/V.SMART.Shared/Data/Configuration/
    MasterConfiguration/CustomerConfiguration.cs
  V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs
  V.SMART/V.SMART.Shared/Pages/Master_Module_pages/
    Customer_Pages/CustomerList.razor
  the commented-out blocks in CustomerRepository.cs
  every other module's service, page and ViewModel

TESTS
Written into tests/V.SMART.Shared.Tests/ (created by M0-12-01).
Characterisation tests, written BEFORE the extraction and
passing both before and after:
  - PAN derivation at GST lengths 14, 15 and 16 (BR-CUST-002)
  - each business type's SupTyp set and default (BR-CUST-004)
  - the Imports/Exports forcing rule AND the asymmetric
    clear-on-switch-back (BR-CUST-005)
  - each GST validation branch (BR-CUST-006)
  - each PAN validation branch (BR-CUST-007)
  - consignee validation including the blank-name skip
    (BR-CUST-009, BR-CUST-012)
  - the OpenBal derivation including the reset-on-edit
    (BR-CUST-010)
  - the child id-set diff: insert, update, delete (BR-CUST-013)
  - ValidateCustomerAsync returns the exact legacy message
    strings for every failure branch
  - round-trip GetCustomerByIdAsync -> UpsertCustomerAsync
    preserves consignees and contacts (proves the BR-CUST-017
    fix)
Where a rule is only reachable inside the Razor page today, note
that in the triage document as a limitation and write the test
against the extracted method immediately after moving it.

Command:
  dotnet test tests/V.SMART.Shared.Tests/

ACCEPTANCE CRITERIA
- docs/kb/business-rules/customer-master-rules.md exists with
  valid KB frontmatter.
- It contains a triage row for EVERY member of BOTH @code
  blocks, each assigned exactly one of Presentation /
  Data loading / Business logic.
- It records the measured @code line counts before AND after for
  CustomerUpsert.razor, and the before count for
  CustomerList.razor.
- Every BR-CUST-nnn allocated carries a statement, file:line
  evidence, a Confirmed/Inferred/Unknown rating, a disposition
  and a migration note.
- KB-030 links to the new document and lists the new ids in its
  frontmatter.
- No BR id already present in KB-030 has been renumbered or
  reused.
- ICustomerService exposes a single-call save and an
  independently callable validation method.
- CustomerUpsert.razor's @code contains NO
  BeginTransactionAsync, NO ExistsByNameAsync, NO
  _unitOfWork.Customers.CreateAsync/UpdateAsync, NO
  _unitOfWork.CustomerIndirects.*, NO _unitOfWork.ContactPersons.*
  and NO Regex.IsMatch — verified by grep.
- Every message string from ValidateCustomer appears
  byte-identical in the extracted service — verified by grep,
  per string.
- CustomerMapping maps both child collections in both
  directions.
- dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj reports
  0 errors and no more than 6,695 warnings.
- dotnet test tests/V.SMART.Shared.Tests/ passes.
- The manual Blazor scenarios were executed and their
  persisted-row evidence is recorded in the triage document.
- git diff --stat V.SMART/V.SMART.Api/ is empty.
- git diff --stat frontend/ is empty.
- No file under V.SMART/V.SMART.Shared/Migrations/ was added or
  modified.
- KB-003's INV-024 row is amended to Partial and names Customer
  Master.
- Every defect found but not fixed is recorded in
  docs/kb/open-questions.md with a new Q-xx id, cross-referenced
  from the rule that describes it.

VERIFICATION COMMANDS
  dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj
  dotnet build V.SMART/V.SMART.Web/V.SMART.Web.csproj
  dotnet test tests/V.SMART.Shared.Tests/
  git status --porcelain
  git diff --stat V.SMART/V.SMART.Api/ frontend/ V.SMART/V.SMART.Shared/Migrations/
  grep -n "BeginTransactionAsync\|ExistsByNameAsync\|Regex.IsMatch\|_unitOfWork.Customers\|_unitOfWork.CustomerIndirects\|_unitOfWork.ContactPersons" "V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/CustomerUpsert.razor"
  grep -n "^@code" "V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/CustomerUpsert.razor"
  awk 'END{print FILENAME": "NR}' "V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/CustomerUpsert.razor"
  grep -rn "CustomerIndirectVMs\|ContactPersonVMs" --include=*.cs V.SMART/

Notes on commands:
  - dotnet test is valid ONLY because M0-12-01 is a
    prerequisite. Before M0-12-01 there is no test project in
    the solution (INV-023, Confirmed) and dotnet test finds
    nothing.
  - dotnet build of V.SMART.Web is a known-valid command whose
    result has NOT yet been measured. Record what you get; do
    not treat an unexpected number as a failure of this task
    without investigating.
  - The Api build baseline is 0 errors and 6,695 warnings,
    taking roughly 3 minutes.

DOCUMENTATION REQUIREMENTS
  docs/kb/business-rules/customer-master-rules.md — CREATE.
    Full KB frontmatter: doc_id, title, module, source_files,
    entities, api_endpoints, database_tables, business_rules,
    status: complete, confidence: mixed, last_verified,
    dependencies. Content: the triage table for both pages, the
    BR-CUST rules, and the manual-validation evidence.
  docs/kb/business-rules/business-rule-inventory.md (KB-030) —
    add a "## Customer Master rules" section linking to the new
    document; append the new ids to frontmatter business_rules;
    add the Customer paths to source_files; bump last_verified.
  docs/kb/investigation-registry.md (KB-003) — amend the INV-024
    row: status Partial, gap "every module other than Customer
    Master", link to the new document; bump last_verified.
  docs/kb/open-questions.md (KB-004) — add a Q-xx for each
    defect found and deliberately not fixed; at minimum
    BR-CUST-003, BR-CUST-010 and BR-CUST-012.
  docs/kb/execution/tasks/M2-D02-01.md — set status: Completed
    and fill the frontmatter business_rules with the ids you
    actually allocated; bump last_verified.
  docs/kb/execution/task-tracker.md (KB-081) — mark M2-D02-01
    Completed ONLY IF THE FILE EXISTS. It is referenced by
    KB-080 but did not exist on 2026-08-12. Do not create it as
    a side effect of this task.

Record these negative results explicitly so no later session
repeats the work:
  - "Grepped docs/kb/ for BR-CUST before starting — no hits.
     The BR-CUST series was unallocated."
  - "Grepped V.SMART/ for CustomerIndirectVMs and
     ContactPersonVMs — hits only in CustomerVM.cs:134-135. No
     producer, no consumer."
  - "ICustomerService had no write method and no get-by-id
     before this task."
  - "CustomerRepository.cs is 266 lines of which one method is
     live."

GIT
Branch:  migration/M2-D02-01-customer-code-triage-extraction
Commit:  M2-D02-01: Triage Customer @code and extract business
         logic into CustomerService
Do not merge. Do not push to master. Leave the branch for review.

FINAL RESPONSE
When finished, report:
1.  Task ID
2.  Task status (Completed / Needs Review / Blocked)
3.  What was implemented
4.  Files created
5.  Files modified
6.  Files deleted
7.  Tests executed
8.  Test results
9.  Documentation updated
10. Investigation registry updated
11. Architectural decisions taken
12. Unexpected findings
13. Assumptions made
14. Deviations from this task
15. Recommended next task

In addition, because this task is the TEMPLATE for every later
module wave, state explicitly in item 11:
  - the final signature of every method added to ICustomerService
    and why that shape was chosen
  - the before and after @code line counts for
    CustomerUpsert.razor
  - the count of @code members in each of the three triage
    buckets, for both pages
  - whether the extraction pattern felt awkward, and if so what
    you would change before sixty more modules copy it

IMPORTANT:
EXECUTE ONLY THIS TASK.
DO NOT START THE NEXT TASK.
============================================================
```
