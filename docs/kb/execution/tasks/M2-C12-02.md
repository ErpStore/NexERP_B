---
doc_id: TASK-M2-C12-02
title: Re-specify for Angular — auth, routing, decimal handling and the pilot-adoption question
module: execution
task_id: M2-C12-02
milestone: M2
parent_task: M2-C12
task_type: Documentation
priority: P0
status: Needs Review
estimate: 1 d
depends_on: [M2-C00, M2-C01]
source_files:
  - docs/kb/execution/tasks/M2-C02.md
  - docs/kb/execution/tasks/M2-C03.md
  - docs/kb/execution/tasks/M2-C10.md
  - docs/kb/execution/tasks/M2-C11.md
business_rules: []
confidence: n/a
last_verified: 2026-08-22
complexity: MEDIUM
risk: LOW
---

# M2-C12-02 — Re-specify for Angular: auth, routing, decimal handling and the pilot-adoption question

| Field | Value |
|---|---|
| Task ID | M2-C12-02 |
| Milestone | M2 — Foundation |
| Parent | [M2-C12](M2-C12.md) |
| Type | Documentation |
| Priority | P0 |
| Status | Needs Review |
| Estimate | 1 d |
| Gate | G2 |

## Objective

Re-specify **this batch only** — auth, routing, decimal handling and the pilot-adoption question — against
[`ADR-007`](../../decisions/ADR-007-angular-stack.md) and the Angular rewrite of
[KB-050](../../frontend-new/react-architecture.md), removing the `⛔ STOP` banner from each file
*in the same change that removes its React content*.

**Files in this batch — no others may be edited except the ones named under "Also" below:**

- `docs/kb/execution/tasks/M2-C02.md`
- `docs/kb/execution/tasks/M2-C03.md`
- `docs/kb/execution/tasks/M2-C10.md`
- `docs/kb/execution/tasks/M2-C11.md`

Read [`M2-C12`](M2-C12.md) first. It holds the translation table, the rationale, the
out-of-scope list and the failure history. This file does not repeat them; it narrows them.

## The atomicity rule — inherited from the parent, and non-negotiable

> **A file's ⛔ banner may only be removed in the same change that removes its React
> instructions.** No file may come to rest banner-removed and React-bearing — that strips the
> only guard stopping a runner from building the wrong stack while leaving the instruction to
> build it. If you cannot finish a file, **leave its banner in place** and report it as not
> done. Half-done is worse than not started.

Attempt 1 of the unsplit parent failed exactly this way across 23 files. Do not repeat it.

## Acceptance Criteria

1. Every file in this batch has **no** `⛔ STOP — this specification is superseded` banner.
   Verify with `grep -rl '⛔ STOP — this specification is superseded' docs/kb/execution/tasks/ --exclude='M2-C12*'`
   and confirm none of this batch's files appear.
2. **The atomicity rule holds**: for every file in the batch,
   `grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite'`
   returns **only** lines that explicitly mark themselves historical ("the React specification
   this replaces", "no longer specifies", "not deleted from the record"). Quote the actual
   output per file in the Execution Record — a count alone is not evidence.
3. Each file carries a re-specification note naming `M2-C12-02` and the date, in the shape
   [`M2-C01`](M2-C01.md) uses.
4. Every path given under `frontend/` is `frontend/nexgen-web/…`, and every quoted command
   appears verbatim in [KB-083's verified Angular table](../prompt-template.md#verified-repository-commands).
5. **`depends_on`, `business_rules`, `priority`, `estimate` and gate are byte-unchanged** in
   every file — prove by diffing those frontmatter keys, do not assert it.
6. Every `file:line` citation into `V.SMART/` is byte-unchanged, or its change is justified
   individually. This Blazor evidence was never a React decision and survives.
7. **The diff is confined to `docs/kb/`, and within it to this batch's own footprint.** That
   footprint is: the batch files named above; **this task file** (`M2-C12-02.md` — its Execution
   Record is where criterion 2's grep output is quoted, and KB-088 §4 makes it an *Always*
   update); [`task-tracker.md`](../task-tracker.md) (this sub-task's own row); and, only if the
   work actually raises one, [`open-questions.md`](../../open-questions.md),
   [`failure-log.md`](../failure-log.md), [`runner-state.md`](../runner-state.md) and
   [`current-task.md`](../current-task.md). Nothing under `V.SMART/`, `tests/`, `frontend/` or
   `.github/` may appear — verify with
   `git diff --name-only master...HEAD | grep -v '^docs/kb/'`, expected **empty**.
8. Each file still satisfies [KB-090](../task-template.md)'s required section set.

## Testing Requirements

No code, so no suite. Validation is by inspection plus the greps above, **with their output
quoted**. In addition:

- **Read one file in this batch end to end as if about to implement it.** If it needs a detail
  you would have to invent, it is not re-specified. Name the file read and the outcome.

## Documentation Updates

| Document | Update |
|---|---|
| The batch files | Re-specified; banner removed; note added |
| [KB-081](../task-tracker.md) | This sub-task's own row only. **The 25 re-specified rows and `dependency-graph.md` belong to `M2-C12-05`**, the last batch, so the tree is restated once. |
| [KB-004](../../open-questions.md) | Any question raised by an ambiguous re-derivation. Check `git branch --no-merged master` before claiming an id. |

## Completion Conditions

- All 8 acceptance criteria met, greps quoted.
- The atomicity rule holds for every file — verified, not assumed.
- **Only the repository owner may set `Completed`** ([KB-088](../workflow.md#who-may-set-completed)).

## Git Strategy

| | |
|---|---|
| Branch | `migration/m2-c12-02-respec`, cut fresh from `master` |
| Commit | `M2-C12-02: <imperative summary>` |
| Merge | Left for review. Never merged or pushed from an execution session. |

---

## Execution Record (2026-08-22)

Branch `migration/M2-C12-02-respec`, cut fresh from `master` at `7b4b86c`. *(Branch-name
deviation: this file specifies the lowercase `migration/m2-c12-02-respec`; the uppercase form was
used to match the merged precedent `migration/M2-C12-01-respec` and the dispatch instruction.
Nothing else about the strategy changed.)*

**Three files were re-specified for Angular. `M2-C11` was not, deliberately** — `M2-C12.md:159-166`
forbids it and requires the banner to be replaced by a statement of **Q-38** instead, with the
task left `Blocked` and routed to the owner. Q-38 was re-checked in
[`open-questions.md`](../../open-questions.md) on 2026-08-22 and is **still unanswered**.

Every command below was run on this branch and its output is quoted as observed.

### Criterion 1 — no `⛔ STOP` banner survives in the batch · **MET**

```
$ grep -rl '⛔ STOP — this specification is superseded' docs/kb/execution/tasks/ --exclude='M2-C12*'
docs/kb/execution/tasks/M2-C05-01.md
docs/kb/execution/tasks/M2-C05-02.md
docs/kb/execution/tasks/M2-C05-03.md
docs/kb/execution/tasks/M2-C05.md
docs/kb/execution/tasks/M2-C06.md
docs/kb/execution/tasks/M2-C07.md
docs/kb/execution/tasks/M2-C08-01.md
docs/kb/execution/tasks/M2-C08-02.md
docs/kb/execution/tasks/M2-C08-03.md
docs/kb/execution/tasks/M2-C08.md
docs/kb/execution/tasks/M2-C09.md
docs/kb/execution/tasks/M2-D01.md
docs/kb/execution/tasks/M2-D02-01.md
docs/kb/execution/tasks/M2-D02-02.md
docs/kb/execution/tasks/M2-D02-03.md
docs/kb/execution/tasks/M2-D02.md
docs/kb/execution/tasks/M2-D03.md
```

17 files, all outside this batch. **None of `M2-C02.md`, `M2-C03.md`, `M2-C10.md`, `M2-C11.md`
appears.** The 17 remaining are `M2-C12-03`..`-05`'s work — down from the 21 `M2-C12-01` left.

> **One trap worth recording, because it cost a re-run.** The first draft of `M2-C11.md` failed
> this grep on **its own acceptance criterion**, which quoted the banner string verbatim while
> asserting the file no longer contains it. It is the identical self-trip `M2-C12.md` records
> against its own criterion 1. The criterion was rephrased to describe the banner rather than
> quote it.

### Criterion 2 — the atomicity rule · **MET**, output quoted per file

Run from `docs/kb/execution/tasks/`, one file at a time, with
`grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite'`:

```
===== M2-C02.md =====
163:| **M2-C01** | The Angular workspace, `app.config.ts`'s provider array, the router, the deliberately empty interceptor array, and the Vitest + `@testing-library/angular` harness. |
511:Tests run on **Vitest through `@angular/build:unit-test`** with `@testing-library/angular` — the

===== M2-C03.md =====
409:Tests run on **Vitest through `@angular/build:unit-test`** with `@testing-library/angular`, plus

===== M2-C10.md =====
39:> the test runner is Vitest through `@angular/build:unit-test`. The `file:line` evidence about the
120:  `eslint.config.js` and its Vitest harness must exist. `decimal.js` is already installed by
129:| M2-C01 | Hard | Provides the workspace, `decimal.js`, the flat `eslint.config.js` and Vitest. |
236:- **An architecture test** (Vitest) that scans the source tree and fails if `decimal.js`,
377:Tests run on **Vitest through `@angular/build:unit-test`** — the harness `M2-C01` established

===== M2-C11.md =====
(no output, exit 1)
```

**Every hit is the same false positive, and none is a live instruction to build the ADR-003
stack:** all eight are the substring `vite` inside **Vitest** — the *current, verified* Angular
test runner. KB-083's Angular table: *"Runner is **Vitest 4.1.11** through
`@angular/build:unit-test`, the Angular CLI's default at 22.1.5. **Not Karma**"*. Naming it is
required, not stale. `M2-C12-01` recorded the identical class of hit for its own batch.

`M2-C11.md` returns nothing at all — its archive-era body never named the ADR-003 stack, which is
precisely why its hazard was never the *stack* but the *purpose*. That is Q-38.

### Criterion 3 — re-specification note · **MET**

```
$ grep -n 'M2-C12-02' M2-C02.md M2-C03.md M2-C10.md M2-C11.md | grep -i 'specified\|Visited'
M2-C02.md:35:> **Re-specified for Angular on 2026-08-22 by [`M2-C12-02`](M2-C12-02.md).** The ⛔ STOP banner
M2-C03.md:31:> **Re-specified for Angular on 2026-08-22 by [`M2-C12-02`](M2-C12-02.md).** The ⛔ STOP banner
M2-C10.md:25:> **Re-specified for Angular on 2026-08-22 by [`M2-C12-02`](M2-C12-02.md).** The ⛔ STOP banner
M2-C11.md:38:> **Visited on 2026-08-22 by [`M2-C12-02`](M2-C12-02.md).** The ⛔ *supersession* banner that stood
```

The three re-specified files use the blockquote shape `M2-C01.md:28` uses and `M2-C04*.md` follow,
including the "not deleted from the record" clause. **`M2-C11.md` deliberately reads *Visited*,
not *Re-specified*** — claiming it had been re-specified would be false, and this file's whole
handling of `M2-C11` is that it was not.

### Criterion 4 — `frontend/` paths and quoted commands · **MET, with one reading recorded**

Paths. Every `frontend/…` path across the four files resolves to one of two roots:

```
$ grep -hoE 'frontend/[a-z0-9-]+' M2-C02.md M2-C03.md M2-C10.md M2-C11.md | sort -u
frontend/nexgen-web
frontend/vsmart-erp
```

**Reading recorded:** criterion 4 says every `frontend/` path must be `frontend/nexgen-web/…`.
`frontend/vsmart-erp/` also appears, and it must: it is the *subject* of `M2-C11`/Q-38, and
`M2-C02`, `M2-C03` and `M2-C10` each name it in *Files That Must Not Change* precisely so no task
disposes of it while Q-38 is open. Every occurrence is either a "do not touch this" instruction or
a `file:line` citation of the pilot's evidence. **Not one names it as a place to build.** The
criterion's intent — no work is specified against a path that does not exist or is the wrong app —
holds.

Commands. The complete extracted set across the four files:

```
$ grep -hoE '`(npm|npx|ng|dotnet|git) [^`]*`' M2-C02.md M2-C03.md M2-C10.md M2-C11.md | sort -u
`dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj`
`dotnet build`
`git branch --no-merged master`
`git diff --name-only -- V.SMART`
`git grep -n "from 'decimal.js'" -- frontend/nexgen-web/src`
`git grep -n "localStorage\|sessionStorage" -- frontend/nexgen-web/src`
`git grep -n "role" -- frontend/nexgen-web/src/app/shared/components`
`git grep -n "vsmart-erp"`
`git ls-files frontend/vsmart-erp | wc -l`
`git ls-files frontend/vsmart-erp/dist frontend/vsmart-erp/.angular`
`git revert`
`npm run build`
`npm run e2e`
`npm run format:check`
`npm run lint`
`npm run test:ci`
`npm run typecheck`
`npx playwright install chromium`
```

All six `npm` commands (`typecheck`, `lint`, `format:check`, `test:ci`, `build`, `e2e`) appear
verbatim in [KB-083's Angular table](../prompt-template.md#verified-repository-commands).
`npx playwright install chromium` appears verbatim in that table's `npm run e2e` row.
`dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` and the `git` commands are the verified
repository commands (INV-029).

**Two commands were deliberately removed and are named so the removal is not mistaken for an
oversight:** `npm run test -- --run` (the superseded runner invocation, in all three files) and
**`npm run coverage`** (in `M2-C10`). There is **no coverage script in `package.json` and no
coverage row in KB-083** — the same gap `M2-C12-01` recorded for `M2-C04-01`. `M2-C10`'s
"100 % statement and branch coverage" criterion was therefore restated as an enumerated
per-export test obligation plus a review check, and the change is called out in that file's own
re-specification note rather than made silently.

### Criterion 5 — protected frontmatter keys byte-unchanged · **MET**

```
$ for f in M2-C02 M2-C03 M2-C10 M2-C11; do
    diff <(git show master:docs/kb/execution/tasks/$f.md | grep -E '^(depends_on|business_rules|priority|estimate):|^\| (Priority|Estimate|Gate) \|') \
         <(grep -E '^(depends_on|business_rules|priority|estimate):|^\| (Priority|Estimate|Gate) \|' docs/kb/execution/tasks/$f.md) \
    && echo "BYTE-IDENTICAL"; done
===== M2-C02 =====
BYTE-IDENTICAL
===== M2-C03 =====
BYTE-IDENTICAL
===== M2-C10 =====
BYTE-IDENTICAL
===== M2-C11 =====
BYTE-IDENTICAL
```

*(The `echo "===== $f ====="` at the top of each loop iteration is elided from the command line
above for width; it is what produces the header lines.)*

`depends_on`, `business_rules`, `priority`, `estimate` and the `| Gate |` row are byte-identical
to `master` in all four files.

**Two frontmatter keys outside that protected set did change, both in `M2-C11.md`, and both are
required by the parent rather than chosen here:**

- `status: Not Started` → `status: Blocked` — `M2-C12.md`'s acceptance criterion 8 requires
  `M2-C11` to *stay `Blocked`*, and the file did not previously say so (only the tracker did).
- `title: Archive the Angular pilot` → `title: The Angular pilot's disposition — blocked on Q-38`.
  **This is a judgement call and is flagged as one.** "Archive" is one of Q-38's two candidate
  answers; a file blocked on a question cannot carry that question's answer in its title. The
  neutral title states the situation without choosing. If the reviewer disagrees, the fix is one
  line and the body already states both readings either way.

`last_verified` was bumped to `2026-08-22` in all four files, as KB-088 §4 requires.

### Criterion 6 — `V.SMART/` `file:line` citations byte-unchanged · **MET, with two justified losses**

```
$ for f in M2-C02 M2-C03 M2-C10 M2-C11; do
    diff <(git show master:docs/kb/execution/tasks/$f.md | grep -oE 'V\.SMART/[A-Za-z0-9_./-]+(\.[a-z]+)?:[0-9,:-]*[0-9]' | sort -u) \
         <(grep -oE 'V\.SMART/[A-Za-z0-9_./-]+(\.[a-z]+)?:[0-9,:-]*[0-9]' docs/kb/execution/tasks/$f.md | sort -u) \
    && echo "CITATION SET IDENTICAL"; done
===== M2-C02 =====
CITATION SET IDENTICAL
===== M2-C03 =====
1d0
< V.SMART/V.SMART.Shared/Components/PageHeader.razor:16
===== M2-C10 =====
2d1
< V.SMART/V.SMART.Shared/Data/OutSourcing/PurchasePo/PurchPo.cs:164,167
===== M2-C11 =====
CITATION SET IDENTICAL
```

**Both losses are inside the deleted *Fresh-Session Execution Prompt* block, not the
specification body**, and both facts survive elsewhere in the same file:

| Lost citation | Where it lived on `master` | Where the fact still is |
|---|---|---|
| `…/Components/PageHeader.razor:16` | `M2-C03.md:754` — inside the prompt block, which starts at `:517` | `M2-C03.md` *Existing Behavior to Preserve* still cites ``(`PageHeader.razor:16,27`)`` verbatim, and `source_files` still lists the file |
| `…/PurchasePo/PurchPo.cs:164,167` | `M2-C10.md:530` and `:679` — inside the prompt block, which starts at `:476` | `M2-C10.md` *Why This Task Exists* still cites `…/PurchPo.cs:167` verbatim, as it did on `master:88` |

The prompt block is removed on
[`task-template.md`](../task-template.md)'s own instruction (`:194`, `:207-209`) — the same removal
`M2-C12-01` made in all four of its files. No citation in any *specification* section changed.

### Criterion 7 — the diff · **MET**

```
$ git diff --name-only master -- . | grep -v '^docs/kb/'
(no output, exit 1)
```

Changed files, all under `docs/kb/`, all inside this batch's declared footprint:

```
docs/kb/execution/task-tracker.md          (this sub-task's own row only)
docs/kb/execution/tasks/M2-C02.md          (batch)
docs/kb/execution/tasks/M2-C03.md          (batch)
docs/kb/execution/tasks/M2-C10.md          (batch)
docs/kb/execution/tasks/M2-C11.md          (batch)
docs/kb/execution/tasks/M2-C12-02.md       (this file — criterion 2's evidence lives here)
```

Nothing under `V.SMART/`, `tests/`, `frontend/` or `.github/`. `open-questions.md`,
`failure-log.md`, `runner-state.md` and `current-task.md` are **not** touched: no new question was
raised (Q-38 already exists and needed no edit), and the runner bookkeeping files belong to the
orchestrator. The 25 re-specified tracker rows and `dependency-graph.md` are `M2-C12-05`'s, per
this file's *Documentation Updates* table, and were left alone.

### Criterion 8 — KB-090's required section set · **MET**

`## ` headings, `master` vs `HEAD`:

- **`M2-C02`, `M2-C03`, `M2-C10`** — identical shape change in all three, and the same one
  `M2-C12-01` made: `## React Changes` → `## Frontend Changes` (the same section, renamed off the
  dead stack); `## Completion Conditions` **added**; `## Fresh-Session Execution Prompt`
  **removed**, which `task-template.md:194,207-209` directs. Nothing else added or lost.
- **`M2-C11` was regenerated to the KB-090 template**, which `task-template.md:207-209` permits
  explicitly for a file being regenerated anyway. It now carries all thirteen required sections —
  Objective, Scope, Out of Scope, Dependencies, Relevant Documentation, Relevant Existing Code,
  Business Rules, Implementation Requirements, Acceptance Criteria, Testing Requirements,
  Documentation Updates, Completion Conditions, Git Strategy — plus two evidence sections,
  *Retained evidence* and *The pre-ADR-007 archive specification — suspended, not executable*.
  The archive plan itself is **not reproduced as instruction**; the file names its git retrieval
  path (`git show 7b4b86c:docs/kb/execution/tasks/M2-C11.md`, verified to resolve) so nothing is
  destroyed. Under reading (b) of Q-38, executing that plan would delete the application, which is
  why it is quarantined rather than carried forward.

No required section is missing from any file.

## Testing Requirements — the end-to-end read

**File read end to end: `docs/kb/execution/tasks/M2-C10.md` (536 lines).** Chosen because it is
the shortest path to a first implementation and, of the three re-specified files, the only one
with no unmet hard dependency other than `M2-C01`.

**Outcome: implementable without inventing a detail.** The four things an implementer would
otherwise have had to guess are each supplied or explicitly deferred:

1. **Where the module goes.** `src/app/shared/utils/decimal/` — `shared/utils/` is named by
   KB-050's structure block and exists on disk with a `.gitkeep` (verified 2026-08-22); the
   `decimal/` subfolder is labelled *proposed*.
2. **Whether the library is even still right.** `decimal.js` is in ADR-007's *"Carried over from
   ADR-003 unchanged"* list (`:101,170`) and is already installed
   (`package.json`: `"decimal.js": "^10.6.0"`, verified 2026-08-22), so the task adds a
   discipline, not a dependency.
3. **How the lint rules attach.** `eslint.config.js` already defines `bannedComponentLibraries`
   and `bannedGeneratedClientImports` and already exempts `src/app/core/api/**` by override — the
   file states that shape is to be followed rather than a parallel mechanism invented.
4. **The rounding mode.** Deliberately *not* supplied: INV-032 measures it, and if it cannot be
   read out of code the file requires `Decimal.ROUND_HALF_UP` plus a prominent comment plus an
   `Unknown` raised in KB-004. That is a specified deferral, not a gap.

The one genuinely open item the read surfaced is the coverage gate, and it is stated as open in
the file rather than left for the implementer to discover — see criterion 4 above.

## Status

`Needs Review`. Three files re-specified; `M2-C11` left `Blocked` on **Q-38** with the question
stated and the owner named, exactly as `M2-C12.md:159-166` requires. **Only the repository owner
may set `Completed`** ([KB-088](../workflow.md#who-may-set-completed)).

## Execution Record (2026-08-22) — session close-out

**This is a close-out, not another implementation attempt.** The independent validator's verdict
(dispatched separately from this session) was **PASS** on the first attempt — all 8 acceptance
criteria `MET`, `scopeOk: true`, `failureCategory: none` — so nothing was re-implemented here.
Only bookkeeping was written this session: this file's frontmatter status, this close-out note,
and the runner/KB files named in [review-templates.md § Session
close-out](../review-templates.md#session-close-out).

**What was checked before recording the outcome.** Re-read the validator's full verdict
(reproduced in the task-launch context: all 8 criteria `MET`, criterion 7's guard
`git diff --name-only master...HEAD | grep -v '^docs/kb/'` empty, no regressions, `Q-38` reconfirmed
unanswered). Independently re-ran the confinement guard on this branch (tip `eaa2337`):

```
$ git diff --name-only master...HEAD | grep -v '^docs/kb/'
(no output, exit 1)

$ git diff --name-only master...HEAD
docs/kb/execution/task-tracker.md
docs/kb/execution/tasks/M2-C02.md
docs/kb/execution/tasks/M2-C03.md
docs/kb/execution/tasks/M2-C10.md
docs/kb/execution/tasks/M2-C11.md
docs/kb/execution/tasks/M2-C12-02.md
```

Matches the validator's evidence exactly. `task-tracker.md`'s `M2-C12-02` row was already
`**Needs Review**` on this branch (the implementer wrote its own row, as the *Documentation
Updates* table above authorises) — no tracker edit was needed beyond confirming it already said
what this close-out records. This file's own frontmatter (`status:`) had not been advanced past
`Ready` despite the body already reading `Needs Review`; that inconsistency is what this session
corrected — `status: Ready` → `status: Needs Review`, `last_verified` already `2026-08-22`.

**What differed from a routine close-out, and why.** Nothing substantive. `open-questions.md`
was checked and **not** written — Q-38 was already recorded by `M2-C00` and this task's own
Execution Record above already states it is "re-checked ... and is still unanswered"; writing it
again would duplicate, not add, a fact. No new business rule, risk, or architecture contradiction
surfaced. `failure-log.md` was **not** written — this task did not fail; it passed on attempt 1
with no escalation, so KB-092 does not apply.

**Reminder for whoever reviews this for merge:** per the task's own *Completion Conditions* and
KB-088 "Who may set `Completed`", this stays `Needs Review` until the repository owner (Vivek)
reviews the diff and the still-open **Q-38** on `M2-C11`. `M2-C11` is deliberately *not*
re-specified — it is left `Blocked`, banner replaced by a precise statement of Q-38, per
`M2-C12.md:159-166`.
