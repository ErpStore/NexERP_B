---
doc_id: TASK-M2-D01
title: Currency end-to-end in Angular
module: execution
task_id: M2-D01
milestone: M2
parent_task: n/a
task_type: Frontend
priority: P0
status: Blocked
estimate: 3 d
depends_on: [M2-C05-03, M2-A02, M2-B10]
source_files:
  - V.SMART/V.SMART.Api/Controllers/CurrencyController.cs
  - V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/AccountsViewModel/CurrencyVM.cs
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/IBusinessService/IMasterServices/IAccountsService/ICurrencyService.cs
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/MasterService/AccountsService/CurrencyService.cs
  - V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Currency_Pages/CurrencyList.razor
  - V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Currency_Pages/CurrencyUpsert.razor
  - V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs
  - frontend/vsmart-erp/src/app/features/currency/
entities: [Currency]
api_endpoints:
  - "GET /api/v1/currencies"
  - "GET /api/v1/currencies/{id}"
  - "POST /api/v1/currencies"
  - "PUT /api/v1/currencies/{id}"
  - "DELETE /api/v1/currencies/{id}"
database_tables: [Currency]
business_rules: []
confidence: n/a
last_verified: 2026-08-26
---

# M2-D01 — Currency end-to-end in Angular

> **Re-specified for Angular on 2026-08-22 by [`M2-C12-05`](M2-C12-05.md).** The ⛔ STOP banner
> that stood here is removed **in the same change that removed the stack-specific instructions it
> guarded**: this file no longer specifies the ADR-003 stack.
> [`ADR-007`](../../decisions/ADR-007-angular-stack.md) is the governing stack decision —
> **Angular 22.x + PrimeNG** — and [KB-050](../../frontend-new/react-architecture.md), rewritten for
> Angular by `M2-C00`, is the structure this feature lives in
> (`frontend/nexgen-web/src/app/features/masters/currency/`, lazily routed).
>
> **The specification this replaces is not deleted from the record**, and almost none of it was
> ever framework-specific. The whole of *Existing Behavior to Preserve* — the `CurrencyController`
> line table, the two-400-shapes finding, the **unanchored** `Symbol` regex, `ScreenCode` 5 and the
> Blazor `@code` measurements — comes through **byte-identical**, because it describes existing
> Blazor and API code and was never a frontend-framework decision. What changed is the *how*: a
> standalone `CurrencyListComponent` and a typed `CurrencyService` over `HttpClient` instead of a
> query library, typed **Reactive Forms + `Validators`** instead of a schema-validated form library,
> `PermissionService` / `*appHasRight` / `requireScreen()` instead of a hooks-based permission
> store, PrimeNG through the `M2-C04`/`M2-C05` primitives instead of a third-party component kit,
> `@testing-library/angular` with `provideHttpClientTesting` instead of a DOM-level HTTP mock, and
> the verified Angular command set of
> [KB-083](../prompt-template.md#verified-frontend-commands).
>
> **The slice's purpose is unchanged.** This is still the first vertical slice, still deliberately
> the smallest real module in the system, and still the control for `M2-D02`.

> ### ⛔ Blocked on arrival — 2026-08-26, execution attempt 1
>
> **This task stopped before writing any feature code, on the instruction of its own
> *Prerequisites* section below (`:160-177`): _"If any of these is incomplete, stop and report
> Blocked. Do not stub the missing piece."_** The three `depends_on` entries (`M2-C05-03`,
> `M2-A02`, `M2-B10`) genuinely are `Completed` and merged — that half of the selection was
> correct. The **transitive** table was never tested, and four of its rows are unmet:
>
> | Prerequisite | Tracker | On disk (Confirmed 2026-08-26) |
> |---|---|---|
> | `M2-C02` | `Blocked` — `task-tracker.md:157` | `frontend/nexgen-web/src/app/core/auth/` holds only `.gitkeep`. No `permission.service.ts`, `auth.guard.ts` or `has-right.directive.ts`. `core/http/` likewise — `error.interceptor.ts`, *"the only place a problem body is parsed"* (`:241`), does not exist. |
> | `M2-C03` | `Blocked` — `:162` | `frontend/nexgen-web/src/app/layout/shell/` holds only `.gitkeep`. |
> | `M2-A04` | `Blocked` — `:114` (on `M0-04`) | — |
> | `M2-A05` | `Blocked` — `:115` | — |
>
> `frontend/nexgen-web/src/app/app.routes.ts:5` still carries `M2-C01`'s comment *"No guard:
> guards arrive with M2-C02"*. **Requirement 10** (`requireScreen('Currency','view')`,
> `*appHasRight`, `PermissionService.forScreen`), **requirement 11** (the single
> `ProblemDetails` handler) and five acceptance criteria depend on code that does not exist,
> and the only way to deliver them today is the stub this section forbids. The **process**
> question — should the selection test walk this table? — is recorded as **`Q-97`** in
> [KB-004](../../open-questions.md).
>
> **What is genuinely ready, so the next attempt need not re-check it:** the generated client
> (`core/api/generated/services/currency.service.ts`, `models/currency-vm.ts`,
> `currency-vm-paged-result.ts`, `me-response.ts`, `screen-right.ts`, `problem-details.ts`),
> and the `data-grid` / form / overlay / feedback primitives under `shared/components/`,
> including `data-grid/grid-export.service.ts` and `data-grid/data-grid-query-state.ts`.
>
> **⚠️ *Existing Behavior to Preserve* below is stale — do not cite it.** It describes the
> pre-M2-A/B controller, exactly as `:257` warned it would. Re-verified against source
> 2026-08-26; **[KB-040](../../api/api-overview.md) already carries the correct contract** and
> needs no amendment, so read KB-040 and treat the tables below as history:
>
> | This file says | Source says (Confirmed 2026-08-26) |
> |---|---|
> | `CurrencyController.cs` is 98 lines; every `:` citation in that section | **172 lines.** `[Route($"{ApiRoutes.V1}/currencies")]` `:12`, `[RequireScreen("Currency")]` `:21`, `GetAll` `:62`, `GetById` `:86`, `Create` `:106`, `Update` `:133`, `Delete` `:156`. |
> | Two 400 shapes (R-24 open) | **One.** `this.ValidationProblemResult()` `:65,:109,:136`, `this.NotFoundProblem(...)` `:90,:167`, `this.BusinessRuleProblem(...)` `:113,:140,:163`. Business refusal is **409**, not 400. |
> | No screen-right check (R-03 open) | Closed. `[RequireScreen("Currency")]` `:21` + `[RequireRight(...)]` `:57,:81,:100,:123,:150`. |
> | `record PagedCurrencyResponse` | Deleted. The shared `Contracts/PagedResult.cs` is the envelope; generated as `CurrencyVMPagedResult`. |
> | `pageSize` default **10** | **20** — `Contracts/PagedQuery.cs:46`; max 100 `:55,:65`. |
> | *"no sort parameter … disable the grid's sort affordances"* (`:502`, and the negative result at `:662`) | **Sorting exists.** `PagedQuery.cs:43,:70-76` (`?sort=`, comma-separated, `-` = descending); `CurrencyQuery.cs:47` allow-lists `currId, currName, currSub, symbol, isSystemDefined, createdBy, createdDate`; an unknown field is a 400 (`PagedQuery.cs:96-97`). **The affordances must be enabled.** |
> | `fromDate`/`toDate` are `string?` | `DateTime?` — `CurrencyQuery.cs:63-69`. New cross-field rule: `fromDate` on or before `toDate`, keyed against **both** wire names (`:77-81`), so one message maps onto two controls. |
> | Five endpoints | **Eight.** `Controllers/CurrencyExcelController.cs` adds `GET /export`, `GET /import-template`, `POST /import` under the same `[Tags("Currency")]`, so the generated client folds all eight into one class. Import is out of scope — do not wire it silently. |
> | Screen seed at `ApplicationDbContext.cs:1156` | `:1155`. The controller's own comment (`CurrencyController.cs:16`) is the correct one. |
> | `app-page-header` exists (`:239`, `:510`) | **It does not.** No `PageHeader`/`page-header` anywhere under `frontend/nexgen-web/src`. Grid states live in `data-grid/data-grid-states.component.ts`; the feedback primitives are `empty-state`, `error-state`, `permission-denied-state`, `busy-overlay`, `inline-alert`, `toast.service`. |
>
> Two further things the next attempt should know, neither of which this file anticipates.
> **A naming collision:** the generated client already exports a class named `CurrencyService`
> (`core/api/generated/services/currency.service.ts`), and `:404`/`:462` require a feature
> service of the same name — an import alias or a different feature-service name is needed.
> **`IsSystemDefined` is a gate, not decoration:** it refuses both update
> (`CurrencyService.cs:145-146`) and delete (`:334-335`), so a 409 is expected on
> system-defined rows *even when the user holds Edit/Delete*.
>
> Two claims in this file were re-verified and **hold**: `CurrencyVM.cs`'s annotation table is
> accurate, and its `Symbol` regex at `:23` is genuinely **unanchored**, so `"$$"` passes
> server-side and the client must not tighten it.
>
> The remaining `Existing Behavior to Preserve` text is left byte-intact below, because it is
> the record of what was believed on 2026-08-12 and `M2-C12-05` carried it through deliberately.

| Field | Value |
|---|---|
| Task ID | M2-D01 |
| Milestone | M2 — Foundation |
| Parent | — (top-level task in M2-D) |
| Type | Frontend |
| Priority | P0 |
| Status | Not Started |
| Estimate | 3 d |
| Gate | G2 |

## Objective

Ship the Currency Master as a complete, production-shaped **Angular** feature against the
existing `/api/v1/currencies` endpoints — list with server paging, filtering and Excel
export; create; edit; delete with the server's refusal message surfaced verbatim; all of it
gated by the permissions the API returns, and all of it refused by the **API** when the
caller lacks the right.

This is the **first vertical slice**: the first time login → tenant resolution → generated
TypeScript client → server-side authorization → `ProblemDetails` error contract →
`app-data-grid` → typed Reactive Form → mutation → explicit `refresh()` runs end-to-end for a
real screen.

The Blazor Currency screens keep working, untouched, against the same database.

## Scope

- The feature folder `frontend/nexgen-web/src/app/features/masters/currency/`, lazily routed.
- A list page on the shared `app-data-grid`: server paging, the four supported filters, the
  column set, the four states, and the shared server-side Excel export.
- A create/edit form in the shared `app-drawer`, route-addressable, on a typed Reactive Form.
- Delete with `app-confirm-dialog`, surfacing the server's refusal verbatim.
- Permission gating on screen `"Currency"`, failing closed.
- Component tests and two Playwright e2e specs.
- The *slice review* written back into KB-050.

## Out of Scope

- **Any change under `V.SMART/`.** The controller, service, ViewModel and Razor pages are
  read-only here. A needed change is a **finding to report**, not an edit.
- **Any shared-primitive change.** `app-data-grid`, `app-form-field`, `app-drawer` and friends
  belong to `M2-C04`/`M2-C05`. Raise a gap against those tasks.
- **Hand-writing an API type or a validator shape.** Both come from the generated client
  (`M2-B10`).
- **A second export path.** `M2-C05-03`'s server-side export is the only one.
- **Any schema change**, and any change under `frontend/vsmart-erp/`.

## Why This Task Exists

**M2-D is the vertical slice that de-risks the project, and M2-D01 is its first cut.**
Everything in M2-A, M2-B and M2-C up to this point has been built in isolation and proved
against nothing. This task is where the independently-built pieces meet. If the
seams do not fit — if the generated client is awkward to use, if `ProblemDetails` does not
carry enough for a useful error toast, if the permission map from `GET /api/v1/me` does not
line up with what the grid needs to hide, if server paging and `app-data-grid` disagree
about parameter names — **this is where it shows.**

That is the entire point of choosing Currency. It is deliberately the smallest real
module in the system: five endpoints, four editable fields, no child collections, no
document numbering, no stock, no tax. If the pattern is awkward here, it is awkward for a
reason that has nothing to do with domain complexity, and it is **far cheaper to change it
here than after sixty more controllers and forty more screens have copied it.**

Concretely it serves:

- **Gate G2**, which requires Currency **and** Customer Master working in the SPA with
  login, tenant resolution, permission-gated CRUD, server paging, validation, error
  contract and Excel export ([KB-080 §9](../README.md#exit-gate--g2)).
- **R-03** (authorization enforced only in the UI) — this task proves the fix by
  demonstrating that a rightless user is refused by the API, not merely hidden from in the
  UI.
- **R-24** (no API error contract) — the two different 400 shapes that
  `CurrencyController` emits today are consumed here through one uniform client-side error
  handler; if `core/http/error.interceptor.ts` needs a special case, R-24 is not actually
  closed.
- **A1, A5, B5, B9, B10** from [KB-041](../../api/api-readiness-assessment.md) — this is
  their joint acceptance test.

**It is also the control for M2-D02.** Currency is the easy case: `ICurrencyService`
already has `GetByIdAsync`, `CreateAsync` and `UpdateAsync`, so `CurrencyController` wraps
it with **zero service changes** (Confirmed —
`ICurrencyService.cs:12-19`, `CurrencyController.cs:41-95`). Customer is the real case:
`ICustomerService` has no write path at all. Doing Currency first establishes what "the
pattern working" looks like, so that when Customer needs an extraction (M2-D02-01) it is
obvious that the extraction — not the pattern — is the new variable.

## Prerequisites

All three hard dependencies must be **Completed**, not merely started:

- **M2-C05-03** — `app-data-grid` empty / loading / error states **and export**. Without
  export there is no Excel export, and G2 names it explicitly.
- **M2-A02** — `[RequireScreen]`/`[RequireRight]` applied to `CurrencyController` **plus
  its denial tests**. Without it the API has no screen-right check and the core
  demonstration of this task is impossible.
- **M2-B10** — OpenAPI polish + TypeScript client generation in CI. The client is
  **generated, never hand-written** (ADR-002 §6, ADR-007).

Transitively required and worth confirming before starting, because each is a silent
blocker:

| Task | Why it must already be done |
|---|---|
| M2-C01 | Created `frontend/nexgen-web/` — the Angular workspace. **Completed, merged `2dd4e53`** |
| M2-C02 | Login, refresh, functional route guards, `PermissionService` |
| M2-C03 | App shell — header, permission-filtered sidebar, ⌘K |
| M2-C04-01/-02/-03 | Tokens/theme, form controls + `app-form-field` validation display, modal/drawer/toast/states |
| M2-C05-01 | Server-paged `app-data-grid` core |
| M2-A04, M2-A05 | Refresh tokens; tenant resolution for a cross-origin SPA + real CORS |
| M2-A06 | Exception middleware → `ProblemDetails` + `traceId` |
| M2-A07 | `GET /api/v1/me` — user, tenant, role, full rights |
| M2-B01 | `/api/v1` versioning — the routes below assume it has landed |
| M2-B02, M2-B03 | Paging/sort/filter contract; codified controller template |
| M2-B07 | `AddVSmartDomain()` — the DI precondition for every controller |

If any of these is incomplete, **stop and report Blocked**. Do not stub the missing piece.

## Dependencies

| Dependency | Class | Note |
|---|---|---|
| M2-C05-03 — `app-data-grid` states + export | Hard | Supplies the grid this screen is built from, including the Excel export G2 requires. |
| M2-A02 — filter applied to `CurrencyController` + denial tests | Hard | Supplies the server-side right check. This task consumes and demonstrates it; it does not implement it. |
| M2-B10 — TypeScript client generated in CI | Hard | The only sanctioned source of API types. Hand-writing a `Currency` interface is a task failure. |
| M2-C02 — auth + `PermissionService` | Hard | The `requireScreen()` guard, `*appHasRight`, and the rights map. |
| M2-A07 — `GET /api/v1/me` | Hard | Where the Currency screen's rights come from. |
| M2-A06 — `ProblemDetails` | Hard | The single error shape this task's error handling assumes. |
| M2-B01 — `/api/v1` | Hard | Route prefix. |
| M2-B02/-B03 — paging contract + template | Information | The query-parameter names and the paged response envelope. |
| M2-C11 — the Angular pilot's disposition | Information | `frontend/vsmart-erp/src/app/features/currency/` is **prior art to read, not code to copy**. Under ADR-007 the pilot is a *baseline* whose useful parts KB-050 has already absorbed; what `M2-C11` is still *for* is **Q-38**, unanswered. Do not wait on it, and do not import from it. |
| M2-D02-01 — Customer `@code` triage | Testing | Consumes this task's conventions. Awkwardness found here must be fixed here, before Customer copies it. |
| M2-A03 — permission-matrix harness | Testing | The CI gate that proves the API-side refusal continuously; this task proves it once, by hand and in an e2e test. |

## Relevant Documentation

| Doc | Path | Why |
|---|---|---|
| KB-083 | `docs/kb/execution/prompt-template.md` | Generation rules + the **verified** command tables, backend and frontend |
| KB-080 §9 | `docs/kb/execution/README.md` | M2, and G2's exact wording |
| ADR-007 | `docs/kb/decisions/ADR-007-angular-stack.md` | The stack, and the rules that bind this task |
| ADR-002 | `docs/kb/decisions/ADR-002-rest-api-layer.md` | §2 contract conventions, §4 error contract, §6 generation |
| ADR-004 | `docs/kb/decisions/ADR-004-server-side-authorization.md` | §3 — client-side checks are UX affordances only |
| KB-050 | `docs/kb/frontend-new/react-architecture.md` | **Project structure**, data-fetching conventions, permission rendering, error handling |
| KB-051 | `docs/kb/frontend-new/design-system.md` | The modal-versus-drawer rule; no generic error toasts |
| KB-052 | `docs/kb/frontend-new/feature-mapping.md` | Currency's row: `masters/currency`, List + Drawer form, complexity **Low ⭐ start here** |
| KB-053 | `docs/kb/frontend-new/page-map.md` | Route conventions + the Currency row |
| KB-040 | `docs/kb/api/api-overview.md` | The as-is Currency endpoints, verbatim |
| KB-041 | `docs/kb/api/api-readiness-assessment.md` | Gaps A1, A5, B5, B9, B10 |
| KB-013 | `docs/kb/architecture/auth-and-permissions.md` | The `UserRight × Screens` model |
| KB-060 | `docs/kb/risks/technical-debt-register.md` | R-03, R-24 |
| KB-003 | `docs/kb/investigation-registry.md` | INV-008, INV-021, INV-004 |

## Investigation Requirements

**Reuse. No new investigation is expected.**

- **INV-008** (existing API surface) is **Complete** → [KB-040](../../api/api-overview.md).
  Reuse it for the Currency contract; cite the doc_id. Do not re-derive the endpoint list.
- **INV-021** (Angular pilot: scope and value) is **Complete** → KB-015. Reuse it. Read
  `frontend/vsmart-erp/src/app/features/currency/` once, for prior art, and move on.
- **INV-004** (auth and screen rights) is **Complete** → KB-013. Reuse it.

**One thing must be verified, not assumed:** the exact shape of the endpoints *after*
M2-A02, M2-A06, M2-B01 and M2-B02 have all modified them. This file's *Existing Behavior to
Preserve* section describes the **pre-M2-A/B** state of `CurrencyController`, because
that is what exists today. Read the controller as it actually is when you run, generate the
client from the live OpenAPI document, and record any divergence from what this file
predicts. If something diverges materially, that is a finding worth writing down — it means
the contract chain moved without this task file being regenerated.

## Relevant Existing Code

| Area | Path | Status |
|---|---|---|
| Angular workspace | `frontend/nexgen-web/` | **Exists** — created by `M2-C01`, merged `2dd4e53` |
| Currency feature | `frontend/nexgen-web/src/app/features/masters/currency/` | **TO BE CREATED by this task** |
| Generated client | `frontend/nexgen-web/src/app/core/api/generated/` | Created by M2-B10; **never hand-edited** |
| Shared components | `frontend/nexgen-web/src/app/shared/components/` | `data-grid/`, `overlay/` (`app-drawer`, `app-confirm-dialog`), `app-page-header`, `app-form-field`, `app-empty-state`, `app-error-state` — from M2-C04/M2-C05 |
| Auth and permissions | `frontend/nexgen-web/src/app/core/auth/` | `permission.service.ts`, `auth.guard.ts`, `has-right.directive.ts` — from M2-C02 |
| HTTP layer | `frontend/nexgen-web/src/app/core/http/` | `error.interceptor.ts` — the **only** place a problem body is parsed |
| API controller | `V.SMART/V.SMART.Api/Controllers/CurrencyController.cs` | Exists (98 lines). **Read-only for this task.** |
| Service | `.../BusinessService/MasterService/AccountsService/CurrencyService.cs` | Exists (249 lines). **Read-only.** |
| ViewModel | `.../ViewModels/MasterViewModel/AccountsViewModel/CurrencyVM.cs` | Exists (36 lines). **Read-only.** The validator source of truth. |
| Blazor pages | `V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Currency_Pages/` | `CurrencyList.razor` (779 lines, `@code` at `:247` = 533 lines), `CurrencyUpsert.razor` (266 lines, `@code` at `:133` = 134 lines). **Read for behaviour; must not change.** |
| Angular prior art | `frontend/vsmart-erp/src/app/features/currency/` | `currency-list/`, `currency-form/`, `currency.service.ts`, `models/currency.model.ts`. **Reference only. Not imported, not copied wholesale.** |

> **The pilot is prior art, and it is the one place a shortcut is tempting.** It is Angular, it
> already has a Currency feature, and it would compile. Do not lift it: it eagerly imports its
> routed components, stores its token in `localStorage` and hard-codes its API base URL — three
> defects KB-050 names explicitly (§ *The Angular pilot — what to keep, what not to copy*, and
> § *Environment configuration — a defect in the pilot, not a pattern*). Take its **filter and
> column choices**, which are product decisions made by the domain team; take nothing else.

## Existing Behavior to Preserve

Verified 2026-08-12. **Re-verify every line number.** Note that M2-A02, M2-A06, M2-B01 and
M2-B02 will all have modified `CurrencyController.cs` before this task runs.

### The controller as it stands today (Confirmed)

`V.SMART/V.SMART.Api/Controllers/CurrencyController.cs` — 98 lines, and it is **the
controller template's proof**:

| Line(s) | Fact |
|---|---|
| `:8-10` | `[ApiController]`, `[Route("api/currencies")]`, `[Authorize]` |
| `:15-18` | Constructor takes `ICurrencyService` and nothing else |
| `:20-24` | `record PagedCurrencyResponse(List<CurrencyVM> Items, int TotalCount, int PageNumber, int PageSize)` |
| `:26-47` | Paged `GET`: `pageNumber` (1), `pageSize` (10), and four optional string filters packed into a `Dictionary<string, object>` and handed to `SearchWithDynamicFilterAsync` |
| `:49-56` | `GET {id:int}` → 200 `CurrencyVM` or 404 `{ message = "Currency not found." }` |
| `:58-69` | `POST` → `ValidationProblem(ModelState)` on invalid model, `BadRequest(new { message })` on service refusal, else **201 + `Location`** via `CreatedAtAction` |
| `:71-82` | `PUT {id:int}` → same two 400 shapes, else 200 |
| `:84-96` | `DELETE {id:int}` → `CanDeleteCurrencyAsync` guard first: refusal is `BadRequest(new { message })`; missing row is 404; success is 204 |

Three things about it matter to this task:

1. **It wraps `ICurrencyService` with zero service changes.** All six members it needs
   already exist (`ICurrencyService.cs:12-19`). This is what "the backend is extended,
   never rewritten" looks like when the service happens to be complete.
2. **It has two different 400 shapes** — `ValidationProblem(ModelState)` at `:62` and
   `:75`, versus a bare `{ message }` at `:66`, `:79` and `:89`. That is **R-24**.
   **M2-A06 standardises both to `ProblemDetails` before this task runs.** This task must
   consume exactly one shape. If it finds it still needs to branch on the body shape,
   R-24 is not closed and that is a reportable finding.
3. **It has no screen-right check** — only a bare `[Authorize]`. That is **R-03**. Today a
   user whose `UserRight` row for `Currency` has every right `false`, and for whom the
   Blazor UI hides the screen entirely, **can create, edit and delete currencies through
   this API** (Confirmed, KB-040, ADR-004 Context). **M2-A02 adds `[RequireScreen("Currency")]`
   and per-action `[RequireRight(...)]` before this task runs.**

By the time M2-D01 executes, the routes are `/api/v1/currencies` (M2-B01), errors are
`application/problem+json` (M2-A06), and every action carries a right (M2-A02).

### The validation contract (Confirmed)

`V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/AccountsViewModel/CurrencyVM.cs` — 36
lines — carries `DataAnnotations`, so `ModelState` validation already works server-side:

| Field | Annotations | Line |
|---|---|---|
| `CurrId` | none (int, the key) | `:12` |
| `CurrName` | `[Required("Please Enter Currency Name")]`, `[StringLength(100, "Currency Name cannot exceed 100 characters")]` | `:14-16` |
| `CurrSub` | `[Required("Please Enter Sub Currency Name")]`, `[StringLength(100, "Sub Currency Name cannot exceed 100 characters")]` | `:18-20` |
| `Symbol` | `[Required("Please Enter Symbol")]`, `[RegularExpression(@"[$€₹¥£₩₿₽]", "Only valid currency symbols are allowed (₹, $, €, £, ¥, ₩, ₿, ₽)")]` | `:22-25` |
| `IsSystemDefined` | `bool`, defaults `false` | `:27` |
| `CreatedBy` / `CreatedDate` / `ModifiedBy` / `ModifiedDate` | none | `:29-33` |

**These same constraints must be mirrored by the form's `Validators`** — same lengths, same
symbol set, same messages. The server stays authoritative: the client validators exist to
give immediate feedback, never to decide. Where M2-B10 generates validator shapes from
OpenAPI, use the generated shape and extend it; do not hand-roll a parallel one — ADR-007
makes generated validator shapes the rule, not a preference.

Note the regex is `[$€₹¥£₩₿₽]` — an unanchored single-character class, so
`"$$$"` and `"a$b"` both **pass** server-side. Mirror the behaviour exactly; do **not**
"fix" it to `^[...]$` on the client, because that would make the client stricter than the
server and produce a form that rejects a value the API would accept. Record it as a
finding.

### Screen identity (Confirmed)

`Currency` is `ScreenCode` **5** —
`V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs:1156`:
`new Screens { Id = 5, ScreenCode = 5, ScreenName = "Currency", IsPrintRequired = false }`.
That string is what `[RequireScreen("Currency")]` matches and what the rights map from
`GET /api/v1/me` is keyed by; the keys are **ordinal and case-sensitive**
(KB-050 § Permission-based rendering). ADR-004's Consequences warn that screen names are
free-text and **a typo silently denies** — verify the exact string, do not retype it from
memory.

### Blazor behaviour worth matching (Confirmed)

- `Pages/Master_Module_pages/Currency_Pages/CurrencyList.razor` — 779 lines, `@code` opens
  at `:247` (533 lines, 68.4 %).
- `Pages/Master_Module_pages/Currency_Pages/CurrencyUpsert.razor` — 266 lines, `@code`
  opens at `:133` (134 lines, 50.4 %).

**Do not modify either file.** Read them to match filter semantics, column set and the
delete confirmation flow. Currency has no `@code` extraction task in M2 — its service was
already complete, which is exactly why it was chosen to go first.

## Business Rules

**None specific to Currency exist in [KB-030](../../business-rules/business-rule-inventory.md)**
(Confirmed — no `BR-CUR-*` id). This task touches no business behaviour and must not
create any. Two general rules bind it:

- **BR-AUTH-002** — screen rights are deny-by-default. A missing `UserRight` row means the
  rights map has **no key** for that screen, and the SPA must fail closed: unknown right ⇒
  hidden/disabled.
- **BR-CALC-001 / BR-STK-001** are not engaged by Currency, but ADR-007's rule that money and
  stock arithmetic stay server-side still binds as a matter of habit: no ERP arithmetic in
  TypeScript.

## Target Result

1. A Currency feature folder following KB-050's convention at
   `frontend/nexgen-web/src/app/features/masters/currency/`, containing `currency.routes.ts`,
   `currency.service.ts`, `models/`, `pages/` and `components/`, importing **only** from
   itself, `shared/` and `core/`.
2. Routes registered per [KB-053](../../frontend-new/page-map.md) and **lazily loaded**:
   `/masters/currencies`, `/masters/currencies/new`, `/masters/currencies/:id`.
3. A list page: `app-data-grid` with **server** paging, the four filters the API supports,
   the column set, empty/loading/error states, and **Excel export** — all from M2-C05-03.
4. A create/edit form on a **typed Reactive Form**, rendered per KB-052 in the shared
   `app-drawer`. Resolve the KB-052/KB-053 conflict explicitly — KB-052 says "List + Drawer
   form", KB-053 assigns dedicated routes — by making the drawer **route-addressable**:
   navigating to `/masters/currencies/new` opens it over the list. Record the decision.
5. Delete with `app-confirm-dialog`, surfacing the server's refusal message **verbatim** — no
   generic "Cannot delete" (ADR-002 §4; KB-051 forbids generic error toasts).
6. Permission gating driven by the rights map: no Create right ⇒ no "New" control; no Edit
   ⇒ read-only form; no Delete ⇒ no delete action; no View ⇒ the route guard refuses. All
   of it **UX affordance only** (ADR-004 §3).
7. **The API refuses independently.** A user with no Currency rights receives 403 from
   `/api/v1/currencies` even when calling it directly, proven by an automated test.
8. All API types come from the **generated** client. A grep for a hand-written
   `interface Currency` finds nothing outside `core/api/generated/`.
9. Component tests (`@testing-library/angular` + `provideHttpClientTesting`, run by
   `npm run test:ci`) and one Playwright e2e covering the CRUD path plus one covering the
   permission-denied path.
10. The Blazor Currency screens are untouched and still work against the same database.

## Implementation Requirements

1. **Read the knowledge base** listed above, then `docs/kb/investigation-registry.md`.
   Reuse INV-008, INV-021 and INV-004; do not re-derive them.
2. **Confirm every prerequisite has landed.** Check that `GET /api/v1/me` returns a rights
   map, that the generated client exists under `core/api/generated/`, and that
   `CurrencyController` carries `[RequireScreen]`/`[RequireRight]`. If any is missing,
   **stop and report Blocked.**
3. **Read the live contract.** Fetch the OpenAPI document from the running API and compare
   it against this file's *Existing Behavior to Preserve* section. Record every divergence.
   Regenerate the TypeScript client if CI's copy is stale.
4. **Read the prior art once** — `frontend/vsmart-erp/src/app/features/currency/`
   (`currency.service.ts`, `currency-list/`, `currency-form/`, `models/currency.model.ts`).
   Take the *filter and column choices*, not the code. Note in your report anything the
   pilot got right that this feature should keep. **Change nothing under
   `frontend/vsmart-erp/`.**
5. **Scaffold the feature folder** per KB-050:
   - `currency.routes.ts` — the lazily-loaded route tree for the three routes;
   - `models/` — re-exports from the generated client **only**;
   - `currency.service.ts` — an `@Injectable` holding signals exposed via `asReadonly()`,
     with an explicit `refresh(query)` plus `getById`, `create`, `update` and `remove`.
     **No query-cache library and no cache-key graph** — ADR-007 removed it; a mutation is
     followed by an explicit `refresh()`;
   - `pages/currency-list/` and `components/currency-form-drawer/`.
6. **Hold grid state in the URL.** Page, size, sort and the four filters live in
   `ActivatedRoute` query params, per KB-050's data-fetching rules, so a filtered grid is
   shareable and survives a reload. `DataGridQueryState` from M2-C05-01 is the mechanism;
   do not invent a second one.
7. **Build the list page** on `app-data-grid`: server paging wired to the API's
   `pageNumber`/`pageSize` and the paged envelope; the four filters
   (`currName`, `createdBy`, `fromDate`, `toDate`); columns `CurrName`, `CurrSub`,
   `Symbol`, `IsSystemDefined`, `CreatedBy`, `CreatedDate`; empty/loading/error states from
   M2-C05-03; Excel export from M2-C05-03. **Do not build a second export path**, and do not
   generate a spreadsheet in the browser.
8. **Build the form** as a typed `FormGroup` with `Validators` mirroring `CurrencyVM`'s
   annotations exactly — including the unanchored symbol regex and the verbatim messages —
   rendered through `app-form-field` inside `app-drawer`, reachable at
   `/masters/currencies/new` and `/masters/currencies/:id`. On submit, call the service; on
   success `refresh()` the list and close; on failure map `ProblemDetails.errors` onto the
   form controls **by key**, showing a summary only for keys that match no control
   (KB-050 § Error handling, the 400 row).
9. **Build delete**: `app-confirm-dialog`, then the service call. On the business refusal
   (409 after M2-A06, or 400 before it), show `ProblemDetails.title` **verbatim** in a toast.
   Do not substitute wording, do not prefix, do not translate, do not truncate.
10. **Wire permissions** from `PermissionService.forScreen('Currency')`. Gate the route with
    `requireScreen('Currency', 'view')` (M2-C02), the New control with
    `*appHasRight="'Currency'; right: 'create'"`, and the form's editability with a
    `computed()` over the same signal. Fail closed: if the screen key is absent from the map,
    every right is `false` (BR-AUTH-002).
11. **Handle errors uniformly.** `core/http/error.interceptor.ts` is the single place a
    problem body is parsed (KB-050 § Error handling); the Currency feature adds no bespoke
    error branching, and **branches on `type`, never on `title`**. If you find yourself
    needing a special case, **stop and report it** — it means R-24 is not closed.
12. **Do not touch money arithmetic.** Currency has none. If a future field needs it,
    `decimal.js` behind M2-C10's `Money`/`Qty` types is the only sanctioned route.
13. **Write the tests** listed under *Testing Requirements*.
14. **Verify the Blazor app still works** against the same tenant database: open
    `/currency` in Blazor, confirm a currency created through the SPA appears, and vice
    versa. Both UIs, one database — that is the strangler-fig contract.
15. **Record the seams.** Write a short *Slice review* section into
    [KB-050](../../frontend-new/react-architecture.md) — or a new note it links — stating
    what fitted, what did not, and what M2-D02 must do differently. This is the deliverable
    that justifies the slice existing.
16. Run the verification commands, review the diff, update documentation, commit on the
    task branch. Do not merge.

### Files Expected to Change

- `frontend/nexgen-web/src/app/app.routes.ts` — register the lazily-loaded Currency routes
- `docs/kb/frontend-new/react-architecture.md` (KB-050) — the slice review
- `docs/kb/api/api-overview.md` (KB-040) — the post-M2-A/B Currency contract
- `docs/kb/execution/tasks/M2-D01.md` (this file)

### Files Expected to Be Created

Under `frontend/nexgen-web/src/app/features/masters/currency/` — **TO BE CREATED**:

- `currency.routes.ts`
- `currency.service.ts`, `currency.service.spec.ts`
- `models/index.ts` — re-exports from the generated client
- `pages/currency-list/currency-list.component.ts` / `.html` / `.spec.ts`
- `components/currency-form-drawer/currency-form-drawer.component.ts` / `.html` / `.spec.ts`
- `a11y.spec.ts`
- An e2e spec under `frontend/nexgen-web/e2e/`

Filenames follow the conventions `M2-C01` and `M2-C04` established — `.component.ts` naming
(`--file-name-style-guide=2016`) and the `app` selector prefix enforced by
`eslint.config.js`. Match them; do not invent a competing one.

### Files That Must Not Change

- `V.SMART/V.SMART.Api/**` — the controller is finished by M2-A02/M2-A06/M2-B01. If it
  needs a change, that is a **finding to report**, not a change to make.
- `V.SMART/V.SMART.Shared/**` — no service, ViewModel, entity or Razor change. In
  particular `Pages/Master_Module_pages/Currency_Pages/*.razor` must be byte-identical.
- `V.SMART/V.SMART.Shared/Migrations/**` — no schema change.
- `frontend/vsmart-erp/**` — the Angular pilot is read-only prior art.
- `frontend/nexgen-web/src/app/core/api/generated/**` — generated; never hand-edited.
- `frontend/nexgen-web/src/app/shared/components/**` — `app-data-grid` and friends belong to
  M2-C04/M2-C05. If Currency needs a grid change, raise it against those tasks.

## API Changes

**None. This task creates and modifies no endpoint.** It is the first *consumer* of the
Currency contract. Documented below is the contract it consumes, as it will exist after
M2-A02, M2-A06, M2-B01 and M2-B02.

| Aspect | Value |
|---|---|
| Method / Route | `GET /api/v1/currencies` · `GET /api/v1/currencies/{id:int}` · `POST /api/v1/currencies` · `PUT /api/v1/currencies/{id:int}` · `DELETE /api/v1/currencies/{id:int}` |
| Request | List: `pageNumber` (default 1), `pageSize` (default 10), `currName`, `createdBy`, `fromDate`, `toDate`. Write: `CurrencyVM` as JSON — the existing ViewModel, unchanged (ADR-002 §2). |
| Response | List: `{ items, totalCount, pageNumber, pageSize }`. Get/Create/Update: `CurrencyVM`. Create also returns 201 + `Location`. Delete: 204. |
| Authentication | JWT Bearer. **Token storage is `M2-C02`'s decision**, recorded in KB-050 § *Token storage is an open decision owned by `M2-C02`*; this task consumes whatever that task settled on and stores nothing itself. The pilot's `localStorage` approach is XSS-exposed and is not the default. |
| Authorization | `[RequireScreen("Currency")]` on the controller + `[RequireRight(View/Create/Edit/Delete)]` per action, added by M2-A02. Screen name is the seeded string at `ApplicationDbContext.cs:1156`. Deny by default. |
| Tenant context | Resolved from the JWT `TenantId` claim (ADR-002 §5). The SPA supplies `tenant` at login only. `TenantProvider`/`TenantDbContextFactory` are unchanged. |
| Error model | `application/problem+json` everywhere (M2-A06). 400 = validation, `errors` keyed by field. 401 = unauthenticated. 403 = screen right denied, carrying `screen` and `right` extensions. 404 = not found. **409 = business-rule refusal, `title` carrying the service message verbatim.** 500 = `traceId` only. Branch on `type`, never on `title`. |
| Paging | Server-side, per M2-B02's contract. The client never pages in memory. |
| Filtering | The four supported filters only. The API packs them into `Dictionary<string, object>` internally; the client sees typed query parameters. |
| Sorting | **Verify against M2-B02's contract when you run.** `CurrencyController` as of 2026-08-12 exposes **no** sort parameter (Confirmed, `:26-33`), and `CurrencyService` orders internally. If M2-B02 added sorting, wire it; if not, the grid's sort affordances must be disabled rather than sorting the current page in memory — a page-local sort over server paging is a correctness bug. |
| Idempotency | n/a — Currency creates no document number. Idempotency keys are C5/R-12 work on document-create endpoints. |

## Frontend Changes

| Aspect | Value |
|---|---|
| **Route** | `/masters/currencies` (list), `/masters/currencies/new` (create), `/masters/currencies/:id` (edit) — [KB-053](../../frontend-new/page-map.md) Masters — Accounts. Registered with `loadChildren` on `currency.routes.ts`, so the feature is a lazy chunk. Legacy `/currency`, `/currency/create`, `/currency/update/{CurrId:int}` remain served by Blazor and are **not** proxied or redirected by this task. |
| **Component** | Standalone `CurrencyListComponent` (list + filters + export) and `CurrencyFormDrawerComponent` (create/edit), both `OnPush` in a **zoneless** workspace, built from `app-data-grid`, `app-page-header`, `app-form-field`, `app-drawer`, `app-confirm-dialog`, `app-empty-state` and `app-error-state`. Per KB-052: "`masters/currency` List + Drawer form · **Low ⭐ start here**". |
| **API integration** | A typed `CurrencyService` over `HttpClient`, calling the **generated** client, holding `list` / `loading` / `error` signals exposed `asReadonly()`, with an explicit `refresh()`. **No query-cache library and no invalidation graph** — a mutation calls `refresh()`. No hand-written types. |
| **State** | Server state in the feature service's signals. Grid query state in the URL (`ActivatedRoute` query params) via `DataGridQueryState`. Auth, tenant and rights stay in `core/auth`'s `PermissionService`. The Currency feature introduces **no new global service**. |
| **Validation** | A typed Reactive `FormGroup` with `Validators` mirroring `CurrencyVM`'s `DataAnnotations` exactly — `CurrName` required ≤100, `CurrSub` required ≤100, `Symbol` required matching the **unanchored** `[$€₹¥£₩₿₽]` — with the server's messages verbatim, rendered through `app-form-field`. Server `ProblemDetails.errors` are mapped back onto controls by key. The server remains authoritative. |
| **Permissions** | From `GET /api/v1/me`, keyed on screen `"Currency"`. Route: `canActivate: [requireScreen('Currency', 'view')]`, resolving to the permission-denied surface **inline**, not a redirect. Controls: `*appHasRight`. Imperative: `PermissionService.forScreen('Currency')` inside a `computed()`. No Create ⇒ no New control. No Edit ⇒ read-only form. No Delete ⇒ no delete action. **Fail closed** when the screen key is absent. UX affordance only — the API re-checks every request (ADR-004 §3). |
| **Loading state** | `app-data-grid`'s loading state from M2-C05-03: skeleton rows on first load, the previous page plus a progress bar on refetch — never a spinner on a blank page. The drawer shows a busy overlay while a mutation is in flight, with the submit control disabled. |
| **Empty state** | `app-empty-state` from M2-C04-03: distinguish "no currencies exist yet" (offer New, if the user has Create) from "no rows match your filters" (offer Clear filters). Two different messages, not one. |
| **Error state** | `app-error-state` for load failure, with retry. Toast for mutation failure. 409 refusals show `ProblemDetails.title` **verbatim**. 403 shows the inline permission-denied surface — not a redirect and not a logout — reading `screen`/`right` from the problem extensions. 500 shows the constant title plus a copyable `traceId`. |
| **Accessibility** | The grid follows M2-C05-01's ARIA `grid` contract. The drawer is a focus-trapped dialog with a labelled heading, `Esc` to close and focus restored to the trigger. Every control renders through `app-form-field`, so labels, `aria-required`, `aria-invalid`, `aria-describedby` and the polite announcement come with it. Full keyboard operation, including the delete confirmation. `angular-eslint`'s template-accessibility rules run as **errors** under `npm run lint` (`--max-warnings=0`), and `a11y.spec.ts` runs `axe` under `npm run test:ci` with no critical violations. |
| **Responsive behaviour** | Desktop-first — the users are full-time operators who work keyboard-first for whole shifts. The grid scrolls horizontally within its own container rather than reflowing; the page body never scrolls horizontally. The drawer becomes full-width below the tablet breakpoint. No mobile-specific screen is built. |

## Business Logic

**Newly introduced — presentation only, with justification.**

No ERP business logic is introduced, moved, or reimplemented. Every decision that matters —
validation outcome, duplicate refusal, delete guard, permission — is made by the server and
merely *rendered* here.

The only genuinely new logic is presentational: URL-bound grid state, form state, drawer
routing, and the mapping of `ProblemDetails` onto form controls. That is justified because it
exists nowhere today: the Blazor page holds the equivalent in `@code`, and it is
UI-framework-specific by nature, so it cannot be shared.

The client-side validators deserve an explicit note, because they *look* like duplicated
business logic. They are not: they are a **mirror** of `CurrencyVM`'s `DataAnnotations`,
generated from OpenAPI where possible, existing purely for immediate feedback. They are never
the authority, never stricter than the server, and where they would diverge — the unanchored
symbol regex — **the server's behaviour wins**.

## Testing Requirements

Component tests use `@testing-library/angular`, invoked by `npm run test:ci`
(KB-083 § Verified frontend commands). HTTP is stubbed with `provideHttpClientTesting` /
`HttpTestingController` — **do not** introduce a service-worker or DOM-level HTTP mock, and
do not add a dependency for one.

| Level | What | Command |
|---|---|---|
| Component | List renders; server paging requests the right page; filters submit as typed query params and round-trip through the URL; export calls the shared export service exactly once | `npm run test:ci` |
| Component | Each `CurrencyVM` constraint fails the form with the verbatim message | `npm run test:ci` |
| Component | `ProblemDetails.errors` map onto the correct form controls by key | `npm run test:ci` |
| Component | A 409 delete refusal shows `title` verbatim — assert the exact string, not a substring | `npm run test:ci` |
| Component | Permission gating: for each of View/Create/Edit/Delete absent, the corresponding control is absent or disabled; an unknown screen key fails closed | `npm run test:ci` |
| Component | The client is **not stricter than the server**: a `Symbol` of `"$$"` is accepted | `npm run test:ci` |
| A11y | `a11y.spec.ts` — `axe` reports no critical violations on the list and on the open drawer | `npm run test:ci` |
| E2E | Login → list → create → edit → delete, against a real API and a **non-production** tenant | `npm run e2e` |
| E2E | **A user with no Currency rights is refused by the API.** Call `/api/v1/currencies` directly with that user's token and assert **403** — not merely that the UI hid the control. This is the G2 criterion and the R-03 proof. | `npm run e2e` |
| Manual | Blazor `/currency` still lists, creates and deletes against the same tenant database; a row created in the SPA appears in Blazor and vice versa | n/a |

**No coverage-threshold command is specified**, because none is verified in KB-083. Do not
invent one.

## Acceptance Criteria

- [ ] `/masters/currencies`, `/masters/currencies/new` and `/masters/currencies/:id` are
      registered, **lazily loaded** and reachable, matching [KB-053](../../frontend-new/page-map.md).
- [ ] The feature folder follows KB-050's convention and contains `currency.routes.ts`,
      `currency.service.ts`, `models/`, `pages/`, `components/`.
- [ ] The feature imports nothing from another feature folder — verified by grep for
      `features/` imports outside `features/masters/currency/`, and by `npm run lint`
      passing with `eslint.config.js`'s `no-restricted-imports` rules in force.
- [ ] Every API type used comes from `core/api/generated/`; a repository search for a
      hand-written `interface Currency` / `type Currency` under `frontend/nexgen-web/src/`
      returns hits **only** under `generated/`.
- [ ] No file under `core/api/generated/` was hand-edited — verified against a fresh
      regeneration producing no diff.
- [ ] The list pages **on the server**: changing page issues a new request; the row count
      shown equals `totalCount`; no client-side slicing of a full result set.
- [ ] All four filters (`currName`, `createdBy`, `fromDate`, `toDate`) submit, round-trip
      and survive a page reload through the URL query string.
- [ ] Excel export is the shared `app-data-grid` export from M2-C05-03 — no second export
      path, and no spreadsheet built in the browser.
- [ ] Loading, empty-with-no-data, empty-with-filters and error states each render
      distinctly.
- [ ] The form rejects a `CurrName` over 100 characters, a missing `CurrSub`, and a `Symbol`
      outside `[$€₹¥£₩₿₽]`, with the server's messages verbatim.
- [ ] The client is **not stricter than the server**: a `Symbol` of `"$$"`, which the
      server's unanchored regex accepts, is accepted by the client too.
- [ ] Server `ProblemDetails.errors` are displayed against the named controls.
- [ ] A delete refusal displays the server's message **byte-identical** — asserted on the
      exact string in a test.
- [ ] With Create absent the New control is absent; with Edit absent the form is read-only;
      with Delete absent the delete action is absent; with View absent the route guard
      refuses **inline**, not by redirect. A screen key missing from the rights map is
      treated as all-false.
- [ ] **A rights-less user calling `GET /api/v1/currencies` directly receives 403**, proven
      by an automated test, not by inspection.
- [ ] `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run test:ci`,
      `npm run build` and `npm run e2e` all exit 0.
- [ ] `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` reports 0 errors.
- [ ] The full `git diff` shows **no** change under `V.SMART/` and **no** change under
      `frontend/vsmart-erp/`.
- [ ] The Blazor Currency screens still work against the same tenant database, verified
      manually and recorded.
- [ ] A *slice review* is recorded in KB-050 stating what fitted, what did not, and what
      M2-D02 must do differently.
- [ ] Every divergence between the live OpenAPI contract and this file's *Existing
      Behavior to Preserve* section is recorded.

## Verification Commands

From the repository root:

```bash
dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj
```

```bash
git status --porcelain
```

**Read the full `git diff` before committing** to confirm that `V.SMART/**` and
`frontend/vsmart-erp/**` are untouched, and search `frontend/nexgen-web/src/` for a
hand-written `interface Currency` / `type Currency` — hits outside `generated/` are a task
failure. Neither a `git diff --stat` shorthand nor an ad-hoc `grep` has a row in KB-083, so
neither is quoted here as a verified command.

From `frontend/nexgen-web/` — every one of these appears verbatim in
[KB-083 § Verified frontend commands](../prompt-template.md#verified-frontend-commands):

```bash
npm ci
npm run typecheck
npm run lint
npm run format:check
npm run test:ci
npm run build
npm run e2e
```

> `npm run e2e` needs `npx playwright install chromium` once per machine and starts
> `ng serve` itself on 127.0.0.1:4300. `npm start` / `ng serve` is **not** a verified
> CI-checkable command — do not put it in an acceptance criterion.

## Documentation Updates

| Document | Change | Frontmatter |
|---|---|---|
| `docs/kb/frontend-new/react-architecture.md` (KB-050) | Add a **Slice review — Currency (M2-D01)** section: what fitted, what did not, what M2-D02 must change. Record the KB-052-Drawer vs KB-053-routes resolution, and whether the signal-service + explicit-`refresh()` convention held up on a real screen. | bump `last_verified` |
| `docs/kb/api/api-overview.md` (KB-040) | Update the Currency section to the post-M2-A02/A06/B01 contract: `/api/v1` routes, `[RequireScreen]`/`[RequireRight]`, one `ProblemDetails` error shape. Note the unanchored `Symbol` regex as a Confirmed behaviour. | bump `last_verified`; update `api_endpoints` |
| `docs/kb/frontend-new/feature-mapping.md` (KB-052) | Mark the Currency row Delivered and reconcile the `masters/currency` folder vs `/masters/currencies` route naming discrepancy explicitly. | bump `last_verified` |
| `docs/kb/execution/tasks/M2-D01.md` (this file) | Record the Execution Record; the owner sets `status`. | bump `last_verified` |
| `docs/kb/execution/task-tracker.md` (KB-081) | Update the M2-D01 row. | n/a |
| `docs/kb/investigation-registry.md` (KB-003) | Record the negative results below. | bump `last_verified` |

### Negative results to record, so no later session re-derives them

- "`CurrencyVM`'s `Symbol` regex `[$€₹¥£₩₿₽]` is **unanchored**, so multi-character values
  pass server-side. Confirmed at `CurrencyVM.cs:23`. The client mirrors this rather than
  tightening it."
- "`CurrencyController` exposed no sort parameter as of 2026-08-12 (`:26-33`); ordering is
  internal to `CurrencyService`."
- "No `BR-CUR-*` business rule exists in KB-030. Currency has no module-specific business
  behaviour to preserve."
- Whether **one** `ProblemDetails` handler sufficed, or whether any endpoint still needed
  special-casing — if the latter, **R-24 is not closed** and that must be said plainly.
- Whether server sorting exists after M2-B02, and what was done about the grid's sort
  affordances if it does not.

If the live contract diverges materially from KB-040, **amend KB-040** rather than opening
a new investigation — INV-008 is the owner of that fact.

## Completion Conditions

- Every acceptance criterion above is objectively met, with its command output recorded.
- The slice review exists in KB-050. This task is not done when the screen works; it is done
  when what the slice *taught* is written down — that is the deliverable M2-D02 consumes.
- **Only the repository owner may set `Completed`**
  ([KB-088](../workflow.md#who-may-set-completed)).

## Git Strategy

| | |
|---|---|
| **Branch** | `migration/M2-D01-currency-end-to-end` |
| **Commit** | `M2-D01: Deliver Currency Master end-to-end in Angular` |
| **Expected changed files** | New files under `frontend/nexgen-web/src/app/features/masters/currency/` and `frontend/nexgen-web/e2e/`; `frontend/nexgen-web/src/app/app.routes.ts`; `docs/kb/frontend-new/react-architecture.md`; `docs/kb/api/api-overview.md`; `docs/kb/frontend-new/feature-mapping.md`; `docs/kb/execution/tasks/M2-D01.md` |
| **Rollback** | Revert the branch's merge commit. The change is additive frontend code only — no backend change, no schema change, no data change — so a revert removes the Angular Currency screens and leaves Blazor, the API and the database exactly as they were. Because both UIs run against one database throughout, rollback carries no data-migration problem (KB-080 §2). |

Do not merge. Do not push to `master`. Leave the branch for review.

## Execution Record (2026-08-26)

**Outcome: STOPPED — Blocked.** This is the close-out session; it wrote no feature code and
changed nothing under `V.SMART/` or `frontend/`. It reconfirms and formalizes the ⛔ banner
the implementer session recorded above the same day, and closes out the tracker, runner-state
and current-task bookkeeping the implementer session left open.

**What was actually done this session:**
- Re-read the ⛔ banner this file already carries (added by commit `55b6c4d`, attempt 1) and
  independently re-checked its four claims against `task-tracker.md` (this session, fresh
  `grep`): `M2-C02` still `Blocked` (`:157`), `M2-C03` still `Blocked` (`:162`), `M2-A04` still
  `Blocked` on `M0-04` (`:114`), `M2-A05` still `Blocked` (`:115`). None changed since the
  implementer's pass.
- Confirmed on disk that `frontend/nexgen-web/src/app/core/auth/`, `core/http/` and
  `layout/shell/` still contain only `.gitkeep` — no new commits landed on this branch or on
  `master` between the implementer's attempt and this close-out that would have changed that.
- No commands were re-run (`dotnet build`, `npm run typecheck`, etc.) — the implementer's
  session already ran and recorded `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj`
  (0 errors, 6695 warnings, baseline) in commit `55b6c4d`, and nothing under `V.SMART/` changed
  since, so re-running it would reproduce the same result without new information.
- Did not implement anything and did not start another task, per this session's instruction.

**What differed from the plan and why:** the task's own plan (Scope, Acceptance Criteria,
Verification Commands) assumes `M2-C02`/`M2-C03`/`M2-A04`/`M2-A05` exist. They do not. The
task's own *Prerequisites* section (`:160-177`) instructs stopping rather than stubbing them,
so the plan was never executed past the readiness check. This is the second and closing
attempt (attempt 1 discovered and reported the block; this session records the close-out); no
third attempt is warranted until the named prerequisite chain moves.

**Validation:** none run. The final validator verdict for this task is `"none"` — "validation
did not complete" — because there is no implementation to validate.

**Unblocking chain, most upstream first:** `M0-04` (credential rotation runbook — currently
`Blocked`, its own branch `migration/M0-04-credential-rotation-runbook` unmerged) → `M2-A04`
(refresh tokens, blocked on `M0-04`) → `M2-A05` (CORS/tenant, blocked on `M2-A04`) and, in
parallel once `M2-A04`/`M2-A07` land, `M2-C02` (auth/guards/permission store) → `M2-C03` (app
shell, blocked on `M2-C02`) → `M2-D01` becomes actually startable only once `M2-C02`, `M2-C03`,
`M2-A04` and `M2-A05` are all `Completed` and merged to `master`.

**Process finding carried forward, not re-derived here:** `Q-97` (`docs/kb/open-questions.md`)
— should the next-task selection test walk a task file's transitive *Prerequisites* table, not
just its `depends_on` list? This task is the reason the question exists; it is not this
session's to answer.
