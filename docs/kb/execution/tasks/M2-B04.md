---
doc_id: TASK-M2-B04
title: Decouple IApprovalService and the other Pages-referencing files from the Razor UI
module: execution
task_id: M2-B04
milestone: M2
parent_task: none
task_type: Backend
priority: P0
status: Needs Review
estimate: 1 wk
depends_on: [M2-B07]
source_files:
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/IBusinessService/IPlanningService/IApprovalService.cs
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/PlanningService/ApprovalService.cs
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/EInvoiceAPIService/EinvoiceDatabaseService.cs
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/InventoryService/StockAddIss_Position/StockAddIssPosition.cs
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/MaintenanceService/MaintenanceProcessService.cs
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/ReportService/Rating_Services/GetRatingsService.cs
  - V.SMART/V.SMART.Shared/Data/AccountsModule/FundTrans.cs
  - V.SMART/V.SMART.Shared/Data/AccountsModule/ReceiptsSub.cs
  - V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs
  - V.SMART/V.SMART.Shared/Data/OutSourcing/MaterialReq.cs
  - V.SMART/V.SMART.Shared/Data/SalesAndLabour/PerformaInvoice/PerformaInv.cs
  - V.SMART/V.SMART.Shared/E_Invoice/E_InvoiceHelper/JsonSerializer.cs
  - V.SMART/V.SMART.Shared/Mappings/ProductionMappingProfile/ProductionIssueAssyMap/ProductionIssuAssyMapping.cs
  - V.SMART/V.SMART.Shared/Repository/OutSourcingRepository/PurchaseSCN_Repository/PurchaseSCNRepository.cs
  - V.SMART/V.SMART.Shared/ViewModels/AccountsViewModel/FundTransFilterVM.cs
  - V.SMART/V.SMART.Shared/Pages/Planning_Module_Pages/Authorization_Pages/Authorization.razor
entities: [UserAuthority, ApprovalHistory]
api_endpoints: []
database_tables: [UserAuthority, ApprovalHistory]
business_rules: [BR-APPR-001]
confidence: n/a
last_verified: 2026-08-21
---

# M2-B04 — Decouple `IApprovalService` + the other `Pages`-referencing files

| Field | Value |
|---|---|
| Task ID | M2-B04 |
| Milestone | M2 — Foundation |
| Parent | — (this is a standalone task) |
| Type | Backend |
| Priority | P0 |
| Status | **Needs Review** *(attempt 2 of 3, 2026-08-21 — implemented, built, tested and committed on `migration/M2-B04-decouple-pages-references`. **10 of 11 acceptance criteria met; one is not checkable here** — the manual approval-workflow regression needs a running Blazor app with `UserAuthority` rows. See [Execution Record — attempt 2](#execution-record--attempt-2-2026-08-21).)* |
| Estimate | 1 wk |
| Gate | G2 |

## Task Objective

Remove every compile-time dependency from non-UI code in `V.SMART.Shared` onto the
`V.SMART.Shared.Pages` namespace, and install an automated guard that fails the build if
one is reintroduced.

After this task, `BusinessLayer/`, `Data/`, `Repository/`, `Mappings/`, `ViewModels/` and
`E_Invoice/` contain **zero** references to `V.SMART.Shared.Pages`, and the approval
workflow can be exposed over HTTP without dragging a Razor component into the API's
activation path.

## Why This Task Exists

Serves **R-11** ([KB-060](../../risks/technical-debt-register.md)) and **A6** in the P0
blocker list of [KB-041 §What must be built](../../api/api-readiness-assessment.md#p0--blockers-nothing-ships-without-these).

The headline case is a business-layer *interface* importing a Razor page type:

```
V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/IBusinessService/IPlanningService/IApprovalService.cs:8
    using static V.SMART.Shared.Pages.Planning_Module_Pages.Authorization_Pages.Authorization;
```

(Confirmed, re-verified 2026-08-12.) The concrete service repeats it at
`V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/PlanningService/ApprovalService.cs:14`.

[KB-030 BR-APPR-001](../../business-rules/business-rule-inventory.md#br-appr-001--rejection-requires-a-reason-approvals-are-audited-by-level)
records the consequence in its migration note: *"The `IApprovalService` interface currently
`using static`-imports a Razor page type — decouple first. Then enforce `UserAuthority`
server-side in the approval endpoints."* This task is that "decouple first", and it gates
the approval endpoints (KB-041 item **B8**) and M3/M4's approval work.

### The scope is larger than KB-060 says, and the fix is smaller

Two corrections the executing session must carry, both established while planning this task
and both re-verifiable in one grep:

**Correction 1 — the count is 15 files, not 14.** [KB-041](../../api/api-readiness-assessment.md)
says "14 reference `Pages`" and [KB-060 R-11](../../risks/technical-debt-register.md) says
"13 other". The actual figure on 2026-08-12 is **16 `using` directives across 15 distinct
non-UI files** (Confirmed —
`grep -rn "V.SMART.Shared.Pages" --include="*.cs" V.SMART/V.SMART.Shared/ | grep -v "/Pages/"`).
`FundTrans.cs` carries two of them. The full list is in *Existing Behavior to Preserve*.

**Correction 2 — most of these `using` directives import nothing, because the namespaces
they name declare no public types.** Scanning every one of the ten referenced page
namespaces for a type declaration
(`^\s*(public|internal)\s+(sealed |static |partial |abstract )*(class|enum|record|struct|interface)\s`)
returns hits in **exactly one** of them (Confirmed):

| Referenced page namespace | Public types declared |
|---|---|
| `Master_Module_pages.Items_Pages` | none |
| `Master_Module_pages.Store_Pages` | none |
| `Master_Module_pages.Machine_Pages` | none |
| `Master_Module_pages.Bank_Pages` | none |
| `Report_Module_Pages.PoPending_Pages` | none |
| `CashFlow_Pages.Payments_Pages` | none |
| `CashFlow_Pages.Receipt_Pages` | none |
| `OutSourcing_Module_pages.PurchaseSCN_Pages` | none |
| `ProductionModule_pages` | none *(the `public static` helpers live one level down in `ProductionModule_pages.DailyProductionLog_Pages` — `ProductionLogUpsert.razor:1596,1620,1622` — which a parent-namespace `using` does **not** import)* |
| `Report_Module_Pages.TrackReports_Pages` | **`public enum GridMode`** — `LabourTrack.razor:1262` and `SalesTrack.razor:1807` |

And the `using static` on the `Authorization` component imports **nothing at all**:
`V.SMART/V.SMART.Shared/Pages/Planning_Module_Pages/Authorization_Pages/Authorization.razor`
is 747 lines and contains **zero occurrences of the keyword `static`** (Confirmed —
`grep -n "static" <that file>` returns nothing). A `using static` on a type with no static
members imports an empty set. The `UserAuthority` parameter in
`IApprovalService.ApproveAsync` (`IApprovalService.cs:15`) comes from
`V.SMART.Shared.Data.Master.Admin_Module`, imported at `IApprovalService.cs:1` — **not**
from the Razor page.

**So the likely fix for 15 of the 16 directives is to delete them.** That is a far better
outcome than KB-060's proposed "move the shared types into `ViewModels/`", which would
create ViewModels nobody uses. The one directive that may be load-bearing is
`ApplicationDbContext.cs:84`; even there, `grep -n "GridMode" ApplicationDbContext.cs`
returns nothing, so it too is probably dead.

**Confidence discipline.** "These usings are unused" is **Inferred (high confidence)** from
the namespace scan, not Confirmed. Only the compiler can confirm it. The task is therefore
structured as *delete, then build, then reinstate only what the compiler demands* — and the
implementation steps below say so explicitly. Do **not** upgrade the classification in the
knowledge base until the build has proved it.

The task keeps its 1-week estimate despite the small expected diff, because the value is in
the **architecture test** (which must be designed, not just written) and in re-verifying the
approval workflow end-to-end after touching `IApprovalService`.

## Prerequisites

- **[M2-B07](M2-B07.md) complete.** The API must be able to resolve `IApprovalService`
  before anyone can claim the decoupling achieved anything;
  `V.SMART/V.SMART.Api/Program.cs:102` registers only `ICurrencyService` today.
- **G0 passed.**

## Dependencies

| Dependency | Class | Note |
|---|---|---|
| [M2-B07](M2-B07.md) — `AddVSmartDomain()` | Hard | Without it `IApprovalService` is not resolvable in `V.SMART.Api`, so the decoupling cannot be demonstrated. |
| INV-011 — business-rule sweep (Partial) | Information | Source of BR-APPR-001. Reuse; the approval rule is already extracted with evidence. |
| [M0-12](M0-12.md) — first test project | Soft | The architecture test needs `tests/V.SMART.Shared.Tests/` (**TO BE CREATED** by M0-12). If M0-12 has not landed, use the grep-based CI guard described in *Implementation Steps* step 8. |
| [M0-07](M0-07.md) — CI pipeline | Deployment | The guard must run in CI to have any force. If no CI workflow exists yet (`.github/` contains only `copilot-instructions.md` and `prompts/` — Confirmed 2026-08-12), record the guard as a script plus a documented CI step for M0-07 to wire in. |
| KB-041 item B8 — approval endpoints | Information | Downstream consumer. Not in this task's scope. |

## Required Existing Knowledge

- [KB-083](../prompt-template.md) — generation rules and the verified-commands table.
- [KB-080 §9](../README.md#9-m2--foundation) — M2-B sequencing.
- [KB-060 R-11](../../risks/technical-debt-register.md) — the risk, including its
  understated file count.
- [KB-041 A6](../../api/api-readiness-assessment.md) — the P0 blocker framing.
- [KB-030 BR-APPR-001](../../business-rules/business-rule-inventory.md) — the approval rule
  that must survive untouched.
- [ADR-004](../../decisions/ADR-004-server-side-authorization.md) §4 — `UserAuthority` gets
  enforced server-side in the approval endpoints. Context only; not this task.
- [KB-002](../../source-of-truth-rules.md), [KB-003](../../investigation-registry.md).

## Investigation Requirements

No scheduled investigation covers this. **INV-011** is `Partial` and already contains
BR-APPR-001 with evidence — reuse it, do not re-extract the approval rule.

The executing session must derive one thing itself, and record it: **for each of the 16
`using` directives, whether it is load-bearing.** The namespace scan above is a strong
prior; the compiler is the authority. Record the result per file in the KB-083 evidence
format, including the negative results ("deleted, build still clean" is a finding).

## Repository Areas

- `V.SMART/V.SMART.Shared/BusinessLayer/` — 5 of the 15 files.
- `V.SMART/V.SMART.Shared/Data/` — 5 files, including `ApplicationDbContext.cs`.
- `V.SMART/V.SMART.Shared/E_Invoice/` — 1 file.
- `V.SMART/V.SMART.Shared/Mappings/` — 1 file.
- `V.SMART/V.SMART.Shared/Repository/` — 1 file.
- `V.SMART/V.SMART.Shared/ViewModels/` — 1 file.
- `V.SMART/V.SMART.Shared/Pages/` — **read only.** No Razor page is edited by this task.

## Existing Behavior to Preserve

**BR-APPR-001** — *Rejection requires a reason; approvals are audited by level.*
Statement and evidence per
[KB-030](../../business-rules/business-rule-inventory.md#br-appr-001--rejection-requires-a-reason-approvals-are-audited-by-level):
`RejectAsync`/`BulkRejectAsync` take a mandatory `reason`
(`V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/IBusinessService/IPlanningService/IApprovalService.cs:16,18`);
every approve/reject writes an `ApprovalHistory` row; a user may act only on the document
types and levels granted in `UserAuthority`. **Confidence: Confirmed.**
**This task must not change any of it.** The five interface members and their exact
signatures (`IApprovalService.cs:14-18`) are frozen:

```
Task<List<ApprovalVM>> GetPendingApprovalsAsync(string type, string level, string userName);          // :14
Task<bool> ApproveAsync(ApprovalVM record, string type, string level, string userName, UserAuthority authority); // :15
Task<bool> RejectAsync(ApprovalVM record, string type, string level, string reason, string userName); // :16
Task<bool> BulkApproveAsync(List<ApprovalVM> records, string type, string level, string username);    // :17
Task<bool> BulkRejectAsync(List<ApprovalVM> records, string type, string level, string reason, string username); // :18
```

The complete inventory of directives to remove (all **Confirmed**, re-verified 2026-08-12):

| # | File | Line | Directive |
|---|---|---|---|
| 1 | `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/EInvoiceAPIService/EinvoiceDatabaseService.cs` | 6 | `using V.SMART.Shared.Pages.Master_Module_pages.Items_Pages;` |
| 2 | `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/IBusinessService/IPlanningService/IApprovalService.cs` | 8 | `using static V.SMART.Shared.Pages.Planning_Module_Pages.Authorization_Pages.Authorization;` |
| 3 | `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/InventoryService/StockAddIss_Position/StockAddIssPosition.cs` | 8 | `using V.SMART.Shared.Pages.Master_Module_pages.Store_Pages;` |
| 4 | `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/MaintenanceService/MaintenanceProcessService.cs` | 8 | `using V.SMART.Shared.Pages.Master_Module_pages.Machine_Pages;` |
| 5 | `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/PlanningService/ApprovalService.cs` | 14 | `using static V.SMART.Shared.Pages.Planning_Module_Pages.Authorization_Pages.Authorization;` |
| 6 | `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/ReportService/Rating_Services/GetRatingsService.cs` | 15 | `using V.SMART.Shared.Pages.Report_Module_Pages.PoPending_Pages;` |
| 7 | `V.SMART/V.SMART.Shared/Data/AccountsModule/FundTrans.cs` | 11 | `using V.SMART.Shared.Pages.CashFlow_Pages.Payments_Pages;` |
| 8 | `V.SMART/V.SMART.Shared/Data/AccountsModule/FundTrans.cs` | 12 | `using V.SMART.Shared.Pages.CashFlow_Pages.Receipt_Pages;` |
| 9 | `V.SMART/V.SMART.Shared/Data/AccountsModule/ReceiptsSub.cs` | 8 | `using V.SMART.Shared.Pages.CashFlow_Pages.Receipt_Pages;` |
| 10 | `V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs` | 84 | `using V.SMART.Shared.Pages.Report_Module_Pages.TrackReports_Pages;` |
| 11 | `V.SMART/V.SMART.Shared/Data/OutSourcing/MaterialReq.cs` | 3 | `using V.SMART.Shared.Pages.Master_Module_pages.Store_Pages;` |
| 12 | `V.SMART/V.SMART.Shared/Data/SalesAndLabour/PerformaInvoice/PerformaInv.cs` | 6 | `using V.SMART.Shared.Pages.Master_Module_pages.Bank_Pages;` |
| 13 | `V.SMART/V.SMART.Shared/E_Invoice/E_InvoiceHelper/JsonSerializer.cs` | 1 | `using V.SMART.Shared.Pages.Master_Module_pages.Items_Pages;` |
| 14 | `V.SMART/V.SMART.Shared/Mappings/ProductionMappingProfile/ProductionIssueAssyMap/ProductionIssuAssyMapping.cs` | 4 | `using V.SMART.Shared.Pages.ProductionModule_pages;` |
| 15 | `V.SMART/V.SMART.Shared/Repository/OutSourcingRepository/PurchaseSCN_Repository/PurchaseSCNRepository.cs` | 5 | `using V.SMART.Shared.Pages.OutSourcing_Module_pages.PurchaseSCN_Pages;` |
| 16 | `V.SMART/V.SMART.Shared/ViewModels/AccountsViewModel/FundTransFilterVM.cs` | 9 | `using V.SMART.Shared.Pages.Master_Module_pages.Bank_Pages;` |

Line numbers **must be re-verified before editing** — other M2 tasks may have touched these
files first.

Also preserve: `V.SMART.Shared` is a single assembly built with
`Microsoft.NET.Sdk.Razor` and multi-targets `net9.0-windows10.0.19041.0;net9.0`
(`V.SMART/V.SMART.Shared/V.SMART.Shared.csproj:1,4` — Confirmed). This task does **not**
split the assembly. Physical separation of `Pages/` into its own project is a much larger
change and is explicitly out of scope.

## Target Result

1. `grep -rn "V.SMART.Shared.Pages" --include="*.cs" V.SMART/V.SMART.Shared/ | grep -v "/Pages/"`
   returns **zero** hits.
2. `IApprovalService` and `ApprovalService` compile with no reference to any Razor type.
   Their five method signatures are byte-for-byte unchanged.
3. Where the compiler proves a directive **was** load-bearing, the type it provided has been
   moved to a non-UI location — `ViewModels/` for a data-shape type, or
   `Utility_Constants/` for an enum — and the Razor page now imports it from there. Based on
   the namespace scan, at most one such case is expected (`GridMode`), and possibly none.
4. An **architecture guard** exists and fails on reintroduction:
   - preferred: a test in `tests/V.SMART.Shared.Tests/` (**TO BE CREATED** by M0-12)
     reflecting over the `V.SMART.Shared` assembly and asserting that no type whose
     namespace starts `V.SMART.Shared.BusinessLayer`, `.Data`, `.Repository`, `.Mappings`,
     `.ViewModels` or `.E_Invoice` has a dependency on a type in `V.SMART.Shared.Pages`;
   - fallback if M0-12 has not landed: a committed script performing the grep in criterion 1
     and exiting non-zero on any hit, plus a documented CI step for [M0-07](M0-07.md) to wire
     in.
5. R-11 is closed in KB-060 **with the corrected file count**, and the "move types to
   `ViewModels/`" action item is amended to reflect what was actually needed.

## Implementation Steps

1. Branch: `git checkout -b migration/M2-B04-decouple-pages-references`.
2. Re-derive the inventory rather than trusting the table above — other tasks may have
   changed the files:
   `grep -rn "V.SMART.Shared.Pages" --include="*.cs" V.SMART/V.SMART.Shared/ | grep -v "/Pages/"`.
   Record the output. If it differs from 16 directives / 15 files, the difference is itself a
   finding to report.
3. Record the baseline build: `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj`
   (expect 0 errors, ~6,695 warnings).
4. **Delete all 16 directives in one pass**, then build. This is faster and more honest than
   reasoning about each in isolation: the compiler enumerates exactly which ones mattered.
5. For each compiler error the deletion produces:
   - Identify the type the file actually needed and where it is declared.
   - **If it is a data shape** (a class or record describing data), move the declaration into
     `V.SMART/V.SMART.Shared/ViewModels/` under the module's existing subfolder, and add the
     corresponding `using` to the Razor page that declared it.
   - **If it is an enum** (the expected case, `GridMode`), move it to
     `V.SMART/V.SMART.Shared/Utility_Constants/` — alongside `CommonConstants.cs` — or to
     `ViewModels/` if it is genuinely part of a ViewModel's shape. Do not create a new
     top-level folder.
   - **If it is a static method on a Razor component**, that is business logic in the UI:
     do **not** move it in this task. Record it as a `@code`-extraction item for INV-024 and
     the relevant module wave, and leave the `using` in place with a `// TODO(M3/M4 …)`
     comment naming the wave. Report it as a deviation. Based on the scan, this case is not
     expected to arise.
   - **Never** copy a type. Move it, and update every reference.
6. Special handling for `IApprovalService.cs:8` and `ApprovalService.cs:14`. These two are
   the reason the task exists, so verify explicitly rather than relying on the bulk build:
   - Confirm `V.SMART/V.SMART.Shared/Pages/Planning_Module_Pages/Authorization_Pages/Authorization.razor`
     still contains no `static` member (`grep -n "static"` → no output).
   - Confirm `UserAuthority` resolves from
     `V.SMART.Shared.Data.Master.Admin_Module` (`IApprovalService.cs:1`).
   - Delete both directives and build.
   - State in the final report, in one sentence, what the `using static` was actually
     importing. If the answer is "nothing", say so — that is the single most useful
     correction this task can hand back to KB-060.
7. Re-run the grep from step 2. It must return zero hits.
8. Build the **architecture guard**:
   - If `tests/V.SMART.Shared.Tests/` exists, add
     `Architecture/NoPagesReferenceFromDomainTests.cs` (**TO BE CREATED**). Reflect over
     `typeof(V.SMART.Shared.Data.ApplicationDbContext).Assembly`, walk each type's
     namespace, and for types in the six domain namespace roots assert that no field type,
     property type, method parameter/return type, base type or implemented interface lives
     in a namespace starting `V.SMART.Shared.Pages`. Assert on a **collected list** of
     violations, not the first one, so the failure message names every offender.
     Note honestly in a comment that reflection cannot see an *unused* `using` directive —
     that is what the grep guard is for; the two are complementary.
   - If it does not exist, create `scripts/check-no-pages-references.sh`
     (**TO BE CREATED**) running the grep from step 2 and exiting non-zero on any hit, and
     add a section to the task's documentation update describing the CI step
     [M0-07](M0-07.md) must add. Do **not** create a test project as a side effect.
9. Rebuild the API and the Blazor host.
10. **Manually re-verify the approval workflow in the running Blazor app** — this is the one
    functional risk in the task. Log in as a user with `UserAuthority` rows, open the
    `/approval` screen served by
    `V.SMART/V.SMART.Shared/Pages/Planning_Module_Pages/Authorization_Pages/Authorization.razor`,
    and exercise: list pending, approve one, reject one **with a reason**, bulk approve, bulk
    reject. Confirm `ApprovalHistory` rows are written with `Level`, `Status`, `ActionBy`,
    `ActionDate` and `Reason` — BR-APPR-001 must be observably intact.
11. Update documentation and the investigation registry.
12. Review the full diff and commit on the branch. Do not push to `master`.

## Files Expected to Change

The 15 files listed in *Existing Behavior to Preserve*, plus:

- Any Razor page under `V.SMART/V.SMART.Shared/Pages/` that declared a type which had to
  move — expected to be at most
  `Pages/Report_Module_Pages/TrackReports_Pages/LabourTrack.razor` and
  `Pages/Report_Module_Pages/TrackReports_Pages/SalesTrack.razor` (each declares its own
  `public enum GridMode`, at `:1262` and `:1807` respectively — note they are **two separate
  declarations of the same name in the same namespace**, which is itself worth reporting).
- `docs/kb/risks/technical-debt-register.md` — R-11 closed and corrected.
- `docs/kb/api/api-readiness-assessment.md` — A6 corrected from 14 to the true count.
- `docs/kb/business-rules/business-rule-inventory.md` — BR-APPR-001's migration note updated
  ("decouple first" is done).
- `docs/kb/investigation-registry.md`.
- `docs/kb/execution/tasks/M2-B04.md` (this file) — `status: Completed`.

## Files Expected to Be Created

- `tests/V.SMART.Shared.Tests/Architecture/NoPagesReferenceFromDomainTests.cs` —
  **TO BE CREATED, only if M0-12 has already created `tests/V.SMART.Shared.Tests/`.**
- `scripts/check-no-pages-references.sh` — **TO BE CREATED**, the fallback guard. No
  `scripts/` directory exists at the repository root today.
- Possibly one moved type file under `V.SMART/V.SMART.Shared/ViewModels/` or
  `V.SMART/V.SMART.Shared/Utility_Constants/` — only if the compiler demands it.

## Files That Must Not Change

- The five method signatures in
  `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/IBusinessService/IPlanningService/IApprovalService.cs:14-18`.
- The body of any method in
  `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/PlanningService/ApprovalService.cs`
  — only its `using` block is in scope.
- `V.SMART/V.SMART.Shared/Data/Planning/ApprovalHistory.cs` and
  `V.SMART/V.SMART.Shared/Data/Master/Admin_Module/UserAuthority.cs`.
- `V.SMART/V.SMART.Shared/Migrations/**` — no schema change; the entity `using` edits in
  `Data/` must not alter any mapping.
- `V.SMART/V.SMART.Api/Program.cs`, `V.SMART/V.SMART.Web/Program.cs`,
  `V.SMART/V.SMART/MauiProgram.cs` — [M2-B07](M2-B07.md) owns those.
- `V.SMART/V.SMART.Shared/V.SMART.Shared.csproj` — no assembly split in this task.
- Any Razor page's `@code` block. Only a type *declaration* may move, and only when the
  compiler forces it.

## API Changes

| Aspect | Change |
|---|---|
| Method | n/a — no endpoint is added by this task |
| Route | n/a |
| Request | n/a |
| Response | n/a |
| Authentication | n/a |
| Authorization | n/a — server-side `UserAuthority` enforcement is ADR-004 §4, delivered by KB-041 item B8, not here |
| Tenant context | n/a |
| Error model | n/a |
| Paging | n/a |
| Filtering | n/a |
| Sorting | n/a |
| Idempotency | n/a |

This task **enables** the approval endpoints; it does not create them.

## React Changes

Not applicable — the React app does not exist until M2-C01.

## Business Logic

**Refactored without behavior change.** Only `using` directives are removed and, if the
compiler demands it, type declarations move between files. No method body, signature,
validation, calculation or persistence path changes. BR-APPR-001 is preserved verbatim and
re-verified manually in step 10.

## Testing

There is **no test project** yet (INV-023, Confirmed). If [M0-12](M0-12.md) has landed,
`tests/V.SMART.Shared.Tests/` exists and `dotnet test` is usable — **check KB-083's command
table first**, because M0-12 is required to update it. Otherwise do not run `dotnet test`.

Verification:

1. `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` — 0 errors; warning count compared
   with the 6,695 baseline. **A warning-count increase is a signal here**: deleting a `using`
   can surface previously-suppressed ambiguity warnings.
2. `dotnet build V.SMART/V.SMART.Web/V.SMART.Web.csproj` — 0 errors.
3. The grep guard returns zero hits.
4. The architecture test passes (or the script exits 0).
5. **Manual approval-workflow regression** — step 10. Report the outcome of each of the five
   operations individually.

The MAUI head is not build-verified in this environment
([KB-083](../prompt-template.md#verified-repository-commands)). If it cannot be built, say
so and mark that half unverified.

## Acceptance Criteria

- [x] `grep -rn "V.SMART.Shared.Pages" --include="*.cs" V.SMART/V.SMART.Shared/ | grep -v "/Pages/"`
      returns **zero** hits. *(MET, 2026-08-21. Note: attempt 1's inline comment in
      `FundTransFilterVM.cs` quoted the namespace literally and produced one hit; attempt 2
      reworded it, so the grep is now genuinely zero.)*
- [x] `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/IBusinessService/IPlanningService/IApprovalService.cs`
      contains no `using static` and no reference to any type under `V.SMART.Shared.Pages`.
      *(MET — the file's seven `using` lines are `Data.Master.Admin_Module`,
      `ViewModels.PlanningViewModel` and five `System.*`.)*
- [x] The five `IApprovalService` member signatures are unchanged, verified by diff.
      *(MET — `git diff` on that file is a single deleted line; the signatures now sit at
      `:13-17`, having moved up one line, with text byte-identical to the frozen list above.)*
- [x] `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/PlanningService/ApprovalService.cs`
      shows a `using`-block-only diff. *(MET — one deleted line, `:14`.)*
- [x] Every type that had to move is **moved**, not copied — the original declaration is gone
      and every reference updated. *(MET vacuously — **no type had to move**. The single
      load-bearing directive was fixed by retyping a property to the EF entity that already
      existed, not by relocating a declaration. `GridMode` never had to move: both
      declarations are inside `@code` and nothing outside referenced them.)*
- [x] An architecture guard exists that fails on reintroduction: either a passing test in
      `tests/V.SMART.Shared.Tests/Architecture/`, or `scripts/check-no-pages-references.sh`
      exiting non-zero on a seeded violation. **The guard must be demonstrated to fail** by
      temporarily reintroducing one `using`, then reverting. *(MET —
      `tests/V.SMART.Shared.Tests/Architecture/NoPagesReferenceFromDomainTests.cs`, two tests.
      Seeded violation on 2026-08-21: both **observed to fail**, then both pass after removal.
      No shell script was created: the test project exists, so the fallback did not apply.)*
- [x] `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` — 0 errors; warning count
      recorded and any increase explained. *(MET — `--no-incremental`: **0 errors, 6,694
      warnings**, equal to KB-083's `--no-incremental` baseline. **No increase.** See the
      warning-count section of the execution record below for the full evidence.)*
- [x] `dotnet build V.SMART/V.SMART.Web/V.SMART.Web.csproj` — 0 errors. *(MET —
      `--no-incremental`: **0 errors, 6,697 warnings**, equal to its recorded baseline.)*
- [ ] The approval screen still lists pending items, approves, rejects with a reason, bulk
      approves and bulk rejects, and `ApprovalHistory` rows are written — BR-APPR-001 intact.
      **NOT CHECKED — not checkable in this session.** It needs `V.SMART.Web` running against
      a tenant database and a login holding `UserAuthority` rows; no session may acquire or
      reuse a credential (Q-14 / R-01 / Q-32). **What would verify it:** an operator with a
      tenant login exercising the five operations on `/approval` and confirming the
      `ApprovalHistory` rows. *Indirect evidence only:* the diff touches no method body, no
      signature and no EF mapping — `ApprovalService.cs` and `IApprovalService.cs` are
      one-deleted-`using`-line diffs — so there is no mechanism by which BR-APPR-001 could
      have changed. That is an argument, not an observation.
- [x] The final report states, for each of the 16 directives, whether it was load-bearing.
      *(MET — the table in the execution record below.)*
- [x] KB-060 R-11 and KB-041 A6 are corrected to the true file count with `file:line`
      evidence. *(MET — R-11 closed and its proposed action corrected; A6 marked delivered;
      KB-041's "14 reference `Pages`" row corrected too.)*
- [x] No file under `V.SMART/V.SMART.Shared/Migrations/` changed. *(MET —
      `git status --porcelain` lists no path under `Migrations/`.)*

## Verification Commands

```bash
grep -rn "V.SMART.Shared.Pages" --include="*.cs" V.SMART/V.SMART.Shared/ | grep -v "/Pages/"
```

```bash
git status --porcelain
```

```bash
dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj
```

```bash
dotnet build V.SMART/V.SMART.Web/V.SMART.Web.csproj
```

> Do **not** run `dotnet build NexGen-ERP---2025-master.sln` — the MAUI head's workloads are
> not verified. Do **not** run `dotnet test` unless M0-12 has landed **and** KB-083's command
> table has been updated to list it.

## Documentation Updates

| Document | Change | Frontmatter |
|---|---|---|
| `docs/kb/risks/technical-debt-register.md` (KB-060) | Close **R-11**. Correct "13 other" to the true count. Replace the "move the shared types into `ViewModels/`" action with what was actually required, and record that the `using static` on `Authorization` imported nothing (if the build confirms it). | bump `last_verified` |
| `docs/kb/api/api-readiness-assessment.md` (KB-041) | Correct the "14 reference `Pages`" figure and mark P0 item **A6** delivered. | bump `last_verified` |
| `docs/kb/business-rules/business-rule-inventory.md` (KB-030) | Update BR-APPR-001's migration note: the decoupling is done; server-side `UserAuthority` enforcement (item B8) remains outstanding. | bump `last_verified` |
| `docs/kb/architecture/backend-architecture.md` (KB-011) | Note the architecture guard and where it lives. | bump `last_verified` |
| `docs/kb/execution/tasks/M2-B04.md` (this file) | `status: Completed`. | bump `last_verified` |
| `docs/kb/execution/task-tracker.md` (KB-081) | Update the row **only if the file exists**. | n/a |

## Investigation Registry Updates

**Yes — one row.** Record the per-directive load-bearing analysis, using the next free
`INV-0xx` (check the file; do not assume a number). Include the negative results explicitly:

```yaml
Finding:        Of the 16 `using V.SMART.Shared.Pages…` directives in non-UI code, N were
                load-bearing and 16-N imported nothing. Nine of the ten referenced page
                namespaces declare no public type at all; the tenth declares
                `public enum GridMode` twice.
Evidence:       V.SMART/V.SMART.Shared/Pages/Report_Module_Pages/TrackReports_Pages/LabourTrack.razor:1262;
                .../SalesTrack.razor:1807;
                V.SMART/V.SMART.Shared/Pages/Planning_Module_Pages/Authorization_Pages/Authorization.razor
                (747 lines, zero occurrences of the keyword `static`)
Business rule:  BR-APPR-001 (preserved, re-verified manually)
Confidence:     Confirmed  # only after the build proves it
Last verified:  <execution date>
```

## Git Strategy

| | |
|---|---|
| **Branch** | `migration/M2-B04-decouple-pages-references` |
| **Commit** | `M2-B04: Remove all V.SMART.Shared.Pages references from non-UI code and add an architecture guard` |
| **Expected changed files** | The 15 source files listed above; possibly `LabourTrack.razor` / `SalesTrack.razor` and one moved type file; the architecture test **or** `scripts/check-no-pages-references.sh`; `docs/kb/risks/technical-debt-register.md`; `docs/kb/api/api-readiness-assessment.md`; `docs/kb/business-rules/business-rule-inventory.md`; `docs/kb/architecture/backend-architecture.md`; `docs/kb/investigation-registry.md`; `docs/kb/execution/tasks/M2-B04.md` |
| **Rollback** | Revert the branch's merge commit. Low risk: the diff is `using`-directive deletions plus at most one type move. If a type was moved, the revert restores its original location — verify the Razor pages still compile after a revert. |

Do not merge. Do not push to `master`. Leave the branch for review.

## Fresh-Session Execution Prompt

Copy everything in the block below into a new AI session.

```text
============================================================
ERP MIGRATION — TASK EXECUTION PROMPT
============================================================

TASK ID:
M2-B04

TASK NAME:
Decouple IApprovalService and the other Pages-referencing files
from the Razor UI

ROLE
You are an engineer working on the V.SMART / NexGen ERP
modernization project. You are executing exactly one task.

PROJECT OBJECTIVE
We are replacing the existing Blazor Server frontend with a new
React frontend while preserving the existing ERP business
behaviour, business services, database behaviour and business
rules wherever possible. The backend is extended, never
rewritten. Business logic currently trapped in Razor @code is
extracted into server-side services before any React screen
replaces it.

CURRENT ARCHITECTURE
Repository root: C:\Kumar\NexGen-ERP---2025-master\NexGen-ERP---2025-master
(this is the git repository root; the parent directory is not
part of the project)

  V.SMART/V.SMART.Shared   .NET 9 class library — ALL domain code:
                           196 EF entity sets, 285 business services,
                           ~190 repositories + UnitOfWork, 274 ViewModels,
                           333 Razor pages, 440 routes
  V.SMART/V.SMART.Web      Blazor Server host (live UI, stays running)
  V.SMART/V.SMART.Api      ASP.NET Core Web API (.NET 9) — the React
                           backend; currently 2 controllers / 6 endpoints
  V.SMART/V.SMART          .NET MAUI Blazor Hybrid host

SQL Server + EF Core 9, code-first, database-per-tenant.

Task-specific architecture context:
V.SMART.Shared is ONE assembly that contains both the domain
(BusinessLayer/, Data/, Repository/, Mappings/, ViewModels/,
E_Invoice/) and the Blazor UI (Pages/ — 333 Razor files). It is
built with Microsoft.NET.Sdk.Razor and multi-targets
net9.0-windows10.0.19041.0;net9.0
(V.SMART/V.SMART.Shared/V.SMART.Shared.csproj:1 and :4).

Because they share an assembly, the compiler does not prevent
domain code from importing the UI namespace — and 15 non-UI files
do exactly that, via 16 `using` directives. This task removes
them and installs a guard.

DO NOT split the assembly. Physically separating Pages/ into its
own project is a much larger change and is explicitly OUT OF
SCOPE for this task.

SOURCE OF TRUTH
The project knowledge base is at docs/kb/. Read it before
investigating the repository. Use docs/kb/INDEX.md for
question-to-document routing.

Required reading for this task:
  KB-083  docs/kb/execution/prompt-template.md
          (generation rules + the VERIFIED COMMANDS table)
  KB-080  docs/kb/execution/README.md  (section 9, M2)
  KB-060  docs/kb/risks/technical-debt-register.md  (R-11)
  KB-041  docs/kb/api/api-readiness-assessment.md  (item A6)
  KB-030  docs/kb/business-rules/business-rule-inventory.md
          (BR-APPR-001)
  ADR-004 docs/kb/decisions/ADR-004-server-side-authorization.md
          (section 4 — context only, NOT this task)
  KB-003  docs/kb/investigation-registry.md
  KB-002  docs/kb/source-of-truth-rules.md
  KB-005  docs/kb/INDEX.md
  This task's full specification:
          docs/kb/execution/tasks/M2-B04.md

Authority order when sources conflict:
  1. Current source code
  2. Database schema / EF migrations (for storage)
  3. The knowledge base (for interpretation)
  4. Older prose documentation — hypothesis only
docs/ARCHITECTURE.md is superseded and contains known factual
errors. Do not rely on it.

INVESTIGATION REGISTRY
Before any repository investigation, search
docs/kb/investigation-registry.md.

Relevant investigations:
  INV-011  Business rules — cross-module sweep. PARTIAL, but
           BR-APPR-001 is already extracted WITH EVIDENCE. Reuse
           it; do not re-extract the approval rule.
  INV-023  Testing and CI — Complete. There is NO test project
           unless M0-12 has landed.
  INV-024  @code triage per module — SCHEDULED, one module ahead
           of each migration wave. If this task uncovers business
           logic sitting in a Razor @code block, it belongs to
           INV-024 and its wave, NOT to this task.

Before investigating the repository, search
docs/kb/investigation-registry.md and the relevant knowledge-base
documents via docs/kb/INDEX.md. If an investigation is Complete
and not stale, reuse its findings and cite the doc_id — do not
re-derive them. If it is Partial, investigate only the documented
gap. If it is absent or contradicted by current code,
investigate, then record the finding, its file:line evidence, and
its confidence in the knowledge base so that future sessions do
not repeat this work. Record negative results too — "grepped for
X, found none" is a finding.

CURRENT TASK
Remove every compile-time dependency from non-UI code in
V.SMART.Shared onto the V.SMART.Shared.Pages namespace, and
install an automated guard that fails when one is reintroduced.

After this task, BusinessLayer/, Data/, Repository/, Mappings/,
ViewModels/ and E_Invoice/ contain ZERO references to
V.SMART.Shared.Pages, and the approval workflow can be exposed
over HTTP without pulling a Razor component into the API's
activation path.

WHY THIS TASK EXISTS
Risk R-11 in KB-060 and P0 blocker A6 in KB-041. The headline
case is a business-layer INTERFACE importing a Razor page type:

  V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/
    IBusinessService/IPlanningService/IApprovalService.cs:8
      using static V.SMART.Shared.Pages.Planning_Module_Pages
                   .Authorization_Pages.Authorization;

repeated at
  V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/
    PlanningService/ApprovalService.cs:14

KB-030's BR-APPR-001 migration note says: "The IApprovalService
interface currently `using static`-imports a Razor page type —
decouple first." This task IS that decoupling, and it gates the
approval endpoints (KB-041 item B8) and all M3/M4 approval work.

TWO CORRECTIONS YOU MUST CARRY, both re-verifiable in one grep:

Correction 1 — THE COUNT IS WRONG IN THE KB.
KB-041 says "14 reference Pages"; KB-060 R-11 says "13 other".
The actual figure on 2026-08-12 is 16 `using` DIRECTIVES across
15 DISTINCT non-UI files (FundTrans.cs carries two). Confirmed.
Correct the KB as part of this task.

Correction 2 — MOST OF THESE USINGS IMPORT NOTHING.
Scanning all ten referenced page namespaces for a type
declaration returns hits in EXACTLY ONE of them (Confirmed):
  Master_Module_pages.Items_Pages          none
  Master_Module_pages.Store_Pages          none
  Master_Module_pages.Machine_Pages        none
  Master_Module_pages.Bank_Pages           none
  Report_Module_Pages.PoPending_Pages      none
  CashFlow_Pages.Payments_Pages            none
  CashFlow_Pages.Receipt_Pages             none
  OutSourcing_Module_pages.PurchaseSCN_Pages  none
  ProductionModule_pages                   none
      (the public static helpers are one level DOWN, in
       ProductionModule_pages.DailyProductionLog_Pages —
       ProductionLogUpsert.razor:1596,1620,1622 — which a
       parent-namespace `using` does NOT import)
  Report_Module_Pages.TrackReports_Pages   public enum GridMode
      declared TWICE: LabourTrack.razor:1262 and
      SalesTrack.razor:1807

And the `using static` on the Authorization component imports
NOTHING AT ALL:
V.SMART/V.SMART.Shared/Pages/Planning_Module_Pages/
Authorization_Pages/Authorization.razor is 747 lines and contains
ZERO occurrences of the keyword `static`. A `using static` on a
type with no static members imports an empty set. The
UserAuthority parameter in IApprovalService.ApproveAsync
(IApprovalService.cs:15) comes from
V.SMART.Shared.Data.Master.Admin_Module, imported at
IApprovalService.cs:1 — NOT from the Razor page.

So the likely fix for most of the 16 directives is simply to
DELETE them. That is a much better outcome than KB-060's
suggested "move the shared types into ViewModels/", which would
create ViewModels that nobody uses.

CONFIDENCE DISCIPLINE: "these usings are unused" is INFERRED
(high confidence) from the namespace scan, NOT Confirmed. Only
the compiler is the authority. Delete, build, and reinstate only
what the compiler demands. Do not upgrade the classification in
the knowledge base until the build has proved it.

PREREQUISITES
M2-B07 (shared AddVSmartDomain() DI extension) must be Completed.
Gate G0 must have passed.

CURRENT IMPLEMENTATION
The complete inventory, all Confirmed and re-verified 2026-08-12.
RE-VERIFY EVERY LINE NUMBER BEFORE EDITING — other M2 tasks may
have touched these files first.

 1. BusinessLayer/BusinessService/EInvoiceAPIService/
      EinvoiceDatabaseService.cs:6
      using V.SMART.Shared.Pages.Master_Module_pages.Items_Pages;
 2. BusinessLayer/BusinessService/IBusinessService/
      IPlanningService/IApprovalService.cs:8
      using static V.SMART.Shared.Pages.Planning_Module_Pages
                   .Authorization_Pages.Authorization;
 3. BusinessLayer/BusinessService/InventoryService/
      StockAddIss_Position/StockAddIssPosition.cs:8
      using V.SMART.Shared.Pages.Master_Module_pages.Store_Pages;
 4. BusinessLayer/BusinessService/MaintenanceService/
      MaintenanceProcessService.cs:8
      using V.SMART.Shared.Pages.Master_Module_pages.Machine_Pages;
 5. BusinessLayer/BusinessService/PlanningService/
      ApprovalService.cs:14
      using static V.SMART.Shared.Pages.Planning_Module_Pages
                   .Authorization_Pages.Authorization;
 6. BusinessLayer/BusinessService/ReportService/Rating_Services/
      GetRatingsService.cs:15
      using V.SMART.Shared.Pages.Report_Module_Pages.PoPending_Pages;
 7. Data/AccountsModule/FundTrans.cs:11
      using V.SMART.Shared.Pages.CashFlow_Pages.Payments_Pages;
 8. Data/AccountsModule/FundTrans.cs:12
      using V.SMART.Shared.Pages.CashFlow_Pages.Receipt_Pages;
 9. Data/AccountsModule/ReceiptsSub.cs:8
      using V.SMART.Shared.Pages.CashFlow_Pages.Receipt_Pages;
10. Data/ApplicationDbContext.cs:84
      using V.SMART.Shared.Pages.Report_Module_Pages.TrackReports_Pages;
      (grep -n "GridMode" on this file returns NOTHING, so this
       one is very likely dead too)
11. Data/OutSourcing/MaterialReq.cs:3
      using V.SMART.Shared.Pages.Master_Module_pages.Store_Pages;
12. Data/SalesAndLabour/PerformaInvoice/PerformaInv.cs:6
      using V.SMART.Shared.Pages.Master_Module_pages.Bank_Pages;
13. E_Invoice/E_InvoiceHelper/JsonSerializer.cs:1
      using V.SMART.Shared.Pages.Master_Module_pages.Items_Pages;
14. Mappings/ProductionMappingProfile/ProductionIssueAssyMap/
      ProductionIssuAssyMapping.cs:4
      using V.SMART.Shared.Pages.ProductionModule_pages;
15. Repository/OutSourcingRepository/PurchaseSCN_Repository/
      PurchaseSCNRepository.cs:5
      using V.SMART.Shared.Pages.OutSourcing_Module_pages
            .PurchaseSCN_Pages;
16. ViewModels/AccountsViewModel/FundTransFilterVM.cs:9
      using V.SMART.Shared.Pages.Master_Module_pages.Bank_Pages;

All paths above are relative to V.SMART/V.SMART.Shared/.

The five IApprovalService members, which are FROZEN
(IApprovalService.cs:14-18):
  Task<List<ApprovalVM>> GetPendingApprovalsAsync(
      string type, string level, string userName);
  Task<bool> ApproveAsync(ApprovalVM record, string type,
      string level, string userName, UserAuthority authority);
  Task<bool> RejectAsync(ApprovalVM record, string type,
      string level, string reason, string userName);
  Task<bool> BulkApproveAsync(List<ApprovalVM> records,
      string type, string level, string username);
  Task<bool> BulkRejectAsync(List<ApprovalVM> records,
      string type, string level, string reason, string username);

TARGET IMPLEMENTATION
1. grep -rn "V.SMART.Shared.Pages" --include="*.cs"
     V.SMART/V.SMART.Shared/ | grep -v "/Pages/"
   returns ZERO hits.
2. IApprovalService and ApprovalService compile with no reference
   to any Razor type; the five signatures are byte-for-byte
   unchanged.
3. Where the compiler proves a directive WAS load-bearing, the
   type it provided is MOVED (never copied) to a non-UI location:
   ViewModels/ for a data shape, Utility_Constants/ for an enum
   (alongside the existing CommonConstants.cs). The Razor page
   then imports it from there. At most one such case is expected
   (GridMode), possibly none.
4. An ARCHITECTURE GUARD exists that fails on reintroduction:
   - preferred: a test in tests/V.SMART.Shared.Tests/
     (TO BE CREATED by M0-12) reflecting over the V.SMART.Shared
     assembly and asserting that no type in the namespaces
     V.SMART.Shared.BusinessLayer / .Data / .Repository /
     .Mappings / .ViewModels / .E_Invoice depends on any type in
     V.SMART.Shared.Pages. Collect ALL violations and assert on
     the list, so the failure names every offender.
   - fallback if M0-12 has not landed: a committed
     scripts/check-no-pages-references.sh (TO BE CREATED — no
     scripts/ directory exists today) running the grep and
     exiting non-zero on any hit, plus a documented CI step for
     M0-07 to wire in.
   Note honestly in a comment that reflection cannot see an
   UNUSED using directive — that is what the grep guard covers.
   The two are complementary.
5. KB-060 R-11 closed with the CORRECTED file count.

RELEVANT FILES
The 15 source files listed above, plus:
  V.SMART/V.SMART.Shared/Pages/Planning_Module_Pages/
      Authorization_Pages/Authorization.razor          (read only)
  V.SMART/V.SMART.Shared/Pages/Report_Module_Pages/
      TrackReports_Pages/LabourTrack.razor    (only if GridMode
      TrackReports_Pages/SalesTrack.razor      must move)
  V.SMART/V.SMART.Shared/Utility_Constants/CommonConstants.cs
      (read only — shows where an enum would go)
  V.SMART/V.SMART.Shared/Data/Planning/ApprovalHistory.cs (read only)
  V.SMART/V.SMART.Shared/Data/Master/Admin_Module/
      UserAuthority.cs                                 (read only)
  docs/kb/risks/technical-debt-register.md
  docs/kb/api/api-readiness-assessment.md
  docs/kb/business-rules/business-rule-inventory.md
  docs/kb/architecture/backend-architecture.md
  docs/kb/investigation-registry.md

BUSINESS RULES TO PRESERVE
BR-APPR-001 — Rejection requires a reason; approvals are audited
by level.
  Statement: RejectAsync/BulkRejectAsync take a MANDATORY reason;
  every approve or reject writes an ApprovalHistory row
  { RecordId, ApprovalType, Level, Status, ActionBy, ActionDate,
  Reason }. A user may act only on the document types and levels
  granted in UserAuthority (12 type/level pairs).
  Evidence: V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/
  IBusinessService/IPlanningService/IApprovalService.cs:16,18;
  V.SMART/V.SMART.Shared/Data/Planning/ApprovalHistory.cs;
  V.SMART/V.SMART.Shared/Data/Master/Admin_Module/UserAuthority.cs
  Confidence: Confirmed.
  THIS TASK MUST NOT CHANGE ANY OF IT. You must re-verify it
  manually in the running Blazor app before declaring done.

CONSTRAINTS
- Do not rewrite existing business services.
- Do not reimplement ERP business logic in React/TypeScript.
- Do not change unrelated modules.
- Do not make assumptions without checking source code.
- Do not repeat completed investigations.
- Do not modify the database schema unless this task explicitly
  authorizes it. (It does not — and note that five of the 15
  files are EF entities. Editing their `using` block must not
  change a single mapping. Nothing under
  V.SMART/V.SMART.Shared/Migrations/ may change.)
- Preserve existing API behaviour wherever practical.
- The server remains authoritative for validation, calculations,
  permissions and document numbering.
- Do not start another task after completing this one.
Task-specific constraints:
- Do NOT change any of the five IApprovalService signatures.
- Do NOT change any method BODY in ApprovalService.cs. Only its
  using block is in scope.
- Do NOT split V.SMART.Shared into separate projects.
- Do NOT create the test project. If tests/V.SMART.Shared.Tests/
  does not exist, use the shell-script guard instead.
- Do NOT move a static METHOD out of a Razor component. If the
  compiler shows a non-UI file depends on one, that is business
  logic trapped in @code: record it for INV-024 and the relevant
  M3/M4 wave, leave the using with a // TODO comment naming the
  wave, and REPORT IT AS A DEVIATION. Based on the namespace
  scan this case is not expected to arise.
- Do NOT copy a type to break the dependency. Move it, and
  update every reference.
- Do NOT touch V.SMART.Api/Program.cs, V.SMART.Web/Program.cs or
  MauiProgram.cs — M2-B07 owns those.
- Do NOT add approval endpoints. That is KB-041 item B8, a later
  task. This task only makes them possible.

EXECUTION PROCEDURE
1. Read the referenced knowledge-base documents.
2. Search the investigation registry.
3. Confirm whether the required investigation already exists.
4. Inspect the actual source code.
5. Verify every assumption against code before acting on it.
6. Implement only this task.
7. Run the required verification commands and tests.
8. Review the full git diff.
9. Update documentation.
10. Update the investigation registry if required.

Implementation order that works best here:
  a. Re-derive the inventory with the grep. Record it. If it is
     not 16 directives / 15 files, that difference is itself a
     finding to report.
  b. Record the baseline build.
  c. DELETE ALL 16 DIRECTIVES IN ONE PASS, then build. The
     compiler then enumerates exactly which ones mattered —
     faster and more honest than reasoning about each one.
  d. Fix only what the compiler complains about, per the rules
     in TARGET IMPLEMENTATION item 3.
  e. Verify IApprovalService and ApprovalService explicitly, not
     just via the bulk build. State in one sentence what the
     `using static` was actually importing. If the answer is
     "nothing", SAY SO — that is the single most useful
     correction this task can hand back to KB-060.
  f. Build the guard, and DEMONSTRATE IT FAILS by temporarily
     reintroducing one using, then revert.
  g. Manually regression-test the approval workflow.

EXPECTED FILE CHANGES
Modified:
  the 15 source files listed under CURRENT IMPLEMENTATION
  possibly V.SMART/V.SMART.Shared/Pages/Report_Module_Pages/
     TrackReports_Pages/LabourTrack.razor
  possibly V.SMART/V.SMART.Shared/Pages/Report_Module_Pages/
     TrackReports_Pages/SalesTrack.razor
  docs/kb/risks/technical-debt-register.md
  docs/kb/api/api-readiness-assessment.md
  docs/kb/business-rules/business-rule-inventory.md
  docs/kb/architecture/backend-architecture.md
  docs/kb/investigation-registry.md
  docs/kb/execution/tasks/M2-B04.md   (status -> Completed)
Created:
  tests/V.SMART.Shared.Tests/Architecture/
      NoPagesReferenceFromDomainTests.cs
      (ONLY if M0-12 already created the test project)
  scripts/check-no-pages-references.sh
      (the fallback guard; no scripts/ directory exists today)
  possibly ONE moved type file under
      V.SMART/V.SMART.Shared/ViewModels/ or
      V.SMART/V.SMART.Shared/Utility_Constants/
Must not change:
  the five IApprovalService signatures
  any method body in ApprovalService.cs
  V.SMART/V.SMART.Shared/Data/Planning/ApprovalHistory.cs
  V.SMART/V.SMART.Shared/Data/Master/Admin_Module/UserAuthority.cs
  V.SMART/V.SMART.Shared/Migrations/**
  V.SMART/V.SMART.Shared/V.SMART.Shared.csproj
  V.SMART/V.SMART.Api/Program.cs
  V.SMART/V.SMART.Web/Program.cs
  V.SMART/V.SMART/MauiProgram.cs
  any Razor @code block

TESTS
There is NO test project unless M0-12 has landed (INV-023).
CHECK KB-083's command table before running `dotnet test` — M0-12
is required to update it. If the table does not list dotnet test,
do not run it.

Perform these checks and report each result:
  1. dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj
     0 errors. Record the warning count and compare with the
     6,695 baseline. A WARNING-COUNT INCREASE IS A SIGNAL here:
     removing a using can surface previously-suppressed
     ambiguity warnings. Investigate any increase.
  2. dotnet build V.SMART/V.SMART.Web/V.SMART.Web.csproj
     0 errors.
  3. The grep guard returns zero hits.
  4. The architecture guard passes, AND has been demonstrated to
     fail on a seeded violation.
  5. MANUAL APPROVAL-WORKFLOW REGRESSION — the one real
     functional risk in this task. In the running Blazor app,
     log in as a user with UserAuthority rows, open the approval
     screen served by
     V.SMART/V.SMART.Shared/Pages/Planning_Module_Pages/
     Authorization_Pages/Authorization.razor, and exercise all
     five operations INDIVIDUALLY, reporting each:
       - list pending approvals
       - approve one
       - reject one WITH A REASON
       - bulk approve
       - bulk reject
     Confirm ApprovalHistory rows are written with Level,
     Status, ActionBy, ActionDate and Reason. BR-APPR-001 must
     be observably intact.
  6. The MAUI head is NOT build-verified in this environment. If
     you cannot build it, SAY SO and mark that half unverified.

ACCEPTANCE CRITERIA
- grep -rn "V.SMART.Shared.Pages" --include="*.cs"
    V.SMART/V.SMART.Shared/ | grep -v "/Pages/"
  returns ZERO hits.
- IApprovalService.cs contains no `using static` and no reference
  to any type under V.SMART.Shared.Pages.
- The five IApprovalService signatures are unchanged, proven by
  diff.
- ApprovalService.cs shows a using-block-only diff.
- Every type that had to move is MOVED, not copied: the original
  declaration is gone and every reference is updated.
- An architecture guard exists AND has been demonstrated to fail
  on a seeded violation, then reverted.
- dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj — 0 errors;
  warning count recorded and any increase explained.
- dotnet build V.SMART/V.SMART.Web/V.SMART.Web.csproj — 0 errors.
- The approval screen still lists, approves, rejects with a
  reason, bulk approves and bulk rejects, and ApprovalHistory
  rows are written.
- The final report states, FOR EACH of the 16 directives,
  whether it was load-bearing.
- KB-060 R-11 and KB-041 A6 are corrected to the true file count
  with file:line evidence.
- No file under V.SMART/V.SMART.Shared/Migrations/ changed.

VERIFICATION COMMANDS
Run each of these and record its output:

  grep -rn "V.SMART.Shared.Pages" --include="*.cs" V.SMART/V.SMART.Shared/ | grep -v "/Pages/"

  git status --porcelain

  dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj

  dotnet build V.SMART/V.SMART.Web/V.SMART.Web.csproj

Do NOT run `dotnet build NexGen-ERP---2025-master.sln` — the MAUI
head's workloads are not verified in this environment.
Do NOT run `dotnet test` unless M0-12 has landed AND KB-083's
command table lists it.

DOCUMENTATION REQUIREMENTS
  docs/kb/risks/technical-debt-register.md (KB-060)
    Close R-11. Correct "13 other" to the true count. Replace
    the "move the shared types into ViewModels/" action item
    with what was ACTUALLY required, and record that the
    `using static` on Authorization imported nothing (if the
    build confirms it). Bump last_verified.
  docs/kb/api/api-readiness-assessment.md (KB-041)
    Correct the "14 reference Pages" figure; mark P0 item A6
    delivered. Bump last_verified.
  docs/kb/business-rules/business-rule-inventory.md (KB-030)
    Update BR-APPR-001's migration note: the decoupling is done;
    server-side UserAuthority enforcement (item B8) remains
    outstanding. Bump last_verified.
  docs/kb/architecture/backend-architecture.md (KB-011)
    Note the architecture guard and where it lives.
    Bump last_verified.
  docs/kb/investigation-registry.md (KB-003)
    Add one row recording the per-directive load-bearing
    analysis, using the NEXT FREE INV number — check the file,
    do not assume. Use the KB-083 evidence format and include
    the negative results explicitly (nine of ten namespaces
    declare no public type; Authorization.razor has no static
    member).
  docs/kb/execution/tasks/M2-B04.md
    Set status: Completed and bump last_verified.

  Note: docs/kb/execution/task-tracker.md (KB-081) — update the
  row ONLY if that file exists when you run. Do not create it.

GIT
Branch:  migration/M2-B04-decouple-pages-references
Commit:  M2-B04: Remove all V.SMART.Shared.Pages references from non-UI code and add an architecture guard
Do not merge. Do not push to master. Leave the branch for review.

FINAL RESPONSE
When finished, report:
1.  Task ID
2.  Task status (Completed / Needs Review / Blocked)
3.  What was implemented
4.  Files created
5.  Files modified
6.  Files deleted
7.  Tests executed
8.  Test results
9.  Documentation updated
10. Investigation registry updated
11. Architectural decisions taken
12. Unexpected findings
13. Assumptions made
14. Deviations from this task
15. Recommended next task

IMPORTANT:
EXECUTE ONLY THIS TASK.
DO NOT START THE NEXT TASK.
============================================================
```

---

## Execution Record (2026-08-21)

**Status: `Blocked`, attempt 1 of 3 used, 0 escalations.** This is a close-out record only —
the session that wrote it did not implement, build, test, commit, or start another task. It
exists so the next session resumes the in-flight attempt 1 instead of re-investigating or
silently discarding work that is already mostly done on disk.

### What actually happened

The `migration-implementer` agent (`opus`) was dispatched on branch
`migration/M2-B04-decouple-pages-references` and did substantial real work before the
connection dropped mid-response. Traced from its own transcript
(`agent-a9c1b219105cd1a3d.jsonl`, workflow `wf_46724a7c-894`, session `93d81236-…`): the last
event is `"error":"server_error"`, text "API Error: The response stopped arriving. The
response above may be incomplete.", timestamped `2026-08-21T05:36:59Z` — a mid-stream
connection drop, not an immediate upstream rejection like the `529 Overloaded` precedent
recorded for `M0-12-01` attempt 1 (`failure-log.md`). The orchestrator accordingly received no
final report and no verdict (`{"verdict":"none","note":"validation did not complete"}`), and
routed this to a close-out rather than a retry, per KB-091 §8 item 1 (empty/incomplete
implementer return).

**Unlike `M0-12-01` attempt 1, this was not an empty return in the working tree.** The agent's
last several tool calls, replayed from the same transcript, show it:

1. Removed the `V.SMART.Shared.Pages` `using` directive from 14 of the 15 non-Razor files in
   `source_files` (all files except `FundTransFilterVM.cs`, which needed the typed fix below,
   and `Authorization.razor` itself, which is the file everything else is being decoupled
   from and needed no change).
2. Ran `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` — this failed, `1 Error(s)`,
   `CS0246: The type or namespace name 'Bank' could not be found`,
   `FundTransFilterVM.cs(26,16)` — exactly the load-bearing case flagged by this task's
   pre-implementation investigation (`FundTransFilterVM.Bank` was typed as the Razor component
   `V.SMART.Shared.Pages.Master_Module_pages.Bank_Pages.Bank`, not the EF entity).
3. Grepped every caller of `FundTransFilter`/`.Bank` to confirm the property is read-but-never
   assigned outside the VM itself (`FundTransRepository.cs`, `Fundtransactions.razor`), so
   either concrete type is behaviour-neutral today.
4. Fixed it: added `using V.SMART.Shared.Data.Master.Accounts_Module;` and retyped
   `public Bank? Bank { get; set; }` to `public Banks? Bank { get; set; }` (the actual EF
   entity), with an inline comment recording why.
5. Re-ran the build — `0 Error(s)`, `6695 Warning(s)`, `Time Elapsed 00:01:12.01`.
6. Started writing a reflection-based architecture guard test,
   `tests/V.SMART.Shared.Tests/Architecture/NoPagesReferenceFromDomainTests.cs`, via a Bash
   heredoc. The heredoc's quoting was malformed (`Exit code 2`, an unterminated-quote parse
   error) and the whole compound command — including its own `mkdir -p` — never executed, so
   no file or directory was created. This is a shell scripting bug in the agent's own command,
   not a build or logic error.
7. The next agent turn is the connection drop quoted above. No commit was made, no
   `dotnet test` was run, and this task file's own acceptance-criteria checklist was never
   filled in (this section is the first).

**Independently re-confirmed in this close-out session** (read-only — no code changed):
`git status --porcelain` on `migration/M2-B04-decouple-pages-references` shows exactly the 15
files above modified, matching the diff described; `dotnet build
V.SMART/V.SMART.Api/V.SMART.Api.csproj` (incremental) prints `0 Error(s)`, confirming the tree
still builds clean right now. `tests/V.SMART.Shared.Tests/Architecture/` does not exist —
step 6's failure is confirmed to have left nothing on disk.

### What differed from the plan, and why

- **The task file's own investigation (reused, not re-derived here) had already predicted**
  the `FundTransFilterVM.cs` load-bearing case and the guard-test plan; the implementer
  followed both. Nothing here contradicts the investigation.
- **Not planned:** the mid-response connection drop itself, and the malformed heredoc that
  silently no-opped the guard-test write. Neither is a specification or business-logic
  problem — both are execution-environment failures.
- **One number needs watching, not yet explained:** the implementer's own full build reported
  `6695 Warning(s)`, one above the `~6,695` / `6,694` baseline recorded across recent tasks
  (e.g. `M2-A01-03`'s `dotnet build --no-incremental`: `6,694`). Whether this is a genuine new
  warning introduced by the `Bank` to `Banks` retype (plausible — a property type change can
  surface a previously-masked nullability or hiding warning) or ordinary noise between full
  and partial builds was **not determined** — the transcript shows only the one number, with
  no `--no-incremental` companion run to compare against, and this close-out session did not
  run one (out of scope for a documentation-only record). **Next session: run
  `dotnet build V.SMART.Api --no-incremental` and diff the warning list against the baseline
  before treating this as either fine or a regression.**

### What the next session needs to do — do not restart from scratch

1. **Do not discard the working tree.** It is not experimental scaffolding; it is a build-
   verified implementation of 15 of this task's 16 `source_files` directives, produced by a
   real attempt that simply never got to report or commit.
2. **Do not treat this as attempt 1 failing on the merits** — nothing here is a
   `business-rule`, `architecture`, or `acceptance-criterion` failure. It is `environment`
   category, same as `M0-12-01` attempt 1, but with more surviving work.
3. Resume where the transcript stops: write
   `tests/V.SMART.Shared.Tests/Architecture/NoPagesReferenceFromDomainTests.cs` (the guard
   test this task's plan calls for — see `## Testing` and `## Files Expected to Be Created`
   above), using the `Write`/`Edit` tools rather than a Bash heredoc so the earlier quoting
   failure cannot recur.
4. Run `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj --no-incremental` and
   `dotnet test tests/V.SMART.Shared.Tests` (and `tests/V.SMART.Api.Tests`, per this
   codebase's standing convention of running both suites), record the actual output, and
   settle the warning-count question above before closing the task.
5. Re-check `## Acceptance Criteria` line by line against the current working tree — most
   already appear met from the transcript, but none has been independently verified against
   the checklist text.
6. Commit, fill in this task file's remaining acceptance-criteria checkboxes, and hand off
   through the normal `Needs Review` path. Do not merge.

### Discoveries recorded elsewhere (not duplicated here — see the doc_id cited)

- Investigation reused: `INV-011` (BR-APPR-001), `INV-023` (superseded — the test project now
  exists on `master`), `INV-005`/`KB-014` (tenancy — none of this task's files resolve a
  tenant), `INV-039` (DI — `IApprovalService` registered via `AddVSmartDomain`).
- **New: `INV-047`** *(claimed as INV-042; renumbered at merge 2026-08-21 — M2-A07 had already claimed INV-042)* in [`investigation-registry.md`](../../investigation-registry.md), filed
  `Partial` in this close-out — the `FundTransFilterVM.cs:27` load-bearing finding, the build
  confirmation (`0 Error(s)` after the fix), and the negative result that `Authorization.razor`
  needs no change. Stays `Partial` until the guard test is written and the acceptance criteria
  actually pass (step 3/6 above).

---

## Execution Record — attempt 2 (2026-08-21)

**Status: `Needs Review`, attempt 2 of 3.** Attempt 1's uncommitted working tree was
**preserved, not restarted** — it is now committed together with attempt 2's work on
`migration/M2-B04-decouple-pages-references`. 10 of 11 acceptance criteria are met; the
eleventh (manual approval-workflow regression) is **not checkable in this environment** and is
recorded as such rather than claimed.

### What attempt 2 added to attempt 1's diff

1. **The guard test** — `tests/V.SMART.Shared.Tests/Architecture/NoPagesReferenceFromDomainTests.cs`
   (new). Written with the `Write` tool, not a Bash heredoc; attempt 1 died on exactly that
   quoting bug. Two `[Fact]`s, deliberately complementary:
   - `Domain_Types_Do_Not_Reference_Pages_Types` — reflects over
     `typeof(ApplicationDbContext).Assembly`, walks every type whose namespace starts with one
     of the six domain roots, and collects any base type, implemented interface, field,
     property, method parameter/return or constructor parameter whose type (unwrapped through
     arrays, by-ref, pointers and generic arguments) lives under `V.SMART.Shared.Pages`.
   - `Domain_Source_Files_Contain_No_Pages_Using_Directives` — scans the source text of
     `V.SMART/V.SMART.Shared/**/*.cs`, excluding `Pages/`, `obj/` and `bin/`. **This one exists
     because reflection is structurally blind to an unused `using`** — an unused directive
     leaves no trace in metadata, and 15 of this task's 16 directives were exactly that. It
     locates the repository root from `[CallerFilePath]`, falling back to walking up from
     `AppContext.BaseDirectory`; if neither finds the marker it returns without failing, so a
     published test bundle cannot produce a false alarm.

   Both assert on a **collected list**, so a failure names every offender at once.
2. **One correction to attempt 1's work.** Its inline comment in `FundTransFilterVM.cs` quoted
   `V.SMART.Shared.Pages...Bank_Pages.Bank` literally, which meant acceptance criterion 1's
   grep still returned **one hit** — the guard would have been red on the very commit that
   installed it. Reworded to name the type without the namespace literal.
3. Everything else in attempt 1's diff was **verified and kept unchanged**: 15 files' `using`
   deletions and the `Bank` → `Banks` retype.

### The 16 directives — load-bearing or not

**One of 16 was load-bearing.** The compiler is the authority here, not the namespace scan.

| # | File | Directive | Load-bearing? |
|---|---|---|---|
| 1 | `BusinessLayer/.../EInvoiceAPIService/EinvoiceDatabaseService.cs:6` | `…Master_Module_pages.Items_Pages` | **No** — deleted, builds clean |
| 2 | `BusinessLayer/.../IPlanningService/IApprovalService.cs:8` | `using static …Authorization_Pages.Authorization` | **No** — imported nothing at all |
| 3 | `BusinessLayer/.../StockAddIss_Position/StockAddIssPosition.cs:8` | `…Master_Module_pages.Store_Pages` | **No** |
| 4 | `BusinessLayer/.../MaintenanceService/MaintenanceProcessService.cs:8` | `…Master_Module_pages.Machine_Pages` | **No** |
| 5 | `BusinessLayer/.../PlanningService/ApprovalService.cs:14` | `using static …Authorization_Pages.Authorization` | **No** — imported nothing at all |
| 6 | `BusinessLayer/.../Rating_Services/GetRatingsService.cs:15` | `…Report_Module_Pages.PoPending_Pages` | **No** |
| 7 | `Data/AccountsModule/FundTrans.cs:11` | `…CashFlow_Pages.Payments_Pages` | **No** |
| 8 | `Data/AccountsModule/FundTrans.cs:12` | `…CashFlow_Pages.Receipt_Pages` | **No** |
| 9 | `Data/AccountsModule/ReceiptsSub.cs:8` | `…CashFlow_Pages.Receipt_Pages` | **No** |
| 10 | `Data/ApplicationDbContext.cs:84` | `…Report_Module_Pages.TrackReports_Pages` | **No** — the predicted `GridMode` risk did not materialise |
| 11 | `Data/OutSourcing/MaterialReq.cs:3` | `…Master_Module_pages.Store_Pages` | **No** |
| 12 | `Data/SalesAndLabour/PerformaInvoice/PerformaInv.cs:6` | `…Master_Module_pages.Bank_Pages` | **No** |
| 13 | `E_Invoice/E_InvoiceHelper/JsonSerializer.cs:1` | `…Master_Module_pages.Items_Pages` | **No** |
| 14 | `Mappings/.../ProductionIssuAssyMapping.cs:4` | `…ProductionModule_pages` | **No** |
| 15 | `Repository/.../PurchaseSCNRepository.cs:5` | `…OutSourcing_Module_pages.PurchaseSCN_Pages` | **No** |
| 16 | `ViewModels/AccountsViewModel/FundTransFilterVM.cs:9` | `…Master_Module_pages.Bank_Pages` | **YES** — removal produced `CS0246: The type or namespace name 'Bank' could not be found` |

**What the `using static` on `Authorization` was importing: nothing.**
`V.SMART/V.SMART.Shared/Pages/Planning_Module_Pages/Authorization_Pages/Authorization.razor`
contains **zero** occurrences of the keyword `static` (re-verified 2026-08-21 —
`grep -c "static"` returns `0`), so a `using static` on that type imports an empty set. The
`UserAuthority` parameter in `ApproveAsync` comes from `V.SMART.Shared.Data.Master.Admin_Module`,
imported at `IApprovalService.cs:1`. The headline dependency this whole task exists to remove
was **dead text**.

**The one real case.** `FundTransFilter.Bank` was declared `public Bank? Bank { get; set; }`,
resolving to the **Razor component** `…Bank_Pages.Bank`. It is now `public Banks? Bank`, the EF
entity at `V.SMART/V.SMART.Shared/Data/Master/Accounts_Module/Banks.cs:6`. **No type was moved
or copied.** The property is referenced by nothing outside its own declaration (Confirmed —
only four files mention `FundTransFilter` at all; `FundTransRepository.cs:43` uses the
*entity's* `Banks` navigation, and `Fundtransactions.razor` has no `.Bank` reference), so the
retype is behaviour-neutral. Whether the property was ever *meant* to carry the entity is
**Q-55**.

### The warning-count question — settled: this diff adds no warning

Attempt 1 reported `6695` and could not say whether it was a regression. It is not, and three
independent lines of evidence say so:

| Measurement on this branch | Result | Recorded baseline (KB-083 / `ci/warning-baseline.json`) |
|---|---|---|
| `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj --no-incremental` | **0 errors, 6,694 warnings**, 2m15s | 6,694 |
| `dotnet restore` then `dotnet build … --no-restore --no-incremental -v normal` (the CI form) | **0 errors, 6,693 warnings**, 2m18s | 6,693 |
| `bash tools/compare-warnings.sh <log> ci/warning-baseline.json V.SMART.Api` | `measured: 6693` → **"Gate: PASSED (equal to baseline)"**, exit 0 | ratchet against the committed baseline |
| `dotnet build V.SMART/V.SMART.Web/V.SMART.Web.csproj --no-incremental` | **0 errors, 6,697 warnings**, 2m18s | 6,697 |

The gate passing is the strongest of these: it fails on a total above baseline **and** on any
warning *code* absent from the baseline, so it would catch a newly-surfaced ambiguity warning
even at an unchanged total.

**Attempt 1's `6695` was never anomalous.** It was a *plain* `dotnet build` — restore included
— and KB-083 records the plain-build baseline as **6,695** too. The two extra warnings are the
`NU1608` restore warnings that the CI form moves out of the build log; KB-083 documents exactly
that offset. The `6,694` figure it was being compared against is the `--no-incremental` form, a
different measurement.

**Per-file confirmation, which is not a totals argument.** Filtering the CI build log by
originating file: **no warning originates on any line this diff added or altered.** The only
warning in `FundTransFilterVM.cs` is a pre-existing `CS8618` on `TransNo` at `:16`; the retyped
`Bank` property produces none. `IApprovalService.cs`, `FundTrans.cs`, `ReceiptsSub.cs`,
`ApplicationDbContext.cs`, `MaterialReq.cs` and `ProductionIssuAssyMapping.cs` produce zero
warnings at all; the other files' counts are entirely pre-existing nullable/analyzer noise on
lines this diff never touched.

### Tests

| Suite | Result |
|---|---|
| `dotnet test tests/V.SMART.Shared.Tests/V.SMART.Shared.Tests.csproj` | **86 passed, 0 failed, 0 skipped**, 11 s — 84 baseline + the 2 new guard tests |
| `dotnet test tests/V.SMART.Api.Tests/V.SMART.Api.Tests.csproj` | **117 passed, 0 failed, 0 skipped**, 2 s — unchanged from baseline, as expected |

**Guard demonstrated to fail, then reverted.** A temporary
`V.SMART/V.SMART.Shared/ViewModels/M2B04GuardProbe.cs` carrying
`using V.SMART.Shared.Pages.Master_Module_pages.Bank_Pages;` and a `Bank?` property was added.
Observed: `Failed: 2, Passed: 0` — the reflection test named all four violation paths (backing
field, property, getter return, setter parameter) and the source test named
`V.SMART/V.SMART.Shared/ViewModels/M2B04GuardProbe.cs:1`. The probe was deleted and the suite
returned to 86/86. Deleting the probe is a clean revert because it was a new, untracked file —
no existing file was touched to seed the violation.

### Not done, and why

- **The manual approval-workflow regression (implementation step 10).** Needs `V.SMART.Web`
  running against a tenant database and a login holding `UserAuthority` rows. Not attempted:
  no session may acquire or reuse a credential. This is the single acceptance criterion not
  met, and it is why the status is `Needs Review` rather than complete.
- **The MAUI head was not built.** KB-083 records it as not build-verified in this environment
  and the task file itself says to say so if it cannot be built. That half is **unverified**.
- **No `scripts/check-no-pages-references.sh`.** The task makes it the *fallback* for when
  `tests/V.SMART.Shared.Tests/` does not exist. It does exist, so the test is the guard, and
  the test's second fact covers the unused-`using` case the script was meant to catch — with
  the advantage that it already runs in CI (`.github/workflows/ci.yml`), which a new script
  would not until M0-07 wired it in. Recorded as a deviation from the literal file list.

### Deviations

1. `scripts/check-no-pages-references.sh` not created — see above. The task's own step 8 makes
   it conditional, and the condition is false.
2. Attempt 1's comment in `FundTransFilterVM.cs` was reworded (not reverted) so that criterion
   1's grep returns zero. Strictly this edits a file beyond deleting its `using`, but the
   comment is attempt-1 text on this same branch, not pre-existing code.
3. `docs/kb/execution/current-task.md`, `task-tracker.md`, `runner-state.md` and
   `failure-log.md` carry attempt 1's close-out edits. Attempt 2 **left all four untouched** —
   they are the orchestrator's files.

### Validation (2026-08-21) — independent verdict `PASS`

Verdict **`PASS`**, `failureCategory: none`, `scopeOk: true`. Attempt 2 of 3. Every number below
was re-derived by the validator, not taken from the implementer's report.

| Command | Observed | KB-083 baseline |
|---|---|---|
| `dotnet build V.SMART.Api --no-incremental` | **0 errors, 6694 warnings**, 2m24s | 6,694 |
| `dotnet build V.SMART.Web --no-incremental` | **0 errors, 6697 warnings**, 1m25s | 6,697 |
| Api CI form (`restore`, then `--no-restore --no-incremental -v normal`) | **0 errors, 6693 warnings** | 6,693 |
| `dotnet test tests/V.SMART.Shared.Tests` | **86 passed** (84 + 2 new) | 84 |
| `dotnet test tests/V.SMART.Api.Tests` | **117 passed** | 117 |
| `grep -rn "V.SMART.Shared.Pages" --include=*.cs V.SMART/V.SMART.Shared/ \| grep -v "/Pages/"` | **0 hits** | — |

**Warning conclusion: established, not merely plausible.** Each measurement was compared against
its *matching* baseline form, and the ratchet gate was run directly:
`compare-warnings.sh … V.SMART.Api` → `measured: 6693`, `=== Gate: PASSED (equal to baseline) ===`,
exit 0. The gate's per-code behaviour was verified in its source (`tools/compare-warnings.sh:34`,
implemented `:147-157`) rather than trusted: a warning **code** absent from the baseline fails it
even at an unchanged total — which is precisely the failure mode this task warned about, since
deleting a `using` can surface a previously-suppressed ambiguity warning. Attempt 1's `6695` was
the *plain*-build baseline, also 6,695; it was never anomalous, only compared to the wrong form.

**The guard was attacked, not trusted.** The implementer's own probe was confirmed deleted
(`M2B04GuardProbe` appears nowhere but as prose). The validator then seeded **two independent
violations** in different namespaces and different forms:

- *Unused `using`, no Pages-typed member* → the source `[Fact]` failed and named the file and
  line; the reflection `[Fact]` **passed**, exactly as its own comment
  (`NoPagesReferenceFromDomainTests.cs:20-23`) says it must, since an unused `using` leaves no
  metadata trace. **The documented blindness is real and honestly stated.**
- *No `using` at all, fully-qualified `global::…Bank_Pages.Bank` member* → **both** facts failed,
  reflection naming all four violation paths (backing field, setter parameter, property, getter
  return).

Both probes deleted, suite back to 86/86, tree byte-identical. The two facts are genuinely
complementary. CI runs them — `.github/workflows/ci.yml:191-195`.

**The headline dependency was dead text — Confirmed independently.** `Authorization.razor`
(747 lines) contains **zero** occurrences of the keyword `static` (`grep -c` → 0), and a regex
for any `class|enum|record|struct|interface` declaration in it returns nothing — so the
`using static` import set was provably empty on both counts. **M2-B04 removed a
documentation-level architectural violation and installed a guard; it did not sever a real
compile-time coupling.** R-11 and BR-APPR-001 now record this without overclaiming.

**Load-bearing verdict 1 of 16 — Confirmed.** `Bank.razor` exists under
`Pages/Master_Module_pages/Bank_Pages/`, so `Bank` is a real generated public component class;
the validator's probe resolved it by reflection. `FundTransFilterVM.cs:9` was the one directive
doing work, and its removal gave `CS0246`.

*Methodological note for the KB:* the task file's Correction 2 namespace scan
(`M2-B04.md:96-113`) grepped for **source-level** type declarations and so concluded `Bank_Pages`
declares none. It missed Razor-**generated** component classes — which is exactly why it
predicted `GridMode` as the risk and missed the real one. Outcome unaffected; the *why* was not
previously recorded.

**`FundTransFilter.Bank` retype verified behaviour-neutral.** The class is named
`FundTransFilter`, not `FundTransFilterVM`. Every `filter.` reference in
`FundTransRepository.cs:46-92` was enumerated — `TransactionType`, `SearchTerm`, `StartDate`,
`EndDate`, `TransactionMode`, `LedgerType`, `MinAmount`, `MaxAmount`, `SortBy`, `SortDescending`,
`Page`, `PageSize` — and **none is `.Bank`**; the bank data comes from the entity's own navigation
at `:43` (`.Include(x => x.Banks)`). `Fundtransactions.razor` has no `filter.Bank` reference.
`FundTransFilter` is not a `DbSet` and not an API DTO. Raising **Q-55** rather than deleting the
property was the right call.

**Criterion 5 is `MET` vacuously, and the vacuity is honest.** No type had to move. Every one of
`GridMode`'s ~30 references is inside `LabourTrack.razor` / `SalesTrack.razor` themselves
(declarations `:1262`, `:1807`); both sit in `@code`, making them *nested* types that a
namespace-level `using` cannot import — which is why `ApplicationDbContext.cs:84` was dead.

**The one criterion that is not met — criterion 9, manual approval-workflow regression: `NOT
CHECKABLE`.** It needs `V.SMART.Web` running against a tenant database as a user holding
`UserAuthority` rows; no session may acquire a credential (Q-14 / R-01 / Q-32). Neither the
implementer nor the validator attempted it or sought a credential. **To close it:** an operator
exercises list / approve / reject-with-reason / bulk-approve / bulk-reject on `/approval` and
confirms `ApprovalHistory` rows carry `Level`, `Status`, `ActionBy`, `ActionDate`, `Reason`. The
MAUI head is likewise **not build-verified** in this environment — unverified, not passing.

> **`PASS` here means "ready for human review", not "BR-APPR-001 observed intact".** The
> mechanical argument that nothing could have changed is as strong as such an argument gets — the
> diff touches no method body, signature, attribute or mapping, and the deleted import set was
> provably empty — but it remains an argument, not an observation. The reviewer should treat the
> manual approval regression as an open gate before `Completed`.

**Status: `Needs Review`.** Not `Completed` — only the repository owner sets that.
