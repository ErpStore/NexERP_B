---
doc_id: TASK-M2-D03
title: Blazor <-> Angular parity test for Customer Master
module: execution
task_id: M2-D03
milestone: M2
parent_task: n/a
task_type: Testing
priority: P0
status: Not Started
estimate: 3 d
depends_on: [M2-D02-03]
source_files:
  - V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/CustomerList.razor
  - V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/CustomerUpsert.razor
  - V.SMART/V.SMART.Api/Controllers/CustomersController.cs
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/MasterService/GeneralService/CustomerService.cs
  - V.SMART/V.SMART.Shared/Data/Master/General_Module/Customer.cs
  - V.SMART/V.SMART.Shared/Data/Master/General_Module/CustomerIndirect.cs
  - V.SMART/V.SMART.Shared/Data/Master/General_Module/ContactPerson.cs
  - V.SMART/V.SMART.Shared/Services/MultiCompanyService/TenantDbContextFactory.cs
entities: [Customer, CustomerIndirect, ContactPerson]
api_endpoints:
  - "POST /api/v1/customers"
  - "PUT /api/v1/customers/{id}"
  - "DELETE /api/v1/customers/{id}"
database_tables: [Customer, CustomerIndirect, ContactPerson]
business_rules: []
confidence: n/a
last_verified: 2026-08-22
---

# M2-D03 — Blazor ↔ Angular parity test for Customer Master

> **Re-specified for Angular on 2026-08-22 by [`M2-C12-05`](M2-C12-05.md).** The ⛔ STOP banner
> that stood here is removed **in the same change that removed the stack-specific instructions it
> guarded**: this file no longer specifies the ADR-003 stack.
> [`ADR-007`](../../decisions/ADR-007-angular-stack.md) is the governing stack decision.
>
> **This task was barely framework-specific to begin with, because it compares database rows.**
> The two write paths, the three tables, the normalisation problem, all thirteen scenarios with
> their `BR-CUST-*` citations, the three harness self-tests and every acceptance criterion come
> through **byte-identical**. What changed: the SPA under test is Angular; the driver is the
> Playwright setup `M2-C01` actually shipped (`npm run e2e`, `frontend/nexgen-web/e2e/`,
> chromium, `ng serve` on 127.0.0.1:4300), which is now a **verified** command rather than a
> to-be-established one; and the one permitted production-code change is a `data-testid` on an
> Angular component rather than a React one.

| Field | Value |
|---|---|
| Task ID | M2-D03 |
| Milestone | M2 — Foundation |
| Parent | — (top-level task in M2-D) |
| Type | Testing |
| Priority | P0 |
| Status | Not Started |
| Estimate | 3 d |
| Gate | G2 |

> **`business_rules` is empty because the `BR-CUST-*` ids are allocated by M2-D02-01.**
> Read them from `docs/kb/business-rules/customer-master-rules.md` when this task runs and
> list them here. This test **proves** those rules survived the migration; it defines none.

## Objective

Build an automated parity harness that performs the **same** Customer Master operation
twice — once through the Blazor UI and once through the Angular UI, both against the **same
tenant database** — and asserts that the **persisted database rows are equivalent**.

Not screenshots. Not API responses alone. **Rows.** Across `Customer`, `CustomerIndirect`
and `ContactPerson`, compared field by field with a declared, written normalisation rule for
every field that is legitimately allowed to differ.

Green parity is the last item on gate G2's checklist.

## Scope

- The parity harness: row snapshot, comparator, declarative normalisation policy, failure
  report.
- Playwright specs driving **both** UIs through their real forms.
- The thirteen scenarios below, each citing a `BR-CUST-*` rule.
- `docs/kb/migration/parity-testing.md` — the method, written for reuse by every `<W>-11`.
- CI wiring, with a visible skip when the parity database is absent.

## Out of Scope

- **Fixing anything the parity test finds.** A divergence is a defect in `CustomerService`
  or the controller, and it gets its **own** task, branch and characterisation test —
  because a fix there changes behaviour for *both* UIs.
- **Permission testing.** That is M2-A03's harness; do not duplicate it.
- **Any change under `V.SMART/`**, and any change to the Customer feature folder beyond a
  justified `data-testid`.
- **Any production database.** Non-production tenant only, and fail loudly if pointed
  elsewhere.

## Why This Task Exists

**Every other test in M2 asks "does the new thing work?" This one asks "does the new thing
do the same thing?" Those are different questions, and only the second one protects the
business.**

M2-D02-01 extracted roughly 630 lines of business logic out of `CustomerUpsert.razor`'s
`@code` block into `CustomerService`. M2-D02-02 wrapped that service in a controller.
M2-D02-03 built an Angular screen against it. Each of those tasks tested itself, and each
could pass while the *composition* silently changed behaviour — a trimmed string, a
`DateTime.Now` that moved, a child row that used to be dropped and now is not, a
`StateName` that is no longer denormalised, an `OpenBalPndg` that no longer resets. **None
of those would fail a unit test written by the person who moved the code.** They fail a
parity test, and only a parity test.

It exists specifically because:

- **The extraction was the risk.** Migration principle 3 says logic leaves `@code`
  "verified against the running Blazor app". This task **is** that verification, made
  automated and repeatable rather than a one-off manual pass.
- **It is the template for every `<W>-11`.** [KB-080 §10](../README.md#10-module-migration-task-pattern)
  puts a Blazor ↔ SPA parity test in every M3 and M4 wave. Whatever comparison method,
  normalisation policy and reporting format this task establishes is what ~20 later waves
  reuse. **Design it to be parameterised over an entity, not hard-wired to Customer.**
- **It defends the strangler-fig contract.** Both UIs serve the same database throughout
  (KB-080 §2), which is precisely what makes rollback free of data migration. That
  guarantee is only real if both UIs write the same rows. This test is the evidence.
- **G2 requires it.** "Parity test M2-D03 passes" is an explicit exit-gate line.

## Prerequisites

- **M2-D02-03** Completed — the Angular screens exist and were manually round-tripped.
- **M2-D02-02**, **M2-D02-01** Completed — the controller and the extraction.
- **M2-C01** — the Playwright setup, which drives both UIs. It exists:
  `frontend/nexgen-web/playwright.config.ts`, `npm run e2e`, chromium, with the config
  starting `ng serve` itself on 127.0.0.1:4300.
- **M0-12-01** — a test project and its CI wiring, as the model for adding this one.
- **A dedicated, non-production tenant database** that can be reset between runs. This is
  an **infrastructure prerequisite, not a coding one** — escalate it on day 1. A parity
  suite that cannot reset state is a parity suite that passes once.
- **Two runnable hosts against that one tenant:** `V.SMART.Web` (Blazor) and `V.SMART.Api`
  + the Angular app. Both must resolve to the **same** `TenantInfo` row and therefore the
  same connection string.

## Dependencies

| Dependency | Class | Note |
|---|---|---|
| M2-D02-03 — Angular screens | Hard | One of the two subjects under test. |
| M2-D02-02 — `CustomersController` | Hard | The SPA path's server side. |
| M2-D02-01 — extraction + `customer-master-rules.md` | Hard | Defines what "the same" means. Every scenario traces to a `BR-CUST-*` rule. |
| M2-C01 — Playwright + CI | Hard | The driver and the pipeline. |
| M0-12-01 — test project pattern | Hard | How a test project is wired into CI here. |
| Non-production tenant DB with reset | **Deployment** | Escalate on day 1. Without it this task cannot complete. |
| M2-A05 — tenant resolution + CORS | Hard | Both UIs must land on one tenant. |
| M3-x `<W>-11` steps | Information | ~20 later waves reuse this harness. Build it generic. |
| M5-06 — parity testing per module | Information | This task is M5-06's first instance and its reference implementation. |

## Relevant Documentation

| Doc | Path | Why |
|---|---|---|
| KB-083 | `docs/kb/execution/prompt-template.md` | Generation rules + verified commands, backend and frontend |
| KB-080 §9, §10, §13 | `docs/kb/execution/README.md` | G2, the wave pattern's step 11, and M5-06 |
| **customer-master-rules** | `docs/kb/business-rules/customer-master-rules.md` | **M2-D02-01's output.** The scenario source. |
| KB-014 | `docs/kb/architecture/multi-tenancy.md` | `TenantInfo`, `TenantDbContextFactory`, why both UIs must share one tenant |
| KB-012 | `docs/kb/architecture/database-architecture.md` | Entity/table shapes for `Customer` and its children |
| KB-084 | `docs/kb/execution/review-templates.md` | The milestone-review format this feeds |
| ADR-002 | `docs/kb/decisions/ADR-002-rest-api-layer.md` | Why messages are surfaced verbatim — parity covers messages too |
| KB-003 | `docs/kb/investigation-registry.md` | INV-005, INV-024 |

## Investigation Requirements

**Reuse. No new investigation.**

- **INV-005** (multi-tenancy resolution and isolation) — Complete → KB-014. Reuse it to
  configure both hosts against one tenant. Do not re-derive the resolution chain.
- **INV-024** — Partial, Customer complete. `customer-master-rules.md` is the scenario
  source. **Every parity scenario must cite the `BR-CUST-*` rule it exercises.** A scenario
  that cites no rule is testing the harness, not the migration.

**One thing must be determined empirically, and it is the crux of this task:** the
**normalisation policy** — which columns are allowed to differ between the two paths, and
why. Nobody can write that list in advance from the source; it comes from running the same
operation twice and looking at the diff. Producing that policy, in writing, with a
justification per column, is a deliverable of this task in its own right.

## Relevant Existing Code

| Area | Path | Role |
|---|---|---|
| Parity harness | `tests/V.SMART.Parity.Tests/` | **TO BE CREATED** — name to match the convention M0-12-01 established for `tests/V.SMART.Shared.Tests/` and M2-A06 for `tests/V.SMART.Api.Tests/`; confirm before creating |
| Playwright specs | `frontend/nexgen-web/e2e/` | Drives both UIs. The config lives at `frontend/nexgen-web/playwright.config.ts` and already binds `127.0.0.1` deliberately — see the KB-083 note |
| Blazor pages | `V.SMART/V.SMART.Shared/Pages/Master_Module_pages/Customer_Pages/` | **Read-only.** Selectors only. |
| Angular screens | `frontend/nexgen-web/src/app/features/masters/customer/` | **Read-only.** Selectors only. |
| Entities | `V.SMART/V.SMART.Shared/Data/Master/General_Module/` | Column inventory for the comparator |
| Tenancy | `V.SMART/V.SMART.Shared/Services/MultiCompanyService/TenantDbContextFactory.cs` | How a tenant connection string is obtained |
| Rules | `docs/kb/business-rules/customer-master-rules.md` | Scenario source |
| Policy | `docs/kb/migration/parity-testing.md` | **TO BE CREATED** — the method + normalisation policy |

## Existing Behavior to Preserve

This task changes no behaviour. It **measures** it. The facts below define what the
comparator must handle.

### The two write paths (Confirmed, 2026-08-12)

| | Blazor | Angular SPA |
|---|---|---|
| Entry | `/customer/create`, `/customer/update/{CustId:int}` — `CustomerUpsert.razor:1-2` | `/masters/customers/new`, `/masters/customers/:id` — KB-053 |
| Server logic | `CustomerService` (post-M2-D02-01) | `CustomerService` (**the same code**) via `CustomersController` |
| Transaction | `BeginTransactionAsync` inside the service | Identical |
| Database | One tenant database, resolved via `ITenantProvider`/`ITenantDbContextFactory` | The same |

**Both paths run the same service after M2-D02-01.** That is exactly why parity should hold
— and exactly why a divergence, if one appears, is a real defect in the extraction or the
controller rather than an expected difference between two implementations. **A parity
failure here is never "just a test problem."**

### The three tables the comparator covers

| Table | Key | Notes |
|---|---|---|
| `Customer` | `CustId` | ~40 scalar columns; entity at `Data/Master/General_Module/Customer.cs:9-146` |
| `CustomerIndirect` | `AltCustId`, FK `CustId` | Consignees; `CustomerIndirect.cs:6-56` |
| `ContactPerson` | `Id`, FK `CustId` | `ContactPerson.cs` |

`CustomerConfiguration` (`Data/Configuration/MasterConfiguration/CustomerConfiguration.cs:16-52`)
declares the key and eight indexes — index definitions are irrelevant to parity, but the
file is worth reading to confirm nothing computed lives there. Customer is one of only
three entities with an explicit configuration
(`ApplicationDbContext.cs:430`, `:432`, `:434` — `grep -c ApplyConfiguration` returns 3,
Confirmed).

### Fields that will legitimately differ — the normalisation problem

**These are known in advance and are why a naive row comparison fails immediately.** The
policy must cover at least:

| Column(s) | Why it differs | Suggested normalisation |
|---|---|---|
| `CustId`, `AltCustId`, `ContactPerson.Id` | Identity columns; two runs get different values | Compare **structurally**: same child count, matched by business key (`AltCustName`, `ContactPersonName`), not by id |
| `CreatedDate`, `ModifiedDate`, `OpenBalDate` | `DateTime.Now` at write time (BR-CUST-010, BR-CUST-011) — the two runs are seconds apart | Compare **presence and ordering**, and equality to a tolerance (e.g. same date, within N minutes). Never compare exact ticks. |
| `CreatedBy`, `ModifiedBy` | The two runs may authenticate as different users | Either use **the same user** for both runs (preferred) or normalise to a placeholder. State which. |
| `CustName` | Must be unique (BR-CUST-001), so the two runs cannot use the same name | Use a deterministic pair — `PARITY-{run}-BLAZOR` / `PARITY-{run}-SPA` — and normalise the discriminator away before comparing |

**Every entry in this table must be justified in writing.** The failure mode this task must
avoid above all others: **normalising away a real difference.** A parity suite that
excludes a column because it "kept failing" is worse than no parity suite, because it
reports green while the behaviour has drifted. **Adding a column to the exclusion list
requires a written reason and a reviewer.** Say so in the policy document.

### Scenarios, each tracing to a rule

Derive the full list from `customer-master-rules.md`. At minimum:

| # | Scenario | Rule |
|---|---|---|
| 1 | Create a Local customer, all required fields, no children | BR-CUST-006, -007, -008, -011 |
| 2 | Create with two consignees and two contacts | BR-CUST-009, -013 |
| 3 | Create an Exports customer — GST forced to `URP`, state 99, PAN cleared | BR-CUST-005 |
| 4 | Create with a 15-character GST — PAN derived | BR-CUST-002 |
| 5 | Create with each business type — `SupTyp` set and default | BR-CUST-004 |
| 6 | Create with an opening balance — `OpenBalPndg` and `OpenBalDate` derived | BR-CUST-010 |
| 7 | Edit: change scalar fields only | BR-CUST-011 |
| 8 | Edit: add one child, update one, remove one | BR-CUST-013 |
| 9 | Edit: include a blank-named child row — **silently discarded** | BR-CUST-012 |
| 10 | Edit an existing customer — confirm `OpenBalPndg` resets identically in both paths | BR-CUST-010 |
| 11 | Attempt a duplicate name — **both paths refuse, with the same message, and write nothing** | BR-CUST-001 |
| 12 | Attempt to delete a referenced customer — both refuse with the same message | BR-CUST-014 |
| 13 | Delete a deletable customer — same rows removed, same audit entry | BR-CUST-015 |

Scenarios 9, 10 and 11 matter most. They are the three behaviours M2-D02-01 flagged as
probable defects and **deliberately preserved**. If the SPA path "fixed" any of them, this
test catches it — and that is the single most likely real failure.

**Negative scenarios (11, 12) must assert on rows too:** after a refusal, the database must
be **unchanged**. A refusal that writes a partial row is exactly the kind of transaction bug
an extraction can introduce.

## Business Rules

**Preserved unchanged.**

This task writes no business logic and moves none. It observes the logic that already
exists in `CustomerService` through two different front doors and asserts the results agree.
Every scenario cites the `BR-CUST-*` rule it exercises; the rules themselves are
M2-D02-01's, recorded with `file:line` evidence in `customer-master-rules.md`.

The one thing it *does* create is a **definition of "the same"** — the normalisation policy.
That is a judgement, not logic, and it is why the policy needs writing down with a reason
per column and a reviewer for every addition. **An over-broad normalisation policy is
indistinguishable from no parity test at all**, and it is far more dangerous because it
reports green.

If a scenario fails, the fix belongs in `CustomerService` — one place, benefiting both UIs
— as a **separate task**. This task reports; it does not repair.

## Target Result

1. `docs/kb/migration/parity-testing.md` — the method: how a scenario is expressed, how the
   two runs are isolated, how rows are captured, the normalisation policy with a
   justification per column, and how a failure is reported. **Written for reuse by every
   `<W>-11`, not for Customer specifically.**
2. A parity harness that, per scenario: resets to a known state → drives Blazor → snapshots
   rows → resets → drives the Angular SPA → snapshots rows → compares under the policy →
   reports a field-level diff.
3. **The comparison reads the database directly**, through EF or SQL, against the same
   tenant connection both UIs used. Not the API. Not the DOM.
4. At least the 13 scenarios above, each citing its `BR-CUST-*` rule.
5. Failure output naming the **table, key, column, Blazor value and SPA value**. "Parity
   failed" is not a usable result.
6. Runs in CI. Skips cleanly and visibly when the parity database is unavailable — **skip,
   never silently pass.**
7. A recorded run against a real tenant database, with its result attached to the G2 review.
8. `V.SMART/**` and the Angular feature folders **unchanged** — this task adds tests only.

## Implementation Requirements

1. Read the knowledge base, **starting with `customer-master-rules.md`**. Search KB-003;
   reuse INV-005 and INV-024.
2. **Secure the parity database first.** Confirm a non-production tenant, its `TenantInfo`
   row, and a reset mechanism. If none exists, **report Blocked immediately** — this is a
   day-1 escalation, not a day-3 discovery.
3. **Confirm both hosts resolve to the same tenant.** Blazor resolves by host /
   `wwwroot/config/tenant.json`; the API resolves from the JWT `TenantId` claim after login
   (ADR-002 §5). Verify empirically that both produce the same connection string. **If they
   do not, every subsequent result is meaningless.** Record how you verified it.
4. **Choose and document the reset mechanism.** Options, in preference order: restore a
   snapshot; run a truncate/reseed script; or delete by a scenario-scoped key prefix.
   Whichever you choose, it must be **deterministic and idempotent**. Record why.
5. **Build the row-snapshot component**: given a `CustId`, return the `Customer` row plus
   its `CustomerIndirect` and `ContactPerson` rows as ordered, comparable structures. Read
   **every** column — enumerate from the EF model rather than hand-listing, so a future
   column is not silently omitted. That is a real risk: a column added later would never be
   compared.
6. **Build the comparator + normalisation policy.** Structural child matching by business
   key; timestamp tolerance; identity exclusion; the name discriminator. Emit a
   **field-level** diff. Make the policy **data, not code** — a declarative rule set the
   harness reads — so `<W>-11` waves can supply their own without forking the harness.
7. **Build the drivers.** Playwright against both UIs, driving real forms and real clicks —
   **not** the API for the SPA side. Calling the API directly would skip exactly the layer
   the parity test exists to check. Prefer accessible role/label selectors over
   `data-testid`; the Angular screens were built to a stated a11y contract, so those
   selectors exist.
8. **Implement the 13 scenarios**, each tagged with its `BR-CUST-*` id.
9. **Implement the negative scenarios properly**: assert both the message text (verbatim,
   per ADR-002 §4) **and** that no rows changed.
10. **Wire into CI** following M0-12-01's pattern, and note that the `frontend-e2e` job has
    **never executed** on a hosted runner (KB-083) — an execution session may not push. Gate
    on the parity database's availability; skip loudly when absent.
11. **Run it and triage every diff.** For each: is it a genuine behavioural difference (a
    defect — fix in the service, benefiting both UIs) or a legitimate environmental
    difference (a normalisation rule — write the justification)? **Default to "defect."**
12. **Record the outcome** in `parity-testing.md` and in KB-084's milestone-review format
    for G2.
13. Review the full diff, update documentation, commit. Do not merge.

### Files Expected to Change

- CI workflow — add the parity job
- `docs/kb/migration/parity-testing.md` — created (below)
- `docs/kb/business-rules/customer-master-rules.md` — a **Parity status** column or section
  per rule
- `docs/kb/execution/prompt-template.md` (KB-083) — the parity command, once it has run
- `docs/kb/execution/tasks/M2-D03.md` (this file)

### Files Expected to Be Created

- `tests/V.SMART.Parity.Tests/` — **TO BE CREATED**. Confirm the naming convention against
  `tests/V.SMART.Shared.Tests/` (M0-12-01) and `tests/V.SMART.Api.Tests/` (M2-A06) before
  choosing a name.
  - the row-snapshot component
  - the comparator + declarative normalisation policy
  - the 13 scenario definitions
- Playwright specs driving both UIs, under `frontend/nexgen-web/e2e/`
- `docs/kb/migration/parity-testing.md` — **TO BE CREATED**

### Files That Must Not Change

- `V.SMART/**` — **the entire backend**, Blazor pages included. If a parity failure needs a
  service fix, that is a **separate task**: it changes behaviour for both UIs and needs its
  own branch, review and characterisation test. Report it; do not fold it in here.
- `frontend/nexgen-web/src/app/features/masters/customer/**` — same reasoning. A client fix
  belongs to M2-D02-03's follow-up.
- `V.SMART/V.SMART.Shared/Migrations/**` — no schema change.
- Any production database. The parity suite runs against a **non-production tenant only**,
  and must fail loudly rather than proceed if pointed at anything else.

## API Changes

**None.** This task adds no endpoint and modifies none.

| Aspect | Value |
|---|---|
| Method / Route | n/a — consumes `POST`/`PUT`/`DELETE /api/v1/customers` indirectly, via the SPA UI |
| Request | n/a |
| Response | n/a |
| Authentication | The harness logs in through each UI as a normal user. Both runs use the **same** user, so `CreatedBy`/`ModifiedBy` compare directly. Credentials come from CI secrets, never from the repository. |
| Authorization | The parity user needs full Customer rights. Permission behaviour is M2-A03's harness, not this one — **do not duplicate it here.** |
| Tenant context | **The crux of this task.** Both UIs must resolve to the same `TenantInfo` and therefore the same connection string. Verified empirically, not assumed. |
| Error model | Negative scenarios assert that both paths surface the **same message text** — Blazor's toast and the SPA's `ProblemDetails.title` must carry the service's string identically (ADR-002 §4). |
| Paging | n/a |
| Filtering | n/a |
| Sorting | n/a |
| Idempotency | n/a — but the **harness itself** must be idempotent: repeated runs must not accumulate rows or depend on run order. |

## Frontend Changes

| Aspect | Value |
|---|---|
| Route | n/a — the harness navigates existing routes; it adds none |
| Component | n/a. **Exception:** if a control cannot be reliably selected, adding a stable `data-testid` to an Angular component is permitted, is the **only** production-code change this task allows, and must be called out in the diff review. Prefer accessible-role selectors first. |
| API integration | n/a — the SPA path is driven **through the UI**, never through the API |
| State | n/a |
| Validation | n/a — but scenario 9 asserts that a blank-named child row is discarded rather than blocked, matching BR-CUST-012 |
| Permissions | n/a — the parity user has full rights; permission testing is M2-A03's |
| Loading state | The harness must wait on deterministic conditions, not fixed sleeps. Flaky waits produce false parity failures, which erode trust in the suite faster than a real bug does. |
| Empty state | n/a |
| Error state | Negative scenarios assert the error text and that no rows changed |
| Accessibility | n/a — but prefer role/label selectors, which incidentally keeps the tests honest about accessibility |
| Responsive behaviour | n/a — the harness runs at a fixed desktop viewport |

## Testing Requirements

This task *is* a test. What follows is how the harness itself is validated — a parity suite
you have not tried to break is a parity suite you should not trust.

| Level | What |
|---|---|
| Harness self-test | **Deliberately inject a difference** (e.g. write a customer directly with a different `PinCode`) and confirm the comparator **fails** with a field-level diff naming the column. A comparator that has never gone red has never been shown to work. |
| Harness self-test | Run the same scenario twice through the **same** UI; parity must be green. If Blazor-vs-Blazor fails, the normalisation policy is wrong, not the migration. **Run this before ever running Blazor-vs-SPA.** |
| Harness self-test | Confirm the column enumeration covers **every** mapped property of all three entities — add a temporary property and confirm the snapshot picks it up, or assert the column count against the EF model. |
| Harness self-test | Confirm the reset mechanism is idempotent: run the suite twice in a row, green both times, with no residual rows. |
| Parity | The 13 scenarios above, each citing its `BR-CUST-*` rule |
| Parity — negative | Duplicate name and delete-refusal: same message **and** no row changes |
| CI | The suite runs on the pipeline and **skips visibly** when the parity database is absent |

## Acceptance Criteria

- [ ] `docs/kb/migration/parity-testing.md` exists with valid KB frontmatter and describes
      the method **generically**, usable by any `<W>-11`.
- [ ] It contains the normalisation policy with a **written justification per column**, and
      states that adding a column requires a reason and a reviewer.
- [ ] The comparison reads **database rows** — through EF or SQL against the tenant
      connection — not API responses and not the DOM. Objectively verifiable from the code.
- [ ] Both UIs are proven to resolve to the **same tenant database**, with the verification
      method recorded.
- [ ] The row snapshot covers `Customer`, `CustomerIndirect` and `ContactPerson`, and
      enumerates columns from the EF model rather than a hand-written list.
- [ ] Child rows are matched **structurally** by business key, not by identity value.
- [ ] All 13 scenarios exist, each tagged with the `BR-CUST-*` rule it exercises.
- [ ] Scenarios 9, 10 and 11 — the three deliberately-preserved probable defects — are
      present and green.
- [ ] Negative scenarios assert both the verbatim message **and** that no rows changed.
- [ ] A failure report names table, key, column, Blazor value and SPA value.
- [ ] The injected-difference self-test makes the comparator go **red**.
- [ ] The Blazor-vs-Blazor self-test is **green**.
- [ ] The suite is idempotent across consecutive runs.
- [ ] The suite runs in CI and **skips visibly** — never silently passes — when the parity
      database is unavailable.
- [ ] A real run against a real tenant database is recorded, with its result.
- [ ] The full `git diff` shows **no** change under `V.SMART/`, and **none** under
      `frontend/nexgen-web/src/app/features/` **except** any `data-testid` additions, each
      individually justified in the diff review.
- [ ] No file under `V.SMART/V.SMART.Shared/Migrations/` changed.
- [ ] `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` reports 0 errors.
- [ ] `npm run e2e` exits 0 for the parity specs, or skips visibly for a documented
      infrastructure reason.
- [ ] Every parity divergence found is either fixed by a **separate** task or recorded in
      `docs/kb/open-questions.md` with a `Q-xx` id — none is silently normalised away.
- [ ] `customer-master-rules.md` records a parity status per rule.

## Verification Commands

From the repository root:

```bash
dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj
```

```bash
dotnet test tests/V.SMART.Shared.Tests/V.SMART.Shared.Tests.csproj
```

```bash
git status --porcelain
```

From `frontend/nexgen-web/`:

```bash
npm run e2e
```

**Read the full `git diff` before committing** and confirm `V.SMART/**` and
`V.SMART/V.SMART.Shared/Migrations/**` are untouched.

> **The parity suite's own command does not exist yet** — it depends on the project this task
> creates (`tests/V.SMART.Parity.Tests/`, or whatever name matches the existing convention).
> Once it has actually run, **add it to
> [KB-083's verified table](../prompt-template.md#verified-repository-commands)** with its
> observed result, and record it in `parity-testing.md` so every later `<W>-11` inherits it.
> That is how the verified-commands table grows; do not quote an unmeasured command as
> though it were verified.
>
> `npm run e2e` **is** verified (KB-083 § Verified frontend commands, measured 2026-08-21:
> 1 passed, chromium, Playwright starting `ng serve` on 127.0.0.1:4300). It needs
> `npx playwright install chromium` once per machine.

## Documentation Updates

| Document | Change | Frontmatter |
|---|---|---|
| `docs/kb/migration/parity-testing.md` | **Created.** The method, the isolation strategy, the normalisation policy with per-column justification, the failure-report format, and the exact commands. Written for reuse by every `<W>-11`. | full KB frontmatter; `status: complete`; `last_verified` today |
| `docs/kb/business-rules/customer-master-rules.md` | Add a **parity status** per `BR-CUST-*` rule: covered / not covered / covered with a normalisation caveat. | bump `last_verified` |
| `docs/kb/execution/prompt-template.md` (KB-083) | Add the parity-suite command to the **Verified repository commands** table once it has actually run, with its observed result. | bump `last_verified` |
| `docs/kb/execution/README.md` (KB-080) | Tick "Parity test M2-D03 passes" in G2's checklist; link §10's step 11 to `parity-testing.md`. | bump `last_verified` |
| `docs/kb/open-questions.md` (KB-004) | A `Q-xx` for every divergence found and not fixed. Check `git branch --no-merged master` before claiming an id. | bump `last_verified` |
| `docs/kb/execution/tasks/M2-D03.md` (this file) | Fill `business_rules` with the `BR-CUST-*` ids; record the Execution Record. | bump `last_verified` |
| `docs/kb/execution/task-tracker.md` (KB-081) | Update the M2-D03 row. | n/a |

### Negative results to record

**No new investigation, but a substantial negative-result record.** Reuse INV-005
(multi-tenancy) and INV-024 (Customer triage). Record:

- "Both UIs were verified to resolve to the same tenant database by **\<method\>**." State
  the method — this is the assumption everything else rests on.
- The full normalisation policy, or a link to it, so no later wave re-derives which columns
  legitimately differ.
- "Ran the same scenario twice through Blazor alone — green — before comparing Blazor to
  the SPA." That ordering is the harness's own control.
- Every divergence found, whether fixed, deferred or explained.

If parity revealed a **behavioural** difference, that is a defect against M2-D02-01 or
M2-D02-02 and belongs in the risk register and open questions — not in a new investigation.

## Completion Conditions

- Every acceptance criterion above is met, with command output recorded.
- The harness has been shown to go **red** on an injected difference, and green on a
  Blazor-vs-Blazor control run, in that order.
- `parity-testing.md` exists and is generic — this task's real deliverable is the method
  that ~20 later waves reuse, not the Customer scenarios.
- **Only the repository owner may set `Completed`**
  ([KB-088](../workflow.md#who-may-set-completed)).

## Git Strategy

| | |
|---|---|
| **Branch** | `migration/M2-D03-customer-parity-test` |
| **Commit** | `M2-D03: Add Blazor/Angular parity harness and Customer Master parity scenarios` |
| **Expected changed files** | `tests/V.SMART.Parity.Tests/**` (new); Playwright specs under `frontend/nexgen-web/e2e/` (new); the CI workflow; `docs/kb/migration/parity-testing.md` (new); `docs/kb/business-rules/customer-master-rules.md`; `docs/kb/execution/prompt-template.md`; `docs/kb/execution/README.md`; `docs/kb/open-questions.md`; `docs/kb/execution/tasks/M2-D03.md` |
| **Rollback** | Revert the branch's merge commit. The change is test code and documentation only — no production code, no schema change, no data change outside the disposable parity database. Reverting removes the parity gate, which **weakens** G2; prefer fixing forward. If a `data-testid` was added to a component, that single change reverts with it and is harmless. |

Do not merge. Do not push to `master`. Leave the branch for review.
