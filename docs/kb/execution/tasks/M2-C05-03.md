---
doc_id: TASK-M2-C05-03
title: Empty / loading / error states + export
module: execution
task_id: M2-C05-03
milestone: M2
parent_task: M2-C05
task_type: Frontend
priority: P1
status: Needs Review
estimate: 2 d
depends_on: [M2-C05-01]
source_files:
  - V.SMART/V.SMART.Shared/Services/ExcelExportService.cs
  - V.SMART/V.SMART.Shared/Services/Extensions/IJSRuntimeExtensions.cs
  - V.SMART/V.SMART.Shared/Components/DetailsModal.razor
  - V.SMART/V.SMART.Shared/Components/ProcessingOverlay.razor
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/SalesService/MfgPoService.cs
api_endpoints:
  - "GET /api/v1/{resource}/export"
business_rules: [BR-SO-001]
confidence: n/a
last_verified: 2026-08-26
---

# M2-C05-03 — Empty / loading / error states + export

> **Re-specified for Angular on 2026-08-22 by [`M2-C12-03`](M2-C12-03.md).** The ⛔ STOP banner
> that stood here is removed **in the same change that removed the stack-specific instructions it
> guarded**: this file no longer specifies the ADR-003 stack.
> [`ADR-007`](../../decisions/ADR-007-angular-stack.md) is the governing stack decision —
> **Angular 22.x + PrimeNG** — and [KB-050](../../frontend-new/react-architecture.md), rewritten
> for Angular by `M2-C00`, is the structure this work is built into.
>
> **The specification this replaces is not deleted from the record.** Its *intent* survives
> unchanged, and in this file the intent *is* almost the whole content: five distinct states
> instead of one undifferentiated "No data found.", a 409 business-rule message rendered
> **byte-for-byte** as the server wrote it, a 403 rendered inline rather than as a redirect, and
> export that fetches bytes from the server and never builds a spreadsheet in the browser
> ([ADR-005](../../decisions/ADR-005-reporting-and-printing.md)). What changed is the *how* —
> Angular templates and signals instead of components and hooks, `HttpClient` with
> `responseType: 'blob'` instead of a request library, and the workspace's own test harness.
> **BR-SO-001 and every `file:line` citation into `V.SMART/` come through byte-identical**, and
> both quoted sentences were re-verified against
> `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/SalesService/MfgPoService.cs` on
> 2026-08-22: still at `:488` and `:598`, still word-for-word.

| Field | Value |
|---|---|
| Task ID | M2-C05-03 |
| Milestone | M2 — Foundation |
| Parent | M2-C05 |
| Type | Frontend |
| Priority | P1 |
| Status | Not Started |
| Estimate | 2 d |
| Gate | G2 |

## Objective

Complete `DataGrid` by implementing the five non-happy-path states it must render, and by
wiring **server-side** export.

States, per [KB-051 §State patterns](../../frontend-new/design-system.md#state-patterns):

1. **Loading (first)** — skeleton rows matching the final column layout.
2. **Loading (refetch)** — previous data retained plus a subtle top progress bar.
3. **Empty (no data yet)** — illustration + explanation + primary action.
4. **Empty (filtered to nothing)** — distinct copy + **Clear filters**.
5. **Error** — the server's message, the `traceId`, and Retry. Plus the two specialised
   error cases: **403 → the inline permission-denied state**, and **409 → the server's
   business-rule message rendered verbatim**.

Export: a toolbar action that requests a file from a server endpoint and saves it. The client
never constructs a spreadsheet, CSV or PDF.

## Scope

The state components, the export service and their wiring into the grid's template slots, plus
the KB updates listed under *Documentation Updates*. Nothing else.

## Out of Scope

- **Re-creating the feedback primitives.** `EmptyState`, `ErrorState`, `Skeleton`,
  `BusyOverlay`, the permission-denied state and the toast are **M2-C04-03's** deliverables.
  Compose them; if one is inadequate, that is a change request against M2-C04-03.
- **Adding an export endpoint.** If none exists, record the gap and report Blocked.
- Report and print export — `GET /api/v1/reports/{slug}/export` and
  `GET /api/v1/{resource}/{id}/print` are **M2-C09's**. This task covers **list export** only.
- Column preferences — **M2-C05-02**, which shares the toolbar slot.

## Why This Task Exists

- **It is on the M2 critical path.** [KB-080 §9 Critical Path](../README.md#critical-path)
  runs `… → M2-C05-01 → M2-C05-03 → M2-D01`. Notably it is *this* child, not M2-C05-02, that
  the vertical slice waits on — because gate **G2** requires *"… server paging, validation,
  error contract, **Excel export**"*.
- **The empty/loading/error triad is a requirement of the component, not a later polish
  pass.** [KB-051](../../frontend-new/design-system.md#data-display) lists
  *"empty/loading/error states built in"* among `DataGrid`'s defining properties. A grid
  shipped without them ships a defect.
- **Business-rule messages are product UX and must not be rewritten.**
  [KB-050 §Error handling](../../frontend-new/react-architecture.md#error-handling) requires
  a 409 to surface *"the **server's message verbatim** — these strings are product UX"*. The
  strings are real, specific, and were written for users. Example, **Confirmed** 2026-08-12
  and re-verified 2026-08-22:
  `"Cannot delete this Sales Order as a Sales DC transaction exists."` at
  `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/SalesService/MfgPoService.cs:488`
  (BR-SO-001). Replacing that with "An error occurred" destroys information the user needs
  and that no client can reconstruct.
- **Export already exists server-side and must be reused, not rebuilt.**
  [ADR-005](../../decisions/ADR-005-reporting-and-printing.md) is explicit: Excel
  export/import stays server-side behind endpoints. `ExcelExportService` is already there
  (`V.SMART/V.SMART.Shared/Services/ExcelExportService.cs:24`, `:113`).

## Prerequisites

- **M2-C05-01** — the `DataGrid` core and its `empty` / `error` / `toolbar` template seams.
- **M2-C04-03** — feedback primitives: modal, drawer, toast, states. `EmptyState`,
  `ErrorState`, `Skeleton`, `BusyOverlay`, the permission-denied state and `Toast` are that
  task's deliverables; this task **composes** them, it does not re-create them.
- **M2-A06** — exception middleware producing `ProblemDetails` with correlation ids. Without
  it there is no `traceId` to show and no `title` to render verbatim.
- A server export endpoint (M2-B06 for generic file download, or the resource's own
  `export` action per M2-B03's controller template).

## Dependencies

| Dependency | Class | Note |
|---|---|---|
| M2-C05-01 | Hard | The seams this task fills. |
| M2-C04-03 | Hard | `EmptyState`, `ErrorState`, `Skeleton`, `Toast`, the permission-denied state. Do not duplicate them inside `DataGrid`. |
| M2-A06 | Hard | `ProblemDetails` shape + correlation id. The error state's contract. |
| M2-B06 | Hard | File download endpoint / content-disposition conventions. |
| M2-B03 | Information | Controller template — where an `export` action lives on a resource. |
| M2-B10 | Soft | Generated client, including the binary-response operation. |
| ADR-005 | Information | Binding: the client never generates files. |
| M2-C05-02 | Deployment | Shares the toolbar slot in the grid template. |
| M5-09 | Testing | axe in CI + manual keyboard pass. This task's own `axe` scan (test 17) is written here, not deferred to it. |

## Relevant Documentation

| doc_id | Path | Why |
|---|---|---|
| KB-051 | [`frontend-new/design-system.md`](../../frontend-new/design-system.md#state-patterns) | The five states, precisely specified. |
| KB-050 | [`frontend-new/react-architecture.md`](../../frontend-new/react-architecture.md#error-handling) | The per-layer error table, including the 409-verbatim rule. |
| ADR-005 | [`decisions/ADR-005-reporting-and-printing.md`](../../decisions/ADR-005-reporting-and-printing.md) | Export and printing stay server-side. |
| KB-030 | [`business-rules/business-rule-inventory.md`](../../business-rules/business-rule-inventory.md) | BR-SO-001 — an example of the refusal messages being displayed. |
| ADR-007 | [`decisions/ADR-007-angular-stack.md`](../../decisions/ADR-007-angular-stack.md) | Stack. |
| KB-083 | [`prompt-template.md`](../prompt-template.md#verified-repository-commands) | The only source of verified commands. |
| — | `tasks/M2-A06.md` | The exact `ProblemDetails` fields. |

## Relevant Existing Code

| Area | Role |
|---|---|
| `frontend/nexgen-web/src/app/shared/components/data-grid/` | **TO BE CREATED (M2-C05-01).** Extended here. |
| `frontend/nexgen-web/src/app/shared/components/` | Home of M2-C04-03's feedback primitives, composed here. |
| `frontend/nexgen-web/src/app/core/http/error.interceptor.ts` | **TO BE CREATED (M2-C02).** The normalised `ProblemDetails` this task renders comes from here — normalise once, not twice. |
| `V.SMART/V.SMART.Shared/Services/ExcelExportService.cs` | Read-only reference — the server-side exporter that stays. |
| `V.SMART/V.SMART.Shared/Components/DetailsModal.razor` | Read-only reference — the existing "no data" and export behaviour. |

## Investigation Requirements

**Reuse, plus one confirmation.**

- **Reuse** INV-009 (Complete — reporting: FastReport + stored procedures) and
  [ADR-005](../../decisions/ADR-005-reporting-and-printing.md). Do not re-derive the
  reporting architecture.
- **Confirm (narrow):** the exact `ProblemDetails` field names M2-A06 settled on for the
  correlation id (`traceId`? `correlationId`? an extension member?) and the field carrying
  the business-rule message (per the brief for this batch, the message arrives in `title`).
  Read M2-A06's produced middleware, not its plan. Record with `file:line` evidence.
- **Confirm (narrow):** the export endpoint's response contract — content type, whether the
  filename comes from `Content-Disposition`, and whether export honours the current filter
  set. If export ignores filters, that is a defect to record, not to paper over: a user who
  exports a filtered grid and receives the unfiltered table has been silently misled.
- **Confirm (narrow, Angular-specific):** whether `Content-Disposition` is actually readable
  from the browser. It is a non-safelisted response header, so the API must send
  `Access-Control-Expose-Headers` for a cross-origin call. Check M2-B06's CORS configuration
  before relying on the filename; if it is not exposed, that is a gap to record against
  M2-B06, not something to work around with a guessed filename.
- **Record the negative result** if there is no export endpoint yet.

## Existing Behavior to Preserve

Verified 2026-08-12.

| Existing behaviour | Evidence | Confidence | Disposition |
|---|---|---|---|
| Empty grid renders a single spanning row reading **"No data found."** | `V.SMART/V.SMART.Shared/Components/DetailsModal.razor:75-82` | Confirmed | **Replaced, deliberately.** One undifferentiated message conflates "nothing exists yet" with "your filter matched nothing" — [KB-051](../../frontend-new/design-system.md#state-patterns) requires these be distinct. Record this as an intentional behaviour change. |
| Export is server-side: `ExcelExportService.ExportListToExcel<T>(List<T>, string sheetName, params object[] lockColumns)` and `ExportPendingListToExcel<T>(List<T>, string sheetName, params object[] Name1)` return `byte[]` | `V.SMART/V.SMART.Shared/Services/ExcelExportService.cs:24`, `:113` | Confirmed | **Preserve.** Reached over HTTP instead of in-process. |
| The byte array is base64-encoded and handed to JS interop to download | `V.SMART/V.SMART.Shared/Components/DetailsModal.razor:241-244`; `V.SMART/V.SMART.Shared/Services/Extensions/IJSRuntimeExtensions.cs:30` (`DownloadExcelAsync(base64String, fileName)`) | Confirmed | Replaced by a blob URL from the HTTP response. The base64 hop exists only because Blazor Server cannot stream bytes to the browser directly; it has no purpose in a SPA. |
| Export failure logs via `LoggingService.LogDeveloperError` and shows a **generic** toast (`"Error while exporting MfgPo!"`) | `V.SMART/V.SMART.Shared/Components/DetailsModal.razor:246-251` | Confirmed | **Improved deliberately.** The SPA shows the server's `ProblemDetails` message and the `traceId`. Generic toasts are what [KB-051 principle 6](../../frontend-new/design-system.md#principles) exists to stop. |
| Busy state is a full overlay component | `V.SMART/V.SMART.Shared/Components/ProcessingOverlay.razor` | Confirmed | Replaced by skeleton-on-first-load and progress-bar-on-refetch; a blocking overlay for a list refresh is worse than useless. |
| Business-rule refusals are user-facing sentences returned from services | `V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/SalesService/MfgPoService.cs:488` — `"Cannot delete this Sales Order as a Sales DC transaction exists."`; `:598` — `"Cannot cancel this Item as a Sales DC transaction exists."` | Confirmed | **Preserve verbatim.** BR-SO-001. The UI renders the string exactly as received. |

**Business rules involved:** **BR-SO-001** ([KB-030](../../business-rules/business-rule-inventory.md))
— not *implemented* here, only *displayed*. This task's obligation is purely that it does
not corrupt, truncate, translate or genericise the message.

## Target Result

- `DataGrid` renders each of the five states distinctly, and a caller can override any of
  them without forking the component.
- **First load:** skeleton rows, one per expected page row up to a cap, with the real
  column widths so the layout does not jump when data arrives. Never a centred spinner on a
  blank page.
- **Refetch:** previous rows stay; a 2 px indeterminate progress bar appears at the top of
  the grid; the grid is not disabled and focus is not moved.
- **Empty (no data):** the `EmptyState` primitive with an illustration, a caller-supplied noun
  ("No customers yet"), and a caller-supplied primary action.
- **Empty (filtered):** distinct copy ("No results for these filters") plus a **Clear
  filters** button that resets `DataGridQueryState` — which, in route-bound mode, resets the
  URL query params. The component distinguishes the two cases by whether any filter is
  active — not by guessing.
- **Error:** the `ErrorState` primitive showing `problem.title` **verbatim**, `problem.detail`
  when present, the correlation id in a copyable form, and Retry.
- **403:** the permission-denied state naming the screen right that is missing, inline — not a
  redirect ([KB-050 §Error handling](../../frontend-new/react-architecture.md#error-handling)).
- **409:** rendered as an inline alert or toast with the server's message verbatim. Never
  reworded, never prefixed with "Error:", never truncated, and — a real hazard in this
  workspace, which ships `@ngx-translate/core` — never passed through a translation lookup
  that could fall back to a key.
- **Export:** a toolbar menu (`Excel` / `CSV`) that issues one request to the server export
  endpoint carrying **the current sort and filter state**, receives a blob, saves it using
  the server-supplied filename, and shows progress while it runs. Long exports do not freeze
  the grid. If the export fails, the server's message is shown, not a generic one.

## Implementation Requirements

1. Read the KB documents above; search `docs/kb/investigation-registry.md`.
2. Confirm M2-C04-03's feedback primitives exist and their input shapes. If they do not, stop
   and report Blocked — do **not** create a second `EmptyState`.
3. Confirm M2-A06's `ProblemDetails` field names against the produced middleware. Record
   with evidence.
4. Create `data-grid-states.component.ts` / `.html`, composing M2-C04-03's primitives. It must
   contain no styling of its own beyond layout — the visuals belong to the primitives.
5. Implement the empty-state discrimination: derive `hasActiveFilters` as a `computed()` over
   `DataGridQueryState`'s filter signal, and select the "no data" vs "filtered out" variant
   from it. Add a `clearFilters()` that resets filters **and** page.
6. Implement the skeleton: row count = `min(pageSize, 12)`; cell widths from the resolved
   column widths; `aria-busy="true"` on the grid; a visually-hidden `role="status"`
   announcing "Loading results".
7. Implement the refetch indicator: a 2 px determinate-if-known / indeterminate progress bar
   above the header. Do **not** dim or disable the table.
8. Implement error rendering in `data-grid-error.component.ts`:
   - Consume the `ProblemDetails` the HTTP error interceptor (M2-C02) already normalised;
     do not re-parse it.
   - Branch on status: `403` → the permission-denied state; `409` → inline business-rule
     alert; everything else → `ErrorState` with `traceId` + Retry.
   - **The 409 branch must pass the message through untouched.** Add a unit test asserting
     byte-for-byte equality with a fixture containing the real sentence from
     `MfgPoService.cs:488`.
9. Implement `grid-export.service.ts`: build the export request from the *current* query
   state, call the generated client's binary operation with `responseType: 'blob'`, read the
   filename from `Content-Disposition` with a sane fallback, create and revoke the object
   URL, and expose `exportAs()`, `exporting` and `error` — the latter two as signals.
10. Add the toolbar export control from PrimeNG. Disable it while an export is in flight;
    show an accessible busy label, not just a spinner.
11. **Add an explicit guard against client-side generation.** No `xlsx`, `exceljs`,
    `papaparse`, `jspdf` or equivalent dependency may be added. Add a lint rule or a test
    asserting these are absent from `package.json` — ADR-005 is easy to violate later under
    schedule pressure, and a rule outlives a code review.
12. Wire everything through the `empty` / `error` / `toolbar` seams. Keep them overridable by
    callers.
13. Write the tests listed under *Testing Requirements*.
14. Run the verification commands, review the full `git diff`, update docs and the registry.

### Files expected to change

- `frontend/nexgen-web/src/app/shared/components/data-grid/data-grid.component.ts` and
  `.html` — render the states, mount the toolbar.
- `frontend/nexgen-web/src/app/shared/components/data-grid/data-grid-query-state.ts` — expose
  `hasActiveFilters` and `clearFilters`.
- `frontend/nexgen-web/src/app/shared/components/data-grid/index.ts` — exports.
- `frontend/nexgen-web/package.json` — **no new runtime dependency expected**; if the export
  needs one, justify it explicitly against ADR-005.
- `docs/kb/frontend-new/design-system.md` (KB-051), `docs/kb/frontend-new/react-architecture.md`
  (KB-050), `docs/kb/execution/task-tracker.md` (KB-081),
  `docs/kb/investigation-registry.md` (KB-003).

### Files expected to be created

All **TO BE CREATED**, under
`frontend/nexgen-web/src/app/shared/components/data-grid/`:

- `data-grid-states.component.ts`, `.html`, `.css`
- `data-grid-error.component.ts`, `.html`
- `data-grid-skeleton.component.ts`, `.html`
- `grid-export.service.ts`
- `data-grid-toolbar.component.ts`, `.html`
- `data-grid-states.component.spec.ts`
- `data-grid-error.component.spec.ts`
- `grid-export.service.spec.ts`
- `fixtures/problem-details.ts` — including the verbatim BR-SO-001 sentence
- `no-client-file-generation.spec.ts`

### Files that must not change

- `V.SMART/**` — including `ExcelExportService.cs`, which is reused as-is over HTTP.
- `frontend/nexgen-web/src/app/shared/components/{empty-state,error-state,skeleton,toast}/` —
  owned by M2-C04-03. Compose them; if one is inadequate, that is a change request against
  M2-C04-03, not an in-place edit here.
- `frontend/vsmart-erp/**`, `db/**`, `docs/kb/decisions/**`.

## API Changes

Not applicable — this task adds no HTTP surface. It **consumes**:

```
GET /api/v1/{resource}/export?format=xlsx|csv&<current sort and filter parameters>
    → the file, with Content-Disposition supplying the filename
```

Per [ADR-005](../../decisions/ADR-005-reporting-and-printing.md), report and print exports
use `GET /api/v1/reports/{slug}/export` and `GET /api/v1/{resource}/{id}/print`; those are
M2-C09's concern. This task covers **list export** only.

If no export endpoint exists, record the gap, report **Blocked**, and propose the contract
above. Do not build the file client-side "for now" — that is precisely the decision ADR-005
already took, in the opposite direction.

## Frontend Changes

| Aspect | Detail |
|---|---|
| **Route** | n/a — shared component behaviour. |
| **Component** | `DataGridStatesComponent`, `DataGridErrorComponent`, `DataGridSkeletonComponent`, `DataGridToolbarComponent` and `GridExportService` under `frontend/nexgen-web/src/app/shared/components/data-grid/` (**TO BE CREATED**), composing M2-C04-03's primitives. |
| **API integration** | The export operation from the generated client, `responseType: 'blob'`, carrying the current sort/filter query state. An imperative call, not held state — a downloaded file is not cacheable state. |
| **State** | `exporting` is a signal local to the service. Everything else derives from `DataGridQueryState` and the grid's existing result signals. No new global state. |
| **Validation** | n/a. |
| **Permissions** | A 403 from the list or export endpoint renders the permission-denied state inline, naming the missing screen right. This is display of a **server** decision; the client enforces nothing (ADR-004). |
| **Loading state** | First load: skeleton rows matching the resolved column widths, `aria-busy="true"`, visually-hidden `role="status"`. Refetch: previous rows retained + 2 px top progress bar; grid stays interactive; focus is not moved. Export: toolbar control disabled with an accessible busy label. |
| **Empty state** | Two distinct variants, selected by `hasActiveFilters`. No data → `EmptyState` with caller-supplied noun and primary action. Filtered out → "No results for these filters" + **Clear filters**, which resets the query state (and, in route-bound mode, the URL). |
| **Error state** | Normalised `ProblemDetails`. `403` → permission-denied state. `409` → inline alert with `problem.title` **verbatim**. Otherwise → `ErrorState` with the message, `detail`, a copyable correlation id, and Retry. Errors are announced via `aria-live="assertive"` for `409`/`403` and `polite` otherwise. |
| **Accessibility** | Each state is announced; none is a colour-only signal. Skeletons are hidden from the accessibility tree (`aria-hidden`) with a single `role="status"` label instead of dozens of announced placeholders. Retry, Clear filters and Export are all keyboard-reachable with visible focus. The export menu traps and restores focus. |
| **Responsive behaviour** | Empty and error states are single-column and readable below 768 px. The export control collapses into the toolbar's overflow only if it remains keyboard-reachable — [KB-051 §Do not](../../frontend-new/design-system.md#do-not) forbids hiding primary actions in overflow, so treat export as secondary and Clear filters as primary. |

## Business Rules

**BR-SO-001 — displayed, never implemented.**

**No ERP business rule is implemented client-side by this task.** The component makes no
domain decision. Its single obligation with respect to business logic is *fidelity of
transport*: a business-rule refusal produced by a server service — for example
`"Cannot delete this Sales Order as a Sales DC transaction exists."`
(`V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/SalesService/MfgPoService.cs:488`,
BR-SO-001) — must reach the user's eyes byte-for-byte as the server wrote it.

Forbidden: rewriting, shortening, sentence-casing, prefixing, appending, mapping to a
generic string, or routing through a translation key lookup that can silently fall back.
These messages are product UX authored for ERP operators and often name the exact downstream
document blocking the action; a generic replacement makes the system unusable in precisely
the moments it matters.

Equally forbidden: generating any exported file in the browser. Export is a server
capability ([ADR-005](../../decisions/ADR-005-reporting-and-printing.md)); the client
requests bytes and saves them.

## Testing Requirements

Unit tests run on the Angular CLI's `@angular/build:unit-test` builder with
`@testing-library/angular`, invoked by `npm run test:ci` (KB-083 § Verified frontend commands).
HTTP is stubbed with `provideHttpClientTesting`.

| # | Test | Asserts |
|---|---|---|
| 1 | First load renders skeleton rows, not a spinner, with `aria-busy="true"` | KB-051 |
| 2 | Skeleton column widths match the resolved column widths (no layout jump) | KB-051 |
| 3 | Refetch keeps previous rows and shows the progress bar; the table is not disabled | KB-051 |
| 4 | Zero rows with no active filters renders the "no data yet" variant with the primary action | State discrimination |
| 5 | Zero rows **with** active filters renders "No results for these filters" + Clear filters | State discrimination |
| 6 | Clear filters resets the query state (and, route-bound, the URL) and refetches | Wiring |
| 7 | A 500 renders `ErrorState` with the server message, the correlation id, and a working Retry | Error contract |
| 8 | A 403 renders the permission-denied state inline and does **not** navigate | KB-050 |
| 9 | A 409 renders `"Cannot delete this Sales Order as a Sales DC transaction exists."` **byte-for-byte** | BR-SO-001 fidelity |
| 10 | The 409 message is not passed through a translation pipe, not prefixed, not truncated | Fidelity, explicitly |
| 11 | Export issues one request carrying the current sort and filter parameters | Correctness — an unfiltered export of a filtered view is a data-integrity bug |
| 12 | Export uses the `Content-Disposition` filename, with a deterministic fallback | UX |
| 13 | The object URL is revoked after download | No leak |
| 14 | Export failure shows the server's message, not a generic one | KB-051 principle 6 |
| 15 | `package.json` contains no client-side file-generation library (`xlsx`, `exceljs`, `papaparse`, `jspdf`, `pdfmake`) | ADR-005 enforcement |
| 16 | Each of the five states exposes its announcement role and is keyboard-reachable | Accessibility |
| 17 | `a11y.spec.ts` — `axe` reports no critical violations in each of the five states | Runtime a11y scan, carried over unchanged |

**Accessibility verification.** Test 17's `axe` scan is carried over **unchanged** from the
specification this file replaces: a runtime scan is stack-independent, so it survives the
Angular re-specification, and the `angular-eslint` template rules do **not** substitute for it
(Q-69, answered 2026-08-22 in the conservative direction — the criterion is restored;
`../../open-questions.md`). No `axe` package is installed in `frontend/nexgen-web/` today
(`package.json`, verified 2026-08-22), and KB-083's verified table has no `axe` command — but
**M2-C04-01** installs `axe-core` as a dev dependency (`M2-C04-01.md:388`), reached through this
task's Hard `depends_on: M2-C05-01` → `M2-C04-02` → `M2-C04-01`, so it is present before this
task runs. Drive it from `a11y.spec.ts` under the existing `npm run test:ci`; do **not** invent
a new npm script or add a dependency here.

Running **in addition, not instead**: `npm run lint` runs `angular-eslint`'s template
accessibility rules **as errors** (`frontend/nexgen-web/eslint.config.js:97-103`); test 16
covers the roles and focusability; the rest is a recorded manual keyboard pass over all five
states. The repository-wide `axe`-in-CI pass remains **M5-09's** scope.

## Acceptance Criteria

- [ ] All five states render distinctly and are each covered by a test.
- [ ] "No data yet" and "filtered to nothing" are visually and textually distinct, chosen by
      active-filter state rather than a guess.
- [ ] **Clear filters** resets the query state and triggers a refetch; in route-bound mode the
      URL query params are reset with it.
- [ ] First load shows skeleton rows sized to the real columns; no spinner on a blank page.
- [ ] Refetch retains previous rows, shows a progress bar, and does not move focus.
- [ ] A 403 renders the permission-denied state inline with no navigation.
- [ ] A 409 renders the server's `ProblemDetails.title` **byte-for-byte**, proven by a test
      using the real BR-SO-001 sentence from `MfgPoService.cs:488`.
- [ ] A generic error renders the server message, the correlation id, and a working Retry.
- [ ] Export issues exactly one request that carries the current sort and filter state.
- [ ] The downloaded filename comes from `Content-Disposition`; the object URL is revoked.
- [ ] `package.json` contains no client-side spreadsheet/CSV/PDF generation library, asserted
      by a test.
- [ ] The states compose M2-C04-03's primitives; no duplicate `EmptyState` or `ErrorState`
      implementation exists inside the `data-grid/` directory.
- [ ] `axe` reports no critical violations in any of the five states (test 17).
- [ ] `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run test:ci` and
      `npm run build` all pass.
- [ ] Nothing under `V.SMART/` appears in the diff.

## Verification Commands

Every `npm` command below appears verbatim in
[KB-083 § Verified frontend commands](../prompt-template.md#verified-repository-commands),
measured on this workstation on 2026-08-21. Run them from `frontend/nexgen-web/`.

```bash
npm ci
```

```bash
npm run typecheck
```

```bash
npm run lint
```

```bash
npm run format:check
```

```bash
npm run test:ci
```

```bash
npm run build
```

Plus this repository command, which is its own row in KB-083 § Verified repository
commands, run from the repository root:

```bash
git status --porcelain
```

> To confirm the .NET solution was untouched, read the full `git diff` rather than quoting a
> diff invocation KB-083 has not verified. There is **no coverage script** in
> `frontend/nexgen-web/package.json` and no coverage row in KB-083 — do not invent one.

## Documentation Updates

| Document | Change | Frontmatter |
|---|---|---|
| `docs/kb/frontend-new/design-system.md` (KB-051) | Mark the state patterns as implemented for `DataGrid`; record the deliberate split of the single "No data found." message into two states, with the superseded evidence (`DetailsModal.razor:75-82`). | Bump `last_verified` |
| `docs/kb/frontend-new/react-architecture.md` (KB-050) | Confirm the *Error handling* table against the implemented behaviour; correct any row that turned out differently. | Bump `last_verified` |
| `docs/kb/execution/task-tracker.md` (KB-081) | M2-C05-03 status. | — |
| `docs/kb/investigation-registry.md` (KB-003) | The `ProblemDetails` field-name confirmation and the export-endpoint contract confirmation (or the negative result). | — |

### Investigation registry updates

```yaml
Finding:        The existing generic picker grid renders a single undifferentiated
                "No data found." row for both "nothing exists" and "filter matched
                nothing"; export is server-side via ExcelExportService and delivered
                to the browser as base64 through JS interop; export failures surface
                a generic toast rather than the server message.
Evidence:       V.SMART/V.SMART.Shared/Components/DetailsModal.razor:75-82, :232-256;
                V.SMART/V.SMART.Shared/Services/ExcelExportService.cs:24, :113;
                V.SMART/V.SMART.Shared/Services/Extensions/IJSRuntimeExtensions.cs:30
Business rule:  n/a
Confidence:     Confirmed
Last verified:  2026-08-12
```

Plus: the confirmed `ProblemDetails` field names from M2-A06, the export endpoint's
contract — including, explicitly, whether it honours the caller's filters — and whether
`Content-Disposition` is exposed to the browser. Record a negative result if the endpoint does
not exist. Check `git branch --no-merged master` before claiming any new Q/R/INV id.

## Completion Conditions

- All acceptance criteria met, with each verification command's **observed** output recorded.
- The BR-SO-001 byte-for-byte test passing against the real sentence.
- The branch left for review, unmerged.
- **Only the repository owner may set `Completed`**
  ([KB-088](../workflow.md#who-may-set-completed)).

## Git Strategy

| Field | Value |
|---|---|
| **Branch** | `migration/M2-C05-03-grid-states-and-export` |
| **Commit** | `M2-C05-03: Implement DataGrid states and server-side export` |
| **Expected changed files** | `frontend/nexgen-web/src/app/shared/components/data-grid/{data-grid-states.component.*,data-grid-error.component.*,data-grid-skeleton.component.*,data-grid-toolbar.component.*,grid-export.service.ts,fixtures/**,*.spec.ts}` (new), `frontend/nexgen-web/src/app/shared/components/data-grid/{data-grid.component.ts,data-grid.component.html,data-grid-query-state.ts,index.ts}`, `docs/kb/frontend-new/design-system.md`, `docs/kb/frontend-new/react-architecture.md`, `docs/kb/investigation-registry.md`, `docs/kb/execution/task-tracker.md` |
| **Rollback approach** | Revert the branch's merge commit. `DataGrid` reverts to the M2-C05-01 core with unimplemented state seams. No server behaviour, schema or data is affected, because this task only reads existing endpoints. Note the consequence: **M2-D01 cannot ship after such a revert**, since G2 requires Excel export — so a revert here is a schedule decision, not a free one. |

Never merge or push to `master` from an execution session.

## Execution Record (2026-08-26)

**Outcome: `Needs Review`, final validator verdict `PASS`** (attempt 1 of 4, 0 escalations).
Branch `migration/M2-C05-03-grid-states-and-export`, commit `a3a8ff5`, left unmerged for
review. `git status --porcelain` at close-out: empty.

### What was actually done

All five `DataGrid` states — first-load skeleton, refetch, empty (no data vs. filtered-to-none,
distinguished by the **committed** filter set, not a guess), error (403 permission-denied inline,
409 business-rule verbatim, generic 500 with retry) — were implemented as new components
(`data-grid-states.component.ts`, `data-grid-error.component.ts`, `data-grid-skeleton.component.ts`)
composing the existing M2-C04-03 feedback primitives (`EmptyState`, `ErrorState`, `InlineAlert`,
`PermissionDeniedState`, `Skeleton`) rather than duplicating them, and wired into
`data-grid.component.html`'s three pre-existing `TODO(M2-C05-03)` seams. `data-grid-query-state.ts`
gained `hasActiveFilters` (a computed over the committed filter set) and a fix to `clearFilters()`:
route-bound filter params are now explicitly nulled rather than left in the URL by
`queryParamsHandling: 'merge'`, which was a real, previously-undetected defect. Server-side export
was implemented as `grid-export.service.ts`: one request per click (concurrent clicks refused),
carrying the current sort and filter state with paging stripped, decoding the filename from
`Content-Disposition` when present and falling back to a deterministic name otherwise, and
revoking the created object URL in a `finally`. A `no-client-file-generation.spec.ts` guard
asserts no spreadsheet/CSV/PDF library ever enters `package.json`.

### What differed from the plan, and why

- **The export endpoint was assumed possibly-absent by the task file ("if none exists, report
  Blocked"); it exists.** `CurrencyExcelController.cs:84-127` (`GET /api/v1/currencies/export`)
  already honours filters and sort, accepts `format=xlsx` only, and 409s over the 10,000-row
  ceiling. No Blocked branch was needed. Recorded as INV-056.
- **`Content-Disposition` cannot be demonstrated end-to-end.** `Program.cs`'s CORS policy
  (:165-171) sets no `WithExposedHeaders`, so the header reads `null` cross-origin; the
  deterministic-fallback path is the one actually exercised by a real browser today. Unit-tested
  both branches; recorded as Q-96/R-79 rather than silently claimed working. Root cause and fix
  (`.WithExposedHeaders(...)` in `V.SMART.Api`) are out of this task's scope
  (`V.SMART/**` untouched, as required).
- **M2-C02's HTTP error interceptor, which the task file told this grid to consume, does not
  exist** (`core/http/`, `core/auth/` empty; M2-C02 Blocked). The grid normalises the raw
  `HttpErrorResponse` once, locally (`toGridProblem`), reusing `ProblemDetailsLike` from
  `form/server-validation.ts:22` instead of inventing a third description of the contract.
  Recorded as Q-94, for M2-C02 to reconcile when it lands.
- **Toolbar export menu ships Excel-only, not "Excel / CSV" as `M2-C05-03.md:226` describes.**
  `CurrencyExcelController.cs:48` accepts `xlsx` exclusively and 400s anything else; offering a
  CSV entry would be a control that always fails. The format list is a component input, so no
  code change is needed if a server endpoint later adds `csv`. Recorded as Q-95, owner-scope.
- **Skeleton row cap.** `data-grid.component.ts:230`'s pre-existing cap of 10 rows (from
  M2-C05-01) was left as merged code and not changed to the spec text's 12 — changing it edits
  M2-C05-01, out of this task's scope; the new `data-grid-skeleton.component.ts` itself uses a
  cap of 12 per this task's own spec, so the two caps now differ by component. Not flagged as a
  new open question since it was already noted in the investigation as an orchestrator-scope
  call.

### Commands run, with observed output

| Command | Result |
|---|---|
| `npm run typecheck` | `EXIT:0`, no diagnostics |
| `npm run lint` | `EXIT:0`, "All files pass linting." |
| `npm run format:check` | `EXIT:0`, "All matched files use Prettier code style!" |
| `npm run test:ci` | `EXIT:0`, "Test Files 55 passed (55) / Tests 436 passed (436)", 32.41 s |
| `npm run build` | `EXIT:0`, initial 571.20 kB raw / 136.72 kB gzip (within KB-050's 250 kB budget) |
| `git status --porcelain` (repo root) | empty, `EXIT:0` |
| `git diff --name-status master...HEAD` | 26 files, all under `frontend/nexgen-web/src/app/shared/components/data-grid/` or `docs/kb/` — nothing under `V.SMART/` |
| `grep -rn WithExposedHeaders V.SMART/` | no match (confirms the Content-Disposition gap) |

`dotnet build` / `dotnet test` were not run — no `V.SMART/**` file changed, so they are not
applicable. `npm ci` was not re-run — `package.json`/`package-lock.json` are absent from the
diff, so its result cannot be affected by this task.

### Not done, and honestly declared (none are acceptance criteria)

1. `task-tracker.md` and this file's own frontmatter were still `Ready`/`Not Started` at the
   implementing commit — closed out by this same session's close-out pass (this section).
2. No live-API, real-browser run and no manual keyboard pass — automated axe scans (test 17)
   cover roles/focusability; no environment was available to run the API and SPA together.

### Discoveries already recorded (by the implementing commit, not duplicated here)

`docs/kb/investigation-registry.md` INV-056 (Complete); `docs/kb/open-questions.md` Q-94, Q-95,
Q-96; `docs/kb/risks/technical-debt-register.md` R-79; `docs/kb/frontend-new/design-system.md`
and `docs/kb/frontend-new/react-architecture.md` (state-pattern and error-handling rows
confirmed/corrected against the implementation). See those documents directly rather than this
record for their full text.

### Why `Needs Review`, not `Completed`

This task's own Completion Conditions state "Only the repository owner may set `Completed`"
([KB-088](../workflow.md#who-may-set-completed)). Validation passed; the branch is left for
Vivek's review and merge decision.
