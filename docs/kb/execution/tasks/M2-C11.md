---
doc_id: TASK-M2-C11
title: The Angular pilot's disposition — blocked on Q-38
module: execution
task_id: M2-C11
milestone: M2
parent_task: n/a
task_type: DevOps
priority: P2
status: Blocked
estimate: 0.5 d
depends_on: [M2-C01, M0-00]  # Q-102, 2026-08-26: added M0-00 — declared Hard in this file's own Dependencies table but missing from depends_on
source_files:
  - frontend/vsmart-erp/package.json
  - frontend/vsmart-erp/angular.json
  - frontend/vsmart-erp/src/app/core/auth/auth.service.ts
  - frontend/vsmart-erp/src/app/core/auth/auth.guard.ts
  - frontend/vsmart-erp/src/app/core/auth/auth.interceptor.ts
  - frontend/vsmart-erp/src/app/features/auth/login/login.component.ts
  - frontend/vsmart-erp/src/app/features/currency/currency.service.ts
  - frontend/vsmart-erp/src/app/features/currency/currency-list/currency-list.component.ts
  - frontend/vsmart-erp/src/app/features/currency/currency-form/currency-form.component.ts
  - frontend/vsmart-erp/src/app/layout/shell/shell.component.ts
  - frontend/vsmart-erp/src/environments/environment.ts
  - frontend/vsmart-erp/src/environments/environment.prod.ts
  - .gitignore
api_endpoints: ["POST /api/auth/login"]
business_rules: []
confidence: n/a
last_verified: 2026-08-22
---

# M2-C11 — The Angular pilot's disposition (blocked on Q-38)

> # ⛔ BLOCKED ON A HUMAN DECISION — **Q-38**. Do not implement this task, and do not infer what
> it is for.
>
> **Visited on 2026-08-22 by [`M2-C12-02`](M2-C12-02.md).** The ⛔ *supersession* banner that stood
> here is removed, because this file is **not** being re-specified for Angular the way its four
> sibling batch files were. `M2-C12`'s own scope says so
> (`M2-C12.md:159-166`): *"Re-specify the other 24, and for `M2-C11` replace the supersession
> banner with a precise statement of the open question, leaving it `Blocked` on Q-38 and routed to
> the owner."* This banner is that statement, and it is a **stronger** guard than the one it
> replaces — the supersession banner said "this file's stack is stale"; this one says **nobody
> knows what this task is for**.
>
> ### The question, stated precisely
>
> **What is `M2-C11` for, now that [`ADR-007`](../../decisions/ADR-007-angular-stack.md) has
> inverted the relationship between the pilot and the app?**
>
> - The specification preserved below — written when **React** was the target — says *archive*
>   `frontend/vsmart-erp/`: the Angular 19.2 + PrimeNG 19.1 pilot becomes dead code the moment a
>   React app exists, so tag it and delete it.
> - ADR-007 says the opposite about the same directory: it **adopts the pilot as the baseline**
>   (`ADR-007-angular-stack.md:76-80`), and `M2-C00`'s scope describes this task as
>   *"`M2-C11` adopts it as the baseline"* (`M2-C00.md:78-80`).
> - [KB-081](../task-tracker.md)'s row for this task already reads **"Adopt the Angular pilot as
>   the app baseline"**, which is `M2-C00`'s characterisation and **not** what this file says.
>
> Those are opposite instructions for one task id, and the contradiction is recorded as **Q-38**
> in [`open-questions.md`](../../open-questions.md) — **unanswered as of 2026-08-22**.
>
> ### The two readings, neither of which may be assumed
>
> **(a)** `M2-C11` ports the pilot's auth service, guard, interceptor and PrimeNG configuration
> into `frontend/nexgen-web/` and then disposes of `frontend/vsmart-erp/`. **The re-specified
> `M2-C01` already assumes (a) and says so explicitly**, which is why it created a fresh
> workspace and left the pilot untouched.
>
> **(b)** `frontend/vsmart-erp/` itself becomes the application. If the owner prefers (b),
> `M2-C01` changes paths but not substance.
>
> ### Consequences while it is open
>
> - **Status is `Blocked`, and the blocker is a person, not a task.** Every hard dependency is
>   satisfied; nothing technical is missing.
> - **Owner: the repository owner.** This is a product/architecture call
>   ([KB-091 §8](../autonomous-runner.md#8-safety-limits--the-runner-stops-and-asks): an
>   autonomous runner may not self-select or answer it).
> - **Nothing under `frontend/vsmart-erp/` may be deleted, moved or edited by any task until
>   Q-38 is answered.** `M2-C02`, `M2-C03` and `M2-C10` all say so in their *Files That Must Not
>   Change* sections. **40 files are tracked there** (`git ls-files frontend/vsmart-erp | wc -l`,
>   observed 2026-08-22).
> - **Nothing else is blocked by it.** `M2-C01` merged without it and the rest of the `M2-C` tree
>   does not depend on it.
>
> **If you are a runner and selected this task: stop and report.** Escalate Q-38; do not choose
> a reading.
>
> **What did not change on 2026-08-22:** `depends_on`, `business_rules`, priority, estimate and
> gate. `status` moved `Not Started` → `Blocked` because `M2-C12`'s criterion 8 requires it, and
> the title was changed from *"Archive the Angular pilot"* to the neutral form above, because the
> old title **is** one of the two disputed answers and a file cannot state the answer to the
> question it is blocked on.

| Field | Value |
|---|---|
| Task ID | M2-C11 |
| Milestone | M2 — Foundation |
| Parent | — (this is a top-level task) |
| Type | DevOps |
| Priority | P2 |
| Status | **Blocked on Q-38** |
| Estimate | 0.5 d |
| Gate | G2 |

## Objective

**Decide, and then execute, what happens to `frontend/vsmart-erp/`.** The decision is Q-38's and
belongs to the repository owner. Until it is answered this task has no executable content, and
the preserved specification below is evidence, not instruction.

Once Q-38 is answered, this file is re-specified against the answer — by whoever picks it up —
and only then does it become selectable.

## Scope

While Q-38 is open, the entire scope of this task is:

1. Keep the question visible and correctly stated (this file, and
   [`open-questions.md`](../../open-questions.md) Q-38).
2. Keep `frontend/vsmart-erp/` intact and unreferenced by the new workspace.

After Q-38 is answered, scope becomes whichever of the two readings the owner chose, re-specified
in full at that point. The facts under
[§ Retained evidence](#retained-evidence--true-under-either-reading) survive into either version.

## Out of Scope

- **Choosing between readings (a) and (b).** That is the whole of Q-38 and it is not
  inferable from source.
- **Deleting, moving, renaming or editing anything under `frontend/vsmart-erp/`.** Under
  reading (b) that directory is the application.
- **Editing `frontend/nexgen-web/`.** Whatever this task eventually does, it does not build the
  new app; `M2-C01`, `M2-C02`, `M2-C03` and the `M2-C04` tree do that.
- **Rewriting git history.** `M0-05` owns history and is already done under G0.
- **Re-opening ADR-007.** The stack decision is settled; only this task's purpose is not.

## Dependencies

| Dependency | Class | State |
|---|---|---|
| **Q-38** | **Hard (human)** | **Unanswered as of 2026-08-22.** Owner: repository owner. |
| M2-C01 | Hard | `Completed`, merged `2dd4e53` 2026-08-21. The new workspace exists, so the repository is never left with zero SPAs under either reading. |
| ADR-007 | Reference | Accepted 2026-08-20. It is the reason this task's purpose is in doubt. |
| M0-08 (`.gitignore` + committed build output) | Soft | May already have removed `dist/` and `.angular/cache/`. Verify at execution time; do not duplicate the work or claim its credit. |
| M0-00 (clean VCS baseline) | Hard | A change of this size is unreviewable on a dirty tree. |
| CI (`.github/workflows/ci.yml`) | Testing | Whatever is decided, CI must end up referencing exactly the directories that still exist. |

## Relevant Documentation

| Doc | Why |
|---|---|
| [KB-004 Q-38](../../open-questions.md) | **The blocker.** Both readings, and who owns the answer. |
| [ADR-007](../../decisions/ADR-007-angular-stack.md) | `:76-80` adopts the pilot as the baseline; `:176-184` refuses its `localStorage` token model and its hardcoded API host. |
| [KB-050 §The Angular pilot — what to keep, what not to copy](../../frontend-new/react-architecture.md#the-angular-pilot--what-to-keep-what-not-to-copy) | The patterns worth porting and the anti-patterns, each with `file:line`. Written for exactly this decision. |
| [KB-015 §The Angular 19 pilot](../../architecture/frontend-architecture-existing.md#the-angular-19-pilot-frontendvsmart-erp) | The pilot's scope, recorded as INV-021. |
| [KB-060 R-14, R-34](../../risks/technical-debt-register.md) | The two risks whose closure depends on the answer. |
| [`M2-C12`](M2-C12.md) `:159-166` | Why this file was left blocked rather than re-specified. |

## Relevant Existing Code

Read-only under either reading, until Q-38 is answered:

- `frontend/vsmart-erp/src/app/core/auth/` — `auth.service.ts`, `auth.guard.ts`,
  `auth.interceptor.ts`
- `frontend/vsmart-erp/src/app/features/currency/` — the proven end-to-end path
- `frontend/vsmart-erp/src/app/layout/shell/shell.component.ts`
- `frontend/vsmart-erp/src/environments/environment.ts`, `environment.prod.ts`
- `frontend/nexgen-web/` — the workspace `M2-C01` created, for comparison only

## Business Rules

**None.** The pilot contains no ERP business logic: it implements login plus a currency
list/form against `CurrencyController`, and every rule it relies on lives server-side in
`V.SMART.Shared`. This task adds, removes and alters no business rule under either reading.

## Implementation Requirements

1. **Do not choose.** Q-38 is answered by the repository owner or it is not answered at all.
2. **Extract knowledge before code moves, whichever way it moves.** The
   [§ Retained evidence](#retained-evidence--true-under-either-reading) block below exists so
   that the `file:line` citations stay resolvable after any directory disappears. Under reading
   (a) it must be copied into KB-015 *before* deletion; deleting first makes the citations
   unverifiable.
3. **Whatever is decided, `frontend/nexgen-web/` must never be the thing that is deleted.**
4. **Record the answer where it was asked** — Q-38 in `open-questions.md` — and only then
   re-specify this file against it.

## Acceptance Criteria

**While Q-38 is open, this task cannot be `Completed`; these are the criteria for its current,
blocked state.**

- [ ] This file carries no supersession banner (the ⛔ STOP block `M2-C12` removes across the
      tree), and instead states Q-38 precisely, names both readings, and names the owner.
- [ ] `status` is `Blocked`, and the blocker named is **Q-38**, not a dependency.
- [ ] Q-38 in [`open-questions.md`](../../open-questions.md) is still present and still
      describes both readings.
- [ ] No file under `frontend/vsmart-erp/` has been deleted, moved or edited.
- [ ] The archive-era instructions are preserved as **evidence** and are explicitly marked as
      not-to-be-executed.

**After Q-38 is answered**, this section is replaced with criteria derived from the answer. The
two facts that must survive into either version:

- Under reading (a), `git ls-files frontend/vsmart-erp/dist frontend/vsmart-erp/.angular` returns
  nothing, and KB-015 carries the retained evidence in the past tense with its pre-deletion
  `file:line` citations.
- Under reading (b), `frontend/nexgen-web/` and `frontend/vsmart-erp/` do not both remain as
  active projects — R-34's "two frontends in one repository" hazard is the point of the task
  either way.

## Testing Requirements

There is nothing to test while the task is blocked. When it is unblocked, verification is
structural and uses only commands verified in
[KB-083](../prompt-template.md#verified-repository-commands):

| Check | How |
|---|---|
| The surviving SPA still builds, lints, typechecks and tests | `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run test:ci`, `npm run build` from the surviving workspace. |
| The backend still builds | `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` — 0 errors. |
| No dangling reference remains | `git grep -n "vsmart-erp"` returns only intentional historical mentions in `docs/kb/`. |
| No committed build output remains | `git ls-files frontend/vsmart-erp/dist frontend/vsmart-erp/.angular` returns nothing. |
| CI references only directories that exist | Read `.github/workflows/ci.yml`. |

> **Note the `.gitignore` trap in the old plan.** Removing "Angular-specific" ignore rules is
> wrong under reading (b) and wrong-by-halves under (a): the generic `node_modules/`, `dist/` and
> cache rules are needed by whichever workspace survives.

## Documentation Updates

| Document | Update |
|---|---|
| [KB-004](../../open-questions.md) | Q-38 — kept open and accurate; the answer recorded here when it arrives. |
| [KB-081](../task-tracker.md) | This task's row states **Q-38** as the blocker. Its wording ("Adopt the Angular pilot as the app baseline") is itself one of the two readings and must not be treated as the answer. *(Tracker rows for the re-specified `M2-C` tree are restated once, by `M2-C12-05`.)* |
| [KB-015](../../architecture/frontend-architecture-existing.md) | Only when Q-38 is answered: rewrite §*The Angular 19 pilot* to match the decision, carrying the retained evidence below. |
| [KB-060](../../risks/technical-debt-register.md) | Only when Q-38 is answered: R-34 and R-14 close, partially or fully, depending on the reading. Do not close either now. |
| [KB-003](../../investigation-registry.md) | Amend **INV-021** with the outcome when there is one. No new INV id. |

## Completion Conditions

- **Q-38 answered by the repository owner**, recorded in `open-questions.md`.
- This file re-specified against the answer, with real acceptance criteria.
- The work then done and verified.
- **Only the repository owner may set `Completed`**
  ([KB-088](../workflow.md#who-may-set-completed)).

## Git Strategy

| Field | Value |
|---|---|
| Branch | `migration/M2-C11-<slug derived from the answer to Q-38>`, cut fresh from `master` |
| Commit | `M2-C11: <imperative summary of what the answer to Q-38 required>` |
| Merge | Left for review. Never merged or pushed from an execution session. Because either reading changes a directory visible to every clone, the review must include the repository owner. |
| Rollback | `git revert` of the merge commit restores the tree. Under reading (a) an annotated tag created **before** any deletion is the independent safety net, and it must be created in the same task, not afterwards. |

---

## Retained evidence — true under either reading

**This section is evidence, not instruction.** It is the part of the pre-ADR-007 specification
that survives the contradiction: facts about the pilot, each with `file:line`, that must outlive
the directory if the directory goes. Nothing here tells anyone to delete anything.

**Inventory, observed 2026-08-22:** `git ls-files frontend/vsmart-erp | wc -l` → **40** tracked
files. Re-measure at execution time — `M0-08` may have removed committed build output since —
and record `dist/` and `.angular/` counts separately, including a zero as a negative result.

| Fact to retain | Evidence |
|---|---|
| The `AuthController` → `CurrencyController` → SPA path works end-to-end. This is the pilot's lasting contribution. | `frontend/vsmart-erp/src/app/core/auth/auth.service.ts:54` posts to `${environment.apiBaseUrl}/api/auth/login`; `V.SMART/V.SMART.Api/Controllers/AuthController.cs:11` declares `[Route("api/auth")]` and line 39 `[HttpPost("login")]`. |
| The de-facto `LoginResponse` shape already implemented server-side: `{ token, username, userId, tenantId, role }`. | KB-013 §REST API; the pilot's `LoginResponse` interface in `auth.service.ts`. |
| **Rejected pattern** — JWT and the whole login response in `localStorage`. | `auth.service.ts:29` `TOKEN_KEY = 'vsmart_jwt'`; lines 34, 60, 67, 75. Refused by `ADR-007-angular-stack.md:176-180`; token custody is `M2-C02`'s decision. |
| **Rejected pattern** — production configuration pointing at localhost. | `src/environments/environment.prod.ts:3` — `apiBaseUrl: 'http://localhost:5144'`, identical to `environment.ts`. Confirmed by inspection 2026-08-12 and restated in KB-050. |
| **Rejected pattern** — eager import of every routed component. | `app.routes.ts:3-6`. Fine at 9 components, fatal to the bundle budget at ~150 screens. |
| **Rejected pattern** — committed build output. | `frontend/vsmart-erp/dist/` and `.angular/cache/` (R-14). |
| **Adopted shapes** — standalone bootstrap via an `ApplicationConfig`, functional `CanActivateFn` and `HttpInterceptorFn`, signals exposed via `asReadonly()`, `core`/`shared`/`features`/`layout` layering, PrimeNG configured once. | `app.config.ts:10-25`, `:14`, `:16-23`; `auth.guard.ts:11-20`; `auth.interceptor.ts:12-24`; `auth.service.ts:34-40`. Full table in [KB-050](../../frontend-new/react-architecture.md#the-angular-pilot--what-to-keep-what-not-to-copy). |

**Two structural facts, both Confirmed:** the pilot is absent from
`NexGen-ERP---2025-master.sln` and nothing in `V.SMART/` references it, so it is not deployed and
no runtime behaviour depends on it. `POST /api/auth/login`
(`V.SMART/V.SMART.Api/Controllers/AuthController.cs:11,39`) appears in this file's frontmatter
only because the pilot consumes it; the endpoint itself is untouched by anything here and is
still consumed by `M2-C02`.

## The pre-ADR-007 archive specification — suspended, not executable

The full archive plan (tag-and-delete vs move-to-`archive/`, the `.gitignore` and CI cleanup, the
KB-015 rewrite, the R-34/R-14 closures) was written on the assumption that a **React** app was
replacing the pilot. ADR-007 removed that assumption, and reading (b) would invert the plan
entirely. It is therefore **not reproduced here as an instruction**; it is retrievable in full
from git:

```bash
git show 7b4b86c:docs/kb/execution/tasks/M2-C11.md
```

Every fact in it worth keeping is in
[§ Retained evidence](#retained-evidence--true-under-either-reading) above. Do not resurrect the
plan without an answer to Q-38 — under reading (b) executing it would delete the application.
