---
doc_id: TASK-M2-C12-05
title: Re-specify for Angular — the M2-D tree, plus the tracker and dependency-graph restatement
module: execution
task_id: M2-C12-05
milestone: M2
parent_task: M2-C12
task_type: Documentation
priority: P0
status: Needs Review
estimate: 1 d
depends_on: [M2-C00, M2-C01, M2-C12-01, M2-C12-02, M2-C12-03, M2-C12-04]
source_files:
  - docs/kb/execution/tasks/M2-D01.md
  - docs/kb/execution/tasks/M2-D02.md
  - docs/kb/execution/tasks/M2-D02-01.md
  - docs/kb/execution/tasks/M2-D02-02.md
  - docs/kb/execution/tasks/M2-D02-03.md
  - docs/kb/execution/tasks/M2-D03.md
  - docs/kb/execution/task-tracker.md
  - docs/kb/execution/dependency-graph.md
business_rules: []
confidence: n/a
last_verified: 2026-08-22
complexity: MEDIUM
risk: LOW
---

# M2-C12-05 — Re-specify for Angular: the M2-D tree, plus the tracker and dependency-graph restatement

| Field | Value |
|---|---|
| Task ID | M2-C12-05 |
| Milestone | M2 — Foundation |
| Parent | [M2-C12](M2-C12.md) |
| Type | Documentation |
| Priority | P0 |
| Status | Needs Review |
| Estimate | 1 d |
| Gate | G2 |

## Objective

Re-specify **this batch only** — the M2-D tree, plus the tracker and dependency-graph restatement — against
[`ADR-007`](../../decisions/ADR-007-angular-stack.md) and the Angular rewrite of
[KB-050](../../frontend-new/react-architecture.md), removing the `⛔ STOP` banner from each file
*in the same change that removes its React content*.

**Files in this batch — no others may be edited except the ones named under "Also" below:**

- `docs/kb/execution/tasks/M2-D01.md`
- `docs/kb/execution/tasks/M2-D02.md`
- `docs/kb/execution/tasks/M2-D02-01.md`
- `docs/kb/execution/tasks/M2-D02-02.md`
- `docs/kb/execution/tasks/M2-D02-03.md`
- `docs/kb/execution/tasks/M2-D03.md`

Read [`M2-C12`](M2-C12.md) first. It holds the translation table, the rationale, the
out-of-scope list and the failure history. This file does not repeat them; it narrows them.

## The atomicity rule — inherited from the parent, and non-negotiable

> **A file's ⛔ banner may only be removed in the same change that removes its React
> instructions.** No file may come to rest banner-removed and React-bearing — that strips the
> only guard stopping a runner from building the wrong stack while leaving the instruction to
> build it. If you cannot finish a file, **leave its banner in place** and report it as not
> done. Half-done is worse than not started.

Attempt 1 of the unsplit parent failed exactly this way across 23 files. Do not repeat it.

## Acceptance Criteria

1. Every file in this batch has **no** `⛔ STOP — this specification is superseded` banner.
   Verify with `grep -rl '⛔ STOP — this specification is superseded' docs/kb/execution/tasks/ --exclude='M2-C12*'`
   and confirm none of this batch's files appear.
2. **The atomicity rule holds**: for every file in the batch,
   `grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite'`
   returns **only** lines that explicitly mark themselves historical ("the React specification
   this replaces", "no longer specifies", "not deleted from the record"). Quote the actual
   output per file in the Execution Record — a count alone is not evidence.
3. Each file carries a re-specification note naming `M2-C12-05` and the date, in the shape
   [`M2-C01`](M2-C01.md) uses.
4. Every path given under `frontend/` is `frontend/nexgen-web/…`, and every quoted command
   appears verbatim in [KB-083's verified Angular table](../prompt-template.md#verified-repository-commands).
5. **`depends_on`, `business_rules`, `priority`, `estimate` and gate are byte-unchanged** in
   every file — prove by diffing those frontmatter keys, do not assert it.
6. Every `file:line` citation into `V.SMART/` is byte-unchanged, or its change is justified
   individually. This Blazor evidence was never a React decision and survives.
7. **The diff is confined to `docs/kb/`, and within it to this batch's own footprint.** That
   footprint is: the batch files named above; **this task file** (`M2-C12-05.md` — its Execution
   Record is where criterion 2's grep output is quoted, and KB-088 §4 makes it an *Always*
   update); [`task-tracker.md`](../task-tracker.md) and
   [`dependency-graph.md`](../dependency-graph.md) (**this batch owns the whole-tree
   restatement**, so it edits all 25 re-specified rows plus the `M2-C12` parent row); and
   [`open-questions.md`](../../open-questions.md), [`failure-log.md`](../failure-log.md),
   [`runner-state.md`](../runner-state.md) and [`current-task.md`](../current-task.md) as the work
   requires. Nothing under `V.SMART/`, `tests/`, `frontend/` or `.github/` may appear — verify
   with `git diff --name-only master...HEAD | grep -v '^docs/kb/'`, expected **empty**.
8. Each file still satisfies [KB-090](../task-template.md)'s required section set.

## Testing Requirements

No code, so no suite. Validation is by inspection plus the greps above, **with their output
quoted**. In addition:

- **Read one file in this batch end to end as if about to implement it.** If it needs a detail
  you would have to invent, it is not re-specified. Name the file read and the outcome.

## Documentation Updates

| Document | Update |
|---|---|
| The batch files | Re-specified; banner removed; note added |
| [KB-081](../task-tracker.md) | **This batch owns the whole-tree restatement**: all 25 re-specified rows plus this sub-task’s own, and the `M2-C12` parent row. |
| [`dependency-graph.md`](../dependency-graph.md) | De-React the M2-C tree description. **This batch owns it.** |
| [KB-004](../../open-questions.md) | Any question raised by an ambiguous re-derivation. Check `git branch --no-merged master` before claiming an id. |

## Completion Conditions

- All 8 acceptance criteria met, greps quoted.
- The atomicity rule holds for every file — verified, not assumed.
- **Only the repository owner may set `Completed`** ([KB-088](../workflow.md#who-may-set-completed)).

## Git Strategy

| | |
|---|---|
| Branch | `migration/m2-c12-05-respec`, cut fresh from `master` |
| Commit | `M2-C12-05: <imperative summary>` |
| Merge | Left for review. Never merged or pushed from an execution session. |

## Execution Record — 2026-08-22

Branch `migration/M2-C12-05-respec`, cut fresh from `master` at `cb788e5`. All six batch files
were **rewritten whole and installed in one operation each** — never banner-removed with React
content still present. The obsolete trailing *Fresh-Session Execution Prompt* blocks were dropped
and each file restructured onto [KB-090](../task-template.md)'s section set, the same shape
`M2-C12-03` and `-04` used. That accounts for the size change: 4,312 lines deleted against 1,183
added, most of it duplicated prompt text.

### Criterion 1 — no banner

```
$ grep -rl '⛔ STOP — this specification is superseded' docs/kb/execution/tasks/ --exclude='M2-C12*'
exit=1  (no output)
```

**Not merely "none of this batch" — nothing at all.** With this branch, the last six bannered files
are gone, so all 25 files across the `M2-C` and `M2-D` trees are re-specified. `M2-C12`'s objective
is discharged.

### Criterion 2 — the atomicity rule, per file

Observed output, quoted rather than counted. Every one of the six is **empty**:

```
$ grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' docs/kb/execution/tasks/M2-D01.md
exit=1  (no output)
$ grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' docs/kb/execution/tasks/M2-D02.md
exit=1  (no output)
$ grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' docs/kb/execution/tasks/M2-D02-01.md
exit=1  (no output)
$ grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' docs/kb/execution/tasks/M2-D02-02.md
exit=1  (no output)
$ grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' docs/kb/execution/tasks/M2-D02-03.md
exit=1  (no output)
$ grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' docs/kb/execution/tasks/M2-D03.md
exit=1  (no output)
```

The "explicitly historical" allowance was not needed. A separate `\breact\b` search returns 18
hits, and **every one is either the literal filename `react-architecture.md` (KB-050's real path,
unchanged by `M2-C00`'s rewrite of its contents) or an explicitly historical sentence** — for
example `M2-D02-03.md:126`, *"it is no less a failure in Angular than it was going to be in
React"*, and the four re-specification notes' *"instead of React"* clauses. Nothing instructs
anyone to build React.

### Criterion 3 — re-specification note

All six carry the `M2-C12-05` + `2026-08-22` note in the shape `M2-C01` established and
`M2-C12-02` / `-03` / `-04` used — one occurrence each.

### Criterion 4 — paths and commands

Every `frontend/` path in the six resolves to exactly two prefixes, verified by enumerating every
match of `frontend/[A-Za-z0-9_.-]+`: **`frontend/nexgen-web`** and **`frontend/vsmart-erp`**. The
second is the Angular pilot and is correct where it appears — as read-only prior art, and under
*files that must not change* — which is `M2-C12-04`'s precedent, and is *more* right under ADR-007
than it was under ADR-003, because the pilot is Angular. Component paths use the realised
`src/app/features/…` and `src/app/shared/…` (with the `app/` segment); the replaced files used
`src/features/…`, which never existed on disk.

Every command quoted in a fenced block, across all six files:

```
dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj
dotnet build V.SMART/V.SMART.Web/V.SMART.Web.csproj
dotnet test tests/V.SMART.Api.Tests/V.SMART.Api.Tests.csproj
dotnet test tests/V.SMART.Shared.Tests/V.SMART.Shared.Tests.csproj
git status --porcelain
npm ci
npm run build
npm run e2e
npm run format:check
npm run lint
npm run test:ci
npm run typecheck
```

All twelve appear **verbatim** in KB-083's two verified tables. Four things the replaced files
quoted are **gone**, each for a stated reason: `dotnet test tests/V.SMART.Shared.Tests/` (the
directory form — the verified row is the `.csproj` form, and that project restores itself, so
`--no-build` must not be added); `git diff --stat <path>` (no row in KB-083 — replaced by an
instruction to read the full `git diff`); ad-hoc `grep`/`awk` verification lines (ordinary
searches, now described in prose so they are not mistaken for verified commands); and the whole
"frontend commands are established by M2-C01, none is verified yet" caveat, which was true on
2026-08-12 and is false now. No coverage command and no separate accessibility-audit command was
invented — `axe` runs inside `npm run test:ci`, as `M2-C12-03` established.

### Criterion 5 — protected frontmatter byte-unchanged

```
$ for f in M2-D01 M2-D02 M2-D02-01 M2-D02-02 M2-D02-03 M2-D03; do
    diff <(git show master:docs/kb/execution/tasks/$f.md | grep -E '^(depends_on|business_rules|priority|estimate):') \
         <(grep -E '^(depends_on|business_rules|priority|estimate):' docs/kb/execution/tasks/$f.md) && echo "$f SAME"; done
M2-D01 SAME
M2-D02 SAME
M2-D02-01 SAME
M2-D02-02 SAME
M2-D02-03 SAME
M2-D03 SAME
```

The same diff over the field table's `| Task ID |`, `| Milestone |`, `| Parent |`, `| Type |`,
`| Priority |`, `| Status |`, `| Estimate |` and `| Gate |` rows also returned `SAME` for all six.
In frontmatter only `last_verified` moved (`2026-08-12 → 2026-08-22`, required by KB-088 §4) and
three `title:` values lost the word "React": `Currency end-to-end in React → … in Angular`,
`… React list + editor screens → … Angular list + editor screens`, and
`Blazor <-> React parity test → Blazor <-> Angular parity test`. **Those three are corrections, not
drift** — `task-tracker.md` has carried the Angular titles since `5cfb3fe` (2026-08-20), so the
frontmatter was the stale copy. `title` is not among the keys criterion 5 protects.

### Criterion 6 — `V.SMART/` citations

Diffed the sorted set of every `*.cs:NNN` / `*.razor:NNN` citation, before and after, per file:

```
M2-D01     SAME
M2-D02     SAME
M2-D02-01  5 removed, 0 added, 0 changed
M2-D02-02  SAME
M2-D02-03  SAME
M2-D03     SAME
```

**The five losses in `M2-D02-01` are all inside the deleted *Fresh-Session Execution Prompt* block,
and every fact survives in the body.** Verified individually against `master`:
`CustomerUpsert.razor:13` (`master`:871) and `:957` (`:872`) both still appear in the body's
negative-findings list; `:954-1125` (`:873`) still appears in extraction step 6.4;
`CustomerUpsert.razor:1000-1012` (`:1125`) was the prompt's coarser paraphrase of BR-CUST-012's
`:1000`, `:1007`, `:1047`, `:1073` and BR-CUST-013's `:1037-1086`, all of which survive; and
`CustomerVM.cs:20-118` (`:1022`) was shorthand for the `DataAnnotations` block that `M2-D02-02`
enumerates line by line, unchanged. **No citation was altered, renumbered or invented in any of the
six files.** The seventeen `BR-CUST-*` rows, with their `file:line` evidence and their
Confirmed / Inferred / Unknown ratings, come through byte-identical — including the three
deliberately preserved probable defects (BR-CUST-003, -010, -012) that `M2-D03`'s scenarios 9, 10
and 11 exist to catch.

### Criterion 7 — diff confinement

```
$ git diff --name-only master...HEAD | grep -v '^docs/kb/'
(empty)
```

Changed files: the six batch files, this file, and `docs/kb/execution/task-tracker.md`. Nothing
under `V.SMART/`, `tests/`, `frontend/` or `.github/`. `docs/kb/execution/current-task.md` and
`docs/kb/execution/runner-state.md` were already modified in the working tree when this session
started and are the **orchestrator's**; they were deliberately left unstaged and uncommitted rather
than raced. No open question, risk or failure-log entry was raised by this work, so those files are
untouched.

### Criterion 8 — KB-090's section set

All six carry `Objective`, `Scope`, `Out of Scope`, `Dependencies`, `Relevant Documentation`,
`Relevant Existing Code`, `Business Rules`, `Implementation Requirements`, `Acceptance Criteria`,
`Testing Requirements`, `Documentation Updates`, `Completion Conditions` and `Git Strategy`, plus
the task-specific sections each already had (`Why This Task Exists`, `Prerequisites`,
`Investigation Requirements`, `Existing Behavior to Preserve`, `Target Result`, `API Changes`,
`Frontend Changes`, `Verification Commands`). `M2-D02` is a parent and keeps `Child Tasks` and
`Rollup Gate Contribution`, exactly as `M2-C12-03` / `-04` left `M2-C05` and `M2-C08`. Section
counts: 22 / 16 / 21 / 21 / 21 / 21.

### The whole-tree restatement (KB-081)

**Twenty-one rows restated, plus this sub-task's own and the `M2-C12` parent's.** Two were
materially wrong, and they are the reason this was worth doing rather than cosmetic: `M2-C10` and
`M2-C04-01` still read *"still **⛔ superseded** … must be re-specified for Angular before it can be
selected"*, which stopped being true when `M2-C12-02` and `M2-C12-01` merged earlier the same day.
A tracker row that refuses a selectable task costs exactly as much as one that offers an
unselectable one. Every other `M2-C*` / `M2-D*` row now names **which sub-batch re-specified it and
what its real remaining blocker is** — a dependency or a gate, never the supersession.

The section heading `### M2-C — React foundation` became
`### M2-C — Frontend foundation (Angular, per ADR-007)`. Footnote ²⁶'s subsection *"All 26
`M2-C*`/`M2-D*` task files carry a ⛔ STOP banner — they were not rewritten"* is **annotated as
superseded, not rewritten**: its reasoning was correct for 2026-08-20, and the condition it named —
a real KB-050 to specify against — was met before any file was touched. Footnote ⁴⁶ carries the new
record.

### `dependency-graph.md` (KB-082) — checked, deliberately unchanged

This batch owns "de-React the M2-C tree description". **There is nothing there to de-React**, and
that is a finding rather than an omission:

```
$ grep -rniE 'react|mantine|tanstack|zustand|\bvite\b|\.tsx|\bzod\b|typescript' docs/kb/execution/dependency-graph.md
151:| M2-D02-01 → M2-D02-03 | **Hard** | extract-before-rebuild. Building the screen first is how logic silently lands in TypeScript |
```

One hit across all 311 lines, and it is framework-neutral — Angular is TypeScript too, so the
sentence is as true now as when it was written. The file describes edges, dependency classes, the
critical path and the selection rule; it never named a stack. Per KB-088 §4 a document is updated
only when something actually changed, so it was left alone and the **negative result recorded here**
instead of a no-op `last_verified` bump. A reviewer checking the *Documentation Updates* table
against the diff should expect this file to be absent.

### Testing requirement — one file read end to end as if about to implement it

**File read: `M2-D02-03.md`** — chosen deliberately, because it is the largest and most
stack-dependent file in the batch and therefore the one most likely to have an invention gap.
Outcome: **implementable without inventing a detail.** The route set, the lazy-loading mechanism,
the component list and their file paths, the service shape (signals + explicit `refresh()`), the
form model (a typed `FormGroup` with a `FormArray` per child collection), the validation-display
mechanism (`app-form-field`, the single such mechanism per `M2-C04-02`), the permission triad
(`requireScreen()` / `*appHasRight` / `forScreen()`), the error mapping including indexed child
paths, the unsaved-changes mechanism (`CanDeactivateFn`), the 33 columns with their default
visibility, the four tabs, the test list, the test harness (`@testing-library/angular` +
`provideHttpClientTesting`) and every command are all stated.

Three things an implementer does not know in advance are **specified as decisions to make and
record**, not left as gaps: whether a states endpoint exists at all (an explicit
Blocked-and-report condition, because inventing a client-side states list would be exactly the
reimplementation this task exists to prevent); whether `LineItemGrid` or a simpler editable table
suits two small unordered child collections; and whether the Blazor status filter's
clear-all-other-filters side effect (`CustomerList.razor:953-980`) is behaviour to preserve or a
defect to raise. Each names its evidence and its consequence for `M2-D03`'s parity test. That is a
specified judgement, not a missing specification.

### Judgement calls, recorded rather than buried

1. **`frontend/vsmart-erp/` references kept.** Under ADR-003 the pilot was "archived, not
   converted"; under ADR-007 it is Angular prior art whose useful parts KB-050 has already absorbed
   (§ *The Angular pilot — what to keep, what not to copy*). `M2-D01` now says explicitly what to
   take from it (filter and column choices) and what not to (eager route imports, `localStorage`
   tokens, a hard-coded API base URL — the three defects KB-050 names). Same call as `M2-C12-04`.
2. **`M2-D02-02` now points at `tests/V.SMART.Api.Tests/`, which exists.** The replaced text said
   "if no API test project exists, one must be added" — `M2-A06` created it on 2026-08-20. The
   re-specified file directs the implementer to **extend** it, and warns, citing **R-43**
   (`technical-debt-register.md:1622`), that it is controller-level only: no
   `Microsoft.AspNetCore.Mvc.Testing`, no host, so nothing in it can currently assert an HTTP
   status over the wire. That matters, because several of that task's criteria are status-code
   assertions.
3. **`M2-D01`'s token-storage line no longer states a rule.** The replaced text asserted "refresh
   token in an httpOnly cookie (ADR-003 rule 5)". KB-050 records token storage as **an open
   decision owned by `M2-C02`**, so the re-specified file defers to that task rather than
   pre-deciding it — while keeping the finding that the pilot's `localStorage` approach is
   XSS-exposed.
4. **`M2-D02-01`'s `dotnet test` invocation was corrected to the `.csproj` form**, with KB-083's
   warning against `--no-build` / `--no-restore` carried in. The directory form was never verified.

**No new open question, risk or ADR.** Nothing in the batch required a decision the repository had
not already made, and `open-questions.md` is correctly untouched — `Q-38` (what `M2-C11` is for)
and `Q-71` (`LineItemGrid`'s table technology) already exist and are referenced, not duplicated.

### Not done

Nothing in scope. `dependency-graph.md` is unchanged **by finding**, not by omission — see above.

### Status

`Needs Review`. Not merged, not pushed. **Only the repository owner may set `Completed`**
([KB-088](../workflow.md#who-may-set-completed)).
