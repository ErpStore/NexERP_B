---
doc_id: TASK-M2-C10
title: Decimal handling — no float money arithmetic
module: execution
task_id: M2-C10
milestone: M2
parent_task: n/a
task_type: Frontend
priority: P0
status: Not Started
estimate: 2 d
depends_on: [M2-C01]
source_files:
  - V.SMART/V.SMART.Shared/Data/Master/Company_Module/Companydetails.cs
  - V.SMART/V.SMART.Shared/Data/SalesAndLabour/SalesInvoice/MfgInv.cs
  - V.SMART/V.SMART.Shared/Data/OutSourcing/PurchasePo/PurchPo.cs
api_endpoints: []
business_rules: [BR-CALC-001, BR-STK-001]
confidence: n/a
last_verified: 2026-08-22
---

# M2-C10 — Decimal handling: no float money arithmetic

> **Re-specified for Angular on 2026-08-22 by [`M2-C12-02`](M2-C12-02.md).** The ⛔ STOP banner
> that stood here is removed **in the same change that removed the stack-specific instructions it
> guarded**: this file no longer specifies the ADR-003 stack.
> [`ADR-007`](../../decisions/ADR-007-angular-stack.md) is the governing stack decision and
> [KB-050](../../frontend-new/react-architecture.md), rewritten for Angular by `M2-C00`, is the
> structure this module lives in.
>
> **Almost nothing about this task's substance changed, and that is the point.** ADR-007 carries
> `decimal.js` over from ADR-003 **explicitly and unchanged** — it is listed under *"Carried over
> from ADR-003 unchanged"* (`ADR-007-angular-stack.md:101,170`) and is already installed in the
> workspace (`frontend/nexgen-web/package.json`: `"decimal.js": "^10.6.0"`, verified 2026-08-22).
> What changed is the *where* and the *how of enforcement*: the module's path is now under
> `src/app/shared/utils/`, the lint rules go into the flat `eslint.config.js` that `M2-C01`
> already wrote, the parser feeds **Reactive Forms validators** rather than a schema library, and
> the test runner is Vitest through `@angular/build:unit-test`. The `file:line` evidence about the
> existing Blazor implementation was never a framework decision and comes through byte-identical.
>
> **One acceptance criterion had to change its mechanism, and it is called out rather than
> quietly dropped.** The specification this replaces required *100 % statement and branch
> coverage* of the module. **This workspace has no verified coverage command** — `package.json`
> declares no `coverage` script and KB-083's Angular table has no coverage row (the same gap
> `M2-C12-01` recorded for `M2-C04-01`). The *intent* — every numeric path tested — survives as an
> explicit enumerated test list plus a review obligation. Do not invent a coverage command, and do
> not quote the superseded one.
>
> **What did not change:** `depends_on`, `business_rules`, priority, estimate and gate.

| Field | Value |
|---|---|
| Task ID | M2-C10 |
| Milestone | M2 — Foundation |
| Parent | — (this is a top-level task) |
| Type | Frontend |
| Priority | P0 |
| Status | Not Started |
| Estimate | 2 d |
| Gate | G2 |

## Task Objective

Establish, in the SPA, a single decimal module built on `decimal.js` — the library
[ADR-007](../../decisions/ADR-007-angular-stack.md) carries over unchanged — through which
**all** money, quantity, rate, percentage and tax values are parsed, held, compared, formatted
and (where unavoidable) arithmetically combined. Then make the alternative impossible: add lint
rules and tests that fail the build when a monetary or quantity value is touched by
JavaScript's `number` arithmetic.

Location: `frontend/nexgen-web/src/app/shared/utils/decimal/` — **TO BE CREATED**.
`src/app/shared/utils/` is named by
[KB-050 §Project structure](../../frontend-new/react-architecture.md#project-structure) and
exists today holding only a `.gitkeep` (verified 2026-08-22); the `decimal/` subfolder and every
file inside it are **proposed** by this task. A display **pipe** belongs in the sibling
`src/app/shared/pipes/`, also named by KB-050.

The complementary half of this task is a **prohibition**: the client never *owns* a
calculated result. Totals, taxes and stock allocations come from the server
(BR-CALC-001, BR-STK-001). The decimal module exists so the client can *display* and
*preview* those numbers exactly, not so it can compute them.

## Why This Task Exists

- **ADR-007 keeps `decimal.js` for "money display arithmetic"** and inherits ADR-003's
  non-negotiable rule against reimplementing `CalculationService` in TypeScript — restated by
  ADR-007 as *"Server-authoritative everything … No ERP business logic in TypeScript"*
  (`ADR-007-angular-stack.md:164-170`). A library in `package.json` that nobody is required to
  use enforces nothing. This task turns the decision into a mechanism.
- **JavaScript `number` is IEEE-754 binary floating point.** `0.1 + 0.2 !== 0.3`.
  In an ERP that prices to `Companydetails.DecimalPlaces` (default **2**, Confirmed:
  `V.SMART/V.SMART.Shared/Data/Master/Company_Module/Companydetails.cs:208`) and carries an
  explicit per-document `RoundOff` field (Confirmed:
  `V.SMART/V.SMART.Shared/Data/OutSourcing/PurchasePo/PurchPo.cs:167`,
  `V.SMART/V.SMART.Shared/Data/SalesAndLabour/SalesInvoice/MfgInv.cs:210` with
  `IsRoundOffEnabled` defaulting to `true`), a one-paisa client/server disagreement is not
  cosmetic — it is a document that fails to reconcile against the printed invoice.
- **The server-side model is `decimal`, not `double`.** Every amount and quantity column in
  `V.SMART.Shared/Data/**` is C# `decimal` (Confirmed by inspection of the entity files in
  `source_files`). Deserialising `decimal` into a JS `number` silently changes the numeric
  type system at the HTTP boundary. This task defines the crossing rule.
- **It must land before the first editable grid.** `M2-C07` (the line-item grid) depends on
  M2-C10 in [KB-080 §9](../README.md#m2-c--react-foundation) precisely because a line grid
  is where float arithmetic would otherwise appear first, in the highest-volume,
  lowest-visibility code in the app.
- **BR-CALC-001 (Confirmed)** states one engine computes every document's gross, discount,
  freight, taxable value, CGST/SGST/IGST, TCS, round-off and grand total:
  `CalculationService.UpdateTotalsAsync(ICalculationDocument)`. There is exactly one
  implementation and it is on the server. This task's rules are how that stays true when
  ~65 document editors are written in the SPA.
- **The enforcement point already exists and is waiting.** `M2-C01` wrote
  `frontend/nexgen-web/eslint.config.js` with a `no-restricted-imports` block whose message
  already reads *"Use dates from date-fns and money from decimal.js"* — the rules below extend
  that file rather than introducing a new mechanism.

## Prerequisites

- **M2-C01 complete** — the Angular workspace, its TypeScript `strict` configuration, its flat
  `eslint.config.js` and its Vitest harness must exist. `decimal.js` is already installed by
  M2-C01 as part of the ADR-007 dependency baseline; this task does not add a dependency, it
  adds a discipline.
- No backend work is required. This task can run in parallel with all of M2-A and M2-B.

## Dependencies

| Dependency | Class | Note |
|---|---|---|
| M2-C01 | Hard | Provides the workspace, `decimal.js`, the flat `eslint.config.js` and Vitest. |
| ADR-007 | Information | Carries `decimal.js` forward; keeps calculation, stock allocation and validation server-side. |
| BR-CALC-001, BR-STK-001 | Information | The server-side rules the client must not duplicate. |
| M2-B10 — generated OpenAPI client | Soft | The serialisation decision made here (how `decimal` crosses the wire) must be reflected when the client is generated. This task records the requirement; M2-B10 honours it. |
| M2-C04-02 — form controls | Testing | The number and currency inputs consume this module; their tests exercise it. |
| M2-C07 — the line-item grid | Testing | The highest-risk consumer. Its acceptance depends on this module existing. |

## Required Existing Knowledge

| Doc | Why |
|---|---|
| [ADR-007](../../decisions/ADR-007-angular-stack.md) | `decimal.js` is carried over unchanged; server-authoritative everything. |
| [KB-030 BR-CALC-001](../../business-rules/business-rule-inventory.md) | The full ordered total-calculation algorithm that must **not** be ported. |
| [KB-050 §Project structure, §What is deliberately not rebuilt in the SPA](../../frontend-new/react-architecture.md) | `shared/utils/` placement; document totals and FIFO allocation stay server-side. |
| [KB-051](../../frontend-new/design-system.md) | Tabular figures, right-aligned numeric columns, number inputs taking decimal places from server settings. |
| [KB-012](../../architecture/database-architecture.md) | Storage types for amounts and quantities. |

## Investigation Requirements

**New, narrow investigation — record it.**

1. **The wire format for `decimal`.** Determine, from the running API and its OpenAPI
   document, whether a C# `decimal` currently serialises as a JSON **number** or a JSON
   **string**. Evidence source: `V.SMART/V.SMART.Api/` serialiser configuration and the
   existing `CurrencyController` responses. This is the single fact the whole task hinges
   on and it must be measured, not assumed.
2. **The authoritative decimal-place source.** `Companydetails.DecimalPlaces` exists with a
   default of `2` (`Companydetails.cs:208`). Determine whether any *other* per-item or
   per-unit precision exists (quantity precision on `Item`, rate precision on price lists).
   Record what you find, including a negative result.
3. **Rounding mode.** Determine which rounding the server applies (banker's vs
   half-away-from-zero) for the `RoundOff` field. If it cannot be determined from code
   within the task's budget, record it as **Unknown** and raise it as a question — do not
   guess a mode and encode it in the client.

Record all three under a **new investigation id, INV-032 — "Decimal representation across
the API boundary"**, using the evidence format from
[KB-083](../prompt-template.md#evidence-format-mandatory-for-new-findings). Check
`git branch --no-merged master` before claiming the id.

## Repository Areas

| Area | Status | Role |
|---|---|---|
| `frontend/nexgen-web/src/app/shared/utils/` | Exists, `.gitkeep` only (named by KB-050) | Parent of the proposed `decimal/` module. |
| `frontend/nexgen-web/src/app/shared/pipes/` | Exists, `.gitkeep` only (named by KB-050) | Where the display pipe lives. |
| `frontend/nexgen-web/eslint.config.js` | Created by M2-C01 | Gains the restriction rules; its `no-restricted-imports` block already names `decimal.js` in its message. |
| `frontend/nexgen-web/src/app/core/api/` | `generated/` exists with a `.gitkeep`; the wrapper layer is M2-B10's | Where the wire-boundary conversion is documented and, later, applied. |
| `V.SMART/V.SMART.Shared/Data/**` | Exists | Read-only evidence for storage types and precision. |
| `V.SMART/V.SMART.Api/` | Exists | Read-only evidence for the serialisation format. |

## Existing Behavior to Preserve

None — this task introduces new presentation code and touches no existing behaviour.

It does, however, **preserve two existing server behaviours by refusing to duplicate them**:

| Rule | Where it lives | What the client must never do |
|---|---|---|
| BR-CALC-001 | `CalculationService.UpdateTotalsAsync(ICalculationDocument)` | Compute a document total, tax, discount, freight, TCS or round-off as the value that gets saved. |
| BR-STK-001 | `StockManagerService` FIFO allocation | Compute any stock allocation or balance quantity. |

No Razor file is superseded. The Blazor UI is untouched.

## Target Result

A module at `frontend/nexgen-web/src/app/shared/utils/decimal/` — **all paths proposed** —
exposing:

| Export | Purpose |
|---|---|
| `type Money` / `type Qty` (branded wrappers over `Decimal`) | Make a money value structurally distinct from a `number` so the compiler catches misuse. |
| `money(input: string \| number \| Decimal): Money`, `qty(...)` | The only constructors. Accept the wire value; reject `NaN`, `Infinity` and `undefined` loudly. |
| `fromApi(value: unknown, field: string): Money` | The single wire-boundary crossing point, implementing whatever INV-032 measures. |
| `toApi(value: Money): string \| number` | The reverse, matching the same contract. |
| `add`, `sub`, `mul`, `div`, `neg`, `abs` | Explicit, total functions over `Money`/`Qty`. There is no operator overloading in TypeScript — arithmetic must be a function call, which is what makes the lint rule enforceable. |
| `eq`, `lt`, `lte`, `gt`, `gte`, `isZero`, `cmp` | Comparison. `===` on wrapped values is meaningless and must never be used. |
| `round(value, places, mode)` | Explicit rounding only. No implicit rounding anywhere else. |
| `format(value, { places, locale, currency? })` | Display. Uses `Intl.NumberFormat` with tabular output per [KB-051 §Typography](../../frontend-new/design-system.md#typography). |
| `parseUserInput(raw: string, { places }): ParseResult<Money>` | Input parsing: locale separators, blank, minus, partial input during typing. Returns a discriminated result, never throws, never silently yields `0`. |
| `ZERO`, `isMoney`, `isQty` | Constants and guards. |

Plus:

- **An Angular display pipe** — `src/app/shared/pipes/money.pipe.ts`, a standalone `@Pipe` that
  is `pure` and delegates to `format()`. Templates never call `format()` directly and never
  interpolate a raw `Decimal`. Angular's built-in `DecimalPipe`/`CurrencyPipe` must **not** be
  used on money: both coerce to `number` and reintroduce the exact hazard this task removes.
- **A documented precision policy.** Default display places come from
  `Companydetails.DecimalPlaces` (server-supplied, default `2`) — never hardcoded in a
  component. Until the settings endpoint exists, the module reads a single configurable
  default with a `TODO` naming the task that will supply it.
- **`Decimal` is configured once**, at module load, with an explicit precision and rounding
  mode, and `decimal.js` is imported **only** inside this module. Nothing else in the app
  imports it.
- **ESLint rules**, added to the existing flat `eslint.config.js`:
  1. `no-restricted-imports` — `decimal.js` may be imported only from
     `src/app/shared/utils/decimal/**`. This extends the `bannedComponentLibraries` /
     `bannedGeneratedClientImports` pattern the file already uses; follow that shape rather
     than inventing a parallel one.
  2. `no-restricted-syntax` — ban `parseFloat`, `Number.parseFloat` and unary `+` coercion
     outside the decimal module.
  3. `no-restricted-properties` — ban `.toFixed()` app-wide; formatting goes through
     `format()` or the pipe.
  4. A rule banning `Math.round`, `Math.floor`, `Math.ceil` outside the decimal module.
  Each rule needs an override block exempting `src/app/shared/utils/decimal/**`, exactly as
  the file already exempts `src/app/core/api/**` from the generated-client ban.
- **An architecture test** (Vitest) that scans the source tree and fails if `decimal.js`,
  `toFixed`, `parseFloat`, `DecimalPipe`, `CurrencyPipe` or a bare arithmetic operator applied
  to an identifier matching `/amount|price|rate|qty|quantity|total|tax|discount|gross|net/i`
  appears outside the module. Lint catches the common cases; this test catches the ones written
  before the rule is configured or suppressed with an inline disable.
- **A written prohibition** in `frontend/nexgen-web/README.md` and in
  [KB-050](../../frontend-new/react-architecture.md): totals come from the server's calculate
  endpoint; a local preview may be shown for responsiveness but is **overwritten by the server
  result before save**, and is visually marked as provisional.

## Implementation Steps

1. **Run INV-032 first.** Determine the wire format for `decimal`, the precision sources,
   and the server's rounding mode. Everything below depends on the answer to step 1a:
   1a. Inspect `V.SMART/V.SMART.Api/Program.cs` and the controllers for JSON serialiser
       configuration; call the running API's `CurrencyController` and read the raw JSON.
   1b. Confirm `Companydetails.DecimalPlaces` (`Companydetails.cs:208`) is the settings
       source, and search for any per-item quantity precision.
   1c. Search `CalculationService` for its rounding call. If ambiguous, record **Unknown**
       and raise it as an open question rather than choosing one.
2. **Create the branch** `migration/M2-C10-decimal-handling`.
3. **Create `src/app/shared/utils/decimal/decimal.ts`** — the only file in the app that imports
   `decimal.js`. Configure `Decimal.set({ precision: 34, rounding: <from step 1c>,
   toExpNeg: -9, toExpPos: 21 })`. If step 1c returned Unknown, use
   `Decimal.ROUND_HALF_UP`, add a prominent comment naming INV-032, and flag the assumption
   in the final report.
4. **Define the branded types** `Money` and `Qty`. Branding matters: without it, `Money` and
   `number` are interchangeable to the compiler and every rule below becomes advisory. Use a
   unique-symbol brand so a raw `Decimal` cannot be passed where `Money` is expected.
5. **Implement the constructors and the wire boundary** (`money`, `qty`, `fromApi`,
   `toApi`). `fromApi` must throw a named error on `null`/`undefined`/`NaN` rather than
   coercing to zero — a missing amount rendered as `0.00` is the exact class of bug this
   task exists to prevent.
6. **Implement arithmetic, comparison, rounding and formatting** as listed in
   [Target Result](#target-result). Keep every function total and pure. No function in this
   module may perform ERP arithmetic — there is no `calculateLineTotal`, no `applyTax`,
   no `computeGrandTotal`. If a caller wants one, the answer is an API call.
7. **Implement `parseUserInput`.** Handle: empty string, `-`, `.`, trailing separator during
   typing, thousands separators for the active locale, and more decimals than allowed. Return
   a discriminated result. It must never throw and never silently substitute `0`. This result
   is what a Reactive Forms `ValidatorFn` for money fields consumes — M2-C04-02 wires it, and
   that is recorded here as a constraint on that task.
8. **Write `src/app/shared/pipes/money.pipe.ts`** as a standalone pure pipe over `format()`.
9. **Add the ESLint rules** listed above to `eslint.config.js`, with the decimal module
   exempted via an override block in the file's existing style.
10. **Write the architecture test** `decimal/no-float-money.spec.ts` that scans `src/**`
    (excluding the decimal module and test fixtures) for the banned patterns and asserts zero
    matches, printing every offending `file:line` on failure.
11. **Write the unit tests** listed under [Testing](#testing), including the classic
    float-failure cases as explicit regression tests.
12. **Document the prohibition** in `frontend/nexgen-web/README.md` (a short "Money and
    quantities" section) and add a cross-reference paragraph to
    [KB-050](../../frontend-new/react-architecture.md).
13. **Verify** with the command set in [Verification Commands](#verification-commands).
14. **Record INV-032**, update the tracker, review the diff, commit.

## Files Expected to Change

| File | Change |
|---|---|
| `frontend/nexgen-web/eslint.config.js` | Add the four restriction rules plus the decimal-module override. |
| `frontend/nexgen-web/README.md` | Add a "Money and quantities" section stating the prohibition. |
| `docs/kb/frontend-new/react-architecture.md` (KB-050) | Record the decimal module's location and the server-authoritative preview rule. Bump `last_verified`. |
| `docs/kb/investigation-registry.md` (KB-003) | Add **INV-032**. |
| `docs/kb/execution/task-tracker.md` (KB-081) | M2-C10 → the status the workflow allows the session to set. |
| `docs/kb/open-questions.md` (KB-004) | **Only if** step 1c returns Unknown — add a question on the server's rounding mode, owned by the backend team, needed by M2-C08-02. |

## Files Expected to Be Created

All **TO BE CREATED**, under `frontend/nexgen-web/src/app/shared/` (the `utils/` and `pipes/`
parents are named by KB-050; the `decimal/` subfolder and every file name below are proposed):

| File | Contents |
|---|---|
| `utils/decimal/decimal.ts` | The `decimal.js` configuration and the branded `Money`/`Qty` types. The only importer of `decimal.js`. |
| `utils/decimal/money.ts` | Constructors, wire boundary (`fromApi`/`toApi`), arithmetic, comparison, rounding. |
| `utils/decimal/format.ts` | `format()` over `Intl.NumberFormat`; tabular, right-aligned defaults. |
| `utils/decimal/parse.ts` | `parseUserInput()` and its result type. |
| `utils/decimal/precision.ts` | The decimal-place policy and its server-settings hook. |
| `utils/decimal/index.ts` | The module's public surface. Nothing outside imports the files above directly. |
| `utils/decimal/money.spec.ts` | Arithmetic, comparison, rounding, wire-boundary tests. |
| `utils/decimal/format.spec.ts` | Formatting and locale tests. |
| `utils/decimal/parse.spec.ts` | User-input parsing tests. |
| `utils/decimal/no-float-money.spec.ts` | The architecture test. |
| `utils/decimal/README.md` | Why this module exists; what it is forbidden to contain. |
| `pipes/money.pipe.ts` + `money.pipe.spec.ts` | The standalone display pipe. |

## Files That Must Not Change

- `V.SMART/**` — in particular `CalculationService` and `StockManagerService`. This task
  must not "help" by adjusting server rounding.
- `V.SMART/V.SMART.Shared/Migrations/**`, `Existing Store Procedures/**`.
- `frontend/vsmart-erp/**` — the pilot. Its disposition is `M2-C11`'s and is open on **Q-38**.
- `frontend/nexgen-web/package.json` — `decimal.js` is already installed (`^10.6.0`, verified
  2026-08-22). If it is missing, that is an M2-C01 defect: report it rather than silently
  adding a dependency.
- `docs/ARCHITECTURE.md` — superseded (R-37).

## API Changes

Not applicable — this task adds no HTTP surface.

It does record **one requirement against a future task**: whatever wire representation
INV-032 measures must be preserved by the generated client in **M2-B10**. If the API
serialises `decimal` as a JSON number, that is a precision hazard for values beyond
2^53 or with more than 15 significant digits, and the recommendation — serialise `decimal`
as a JSON **string** — must be raised as a finding for M2-B10 and M2-A06, **not**
implemented here.

## Frontend Changes

| Aspect | This task |
|---|---|
| Route | n/a — no route is added or changed. |
| Component | n/a — no component is created. This is a `shared/utils` module plus one `shared/pipes` pipe, consumed by M2-C04-02, M2-C05 and M2-C07. |
| API integration | n/a — no request is made. `fromApi`/`toApi` define the *contract* for values crossing the boundary; the `HttpClient` services that use them arrive with M2-C02 and M2-B10. |
| State | n/a — the module is stateless and pure, except for the single `Decimal.set()` configuration at module load. No signal, no service. |
| Validation | `parseUserInput` returns a typed parse error that M2-C04-02's form field renders. A money field's Reactive Forms `ValidatorFn` must use this parser; `Validators.pattern` over a numeric string is not a substitute. Recorded here as a constraint on M2-C04-02. |
| Permissions | n/a. |
| Loading state | n/a — synchronous, pure functions. |
| Empty state | A `Money` that is absent is `undefined`, never `0`. Components render an em dash for absent amounts, never `0.00`. This is a display rule stated here and implemented by the consuming components. |
| Error state | `fromApi` throws a named `DecimalBoundaryError` naming the field; it surfaces through the `GlobalErrorHandler` M2-C01 installed at `src/app/core/errors/global-error-handler.ts`. `parseUserInput` never throws — it returns an error result for inline field display. |
| Accessibility | Formatted numbers use `font-variant-numeric: tabular-nums` and are right-aligned (KB-051). Negative amounts are rendered with an explicit minus sign and an `aria-label` that says "minus" — parentheses-for-negative alone is a colour/typography-only encoding and is not acceptable. Keyboard interaction: none — this module has no interactive surface. |
| Responsive behaviour | n/a — formatting is layout-independent. |

## Business Logic

**Newly introduced — presentation behaviour only.**

The behaviour introduced is numeric representation, parsing and formatting for display.
**No ERP business rule is implemented.** Specifically, this module contains no line-total,
tax, discount, freight, TCS, round-off, costing or stock-allocation logic. BR-CALC-001 and
BR-STK-001 remain solely server-side, and this task's lint and architecture tests exist to
keep them there.

The one behaviour that touches money semantics — the rounding mode — is **taken from the
server**, not chosen by the client. If it cannot be determined, it is recorded as Unknown
and raised, per [KB-002](../../source-of-truth-rules.md).

## Testing

Tests run on **Vitest through `@angular/build:unit-test`** — the harness `M2-C01` established
and KB-083 verified (`npm run test:ci`).

| Test | Assertion |
|---|---|
| `money.spec.ts` — float regressions | `add(money('0.1'), money('0.2'))` formats as `0.30`, and the raw JS `0.1 + 0.2` case is asserted **not** to be reproduced. |
| `money.spec.ts` — large values | A 20-significant-digit amount survives `fromApi` → `toApi` unchanged. |
| `money.spec.ts` — wire boundary | `fromApi(null)`, `fromApi(undefined)`, `fromApi(NaN)`, `fromApi('')` all throw `DecimalBoundaryError` naming the field; none returns zero. |
| `money.spec.ts` — comparison | `eq`, `lt`, `gt` behave correctly for values that are `===`-unequal but numerically equal (`'1.10'` vs `'1.1'`). |
| `money.spec.ts` — rounding | `round()` is explicit; the configured mode is asserted against the value INV-032 recorded. |
| `money.spec.ts` — branding | A `@ts-expect-error` case proves a `number` and a bare `Decimal` cannot be passed where `Money` is expected. |
| `format.spec.ts` | Two decimal places by default; the places argument overrides; negative values render with a minus sign; zero renders as `0.00`, absent renders as the em dash; thousands grouping matches the locale. |
| `parse.spec.ts` | Empty, `-`, `.`, `1.`, `1,234.56`, `1.234,56` (other locale), `1.999` with 2 places, and a 40-digit input each produce the documented result — never a silent `0`. |
| `money.pipe.spec.ts` | The pipe is pure, delegates to `format()`, and renders the em dash for `undefined`. |
| `no-float-money.spec.ts` | Scanning `src/**` outside the decimal module finds zero occurrences of `decimal.js` imports, `toFixed`, `parseFloat`, `Math.round/floor/ceil`, `DecimalPipe`, `CurrencyPipe`, or arithmetic operators applied to money-named identifiers. Failure output lists every `file:line`. |
| Lint | `npm run lint` fails when a fixture file importing `decimal.js` from outside the module is added, and passes when it is removed. Verified manually during the task and the observed output recorded. |

**On coverage.** The specification this replaces demanded 100 % statement and branch coverage of
the module. **There is no verified coverage command in this workspace** (see the note at the top
of this file). The obligation is therefore discharged as: every exported function above has at
least one test row named for it in the table, and the reviewer checks the export list in
`index.ts` against the test files. If a coverage script is added to `package.json` and verified
into KB-083's table by some later task, restore the numeric gate then — do not add one here as a
side effect of this task.

## Acceptance Criteria

- [ ] `frontend/nexgen-web/src/app/shared/utils/decimal/index.ts` exists and exports `Money`,
      `Qty`, `money`, `qty`, `fromApi`, `toApi`, `add`, `sub`, `mul`, `div`, `neg`, `abs`,
      `eq`, `lt`, `lte`, `gt`, `gte`, `cmp`, `isZero`, `round`, `format`, `parseUserInput`,
      `ZERO`.
- [ ] `Money` and `Qty` are branded types; assigning a `number` or a bare `Decimal` to a
      `Money` parameter is a compile error, proven by a `@ts-expect-error` test.
- [ ] `decimal.js` is imported in exactly one file
      (`src/app/shared/utils/decimal/decimal.ts`), proven by
      `git grep -n "from 'decimal.js'" -- frontend/nexgen-web/src`.
- [ ] `eslint.config.js` restricts `decimal.js` imports, `parseFloat`, unary `+` coercion,
      `.toFixed()` and `Math.round/floor/ceil` outside the decimal module, and
      `npm run lint` passes (it already runs `--max-warnings=0`).
- [ ] `no-float-money.spec.ts` passes and demonstrably fails when a deliberate violation is
      introduced (the demonstration is recorded in the final report, the fixture is not
      committed).
- [ ] Every exported function has at least one named test, and the reviewer has checked the
      `index.ts` export list against the test files. **No coverage percentage is claimed** —
      this workspace has no verified coverage command and none is added here.
- [ ] `fromApi` throws `DecimalBoundaryError` — naming the offending field — for `null`,
      `undefined`, `NaN` and `''`, and never returns zero for them.
- [ ] `add(money('0.1'), money('0.2'))` formats as exactly `0.30`.
- [ ] A standalone `money.pipe.ts` exists in `src/app/shared/pipes/`, and no template uses
      Angular's `DecimalPipe` or `CurrencyPipe` on a money value.
- [ ] The default decimal places are sourced from a single configurable policy in
      `precision.ts` traceable to `Companydetails.DecimalPlaces`
      (`V.SMART/V.SMART.Shared/Data/Master/Company_Module/Companydetails.cs:208`), and no
      component hardcodes `2`.
- [ ] The module contains **no** function that computes an ERP total, tax, discount,
      freight, TCS, round-off, cost or stock allocation. Verified by review and stated in
      the module `README.md`.
- [ ] `frontend/nexgen-web/README.md` and KB-050 both state that totals come from the
      server, that a local preview is provisional, and that it is overwritten before save.
- [ ] **INV-032** is recorded with the measured wire format, the precision source, and the
      rounding mode (or an explicit `Unknown` plus a raised question in KB-004).
- [ ] No file under `V.SMART/` changed, and no file under `frontend/vsmart-erp/` changed.
- [ ] `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run test:ci` and
      `npm run build` all pass.

## Verification Commands

Every `npm` command below appears verbatim in
[KB-083 § Verified frontend commands](../prompt-template.md#verified-repository-commands),
measured on this workstation on 2026-08-21. Run them from `frontend/nexgen-web/`.

```bash
npm run typecheck
```

```bash
npm run lint
```

```bash
npm run format:check
```

```bash
npm run test:ci
```

```bash
npm run build
```

Plus these repository commands (verified working, INV-029), run from the repository root:

```bash
git grep -n "from 'decimal.js'" -- frontend/nexgen-web/src
```

```bash
git grep -n "toFixed\|parseFloat" -- frontend/nexgen-web/src
```

```bash
git diff --name-only -- V.SMART
```

> **There is no verified coverage command for this workspace.** `package.json` declares no
> `coverage` script and KB-083's Angular table has no coverage row. Do not invent one and do not
> quote the superseded command the earlier specification used.
> `git diff --name-only -- V.SMART` must print nothing.

## Documentation Updates

| Document | Update |
|---|---|
| [KB-050](../../frontend-new/react-architecture.md) | Record the decimal module's location, its public surface, and the "preview is provisional, the server owns the result" rule. Bump `last_verified`. |
| [KB-003](../../investigation-registry.md) | Add **INV-032 — Decimal representation across the API boundary**, Complete, with evidence. |
| [KB-004](../../open-questions.md) | Only if the server's rounding mode is Unknown: add the question, owner backend, needed by M2-C08-02. |
| [KB-081](../task-tracker.md) | M2-C10 → the status the workflow allows the session to set. |
| `frontend/nexgen-web/README.md` | New "Money and quantities" section. |
| `frontend/nexgen-web/src/app/shared/utils/decimal/README.md` | **Created.** Why the module exists; the explicit list of what it must never contain. |

## Investigation Registry Updates

Add **INV-032**:

```yaml
Finding:        C# decimal values cross the V.SMART.Api boundary as JSON <number|string>;
                display precision is governed by Companydetails.DecimalPlaces (default 2);
                the server's rounding mode for the RoundOff field is <mode|Unknown>.
Evidence:       V.SMART/V.SMART.Shared/Data/Master/Company_Module/Companydetails.cs:208 ;
                V.SMART/V.SMART.Api/Program.cs:<line> ; raw CurrencyController response
Business rule:  BR-CALC-001
Confidence:     Confirmed | Unknown (per sub-finding)
Last verified:  <date of execution>
```

Each of the three sub-findings carries its own confidence rating. Do not present the
rounding mode as Confirmed unless it was read out of code.

## Completion Conditions

- All acceptance criteria met, with each verification command's **observed** output recorded —
  not its expected output.
- INV-032 recorded, with per-sub-finding confidence and any Unknown raised in KB-004.
- The deliberate lint-failure demonstration performed and its output recorded; the fixture not
  committed.
- The branch is left for review, unmerged.
- **Only the repository owner may set `Completed`**
  ([KB-088](../workflow.md#who-may-set-completed)).

## Git Strategy

| Field | Value |
|---|---|
| Branch | `migration/M2-C10-decimal-handling` |
| Commit | `M2-C10: Add decimal.js money module and ban float arithmetic on money` |
| Expected changed files | New `frontend/nexgen-web/src/app/shared/utils/decimal/**` and `src/app/shared/pipes/money.pipe.ts`; modified `frontend/nexgen-web/eslint.config.js`, `frontend/nexgen-web/README.md`, `docs/kb/frontend-new/react-architecture.md`, `docs/kb/investigation-registry.md`, `docs/kb/execution/task-tracker.md`, and possibly `docs/kb/open-questions.md`. |
| Rollback | Revert the branch's merge commit. Nothing consumes the module yet (M2-C04-02 and M2-C07 come later), so the revert is clean. Reverting also removes the lint rules — note in the revert message that doing so re-opens the float hazard. |

Do not merge. Do not push to `master`. Leave the branch for review.
