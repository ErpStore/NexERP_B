---
doc_id: TASK-M2-C12-04
title: Re-specify for Angular — documents and reports
module: execution
task_id: M2-C12-04
milestone: M2
parent_task: M2-C12
task_type: Documentation
priority: P0
status: Ready
estimate: 1 d
depends_on: [M2-C00, M2-C01]
source_files:
  - docs/kb/execution/tasks/M2-C07.md
  - docs/kb/execution/tasks/M2-C08.md
  - docs/kb/execution/tasks/M2-C08-01.md
  - docs/kb/execution/tasks/M2-C08-02.md
  - docs/kb/execution/tasks/M2-C08-03.md
  - docs/kb/execution/tasks/M2-C09.md
business_rules: []
confidence: n/a
last_verified: 2026-08-22
complexity: MEDIUM
risk: LOW
---

# M2-C12-04 — Re-specify for Angular: documents and reports

| Field | Value |
|---|---|
| Task ID | M2-C12-04 |
| Milestone | M2 — Foundation |
| Parent | [M2-C12](M2-C12.md) |
| Type | Documentation |
| Priority | P0 |
| Status | Ready |
| Estimate | 1 d |
| Gate | G2 |

## Objective

Re-specify **this batch only** — documents and reports — against
[`ADR-007`](../../decisions/ADR-007-angular-stack.md) and the Angular rewrite of
[KB-050](../../frontend-new/react-architecture.md), removing the `⛔ STOP` banner from each file
*in the same change that removes its React content*.

**Files in this batch — no others may be edited except the ones named under "Also" below:**

- `docs/kb/execution/tasks/M2-C07.md`
- `docs/kb/execution/tasks/M2-C08.md`
- `docs/kb/execution/tasks/M2-C08-01.md`
- `docs/kb/execution/tasks/M2-C08-02.md`
- `docs/kb/execution/tasks/M2-C08-03.md`
- `docs/kb/execution/tasks/M2-C09.md`

Read [`M2-C12`](M2-C12.md) first. It holds the translation table, the rationale, the
out-of-scope list and the failure history. This file does not repeat them; it narrows them.

## The atomicity rule — inherited from the parent, and non-negotiable

> **A file's ⛔ banner may only be removed in the same change that removes its React
> instructions.** No file may come to rest banner-removed and React-bearing — that strips the
> only guard stopping a runner from building the wrong stack while leaving the instruction to
> build it. If you cannot finish a file, **leave its banner in place** and report it as not
> done. Half-done is worse than not started.

Attempt 1 of the unsplit parent failed exactly this way across 23 files. Do not repeat it.

## Acceptance Criteria

1. Every file in this batch has **no** `⛔ STOP — this specification is superseded` banner.
   Verify with `grep -rl '⛔ STOP — this specification is superseded' docs/kb/execution/tasks/ --exclude='M2-C12*'`
   and confirm none of this batch's files appear.
2. **The atomicity rule holds**: for every file in the batch,
   `grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite'`
   returns **only** lines that explicitly mark themselves historical ("the React specification
   this replaces", "no longer specifies", "not deleted from the record"). Quote the actual
   output per file in the Execution Record — a count alone is not evidence.
3. Each file carries a re-specification note naming `M2-C12-04` and the date, in the shape
   [`M2-C01`](M2-C01.md) uses.
4. Every path given under `frontend/` is `frontend/nexgen-web/…`, and every quoted command
   appears verbatim in [KB-083's verified Angular table](../prompt-template.md#verified-repository-commands).
5. **`depends_on`, `business_rules`, `priority`, `estimate` and gate are byte-unchanged** in
   every file — prove by diffing those frontmatter keys, do not assert it.
6. Every `file:line` citation into `V.SMART/` is byte-unchanged, or its change is justified
   individually. This Blazor evidence was never a React decision and survives.
7. **The diff is confined to `docs/kb/`, and within it to this batch's own footprint.** That
   footprint is: the batch files named above; **this task file** (`M2-C12-04.md` — its Execution
   Record is where criterion 2's grep output is quoted, and KB-088 §4 makes it an *Always*
   update); [`task-tracker.md`](../task-tracker.md) (this sub-task's own row); and, only if the
   work actually raises one, [`open-questions.md`](../../open-questions.md),
   [`failure-log.md`](../failure-log.md), [`runner-state.md`](../runner-state.md) and
   [`current-task.md`](../current-task.md). Nothing under `V.SMART/`, `tests/`, `frontend/` or
   `.github/` may appear — verify with
   `git diff --name-only master...HEAD | grep -v '^docs/kb/'`, expected **empty**.
8. Each file still satisfies [KB-090](../task-template.md)'s required section set.

## Testing Requirements

No code, so no suite. Validation is by inspection plus the greps above, **with their output
quoted**. In addition:

- **Read one file in this batch end to end as if about to implement it.** If it needs a detail
  you would have to invent, it is not re-specified. Name the file read and the outcome.

## Documentation Updates

| Document | Update |
|---|---|
| The batch files | Re-specified; banner removed; note added |
| [KB-081](../task-tracker.md) | This sub-task's own row only. **The 25 re-specified rows and `dependency-graph.md` belong to `M2-C12-05`**, the last batch, so the tree is restated once. |
| [KB-004](../../open-questions.md) | Any question raised by an ambiguous re-derivation. Check `git branch --no-merged master` before claiming an id. |

## Completion Conditions

- All 8 acceptance criteria met, greps quoted.
- The atomicity rule holds for every file — verified, not assumed.
- **Only the repository owner may set `Completed`** ([KB-088](../workflow.md#who-may-set-completed)).

## Git Strategy

| | |
|---|---|
| Branch | `migration/m2-c12-04-respec`, cut fresh from `master` |
| Commit | `M2-C12-04: <imperative summary>` |
| Merge | Left for review. Never merged or pushed from an execution session. |

---

## Execution Record — 2026-08-22

Branch `migration/M2-C12-04-respec`, cut fresh from `master` at `f8b4dad`. All six batch files
re-specified for Angular/ADR-007; **each `⛔` banner was removed in the same write that removed
that file's stack-specific content** — no file came to rest banner-removed and React-bearing at
any point, because each file was written whole and installed in one operation.

Two structural changes beyond translation, both following the `M2-C12-03` precedent and
[KB-090](../task-template.md) §*Existing task files*: the obsolete trailing **Fresh-Session
Execution Prompt** blocks were dropped (they restated `CLAUDE.md`), and each file was restructured
onto KB-090's section set (`Objective` / `Scope` / `Out of Scope` / … / `Completion Conditions` /
`Git Strategy`).

### Criterion 1 — no banner remains in this batch

```
$ grep -rl '⛔ STOP — this specification is superseded' docs/kb/execution/tasks/ --exclude='M2-C12*'
docs/kb/execution/tasks/M2-C02.md
docs/kb/execution/tasks/M2-C03.md
docs/kb/execution/tasks/M2-C05-01.md
docs/kb/execution/tasks/M2-C05-02.md
docs/kb/execution/tasks/M2-C05-03.md
docs/kb/execution/tasks/M2-C05.md
docs/kb/execution/tasks/M2-C06.md
docs/kb/execution/tasks/M2-C10.md
docs/kb/execution/tasks/M2-C11.md
docs/kb/execution/tasks/M2-D01.md
docs/kb/execution/tasks/M2-D02-01.md
docs/kb/execution/tasks/M2-D02-02.md
docs/kb/execution/tasks/M2-D02-03.md
docs/kb/execution/tasks/M2-D02.md
docs/kb/execution/tasks/M2-D03.md
```

**None of this batch's six files appears.** The files that do are `M2-C12-02`'s batch
(`M2-C02`, `M2-C03`, `M2-C10`, `M2-C11`), `M2-C12-03`'s batch (`M2-C05*`, `M2-C06` — re-specified
on its own unmerged branch, so still bannered on this branch's lineage from `master`) and
`M2-C12-05`'s batch (`M2-D*`). None is in this task's scope.

### Criterion 2 — the atomicity rule, per file

Observed output, quoted rather than counted. Every one of the six is **empty**:

```
$ grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' docs/kb/execution/tasks/M2-C07.md
exit=1  (no output)
$ grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' docs/kb/execution/tasks/M2-C08.md
exit=1  (no output)
$ grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' docs/kb/execution/tasks/M2-C08-01.md
exit=1  (no output)
$ grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' docs/kb/execution/tasks/M2-C08-02.md
exit=1  (no output)
$ grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' docs/kb/execution/tasks/M2-C08-03.md
exit=1  (no output)
$ grep -niE 'mantine|tanstack|zustand|react hook form|\bzod\b|\.tsx|jsx|axios|\bmsw\b|vite' docs/kb/execution/tasks/M2-C09.md
exit=1  (no output)
```

No historical-marked survivors were needed: none of the banned terms remained anywhere, so the
"explicitly historical" allowance was not used. Where the replaced specification had to be
referred to, it is named as *"the ADR-003 stack"*, *"the specification this replaces"* or
*"the superseded specification"* — none of which matches the grep.

### Criterion 3 — re-specification note

All six carry the `M2-C12-04` + `2026-08-22` note in the shape `M2-C01` established and
`M2-C12-02` / `-03` used — one occurrence each, verified by `grep -c`.

### Criterion 4 — paths and commands

Every `frontend/` path in the six is `frontend/nexgen-web/…`, except references to
`frontend/vsmart-erp/**` under *files that must not change*, which is the Angular pilot and is
correct there. Verified by enumerating every match of `frontend/[A-Za-z0-9_.-]+` across the six:
the only two distinct prefixes are `frontend/nexgen-web` and `frontend/vsmart-erp`. Component
paths were also corrected from `src/shared/…` to `src/app/shared/…` and `src/features/…` to
`src/app/features/…`, matching KB-050's realised project structure and `M2-C12-03`'s output.

Every quoted command is one of `npm ci`, `npm run typecheck`, `npm run lint`,
`npm run format:check`, `npm run test:ci`, `npm run build` and `git status --porcelain` — all
present verbatim in [KB-083's verified table](../prompt-template.md#verified-repository-commands).
The replaced files quoted **`npm run test`**, which is *not* verified (only `test:ci` is), and
`git diff --stat -- V.SMART/`, which has no row in KB-083; both are gone, the second replaced by
an instruction to read the full `git diff`. No coverage command and no accessibility-audit command
was invented — each file says so explicitly.

### Criterion 5 — protected frontmatter byte-unchanged

Proved by diff against `master` for all six, for `depends_on`, `business_rules`, `priority` and
`estimate`, and additionally for the `| Gate |`, `| Priority |`, `| Estimate |`, `| Milestone |`,
`| Type |` and `| Status |` rows of the field table:

```
$ for f in M2-C07 M2-C08 M2-C08-01 M2-C08-02 M2-C08-03 M2-C09; do
    diff <(git show master:docs/kb/execution/tasks/$f.md | grep -E '^(depends_on|business_rules|priority|estimate):') \
         <(grep -E '^(depends_on|business_rules|priority|estimate):' docs/kb/execution/tasks/$f.md) && echo "$f SAME"; done
M2-C07 SAME
M2-C08 SAME
M2-C08-01 SAME
M2-C08-02 SAME
M2-C08-03 SAME
M2-C09 SAME
```

The field-table diff returned `SAME` for all six as well. Only `last_verified` moved,
`2026-08-12 → 2026-08-22`, which KB-088 §4 requires of a touched document.

### Criterion 6 — `V.SMART/` citations

Diffed the sorted set of every `*.cs:NNN` / `*.razor:NNN` citation, before and after, per file:

```
M2-C07     SAME
M2-C08     SAME
M2-C08-01  SAME
M2-C08-02  SAME
M2-C08-03  0a1 > ApiProblems.cs:43 ; 13a15 > ProblemTypes.cs:17   (two ADDED; none removed or changed)
M2-C09     SAME
```

**The two additions in `M2-C08-03` are justified individually.** The replaced text described the
error body as carrying a *"correlation id"* — a placeholder written before `M2-A06` shipped. The
contract now exists in code, so the file names the real fields: `traceId`
(`V.SMART/V.SMART.Api/Middleware/ApiProblems.cs:43`, `problem.Extensions["traceId"] = …`) and the
stable `type` base URI (`V.SMART/V.SMART.Api/Middleware/ProblemTypes.cs:17`,
`public const string Base = "https://api.v-smart.local/problems/"`). Both were read on 2026-08-22
and are **Confirmed**. The file still requires the implementer to re-confirm them against the
middleware rather than take the note on trust. No pre-existing citation was altered, renumbered or
dropped in any of the six files.

### Criterion 7 — diff confinement

```
$ git diff --name-only master...HEAD | grep -v '^docs/kb/'
(empty)
```

Changed files: the six batch files, this file, and `docs/kb/execution/task-tracker.md` (this
sub-task's own row plus one footnote). Nothing under `V.SMART/`, `tests/`, `frontend/` or
`.github/`. No open question, risk, failure-log or runner-state entry was raised by this work, so
those files are untouched.

### Criterion 8 — KB-090's section set

All six carry `Objective`, `Scope`, `Out of Scope`, `Dependencies`, `Relevant Documentation`,
`Relevant Existing Code`, `Business Rules`, `Implementation Requirements`, `Acceptance Criteria`,
`Testing Requirements`, `Documentation Updates`, `Completion Conditions` and `Git Strategy`, plus
the task-specific sections each already had. `M2-C08` is a parent and keeps `Child Tasks` and
`Rollup Gate Contribution`, exactly as `M2-C12-03` left `M2-C05`.

### Testing requirement — one file read end to end as if about to implement it

**File read: `M2-C07.md`** — chosen deliberately, because it is the file **Q-71** was expected to
bite. Outcome: **implementable without inventing a detail.** The component name, selector, target
directory, row store (a typed Reactive `FormArray` owned by the caller), change-detection
strategy, keyboard model, cell-editor list, event union, file list, test list, enforcement test
and command list are all stated. The one thing an implementer does not know in advance — whether
PrimeNG's editable `p-table` can carry this keyboard model at 200 rows — is not an invention gap:
`ADR-007-angular-stack.md:151-152` explicitly assigns that evaluation to `M2-C07`, and the file
specifies it as a required, ordered-first measurement with a recorded outcome and an escalation
path if it fails. That is a specified investigation, not a missing decision.

**Q-71 handling.** `ADR-007-angular-stack.md:98`'s *"see below"* has no resolving section beneath
it. The re-specified `M2-C07` therefore cites `:144-152` **directly**, states the AG Grid fallback
exactly as ADR-007 states it, and **does not pre-decide a table technology**. Q-71 stays open and
owner-owned; no ADR was edited. No new open question was raised by this batch, so
`open-questions.md` is untouched.

### Not done

Nothing in scope. The 25-row tracker restatement and `dependency-graph.md` belong to
`M2-C12-05`, by this task's own *Documentation Updates* table.
