---
doc_id: TASK-M2-C05-02
title: Column preferences + persistence
module: execution
task_id: M2-C05-02
milestone: M2
parent_task: M2-C05
task_type: Frontend
priority: P1
status: Not Started
estimate: 3 d
depends_on: [M2-C05-01]
source_files:
  - V.SMART/V.SMART.Shared/Shared/BaseUserRightsComponent.cs
  - V.SMART/V.SMART.Shared/Components/ColumnMenu.razor
  - V.SMART/V.SMART.Shared/Services/ColumnPreferenceService.cs
  - V.SMART/V.SMART.Shared/Services/IColumnPreferenceService.cs
  - V.SMART/V.SMART.Shared/Services/ScreenMappingAttribute.cs
  - V.SMART/V.SMART.Shared/ViewModels/GridColumn.cs
  - V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/AdminViewmodel/ColumnPreferenceVM.cs
  - V.SMART/V.SMART.Shared/Data/Master/Admin_Module/UserColumnPreference.cs
  - V.SMART/V.SMART.Shared/Data/Master/MasterScreeenManagement_Module/UserPreference.cs
  - V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs
  - V.SMART/V.SMART.Shared/Repository/UnitOfWork.cs
  - V.SMART/V.SMART.Shared/ViewModels/MfgAndlabourViewModel/ManufacturingPoVM/MfgPOSubVM.cs
api_endpoints:
  - "GET /api/v1/me/column-preferences/{screenName}"
  - "PUT /api/v1/me/column-preferences/{screenName}"
business_rules: []
confidence: n/a
last_verified: 2026-08-12
---

# M2-C05-02 — Column preferences + persistence

| Field | Value |
|---|---|
| Task ID | M2-C05-02 |
| Milestone | M2 — Foundation |
| Parent | M2-C05 |
| Type | Frontend |
| Priority | P1 |
| Status | Not Started |
| Estimate | 3 d |
| Gate | G2 |

## Task Objective

Give `DataGrid` per-user, per-screen column preferences — visibility, order, width, pinning
and density — that survive a session, and persist them **through the existing database
contracts** so that a user's column layout is not lost when their screen moves from Blazor
to React, and so that a user working in both UIs during the strangler-fig period sees one
consistent layout.

This task delivers:

- A `ColumnManager` control in the grid toolbar (the `ColumnMenu.razor` replacement).
- A `useColumnPreferences` hook: load on mount, apply, debounce-save on change, reset to
  defaults.
- Consumption of the read/write endpoints for column preferences.
- A verified round-trip against the persisted JSON shapes that already exist in the
  database.

## Why This Task Exists

**Because column preferences are already persisted, and breaking that persistence would be a
silent, per-user data loss that nobody reports as a bug — they just quietly re-hide the same
twelve columns every morning.**

Two independent persistence mechanisms exist today, both **Confirmed** on 2026-08-12. Both
must keep working:

| # | Mechanism | Storage | JSON payload type | Consumers |
|---|---|---|---|---|
| 1 | `IColumnPreferenceService` / `ColumnPreferenceService` | `UserPreference` — `UserId`, `ScreenName`, `PreferenceJson` | `List<GridColumn>` | `ColumnMenu.razor`, injected by **95** `.razor` files |
| 2 | `BaseUserRightsComponent` | `UserColumnPreference` — `UserName`, `ScreenName`, `ColumnJson` | `List<ColumnPreferenceVM>` | pages deriving from `BaseUserRightsComponent` |

Evidence:

- `V.SMART/V.SMART.Shared/Services/IColumnPreferenceService.cs:5-9` —
  `LoadPreferencesAsync(string screenName, string userId)` and
  `SavePreferencesAsync(string screenName, string userId, List<GridColumn> settings)`.
- `V.SMART/V.SMART.Shared/Services/ColumnPreferenceService.cs` — serialises
  `List<GridColumn>` into `UserPreference.PreferenceJson`, keyed by `(ScreenName, UserId)`.
- `V.SMART/V.SMART.Shared/Components/ColumnMenu.razor:2` — `@inject IColumnPreferenceService PreferenceService`.
- `V.SMART/V.SMART.Shared/Shared/BaseUserRightsComponent.cs:46-87` (`LoadColumns`),
  `:88-112` (`GetColumnsByScreen`), `:113-147` (`SaveColumnPreferences`).
- `V.SMART/V.SMART.Shared/Data/Master/MasterScreeenManagement_Module/UserPreference.cs`
  (`Id`, `UserId`, `ScreenName`, `PreferenceJson`).
- `V.SMART/V.SMART.Shared/Data/Master/Admin_Module/UserColumnPreference.cs:10-30`
  (`Id`, `UserName`, `ScreenName`, `ColumnJson`, plus audit columns).
- `V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs:127` — `DbSet<UserPreference> UserPreferences`.
- `V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs:411` — `DbSet<UserColumnPreference> UserColumnPreference`.
- `V.SMART/V.SMART.Shared/Repository/UnitOfWork.cs:456` and `:775` — the
  `UserColumnPreferences` repository.

It also serves [KB-051](../../frontend-new/design-system.md#data-display), which lists
*"per-user column show/hide (persisted to `UserColumnPreference`)"* as a `DataGrid`
requirement, and [KB-050](../../frontend-new/react-architecture.md), which maps
`ColumnMenu.razor` to the DataGrid column manager
([KB-015 §Shared components](../../architecture/frontend-architecture-existing.md#shared-components-22-components)).

> **Correction to the received brief — record this.** The brief for this task stated that
> column preferences are built by `BaseUserRightsComponent` and persisted to
> `UserColumnPreference.ColumnJson`. That is **true but incomplete**: it is mechanism 2. The
> mechanism that `ColumnMenu.razor` actually uses — and `ColumnMenu` is referenced by 95
> files — is mechanism 1, `UserPreference.PreferenceJson` with `List<GridColumn>`. Preserving
> only `UserColumnPreference` would leave the majority of screens' preferences stranded.

## Prerequisites

- **M2-C05-01** — the `DataGrid` core, including the typed `columnVisibility` /
  `onColumnVisibilityChange` seam this task fills in.
- A server endpoint pair for reading and writing column preferences. If none exists, see
  *API Changes* — this task does **not** unilaterally add a controller; it raises the gap.

## Dependencies

| Dependency | Class | Note |
|---|---|---|
| M2-C05-01 | Hard | Provides the column model and the controlled visibility seam. |
| M2-C02 | Hard | Supplies the authenticated identity. Preferences are keyed by the **server's** notion of the user, never by a client-supplied username. |
| M2-A07 (`GET /api/v1/me`) | Hard | The canonical user identity the preference key is derived from. |
| M2-B03 | Information | Controller conventions, if the endpoint pair has to be specified. |
| M2-B10 | Soft | Generated client for the preference endpoints. |
| M2-B05 (typed `ScreenCodes`) | Soft | `screenName` is the preference key. A typed constant set removes a whole class of silent key mismatch. If M2-B05 has landed, use it. |
| M2-C05-03 | Deployment | Shares `DataGrid.tsx`'s toolbar slot; coordinate if both run in parallel. |
| M5-03 | Testing | Component tests for design-system primitives. |

## Required Existing Knowledge

| Doc | Why |
|---|---|
| [KB-015 §Shared components](../../architecture/frontend-architecture-existing.md#shared-components-22-components) | `ColumnMenu.razor` → DataGrid column manager mapping. |
| [KB-015 §State management (as-is)](../../architecture/frontend-architecture-existing.md#state-management-as-is) | Records grid columns as `BaseUserRightsComponent.Columns` + `UserColumnPreference`. |
| [KB-051 §Data display](../../frontend-new/design-system.md#data-display) | The `DataGrid` requirement list. |
| [KB-050 §Project structure](../../frontend-new/react-architecture.md#project-structure) | Where the hook and component live. |
| [ADR-003](../../decisions/ADR-003-react-stack.md) | Stack; generated client mandate. |
| [KB-013](../../architecture/auth-and-permissions.md) | How the server identifies the current user. |
| M2-C05-01's task file | The column model this task persists. |

## Investigation Requirements

**Update an existing investigation, plus one new narrow one.**

- **Update INV-004** (Complete — authentication, screen rights, approval authority; it
  already cites `Shared/BaseUserRightsComponent.cs`). Add the finding that **two** column
  preference mechanisms coexist, with the evidence listed under *Why This Task Exists*.
- **New (narrow):** determine which of the two mechanisms is authoritative for each of the
  ~150 list screens that will be migrated, or establish that they are per-screen mutually
  exclusive. Method: grep `.razor` files for `<ColumnMenu` versus `: BaseUserRightsComponent`
  and cross-tabulate. Record the counts and the overlap set with `file:line` evidence and a
  confidence rating. **A screen appearing in both lists is the interesting case** — resolve
  what wins today before deciding what the React grid writes.
- **Also confirm** how many types carry `[ScreenMapping]`. On 2026-08-12 exactly **two** do:
  `V.SMART/V.SMART.Shared/ViewModels/MfgAndlabourViewModel/ManufacturingPoVM/MfgPOSubVM.cs`
  and `V.SMART/V.SMART.Shared/ViewModels/OutSourcingViewModel/PurchPoVM/PurchPoSubVM.cs`
  (**Confirmed**). This matters: `BaseUserRightsComponent.GetColumnsByScreen`
  (`:88-112`) returns an **empty list** when no type carries a matching `[ScreenMapping]`,
  so mechanism 2's default-column derivation is effectively inert for all but two screens.
  Verify this before relying on it, and record the result — including if it has changed.
- **Record the negative result** for anything you grep for and do not find.

## Repository Areas

| Area | Role |
|---|---|
| `frontend/nexgen-web/src/shared/components/DataGrid/` | **TO BE CREATED (M2-C05-01).** Extended here. |
| `frontend/nexgen-web/src/shared/components/DataGrid/ColumnManager/` | **TO BE CREATED.** This task. |
| `V.SMART/V.SMART.Shared/Services/ColumnPreferenceService.cs` | Read-only reference — the persistence contract. |
| `V.SMART/V.SMART.Shared/Shared/BaseUserRightsComponent.cs` | Read-only reference — the second persistence contract. |
| `V.SMART/V.SMART.Shared/Components/ColumnMenu.razor` | Read-only reference — the UI being superseded. |
| `V.SMART/V.SMART.Shared/Data/**` | Read-only. **No schema change is authorised by this task.** |

## Existing Behavior to Preserve

Superseded UI, with evidence verified 2026-08-12:

- **`V.SMART/V.SMART.Shared/Components/ColumnMenu.razor`** — a Bootstrap dropdown of
  checkboxes, one per `GridColumn`, with a **maximum-visible-columns cap**: the checkbox is
  `disabled` when `!col.IsVisible && VisibleCount >= MaxVisible`
  (`ColumnMenu.razor:22-28`). Referenced by 95 files. **Confirmed.**
- **`ColumnPreferenceService.LoadPreferencesAsync` / `SavePreferencesAsync`** — read/write
  `UserPreference.PreferenceJson` as a serialised `List<GridColumn>`, upserting on
  `(ScreenName, UserId)`. **Confirmed.**
- **`BaseUserRightsComponent.LoadColumns`** (`:46-87`) — derives defaults from
  `GetColumnsByScreen(ScreenName)` then **overwrites the whole list** from
  `UserColumnPreference.ColumnJson` when a row exists for `(UserName, ScreenName)`; note it
  keys on `GetUsernameAsync()` here while `LoadRightsAsync` (`:29-40`) keys rights on
  `GetUserIdAsync()`. **Confirmed.**
- **`BaseUserRightsComponent.SaveColumnPreferences`** (`:113-147`) — serialises the full
  `List<ColumnPreferenceVM>` and upserts on `(UserName, ScreenName)`. **Confirmed.**
- **`BaseUserRightsComponent.GetColumnsByScreen`** (`:88-112`) — reflects over every loaded
  assembly for a type carrying `[ScreenMapping(screenName)]`
  (`V.SMART/V.SMART.Shared/Services/ScreenMappingAttribute.cs:10-19`) and projects its public
  properties into `ColumnPreferenceVM { Field = p.Name, Title = p.Name, IsVisible = true,
  IsDate = … }`. Returns `new()` when no type matches. **Confirmed.**

**What must be preserved is the persistence contract, not the reflection mechanism.**
Assembly-scanning reflection is a Blazor-server-side implementation detail with no React
equivalent and no reason to have one. What survives the migration is:

1. The **storage location and key**: `(ScreenName, UserId)` → `UserPreference.PreferenceJson`
   and `(ScreenName, UserName)` → `UserColumnPreference.ColumnJson`.
2. The **JSON payload shape**: an array of objects with `Title`, `Field`, `IsVisible`,
   `IsDate` (and, for `GridColumn`, additionally `Width`, `Align`, `IsDetailColumn`) —
   `V.SMART/V.SMART.Shared/ViewModels/GridColumn.cs:3-14`,
   `V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/AdminViewmodel/ColumnPreferenceVM.cs:9-18`.
3. The **screen-name key vocabulary**, so a preference saved from Blazor is found by React.

**Business rules:** none applies. Column layout is a user preference, not an ERP rule.

## Target Result

- A `ColumnManager` popover in the grid toolbar listing every `hideable` column with a
  checkbox, plus drag-to-reorder, a pin toggle (left/none/right), a density control and a
  **Reset to defaults** action.
- `useColumnPreferences(screenName, defaultColumns)` returning
  `{ columns, setVisibility, setOrder, setPinned, setWidth, reset, isLoading, isSaving }`.
- Load: on mount, one request; defaults applied while it is in flight, so the grid never
  flashes an empty column set.
- Save: debounced (750 ms) `PUT`, last-write-wins, with an inline "saved" affordance and an
  error toast that does **not** discard the user's local change.
- **Serialisation is contract-faithful.** The persisted JSON deserialises cleanly into
  `List<GridColumn>` (mechanism 1) — same property names, same casing behaviour as
  `System.Text.Json` defaults used by `ColumnPreferenceService`. Any additional React-only
  fields (`order`, `pinned`) are additive and must not break the C# deserialiser.
- **Round-trip proven both ways**: a preference row written by the Blazor `ColumnMenu` is
  read and applied correctly by the React grid, and a row written by the React grid is read
  and applied correctly by Blazor.
- **The max-visible-columns cap is a server/config concern, not a React invention.** If the
  investigation shows `MaxVisible` is a real per-screen constraint, surface it from the API;
  if it is a hardcoded UI nicety, drop it and record that decision. Do not silently
  reimplement a magic number in TypeScript.

## Implementation Steps

1. Read the KB documents listed above and search `docs/kb/investigation-registry.md`.
2. Read all five existing source files named under *Existing Behavior to Preserve* and
   re-verify every line number quoted. Record any that differ.
3. Run the cross-tabulation investigation described under *Investigation Requirements* and
   record its result before writing any code. The result decides which mechanism the React
   grid writes to — that is a decision, and it must be evidence-based.
4. Confirm the preference endpoint pair exists. If it does not, **stop, record the gap, and
   report Blocked with a proposed contract** (see *API Changes*). Do not add a controller
   under this task id.
5. Define the wire types in
   `frontend/nexgen-web/src/shared/components/DataGrid/ColumnManager/types.ts` from the
   generated client. If they are not yet generated, define them once in a clearly marked
   adapter with a `TODO(M2-B10)` comment.
6. Implement `useColumnPreferences.ts`:
   - TanStack Query `useQuery` keyed `['column-preferences', screenName]` with a long
     `staleTime` — preferences are reference data for the session.
   - Merge strategy: start from `defaultColumns`; apply the persisted array by `field`;
     **ignore unknown fields** and **append newly added columns at their default position
     with their default visibility**. A column added by a later release must not be
     invisible for every existing user, and a column removed from code must not crash the
     merge.
   - `useMutation` for the save, debounced 750 ms, cancelling in-flight saves on a newer
     change, invalidating the query key on success.
7. Implement `ColumnManager.tsx`: a Mantine popover from a toolbar button. Checkbox list,
   keyboard-reorderable (`Alt+↑`/`Alt+↓`) as well as drag-reorderable, pin control, density
   segmented control, and Reset.
8. Wire it into `DataGrid.tsx` through the `columnVisibility` /
   `onColumnVisibilityChange` seam left by M2-C05-01, plus the toolbar slot. Do not
   restructure the core.
9. Persist **density** alongside columns under the same screen key, since
   [KB-051](../../frontend-new/design-system.md#principles) makes density a per-user
   ergonomic choice.
10. Handle the unauthenticated / no-identity case: fall back to in-memory defaults, never
    write a preference row keyed by an empty user. Note that
    `BaseUserRightsComponent.LoadColumns:52-53` and `SaveColumnPreferences:117-118` both
    return early on a blank user name — mirror that defensiveness.
11. Write the tests listed under *Testing*, including the two round-trip fixtures built from
    JSON captured from a real `UserPreference.PreferenceJson` value.
12. Run the verification commands, review the full `git diff`, update docs and the registry.

## Files Expected to Change

- `frontend/nexgen-web/src/shared/components/DataGrid/DataGrid.tsx` — consume the hook,
  render the toolbar entry.
- `frontend/nexgen-web/src/shared/components/DataGrid/types.ts` — add `order`, `pinned`,
  `hideable` persistence metadata if not already present.
- `frontend/nexgen-web/src/shared/components/DataGrid/index.ts` — export the hook.
- `frontend/nexgen-web/src/shared/api/queryKeys.ts` — add the `columnPreferences` key.
- `docs/kb/architecture/frontend-architecture-existing.md` (KB-015) — correct the
  *State management* row to name **both** mechanisms.
- `docs/kb/frontend-new/design-system.md` (KB-051) — correct the `DataGrid` bullet, which
  currently names only `UserColumnPreference`.
- `docs/kb/investigation-registry.md` (KB-003) — INV-004 amendment + the new finding.
- `docs/kb/execution/task-tracker.md` (KB-081).

## Files Expected to Be Created

All **TO BE CREATED**, under
`frontend/nexgen-web/src/shared/components/DataGrid/ColumnManager/`:

- `ColumnManager.tsx`
- `ColumnManager.module.css`
- `useColumnPreferences.ts`
- `preferenceSerialisation.ts` — the single module that knows the persisted JSON shape
- `types.ts`
- `index.ts`
- `__tests__/useColumnPreferences.test.ts`
- `__tests__/ColumnManager.test.tsx`
- `__tests__/blazorRoundTrip.test.ts`
- `__tests__/fixtures/userPreference.blazor.json` — captured `PreferenceJson` payload
- `__tests__/fixtures/userColumnPreference.blazor.json` — captured `ColumnJson` payload

## Files That Must Not Change

- `V.SMART/**` — the whole .NET solution, including
  `ColumnPreferenceService.cs`, `BaseUserRightsComponent.cs` and `ColumnMenu.razor`. The
  Blazor app must keep working against the same rows.
- `V.SMART/V.SMART.Shared/Data/**` and `V.SMART/V.SMART.Shared/Migrations/**` — **no schema
  change.** If you believe one is needed, that is a finding to record, not a change to make.
- `frontend/vsmart-erp/**`.

## API Changes

**This task consumes, and does not create, HTTP surface.** The endpoints it requires:

```
GET /api/v1/me/column-preferences/{screenName}   → { screenName, columns: [...] }
PUT /api/v1/me/column-preferences/{screenName}   ← { columns: [...] }   → 204
```

Both are scoped to the **authenticated** user; `{screenName}` is the only route parameter.
The user identity is never accepted from the client — that would be an IDOR on another
user's preferences (an M5-08 concern).

If these endpoints do not exist when this task runs:

1. Record the gap in the investigation registry as a negative result.
2. Report **Blocked**, proposing the contract above and naming
   `IColumnPreferenceService` (`V.SMART/V.SMART.Shared/Services/IColumnPreferenceService.cs:5-9`)
   as the service to wrap — it already has exactly the two methods needed.
3. Do **not** implement the controller inside this task. One task per branch
   ([KB-080 §19](../README.md#19-git-strategy)).

## React Changes

| Aspect | Detail |
|---|---|
| **Route** | n/a — shared component behaviour. |
| **Component** | `ColumnManager` + `useColumnPreferences` under `frontend/nexgen-web/src/shared/components/DataGrid/ColumnManager/` (**TO BE CREATED**). Replaces `V.SMART/V.SMART.Shared/Components/ColumnMenu.razor`. |
| **API integration** | `GET`/`PUT /api/v1/me/column-preferences/{screenName}` through the generated client, wrapped in TanStack Query with a long `staleTime` and a debounced mutation. |
| **State** | Server state in TanStack Query. Working column state is local to the grid instance and reconciled with the server copy. No Zustand store. |
| **Validation** | Zod-validate the response shape before applying it, so a corrupt `PreferenceJson` row degrades to defaults instead of blanking the grid. This is UX resilience, not domain validation. |
| **Permissions** | n/a — a user's own preferences carry no screen right. Preference access must not be a way to discover screen names the user cannot see; if the API returns 403 for an unauthorised screen, render defaults silently. |
| **Loading state** | Defaults render immediately; the persisted layout applies when the request resolves. Never render a grid with zero columns while loading. |
| **Empty state** | No stored preference is the normal first-run case, not an empty state — apply defaults with no message. |
| **Error state** | Load failure → defaults + a single non-blocking inline notice. Save failure → error toast showing the server's `ProblemDetails` message verbatim, and the local change is **kept**, not reverted. |
| **Accessibility** | Popover is a focus trap with `Esc` to close and focus restored to the trigger. The checkbox list is a labelled group. Reorder is keyboard-operable via `Alt+↑`/`Alt+↓`, not drag-only. Column visibility changes announce via `aria-live="polite"`. |
| **Responsive behaviour** | Below 1024 px the manager opens as a full-height drawer rather than a popover, matching [KB-051 §Responsive behaviour](../../frontend-new/design-system.md#responsive-behaviour). Automatic column dropping at narrow widths is a **view-time** behaviour and must never be written back as a saved preference. |

## Business Logic

**Preserved unchanged — as a persistence contract, not as code.**

The behaviour preserved is *where* and *in what shape* a user's column layout is stored:
`UserPreference.PreferenceJson` (`List<GridColumn>`) and
`UserColumnPreference.ColumnJson` (`List<ColumnPreferenceVM>`). The C# services that own
those rows are not modified, not ported, and not duplicated.

**No ERP business rule is implemented client-side by this task.** Column visibility is
presentation preference. It is explicitly **not** a security control: hiding a column in the
grid does not withhold the field from the response, and this task must not create the
impression that it does. Field-level authorisation, if it is ever required, is a server
concern (ADR-004 / M2-A08), and if the investigation surfaces any screen relying on column
hiding for confidentiality, that is a finding to escalate immediately, not to solve here.

## Testing

| # | Test | Asserts |
|---|---|---|
| 1 | Defaults render while the preference query is in flight | No empty-column flash |
| 2 | A stored preference applies visibility, order, width and pinning on load | Core behaviour |
| 3 | Toggling a column debounces and issues exactly one `PUT` after 750 ms | Save contract |
| 4 | Rapid toggles cancel the in-flight save and send only the latest state | Debounce/cancel |
| 5 | A save failure shows the server's `ProblemDetails` message verbatim and **keeps** the local change | Error UX; message not genericised |
| 6 | A corrupt/unparseable stored payload falls back to defaults without throwing | Resilience |
| 7 | A column present in defaults but absent from the stored payload appears with its default visibility | Forward compatibility |
| 8 | A column present in the stored payload but absent from defaults is ignored, not rendered | Backward compatibility |
| 9 | **Blazor → React round-trip**: the captured `userPreference.blazor.json` fixture deserialises and applies correctly | Contract fidelity |
| 10 | **React → Blazor round-trip**: the payload this hook serialises contains `Title`, `Field`, `IsVisible`, `IsDate` (and `Width`, `Align`, `IsDetailColumn` for mechanism 1) with the same names and types `GridColumn` expects | Contract fidelity |
| 11 | The same round-trip against `userColumnPreference.blazor.json` / `ColumnPreferenceVM` | Mechanism 2 |
| 12 | Density persists and restores with the column set | KB-051 |
| 13 | No preference write occurs when there is no authenticated identity | Mirrors `BaseUserRightsComponent.cs:52-53`, `:117-118` |
| 14 | Responsive column dropping is never written back to the server | View-time vs stored state |
| 15 | axe: no critical violations with the manager open; `Esc` closes and restores focus | M5-09 |

Tests 9–11 are the ones that actually protect users. Build their fixtures from **real**
captured JSON, not from JSON you wrote to match your own serialiser.

## Acceptance Criteria

- [ ] `ColumnManager` replaces `ColumnMenu.razor`'s function: per-column visibility toggles
      in a labelled, keyboard-operable list, plus reorder, pin, density and reset.
- [ ] `useColumnPreferences(screenName, defaultColumns)` loads on mount and saves debounced
      at 750 ms, with in-flight cancellation.
- [ ] The persisted payload deserialises into `List<GridColumn>` as defined at
      `V.SMART/V.SMART.Shared/ViewModels/GridColumn.cs:3-14` — verified by fixture test.
- [ ] The persisted payload for mechanism 2 deserialises into `List<ColumnPreferenceVM>` as
      defined at `…/ColumnPreferenceVM.cs:9-18` — verified by fixture test.
- [ ] A row written by the Blazor UI is applied correctly by the React grid (fixture test 9).
- [ ] A row written by the React grid satisfies the C# shape (fixture test 10/11).
- [ ] Unknown persisted columns are ignored; new default columns appear at defaults.
- [ ] A corrupt stored payload degrades to defaults with no thrown error.
- [ ] No preference is written when there is no authenticated identity.
- [ ] Responsive column dropping is never persisted.
- [ ] No database schema change: `git diff --stat -- V.SMART/` produces **no output**.
- [ ] The `[ScreenMapping]` finding (currently 2 types) is re-verified and recorded, whatever
      the result.
- [ ] The two-mechanism finding is recorded in the investigation registry and KB-015 and
      KB-051 are corrected.
- [ ] `npm run lint`, `npm run test`, `npm run build` all pass.
- [ ] axe reports no critical violations with the manager open.

## Verification Commands

Frontend commands as established by **M2-C01**; run from `frontend/nexgen-web/`.

```bash
npm ci
```

```bash
npm run lint
```

```bash
npm run test
```

```bash
npm run build
```

Repository-level (verified per [KB-083](../prompt-template.md#verified-repository-commands)):

```bash
git status --porcelain
```

```bash
git diff --stat -- V.SMART/
```

Must produce no output — this task changes no .NET code and no schema.

## Documentation Updates

| Document | Change |
|---|---|
| `docs/kb/architecture/frontend-architecture-existing.md` (KB-015) | *State management (as-is)* currently records grid columns as `BaseUserRightsComponent.Columns` + `UserColumnPreference` only. Correct it to record **both** mechanisms, with `file:line` evidence and the 95-file `ColumnMenu` usage count. Bump `last_verified`. |
| `docs/kb/frontend-new/design-system.md` (KB-051) | *Data display* says preferences are *"persisted to `UserColumnPreference`"*. Correct to name both stores. Bump `last_verified`. |
| `docs/kb/execution/task-tracker.md` (KB-081) | M2-C05-02 status. |
| `docs/kb/investigation-registry.md` (KB-003) | INV-004 amendment + the new finding rows. |

Preserve full frontmatter on every edited document.

## Investigation Registry Updates

Amend **INV-004** and add:

```yaml
Finding:        Two independent column-preference mechanisms coexist. (1)
                IColumnPreferenceService/ColumnPreferenceService persists
                List<GridColumn> to UserPreference.PreferenceJson keyed by
                (ScreenName, UserId); it is what ColumnMenu.razor injects, and
                ColumnMenu is referenced by 95 .razor files. (2)
                BaseUserRightsComponent persists List<ColumnPreferenceVM> to
                UserColumnPreference.ColumnJson keyed by (UserName, ScreenName).
Evidence:       V.SMART/V.SMART.Shared/Services/IColumnPreferenceService.cs:5-9;
                V.SMART/V.SMART.Shared/Services/ColumnPreferenceService.cs;
                V.SMART/V.SMART.Shared/Components/ColumnMenu.razor:2;
                V.SMART/V.SMART.Shared/Shared/BaseUserRightsComponent.cs:46-147;
                V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs:127 and :411
Business rule:  n/a
Confidence:     Confirmed
Last verified:  2026-08-12
```

```yaml
Finding:        BaseUserRightsComponent.GetColumnsByScreen derives default columns by
                reflecting for a type carrying [ScreenMapping(screenName)]. Exactly two
                types carry that attribute, so the default-column derivation returns an
                empty list for every other screen.
Evidence:       V.SMART/V.SMART.Shared/Shared/BaseUserRightsComponent.cs:88-112;
                V.SMART/V.SMART.Shared/Services/ScreenMappingAttribute.cs:10-19;
                V.SMART/V.SMART.Shared/ViewModels/MfgAndlabourViewModel/ManufacturingPoVM/MfgPOSubVM.cs;
                V.SMART/V.SMART.Shared/ViewModels/OutSourcingViewModel/PurchPoVM/PurchPoSubVM.cs
Business rule:  n/a
Confidence:     Confirmed
Last verified:  2026-08-12
```

Plus the cross-tabulation result (which screens use which mechanism, and the overlap), and a
negative result if the preference endpoints do not yet exist.

## Git Strategy

| Field | Value |
|---|---|
| **Branch** | `migration/M2-C05-02-column-preferences` |
| **Commit** | `M2-C05-02: Persist DataGrid column preferences against existing contracts` |
| **Expected changed files** | `frontend/nexgen-web/src/shared/components/DataGrid/ColumnManager/**` (new), `frontend/nexgen-web/src/shared/components/DataGrid/{DataGrid.tsx,types.ts,index.ts}`, `frontend/nexgen-web/src/shared/api/queryKeys.ts`, `docs/kb/architecture/frontend-architecture-existing.md`, `docs/kb/frontend-new/design-system.md`, `docs/kb/investigation-registry.md`, `docs/kb/execution/task-tracker.md` |
| **Rollback approach** | Revert the branch's merge commit. `DataGrid` returns to uncontrolled default columns; **no data is lost**, because this task only reads and writes rows the Blazor app already owns and never migrates, transforms or deletes them. That read-only-plus-upsert posture is deliberately what makes the rollback free. |

Never merge or push to `master` from an execution session.

## Fresh-Session Execution Prompt

Copy everything in the block below into a new AI session.

```text
============================================================
ERP MIGRATION — TASK EXECUTION PROMPT
============================================================

TASK ID:
M2-C05-02

TASK NAME:
Column preferences + persistence (DataGrid)

ROLE
You are an engineer working on the V.SMART / NexGen ERP
modernization project. You are executing exactly one task.

PROJECT OBJECTIVE
We are replacing the existing Blazor Server frontend with a new
React frontend while preserving the existing ERP business
behaviour, business services, database behaviour and business
rules wherever possible. The backend is extended, never
rewritten. Business logic currently trapped in Razor @code is
extracted into server-side services before any React screen
replaces it.

CURRENT ARCHITECTURE
Repository root: C:\Kumar\NexGen-ERP---2025-master\NexGen-ERP---2025-master
(this is the git repository root; the parent directory is not
part of the project)

  V.SMART/V.SMART.Shared   .NET 9 class library — ALL domain code:
                           196 EF entity sets, 285 business services,
                           ~190 repositories + UnitOfWork, 274 ViewModels,
                           333 Razor pages, 440 routes
  V.SMART/V.SMART.Web      Blazor Server host (live UI, stays running)
  V.SMART/V.SMART.Api      ASP.NET Core Web API (.NET 9) — the React
                           backend; currently 2 controllers / 6 endpoints
  V.SMART/V.SMART          .NET MAUI Blazor Hybrid host

SQL Server + EF Core 9, code-first, database-per-tenant.

Task-specific architecture context:
The React application DOES NOT EXIST YET. It is created by task
M2-C01 at frontend/nexgen-web/. Every frontend path in this
prompt is TO BE CREATED. The DataGrid core it extends is created
by M2-C05-01. The stack is fixed by ADR-003: React 19,
TypeScript strict, Vite 6, React Router v7, TanStack Query v5,
Zustand, React Hook Form, Zod, Mantine 7, CSS Modules + design
tokens, TanStack Table v8 + TanStack Virtual, Recharts, Axios
with a GENERATED OpenAPI client, decimal.js, react-i18next,
Vitest/RTL/MSW/Playwright.

Both the Blazor app and the React app run against the SAME
database throughout the migration. Column preferences are stored
per user per screen and are therefore SHARED STATE between the
two UIs.

SOURCE OF TRUTH
The project knowledge base is at docs/kb/. Read it before
investigating the repository. Use docs/kb/INDEX.md for
question-to-document routing.

Required reading for this task:
  ADR-003  docs/kb/decisions/ADR-003-react-stack.md
  KB-050   docs/kb/frontend-new/react-architecture.md
  KB-051   docs/kb/frontend-new/design-system.md
  KB-015   docs/kb/architecture/frontend-architecture-existing.md
  KB-013   docs/kb/architecture/auth-and-permissions.md
  KB-083   docs/kb/execution/prompt-template.md
  KB-002   docs/kb/source-of-truth-rules.md
  TASK     docs/kb/execution/tasks/M2-C05-02.md  (the full task spec)
  TASK     docs/kb/execution/tasks/M2-C05-01.md  (the DataGrid core you extend)

Authority order when sources conflict:
  1. Current source code
  2. Database schema / EF migrations (for storage)
  3. The knowledge base (for interpretation)
  4. Older prose documentation — hypothesis only
docs/ARCHITECTURE.md is superseded and contains known factual
errors. Do not rely on it.

INVESTIGATION REGISTRY
Before any repository investigation, search
docs/kb/investigation-registry.md.

Relevant investigations:
  INV-004  Authentication, screen rights, approval authority — Complete
           (already cites Shared/BaseUserRightsComponent.cs; you will AMEND it)
  INV-006  Existing UI: routing, layout, components — Complete

Before investigating the repository, search
docs/kb/investigation-registry.md and the relevant knowledge-base
documents via docs/kb/INDEX.md. If an investigation is Complete
and not stale, reuse its findings and cite the doc_id — do not
re-derive them. If it is Partial, investigate only the documented
gap. If it is absent or contradicted by current code,
investigate, then record the finding, its file:line evidence,
and its confidence in the knowledge base so that future sessions
do not repeat this work. Record negative results too — "grepped
for X, found none" is a finding.

CURRENT TASK
Give the React DataGrid per-user, per-screen column preferences
(visibility, order, width, pinning, density) that survive a
session, persisted THROUGH THE EXISTING DATABASE CONTRACTS so
that a user's layout is not lost when a screen moves from Blazor
to React, and so that a user working in both UIs sees one
consistent layout.

Deliver:
  - ColumnManager: the toolbar popover replacing ColumnMenu.razor
  - useColumnPreferences(screenName, defaultColumns): load,
    apply, debounced save, reset
  - Consumption of the read/write preference endpoints
  - Round-trip tests proving contract fidelity in BOTH directions

WHY THIS TASK EXISTS
Column preferences are ALREADY persisted today. Breaking that
persistence is a silent per-user data loss that nobody reports —
users simply re-hide the same columns every morning. KB-051 lists
per-user column show/hide as a DataGrid requirement, and KB-015
maps ColumnMenu.razor to the DataGrid column manager.

CURRENT IMPLEMENTATION
Verified 2026-08-12. Classification per KB-002.

TWO independent persistence mechanisms coexist. BOTH must keep
working. (The commonly repeated statement that preferences live
only in UserColumnPreference.ColumnJson is TRUE BUT INCOMPLETE —
it describes mechanism 2 only, while mechanism 1 is the one used
by 95 files.)

  Mechanism 1 — Confirmed
    IColumnPreferenceService:
      Task<List<GridColumn>> LoadPreferencesAsync(string screenName, string userId)
      Task SavePreferencesAsync(string screenName, string userId, List<GridColumn> settings)
      V.SMART/V.SMART.Shared/Services/IColumnPreferenceService.cs:5-9
    ColumnPreferenceService serialises List<GridColumn> into
      UserPreference.PreferenceJson, upserting on (ScreenName, UserId):
      V.SMART/V.SMART.Shared/Services/ColumnPreferenceService.cs
    Entity UserPreference { Id, UserId, ScreenName, PreferenceJson }:
      V.SMART/V.SMART.Shared/Data/Master/MasterScreeenManagement_Module/UserPreference.cs
    DbSet: V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs:127
    UI: V.SMART/V.SMART.Shared/Components/ColumnMenu.razor:2 injects
      IColumnPreferenceService. ColumnMenu is referenced by 95 .razor files.
      It caps visible columns: the checkbox is disabled when
      (!col.IsVisible && VisibleCount >= MaxVisible) — ColumnMenu.razor:22-28.
    Payload type GridColumn { Title, Field, IsVisible, IsDate, Width,
      Align, IsDetailColumn }:
      V.SMART/V.SMART.Shared/ViewModels/GridColumn.cs:3-14

  Mechanism 2 — Confirmed
    BaseUserRightsComponent.LoadColumns():46-87 derives defaults from
      GetColumnsByScreen(ScreenName) then OVERWRITES the whole list from
      UserColumnPreference.ColumnJson when a row exists for
      (UserName, ScreenName). Note it keys columns on GetUsernameAsync()
      while LoadRightsAsync():29-40 keys rights on GetUserIdAsync().
    BaseUserRightsComponent.SaveColumnPreferences():113-147 serialises
      List<ColumnPreferenceVM> and upserts on (UserName, ScreenName).
    BaseUserRightsComponent.GetColumnsByScreen():88-112 reflects over all
      loaded assemblies for a type carrying [ScreenMapping(screenName)]
      (V.SMART/V.SMART.Shared/Services/ScreenMappingAttribute.cs:10-19)
      and projects its public properties into ColumnPreferenceVM.
      It returns an EMPTY list when no type matches — and exactly TWO
      types carry [ScreenMapping] today:
        V.SMART/V.SMART.Shared/ViewModels/MfgAndlabourViewModel/ManufacturingPoVM/MfgPOSubVM.cs
        V.SMART/V.SMART.Shared/ViewModels/OutSourcingViewModel/PurchPoVM/PurchPoSubVM.cs
      Re-verify this count yourself; record whatever you find.
    Entity UserColumnPreference { Id, UserName, ScreenName, ColumnJson,
      CreatedBy, CreatedDate, ModifiedBy, ModifiedDate }:
      V.SMART/V.SMART.Shared/Data/Master/Admin_Module/UserColumnPreference.cs:10-30
    DbSet: V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs:411
    Repository: V.SMART/V.SMART.Shared/Repository/UnitOfWork.cs:456 and :775
    Payload type ColumnPreferenceVM { Title, Field, IsVisible, IsDate }:
      V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/AdminViewmodel/ColumnPreferenceVM.cs:9-18

  Confirmed: there is NO React code in this repository today.

RE-VERIFY EVERY LINE NUMBER ABOVE against the current source
before relying on it, and report any that differ.

TARGET IMPLEMENTATION
  - A ColumnManager popover in the grid toolbar: checkbox per
    hideable column, drag-to-reorder AND keyboard reorder
    (Alt+Up / Alt+Down), pin toggle (left/none/right), density
    control, and Reset to defaults.
  - useColumnPreferences(screenName, defaultColumns) returning
    { columns, setVisibility, setOrder, setPinned, setWidth,
      reset, isLoading, isSaving }.
  - Load once on mount with a long staleTime; defaults render
    immediately so the grid NEVER flashes an empty column set.
  - Save debounced at 750 ms, cancelling in-flight saves,
    last-write-wins.
  - Merge strategy: start from defaultColumns; apply the stored
    array by `field`; IGNORE stored columns that no longer exist;
    APPEND new default columns at their default position and
    visibility. A column added in a later release must not be
    invisible for every existing user, and a removed column must
    not crash the merge.
  - Serialisation fidelity: the persisted JSON must deserialise
    cleanly into List<GridColumn> (mechanism 1) and
    List<ColumnPreferenceVM> (mechanism 2) using System.Text.Json
    defaults. React-only additions (order, pinned) must be purely
    additive and must not break the C# deserialiser.
  - Round-trip proven BOTH ways with fixtures built from REAL
    captured JSON, not JSON you wrote to match your own
    serialiser.
  - Density persists under the same screen key.
  - The MaxVisible column cap: decide from evidence whether it is
    a real per-screen constraint (then surface it from the API)
    or a hardcoded UI nicety (then drop it and record the
    decision). Do not reimplement a magic number in TypeScript.

RELEVANT FILES
Read-only reference (exist today; DO NOT MODIFY):
  V.SMART/V.SMART.Shared/Services/IColumnPreferenceService.cs
  V.SMART/V.SMART.Shared/Services/ColumnPreferenceService.cs
  V.SMART/V.SMART.Shared/Components/ColumnMenu.razor
  V.SMART/V.SMART.Shared/Shared/BaseUserRightsComponent.cs
  V.SMART/V.SMART.Shared/Services/ScreenMappingAttribute.cs
  V.SMART/V.SMART.Shared/ViewModels/GridColumn.cs
  V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/AdminViewmodel/ColumnPreferenceVM.cs
  V.SMART/V.SMART.Shared/Data/Master/Admin_Module/UserColumnPreference.cs
  V.SMART/V.SMART.Shared/Data/Master/MasterScreeenManagement_Module/UserPreference.cs
  V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs
  V.SMART/V.SMART.Shared/Repository/UnitOfWork.cs

To be created, under
frontend/nexgen-web/src/shared/components/DataGrid/ColumnManager/:
  ColumnManager.tsx
  ColumnManager.module.css
  useColumnPreferences.ts
  preferenceSerialisation.ts
  types.ts
  index.ts
  __tests__/useColumnPreferences.test.ts
  __tests__/ColumnManager.test.tsx
  __tests__/blazorRoundTrip.test.ts
  __tests__/fixtures/userPreference.blazor.json
  __tests__/fixtures/userColumnPreference.blazor.json

To be modified:
  frontend/nexgen-web/src/shared/components/DataGrid/DataGrid.tsx
  frontend/nexgen-web/src/shared/components/DataGrid/types.ts
  frontend/nexgen-web/src/shared/components/DataGrid/index.ts
  frontend/nexgen-web/src/shared/api/queryKeys.ts

REQUIRED INVESTIGATION
Before writing code:
  a) Cross-tabulate which .razor list screens use mechanism 1
     (grep for "<ColumnMenu") versus mechanism 2 (grep for
     ": BaseUserRightsComponent"). Record the counts and, most
     importantly, the OVERLAP set. A screen in both lists is the
     interesting case — establish what wins today before deciding
     what React writes.
  b) Re-verify how many types carry [ScreenMapping].
  c) Confirm whether the preference endpoints exist.
Record all three results, including negative ones, in
docs/kb/investigation-registry.md with file:line evidence and a
Confirmed/Inferred/Unknown rating.

API SURFACE CONSUMED (NOT created by this task)
  GET /api/v1/me/column-preferences/{screenName}
      -> { screenName, columns: [...] }
  PUT /api/v1/me/column-preferences/{screenName}
      <- { columns: [...] }  -> 204

Both are scoped to the AUTHENTICATED user. The user identity is
never accepted from the client — that would be an IDOR on another
user's preferences.

If these endpoints do not exist when you run:
  1. Record the gap as a negative result in the registry.
  2. Report Blocked, proposing the contract above and naming
     IColumnPreferenceService
     (V.SMART/V.SMART.Shared/Services/IColumnPreferenceService.cs:5-9)
     as the service to wrap — it already has exactly the two
     methods needed.
  3. DO NOT implement the controller under this task id.

BUSINESS RULES TO PRESERVE
None — this task does not touch business behaviour.

What IS preserved is a persistence contract: where and in what
JSON shape a user's column layout is stored. The C# services that
own those rows are not modified, not ported and not duplicated.

Column visibility is presentation preference and is explicitly
NOT a security control: hiding a column does not withhold the
field from the response, and nothing you build may imply that it
does. If your investigation finds any screen relying on column
hiding for confidentiality, that is a finding to escalate
immediately (ADR-004 / M2-A08), not something to solve here.

CONSTRAINTS
- Do not rewrite existing business services.
- Do not reimplement ERP business logic in React/TypeScript.
- Do not change unrelated modules.
- Do not make assumptions without checking source code.
- Do not repeat completed investigations.
- Do not modify the database schema unless this task explicitly
  authorizes it. IT DOES NOT. If you believe a schema change is
  needed, record it as a finding and report Blocked.
- Preserve existing API behaviour wherever practical.
- The server remains authoritative for validation, calculations,
  permissions and document numbering.
- Do not start another task after completing this one.

Task-specific constraints:
- Do not modify anything under V.SMART/.
  `git diff --stat -- V.SMART/` must produce no output.
- Do not migrate, transform, backfill or delete any existing
  preference row. Read and upsert only.
- Do not restructure the DataGrid core from M2-C05-01; fill in
  the seams it left (columnVisibility / onColumnVisibilityChange,
  toolbar slot).
- Do not write a preference row keyed by an empty or missing
  user — mirror the early returns at
  BaseUserRightsComponent.cs:52-53 and :117-118.
- Do not persist responsive (view-time) column dropping.
- Do not add a second UI component library.

EXECUTION PROCEDURE
1. Read the referenced knowledge-base documents.
2. Search the investigation registry.
3. Confirm whether the required investigation already exists.
4. Inspect the actual source code.
5. Verify every assumption against code before acting on it.
6. Implement only this task.
7. Run the required verification commands and tests.
8. Review the full git diff.
9. Update documentation.
10. Update the investigation registry if required.

EXPECTED FILE CHANGES
Modified:
  frontend/nexgen-web/src/shared/components/DataGrid/DataGrid.tsx
  frontend/nexgen-web/src/shared/components/DataGrid/types.ts
  frontend/nexgen-web/src/shared/components/DataGrid/index.ts
  frontend/nexgen-web/src/shared/api/queryKeys.ts
  docs/kb/architecture/frontend-architecture-existing.md
  docs/kb/frontend-new/design-system.md
  docs/kb/investigation-registry.md
  docs/kb/execution/task-tracker.md

Created:
  frontend/nexgen-web/src/shared/components/DataGrid/ColumnManager/**

Must not change:
  V.SMART/**  (especially ColumnPreferenceService.cs,
               BaseUserRightsComponent.cs, ColumnMenu.razor,
               Data/** and Migrations/**)
  frontend/vsmart-erp/**
  db/**

TESTS
Vitest + React Testing Library + MSW, via `npm run test` from
frontend/nexgen-web/. Write all of these:

 1. Defaults render while the preference query is in flight (no
    empty-column flash).
 2. A stored preference applies visibility, order, width and
    pinning on load.
 3. Toggling a column debounces and issues exactly one PUT after
    750 ms.
 4. Rapid toggles cancel the in-flight save; only the latest
    state is sent.
 5. A save failure shows the server's ProblemDetails message
    VERBATIM and KEEPS the local change.
 6. A corrupt/unparseable stored payload falls back to defaults
    without throwing.
 7. A column in defaults but absent from the stored payload
    appears with its default visibility.
 8. A column in the stored payload but absent from defaults is
    ignored, not rendered.
 9. Blazor -> React round-trip using the captured
    userPreference.blazor.json fixture.
10. React -> Blazor round-trip: the serialised payload carries
    Title, Field, IsVisible, IsDate (plus Width, Align,
    IsDetailColumn for mechanism 1) with the names and types
    GridColumn expects.
11. The same round-trip against userColumnPreference.blazor.json
    / ColumnPreferenceVM.
12. Density persists and restores with the column set.
13. No preference write occurs without an authenticated identity.
14. Responsive column dropping is never written back.
15. axe: no critical violations with the manager open; Esc closes
    it and restores focus to the trigger.

Tests 9-11 are the ones that actually protect users. Build their
fixtures from REAL captured JSON.

ACCEPTANCE CRITERIA
- ColumnManager replaces ColumnMenu.razor's function: per-column
  visibility in a labelled keyboard-operable list, plus reorder,
  pin, density and reset.
- useColumnPreferences loads on mount and saves debounced at
  750 ms with in-flight cancellation.
- The persisted payload deserialises into List<GridColumn> per
  V.SMART/V.SMART.Shared/ViewModels/GridColumn.cs:3-14 — proven
  by fixture test.
- The mechanism-2 payload deserialises into
  List<ColumnPreferenceVM> per ColumnPreferenceVM.cs:9-18 —
  proven by fixture test.
- A row written by Blazor is applied correctly by React (test 9).
- A row written by React satisfies the C# shapes (tests 10, 11).
- Unknown stored columns are ignored; new default columns appear
  at their defaults.
- A corrupt payload degrades to defaults without throwing.
- No preference is written without an authenticated identity.
- Responsive column dropping is never persisted.
- git diff --stat -- V.SMART/ produces no output (no schema
  change, no C# change).
- The [ScreenMapping] count is re-verified and recorded.
- The two-mechanism finding is recorded in the registry, and
  KB-015 and KB-051 are corrected.
- npm run lint, npm run test and npm run build all pass.
- axe reports no critical violations with the manager open.

VERIFICATION COMMANDS
The frontend commands below are the ones established by task
M2-C01; they are cited as established there, not as
independently verified. Run each from frontend/nexgen-web/.

  npm ci
  npm run lint
  npm run test
  npm run build

Repository-level commands (verified for this repository per
KB-083):

  git status --porcelain
  git diff --stat -- V.SMART/     # must produce no output

DOCUMENTATION REQUIREMENTS
Update, keeping full frontmatter (doc_id, title, module,
source_files, entities, api_endpoints, database_tables,
business_rules, status, confidence, last_verified, dependencies)
and bumping last_verified:

  docs/kb/architecture/frontend-architecture-existing.md (KB-015)
    — its "State management (as-is)" table records grid columns
      as BaseUserRightsComponent.Columns + UserColumnPreference
      only. Correct it to record BOTH mechanisms, with file:line
      evidence and the 95-file ColumnMenu usage count.
  docs/kb/frontend-new/design-system.md (KB-051)
    — its DataGrid bullet says preferences are "persisted to
      UserColumnPreference". Correct it to name both stores.
  docs/kb/execution/task-tracker.md (KB-081)
    — set M2-C05-02 to Needs Review.

Amend INV-004 in docs/kb/investigation-registry.md and add the
two-mechanism finding, the [ScreenMapping] count finding, the
cross-tabulation result, and a negative result if the preference
endpoints do not yet exist — all in the KB-083 evidence format
(Finding / Evidence / Business rule / Confidence / Last verified).

GIT
Branch:  migration/M2-C05-02-column-preferences
Commit:  M2-C05-02: Persist DataGrid column preferences against existing contracts
Do not merge. Do not push to master. Leave the branch for review.

FINAL RESPONSE
When finished, report:
1.  Task ID
2.  Task status (Completed / Needs Review / Blocked)
3.  What was implemented
4.  Files created
5.  Files modified
6.  Files deleted
7.  Tests executed
8.  Test results
9.  Documentation updated
10. Investigation registry updated
11. Architectural decisions taken
12. Unexpected findings
13. Assumptions made
14. Deviations from this task
15. Recommended next task

IMPORTANT:
EXECUTE ONLY THIS TASK.
DO NOT START THE NEXT TASK.
============================================================
```
