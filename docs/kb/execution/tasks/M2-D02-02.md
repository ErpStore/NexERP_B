---
doc_id: TASK-M2-D02-02
title: CustomersController + API tests
module: execution
task_id: M2-D02-02
milestone: M2
parent_task: M2-D02
task_type: Backend
priority: P0
status: Not Started
estimate: 3 d
depends_on: [M2-D02-01]
source_files:
  - V.SMART/V.SMART.Api/Controllers/CurrencyController.cs
  - V.SMART/V.SMART.Api/Program.cs
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/IBusinessService/IMasterServices/IGeneralService/ICustomerService.cs
  - V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/MasterService/GeneralService/CustomerService.cs
  - V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/GeneralViewModel/CustomerVM.cs
  - V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs
entities: [Customer, CustomerIndirect, ContactPerson]
api_endpoints:
  - "GET /api/v1/customers"
  - "GET /api/v1/customers/{id}"
  - "POST /api/v1/customers"
  - "PUT /api/v1/customers/{id}"
  - "DELETE /api/v1/customers/{id}"
database_tables: [Customer, CustomerIndirect, ContactPerson]
business_rules: []
confidence: n/a
last_verified: 2026-08-12
---

# M2-D02-02 — `CustomersController` + API tests

| Field | Value |
|---|---|
| Task ID | M2-D02-02 |
| Milestone | M2 — Foundation |
| Parent | M2-D02 |
| Type | Backend |
| Priority | P0 |
| Status | Not Started |
| Estimate | 3 d |
| Gate | G2 |

> **`business_rules` is empty in the frontmatter because the `BR-CUST-*` ids are allocated
> by M2-D02-01, not by this task.** When this task runs, read the ids actually allocated
> from `docs/kb/business-rules/customer-master-rules.md` and list them here. This task
> **preserves** those rules; it does not create or modify any.

## Task Objective

Expose Customer Master over HTTP as `CustomersController`, following the controller
template codified in M2-B03 and proved by `CurrencyController`, wrapping the
`ICustomerService` methods that M2-D02-01 extracted — **with zero further service
changes** — and prove it with API integration tests plus a full permission-matrix row.

Five endpoints under `/api/v1/customers`: paged list, get-by-id, create, update, delete.
Every action annotated with `[RequireScreen("Customer")]` + `[RequireRight(...)]`. Every
error shaped as `ProblemDetails`. Every service message surfaced verbatim.

## Why This Task Exists

**This is the template's second independent user, and that is what makes the template
real.** [KB-080 §9's Definition of Done](../README.md#definition-of-done) for M2 says so
explicitly: G2 is not met until "the controller template [is] demonstrably followed by both
existing controllers — the template is only real once it has two independent users."
`CurrencyController` alone proves nothing; one controller is an example, two is a pattern.

It is also the **payoff test for M2-D02-01**. If the extraction produced the right service
shape, this controller is thin and mechanical: bind → authorize → one service call → map.
If this controller finds itself validating, orchestrating, or reaching for `IUnitOfWork`,
**the extraction was incomplete and the correct response is to send it back to M2-D02-01,
not to compensate here.** ADR-002 §2 is unambiguous: "Controllers are thin: bind →
authorize → one service call → map. No business logic."

Concretely it serves:

- **Gate G2** — Customer Master reachable through the API with permission-gated CRUD,
  server paging, validation and the error contract.
- **B1** from [KB-041](../../api/api-readiness-assessment.md) — the ~60–80 resource
  controllers. This is the second of them, and the one that sets the cost estimate for the
  other 58–78.
- **A1 / R-03** — every endpoint authorized server-side, with its permission-matrix row.
  ADR-004's closing line is the only rule in the knowledge base with no exceptions: "No
  controller ships without `[RequireScreen]` + `[RequireRight]` and its permission-matrix
  test."
- **A5 / R-24** — one `ProblemDetails` error shape, no repeat of `CurrencyController`'s two
  different 400 bodies.

**Customer is harder than Currency in exactly one interesting way, and that is why it is
the second controller:** it has child collections. A `CustomerVM` carries
`CustomerIndirectVMs` and `ContactPersonVMs` (`CustomerVM.cs:134-135`). Currency had
nothing of the kind. If the template cannot carry a parent-with-children through
`POST`/`PUT` cleanly, then it cannot carry a single ERP document either — every sales
order, purchase order and invoice in M3 and M4 is a parent with lines. **Finding that out
on a two-child master is enormously cheaper than finding it out on Sales Order.**

## Prerequisites

- **M2-D02-01** Completed. Specifically: `ICustomerService` must expose a single-call save
  and an independently callable validation method, and `CustomerMapping` must round-trip
  the child collections. Without those, this controller cannot be thin, and the task is
  **Blocked**.
- **M2-B07** Completed — `AddVSmartDomain()`. As of 2026-08-12,
  `V.SMART/V.SMART.Api/Program.cs:102` registers `ICurrencyService` and **nothing else**
  (Confirmed). `ICustomerService` is not registered in the API host, so
  `CustomersController` will fail at runtime on DI resolution the moment it is requested.
  KB-080 §9 names this the hard prerequisite for *every* controller. **This task must not
  add a one-off `AddScoped` line to work around it.**
- **M2-A01-03**, **M2-A02** — the `[RequireScreen]`/`[RequireRight]` filter and its
  per-request rights resolution must exist and be proven on `CurrencyController`.
- **M2-A06** — `ProblemDetails` middleware.
- **M2-B01** — `/api/v1` versioning.
- **M2-B02**, **M2-B03** — paging/sort/filter contract and the codified template. This task
  **follows** the template; it does not invent one.
- **M2-A03** — the permission-matrix harness. This task adds a row to it.
- **M0-12-01** — the test project. API tests need somewhere to live.

## Dependencies

| Dependency | Class | Note |
|---|---|---|
| M2-D02-01 — extraction | Hard | Supplies every service method this controller calls. If the controller cannot stay thin, send it back. |
| M2-B07 — `AddVSmartDomain()` | Hard | DI precondition. `Program.cs:102` registers only `ICurrencyService` today. |
| M2-A01-03 / M2-A02 — authorization filter | Hard | `[RequireScreen]`/`[RequireRight]`. Non-negotiable per ADR-004. |
| M2-A06 — `ProblemDetails` | Hard | The one error shape. |
| M2-B01 — `/api/v1` | Hard | Route prefix. |
| M2-B02 — paging contract | Hard | Query-parameter names and the paged envelope. |
| M2-B03 — codified controller template | Hard | The document this controller must demonstrably follow. |
| M2-A03 — permission-matrix harness | Hard | This task adds Customer's row. No controller merges without one. |
| M0-12-01 — test project | Hard | `tests/V.SMART.Shared.Tests/` (TO BE CREATED). An API integration project may need adding alongside it — see *Files Expected to Be Created*. |
| M2-B10 — TypeScript client generation | Testing | Regenerates from this controller's OpenAPI. A shape that generates badly is a finding. |
| M2-D02-03 — React screens | Testing | The consumer. Contract problems surface there. |
| M2-B09 — reference-data endpoints | Soft | States and currencies are needed by the Customer form. This task does **not** build them; M2-D02-03 decides whether to wait for M2-B09 or use existing endpoints. |
| M2-A08 — row-level scoping | Information | If `User.StateCodesCsv` scoping turns out real, the customer list must respect it. M2-A08 is P0 and gated before M2-D01, so its answer is known by now — **check it**. |

## Required Existing Knowledge

| Doc | Path | Why |
|---|---|---|
| KB-083 | `docs/kb/execution/prompt-template.md` | Generation rules + verified commands |
| KB-080 §9 | `docs/kb/execution/README.md` | M2, G2, and the "two independent users" Definition of Done |
| ADR-002 | `docs/kb/decisions/ADR-002-rest-api-layer.md` | §2 contract conventions, §3 workflow commands, §4 error contract, §5 tenant, §6 versioning |
| ADR-004 | `docs/kb/decisions/ADR-004-server-side-authorization.md` | The whole document; §6 and *Non-negotiable* especially |
| KB-040 | `docs/kb/api/api-overview.md` | The as-is surface and the "worth keeping / worth fixing" tables |
| KB-041 | `docs/kb/api/api-readiness-assessment.md` | The controller template sample and the standard error contract |
| KB-013 | `docs/kb/architecture/auth-and-permissions.md` | `UserRight × Screens` |
| KB-014 | `docs/kb/architecture/multi-tenancy.md` | Why tenant isolation is structural, and what still needs checking |
| **customer-master-rules** | `docs/kb/business-rules/customer-master-rules.md` | **Created by M2-D02-01.** The `BR-CUST-*` rules this controller must not break. |
| KB-030 | `docs/kb/business-rules/business-rule-inventory.md` | BR-SO-001's delete-message precedent |
| KB-003 | `docs/kb/investigation-registry.md` | INV-008, INV-024, INV-025 |
| M2-B03's output | the codified controller template | The document this task is judged against |

## Investigation Requirements

**Reuse. No new investigation.**

- **INV-008** (existing API surface) — Complete → KB-040. Reuse for the template and its
  known problems. Do not re-derive the endpoint inventory.
- **INV-024** — Partial after M2-D02-01, with Customer Master complete. Reuse
  `customer-master-rules.md` wholesale; **do not re-read the Razor pages to rediscover
  business rules.** They have been extracted; the service is now the source of truth.
- **INV-025** (delete-guard audit) — if M0-10 completed it, check whether
  `CanDeleteCustomerAsync` was flagged for the R-08 copy-paste pattern. If it was, the fix
  belongs to that audit's follow-up, not to this task; surface it and move on.

**One thing must be verified, not assumed:** whether **M2-A08** found real row-level
scoping via `User.StateCodesCsv` (Q-08). If it did, the customer list endpoint must apply
it, and that requirement is not written here because the answer did not exist at
generation time. Read M2-A08's output first.

## Repository Areas

| Area | Path | Role |
|---|---|---|
| New controller | `V.SMART/V.SMART.Api/Controllers/CustomersController.cs` | **TO BE CREATED** |
| Query DTO | `V.SMART/V.SMART.Api/Contracts/` or wherever M2-B03 placed Currency's | **TO BE CREATED** — follow M2-B03 |
| Reference controller | `V.SMART/V.SMART.Api/Controllers/CurrencyController.cs` | Read-only. The pattern. |
| API host | `V.SMART/V.SMART.Api/Program.cs` | **Read-only in this task.** DI is M2-B07's. |
| Service | `.../MasterService/GeneralService/CustomerService.cs` | **Read-only.** Consumed unchanged. |
| Service interface | `.../IGeneralService/ICustomerService.cs` | **Read-only.** |
| ViewModel | `.../GeneralViewModel/CustomerVM.cs` | **Read-only.** The wire payload (ADR-002 §2). |
| Screen seed | `V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs:1166` | The exact `[RequireScreen]` string |
| Tests | `tests/V.SMART.Shared.Tests/` and an API test project | The latter may be **TO BE CREATED** |
| Permission matrix | M2-A03's harness | Gains Customer's row |

## Existing Behavior to Preserve

### The template, as `CurrencyController` proves it (Confirmed, 2026-08-12)

`V.SMART/V.SMART.Api/Controllers/CurrencyController.cs` — 98 lines — establishes the shape
this controller copies:

| Line(s) | Element |
|---|---|
| `:8-10` | `[ApiController]` · `[Route("api/currencies")]` · `[Authorize]` |
| `:15-18` | Constructor takes the service interface and nothing else |
| `:20-24` | A paged-response `record` with `Items`, `TotalCount`, `PageNumber`, `PageSize` |
| `:26-47` | Paged `GET` with `pageNumber`/`pageSize` + typed filter params folded into the service's `Dictionary<string, object>` |
| `:49-56` | `GET {id:int}` → 200 or 404 |
| `:58-69` | `POST` → 201 + `Location` via `CreatedAtAction` |
| `:71-82` | `PUT {id:int}` → 200 |
| `:84-96` | `DELETE {id:int}` → `CanDelete…Async` guard, then delete → 204 |

**Two things about it must NOT be copied**, because M2-A02/M2-A06 fixed them and copying
them would reintroduce closed risks:

- **R-24 — two different 400 shapes.** `ValidationProblem(ModelState)` at `:62` and `:75`
  versus a bare `BadRequest(new { message })` at `:66`, `:79` and `:89`. `CustomersController`
  emits **one** shape: `ProblemDetails`. Model/`DataAnnotations` failures → 400 with
  `errors` keyed by field. Business-rule refusals → **409** with the service's message in
  `title`, verbatim (ADR-002 §4). A business refusal returned as 400 is a defect.
- **R-03 — no screen-right check.** `CurrencyController` originally carried only
  `[Authorize]`. `CustomersController` carries `[RequireScreen("Customer")]` from day one
  plus a `[RequireRight(...)]` on every action.

Two further known template gaps ([KB-040](../../api/api-overview.md), "Worth fixing before
replication") that this controller must **not** inherit:

- **No route/body id consistency check.** `CurrencyController.Update` never checks that
  `vm.CurrId == id` (`:71-82`). `CustomersController.Update` **must** — a mismatch is 400.
- **Untyped `Dictionary<string, object>` filters at the boundary.** The service signature
  keeps its dictionary (it is not being rewritten), but the controller exposes a **typed
  query DTO** and adapts, exactly as ADR-002 §2 and KB-041's template require.

### Service contract after M2-D02-01

**Read `ICustomerService.cs` as it actually is when you run.** As of 2026-08-12 — i.e.
*before* M2-D02-01 — it declared exactly four members (`:14-18`), none of which writes a
customer: `CanDeleteCustomerAsync`, `SearchWithDynamicFilterAsync`,
`DeleteCustomerByCustIdAsync`, `GetCustomerListAsync`. M2-D02-01 adds the write path,
get-by-id and validation.

The pre-existing four behave as follows (Confirmed):

| Member | Behaviour | Evidence |
|---|---|---|
| `SearchWithDynamicFilterAsync(page, size, filters)` | Returns `(List<CustomerVM>, int TotalCount)`. Eager-loads `Currency`, `AsNoTracking`. Filters: `Customer`, `CreatedBy`, `FromDate`, `ToDate`, `Status`; unknown keys ignored. Ordering fixed at `OrderByDescending(CustId)`. Wraps failures in `InvalidOperationException("Failed to load customer list.", ex)`. | `CustomerService.cs:43-79`, `:81-139` |
| `CanDeleteCustomerAsync(custId)` | `(bool, string)`. Refuses on missing row, on FK usage via `ForeignKeyUsageChecker`, and on an inactive customer. Three distinct product-UX messages. | `CustomerService.cs:142-168` |
| `DeleteCustomerByCustIdAsync(custId)` | Transactional; returns `false` when the row is absent; audits with screen `"Customer List"`; rethrows on failure after rollback. | `CustomerService.cs:171-212` |
| `GetCustomerListAsync(topic, from, to)` | A **report-shaped** query (`Sales` / `Exports`), not CRUD. **Do not expose it from this controller.** It belongs to the reporting surface (ADR-005 / M2-B08). Returns an empty list for any other topic. | `CustomerService.cs:215-274` |

### Screen identity (Confirmed)

`ApplicationDbContext.cs:1166` —
`new Screens { Id = 15, ScreenCode = 15, ScreenName = "Customer", IsPrintRequired = false }`.
`[RequireScreen("Customer")]` must match that string **exactly**. Both Blazor pages agree:
`CustomerList.razor:398` and `CustomerUpsert.razor:681` both declare
`protected override string ScreenName => "Customer"`. ADR-004's Consequences warn that a
typo **silently denies** — verify, do not retype from memory. If M2-B05 has landed, use the
generated `ScreenCodes`/`ScreenNames` constant instead of a string literal.

### Validation already on the wire (Confirmed)

`CustomerVM` carries `DataAnnotations` that `ModelState` enforces for free — and they are
**not** the same as, nor a superset of, the `@code` rules M2-D02-01 extracted. Both layers
must survive:

| Field | Annotations | Line |
|---|---|---|
| `CustName` | `[Required]`, `[StringLength(100)]` | `:20-22` |
| `CustAddr` | `[Required]`, `[StringLength(250)]` | `:24-26` |
| `GSTNo` | `[Required]`, `[StringLength(15)]` | `:28-30` |
| `PANNo` | `[StringLength(10)]` | `:32-33` |
| `ContactNo` | `[Phone]`, `[StringLength(15)]` | `:35-37` |
| `StateId` | `[Required]` | `:39-40` |
| `StateName` | `[Required]`, `[StringLength(100)]` | `:42-44` |
| `OpenBal`, `OpenBalPndg` | `[Range(0, double.MaxValue)]`, `[Precision(18,2)]` | `:46-52` |
| `Email` | `[StringLength(100)]`, `[EmailAddress]` | `:56-58` |
| `PinCode` | `[Required]`, `[RegularExpression(@"^[1-9][0-9]{5}$")]`, `[StringLength(6)]` | `:65-68` |
| `BusiType` | `[Required]`, `[StringLength(50)]` | `:73-75` |
| `CurrId` | `[Required]` — **on an `int?`** | `:85-86` |
| `Location` | `[Required]`, `[RegularExpression(@"^[a-zA-Z\s]+$")]`, `[StringLength(50)]` | `:92-95` |
| `Distance` | `[Required]`, `[Range(1, double.MaxValue)]` | `:97-99` |
| `SupTyp` | `[Required]`, `[StringLength(10)]` | `:116-118` |
| Children | `CustomerIndirectVMs`, `ContactPersonVMs`, `ItemSubVMs` — plain lists, no validation attributes | `:134-136` |

Two things worth flagging rather than silently discovering:

- **`StateName` is `[Required]` on the wire but is a *derived* field** — BR-CUST-008 says
  the server denormalises it from the matched `State` row. A client that omits it gets a
  400 for a field the server was going to overwrite anyway. Record this; do not fix the
  ViewModel here (that is a contract change needing its own decision).
- **`Distance` is `[Range(1, …)]` and non-nullable `double`** — so `0` is invalid and there
  is no "unspecified". Record it.

### Business rules

**All `BR-CUST-*` rules from `docs/kb/business-rules/customer-master-rules.md` are
preserved unchanged.** This controller enforces none of them itself — it calls the service
and translates the result. In particular:

- Validation failures → 400 `ProblemDetails` with `errors`; the service's message strings
  survive verbatim.
- The duplicate-name refusal (BR-CUST-001) and the three delete refusals (BR-CUST-014) →
  **409**, `title` carrying the service's message verbatim. Not 400. Not paraphrased.
  Precedent: BR-SO-001's migration note, and ADR-002 §4's 409 row.
- **BR-AUTH-002** — deny by default. A missing `UserRight` row means no right.
- The controller adds **no** business logic. If it needs to, M2-D02-01 was incomplete.

## Target Result

1. `V.SMART/V.SMART.Api/Controllers/CustomersController.cs` exists, ≤ ~140 lines, with no
   `if` that decides a business outcome, no `IUnitOfWork`, no `DbContext`, no `Regex`, and
   no `AutoMapper` usage beyond what the template sanctions.
2. Five endpoints under `/api/v1/customers`, matching ADR-002 §2's shape exactly.
3. `[RequireScreen("Customer")]` on the class; `[RequireRight(Right.View)]` on both GETs,
   `Create` on POST, `Edit` on PUT, `Delete` on DELETE.
4. A **typed query DTO** for the list filters, adapted to the service's dictionary inside
   the controller.
5. `Update` rejects a route/body id mismatch with 400.
6. Exactly one error shape — `ProblemDetails`. 400 validation, 403 right denied, 404 not
   found, **409 business refusal with the message verbatim**, 500 with `traceId`.
7. `POST` returns 201 + `Location`, and the returned `CustomerVM` **includes the persisted
   children** with their generated ids — otherwise the React form cannot re-render after
   save without a second round trip.
8. Customer's row exists in the M2-A03 permission-matrix harness, covering every endpoint ×
   every right.
9. API integration tests covering the CRUD path, the child-collection round trip, each
   error status, and tenant isolation on the `{id}` routes.
10. OpenAPI documents all five endpoints with accurate response types, and M2-B10
    regenerates the TypeScript client cleanly.
11. `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` → 0 errors.
12. `V.SMART/V.SMART.Shared/` is **unchanged**. `git diff --stat V.SMART/V.SMART.Shared/`
    is empty — that is the proof the backend was extended, not rewritten.

## Implementation Steps

1. Read the knowledge base above, **including `customer-master-rules.md` from M2-D02-01**
   and M2-B03's codified template. Search KB-003; reuse INV-008 and INV-024.
2. **Check M2-A08's output** for row-level scoping via `User.StateCodesCsv` (Q-08). If
   scoping is real, the list endpoint must honour it and you must say how. If M2-A08 found
   none, record that negative result and continue.
3. **Read `ICustomerService.cs` as it now is.** Do not assume M2-D02-01's exact signatures
   — read them. If there is no single-call save, or no independently callable validation,
   **stop and report Blocked**: the controller cannot be thin without them.
4. **Confirm DI.** Verify `AddVSmartDomain()` (M2-B07) registers `ICustomerService`. If it
   does not, stop — do **not** add `AddScoped<ICustomerService, CustomerService>()` to
   `Program.cs`. That line is M2-B07's, and adding it here starts the 60-controller
   copy-paste that M2-B07 exists to prevent.
5. **Create the typed query DTO** — `pageNumber`, `pageSize`, and the five filters
   BR-CUST-016 supports (`customer`, `createdBy`, `fromDate`, `toDate`, `status`). Type
   the dates as `DateOnly?`/`DateTime?`, not `string`; KB-040 flags string dates in an
   untyped dictionary as weak typing at the boundary. Adapt to the service's
   `Dictionary<string, object>` inside the controller — **the service signature does not
   change.** Preserve the key names the service switches on exactly
   (`CustomerService.cs:94-125`): `Customer`, `CreatedBy`, `FromDate`, `ToDate`, `Status`.
6. **Write the controller**, following M2-B03's template file-for-file. Each action: bind,
   authorize (by attribute), call one service method, translate the result. Nothing else.
7. **Map results to status codes** in one small private helper, not repeated per action:
   service `Success == false` → 409 with `title` = the service message; validation errors →
   400 `ProblemDetails` with `errors`; `null` from get-by-id → 404; delete guard refusal →
   409; delete of a missing row → 404; success → 200/201/204 per ADR-002 §2.
8. **Handle the children.** `POST`/`PUT` accept a `CustomerVM` carrying
   `CustomerIndirectVMs` and `ContactPersonVMs`; the response echoes them **with generated
   ids**. Verify against the database, not against the in-memory object. If the round trip
   does not work, the BR-CUST-017 mapping fix from M2-D02-01 is incomplete — report it
   rather than patching the mapping here.
9. **Do not expose `GetCustomerListAsync`.** It is a report query (`CustomerService.cs:215-274`),
   not CRUD, and belongs to M2-B08's reporting surface. Say so in the OpenAPI description
   if it helps, but add no endpoint.
10. **Add Customer's row to the M2-A03 permission-matrix harness**: every endpoint × every
    right, asserting **403** wherever the right is absent. ADR-004 §6 makes this a merge
    gate.
11. **Write API integration tests** (see *Testing*).
12. **Verify OpenAPI**, then have M2-B10's generator produce the TypeScript client. A shape
    that generates awkwardly is a **finding to report before 58 more controllers copy it**.
13. **Verify Blazor still works.** Create a customer through the API, open it in the Blazor
    `/customer/update/{id}` page, confirm every field and both child collections are
    correct; then edit in Blazor and re-fetch through the API. Both UIs, one database.
14. Build, review the diff, update documentation, commit. Do not merge.

## Files Expected to Change

- `V.SMART/V.SMART.Api/Controllers/CustomersController.cs` — created (see below)
- The M2-A03 permission-matrix harness — Customer's row
- `docs/kb/api/api-overview.md` (KB-040) — the new endpoints
- `docs/kb/frontend-new/feature-mapping.md` (KB-052) — Customer's "Existing API/logic" cell
  no longer says the endpoints do not exist
- `docs/kb/execution/tasks/M2-D02-02.md` (this file)

## Files Expected to Be Created

- `V.SMART/V.SMART.Api/Controllers/CustomersController.cs` — **TO BE CREATED**
- A query-DTO file, in whatever location M2-B03 chose for Currency's — **TO BE CREATED**
- API integration tests. **If no API test project exists**, one must be added and wired
  into CI, mirroring what M0-12-01 did for `tests/V.SMART.Shared.Tests/`. Confirm before
  assuming: M0-12-01 creates a **`V.SMART.Shared`** test project, which is a unit-test
  project, not necessarily a `WebApplicationFactory`-based integration one. If the API test
  project does not exist, creating it is in scope for this task — it is the first
  controller that needs it beyond M2-A02's denial tests. **Check what M2-A02 created first
  and extend that**, rather than starting a third project.

## Files That Must Not Change

- `V.SMART/V.SMART.Shared/**` — **the entire project.** No service, ViewModel, entity,
  mapping, repository or Razor change. If something there needs changing, the extraction
  (M2-D02-01) was incomplete: report it and stop.
- `V.SMART/V.SMART.Api/Program.cs` — DI belongs to M2-B07; middleware to M2-A05/M2-A06;
  versioning to M2-B01.
- `V.SMART/V.SMART.Api/Controllers/CurrencyController.cs` — read-only reference.
- `V.SMART/V.SMART.Shared/Migrations/**` — no schema change.
- `frontend/**` — M2-D02-03 owns the client.
- The `[RequireScreen]`/`[RequireRight]` filter implementation — M2-A01-02 owns it. If it
  cannot express what Customer needs, that is a finding.

## API Changes

| Aspect | Value |
|---|---|
| **Method / Route** | `GET /api/v1/customers` · `GET /api/v1/customers/{id:int}` · `POST /api/v1/customers` · `PUT /api/v1/customers/{id:int}` · `DELETE /api/v1/customers/{id:int}` |
| **Request** | *List:* typed query DTO — `pageNumber` (default 1), `pageSize` (default 10), `customer`, `createdBy`, `fromDate`, `toDate`, `status` (`Active` \| `In Active`). *Get/Delete:* `id` from the route. *Create/Update:* `CustomerVM` as JSON, including `CustomerIndirectVMs` and `ContactPersonVMs`. The ViewModel is used unchanged (ADR-002 §2) — no parallel DTO hierarchy. |
| **Response** | *List:* `{ items, totalCount, pageNumber, pageSize }`. *Get:* `CustomerVM` with children populated. *Create:* **201** + `Location: /api/v1/customers/{CustId}` + the persisted `CustomerVM` **with generated child ids**. *Update:* **200** + the persisted `CustomerVM`. *Delete:* **204**. |
| **Authentication** | JWT Bearer, `[Authorize]` at class level. Access token in memory, refresh token in an httpOnly cookie (ADR-003 rule 5). |
| **Authorization** | `[RequireScreen("Customer")]` — the exact seeded string from `ApplicationDbContext.cs:1166`. `[RequireRight(Right.View)]` on both GETs, `Right.Create` on POST, `Right.Edit` on PUT, `Right.Delete` on DELETE. Deny by default (BR-AUTH-002). Rights resolved per request with the ≈60 s cache from M2-A01-03 — **not** from JWT claims. |
| **Tenant context** | From the JWT `TenantId` claim (ADR-002 §5). The tenant selects the connection string, so cross-tenant access is structurally impossible — **but** `{id}` route parameters must still be validated against the resolved tenant's data to prevent IDOR *within* a tenant (ADR-004 §5). Covered by the required tenant-isolation test. `TenantProvider` / `TenantDbContextFactory` unchanged. |
| **Error model** | `application/problem+json` **only**. 400 — model binding / `DataAnnotations` / route-body id mismatch, with `errors` keyed by field. 401 — unauthenticated. 403 — screen right denied, naming the screen and right. 404 — not found. **409 — business-rule refusal, `title` carrying the service's message verbatim** (duplicate name; the three delete refusals). 500 — `traceId` only. **No endpoint returns two different shapes for one status.** |
| **Paging** | Server-side per M2-B02. `pageNumber` is 1-based, matching the service (`CustomerService.cs:66`: `Skip((pageNumber - 1) * pageSize)`). Enforce a maximum `pageSize` per M2-B02 — the service applies none. |
| **Filtering** | The five keys BR-CUST-016 supports, typed at the boundary and adapted to the service's dictionary using the exact key names it switches on (`CustomerService.cs:94-125`). Unknown filters are ignored by the service; the DTO makes them unrepresentable, which is the improvement. |
| **Sorting** | **n/a — and this must be explicit, not silent.** The service hard-codes `OrderByDescending(x => x.CustId)` (`CustomerService.cs:65`) and accepts no sort parameter. If M2-B02's contract mandates a `sort` parameter, adding it requires a service change, which this task forbids — **raise it as a finding** and let M2-B02/M2-D02-01 own the fix. Do **not** sort the current page in memory: a page-local sort over server paging is a correctness bug. |
| **Idempotency** | n/a. Customer is a master, not a document; it allocates no document number, so R-12/C5 idempotency keys do not apply. `PUT` is naturally idempotent. **Note for the record:** `POST` is not — two identical posts produce a duplicate-name 409 on the second, which is the correct behaviour, not an idempotency mechanism. |

## React Changes

| Aspect | Value |
|---|---|
| Route | n/a — no React code in this task |
| Component | n/a |
| API integration | n/a. This task **produces** the contract that M2-D02-03 consumes; M2-B10 regenerates the TypeScript client from it. |
| State | n/a |
| Validation | n/a — but the `CustomerVM` `DataAnnotations` and the `BR-CUST-*` rules together define what M2-D02-03's Zod schema mirrors. |
| Permissions | n/a — but the screen name `"Customer"` is the key M2-D02-03 looks up in the rights map. |
| Loading state | n/a |
| Empty state | n/a |
| Error state | n/a — but the `ProblemDetails` shapes above are exactly what M2-D02-03 renders. |
| Accessibility | n/a |
| Responsive behaviour | n/a |

## Business Logic

**Preserved unchanged.**

This task moves no logic and creates none. Every business decision — validation outcome,
duplicate refusal, delete guard, child-collection synchronisation, audit stamping,
transaction boundary — was extracted into `CustomerService` by M2-D02-01 and is invoked
here through a single service call per action. The controller's only responsibilities are
binding, authorization (declarative, by attribute), and translating a service result into a
status code and a `ProblemDetails` body.

The one-line test: **if any `if` statement in `CustomersController` decides a business
outcome, this task has failed.** Status-code selection is not a business decision;
"is this customer deletable" is, and it lives in `CanDeleteCustomerAsync`.

`git diff --stat V.SMART/V.SMART.Shared/` being empty is the objective proof.

## Testing

| Level | What | Where |
|---|---|---|
| **Permission matrix** (merge gate) | Every endpoint × every right. 403 wherever the right is absent; success where present. Includes a user with **no** `UserRight` row for `Customer` at all — deny by default. **ADR-004: no controller merges without this row.** | M2-A03's harness |
| API integration | `GET` list: paging (`pageNumber` 1-based), each of the five filters, `totalCount` correctness, and the fixed `CustId DESC` ordering | API test project |
| API integration | `GET {id}`: 200 with **children populated**; 404 for an unknown id |  |
| API integration | `POST`: 201, `Location` header correct, response body carries **generated child ids**; verified against the database, not the in-memory object |  |
| API integration | `POST` duplicate name → **409**, `title` byte-identical to the service's message (BR-CUST-001) |  |
| API integration | `POST`/`PUT` invalid model → 400 `ProblemDetails` with `errors` keyed by field name |  |
| API integration | `PUT` with `id` ≠ `vm.CustId` → **400**. (`CurrencyController` has no such check — this is the template gap being closed.) |  |
| API integration | `PUT` child sync: one child updated, one added, one removed → exactly the expected rows persist (BR-CUST-013) |  |
| API integration | `PUT` with a blank-named child → silently discarded, matching BR-CUST-012 exactly. **Assert the legacy behaviour**, do not assert an improvement. |  |
| API integration | `DELETE` on a referenced customer → **409**, message verbatim (BR-CUST-014); on an inactive customer → 409 with its distinct message; on an unknown id → 404; on a deletable one → 204 |  |
| API integration | **Tenant isolation / IDOR**: a token for tenant A requesting a `{id}` belonging to tenant B receives 404 (or 403), never tenant B's data (ADR-004 §5) |  |
| API integration | Unauthenticated request → 401 |  |
| Contract | OpenAPI lists all five endpoints with correct response types and status codes; M2-B10 regenerates the TypeScript client with no manual fix-ups |  |
| Manual | A customer created through the API opens correctly in Blazor `/customer/update/{id}` with both child collections intact; an edit in Blazor is visible through the API | non-production tenant DB |

## Acceptance Criteria

- [ ] `V.SMART/V.SMART.Api/Controllers/CustomersController.cs` exists and demonstrably
      follows M2-B03's codified template.
- [ ] All five routes are under `/api/v1/customers` and match ADR-002 §2's shape.
- [ ] `[RequireScreen("Customer")]` is present, and its string matches
      `ApplicationDbContext.cs:1166` **exactly** — verified by grep, not by eye.
- [ ] Every action carries a `[RequireRight(...)]` matching its verb.
- [ ] The list endpoint takes a **typed query DTO**, not `Dictionary<string, object>`, and
      not loose `string` dates.
- [ ] `PUT` returns 400 when the route id and `vm.CustId` disagree.
- [ ] Exactly one error shape is emitted. `git grep -n "BadRequest(new {" V.SMART/V.SMART.Api/Controllers/CustomersController.cs`
      returns nothing.
- [ ] Business refusals return **409**, not 400, with the service's message verbatim in
      `title` — asserted on the exact string in a test.
- [ ] `POST` returns 201 with a correct `Location` and a body whose child collections carry
      generated ids.
- [ ] The controller contains no `IUnitOfWork`, no `DbContext`, no `Regex`, no `Transaction`
      — verified by grep.
- [ ] No `if` in the controller decides a business outcome (reviewer judgement, recorded in
      the PR description).
- [ ] `git diff --stat V.SMART/V.SMART.Shared/` is **empty**.
- [ ] `git diff --stat V.SMART/V.SMART.Api/Program.cs` is **empty**.
- [ ] `git diff --stat frontend/` is **empty**.
- [ ] No file under `V.SMART/V.SMART.Shared/Migrations/` changed.
- [ ] Customer's permission-matrix row exists in M2-A03's harness and passes, including the
      no-`UserRight`-row case.
- [ ] Every API integration test in the *Testing* table exists and passes.
- [ ] The tenant-isolation test passes.
- [ ] `dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj` reports 0 errors and no more
      than 6,695 warnings.
- [ ] OpenAPI documents all five endpoints; M2-B10 regenerates the client with no manual
      fix-ups.
- [ ] `GetCustomerListAsync` is **not** exposed by this controller.
- [ ] The Blazor round-trip check was performed and recorded.
- [ ] KB-040 lists the five new endpoints in its `api_endpoints` frontmatter.

## Verification Commands

```bash
dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj
```

```bash
dotnet test tests/V.SMART.Shared.Tests/
```

```bash
git diff --stat V.SMART/V.SMART.Shared/ V.SMART/V.SMART.Api/Program.cs frontend/
```

```bash
git status --porcelain
```

```bash
grep -n "RequireScreen\|RequireRight" V.SMART/V.SMART.Api/Controllers/CustomersController.cs
```

```bash
grep -n "BadRequest(new {\|IUnitOfWork\|DbContext\|Regex\|BeginTransaction" V.SMART/V.SMART.Api/Controllers/CustomersController.cs
```

```bash
grep -n "ScreenName = \"Customer\"" V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs
```

> The API integration tests run under whatever project M2-A02 created for its denial tests,
> or a new one added by this task; use **that** project's command. `dotnet test
> tests/V.SMART.Shared.Tests/` is valid only because M0-12-01 is a prerequisite — before it,
> there is no test project at all (INV-023, Confirmed).

## Documentation Updates

| Document | Change | Frontmatter |
|---|---|---|
| `docs/kb/api/api-overview.md` (KB-040) | Add the five Customer endpoints with request/response/error detail, in the same format as the Currency section. Note that the surface is no longer "2 controllers / 6 endpoints". | add to `api_endpoints`, `entities`, `database_tables`, `source_files`; bump `last_verified` |
| `docs/kb/frontend-new/feature-mapping.md` (KB-052) | Customer's "Existing service" cell currently implies no endpoints exist. Update it to reflect that `/api/v1/customers` now does. | bump `last_verified` |
| M2-B03's controller-template document | Record Customer as the template's **second independent user** — the condition KB-080 §9's Definition of Done requires. Note anything the template did not cover (child collections, in particular) and amend the template accordingly. | bump `last_verified` |
| `docs/kb/execution/tasks/M2-D02-02.md` (this file) | `status: Completed`; fill `business_rules` with the `BR-CUST-*` ids from M2-D02-01. | bump `last_verified` |
| `docs/kb/execution/task-tracker.md` (KB-081) | Mark M2-D02-02 Completed — **only if the file exists**; it did not on 2026-08-12. Do not create it as a side effect. | n/a |

## Investigation Registry Updates

**No new investigation.** Reuse INV-008 (API surface), INV-024 (now Partial, Customer
complete) and INV-004 (auth and rights).

Record these negative results:

- "`V.SMART.Shared` was not modified by this task — `git diff --stat` empty. The controller
  wrapped the post-M2-D02-01 `ICustomerService` with zero service changes."
- "`GetCustomerListAsync` was deliberately **not** exposed: it is a report query
  (`CustomerService.cs:215-274`) and belongs to M2-B08's reporting surface."
- "`CustomerService` accepts no sort parameter (`CustomerService.cs:65` hard-codes
  `OrderByDescending(CustId)`), so the endpoint exposes none."
- The result of the M2-A08 row-level-scoping check, whichever way it went.

If the child-collection round trip revealed that the template needs a new rule, that is an
**amendment to M2-B03's document**, not a new investigation.

## Git Strategy

| | |
|---|---|
| **Branch** | `migration/M2-D02-02-customers-controller` |
| **Commit** | `M2-D02-02: Add CustomersController with permission-matrix and API tests` |
| **Expected changed files** | `V.SMART/V.SMART.Api/Controllers/CustomersController.cs` (new); a query-DTO file (new); API integration test files (new); the M2-A03 harness; `docs/kb/api/api-overview.md`; `docs/kb/frontend-new/feature-mapping.md`; M2-B03's template document; `docs/kb/execution/tasks/M2-D02-02.md` |
| **Rollback** | Revert the branch's merge commit. The change is purely additive at the HTTP layer — a new controller file plus tests — with no change to `V.SMART.Shared`, no schema change and no data change. Reverting removes the endpoints and leaves Blazor, the services and the database untouched. M2-D02-03 depends on these endpoints, so a revert must be paired with reverting or pausing that task. |

Do not merge. Do not push to `master`. Leave the branch for review.

## Fresh-Session Execution Prompt

Copy everything in the block below into a new AI session.

```text
============================================================
ERP MIGRATION — TASK EXECUTION PROMPT
============================================================

TASK ID:
M2-D02-02

TASK NAME:
CustomersController + API tests

ROLE
You are an engineer working on the V.SMART / NexGen ERP
modernization project. You are executing exactly one task.

PROJECT OBJECTIVE
We are replacing the existing Blazor Server frontend with a new
React frontend while preserving the existing ERP business
behaviour, business services, database behaviour and business
rules wherever possible. The backend is extended, never
rewritten. Business logic currently trapped in Razor @code is
extracted into server-side services before any React screen
replaces it.

CURRENT ARCHITECTURE
Repository root: C:\Kumar\NexGen-ERP---2025-master\NexGen-ERP---2025-master
(this is the git repository root; the parent directory is not
part of the project)

  V.SMART/V.SMART.Shared   .NET 9 class library — ALL domain code:
                           196 EF entity sets, 285 business services,
                           ~190 repositories + UnitOfWork, 274 ViewModels,
                           333 Razor pages, 440 routes
  V.SMART/V.SMART.Web      Blazor Server host (live UI, stays running)
  V.SMART/V.SMART.Api      ASP.NET Core Web API (.NET 9) — the React
                           backend
  V.SMART/V.SMART          .NET MAUI Blazor Hybrid host

SQL Server + EF Core 9, code-first, database-per-tenant.

Task-specific architecture context:
Multi-tenancy is DATABASE PER TENANT. Every request resolves its
tenant from the JWT TenantId claim; ITenantProvider /
ITenantDbContextFactory then build the ApplicationDbContext from
that tenant's connection string. Because the tenant selects the
connection string, cross-tenant access is STRUCTURALLY
impossible. But {id} route parameters must still be validated
against the resolved tenant's data to prevent IDOR WITHIN a
tenant (ADR-004 section 5). Do not change any tenancy code.

Business services take and return ViewModels, never entities.
Service naming: SearchWithDynamicFilterAsync, Get...ByIdAsync,
Upsert...Async, CanDelete...Async. Services contain NO permission
checks — that is intentional (BR-AUTH-002 / ADR-004): enforcement
lives in the API authorization filter.

SOURCE OF TRUTH
The project knowledge base is at docs/kb/. Read it before
investigating the repository. Use docs/kb/INDEX.md for
question-to-document routing.

Required reading for this task:
  KB-083  docs/kb/execution/prompt-template.md
  KB-080  docs/kb/execution/README.md — section 9 (M2), gate G2,
          and the M2 Definition of Done, which says the
          controller template is only real once it has TWO
          independent users
  ADR-002 docs/kb/decisions/ADR-002-rest-api-layer.md
          (sections 2, 3, 4, 5 and 6)
  ADR-004 docs/kb/decisions/ADR-004-server-side-authorization.md
          (all of it; section 6 and "Non-negotiable" especially)
  KB-040  docs/kb/api/api-overview.md — the as-is surface, and
          its "worth keeping / worth fixing" tables
  KB-041  docs/kb/api/api-readiness-assessment.md — the
          controller template sample and the standard error
          contract
  KB-013  docs/kb/architecture/auth-and-permissions.md
  KB-014  docs/kb/architecture/multi-tenancy.md
  docs/kb/business-rules/customer-master-rules.md
          — CREATED BY M2-D02-01. The BR-CUST rules this
            controller must not break. READ IT FIRST; do not
            re-read the Razor pages to rediscover them.
  KB-030  docs/kb/business-rules/business-rule-inventory.md
          (BR-SO-001's delete-message precedent)
  KB-002  docs/kb/source-of-truth-rules.md
  KB-003  docs/kb/investigation-registry.md
  KB-005  docs/kb/INDEX.md
  M2-B03's codified controller-template document — the document
          this controller is judged against
  This task's full specification:
          docs/kb/execution/tasks/M2-D02-02.md

Authority order when sources conflict:
  1. Current source code
  2. Database schema / EF migrations (for storage)
  3. The knowledge base (for interpretation)
  4. Older prose documentation — hypothesis only
docs/ARCHITECTURE.md is superseded and contains known factual
errors. Do not rely on it.

INVESTIGATION REGISTRY
Before any repository investigation, search
docs/kb/investigation-registry.md.

Relevant investigations:
  INV-008  Existing API surface — Complete. REUSE IT (KB-040).
           Do not re-derive the endpoint inventory.
  INV-024  @code triage per module — Partial after M2-D02-01,
           with Customer Master complete. REUSE
           customer-master-rules.md wholesale. The service, not
           the Razor page, is now the source of truth for
           Customer behaviour.
  INV-025  Delete-guard audit for the R-08 copy-paste pattern.
           If M0-10 completed it, check whether
           CanDeleteCustomerAsync was flagged. If it was, the
           fix belongs to that audit's follow-up, NOT to this
           task — surface it and move on.
  INV-004  Authentication and screen rights — Complete. REUSE.

Before investigating the repository, search
docs/kb/investigation-registry.md and the relevant knowledge-base
documents via docs/kb/INDEX.md. If an investigation is Complete
and not stale, reuse its findings and cite the doc_id — do not
re-derive them. If it is Partial, investigate only the documented
gap. If it is absent or contradicted by current code,
investigate, then record the finding, its file:line evidence, and
its confidence in the knowledge base so that future sessions do
not repeat this work. Record negative results too — "grepped for
X, found none" is a finding.

CURRENT TASK
Expose Customer Master over HTTP as CustomersController,
following the controller template codified by M2-B03 and proved
by CurrencyController, wrapping the ICustomerService methods that
M2-D02-01 extracted — WITH ZERO FURTHER SERVICE CHANGES — and
prove it with API integration tests plus a full permission-matrix
row.

Five endpoints under /api/v1/customers: paged list, get-by-id,
create, update, delete. Every action annotated with
[RequireScreen("Customer")] + [RequireRight(...)]. Every error
shaped as ProblemDetails. Every service message surfaced
verbatim.

WHY THIS TASK EXISTS
This is the controller template's SECOND INDEPENDENT USER, and
that is what makes the template real. KB-080's Definition of Done
for M2 says it outright: G2 is not met until the template is
"demonstrably followed by both existing controllers — the
template is only real once it has two independent users." One
controller is an example; two is a pattern.

It is also the PAYOFF TEST for M2-D02-01. If the extraction
produced the right service shape, this controller is thin and
mechanical: bind -> authorize -> one service call -> map. If you
find yourself validating, orchestrating, or reaching for
IUnitOfWork inside the controller, THE EXTRACTION WAS INCOMPLETE
and the correct response is to send it back to M2-D02-01, not to
compensate here. ADR-002 section 2: "Controllers are thin: bind
-> authorize -> one service call -> map. No business logic."

It serves gate G2; gap B1 from KB-041 (the ~60-80 resource
controllers — this is the second, and it sets the cost estimate
for the other 58-78); A1 / R-03 (every endpoint authorized
server-side, with its permission-matrix row); and A5 / R-24 (one
ProblemDetails error shape, no repeat of CurrencyController's two
different 400 bodies).

Customer is harder than Currency in exactly one interesting way,
and that is why it is second: IT HAS CHILD COLLECTIONS. A
CustomerVM carries CustomerIndirectVMs and ContactPersonVMs
(CustomerVM.cs:134-135). Currency had nothing of the kind. If the
template cannot carry a parent-with-children through POST/PUT
cleanly, it cannot carry a single ERP document either — every
sales order, purchase order and invoice in M3 and M4 is a parent
with lines. Finding that out on a two-child master is enormously
cheaper than finding it out on Sales Order.

PREREQUISITES
  M2-D02-01  Customer @code triage + extraction. ICustomerService
             MUST expose a single-call save and an independently
             callable validation method, and CustomerMapping MUST
             round-trip the child collections. Without those the
             controller cannot be thin — report Blocked.
  M2-B07     AddVSmartDomain() DI extension.
  M2-A01-03  Per-request rights resolution + caching.
  M2-A02     [RequireScreen]/[RequireRight] proven on
             CurrencyController.
  M2-A06     Exception middleware -> ProblemDetails.
  M2-B01     /api/v1 versioning.
  M2-B02     Server-side paging/sort/filter contract.
  M2-B03     Codified controller template. FOLLOW it; do not
             invent one.
  M2-A03     Permission-matrix harness. This task adds a row.
  M0-12-01   Test project.

CURRENT IMPLEMENTATION
All Confirmed on 2026-08-12 unless stated. RE-VERIFY EVERY LINE
NUMBER — M2-A02, M2-A06, M2-B01 and M2-D02-01 will all have
changed things before you run.

THE TEMPLATE, AS CurrencyController PROVES IT:
V.SMART/V.SMART.Api/Controllers/CurrencyController.cs — 98 lines:
  :8-10   [ApiController] [Route("api/currencies")] [Authorize]
  :15-18  constructor takes the service interface and nothing
          else
  :20-24  a paged-response record with Items, TotalCount,
          PageNumber, PageSize
  :26-47  paged GET with pageNumber/pageSize plus typed filter
          params folded into the service's
          Dictionary<string, object>
  :49-56  GET {id:int} -> 200 or 404
  :58-69  POST -> 201 + Location via CreatedAtAction
  :71-82  PUT {id:int} -> 200
  :84-96  DELETE {id:int} -> CanDelete...Async guard, then
          delete -> 204

TWO THINGS ABOUT IT MUST NOT BE COPIED, because M2-A02 and
M2-A06 fixed them and copying them reintroduces closed risks:
  R-24 — TWO DIFFERENT 400 SHAPES.
    ValidationProblem(ModelState) at :62 and :75, versus a bare
    BadRequest(new { message }) at :66, :79 and :89.
    CustomersController emits ONE shape: ProblemDetails.
    Model/DataAnnotations failures -> 400 with errors keyed by
    field. Business-rule refusals -> 409 with the service's
    message in title, VERBATIM (ADR-002 section 4). A business
    refusal returned as 400 is a defect.
  R-03 — NO SCREEN-RIGHT CHECK.
    CurrencyController originally carried only [Authorize].
    CustomersController carries [RequireScreen("Customer")] from
    day one plus a [RequireRight(...)] on every action.

TWO FURTHER TEMPLATE GAPS (KB-040, "Worth fixing before
replication") THAT THIS CONTROLLER MUST NOT INHERIT:
  - NO ROUTE/BODY ID CONSISTENCY CHECK. CurrencyController.Update
    never checks vm.CurrId == id (:71-82). CustomersController's
    Update MUST — a mismatch is 400.
  - UNTYPED Dictionary<string, object> FILTERS AT THE BOUNDARY.
    The SERVICE keeps its dictionary (it is not being
    rewritten), but the CONTROLLER exposes a TYPED QUERY DTO and
    adapts, exactly as ADR-002 section 2 and KB-041's template
    require.

THE SERVICE CONTRACT:
READ ICustomerService.cs AS IT ACTUALLY IS WHEN YOU RUN. As of
2026-08-12 — i.e. BEFORE M2-D02-01 — it declared exactly four
members at :14-18, none of which writes a customer:
  CanDeleteCustomerAsync, SearchWithDynamicFilterAsync,
  DeleteCustomerByCustIdAsync, GetCustomerListAsync
M2-D02-01 adds the write path, get-by-id and validation.

The pre-existing four behave as follows (Confirmed):
  SearchWithDynamicFilterAsync(page, size, filters)
    -> (List<CustomerVM>, int TotalCount). Eager-loads Currency,
    AsNoTracking. Filter keys: Customer, CreatedBy, FromDate,
    ToDate, Status; unknown keys ignored. Ordering FIXED at
    OrderByDescending(CustId). Failures wrapped in
    InvalidOperationException("Failed to load customer list.").
    Evidence: CustomerService.cs:43-79 and :81-139
  CanDeleteCustomerAsync(custId) -> (bool, string). Refuses on a
    missing row, on FK usage via ForeignKeyUsageChecker, and on
    an inactive customer. Three distinct product-UX messages.
    Evidence: CustomerService.cs:142-168
  DeleteCustomerByCustIdAsync(custId) -> transactional; returns
    false when the row is absent; audits with screen
    "Customer List"; rethrows after rollback on failure.
    Evidence: CustomerService.cs:171-212
  GetCustomerListAsync(topic, from, to) -> a REPORT-SHAPED query
    (topics "Sales" and "Exports"), not CRUD. DO NOT EXPOSE IT
    from this controller — it belongs to the reporting surface
    (ADR-005 / M2-B08). Returns an empty list for any other
    topic.
    Evidence: CustomerService.cs:215-274

SCREEN IDENTITY (Confirmed):
V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs:1166
  new Screens { Id = 15, ScreenCode = 15,
                ScreenName = "Customer",
                IsPrintRequired = false }
[RequireScreen("Customer")] must match that string EXACTLY. Both
Blazor pages agree — CustomerList.razor:398 and
CustomerUpsert.razor:681 both declare
  protected override string ScreenName => "Customer"
ADR-004 warns that a typo SILENTLY DENIES. Verify the exact
string; do not retype it from memory. If M2-B05 has landed, use
the generated ScreenCodes/ScreenNames constant rather than a
string literal.

VALIDATION ALREADY ON THE WIRE (Confirmed):
CustomerVM carries DataAnnotations that ModelState enforces for
free. They are NOT the same as, nor a superset of, the @code
rules M2-D02-01 extracted. BOTH layers must survive:
  :20-22   CustName   [Required] [StringLength(100)]
  :24-26   CustAddr   [Required] [StringLength(250)]
  :28-30   GSTNo      [Required] [StringLength(15)]
  :32-33   PANNo      [StringLength(10)]
  :35-37   ContactNo  [Phone] [StringLength(15)]
  :39-40   StateId    [Required]
  :42-44   StateName  [Required] [StringLength(100)]
  :46-52   OpenBal, OpenBalPndg
                      [Range(0, double.MaxValue)]
                      [Precision(18,2)]
  :56-58   Email      [StringLength(100)] [EmailAddress]
  :65-68   PinCode    [Required]
                      [RegularExpression(@"^[1-9][0-9]{5}$")]
                      [StringLength(6)]
  :73-75   BusiType   [Required] [StringLength(50)]
  :85-86   CurrId     [Required] — on an int?
  :92-95   Location   [Required]
                      [RegularExpression(@"^[a-zA-Z\s]+$")]
                      [StringLength(50)]
  :97-99   Distance   [Required] [Range(1, double.MaxValue)]
  :116-118 SupTyp     [Required] [StringLength(10)]
  :134-136 CustomerIndirectVMs, ContactPersonVMs, ItemSubVMs —
           plain lists, no validation attributes

Two oddities to RECORD, not fix:
  - StateName is [Required] on the wire but is a DERIVED field:
    BR-CUST-008 says the server denormalises it from the matched
    State row. A client that omits it gets a 400 for a field the
    server was going to overwrite. Record it; changing the
    ViewModel is a contract change needing its own decision.
  - Distance is [Range(1, ...)] on a non-nullable double, so 0 is
    invalid and there is no "unspecified".

BUSINESS RULES TO PRESERVE
ALL BR-CUST rules from
docs/kb/business-rules/customer-master-rules.md, created by
M2-D02-01. Read that document; it is authoritative. This
controller enforces NONE of them itself — it calls the service
and translates the result. In particular:
  - Validation failures -> 400 ProblemDetails with errors keyed
    by field; the service's message strings survive verbatim.
  - The duplicate-name refusal (BR-CUST-001) and the three
    delete refusals (BR-CUST-014) -> 409, with title carrying
    the service's message VERBATIM. Not 400. Not paraphrased.
    Precedent: BR-SO-001's migration note and ADR-002 section 4.
  - BR-AUTH-002 — deny by default. A missing UserRight row means
    no right.
  - The controller adds NO business logic. If it needs to,
    M2-D02-01 was incomplete.

TARGET IMPLEMENTATION
1. V.SMART/V.SMART.Api/Controllers/CustomersController.cs
   exists, roughly 140 lines or fewer, with no if that decides a
   business outcome, no IUnitOfWork, no DbContext, no Regex, and
   no AutoMapper use beyond what the template sanctions.
2. Five endpoints under /api/v1/customers matching ADR-002
   section 2's shape exactly.
3. [RequireScreen("Customer")] on the class;
   [RequireRight(Right.View)] on both GETs, Create on POST, Edit
   on PUT, Delete on DELETE.
4. A TYPED QUERY DTO for the list filters, adapted to the
   service's dictionary inside the controller.
5. Update rejects a route/body id mismatch with 400.
6. Exactly one error shape — ProblemDetails. 400 validation,
   403 right denied, 404 not found, 409 business refusal with
   the message verbatim, 500 with traceId.
7. POST returns 201 + Location, and the returned CustomerVM
   INCLUDES THE PERSISTED CHILDREN WITH THEIR GENERATED IDS —
   otherwise the React form cannot re-render after save without
   a second round trip.
8. Customer's row exists in the M2-A03 permission-matrix
   harness, covering every endpoint x every right.
9. API integration tests covering the CRUD path, the
   child-collection round trip, each error status, and tenant
   isolation on the {id} routes.
10. OpenAPI documents all five endpoints with accurate response
    types, and M2-B10 regenerates the TypeScript client cleanly.
11. dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj -> 0
    errors.
12. V.SMART/V.SMART.Shared/ is UNCHANGED. git diff --stat on it
    is empty — that is the proof the backend was extended, not
    rewritten.

RELEVANT FILES
Verified to exist on 2026-08-12:
  V.SMART/V.SMART.Api/Controllers/CurrencyController.cs    (98)
    — read-only reference; THE pattern
  V.SMART/V.SMART.Api/Program.cs
    — READ-ONLY in this task
  V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/
    IBusinessService/IMasterServices/IGeneralService/
    ICustomerService.cs
  V.SMART/V.SMART.Shared/BusinessLayer/BusinessService/
    MasterService/GeneralService/CustomerService.cs        (279)
  V.SMART/V.SMART.Shared/ViewModels/MasterViewModel/
    GeneralViewModel/CustomerVM.cs                         (141)
  V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs
  V.SMART/V.SMART.Shared/Data/Master/General_Module/
    Customer.cs, CustomerIndirect.cs, ContactPerson.cs

TO BE CREATED:
  V.SMART/V.SMART.Api/Controllers/CustomersController.cs
  a query-DTO file, in whatever location M2-B03 chose for
    Currency's
  API integration tests. If no API test project exists, one must
    be added and wired into CI. CHECK WHAT M2-A02 CREATED FOR
    ITS DENIAL TESTS AND EXTEND THAT rather than starting a third
    project. Note that M0-12-01 creates a V.SMART.Shared UNIT
    test project, which is not necessarily a
    WebApplicationFactory-based integration one.

CONSTRAINTS
- Do not rewrite existing business services.
- Do not reimplement ERP business logic in React/TypeScript.
- Do not change unrelated modules.
- Do not make assumptions without checking source code.
- Do not repeat completed investigations.
- Do not modify the database schema unless this task explicitly
  authorizes it. IT DOES NOT.
- Preserve existing API behaviour wherever practical.
- The server remains authoritative for validation, calculations,
  permissions and document numbering.
- Do not start another task after completing this one.
- DO NOT MODIFY ANYTHING UNDER V.SMART/V.SMART.Shared/ — the
  entire project. No service, ViewModel, entity, mapping,
  repository or Razor change. If something there needs changing,
  the extraction (M2-D02-01) was incomplete: REPORT IT AND STOP.
- Do not modify V.SMART/V.SMART.Api/Program.cs. DI belongs to
  M2-B07, middleware to M2-A05/M2-A06, versioning to M2-B01. In
  particular DO NOT add
  AddScoped<ICustomerService, CustomerService>() — that line is
  M2-B07's, and adding it here starts the 60-controller
  copy-paste that M2-B07 exists to prevent.
- Do not modify CurrencyController.cs.
- Do not modify the [RequireScreen]/[RequireRight] filter
  implementation — M2-A01-02 owns it. If it cannot express what
  Customer needs, that is a finding.
- Do not write React code.
- Do not expose GetCustomerListAsync.
- Do not sort the current page in memory. The service hard-codes
  OrderByDescending(CustId) (CustomerService.cs:65) and accepts
  no sort parameter. If M2-B02's contract mandates a sort
  parameter, adding it needs a service change, which this task
  forbids — RAISE IT AS A FINDING and let M2-B02 / M2-D02-01 own
  the fix.
- Do not "improve" any BR-CUST behaviour. Assert the LEGACY
  behaviour in tests, including the silent discard of
  blank-named child rows (BR-CUST-012).

EXECUTION PROCEDURE
1. Read the referenced knowledge-base documents, including
   customer-master-rules.md and M2-B03's template.
2. Search the investigation registry.
3. Confirm whether the required investigation already exists.
4. CHECK M2-A08's OUTPUT for row-level scoping via
   User.StateCodesCsv (Q-08). If scoping is real, the list
   endpoint must honour it and you must say how. If M2-A08 found
   none, record that negative result and continue.
5. Read ICustomerService.cs as it NOW is. If there is no
   single-call save or no independently callable validation,
   STOP and report Blocked.
6. Confirm AddVSmartDomain() registers ICustomerService. If it
   does not, STOP.
7. Verify every assumption against code before acting on it.
   Re-verify every line number in this prompt.
8. Create the typed query DTO: pageNumber, pageSize, and the
   five filters BR-CUST-016 supports (customer, createdBy,
   fromDate, toDate, status). Type the dates as DateTime? /
   DateOnly?, NOT string. Adapt to the service's
   Dictionary<string, object> inside the controller, preserving
   the EXACT key names the service switches on
   (CustomerService.cs:94-125): Customer, CreatedBy, FromDate,
   ToDate, Status.
9. Write the controller following M2-B03's template
   file-for-file. Each action: bind, authorize by attribute,
   call ONE service method, translate the result.
10. Map results to status codes in ONE small private helper, not
    repeated per action.
11. Handle the children: POST/PUT accept a CustomerVM carrying
    CustomerIndirectVMs and ContactPersonVMs; the response
    echoes them WITH GENERATED IDS. Verify against the DATABASE,
    not the in-memory object. If the round trip does not work,
    the BR-CUST-017 mapping fix from M2-D02-01 is incomplete —
    report it rather than patching the mapping here.
12. Add Customer's row to the M2-A03 permission-matrix harness:
    every endpoint x every right, asserting 403 wherever the
    right is absent, including a user with NO UserRight row for
    Customer at all.
13. Write the API integration tests.
14. Verify OpenAPI, then have M2-B10's generator produce the
    TypeScript client. A shape that generates awkwardly is a
    FINDING TO REPORT before 58 more controllers copy it.
15. Run the required verification commands and tests.
16. Verify Blazor still works: create a customer through the
    API, open it in Blazor /customer/update/{id}, confirm every
    field and BOTH child collections are correct; then edit in
    Blazor and re-fetch through the API.
17. Review the full git diff.
18. Update documentation.
19. Update the investigation registry.

EXPECTED FILE CHANGES
Created:
  V.SMART/V.SMART.Api/Controllers/CustomersController.cs
  the query-DTO file
  API integration test files
Modified:
  the M2-A03 permission-matrix harness
  docs/kb/api/api-overview.md
  docs/kb/frontend-new/feature-mapping.md
  M2-B03's controller-template document
  docs/kb/execution/tasks/M2-D02-02.md
Must not change:
  V.SMART/V.SMART.Shared/**          (all of it)
  V.SMART/V.SMART.Api/Program.cs
  V.SMART/V.SMART.Api/Controllers/CurrencyController.cs
  V.SMART/V.SMART.Shared/Migrations/**
  frontend/**

TESTS
Permission matrix (MERGE GATE, in M2-A03's harness):
  - every endpoint x every right; 403 wherever the right is
    absent; success where present; includes a user with NO
    UserRight row for Customer — deny by default.
    ADR-004: no controller merges without this row.
API integration:
  - GET list: paging (pageNumber is 1-based, matching
    CustomerService.cs:66's Skip((pageNumber - 1) * pageSize)),
    each of the five filters, totalCount correctness, and the
    fixed CustId DESC ordering
  - GET {id}: 200 with children populated; 404 for an unknown id
  - POST: 201, correct Location header, response body carrying
    GENERATED CHILD IDS, verified against the database
  - POST duplicate name -> 409, title BYTE-IDENTICAL to the
    service's message (BR-CUST-001)
  - POST/PUT invalid model -> 400 ProblemDetails with errors
    keyed by field name
  - PUT with id != vm.CustId -> 400 (the template gap being
    closed)
  - PUT child sync: one child updated, one added, one removed ->
    exactly the expected rows persist (BR-CUST-013)
  - PUT with a blank-named child -> SILENTLY DISCARDED, matching
    BR-CUST-012 exactly. Assert the LEGACY behaviour; do not
    assert an improvement.
  - DELETE on a referenced customer -> 409 with the message
    verbatim (BR-CUST-014); on an inactive customer -> 409 with
    its distinct message; on an unknown id -> 404; on a
    deletable one -> 204
  - TENANT ISOLATION / IDOR: a token for tenant A requesting an
    {id} belonging to tenant B receives 404 (or 403), never
    tenant B's data
  - unauthenticated request -> 401
Contract:
  - OpenAPI lists all five endpoints with correct response types
    and status codes; M2-B10 regenerates the TypeScript client
    with no manual fix-ups
Manual:
  - the Blazor round trip described in step 16

Commands:
  dotnet test tests/V.SMART.Shared.Tests/
  plus the API integration test project's own command — use
  whichever project M2-A02 created or this task added.

ACCEPTANCE CRITERIA
- CustomersController.cs exists and demonstrably follows
  M2-B03's codified template.
- All five routes are under /api/v1/customers and match ADR-002
  section 2's shape.
- [RequireScreen("Customer")] is present and its string matches
  ApplicationDbContext.cs:1166 EXACTLY — verified by grep, not
  by eye.
- Every action carries a [RequireRight(...)] matching its verb.
- The list endpoint takes a typed query DTO, not
  Dictionary<string, object>, and not loose string dates.
- PUT returns 400 when the route id and vm.CustId disagree.
- Exactly one error shape is emitted; grepping the controller
  for BadRequest(new { returns nothing.
- Business refusals return 409, not 400, with the service's
  message verbatim in title — asserted on the exact string.
- POST returns 201 with a correct Location and a body whose
  child collections carry generated ids.
- The controller contains no IUnitOfWork, no DbContext, no
  Regex, no Transaction — verified by grep.
- No if in the controller decides a business outcome (reviewer
  judgement, recorded in the PR description).
- git diff --stat V.SMART/V.SMART.Shared/ is EMPTY.
- git diff --stat V.SMART/V.SMART.Api/Program.cs is EMPTY.
- git diff --stat frontend/ is EMPTY.
- No file under V.SMART/V.SMART.Shared/Migrations/ changed.
- Customer's permission-matrix row exists and passes, including
  the no-UserRight-row case.
- Every API integration test listed above exists and passes.
- The tenant-isolation test passes.
- dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj reports 0
  errors and no more than 6,695 warnings.
- OpenAPI documents all five endpoints; M2-B10 regenerates the
  client with no manual fix-ups.
- GetCustomerListAsync is NOT exposed by this controller.
- The Blazor round-trip check was performed and recorded.
- KB-040 lists the five new endpoints in its api_endpoints
  frontmatter.

VERIFICATION COMMANDS
  dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj
  dotnet test tests/V.SMART.Shared.Tests/
  git diff --stat V.SMART/V.SMART.Shared/ V.SMART/V.SMART.Api/Program.cs frontend/
  git status --porcelain
  grep -n "RequireScreen\|RequireRight" V.SMART/V.SMART.Api/Controllers/CustomersController.cs
  grep -n "BadRequest(new {\|IUnitOfWork\|DbContext\|Regex\|BeginTransaction" V.SMART/V.SMART.Api/Controllers/CustomersController.cs
  grep -n "ScreenName = \"Customer\"" V.SMART/V.SMART.Shared/Data/ApplicationDbContext.cs

Note: dotnet test is valid only because M0-12-01 is a
prerequisite; before it there was no test project in the solution
(INV-023, Confirmed). The API integration tests run under
whichever project M2-A02 created or this task added — use that
project's own command.
The Api build baseline is 0 errors and 6,695 warnings, ~3 min.

DOCUMENTATION REQUIREMENTS
  docs/kb/api/api-overview.md (KB-040) — add the five Customer
    endpoints with request/response/error detail, in the same
    format as the Currency section. Note that the surface is no
    longer "2 controllers / 6 endpoints". Add to api_endpoints,
    entities, database_tables and source_files; bump
    last_verified.
  docs/kb/frontend-new/feature-mapping.md (KB-052) — Customer's
    "Existing service" cell currently implies no endpoints
    exist; update it. Bump last_verified.
  M2-B03's controller-template document — record Customer as the
    template's SECOND INDEPENDENT USER, the condition KB-080's
    M2 Definition of Done requires. Note anything the template
    did not cover — CHILD COLLECTIONS in particular — and amend
    the template accordingly.
  docs/kb/execution/tasks/M2-D02-02.md — set status: Completed
    and fill frontmatter business_rules with the BR-CUST ids
    allocated by M2-D02-01; bump last_verified.
  docs/kb/execution/task-tracker.md (KB-081) — mark M2-D02-02
    Completed ONLY IF THE FILE EXISTS; it did not on 2026-08-12.
    Do not create it as a side effect.

Record these negative results in the investigation registry:
  - "V.SMART.Shared was not modified by this task —
     git diff --stat empty. The controller wrapped the
     post-M2-D02-01 ICustomerService with zero service changes."
  - "GetCustomerListAsync was deliberately NOT exposed: it is a
     report query (CustomerService.cs:215-274) belonging to
     M2-B08's reporting surface."
  - "CustomerService accepts no sort parameter
     (CustomerService.cs:65 hard-codes OrderByDescending(CustId)),
     so the endpoint exposes none."
  - the result of the M2-A08 row-level-scoping check, whichever
    way it went.
If the child-collection round trip revealed that the template
needs a new rule, that is an AMENDMENT TO M2-B03's DOCUMENT, not
a new investigation.

GIT
Branch:  migration/M2-D02-02-customers-controller
Commit:  M2-D02-02: Add CustomersController with
         permission-matrix and API tests
Do not merge. Do not push to master. Leave the branch for review.

FINAL RESPONSE
When finished, report:
1.  Task ID
2.  Task status (Completed / Needs Review / Blocked)
3.  What was implemented
4.  Files created
5.  Files modified
6.  Files deleted
7.  Tests executed
8.  Test results
9.  Documentation updated
10. Investigation registry updated
11. Architectural decisions taken
12. Unexpected findings
13. Assumptions made
14. Deviations from this task
15. Recommended next task

In addition, because this controller is the template's SECOND
INDEPENDENT USER and 58-78 more will copy it, state explicitly in
item 11:
  - the final line count of CustomersController.cs, and whether
    it stayed thin
  - whether the extracted ICustomerService shape was sufficient,
    or whether you were tempted to put logic in the controller
    (and where)
  - how child collections were carried through POST and PUT, and
    whether M2-B03's template needed amending to cover them —
    this is the finding that matters most for M3 and M4
    documents
  - whether one ProblemDetails mapping helper covered every
    case, or whether any endpoint needed special-casing (if so,
    R-24 is not closed)
  - whether the generated TypeScript client came out clean
  - the answer to the M2-A08 row-level-scoping question and what
    you did about it

IMPORTANT:
EXECUTE ONLY THIS TASK.
DO NOT START THE NEXT TASK.
============================================================
```
