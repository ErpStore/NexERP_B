---
doc_id: KB-004
title: Open Questions and Unknowns
module: meta
status: active
confidence: n/a
last_verified: 2026-08-21
---

# Open Questions and Unknowns

Each entry states who can answer it, why it matters, and what it blocks. **Nothing here has
been assumed away in the analysis** — where a question affects a recommendation, the
recommendation says so.

## Product / business decisions (need the product owner)

| ID | Question | Why it matters | Blocks |
|---|---|---|---|
| ~~**Q-19**~~ **ANSWERED 2026-08-12 — owner decision, superseding the same-day correction** | ~~Is the public visibility of `https://github.com/ErpStore/NexERP_B.git` intended?~~ **Yes — Kumar (confirmed repo owner) deliberately set the repository to public on 2026-08-12**, after the sequence of findings below. Timeline, same day: (1) INV-029 originally reported "public" based on a flawed test (`git ls-remote` "succeeding without authentication" — actually silently authenticated by Windows Git Credential Manager); (2) INV-034 corrected this to **private**, re-tested with the credential helper explicitly disabled; (3) the owner then **deliberately made it public**, re-verified the same way as INV-034 (credential-helper-disabled `git ls-remote` succeeds; unauthenticated REST call returns `200`) — this is not a repeat of the original flawed test. **As of this decision, commit `c12c5b2`'s SA password, production host `154.61.76.112,1533`, `bspl` credential, and the JWT secret introduced into `master`'s history during M0-00 (see R-02) are genuinely reachable by anyone on the internet — treat all of them as harvested from this point forward.** See [KB-085](execution/M0-00-baseline-decisions.md#repository-visibility-correction-inv-034). | Now resolved as intended-public. Rotation (M0-04) and the history purge (M0-05) are urgent for real, not hypothetically — the credentials are live-exposed as of the date above. | **M0-04, immediately — this is no longer a "before it's exposed" rotation, it's an active-exposure one.** Also M0-05 |
| **Q-01** | Is the silent stock under-issue (R-07 / BR-STK-002) a bug or relied-upon behaviour? `TrackStockUsageAsync` errors only when *no* batch exists; if batches exist but total balance is short, it allocates what it can and returns success, leaving the ledger unbalanced. | **ANSWERED 2026-08-19 by the repository owner: PRESERVE BUT SURFACE.** In his words: *"preserve but surface we can plan it after the Milestone-2 is done"*. **The decision:** the API reproduces today's allocation behaviour exactly — a short issue still succeeds and still allocates what is available — but the shortfall is **no longer silent**: it is returned to the caller and surfaced in the UI. This is Option B *with visibility*, which this task's own acceptance criteria already require of Option B ("addresses **visibility**, not merely 'leave it as it is'"). **Rationale, recorded so it is not re-litigated:** tightening risks blocking legitimate back-dated entry where stock is recorded after the issue, and no evidence exists either way about tenant reliance on the drift — that was `Unknown` and not determinable from source. Preserving keeps every existing workflow working; surfacing stops the drift being invisible. **Scope split, explicitly:** the *decision* is made now and closes G0 criterion 7. The *implementation* of surfacing is **deferred until after Milestone 2** by the owner, and needs a task id that does not exist yet — it is not covered by `M0-11`, whose deliverable is `ADR-006` (the brief), nor by any current M2 task. **Two things the implementation must still address when scheduled:** (a) the orphan `StockIssue` row committed at `StockManagerService.cs:154-155` *before* allocation is attempted — a refused issue still writes a row for the full quantity, so "refused" is not clean today either; (b) the four `S13`–`S16` characterisation tests pin the current behaviour and must be updated in the same commit as any change, or they will correctly go red. Original framing retained: determines whether the API tightens or preserves the behaviour. Tightening could block legitimate back-dated entry; not tightening perpetuates ledger drift. **2026-08-19 (M0-13): the behaviour is now PINNED by green characterisation tests, so this question is being asked about a measured baseline, not about unpinned code.** The tests, in `tests/V.SMART.Shared.Tests/Services/StockManagerServiceCharacterisationTests.cs`, are `S13_R07_IssueOrUpdateStock_WhenNoBatchHasBalance_ThrowsNoAvailableStockToIssue`, `S14_R07_IssueOrUpdateStock_WhenBatchesExistButTotalBalanceIsShort_SilentlyUnderAllocatesAndDoesNotThrow` (drift asserted as exactly `70m`), `S15_R07_IssueOrUpdateStock_WhenReIssueIncreasesQuantityBeyondAvailableStock_SilentlyUnderAllocatesOnTheUpdatePathToo` (drift `95m`, update path) and `S16_R07_IssuingOneHundred_ThrowsAgainstZeroStock_ButSilentlyDriftsByNinetyNineAgainstOneUnit` — the last stating the decision in one line: **100 requested against 0 stock is refused; 100 against 1 succeeds and loses 99 silently.** Two further facts the answer must cover: (a) a *refused* issue still leaves an orphan `StockIssue` row for the full quantity, because the row is committed at `StockManagerService.cs:154-155` before allocation is attempted; (b) whether any tenant actually relies on the drift is **not determinable from source** — it needs production data or an owner's statement. **M0-11 turns these tests into the decision brief.** | Phase 4.2 (Inventory), and the `IStockManagerService` test suite |
| **Q-23** *(next free id after Q-22; raised 2026-08-19 by M0-12-02)* | Is it intended that a **fixed** header discount does not reduce the item-wise taxable base? When `DiscAmtOrPer` is `true`, `proportionalDiscount` is forced to `0` (`V.SMART/V.SMART.Shared/Services/CalculationService.cs:63-65`), so the discount reduces `TotalBasicAmount` and `TotalTaxable` but **GST is still charged on the undiscounted line amounts**. Entering the same 100 of discount as a *percentage* instead spreads it across the lines and taxes the discounted amount. Same document, same discount, different tax — decided by one boolean. | This is tax on real invoices, not a display quirk. Pinned as a **measured baseline**, both sides of the branch, by `S11_ItemWiseBranch_WhenHeaderDiscountIsAFixedAmount_ItDoesNotReduceTheTaxableBase` (1000 gross less 100 fixed discount → CGST **90**, i.e. 9% of 1000, not 81) and `S11_ItemWiseBranch_WhenHeaderDiscountIsAPercentage_ItIsSpreadAcrossLinesByProportion` (same discount as 10% → CGST **81**) in `tests/V.SMART.Shared.Tests/Services/CalculationServiceCharacterisationTests.cs`. So the question is asked about pinned code. **Not determinable from source** — whether it is a deliberate commercial convention or a defect needs the product owner. If it is a defect, the fix belongs in `CalculationService` once, so Blazor and the future `POST /api/documents/calculate` get it together; the two `S11_` tests must then be updated in the same commit. Note the parallel with **Q-01**: a pinned surprising behaviour awaiting a product decision, not an engineering one. | `POST /api/documents/calculate` (M2), and any parity test asserting Blazor/API agreement on discounted documents |
| **Q-34** *(next free id after Q-33; raised 2026-08-20 by M2-A06)* | When a delete guard returns `(false, message)`, is the refusal always a *business-rule* refusal? Three confirmed counter-examples in one service: `CurrencyService.cs:197` returns `"Currency not found or already removed."` (404 semantics), `:120` returns `"Currency not found."` on the update path, and `:208-211` returns `"Unexpected error occurred while validating Currency."` from a `catch (Exception)` — an **infrastructure fault travelling down the business-refusal channel**. The same tuple shape is used by 79 methods across 61 services (INV-040), so whatever is true here is true at scale. | M2-A06 chose the only option that cannot invent a rule: **the boolean is the signal, the string is never parsed**, so all three are reported as `409`. That is honest but imprecise — a client sees `409` where `404` or `503` would be correct, and the R-19 failure mode (an outage reported as a business refusal) survives inside the tuple even though M2-A06 closed it for exceptions. Fixing it properly means either the domain team classifying the guards, or a refusal type in `V.SMART.Shared` — which is live under Blazor Server and was out of M2-A06's scope. **Not determinable from source:** one string carries two meanings. | M2-B03 (the controller template every module copies), and any React error handling that branches on status |
| **Q-09** | Is the `/register` route still wanted? Self-registration into a multi-tenant ERP with a per-screen permission model is unusual. | Determines whether the React app has a public registration surface at all. | Phase 2 auth |
| **Q-11** | What is the intended future of the MAUI desktop/mobile app? Is it decommissioned once React is responsive, or does it remain for offline shop-floor use? | Determines whether the domain layer must keep supporting a third host, and whether shop-floor React screens need offline capability. | Phase 6, and the shop-floor Production Log design (4.4) |
| **Q-12** | Which tenants are in production, and what are their data volumes? Five template folders exist (`acucom`, `sns`, `srinuenggind`, `sharadaelectrou1`, `default`) — is that the full list? | Sizing for performance testing, pilot-tenant selection, and migration sequencing. | Phase 5 load testing, Phase 6 rollout |
| **Q-13** | Is there a feature freeze on the Blazor app during migration? | Two UIs over one database diverge quickly if new features land only in Blazor. | Phase 3 onward |
| **Q-62** *(claimed 2026-08-21 by M0-10; from the reserved Q-60…Q-64 block)* | Is it **intended** that a Sales DC, a Tax Invoice and an Export Invoice can each be deleted with **no eligibility check at all**, while the Sales Order upstream of them refuses deletion precisely *because they exist* (BR-SO-001)? The integrity model guards upstream only. | It reads either way and the code cannot say which. As a deliberate **unwind order** — delete the DC, then the order becomes deletable — it is coherent. As an **omission** it is a referential-integrity hole in the most financially significant documents in the system. Evidence: `MfgPoService.cs:465-565` (the guard) vs `MfgDcService.cs:322`, `MfgInvService.cs:936`, `ExpInvService` (no guard). [KB-061](risks/delete-guard-audit.md) §5.1, R-63. | `M0-10d`; every `DELETE` endpoint for Manufacturing Work (module 9) |
| **Q-63** *(claimed 2026-08-21 by M0-10)* | The Payment, Receipt and Advance Adjustment delete buttons are **live with their guard commented out** (`PaymentList.razor:451`, `ReceiptList.razor:452`, `AdvanceAdjustmentList.razor:433`). Restore a guard, or accept unguarded deletes in Cash Flow? | **The commented code cannot simply be uncommented.** It calls `EnquiryPurchaseService.CanDeleteEnquiryAsync(enquiry.EnquiryId)` — a copy-paste from the Purchase Enquiry page that never applied to Payments; the surviving field name `DeleteEnqID` in `PaymentList.razor:457` is the fingerprint. Restoring it verbatim would guard the wrong document. So the real question is what *should* block deleting a Payment, given TDS and adjustment records downstream. Product decision. [KB-061](risks/delete-guard-audit.md) §3.4, R-63. | `M0-10e`; the Cash Flow module's migration wave |
| **Q-64** *(claimed 2026-08-21 by M0-10)* | **Three** guards can **never refuse** — `StoreInterTransService.cs:211`, `GroupingService.cs:136`, `EstimateService.cs:735` — and a fourth, `AppointmentLetterService.cs:43`, refuses only on a `Staff`-name match (`:58-61`) and is inert after it. Three of the four are wired to live delete buttons. Are Groupings, Estimates and Appointment Letters genuinely always deletable, or are these **unfinished stubs**? | Two of the four are visibly unfinished rather than deliberately permissive: `EstimateService.cs:747-749` builds `EstimateSubIds` and discards it, and `AppointmentLetterService.cs:65-67` does the same with `PoSubIds` — the shape of a guard someone started and did not finish. `StoreInterTransService.cs:211` has a one-line body. But "unfinished" is an inference about intent, and only the owner can confirm whether these documents have downstream references worth blocking. [KB-061](risks/delete-guard-audit.md) §3.3, R-62. | `M0-10d`; the Masters, Planning and HR migration waves |

## Technical unknowns (need code, database, or ops access)

| ID | Question | Current state | Blocks |
|---|---|---|---|
| **Q-02** | How are EF migrations currently rolled out to each tenant database? | No deployment script anywhere in the repo. **Still Unknown for production.** **2026-08-13 (M0-01-03):** `db/RUNBOOK-rebuild-tenant-database.md` §5 records one candidate method for a single, directly-reached database — `dotnet ef database update --context ApplicationDbContext --connection <explicit>` — as a step in the rebuild drill this task authored. This is **UNVERIFIED** (no database access in the session that wrote it) and is explicitly **not** a description of how production tenants are actually migrated today, nor a per-tenant rollout procedure. Treat it as one input for M6-06 to evaluate, not an answer. **2026-08-21 (M0-01-03 drill): that method is now VERIFIED as one working method, and the question is still Open.** Executed against SQL Server 2019 Express: `dotnet ef database update --project V.SMART\V.SMART.Shared\V.SMART.Shared.csproj --startup-project <same> --context ApplicationDbContext --framework net9.0 --connection <redacted>`, **with `$env:ConnectionStrings__DesignTimeTenantDb` set** — that environment variable is mandatory, because the design-time factory now fails fast rather than falling back to a committed credential, and it throws *before* `dotnet ef` applies `--connection` (`db/REBUILD-DRILL-LOG.md` **F1**). Result: an empty database became a 197-table tenant with **108** migrations applied in **~50 s**, plus 150 seeded `Screens` and the `Administrator` user. **What this does not do is narrow the actual question.** It says nothing about rolling out to *many* tenants, who runs it, ordering relative to a deployment, or recovery when one tenant fails mid-rollout. Status stays **Open**, owner stays **ops**, target stays **M6-06**. | Phase 0 (fresh-environment rebuild), Phase 6 |
| **Q-03** | What indexes actually exist in the production tenant databases? | Only one index-related migration (`AddInDexingToCustomer`); the EF model is not necessarily the deployed truth. **Unknown.** | R-13, Phase 5 |
| **Q-04** | Which e-Invoice/e-Way gateway does each tenant use — Alankit or eRaahi? How is it selected? | Both are wired in `E_Invoice/`. Selection is **Inferred** to be configuration- or tenant-driven, not confirmed. | INV-015, Phase 4.5 |
| ~~**Q-05**~~ **ANSWERED 2026-08-12** | Is `QrExpiryDate` enforced anywhere? | **Yes — but not in the query.** `UserRepository.GetUserByQrToken:52-60` still returns expired users (re-verified). Expiry is checked *post-query*, in **two duplicated copies**: `QrLogin.razor:50-56` and `Login.razor:422-429`. The earlier note "no other enforcement point found" was wrong. Correctness depends on which caller checks → the API must enforce it in the query or the service. | R-16, R-38, M2-A08 |
| ~~**Q-06**~~ **ANSWERED 2026-08-12** | Where are `User.TrialDays` / `User.ExpiryDate` enforced? | **`Login.razor:271-275` only** — Blazor `@code`, not the service. Three carve-outs: `!IsDesktop`, `UserId > 1`, `TrialDays > 0` (so an expired user with `TrialDays == 0` is exempt). Contains a dead `user != null` check placed *after* `user.UserId` is dereferenced. **`GetUserTrialAsync` has zero call sites — dead code.** | R-38, M2-A08 |
| ~~**Q-07**~~ **ANSWERED 2026-08-12** | Are device bindings enforced, or only recorded? | **Both, badly.** `Login.razor:277-322` gates on them (only when `UserId > 1 && (IsMobile \|\| IsDesktop)`, trust-on-first-use), but **device identity is client-asserted** via `deviceHelper.getDeviceId`/`isMobile`. `UserService.UpdateUserDeviceAsync:713-757` only **records** — never compares, never refuses, never writes `IsMobile`/`IsDesktop` — and calls `IJSRuntime` directly, so it **cannot run in an API request**. Not portable as-is. | R-38, M2-A08 |
| ~~**Q-08**~~ **ANSWERED 2026-08-12 — premise was wrong in both directions** | Where is `User.StateCodesCsv` row-level scoping applied? | **It does NOT scope customers or vendors anywhere** (Confirmed negative result). Grepping `StateCodesCsv\|StateCodes\b` across `V.SMART/` hits only write/audit paths — `User.cs:51,53-64`, `UserVM.cs:108-113`, `RegisterModel.cs:58`, `UserMapping.cs:20-39`, `UserService.cs:373,437,491,532-535` — plus `LeadService.cs:41-44,136` and `LeadsUpsert.razor:695-698,826-827`. The only two `Pages/` hits populate an **assignable-user picker**, not row security. **Real scope exists on `Leads` only**, in the *service* layer: `LeadService.GetAllLoadLeadsAsync:128-152`, one call site (`LeadsList.razor:483`). **The real risk is that it is opt-in:** `GetAllLeadsAsync:95-106` on the same interface (`ILeadService.cs:16,28`) returns every lead **unscoped** — so which method a controller calls decides whether scoping happens. It also filters **in memory** (`:133,141-144`), resolves the user by **username string equality** (`:132-134`), fails **closed** on a blank `StateCodesCsv` (zero leads, `:136-142`), and has a second divergent SQL implementation at `:35-47`. | INV-028, M2-A08 |
| **Q-35** *(next free id after Q-34; raised 2026-08-20 by M2-A06)* | Is `503` the right status for an API request whose tenant cannot be resolved, and should a caller-supplied `X-Correlation-Id` ever be honoured? | Both are **design choices M2-A06 had to make with no KB position**, so they are recorded rather than presented as settled. (a) `503` was chosen for the mid-request failure because `TenantProvider.cs:77-80` swallows every exception before returning `null`, making an unknown tenant indistinguishable from a MasterDb outage — a `4xx` would blame the caller on a coin-flip. `POST /api/auth/login` keeps its pre-existing `400` for the same condition, so the API answers **two different statuses for one root cause**; if the owner prefers one, it is a two-line change in `Middleware/ApiProblems.cs` and `AuthController`. (b) An inbound `X-Correlation-Id` is **ignored** — the id is always server-generated, since a client-controlled value can forge log correlation. If distributed tracing is later wanted, W3C `traceparent` is the mechanism (`Activity` already honours it) and no code change is needed. | M2-B11 (log correlation), M2-C04-03 (client error handling), and any future gateway that wants to stamp requests |
| **Q-10** | Do the document-number columns carry unique constraints **in the live tenant databases**? | **Wording corrected 2026-08-12.** "Not visible in the EF model" was wrong: `ApplicationDbContext.cs:579-581` declares `MfgQuote(QuoteNo, Suffix)` unique (confirmed in `ApplicationDbContextModelSnapshot.cs:19817-19818`). That is the **only** document-number unique index in the model — the snapshot's five other `IsUnique()` calls cover `User.EmailId`, `User.UserName`, `AssmblyDef`, `AssemblyDefLabour` and an `InspectId`. So 1 of ~40 document types is protected in the model, and whether even that reached the deployed databases is unknown (Q-02: no migration rollout procedure is documented). If absent, R-12 may **already** be producing duplicates — so the check must also **detect existing duplicates**, not just verify constraints. Note `MfgDcService.IsDuplicateDcNoAsync:771-790` scopes by `CustId`, so duplicates must be counted under both scopings. | R-12, M2-B12-02 |
| **Q-14** | Do the 94 stored procedures differ between tenant databases? | If they have drifted per tenant, scripting them (INV-027) is not a single-artefact job. **Input now available (2026-08-13, M0-01-02):** INV-027 is Complete — 78/82 `missing` procedures captured, source is a demo tenant (`IQSMARTDEMO_DB_2025-26`) relayed through a local database, not a nominated production tenant. This question is still open and still M0-02's to answer — the capture is a starting point to diff against other tenants, not evidence of representativeness. See `db/stored-procedures/CAPTURE-STATUS.md`. **Still OPEN and UNDECIDED as of 2026-08-17 (M0-02, tooling half, INV-030 `Partial`).** The *method and tooling* now exist and require no database: the fingerprint query gained `hash_raw` + `hash_normalised` (`db/tools/list-deployed-procedures.sql`, Query B / `FINGERPRINT_QUERY_VERSION 2`), plus `db/tools/compare-tenant-fingerprints.sh`, `db/RUNBOOK-tenant-drift-check.md`, `db/drift/README.md` and [KB-103](architecture/stored-procedure-drift.md). **No comparison has run — `db/drift/` holds zero fingerprints, so nothing here means "no drift"; it means undecided.** Blocker: a DBA with `VIEW DEFINITION` on **≥2** tenant databases plus a working tenant list (Q-12); a session may not acquire or reuse any credential. Owner: **DBA** — first candidate operator **PavanKunar**, who ran the M0-01-02 capture (`db/stored-procedures/CAPTURE-STATUS.md`); the migration lead must also decide which database the "baseline" label denotes, given that capture's provenance caveat. Consequence while it stays open: `db/stored-procedures/` is a single artefact set *by assumption*, and `db/deploy-stored-procedures.ps1` has no per-tenant path. **EXPLICITLY DEFERRED (2026-08-18) — decision by Vivek (repository owner / migration lead), the named owner.** Deferred, not left silently open: [KB-080 §7](execution/README.md) lists the G0 deliverable as "Q-14 answered **or explicitly deferred with reason**", and this is that second path. **Reason:** no DBA holding `VIEW DEFINITION` on ≥2 tenant databases has been scheduled, and a session may not acquire or reuse a credential (R-01); `db/drift/` holds zero fingerprint CSVs, so **zero tenants have been compared**. **This is a statement about the evidence, not about the tenants — it does not mean "no drift".** One fingerprint compared against nothing classifies everything `identical`, which is arithmetic, not a finding. **Risk accepted while deferred:** `db/stored-procedures/` is a single artefact set *by assumption* and `db/deploy-stored-procedures.ps1` has no per-tenant path, so a deployment could overwrite one tenant's customised procedure with another tenant's — silently, with no test to catch it (INV-023: no test project exists) — and the captured DDL's own provenance (`IQSMARTDEMO_DB_2025-26`, a demo database relayed through a local `NexGenErpDb`) means it may describe no production tenant at all. **Reopen trigger:** any tenant fingerprint CSV landing in `db/drift/`, or any per-tenant report / statutory-document surprise in the field; resume at [`tasks/M0-02.md`](execution/tasks/M0-02.md) Implementation Steps §9 — **the tooling is complete and must not be re-derived**. Hand `db/RUNBOOK-tenant-drift-check.md` to a DBA; **PavanKunar** remains the first candidate operator. **Still owed by Vivek if reopened:** which database the "baseline" label denotes, given the provenance caveat — ideally fingerprint both, labelled `baseline-demo-origin` and `baseline-relay`. | **Phase 0 — INV-027 (owned by M0-02)** |
| **Q-15** | Are there scheduled jobs outside the application (SQL Agent jobs, Windows Task Scheduler) doing ERP work? | The application has **no** background processing (INV-022 confirmed). Anything periodic must therefore live outside it — but nothing was found in the repo. | Phase 6 completeness |
| **Q-16** | What are the actual reverse-proxy / deployment topology and TLS termination? — **and, added 2026-08-21, is the uploads directory durable in it?** | **Inferred** from `UseForwardedHeaders` with cleared `KnownNetworks`/`KnownProxies` and per-tenant subdomains. Not confirmed. **M2-B06 note (2026-08-21, INV-045).** Uploaded files live on the **local filesystem**, under the host's `WebRootPath` (configurable as `FileStorage:Root` for the API) — four folder trees, each segmented per tenant. **Whether that root survives a restart, a redeploy or a scale-out is Unknown, and it depends on this question.** In a containerised deployment with no mounted volume the files are lost on every redeploy; behind more than one instance, a file uploaded to instance A is invisible to instance B, and both failures are silent. No blob storage, CDN or virus scanning exists anywhere in the repository — grepped for `BlobServiceClient`, `S3` and `Azure.Storage`, **no hits**, recorded as a negative result. M2-B06 deliberately designed no blob-storage migration: that is a decision this question has to be answered before anyone can take. Note also that `WebRootPath` resolves to a *different directory* per host (`V.SMART.Web/wwwroot` vs `V.SMART.Api/wwwroot`), which is why the API takes a configurable root at all — pointing both hosts at one path is what lets either read the other's files. | Phase 6, CORS and cookie configuration; **and the storage design behind `POST /api/v1/files`** |
| **Q-17** | Are `ScreenManagement` rows (distinct from `Screens`) used at runtime, and how do the two relate? | Both are seeded; `Screens` is clearly the permission catalogue. `ScreenManagement`'s role is **Unknown**. | Admin module (3.3) |
| **Q-20** *(claimed 2026-08-17 by M0-07 — Q-19 was the previous highest; `M3-1-01` expected to start here and must now start at Q-21)* | Are GitHub Actions **hosted** runners available and approved for the `ErpStore` organisation, and who holds the GitHub **admin** rights needed to add a required status check to `master`? | **ANSWERED 2026-08-19, except for one half.** **(a) Hosted runners — YES, and CI is now green on `master`.** The owner authorised pushing `master` (`44e3614..20be92f`, 37 commits), putting `.github/workflows/ci.yml` on `origin/master` for the first time, and confirmed the resulting run **green**. G0 criteria 5 and 6 both close on that single run. **(b) Who holds admin rights — the owner does, demonstrated.** The push returned `remote: Bypassed rule violations for refs/heads/master: - Changes must be made through a pull request.` So the branch-protection mechanism that `/rulesets` could not locate **does exist**, it requires pull requests, and the pushing identity holds **bypass** permission. **(c) STILL OPEN: no required status check is attached.** CI does not gate merges — the push succeeded despite the PR rule, and nothing made it wait for a green run. That is the remaining work, and it is a repository-settings change, not an engineering one. **Note for future sessions: direct pushes to `master` violate a configured rule and only succeed because of bypass rights — prefer a PR unless the owner says otherwise in-conversation.** Earlier partial answer retained: **hosted runners are available and approved for `ErpStore`:** `migration/M0-12-01-test-project` was pushed with the owner's authorisation, `ci.yml` triggered on `windows-latest`, and the owner reports the run **green** — the first execution of this pipeline in the repository's history. The warning gate therefore passed against the provisional baseline on a real runner. **Still unanswered: who holds the GitHub admin rights to add a required status check to `master`**, which is the half of this question that blocks the branch-protection row. **And a separate finding that explains the whole puzzle: `origin/master` does not carry the workflow at all.** Its tip is `44e3614`; `.github/workflows/ci.yml` is absent there. Local `master` is **15 commits ahead and unpushed**, and those commits include M0-07's entire CI pipeline. That is *why* CI has never run on `master` — not a runner or permissions problem, simply that the work was never published. **G0's "CI green on `master`" criterion needs those 15 commits pushed**, which is a separate and much larger authorisation than the single feature branch pushed here. Original assessment, retained: M0-07 created `ci.yml` assuming hosted `windows-latest`; an execution session cannot push, so at the time there was no green run, no run URL, and no required check. `ci/warning-baseline.json` is therefore marked `"provisional": true` and must be regenerated from the runner's own numbers on the first run — where runner and local disagree, the runner wins. If the organisation requires self-hosted runners, `runs-on:` and the whole cost argument in [KB-087](execution/ci-pipeline.md) §2 need revisiting before any of this is true. | **Gate G0's "CI green on `master`" criterion**, and the branch-protection row in [KB-080](execution/README.md) §19. Needs a human with organisation admin access — no AI session can resolve it |
| **Q-18** | What is the retention/backup policy for tenant databases and for the flat-file logs? | **Unknown.** Relevant to R-23 and to rollback planning. | Phase 6 |
| **Q-21** *(next free id after Q-20)* | Is the autonomous runner's agent-dispatch layer healthy? `M0-12-01` was dispatched to the `opus` implementer twice (2026-08-18) and **both attempts returned no result at all** — no diff, no text, no tool output, nothing for the validator to check. Attempt 1 was traced, from the workflow's own agent-completion log (visible only from inside that run), to a transient upstream `529 Overloaded` on both its `investigate` and `implement` agents. Attempt 2 failed identically, but no close-out session run afterward has access to that log, so its cause is **not independently confirmed** — it could be a second unrelated transient error, or it could be a systemic dispatch/runner problem that two `529`s coincidentally resemble. | **ANSWERED 2026-08-19 — the dispatch layer is healthy. Both failures were transient upstream `529 Overloaded`, on every agent, in both attempts.** The premise that kept this open is itself wrong: the per-agent transcripts **are** readable after the run, at `~/.claude/projects/<project>/<sessionId>/subagents/workflows/<runId>/agent-<agentId>.jsonl`. Session `9322ef8e`, run `wf_b5cfd63e-cd2` (attempt 1): `migration-investigator` (`opus`) ended `"apiErrorStatus":529, "error":"server_error"` at 16:41:00Z, `req_011CeAYN4EMJrAe6z7CZ1qX8`, **after 158,887 bytes of successful tool work**; `migration-implementer` (`opus`) ended `529` at 16:44:18Z, `req_011CeAYdkQF6u4n5sSMXvwoi`, 4,199 bytes, dying on its first call. Run `wf_8f353233-789` (attempt 2): `migration-investigator` ×2 both `529`, `migration-implementer` `529`. **No systemic fault exists** — a broken dispatch layer cannot produce an investigator that reads 158 KB of source before failing. Corroborated the same day: two runner invocations on 2026-08-19 dispatched 4 of 4 agents with `agents_error: 0`, `agents_empty_result: 0`. **The question is closed, and the gate it guarded was cleared by the owner on 2026-08-19** — *"yes, the 529 evidence clears the gate — run it"*. Recorded because the sequence matters: a session first took this evidence, moved `M0-12-01` to `Ready` and dispatched it, and was correctly stopped by the harness safety classifier — the gate reserves the confirmation for a human, and performing the check does not confer authority to declare it satisfied. The flip was withdrawn and the evidence put to Vivek, who cleared it himself. `M0-12-01` is `Ready` on **his** authority. See [`tasks/M0-12-01.md` § Execution Record (2026-08-18) — Attempt 2](execution/tasks/M0-12-01.md#execution-record-2026-08-18--attempt-2-repeated-empty-return), [`failure-log.md`](execution/failure-log.md#m0-12-01--attempt-2--2026-08-18), [`task-tracker.md`](execution/task-tracker.md) footnote 12. | `M0-12-01`, and transitively `M0-12-02`, `M0-13`, `M0-09`, `M0-06`, `M0-10`, `M0-11` |
| **Q-22** *(next free id after Q-21)* | `M0-12-01` step 14 (`execution/tasks/M0-12-01.md:289-291`) instructs the executor to "push the branch, confirm CI goes red, then revert" as proof the new CI test gate works. `CLAUDE.md` § Standing constraints forbids exactly that: *"Never merge or push without an explicit instruction in the current conversation"*, and the autonomous runner dispatches with `allowMerge=false`. Attempt 3 (2026-08-19) implemented everything else the task asks for — test project, `.sln` wiring, CI step, `INV-031` — and independently re-verified 10 of its 11 acceptance criteria `MET`, but stopped on this one: no branch was ever pushed, no hosted run ever executed, no run identifier exists. This is the same underlying gap **Q-20** already names for `M0-07`'s own CI criterion (`M0-07` was signed off `Completed` with it open, `d79e1a4`) — but here it blocks a task's completion outright rather than sitting as a footnote, so it is recorded separately for `M0-12-01`'s own tracking. | **RESOLVED 2026-08-19 — option (A), authorised by the repository owner and carried out in full.** The owner authorised the push in-conversation; the loop then ran end to end: `dec5790` pushed **green** (the first CI execution in the repository's history), `821e923` added a single deliberately-failing fact in its own file and went **red at the `Test - V.SMART.Shared.Tests` step** — confirmed by the owner and by the runner's own log (`Failed: 1, Passed: 11, Total: 12`, workspace `D:\a\NexERP_B\NexERP_B`, i.e. a hosted runner, not a workstation) — and `e642797` deleted the probe, returning the branch to green. **Criterion 6 is met**, and rather more than its literal wording: the test step provably executes on the runner, a failing test provably produces the non-zero exit that fails the job (`ci.yml`'s explicit `$LASTEXITCODE` check, which exists because a PowerShell pipeline swallows it, is confirmed working rather than merely written), and the 11 real tests are shown passing **on the runner** in the same log. One gap, stated plainly: the numeric GitHub run id was never captured — what stands in its place is commit `821e923`, which identifies the run unambiguously in the Actions history. A reviewer may prefer to paste the run URL into `tasks/M0-12-01.md`. | `M0-12-01`'s own completion (currently `Blocked`), and transitively `M0-12-02`, `M0-13`, `M0-09`, `M0-06`, `M0-10`, `M0-11` |
| **Q-24** *(next free id after Q-23; raised 2026-08-19 by M0-12-02)* | Is the decimal **scale** of `CalculationService`'s computed totals part of the contract the React client (M2-C10) and `POST /api/documents/calculate` must honour, or only the numeric value? | **Unknown, and it cannot be closed by the characterisation suite as specified.** M0-12-02 required exact `decimal` equality assertions; xUnit's `Assert.Equal(decimal, decimal)` uses `decimal.Equals`, which is numerically equal **across scales** — `18.000m` equals `18m`. So every assertion in `tests/V.SMART.Shared.Tests/Services/CalculationServiceCharacterisationTests.cs` pins the value and **not** the trailing precision: a change that altered only scale would pass green. The engine itself never rounds intermediates (`CalculationService.cs:12-114`, one `Math.Round` at `:103`), so scale falls out of decimal arithmetic and is not deliberately controlled anywhere. Pinning it needs `ToString(CultureInfo.InvariantCulture)` or `decimal.GetBits` assertions, which exceed what M0-12-02 authorised — hence this question rather than a unilateral test. It matters because JSON serialisation of `18.000` versus `18` is observable to a client, and a UI that formats server-supplied strings would render differently. | M2-C10 (decimal handling in React), the JSON serialisation settings of `POST /api/documents/calculate`, and any M2-D03 parity test comparing API output to Blazor output |
| **Q-27** *(claimed 2026-08-18 by M2-A01-01)* | Do duplicate `(UserId, ScreenId)` rows exist in the live tenant databases — and if so, should a unique index plus a deterministic `OrderBy` be added? | **Unknown, and nothing in the repository prevents duplicates.** `UserRight` carries no unique constraint or alternate key: the EF model declares only non-unique FK indexes (`ApplicationDbContextModelSnapshot.cs:4676-4680`), and `ApplicationDbContext.cs` configures no `HasIndex`/`HasAlternateKey` on the entity at all (grepped — 5 hits, all on other entities). **Confirmed.** `SyncRightsForUserAsync` cannot create one (`UserRightService.cs:47-54`), but `UserRights.razor:462` and `UserService.cs:464` create rows with no equivalent visible guard. Compounding it, `GetUserRightsWithScreensAsync` applies **no `OrderBy`** (`UserRightsRepository.cs:24-27`), so `RightsHelper`'s `FirstOrDefault` (`RightsHelper.cs:8`) is **already non-deterministic under duplicates today, in Blazor**. Answering needs a `SELECT UserId, ScreenId, COUNT(*) … HAVING COUNT(*) > 1` against ≥1 real tenant database — no session has that access. | Not `M2-A01-02` — the filter reproduces `FirstOrDefault` faithfully either way ([KB-105](architecture/server-side-authorization-spec.md) § D-2). It decides whether that reproduction is faithful-to-correct or faithful-to-a-latent-bug, and therefore whether a schema fix belongs in M2 |
| **Q-28** *(claimed 2026-08-18 by M2-A01-01)* | How does a user who authenticates **only through the API** ever acquire `UserRight` rows? | **Today, they do not — Confirmed.** Rights seeding runs on the Blazor login path only: `Login.razor:345-349` calls `SyncRightsForUserAsync`, and only when `user.UserId == 1`. `AuthController.Login` (`AuthController.cs:39-59`) does **not** call it. The two seeding paths also disagree on defaults: `SyncRightsForUserAsync` inserts rows with all four operation rights `true` (`UserRightService.cs:67-71`), while `UserService` inserts rows only when `vm.IsViewOnly` and then with `CanView = true` and everything else `false` (`UserService.cs:442-464`) — a non-`IsViewOnly` user gets no rows at all. Once ADR-004's filter is live and deny-by-default is enforced server-side, such a user receives **403 from every annotated endpoint**, including an administrator who has only ever used the API. This is pre-existing legacy inconsistency that server-side enforcement makes *visible*, not a defect introduced by M2-A01. | **`M2-A02`** — applying the filter to `CurrencyController` without settling this locks out the vertical slice on its first request. Also `M2-A07` (`GET /api/v1/me`), where an empty right set first becomes observable to the client |
| **Q-29** *(claimed 2026-08-18 by M2-A01-01)* | Is a 60-second staleness window on **revoked** screen rights acceptable, given that no cross-process invalidation exists between the Blazor and API hosts? | **The window is real and unavoidable with an in-process cache — Confirmed.** All five `UserRight` write sites execute in the Blazor host and none in the API: `UserRightService.cs:77`, `UserService.cs:464`, `EmployeeService.cs:191`, `EmployeeUpsert.razor:921`, `UserRights.razor:446,462`. Rights are edited through `UserRights.razor`, the Blazor admin screen, which a separate API process cannot observe. ADR-004 §2's "invalidated when `UserRight` rows are written" therefore has **nothing to hook today**, and the ≈60 s TTL is the only real bound on how long a revoked right keeps working over the API. Closing it needs a distributed cache (Redis pub/sub, or a change token both hosts poll) — out of M2's scope. **`M2-A01-03` landed the cache on 2026-08-20 and re-verified all five write sites unchanged (INV-037 amendment); the engineering half is settled and the product half is not.** As implemented: TTL `Authorization:RightsCacheSeconds`, default 60, **absolute**, `0` disables caching, and a configured value above 300 fails startup so the window cannot be widened silently. `IUserRightsProvider.Invalidate(tenantId, userId)` exists but has no caller that can fire, exactly as this question predicted. **Still open, and only the owner can answer it: is "a revoked screen right keeps working over the API for up to 60 more seconds" acceptable documented product behaviour?** If it is not, the fix is a distributed cache and a new task, not a shorter TTL. | The scope of `M2-A01-03`, and whether the product accepts "revocation takes effect within a minute" as documented behaviour. See [KB-105](architecture/server-side-authorization-spec.md) § 8.4 |
| **Q-30** *(claimed 2026-08-19 by M2-C01)* | ADR-003 pins Vite 6, Mantine 7, React Router 7, TypeScript 5 and ESLint-9-era flat config. As of 2026-08-19 the registry ships Vite 8, Mantine 9, React Router 8, TypeScript 7 and ESLint 10. Do we refresh the pins now, or hold until a concrete need appears? | The ADR majors are all still installable today, so M2-C01 honoured them exactly — but the drift is already sharp enough that the task file's own scaffold command (`npm create vite@latest`) now produces an ADR-violating tree (Vite 8 + `@vitejs/plugin-react@6`, which peer-requires `vite ^8`). Every later M2-C task inherits whatever this answers. Refreshing is cheap **now**, with one placeholder route and one test; it gets steadily more expensive per screen. Holding is defensible — none of the newer majors buys this project anything identified — but should be a decision, not a drift. | Nothing today. Touches every M2-C task, and would require superseding **ADR-003** (accepted ADRs are immutable) rather than editing it |
| **Q-31** *(claimed 2026-08-19 by M2-B07, INV-039; **premise corrected 2026-08-19 by M2-B07 attempt 2**)* | ~~Eight~~ **Six** services present pre-`M2-B07` in `MauiProgram.cs` but missing from `V.SMART.Web/Program.cs` — `IRouteCardRepository`, `IRouteCardSubRepository`, `IProductionReturnAssyRepository`, `IProductionReturnAssySubRepository`, `IProductionSCNAssyRepository`, `IProductionSCNAssySubRepository` — would have thrown a DI resolution error in any Blazor screen that injected one. Symmetrically, **seven** were registered only in Web and missing from MAUI: `IAssemblyDefLabourService`, `IEstimateService`, `IJobOrderRepository`, `IJobOrderSubRepository`, `ILabourTrackRepository`, `IPrPoRatingService`, `IToolCribServices`. Which of these screens are actually reachable in production, or are they dead navigation entries nobody has hit? | **Unknown — source alone cannot answer it.** Determinable only by checking the `Screens`/navigation-menu configuration, or by running each host and navigating. `M2-B07`'s `AddVSmartDomain()` unions the two graphs, so this stops mattering going forward regardless of the answer — but the answer would say whether this was a live production bug (R-26 manifesting) or latent dead code, which matters for how the fix is described in any release note. **Correction (Confirmed, 2026-08-19):** the original wording named `IContractReviewService` and `IRouteCardService` as MAUI-only. They are not — `V.SMART/V.SMART.Web/Program.cs:467` and `:518` on `master` register both, and both are duplicated within `MauiProgram.cs`, which is what made them look one-sided in the first pass. The specific claim that `/contractReviewMasterList` and `/routeCardList` were broken in Blazor therefore has **no evidence behind it** and should not be repeated. | Nothing blocked technically — `M2-B07` closes the gap either way. Informational, for whoever writes the `M2-B07` handoff or a release note |
| **Q-32** *(next free id after Q-31; raised 2026-08-19 by the M0 milestone-review correction)* | The tenant row in `NexGenErpDb_Master.Tenants` stores its per-tenant connection string **in plaintext, including `User Id=sa` and that account's password**. Any process that can read one row of the `Tenants` table therefore obtains `sa` on the database server. Is this the intended production provisioning model, or an artefact of this development workstation? And if it is production's model, what is the migration path — integrated auth, a per-tenant least-privilege login, or a secret store keyed by tenant? | **Unknown, and deliberately not investigated further.** Confirmed to exist by direct observation on 2026-08-19 (`select * from Tenants` on `.\SQLEXPRESS` via Windows integrated auth); the password value itself was **redacted on sight and never recorded**, per the standing constraint that no session may acquire or reuse a credential (Q-14 / R-01). Whether production's master database looks the same cannot be established from this workstation, and confirming it would require production access nobody on this project has. **Note this is a different exposure from R-01.** `M0-04` (rotate) and `M0-05` (purge history) both address secrets **in the repository**; neither touches a secret sitting in a database row, and rotating the `sa` password would *break* every tenant row that embeds it until they are rewritten. So the two remediations interact, and doing R-01's rotation without answering this question first would take the ERP down. | `M0-04`'s rotation plan directly — it must not be executed until this is answered. Also any multi-tenant provisioning work (Q-02, Q-26) and `M2-A02` |
| **Q-33** *(next free id after Q-32; raised 2026-08-19 by M2-C04-01)* | Should `UserThemePreference` become tri-state so a user can persist the `system` colour-scheme preference, or does `system` resolve client-side to a boolean before it is ever persisted? | **Undecided — and it must be decided before any settings endpoint is written, not after.** The facts are Confirmed. The entity is four members and one of them is the answer: `public bool IsDarkMode { get; set; } = false;` — `V.SMART/V.SMART.Shared/Data/Master/MasterScreeenManagement_Module/UserThemePreference.cs:20`, whole file 22 lines. There is **no representation for `system`**, and KB-051 §Tokens specifies `system` as the default. **Negative result, and it is the one that justifies local-only persistence for now:** the service has no HTTP surface at all — `git grep -ni theme -- V.SMART/V.SMART.Api` returns exactly one hit, a *comment* at `V.SMART/V.SMART.Api/Program.cs:117`, and `V.SMART.Api/Controllers/` holds only `AuthController` and `CurrencyController`. So M2-C04-01 persists the preference in `localStorage` and changed nothing server-side. Two further constraints whoever answers this must weigh: (a) `IUserThemePreferenceService` **cannot be lifted into the API as-is** — it injects `IJSRuntime` for `localStorage` and is one of the seam-coupled registrations unresolvable in the API host (`ServiceCollectionExtensions.cs:238,348`; INV-039), so an endpoint means refactoring the service, not just adding a controller; (b) `UserThemePreference` is a `DbSet` on `ApplicationDbContext` (`:128`), i.e. the **per-tenant** database, so one user across two tenants already has two independent theme rows and no single answer. A third option therefore exists and may be the cheapest: store the preference client-side permanently and treat it as device-scoped, which is what most products do. **Do not extend the schema without answering this** — it is a migration against every tenant database. | **M3-3 (Admin & Settings)**. Nothing in M2 is blocked: React persists locally today, and Blazor's own `ThemeStateService` + `UserThemePreference` path is untouched and keeps working. Owner: **product + backend** |
| **Q-65** *(next free id after **Q-64**, which is held by the unmerged `migration/M0-10-candelete-guard-audit` branch — checked with `git branch --no-merged master` per KB-093's id-allocation process note; raised 2026-08-21 by the M0-01-03 rebuild drill)* | `V.SMART/V.SMART.Shared/Migrations/20260324053747_AddnewTemperveryTable.cs` is the **only** migration file in the repository with no `.Designer.cs` companion, so EF Core does not recognise it as a migration and **has never applied it to any database**. Delete it as dead weight, or generate the missing Designer file? | **Confirmed by direct observation, 2026-08-21.** 109 migration classes on disk, **108** rows in `__EFMigrationsHistory` after a full rebuild, and the set difference is exactly this id. Every other migration file has its partner (verified by iterating all of them). `git log` shows it arrived in the initial `Add project files.` commit, so no database — rebuilt or live — has ever had it applied. Its `Up()` is ~100 `RenameTable` calls that move every table into the `dbo` schema **while keeping the same name**, i.e. almost certainly auto-generated noise against tables already in `dbo`. ⚠ **Do not resolve this by generating the Designer and applying it** — that would rename ~100 tables on every existing database. Deleting the file is the likely right answer but it is a schema-affecting decision, and M0-01-03 was not authorised to make one. Owner **Vivek**. | `db/REBUILD-DRILL-LOG.md` **F2**; the corrected migration counts in **F3** and [KB-012](architecture/database-architecture.md) |
| **Q-36** *(next free id after Q-35; raised 2026-08-20 by M2-B02)* | The Currency list's **status filter filters nothing, silently.** `CurrencyList.razor:758-760` writes `SearchFilters["Status"] = selectedStatus`, but `CurrencyFilterBuilder`'s `field switch` has no `"Status"` case (`CurrencyService.cs:193-207`) and the `Currency` entity has no status column (`Data/Master/Accounts_Module/Currency.cs:9-29`), so the key falls through `_ => query` (`:206`) and is discarded. The surrounding code is visibly copy-pasted from another screen (`#region Job order Status Changes`; a toast reading "Failed to Load Machine List filters."). Is this dead copy-paste to delete, or a lost feature a user expects? | **Not determinable from source** — the code cannot say whether anyone ever relied on it. It is **not** M2-B02's to fix (out of scope, and the fix differs completely between the two answers). It is recorded because it is the concrete, in-production evidence for [ADR-002 §2a](decisions/ADR-002-rest-api-layer.md)'s rejection of passing `sort` through the filter dictionary: an unrecognised key is swallowed and the request still answers 200. Anyone auditing filter keys should assume the same class of defect exists on other screens — 67 `*FilterBuilder` classes all end the same way. | The Currency module's own migration wave, and any audit of filter keys across the 67 filter builders |
| **Q-60** *(claimed 2026-08-21 by M0-10; from the reserved Q-60…Q-64 block)* | Should delete guards run **inside** the delete transaction? Today **2 of 79** do. | **Confirmed.** Only `EmployeeService.cs:162 DeleteEmployeeAsync` (transaction `:164`, guard `:174`) and `ProductionLogService.cs:275 DeleteProductionLogByLogId` (`:277`, guard `:288`) call a guard transactionally. The other 77 are advisory, called from Razor `@code` (67 pages) plus `CurrencyController.cs:101`, in a check-then-act pattern **with a user round-trip in the gap** — `HandleDelete` runs the guard and opens a confirmation modal; `ConfirmDelete_Click` deletes without re-checking (`CurrencyList.razor:597/630`, `ItemList.razor:855/919`, `MfgPOList.razor:1083/1118`). Moving the guard inside the transaction is a behaviour change (it converts a permitted-then-raced delete into a refusal) and it costs a second query per delete. **Decide once — it binds every delete endpoint in M3/M4**, and deciding it per endpoint guarantees an inconsistent API. [KB-061](risks/delete-guard-audit.md) §5.2, R-64. | Every `DELETE` endpoint from M3-5 onward |
| **Q-61** *(claimed 2026-08-21 by M0-10)* | When the row is **not found**, should a delete guard answer `(true, "…can be safely deleted.")` or `(false, "…not found.")`? Both conventions are live. | **Confirmed split.** Of 79 guards: **24** permissive, **35** refusing, **20** have no header-row null check at all. `CanDeleteSalesOrderAsync` is permissive (`MfgPoService.cs:474-475`). This is not cosmetic: a `GET /api/v1/<doc>/{id}/can-delete` must pick one, and the two answers differ for a **concurrently deleted** row — permissive says "go ahead" about a row that is already gone; refusing says "not found", which a client will likely render as an error. Related to Q-34 (whether a `(false, …)` is always a *business* refusal). [KB-061](risks/delete-guard-audit.md) §5.3. | `M0-10c`; the `can-delete` endpoint contract in ADR-002 |

| **Q-37** *(next free id after Q-36; raised 2026-08-20 by M2-A07's close-out validation)* | `TenantProvider.cs:46-58` falls back to **host-based** tenant resolution when a JWT's `TenantId` claim names no row in `Tenants`, while `UserRightsProvider` keys its rights cache on the **claimed** tenant id (`UserRightsProvider.cs:50-53`). Observed live: a token with an unresolvable `TenantId` claim against `GET /api/v1/me` returned `200` with a *different* tenant's 150-row rights map, not an error. Should `TenantProvider` fail closed on an unresolvable claim, or should every reader downstream (starting with `UserRightsProvider`) independently re-verify tenant existence? | **Not determinable from this task's source access — a design decision, not a fact.** It contradicts the unqualified sentence at `UserRightsProvider.cs:17-22` ("a cross-tenant read is structurally impossible through this repository") and the matching sentence in KB-040's Tenancy paragraph, so at minimum one of those needs a caveat. **Bounded today**: `AuthController` only ever mints a `TenantId` claim for a tenant that exists, so the fallback path is not reachable through the shipped login flow — it becomes live the moment any other issuer, a hand-crafted token, or a deleted/stale tenant row is involved. See **R-44** ([KB-060](risks/technical-debt-register.md)). | `M2-A02` (the first controller enforcing rights per-request) and `M2-A08` (row-level scoping/account gates) — both should decide this before assuming tenant isolation is airtight |
| **Q-38** *(next free id after Q-37; raised 2026-08-20 by M2-C00, renumbered at merge — M2-A07 had already claimed Q-37)* | **What is `M2-C11` for, now that ADR-007 has inverted it?** `execution/tasks/M2-C11.md` specifies *archiving* the Angular pilot — a decision taken when React was the target. ADR-007 makes `frontend/vsmart-erp/` the **baseline** instead (`decisions/ADR-007-angular-stack.md:76-80`), and `M2-C00`'s own scope says "`M2-C11` adopts it as the baseline" (`execution/tasks/M2-C00.md:78-80`). Those are opposite instructions for the same task. | **Unknown — recorded, not guessed.** `M2-C00`'s scope forbids re-specifying any `M2-C` task except `M2-C01`, so this was written down rather than decided. Two readings are open: **(a)** `M2-C11` ports the pilot's auth service, guard, interceptor and PrimeNG configuration into the new workspace and then disposes of the pilot; **(b)** the pilot directory itself becomes the application. The re-specified `M2-C01` assumes **(a)** and says so explicitly (`execution/tasks/M2-C01.md` §*Two scope decisions*), leaving `frontend/vsmart-erp/` untouched. If the owner prefers (b), `M2-C01` changes paths but not substance. | `M2-C11`, and the disposal of `frontend/vsmart-erp/`. **Does not block `M2-C01`.** Owner: repository owner |
| **Q-55** *(claimed 2026-08-21 by M2-B04, from the block Q-55…Q-59 reserved for that task)* | `FundTransFilter.Bank` was typed against the **Razor component** `Bank_Pages.Bank`, not the EF entity `Banks`. M2-B04 retyped it to `Banks?` to break the UI dependency. Was the property ever *meant* to carry the bank entity, or is it dead scaffolding that should be deleted? | **Not determinable from source.** Only four files mention `FundTransFilter` at all (Confirmed — `grep -rln "FundTransFilter" V.SMART/`): its own declaration, `Repository/IRepository/IAccountRepository/IPaymentsRepository/IFundTransRepository.cs`, `Repository/AccountsRepository/FundTransRepository.cs` and `Pages/CashFlow_Pages/Payments_Pages/Fundtransactions.razor`. **Neither consumer touches the `Bank` property** — the repository's only `Bank` references are to the *entity's* own navigation, `_db.FundTrans…Include(x => x.Banks)` (`FundTransRepository.cs:43`), and the Razor page has no `.Bank` reference at all. The property is therefore **unreferenced**, so **either concrete type is behaviour-neutral today** and the retype changed no observable behaviour (Confirmed — `dotnet build V.SMART.Api --no-incremental` 0 errors, `dotnet build V.SMART.Web --no-incremental` 0 errors, both suites green, 2026-08-21). What cannot be established from source is *intent*: a property typed against a Razor component is a mistake either way, but whether the fix is "carry the entity" or "delete the property" needs someone who knows the Fund Transfer screen. Note the sibling `Expense? Expense` property immediately below it, which **is** an entity — suggesting the entity was the intent, but that is Inferred, not Confirmed. | The Accounts / Fund Transfer module's own migration wave. Nothing is blocked today |

## Questions the analysis answered — recorded so they are not re-asked

| Question | Answer | Source |
|---|---|---|
| Can the existing backend serve a React SPA without modification? | No — but additively, not by rewriting. Six specific additions required. | [KB-041](api/api-readiness-assessment.md) |
| Is there an existing API to build on? | 6 endpoints, 2 controllers. `CurrencyController` is a valid template. | [KB-040](api/api-overview.md) |
| Where is business logic? | Split: 128,518 LOC in services (reusable) and ~184,000 LOC inside Razor `@code` (must be extracted). | [KB-011](architecture/backend-architecture.md), [KB-015](architecture/frontend-architecture-existing.md) |
| Is authorization enforced server-side? | **No.** UI only. Hard blocker. | [KB-013](architecture/auth-and-permissions.md), [ADR-004](decisions/ADR-004-server-side-authorization.md) |
| Are there background jobs? | None. | INV-022 |
| Are there tests or CI? | Tests: **still none**. CI: a pipeline now exists in the repository (M0-07, 2026-08-17) but has never executed on a runner — see Q-20. | INV-023, [KB-087](execution/ci-pipeline.md) |
| How is multi-tenancy done? | Database per tenant; host- or JWT-resolved. | [KB-014](architecture/multi-tenancy.md) |
| Should reporting be rebuilt? | No — keep FastReport + stored procedures, expose over HTTP. | [ADR-005](decisions/ADR-005-reporting-and-printing.md) |
| Should the Angular pilot be converted? | No — archive it. It is a 1-screen learning spike. | [KB-015](architecture/frontend-architecture-existing.md#the-angular-19-pilot-frontendvsmart-erp) |
