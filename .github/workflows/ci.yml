# .github/workflows/ci.yml
#
# The repository's first CI pipeline. Task M0-07; documented in full at
# docs/kb/execution/ci-pipeline.md (KB-087). Read that before changing anything here.
#
# Stages, in this order and deliberately as separate steps:
#     hygiene guard -> restore -> build -> analyzer warning gate -> test
#
# It compiles. It ships nothing: there is no publish, deploy or release step
# (deployment topology is open question Q-16), and it needs no secret -- if the
# build ever starts needing a connection string, that is a finding for M0-03, not
# something to paste into Actions secrets. This repository is public.
#
# WHAT IS DELIBERATELY NOT HERE
#   * Any test project other than tests/V.SMART.Shared.Tests. M0-12-01 created
#     the repository's first one and wired the step below; it holds smoke and
#     harness tests only. Real business-behaviour assertions arrive with M0-12-02
#     (CalculationService) and M0-13 (StockManagerService).
#   * A coverage gate -- there is nothing meaningful to measure yet, and a
#     threshold set against near-zero coverage teaches the team to ignore it.
#   * `-warnaserror` / `TreatWarningsAsErrors` -- the baseline is ~6,693 warnings
#     (ci/warning-baseline.json). Warnings-as-errors would make CI permanently red.
#     The gate is a ratchet against a committed baseline instead.
#   * A BLOCKING end-to-end job. M2-C01 added the `frontend` job (blocking) and
#     the `frontend-e2e` job (continue-on-error). E2E becomes blocking when the
#     first real React screen lands (M2-D01) -- until then it asserts only a
#     placeholder heading, and a flake would fail CI for no information.
#   * Any job for the Angular pilot under frontend/vsmart-erp/. It is archived
#     by M2-C11, not built.
#
# PROJECTS BUILT, AND THE ONE EXCLUDED
#   Built:    V.SMART/V.SMART.Api  and  V.SMART/V.SMART.Web
#             (each transitively builds V.SMART/V.SMART.Shared)
#   EXCLUDED: V.SMART/V.SMART (the .NET MAUI head) -- and therefore the whole
#             solution build, `dotnet build NexGen-ERP---2025-master.sln`.
#             Why: KB-086 (docs/kb/execution/M0-15-build-baseline.md) sections 4 and 7.
#             The solution build succeeds on a developer workstation only when the
#             MAUI workloads are installed AND obj/ is fully clean; a dirty obj/
#             produced file-lock failures, and whether a workload-free hosted
#             runner can build it at all is recorded as **Unknown** -- untested.
#             Cost of the exclusion, stated plainly: a regression in the MAUI head
#             is NOT caught by CI until someone builds the solution locally.
#             Revisit when Q-11 (the MAUI app's future -- docs/kb/open-questions.md)
#             is answered. If the answer is "keep it", a follow-up task adds a
#             separate job with `dotnet workload install` on a Windows runner.
#
# RUNNER: windows-latest. See KB-087 "Runner choice" for the decision and its cost.

name: CI

on:
  push:
    # All branches. migration/* branches must be checked before review, not at it.
    branches: ['**']
  pull_request:
    branches: [master]

# Superseded pushes to the same ref are cancelled rather than queued behind each
# other -- a full cold build here is measured in minutes, not seconds.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Restore, build and gate analyzer warnings
    runs-on: windows-latest
    # Local cold builds: Api ~1m23s-2m27s, Web ~1m19s-1m20s (KB-086 section 3), plus a
    # cold NuGet restore on a runner with no warm cache. 45 = roughly double a
    # generous estimate of the whole job; it is a runaway-guard, not a target.
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # History is not needed: nothing here inspects past commits.
          fetch-depth: 1

      # M0-08's repository-hygiene guard. Fails the job (exit 1, listing every
      # offending path) if build output, IDE state or a dependency directory is
      # ever TRACKED by git. This is what keeps R-14 closed rather than
      # closed-once. M0-08 owns the script; this workflow only calls it.
      # Runs first: it is instant, and there is no point building a tree that has
      # bin/ committed into it.
      - name: Hygiene guard - no build output tracked
        shell: bash
        run: bash tools/check-no-build-output.sh

      # global.json (owned by M0-15) pins the SDK to 10.0.400 with
      # rollForward: latestFeature. setup-dotnet honours it, so the runner and a
      # developer workstation resolve the same SDK. Projects target net9.0 and
      # build through roll-forward; do NOT assume `dotnet --version` reports 9.x.
      - name: Set up .NET SDK (pinned by global.json)
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Report toolchain
        run: |
          dotnet --version
          dotnet --list-sdks

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj', 'global.json') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      # ---------------------------------------------------------------- restore
      # Its own step on purpose: a NuGet-feed failure is then diagnosable at a
      # glance instead of being buried in build output. Every build below uses
      # --no-restore, so restore happens exactly once, here.
      - name: Restore
        run: |
          dotnet restore V.SMART/V.SMART.Api/V.SMART.Api.csproj
          dotnet restore V.SMART/V.SMART.Web/V.SMART.Web.csproj

      # ------------------------------------------------------------------ build
      # --no-incremental is REQUIRED, not stylistic: an incremental build reports
      # FEWER warnings than a cold one, so a gate reading an incremental log
      # silently under-reports and passes builds it should fail.
      # -v normal is REQUIRED: the counting rule reads MSBuild's own warning
      # summary block, which lower verbosities do not print.
      # -bl produces a binary log, uploaded below -- for the first CI failure
      # nobody can reproduce locally.
      - name: Build V.SMART.Api
        run: |
          New-Item -ItemType Directory -Force -Path artifacts | Out-Null
          dotnet build V.SMART/V.SMART.Api/V.SMART.Api.csproj `
            --no-restore --no-incremental -v normal -nologo `
            -bl:artifacts/api.binlog | Tee-Object -FilePath artifacts/api-build.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Build V.SMART.Web
        run: |
          dotnet build V.SMART/V.SMART.Web/V.SMART.Web.csproj `
            --no-restore --no-incremental -v normal -nologo `
            -bl:artifacts/web.binlog | Tee-Object -FilePath artifacts/web-build.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # --------------------------------------------------- analyzer warning gate
      # Ratchet against ci/warning-baseline.json:
      #   total above baseline           -> FAIL
      #   a code absent from baseline    -> FAIL
      #   total below baseline           -> PASS + an explicit instruction to
      #                                     lower the committed baseline
      # Never -warnaserror. The counting rule is documented in the script header
      # and in ci/warning-baseline.json. Each project is gated against its OWN
      # baseline (Web's is higher than Api's -- KB-086 section 8).
      - name: Analyzer warning gate - V.SMART.Api
        run: >
          pwsh tools/compare-warnings.ps1
          -LogPath artifacts/api-build.log
          -BaselinePath ci/warning-baseline.json
          -Target V.SMART.Api

      - name: Analyzer warning gate - V.SMART.Web
        run: >
          pwsh tools/compare-warnings.ps1
          -LogPath artifacts/web-build.log
          -BaselinePath ci/warning-baseline.json
          -Target V.SMART.Web

      # =============================================================== tests ===
      # Added by M0-12-01, at the placeholder that task reserved. Runs AFTER the
      # warning gates on purpose: a build that regressed its analyzer baseline is
      # a failure regardless of what the tests say, and failing fast there keeps
      # the red-CI reason unambiguous.
      #
      # `dotnet test` restores and builds the test project ITSELF -- no
      # --no-restore, no --no-build. The Restore step above deliberately restores
      # only V.SMART.Api and V.SMART.Web, so nothing has restored this project;
      # --no-restore or --no-build here fails with a missing assets file. The
      # incremental cost is small: V.SMART.Shared is already built by the two
      # builds above, so this compiles the test assembly only.
      #
      # No `--no-incremental` here: unlike the build steps, nothing counts
      # warnings from this log, so an incremental compile is fine.
      #
      # A failing test fails the job through dotnet test's own non-zero exit code
      # (checked explicitly, because a PowerShell pipeline swallows it).
      - name: Test - V.SMART.Shared.Tests
        run: |
          dotnet test tests/V.SMART.Shared.Tests/V.SMART.Shared.Tests.csproj `
            -v minimal -nologo `
            --logger "trx;LogFileName=V.SMART.Shared.Tests.trx" `
            --results-directory artifacts/test-results
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # Same artefact convention as the build logs below: one upload, name keyed
      # by run id, 14-day retention. No new convention is introduced.
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ github.run_id }}
          path: artifacts/test-results/*.trx
          retention-days: 14
          if-no-files-found: warn

      - name: Upload build logs and binary logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ github.run_id }}
          path: |
            artifacts/*.log
            artifacts/*.binlog
          retention-days: 14
          if-no-files-found: warn

  # ======================================================== frontend (M2-C01) ===
  # The React app at frontend/nexgen-web/. A SEPARATE job, not extra steps on
  # `build`: it shares no toolchain, no cache and no failure mode with the .NET
  # build, and running them in parallel keeps either one's red signal readable.
  #
  # RUNNER: windows-latest, matching `build` and -- more importantly -- matching
  # the workstation where every command below was verified locally (M2-C01).
  # ubuntu-latest would be faster and cheaper; package-lock.json does carry the
  # linux optional binaries for rollup and esbuild, so the switch is available.
  # It is not taken here because it would make the job's first ever run its first
  # ever run on an unverified platform.
  #
  # `npm ci`, never `npm install` -- CI must install exactly what the committed
  # lockfile pins.
  frontend:
    name: Frontend - typecheck, lint, test and build
    runs-on: windows-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: frontend/nexgen-web

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # Same guard as `build`, for the same reason: instant, and there is no
      # point installing into a tree that has dist/ or node_modules/ committed.
      - name: Hygiene guard - no build output tracked
        shell: bash
        working-directory: .
        run: bash tools/check-no-build-output.sh

      # node-version-file pins from .nvmrc (22) so the runner and a developer
      # workstation agree. package.json declares engines >=22 with an open upper
      # bound -- see M2-C01's report and frontend/nexgen-web/README.md.
      - name: Set up Node (pinned by .nvmrc)
        uses: actions/setup-node@v4
        with:
          node-version-file: frontend/nexgen-web/.nvmrc
          cache: npm
          cache-dependency-path: frontend/nexgen-web/package-lock.json

      - name: Report toolchain
        run: |
          node --version
          npm --version

      - name: Install (npm ci)
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      # --max-warnings=0 is inside the script. A warning is a failure here from
      # commit one, including every jsx-a11y warning: accessibility is a
      # build-time gate, not a later pass (KB-051).
      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Unit tests
        run: npm run test -- --run

      - name: Build
        run: npm run build

  # ------------------------------------------------- frontend e2e (M2-C01) ---
  # NON-BLOCKING on purpose (continue-on-error). Today the only spec asserts a
  # placeholder heading; a Playwright flake failing CI would buy nothing and
  # teach the team to ignore red. This becomes blocking when the first real
  # screen lands (M2-D01) -- remove continue-on-error there, and say so in
  # KB-087.
  frontend-e2e:
    name: Frontend - Playwright smoke (non-blocking)
    runs-on: windows-latest
    needs: frontend
    timeout-minutes: 20
    continue-on-error: true
    defaults:
      run:
        working-directory: frontend/nexgen-web

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Node (pinned by .nvmrc)
        uses: actions/setup-node@v4
        with:
          node-version-file: frontend/nexgen-web/.nvmrc
          cache: npm
          cache-dependency-path: frontend/nexgen-web/package-lock.json

      - name: Install (npm ci)
        run: npm ci

      # Browser binaries are not in the npm cache; --with-deps is a no-op on
      # Windows but keeps the step portable if the runner ever moves to Linux.
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      # playwright.config.ts starts the Vite dev server itself (webServer).
      - name: E2E smoke
        run: npm run e2e

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ github.run_id }}
          path: frontend/nexgen-web/playwright-report
          retention-days: 14
          if-no-files-found: ignore
